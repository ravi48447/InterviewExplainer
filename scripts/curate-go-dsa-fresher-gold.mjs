import { curateModule } from "./.work-curate-go-shells-lib.mjs";

const goCode = (...lines) => ["~~~go", ...lines, "~~~"].join("\n");

curateModule("dsa-go-fresher", {
  "big-o-with-go": {
    label: "Big-O reasoning for Go code",
    alt: "measuring only one small run",
    questions: [
      "How do you calculate time and space complexity for Go code?",
      "When does preallocating a Go slice improve an algorithm?",
      "Why is append only amortized O(1)?",
      "How do slice search and map lookup compare in Go?",
      "How would you investigate a Go function that slows as input grows?",
    ],
    definition: "Big-O describes how work or extra memory grows as input size grows. For Go code, count the dominant loops, recursive calls, allocations, and collection operations rather than individual syntax tokens.",
    mechanism: "A loop over `n` items is O(n); nested full loops are usually O(n²). `len` is O(1), map lookup is expected O(1), and appending to a slice is amortized O(1) because occasional growth copies existing elements.",
    use: "Preallocate a slice when the result size or a safe upper bound is known. Giving `make` a capacity avoids some backing-array growth while keeping length zero for later appends.",
    example: "building exactly one thousand results with `make([]T, 0, 1000)` still performs one thousand appends, but normally avoids repeated capacity growth and copying.",
    mistake: "Calling every `append` O(1) hides the occasional O(n) copy. Another common error is multiplying sequential loops instead of adding their costs, or ignoring a helper called inside a loop.",
    contrast: "Scanning a slice for a value is O(n) and keeps data compact. A map uses extra memory to provide expected O(1) lookup by key; it is useful when searches repeat or input grows.",
    boundary: "Big-O does not predict exact latency, cache behaviour, garbage collection, or map worst cases. Use it to compare growth, then benchmark realistic data when constant costs matter.",
    repair: "Name the input variable, expand the important operations, keep the fastest-growing term, and include auxiliary allocations. Confirm suspicious code with benchmarks and profiles across several input sizes.",
    scenario: "If doubling input roughly quadruples time, look for nested work, repeated copying, or a linear helper inside a loop. If allocations climb, inspect slice growth, string building, and retained backing arrays.",
    quick: ["Define the input size", "Keep the dominant growth term", "Sequential costs add; nested costs often multiply", "Map operations are expected O(1)", "Append is amortized O(1)", "Measure constants with benchmarks"],
    deepByMode: {
      fundamentals: "Complexity analysis follows the path executed for an input, not the number of source lines. A loop body that performs a map lookup still has expected linear total work; a loop body that scans another full slice produces quadratic work. Space analysis counts temporary collections and call-stack depth in addition to the returned result.",
      usage: "Preallocation changes allocation frequency rather than the number of logical elements processed. `make([]T, 0, n)` is right for append-based construction; `make([]T, n)` already creates n elements and should be filled by index. Overestimating capacity can retain more memory than the program needs, so use a defensible bound.",
      mistake: "A slice append that fits writes into the existing array. Once capacity is exhausted, Go allocates a larger array and copies elements, making that operation linear. Across many appends the copying cost is spread out, which is why the average per append is described as amortized constant time rather than guaranteed constant time.",
      comparison: "A one-time scan over a small slice may be faster and simpler than building a map. Repeated membership checks change the calculation: m searches through n values can approach O(mn), while building a map once costs O(n) expected time and then serves expected constant-time lookups. Memory is the price of the index.",
      scenario: "A useful benchmark varies input size instead of repeating only one size. CPU profiles identify where time is spent; allocation profiles show growth and retention. Complexity predicts the shape to look for, while measurement reveals constants, compiler effects, and library behaviour in the real workload.",
    },
    code: goCode(
      "func unique(nums []int) []int {",
      "    seen := make(map[int]struct{}, len(nums))",
      "    out := make([]int, 0, len(nums))",
      "    for _, n := range nums {",
      "        if _, ok := seen[n]; ok { continue }",
      "        seen[n] = struct{}{}",
      "        out = append(out, n)",
      "    }",
      "    return out // expected O(n) time, O(n) extra space",
      "}",
    ),
    table: "| Go operation | Typical complexity | Important detail |\n|---|---:|---|\n| `len(slice)` | O(1) | Stored in the slice header |\n| Slice index | O(1) | Bounds checked |\n| Search unsorted slice | O(n) | May stop early |\n| Map lookup/insert | Expected O(1) | Uses extra memory |\n| `append` | Amortized O(1) | Growth step can copy O(n) items |\n| Sort n values | O(n log n) | Comparator cost also matters |",
  },

  "slice-as-stack-and-queue": {
    label: "using slices as stacks and queues",
    alt: "a linked or circular queue",
    questions: [
      "How do you implement a stack and a queue with Go slices?",
      "When is a slice the right stack or queue representation?",
      "Why can a slice-based queue retain too much memory?",
      "How do a slice queue and a circular buffer compare?",
      "How would you make slice stack and queue operations safe?",
    ],
    definition: "A stack removes the most recently added item, while a queue removes the earliest. A Go slice supports stack push and pop at the end; a simple queue appends at the end and advances from the front.",
    mechanism: "Push uses `append`. Stack pop reads `s[len(s)-1]` and shortens to `s[:len(s)-1]`. Queue dequeue reads `s[0]` and shortens to `s[1:]`, after checking for emptiness.",
    use: "A slice is the simplest choice for an in-memory stack and for a small or short-lived queue. It is idiomatic, cache-friendly, and needs no node allocation.",
    example: "pushing `10`, then `20` gives stack `[10 20]`; popping returns `20`. Enqueuing the same values and dequeuing returns `10` because a queue is first-in, first-out.",
    mistake: "Repeatedly taking `queue = queue[1:]` can keep the old backing array alive. Removed elements may also keep referenced objects reachable unless their slots are cleared.",
    contrast: "A head-index or circular buffer reuses storage without continually moving the visible start. A linked queue avoids one large backing array but adds an allocation and pointer per node.",
    boundary: "Every pop or dequeue must define empty behaviour. These basic containers are not safe for concurrent access without synchronization, and copying a slice value still shares its backing storage.",
    repair: "Check length, clear removed reference values, periodically compact a long-lived queue or use a ring, and hide the slice behind methods that preserve its invariants.",
    scenario: "For memory growth, compare logical length with capacity and heap retention. For wrong ordering, trace the slice, head index, and returned value after every operation.",
    quick: ["Stack is LIFO", "Queue is FIFO", "Append and end-pop form a stack", "Front reslicing may retain storage", "Check empty state before indexing", "Add synchronization only when sharing"],
    deepByMode: {
      fundamentals: "The slice header tracks a pointer, length, and capacity. Stack operations at the end align naturally with this representation and are amortized O(1). A front reslice is also O(1) because it changes the header, but that does not mean the unused prefix has been released to the allocator.",
      usage: "For parsing, DFS, undo history, and other local stacks, a slice is normally ideal. A short request-local queue is also fine. A service queue that lives for hours, holds large objects, or has a fixed capacity deserves a head index, compaction rule, or circular buffer so memory behaviour stays predictable.",
      mistake: "A tiny remaining subslice can keep an entire large backing array reachable. Clearing the removed slot breaks references to its value, but the array itself remains until no slice points to it. Compaction copies live elements into a fresh or reset region when the consumed prefix becomes large.",
      comparison: "A circular buffer stores head, tail, and size and wraps indexes at capacity. It provides bounded storage and stable O(1) enqueue/dequeue, but full-versus-empty state needs careful handling. A growing slice is less code and adapts its capacity, which is often the right trade for interview basics.",
      scenario: "A small container API can return `(T, bool)` for empty operations and zero the removed slot for reference-containing types. Tests should cover empty, one item, wrap or compaction boundaries, alternating operations, and preservation of FIFO or LIFO order. A mutex belongs around the whole invariant if multiple goroutines share it.",
    },
    code: goCode(
      "func pop[T any](stack []T) ([]T, T, bool) {",
      "    if len(stack) == 0 { var zero T; return stack, zero, false }",
      "    i := len(stack) - 1",
      "    value := stack[i]",
      "    var zero T",
      "    stack[i] = zero",
      "    return stack[:i], value, true",
      "}",
    ),
    table: "| Operation | Slice expression | Typical cost |\n|---|---|---:|\n| Stack push | `s = append(s, x)` | Amortized O(1) |\n| Stack pop | `x; s = s[:len(s)-1]` | O(1) |\n| Queue enqueue | `q = append(q, x)` | Amortized O(1) |\n| Simple dequeue | `x; q = q[1:]` | O(1), may retain array |",
  },

  "linked-list-go": {
    label: "a singly linked list in Go",
    alt: "a slice-backed sequence",
    questions: [
      "How do you implement and traverse a singly linked list in Go?",
      "When is a linked list useful compared with a slice?",
      "What mistakes commonly break linked-list operations?",
      "How do linked lists and slices compare in Go?",
      "How do you reverse and verify a singly linked list?",
    ],
    definition: "A singly linked list is a chain of nodes where each node stores a value and a pointer to the next node. The head pointer identifies the first node, and `nil` marks the end.",
    mechanism: "Traversal starts at the head and follows `Next` one node at a time. Insertion or deletion rewires pointers; finding a position still requires walking from the head unless its node is already known.",
    use: "A linked list is useful when code already holds node positions and performs frequent local insertions or removals. It can also model chains whose nodes need stable addresses.",
    example: "for `10 -> 20 -> 30`, inserting `15` after the first node sets the new node's `Next` to `20`, then changes the first node's `Next` to the new node.",
    mistake: "Changing pointers in the wrong order can lose the rest of the chain. Failing to update the head breaks operations at the first node, and a cycle can make traversal never reach `nil`.",
    contrast: "Slices provide O(1) indexing, compact storage, and good cache locality. Linked lists use a pointer per node and O(n) indexing but can change links in O(1) time when the relevant node is known.",
    boundary: "Go's standard library rarely needs a custom singly linked list for ordinary application collections. Choose it for a matching access pattern, not because insertion is described as O(1) without including the search cost.",
    repair: "Save the next pointer before rewriting links, handle head and empty cases explicitly, and test the chain's values, tail, length, and absence of unintended cycles after every operation.",
    scenario: "To reverse a list, carry `previous`, `current`, and saved `next` pointers. Afterward, traverse the result and use a slow/fast pointer check if a cycle is possible.",
    quick: ["Head points to the first node", "Nil ends the chain", "Traversal and indexing are O(n)", "Known-position link changes are O(1)", "Save `next` before rewiring", "Slices are often the practical default"],
    deepByMode: {
      fundamentals: "The list invariant is that every reachable node has exactly one `Next` link and repeated traversal eventually reaches `nil` for an acyclic list. An empty list has a nil head. Methods that may replace the head must return the new head or operate through a list owner or pointer to it.",
      usage: "The advertised O(1) insertion applies only after the location is known. If an operation first searches by index or value, the full cost is O(n). Slices usually outperform lists for iteration because values are contiguous; lists matter when link identity and local rewiring are central to the problem.",
      mistake: "For deletion after a node, point around the removed node. For insertion, connect the new node to the old successor before replacing the predecessor's link. Drawing three boxes and arrows before coding makes aliasing visible and prevents the only reference to the remainder from being overwritten.",
      comparison: "A slice resize or middle insertion may move many elements, while linked nodes stay in place. That flexibility costs pointer storage, allocator work, and cache misses. Go slices also carry length and capacity automatically; a custom list must maintain size and tail fields consistently if it exposes them.",
      scenario: "During reversal, `next` preserves the unexplored suffix, `previous` is the already reversed prefix, and `current` is the node being moved. Each step flips one arrow. The old head becomes the tail, and the final `previous` value becomes the new head when `current` reaches nil.",
    },
    code: goCode(
      "type Node[T any] struct {",
      "    Value T",
      "    Next  *Node[T]",
      "}",
      "",
      "func reverse[T any](head *Node[T]) *Node[T] {",
      "    var previous *Node[T]",
      "    for current := head; current != nil; {",
      "        next := current.Next",
      "        current.Next = previous",
      "        previous, current = current, next",
      "    }",
      "    return previous",
      "}",
    ),
    flow: "```mermaid\nflowchart LR\n  P[previous] <-- C[current]\n  C -. saved .-> N[next]\n  N --> R[remaining nodes]\n```",
  },

  "sorting-in-go": {
    label: "sorting collections in Go",
    alt: "writing a sorting algorithm by hand",
    questions: [
      "How do you sort built-in and custom values in Go?",
      "When should a Go sort preserve equal-element order?",
      "Why can an incorrect comparison function break sorting?",
      "How do slices.Sort, slices.SortFunc, and a manual sort differ?",
      "How would you sort records by more than one field?",
    ],
    definition: "Go's `slices` package sorts ordered values with `slices.Sort` and custom values with `slices.SortFunc`. Older APIs in `sort` remain common, including `sort.Ints`, `sort.Strings`, and `sort.Slice`.",
    mechanism: "A comparator defines the ordering between two values. For `slices.SortFunc`, a negative result means the first value comes before the second, zero means equal in ordering, and positive means after.",
    use: "Choose a stable sort when records considered equal by the selected key must keep their original relative order, such as preserving input order among employees with the same department.",
    example: "sorting people by age and then name compares ages first; only when ages match does it compare names. This produces deterministic output for equal ages.",
    mistake: "A comparator that changes its mind, treats both `a<b` and `b<a` as true, or ignores required tie-breakers can produce incorrect or nondeterministic-looking order.",
    contrast: "Standard sorting helpers are tested, concise, and normally O(n log n). A manual algorithm is useful when the interview explicitly asks for it or the data has a special structure such as a tiny bounded integer range.",
    boundary: "Most sort helpers rearrange the slice in place. Copy first when callers need the original order, and remember that comparator cost is paid many times.",
    repair: "Write the ordering rule as key comparisons, handle equality consistently, add a tie-breaker when deterministic order matters, and test already sorted, reverse, duplicate, and empty inputs.",
    scenario: "For multi-field records, compare the primary field, return only when it differs, and continue to the next field. Verify both ascending and descending requirements and whether stability is part of the contract.",
    quick: ["Standard helpers sort in place", "Comparator results must be consistent", "Use tie-breakers for deterministic order", "Stable sort preserves equal-item order", "Copy before sorting when original order matters"],
    deepByMode: {
      fundamentals: "Sorting separates the collection from the ordering rule. Ordered primitive values already have a natural order; structs need a function that compares their chosen keys. The overall algorithm is handled by the library, while correctness still depends on that comparison defining a coherent order.",
      usage: "Stability matters only for elements equal under the active comparison. It is especially useful when applying sorts from secondary key to primary key, or when the original order has business meaning. An unstable sort can be faster or use different resources and is sufficient when equal items are interchangeable.",
      mistake: "A valid comparison is antisymmetric and transitive in practical terms: if A belongs before B and B before C, A must belong before C. Subtraction-based integer comparators can overflow for extreme values; direct comparisons avoid that problem. Tests should include equal keys and extremes.",
      comparison: "`slices.Sort` is the shortest route for ordered types. `slices.SortFunc` supports structs and alternate orders with compile-time types. `sort.Slice` uses a closure over a slice of any type and remains widely seen. Hand-written sorting adds much more surface for off-by-one and partition errors.",
      scenario: "Multi-key sorting is a sequence of decisions, not one large expression. Compare department, then descending score, then name, returning zero only when all keys match. A final stable or unique tie-breaker makes output repeatable across runs and simplifies tests.",
    },
    code: goCode(
      "slices.SortFunc(people, func(a, b Person) int {",
      "    if a.Age != b.Age { return cmp.Compare(a.Age, b.Age) }",
      "    return cmp.Compare(a.Name, b.Name)",
      "})",
    ),
    table: "| API | Data | Ordering | Mutates slice |\n|---|---|---|---|\n| `slices.Sort` | Ordered values | Natural order | Yes |\n| `slices.SortFunc` | Any element type | Typed comparator | Yes |\n| `sort.Slice` | Any slice | Index-based less function | Yes |\n| Stable variants | Custom records | Preserve equal order | Yes |",
  },

  "binary-search-go": {
    label: "binary search in Go",
    alt: "a linear scan",
    questions: [
      "How does binary search work on a sorted Go slice?",
      "When should you use sort.Search instead of a manual loop?",
      "What off-by-one mistakes break binary search?",
      "How do binary and linear search compare?",
      "How do you find the first duplicate or insertion position with binary search?",
    ],
    definition: "Binary search finds a target in sorted data by comparing the middle element and discarding the half that cannot contain the answer. Its search interval shrinks by roughly half each step.",
    mechanism: "Keep a half-open interval `[low, high)`. If the middle value is below the target, move `low` to `mid+1`; otherwise move `high` to `mid`. The final position is the first value not less than the target.",
    use: "Use `sort.Search` when the condition can be written as false for an initial range and true afterward. It encodes the boundary search and avoids repeating index arithmetic.",
    example: "in `[2,4,4,9]`, the predicate `values[i] >= 4` first becomes true at index one. The same index is the insertion point that keeps the slice sorted.",
    mistake: "Mixing inclusive and half-open bounds can skip an element or loop forever. Searching unsorted data also invalidates the decision that lets one half be discarded.",
    contrast: "Linear search is O(n), needs no ordering, and can stop early. Binary search is O(log n) after data is sorted, but sorting solely for one lookup usually costs more than one scan.",
    boundary: "`sort.Search(n, predicate)` can return `n` when no position satisfies the predicate. Always check the index before reading the slice, and define how duplicates should be handled.",
    repair: "Choose one interval convention, prove what it contains, update a bound past `mid`, and test empty, one-item, absent, end, and duplicate cases.",
    scenario: "For the first duplicate, search for the lower bound rather than returning on any equality. Trace `low`, `mid`, and `high` until the interval is empty, then validate the candidate.",
    quick: ["Input or predicate must be monotonic", "Half-open `[low, high)` avoids mixed bounds", "Each step discards half", "Search may return `len(slice)`", "Lower bound finds the first matching position"],
    deepByMode: {
      fundamentals: "The key invariant is that the answer, if it exists, remains inside the current interval. Sorted order tells which side cannot contain it after one comparison. Halving an interval of n elements takes logarithmically many steps, while the algorithm uses constant extra space in its iterative form.",
      usage: "`sort.Search` is broader than equality lookup. It finds the first index where a monotonic predicate becomes true, which covers thresholds, insertion points, and lower bounds. A manual loop is reasonable when teaching the invariant or returning a specialised result, but the library form reduces boundary bugs.",
      mistake: "If `high` is inclusive in one line and exclusive in another, a one-element interval behaves incorrectly. Another classic error is assigning `low=mid` when mid already failed; the interval may stop shrinking. Half-open bounds with `mid := low+(high-low)/2` give consistent updates.",
      comparison: "Binary search pays off when sorted order already exists or many lookups share the sorting cost. For an unsorted slice and one query, a linear pass is O(n), while sorting then searching is O(n log n). Data updates also matter because maintaining sorted order has a cost.",
      scenario: "A lower-bound search does not stop at equality. It moves the upper bound left whenever the middle value is at least the target, preserving the possibility of an earlier equal value. After convergence, checking both range and equality distinguishes a found target from a valid insertion position.",
    },
    code: goCode(
      "func lowerBound(values []int, target int) (int, bool) {",
      "    i := sort.Search(len(values), func(i int) bool {",
      "        return values[i] >= target",
      "    })",
      "    return i, i < len(values) && values[i] == target",
      "}",
    ),
    flow: "```mermaid\nflowchart TD\n  A[Sorted interval low..high] --> B[Check middle]\n  B --> C{middle below target?}\n  C -- yes --> D[Move low right of middle]\n  C -- no --> E[Move high to middle]\n  D --> A\n  E --> A\n```",
  },

  "two-pointer-pattern": {
    label: "the two-pointer pattern",
    alt: "nested pair checking",
    questions: [
      "How does the two-pointer pattern work in Go?",
      "When can two pointers replace a nested loop?",
      "Why does pointer movement need a proof?",
      "How do two pointers and a sliding window differ?",
      "How would you trace a sorted pair-sum solution?",
    ],
    definition: "The two-pointer pattern tracks two positions whose movement removes impossible candidates. Common forms move inward from both ends or move at different speeds through one sequence.",
    mechanism: "For pair sum in ascending data, add the end values. A small sum requires moving the left pointer right; a large sum requires moving the right pointer left.",
    use: "Two pointers can replace a nested pair loop when ordering or another invariant proves that one movement discards a whole group of candidates safely.",
    example: "with `[1,3,4,7,10]` and target `11`, `1+10` succeeds immediately. For target `10`, `1+10` is too large, so moving the right pointer removes `10` from consideration.",
    mistake: "Moving a pointer by intuition rather than an invariant can skip valid answers. Applying the sorted pair-sum rule to unsorted input is a common example.",
    contrast: "Two pointers often use a fixed pair of positions. A sliding window represents a contiguous range and expands or shrinks it while maintaining a window property.",
    boundary: "The pattern is not automatically O(n) unless each pointer moves only one direction a bounded number of times. Sorting first also adds O(n log n) time and may lose original indexes.",
    repair: "State why the discarded values cannot participate, ensure each step makes progress, and test duplicates, no match, pointer meeting, and the smallest inputs.",
    scenario: "Trace pointer indexes, values, the current sum, the comparison with target, and the chosen move. A wrong movement becomes obvious in one row of the trace.",
    quick: ["Pointers summarize the remaining search", "Movement must discard only impossible candidates", "Sorted pair sum moves inward", "Each pointer should make bounded progress", "Sliding windows maintain a contiguous range"],
    deepByMode: {
      fundamentals: "The pattern gains speed by exploiting structure. Instead of trying every pair, one comparison rules out many pairs at once. In a sorted pair-sum search, if the smallest remaining value plus the largest is too small, pairing that same smallest value with anything else cannot reach the target.",
      usage: "Useful signals include sorted data, symmetric comparison, in-place compaction, and slow/fast traversal. Without such a monotonic relationship, a map or nested search may be more appropriate. Sorting can create the relationship, but its cost and effect on indexes must be included.",
      mistake: "Every pointer update needs a sentence explaining what has been eliminated. If no such sentence is true, the algorithm may skip candidates. Progress is the second proof: at least one pointer must move toward termination on every non-returning iteration.",
      comparison: "A sliding window also uses two indexes, but the indexes describe a whole contiguous segment whose sum, count, or validity is updated incrementally. Opposite-end two pointers usually compare a pair. Recognising which state matters prevents forcing one template onto a different problem.",
      scenario: "A dry-run table is more useful than a final output because it records the invariant in motion. Include left and right indexes, their values, the derived sum, and the reason for the move. At exit, explain whether a match was found or the candidate interval became empty.",
    },
    code: goCode(
      "func pairSum(sorted []int, target int) (int, int, bool) {",
      "    left, right := 0, len(sorted)-1",
      "    for left < right {",
      "        sum := sorted[left] + sorted[right]",
      "        if sum == target { return left, right, true }",
      "        if sum < target { left++ } else { right-- }",
      "    }",
      "    return 0, 0, false",
      "}",
    ),
    table: "| State | Meaning | Safe move |\n|---|---|---|\n| Sum below target | Left value is too small with largest remaining | `left++` |\n| Sum above target | Right value is too large with smallest remaining | `right--` |\n| Sum equals target | Pair found | Return |",
  },

  "hash-map-patterns": {
    label: "hash-map patterns in Go",
    alt: "repeated slice scans",
    questions: [
      "Which interview patterns are commonly solved with Go maps?",
      "When should you use a map as a set or frequency table?",
      "What Go map mistakes cause incorrect algorithm results?",
      "How do maps and direct slice indexing compare?",
      "How would you trace and test a map-based algorithm?",
    ],
    definition: "Go maps support key-to-value lookup and are commonly used for sets, frequency counts, grouping, memoization, and complement searches. Keys must be comparable.",
    mechanism: "A set uses `map[T]struct{}`, a counter uses `map[T]int`, and the comma-ok form distinguishes an absent key from a present key holding its zero value.",
    use: "Use a set for membership and a frequency table when duplicates matter. A map is especially valuable when the key range is large or unknown and direct array indexing is not practical.",
    example: "to find the first repeated number, check `seen[n]` before adding `n`. For word counts, increment `counts[word]`; a missing key naturally starts from integer zero.",
    mistake: "Writing to a nil map panics, while reading it returns zero values. Depending on iteration order or forgetting comma-ok when zero is meaningful also causes subtle errors.",
    contrast: "A slice or array indexed by a small dense integer key is compact and predictable. A map handles sparse or non-integer keys with expected O(1) operations but adds hashing and allocation overhead.",
    boundary: "Map iteration order is unspecified, and maps are not safe for unsynchronized concurrent read/write access. Expected constant-time operations do not remove the need to analyse memory growth.",
    repair: "Initialize maps before writes, choose the value type to match the pattern, use comma-ok for presence, sort extracted keys when output order matters, and synchronize shared mutation.",
    scenario: "Trace the map before and after each input. Tests should cover absent keys, present zero values, duplicates, nil initialization, deterministic output needs, and large distinct-key counts.",
    quick: ["Set: `map[T]struct{}`", "Counter: `map[T]int`", "Comma-ok tests presence", "Writes need a non-nil map", "Iteration order is unspecified", "Concurrent writes need coordination"],
    deepByMode: {
      fundamentals: "A map turns information from earlier input into direct later lookup. That is why the same structure solves apparently different problems: a set remembers existence, a counter remembers multiplicity, and a complement map remembers where a useful partner appeared. The value type is the state the algorithm needs.",
      usage: "Use `struct{}` when presence is the only fact, `int` for counts or indexes, and a slice value for grouping several items per key. A fixed array can replace a map for a guaranteed small dense alphabet, but using it without that guarantee changes the accepted input.",
      mistake: "The zero-value read is convenient for `counts[key]++`, yet it can hide absence when zero is a valid stored value. The comma-ok result carries that missing bit. Nil maps are similarly asymmetric: reads are allowed, but the first assignment panics, so construction ownership must be clear.",
      comparison: "Array indexing gives worst-case constant access with no hashing when a key maps safely into a bounded dense range. Maps support strings, structs of comparable fields, and sparse integers without reserving every possible slot. Their flexibility costs metadata, buckets, and less predictable locality.",
      scenario: "A map trace should state its invariant, such as 'contains values from indexes before i.' Recording state before and after insertion exposes self-matches and wrong update order. If results vary across runs, inspect any output built directly from map iteration and sort keys when order is part of the result.",
    },
    code: goCode(
      "func frequencies(words []string) map[string]int {",
      "    counts := make(map[string]int, len(words))",
      "    for _, word := range words { counts[word]++ }",
      "    return counts",
      "}",
      "",
      "if count, ok := counts[\"go\"]; ok {",
      "    fmt.Println(count)",
      "}",
    ),
    table: "| Pattern | Map type | Stored fact |\n|---|---|---|\n| Set | `map[T]struct{}` | Seen or not |\n| Frequency | `map[T]int` | Number of occurrences |\n| Index lookup | `map[T]int` | Earlier position |\n| Grouping | `map[K][]V` | Values sharing a key |\n| Memoization | `map[State]Result` | Solved subproblem |",
  },

  comparisons: {
    label: "core data-structure trade-offs in Go",
    alt: "choosing by habit",
    questions: [
      "How do arrays and slices differ in Go algorithms?",
      "How do stack and queue access patterns differ?",
      "When is a map better than scanning a slice?",
      "When should you use linear search instead of binary search?",
      "What is the difference between stable and unstable sorting?",
    ],
    definition: "Data-structure choices define which operations are cheap and which guarantees the algorithm can use. Go slices, arrays, maps, linked nodes, stacks, and queues are not interchangeable labels.",
    mechanism: "List the operations the problem repeats—index, membership, append, front removal, ordered search, or key lookup—and select a representation that makes those operations correct and clear.",
    use: "Choose from the actual access pattern and constraints. A small linear scan can be better than constructing an index, while repeated lookups over growing data often justify a map or sorted representation.",
    example: "a parser uses a slice stack for nested delimiters, a scheduler uses FIFO queue order, and a frequency counter uses a map because each value needs a count.",
    mistake: "Choosing only from a headline complexity can ignore sorting cost, allocation, retained memory, required ordering, or the fact that finding a linked-list position is itself linear.",
    contrast: "The simpler structure is often best until repeated operations dominate. Changing representation is an algorithm decision because it changes both guarantees and cost.",
    boundary: "Complexity is only one requirement. Mutation, concurrency, stable order, Unicode, memory layout, and the need to preserve original indexes can decide the correct structure.",
    repair: "Write an operation table for the problem, include setup costs, and test the behaviour that the chosen structure promises.",
    scenario: "If performance or correctness disappoints, compare the real operation sequence with the assumption used when the structure was chosen.",
    quick: ["Arrays have fixed length in their type", "Slices are views over backing arrays", "Stacks are LIFO; queues are FIFO", "Maps accelerate repeated key lookup", "Binary search needs monotonic order", "Stable sort preserves equal-item order"],
    deepByMode: {
      fundamentals: "An array value has a fixed length that is part of its type and copying it copies its elements. A slice is a small descriptor over a backing array and can change length within capacity. Passing or copying a slice header can still share elements, which matters for mutation and append behaviour.",
      usage: "A stack exposes the newest item and naturally supports depth-first work, undo, and nested parsing. A queue exposes the oldest item and supports breadth-first work or arrival order. Both can use slices, but their removal ends and memory behaviour differ.",
      mistake: "For one lookup, scanning a compact slice avoids map construction. For many lookups, the scan repeats linear work and a map can amortize its build cost. The break-even point depends on input size, key type, memory, and how many queries the program actually performs.",
      comparison: "Linear search accepts unsorted data and is often simplest for one small scan. Binary search reduces lookup to O(log n) only when data or a predicate is monotonic. If sorting is performed solely for one query, its O(n log n) setup usually loses to O(n) scanning.",
      scenario: "A stable sort keeps the original relative order of items that compare equal; an unstable sort may rearrange them. Stability is observable when equal-key records carry other fields. It is required when original order or an earlier sort acts as a secondary key, not for interchangeable primitive values.",
    },
    codeByMode: {
      fundamentals: goCode("a := [3]int{1, 2, 3}", "s := a[:2] // slice shares a's backing array", "s[0] = 9"),
      usage: goCode("stack = append(stack, item)", "item = queue[0]", "queue = queue[1:]"),
      mistake: goCode("index := make(map[string]int, len(names))", "for i, name := range names { index[name] = i }"),
      comparison: goCode("i := sort.SearchInts(sorted, target)", "found := i < len(sorted) && sorted[i] == target"),
      scenario: goCode("slices.SortStableFunc(records, func(a, b Record) int {", "    return cmp.Compare(a.Group, b.Group)", "})"),
    },
    table: "| Need | Natural structure/algorithm | Main condition |\n|---|---|---|\n| Last item first | Slice stack | End operations |\n| First item first | Queue/ring | Front lifecycle |\n| Repeated key lookup | Map | Extra memory allowed |\n| Search ordered values | Binary search | Order already available |\n| Preserve equal order | Stable sort | Equality order matters |",
  },
});

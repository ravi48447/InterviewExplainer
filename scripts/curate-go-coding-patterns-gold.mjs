import { curateModule } from "./.work-curate-go-shells-lib.mjs";

const goCode = (...lines) => ["~~~go", ...lines, "~~~"].join("\n");

curateModule("coding-patterns-go", {
  "fizzbuzz-go": {
    label: "FizzBuzz in Go",
    alt: "a table-driven rule loop",
    questions: [
      "How does a clean FizzBuzz solution work in Go?",
      "When is a switch-based FizzBuzz solution a good choice?",
      "Why does checking 3 before 15 break FizzBuzz?",
      "How do switch and string-building FizzBuzz solutions differ?",
      "How would you test and debug a configurable FizzBuzz?",
    ],
    definition: "FizzBuzz loops from one to a limit and replaces multiples of three with `Fizz`, multiples of five with `Buzz`, and multiples of both with `FizzBuzz`. Other numbers are written normally.",
    mechanism: "The decision for a number uses the remainder operator. A `switch` should test `n%15 == 0` before the separate three and five cases because the combined case is more specific.",
    use: "A switch-based version is a good fit when the rules are fixed and few. It keeps the four outcomes together and makes their priority easy to review.",
    example: "for `15`, both divisibility rules match, so the result must be `FizzBuzz`; for `9` only the three rule matches, so the result is `Fizz`.",
    mistake: "If `n%3 == 0` is checked first in an `if` chain, `15` is accepted by that branch and the program never reaches the combined rule.",
    contrast: "A switch expresses a small fixed decision clearly. Building a string by appending `Fizz` and `Buzz` avoids a special multiple-of-fifteen branch and adapts more easily when independent rules are added.",
    boundary: "The function should define what happens for a limit below one and whether it returns values or prints them. Returning a slice is normally easier to test than writing directly to standard output.",
    repair: "Put the most specific condition first, or build the word from every matching rule. Then test boundary values around each divisor, especially `3`, `5`, `15`, and `16`.",
    scenario: "For a wrong line, trace that number through the conditions in order. With configurable rules, also check zero divisors, duplicate divisors, and the order in which matching words are joined.",
    quick: ["Remainder zero means divisible", "The combined case has priority in an if-chain", "A switch fits a small fixed rule set", "Returning results makes tests simple", "Test the overlap at fifteen"],
    deepByMode: {
      fundamentals: "Each loop iteration makes one decision and produces one output, so the algorithm uses linear time and only constant working space apart from returned strings. Keeping formatting separate from output lets the same function serve a command, a web handler, or a unit test. In Go, a conditionless `switch` reads naturally because each case is a Boolean expression.",
      usage: "A fixed four-way switch is easy to read during an interview because the priority is visible from top to bottom. A rule collection becomes more useful only when divisors or labels come from configuration. That version needs validation and a clear joining order, which is extra complexity the basic problem does not require.",
      mistake: "An `if`/`else if` chain stops at its first true branch. Since every multiple of fifteen is also a multiple of three and five, a broad branch can hide the combined one. The issue is decision order, not arithmetic. Truth-table tests make this visible without inspecting printed output by eye.",
      comparison: "The switch version lists complete outcomes: combined, Fizz, Buzz, or number. The string-building version evaluates independent rules and falls back to the number only when the result is empty. Both are correct for two divisors; the second scales better, while the first usually explains the original exercise more directly.",
      scenario: "A configurable implementation changes the problem from four hard-coded outcomes to rule evaluation. Validate each divisor before using `%`, preserve a documented rule order, and decide how duplicate matches behave. Table-driven tests should cover no match, one match, several matches, a small or empty range, and invalid configuration.",
    },
    code: goCode(
      "func fizzBuzz(limit int) []string {",
      "    out := make([]string, 0, max(limit, 0))",
      "    for n := 1; n <= limit; n++ {",
      "        switch {",
      "        case n%15 == 0:",
      "            out = append(out, \"FizzBuzz\")",
      "        case n%3 == 0:",
      "            out = append(out, \"Fizz\")",
      "        case n%5 == 0:",
      "            out = append(out, \"Buzz\")",
      "        default:",
      "            out = append(out, strconv.Itoa(n))",
      "        }",
      "    }",
      "    return out",
      "}",
    ),
    table: "| Number | `%3` | `%5` | Output |\n|---:|---:|---:|---|\n| 9 | 0 | 4 | `Fizz` |\n| 10 | 1 | 0 | `Buzz` |\n| 15 | 0 | 0 | `FizzBuzz` |\n| 16 | 1 | 1 | `16` |",
  },

  "fibonacci-iterative-recursive": {
    label: "Fibonacci in Go",
    alt: "the recursive definition",
    questions: [
      "How do iterative and recursive Fibonacci solutions work in Go?",
      "When should you choose an iterative Fibonacci solution?",
      "Why is the naive recursive Fibonacci function slow?",
      "How do iteration, memoization, and recursion compare for Fibonacci?",
      "How would you handle large Fibonacci values safely in Go?",
    ],
    definition: "The Fibonacci sequence starts with zero and one, and each later value is the sum of the previous two. A function must state whether `F(0)=0` and `F(1)=1` before calculating later terms.",
    mechanism: "An iterative solution carries only the previous two values through a loop. The direct recursive definition calls `fib(n-1)` and `fib(n-2)`, so it repeats the same subproblems many times.",
    use: "Choose iteration for a single non-negative index when you want simple code, linear time, and constant extra space. It also avoids growing the call stack.",
    example: "starting with `a=0` and `b=1`, three updates produce pairs `(1,1)`, `(1,2)`, and `(2,3)`, so `F(4)` is `3` under the zero-based definition.",
    mistake: "Naive recursion builds an overlapping call tree. Computing `F(5)` asks for `F(3)` from more than one branch, and that duplication grows exponentially.",
    contrast: "Iteration is usually best for one value. Memoized recursion preserves the mathematical shape while caching each index, and fast-doubling methods are useful when the index is very large.",
    boundary: "A machine integer eventually overflows even when the algorithm is correct. Negative indexes need rejection or an explicitly defined extension, and huge exact values need `math/big.Int`.",
    repair: "Validate the index, replace repeated recursion with a loop or cache, and choose the numeric type from the largest supported input. Add base-case and overflow-boundary tests.",
    scenario: "If results turn negative or wrap, the first question is the numeric range, not the recurrence. If runtime grows sharply, count repeated calls or profile the recursive tree.",
    quick: ["State the base cases", "Iteration is O(n) time and O(1) extra space", "Naive recursion repeats work", "Memoization stores each subproblem", "Fixed-width integers can overflow"],
    deepByMode: {
      fundamentals: "The recurrence describes the relationship between values, but it does not force a recursive implementation. A loop computes the same dependency chain from the known base cases upward. Tracking the pair before every update gives a useful invariant: `a` is the current Fibonacci value and `b` is the next one.",
      usage: "For an interview function that returns one ordinary-sized term, iteration has the smallest operational cost and the clearest failure surface. Recursion can still teach the recurrence, and memoization is helpful when a larger problem reaches Fibonacci-like states in irregular order. The input and required output should drive the choice.",
      mistake: "The recursive call graph branches twice at most non-base nodes. Many branches request the same indexes, so the number of calls grows roughly like the sequence itself. A cache changes that behaviour because the first result for each index is reused; bottom-up iteration avoids the calls entirely.",
      comparison: "Iteration and memoization both reduce the common version to linear work. Iteration stores two values, while a memo table and recursive stack use memory proportional to the index. Fast doubling computes pairs such as `F(2k)` and `F(2k+1)` in logarithmic steps, but its formulas add complexity that a small input does not justify.",
      scenario: "Correctness includes the number representation. On common platforms an `int` has a finite signed range, and the sequence crosses it quickly. Exact large results can use `big.Int`; modular problems should reduce at every step. Document the accepted range so callers never mistake wrapped arithmetic for a valid term.",
    },
    code: goCode(
      "func fibonacci(n int) (uint64, error) {",
      "    if n < 0 { return 0, errors.New(\"negative index\") }",
      "    var a, b uint64 = 0, 1",
      "    for i := 0; i < n; i++ {",
      "        if math.MaxUint64-b < a { return 0, errors.New(\"overflow\") }",
      "        a, b = b, a+b",
      "    }",
      "    return a, nil",
      "}",
    ),
    table: "| Method | Time | Extra space | Main use |\n|---|---:|---:|---|\n| Naive recursion | Exponential | O(n) stack | Explain recurrence only |\n| Memoized recursion | O(n) | O(n) | Reuse recursive subproblems |\n| Iteration | O(n) | O(1) | Ordinary single result |\n| Fast doubling | O(log n) | O(log n) or O(1) | Very large index |",
  },

  "palindrome-check-go": {
    label: "palindrome checking in Go",
    alt: "reversing the whole value first",
    questions: [
      "How do you check whether a string is a palindrome in Go?",
      "When should a palindrome check use runes instead of bytes?",
      "Why can byte indexing give the wrong palindrome result?",
      "How do two-pointer and reverse-then-compare palindrome checks differ?",
      "How would you normalize text for a phrase-palindrome check?",
    ],
    definition: "A palindrome reads the same from left to right and right to left under a chosen comparison rule. A two-pointer check compares matching positions from the outside toward the centre.",
    mechanism: "Go strings contain bytes, while `[]rune(s)` exposes Unicode code points. With runes, move `left` forward and `right` backward until a pair differs or the pointers meet.",
    use: "Use runes whenever non-ASCII text is valid input. Byte indexing is enough only when the contract explicitly limits input to single-byte characters such as ASCII.",
    example: "`level` matches at `l/l` and `e/e`, then reaches its centre. The word `été` also works with runes even though its accented letters occupy more than one UTF-8 byte.",
    mistake: "Comparing `s[left]` and `s[right]` treats UTF-8 bytes as characters. A multi-byte rune can be split, so positions and comparisons no longer represent the visible text.",
    contrast: "Two pointers stop on the first mismatch and need no second reversed value after conversion. Reverse-then-compare can be easy to read but allocates another sequence and usually scans all of it.",
    boundary: "Case, spaces, punctuation, accents, and Unicode normalization are product rules. `A man, a plan...` is a palindrome only if the function deliberately ignores separators and case.",
    repair: "Write the comparison contract first, convert to runes when required, normalize only the characters the contract says to ignore, and test empty, one-rune, even, odd, and Unicode inputs.",
    scenario: "When a phrase check fails, print or inspect the normalized rune sequence and the first mismatched pair. This separates an algorithm error from a disagreement about normalization.",
    quick: ["Define what counts as equal", "Two pointers move inward", "Strings are bytes; runes represent code points", "Stop at the first mismatch", "Normalization belongs to the requirement"],
    deepByMode: {
      fundamentals: "The invariant is simple: everything outside the two pointers has already matched. Each successful comparison shrinks the unchecked region by two. That gives linear time and, after converting to runes, linear space. An ASCII-only version can work directly on bytes with constant extra space.",
      usage: "UTF-8 lets Go store text compactly, but a character may span several bytes. Rune conversion is the straightforward interview choice when code-point comparison is intended. Full user-facing text can be more complex because a displayed character may contain multiple code points; the function contract should not promise more normalization than it performs.",
      mistake: "A byte loop can appear correct for English tests and fail only after production receives other scripts. The bug is hidden by the test data. Add at least one multi-byte case and inspect `len(s)` beside `utf8.RuneCountInString(s)` to show why byte positions are not character positions.",
      comparison: "Both common methods compare symmetric content. Two pointers express that relationship directly and can exit early. Creating a reversed rune slice may be convenient when the reversed value is also needed, but for a Boolean check it performs extra writes and allocation without improving the asymptotic time.",
      scenario: "Phrase palindromes need a normalization pipeline before the pointer loop. A common rule keeps letters and digits, converts letters to one case, and then compares. That rule must be visible in tests because removing punctuation or folding case can change meaning for some languages.",
    },
    code: goCode(
      "func isPalindrome(s string) bool {",
      "    r := []rune(s)",
      "    for left, right := 0, len(r)-1; left < right; left, right = left+1, right-1 {",
      "        if r[left] != r[right] { return false }",
      "    }",
      "    return true",
      "}",
    ),
    table: "| Input contract | Representation | Example |\n|---|---|---|\n| ASCII only | Bytes | `level` |\n| Unicode code points | Runes | `été` |\n| Ignore case/punctuation | Normalized runes | `Never odd or even` |",
  },

  "anagram-check-go": {
    label: "anagram checking in Go",
    alt: "sorting both strings",
    questions: [
      "How do you check whether two strings are anagrams in Go?",
      "When is a rune-frequency map the right anagram solution?",
      "What causes false results in an anagram checker?",
      "How do frequency counting and sorting compare for anagram checks?",
      "How would you test an anagram checker with Unicode and repeated letters?",
    ],
    definition: "Two strings are anagrams when they contain the same symbols with the same frequencies under the agreed normalization rules. Order does not matter, but duplicate counts do.",
    mechanism: "Count every rune in the first string, subtract counts while scanning the second, and reject a missing or exhausted count. A remaining non-zero count means the inputs differ.",
    use: "A rune-frequency map is a strong default when input may contain Unicode and the check should run in linear time. A fixed array is faster only when the alphabet is small and guaranteed.",
    example: "`listen` and `silent` each contain one of the same six letters. By contrast, `aab` and `abb` use the same set of letters but different frequencies, so they are not anagrams.",
    mistake: "Comparing only lengths or a set of characters ignores repetition. Counting bytes instead of runes can also make the function's idea of a symbol disagree with Unicode input.",
    contrast: "Frequency counting takes expected linear time and extra space for distinct symbols. Sorting is usually O(n log n), modifies or copies rune slices, and can be concise when sorted values are needed anyway.",
    boundary: "The caller must decide whether spaces, punctuation, letter case, and canonically equivalent Unicode forms are significant. Lowercasing alone is not a complete language-independent normalization rule.",
    repair: "Normalize both inputs by the same documented rule, count actual frequencies, fail as soon as the second string needs an unavailable rune, and verify that all counts return to zero.",
    scenario: "For a surprising result, display the normalized rune sequence and frequency changes for the first differing rune. Tests should include repeated letters, empty strings, different lengths, case, spaces, and multi-byte runes.",
    quick: ["Same symbols is not enough; counts must match", "Use `map[rune]int` for code-point input", "Subtract while scanning the second value", "Sorting costs O(n log n)", "Normalization must be explicit"],
    deepByMode: {
      fundamentals: "The frequency map represents a balance sheet. Adding the first input and subtracting the second should leave every key at zero. Early rejection is safe when a rune from the second input has no positive balance. The expected running time is linear in the number of decoded runes.",
      usage: "A map handles an open alphabet without guessing its size. For guaranteed lowercase English letters, an array of 26 counters reduces allocation and hashing. That optimization depends on a real input guarantee; using it for arbitrary text silently excludes valid symbols.",
      mistake: "Length checks can reject many mismatches quickly, but byte length and rune count are different measurements. Sets are also insufficient because they erase multiplicity. The smallest counterexample is useful: `aab` and `abb` share a set and length while proving that counts matter.",
      comparison: "Counting and sorting both create a canonical way to ignore original order. A counter stores how often each symbol occurs, while sorting groups equal symbols into the same sequence. Counting normally wins on time; sorting may win on simplicity when the data is already sortable and the alphabet map would be large.",
      scenario: "Unicode tests should separate encoding from normalization. A `range` loop decodes UTF-8 into runes, but visually similar text can still use different code-point sequences. If canonical equivalence matters, normalize with an appropriate Unicode package before counting and test that policy directly.",
    },
    code: goCode(
      "func areAnagrams(a, b string) bool {",
      "    counts := make(map[rune]int)",
      "    for _, r := range a { counts[r]++ }",
      "    for _, r := range b {",
      "        if counts[r] == 0 { return false }",
      "        counts[r]--",
      "    }",
      "    for _, n := range counts { if n != 0 { return false } }",
      "    return true",
      "}",
    ),
    table: "| Approach | Time | Extra space | Best fit |\n|---|---:|---:|---|\n| Rune frequency map | Expected O(n) | O(k) | General Unicode code points |\n| Fixed counter array | O(n) | O(1) | Guaranteed small alphabet |\n| Sort rune slices | O(n log n) | Depends on sort/copies | Simple comparison of sortable data |",
  },

  "two-sum-go": {
    label: "the Two Sum problem in Go",
    alt: "checking every pair",
    questions: [
      "How do you solve Two Sum with a map in Go?",
      "When is the one-pass Two Sum algorithm the right choice?",
      "Why do duplicate values break some Two Sum solutions?",
      "How do the hash-map and brute-force Two Sum solutions compare?",
      "How would you test and debug a Two Sum implementation?",
    ],
    definition: "Two Sum asks for two different elements whose values add to a target, usually returning their indexes. A one-pass solution stores values already seen in a map.",
    mechanism: "At index `i`, compute `needed := target-nums[i]`. If `needed` is already in the map, return its earlier index and `i`; otherwise store the current value and index.",
    use: "Use the one-pass map when the input is unsorted, expected linear time matters, and O(n) extra space is acceptable. It preserves original indexes without rearranging the slice.",
    example: "for `[2, 7, 11, 15]` and target `9`, the scan stores `2` at index zero, then finds that `9-7` is already present and returns `[0,1]`.",
    mistake: "Storing the current element before checking its complement can reuse the same index when the target is twice that value. Duplicate values also require indexes, not just a Boolean set.",
    contrast: "Brute force checks each pair in O(n²) time and O(1) extra space. The map reduces expected time to O(n) by spending O(n) space and relying on hash lookups.",
    boundary: "The contract must say whether exactly one solution exists, what to return when none exists, and whether any valid pair or all pairs are required. The standard one-pass method returns one pair.",
    repair: "Check before storing, map each seen value to an index, and test duplicate complements such as `[3,3]` with target six. Return an explicit Boolean or error when absence is allowed.",
    scenario: "For a wrong pair, log each index, value, needed complement, lookup result, and map update. This reveals self-matches, overwritten indexes, and misunderstandings about the required output.",
    quick: ["Store seen value to index", "Look for `target-current`", "Check before storing the current value", "One pass is expected O(n) time", "Define the no-solution result"],
    deepByMode: {
      fundamentals: "The map contains exactly the elements to the left of the current index. That invariant guarantees a found complement uses a different, earlier position. Because each item performs a constant expected number of map operations, the scan is expected linear time.",
      usage: "The map method is designed for unsorted data where indexes matter. Sorting plus two pointers can reduce auxiliary lookup storage in some settings, but sorting changes order and needs a way to recover original indexes. For one interview-sized slice, the map is normally the clearest match.",
      mistake: "The order of two lines controls correctness. If the current value enters the map first, a lookup can find the same element. With `[3,3]`, check-then-store succeeds on the second index; with a single `[3]`, it correctly finds nothing.",
      comparison: "The nested-loop version is a useful baseline because it directly expresses every candidate pair and uses no additional collection. The map version remembers earlier work so later elements need one complement lookup rather than another full scan. The trade-off is memory and expected hash behaviour.",
      scenario: "Tests should cover a pair at the ends, negative values, zero, duplicate values, no result, and more than one valid result if the contract permits it. Trace data should show the map before the current insertion; that moment is where most implementation errors appear.",
    },
    code: goCode(
      "func twoSum(nums []int, target int) (int, int, bool) {",
      "    seen := make(map[int]int, len(nums))",
      "    for i, value := range nums {",
      "        if j, ok := seen[target-value]; ok { return j, i, true }",
      "        seen[value] = i",
      "    }",
      "    return 0, 0, false",
      "}",
    ),
    flow: "```mermaid\nflowchart LR\n  A[Read current value] --> B[Compute target - value]\n  B --> C{Complement in seen map?}\n  C -- yes --> D[Return earlier and current indexes]\n  C -- no --> E[Store current value and index]\n  E --> A\n```",
  },

  "reverse-string-go": {
    label: "reversing a string in Go",
    alt: "reversing raw bytes",
    questions: [
      "How do you reverse a UTF-8 string in Go?",
      "When is byte-wise string reversal safe?",
      "Why does a byte-reversal function corrupt some Go strings?",
      "How do rune-slice and range-based string reversal compare?",
      "How would you test a string reversal function thoroughly?",
    ],
    definition: "A Unicode-aware reversal normally reverses the sequence of runes, not the UTF-8 bytes that encode them. Convert the string to `[]rune`, swap from both ends, and convert back.",
    mechanism: "Strings are immutable byte sequences. Rune conversion decodes code points into a mutable slice, and parallel assignment swaps each outside pair until the indexes meet.",
    use: "Byte-wise reversal is safe only when the input contract guarantees one-byte characters, such as ASCII. For ordinary user text, rune-wise reversal is the clearer baseline.",
    example: "reversing `Go界` by runes produces `界oG`. Reversing its bytes moves pieces of the three-byte encoding for `界` and creates invalid UTF-8.",
    mistake: "A loop over byte indexes assumes every visible symbol occupies one byte. That assumption passes basic English tests and corrupts multi-byte UTF-8 input.",
    contrast: "A rune slice is simple and allows in-place swaps after conversion. A range-based method can prepend or fill a destination from the end, but must handle byte offsets carefully and still allocate output.",
    boundary: "Rune reversal preserves code points, not necessarily user-perceived grapheme clusters. Emoji sequences and letters with combining marks can still be split if the product needs visual-character reversal.",
    repair: "Choose the unit—bytes, runes, or grapheme clusters—from the requirement, then test ASCII, multi-byte runes, combining marks, empty input, and invalid UTF-8 policy.",
    scenario: "If output contains replacement characters or fails `utf8.ValidString`, compare the original bytes with the decoded runes. This quickly identifies a byte-order bug.",
    quick: ["Go strings store bytes", "Rune conversion decodes UTF-8 code points", "Swap outside pairs inward", "Byte reversal is for explicit byte data", "Runes are not full grapheme clusters"],
    deepByMode: {
      fundamentals: "After conversion, the swap loop maintains a clear invariant: positions outside the current pair already contain their final reversed rune. It performs half as many swaps as runes and uses linear space for the decoded slice and result string.",
      usage: "Protocol fields, hashes, and encoded binary data may intentionally use bytes. Human text usually should not. Naming the contract prevents an optimization from quietly changing behaviour when the service later receives names or messages outside ASCII.",
      mistake: "UTF-8 represents common ASCII runes with one byte and many other runes with two to four. Reversing that byte order destroys the encoding inside each multi-byte rune. `utf8.ValidString` is a useful test signal, but the real fix is reversing the correct unit.",
      comparison: "Converting to runes is concise and easy to verify. A manual decoder can reduce temporary storage patterns in specialised code, but byte offsets, invalid encodings, and output sizing make it harder to explain. The simple slice method is normally the right interview answer.",
      scenario: "A complete test suite checks that reversing twice returns the original for supported input, while also asserting exact examples. Decide how invalid UTF-8 is handled because rune conversion replaces invalid byte sequences; a byte-preserving utility has a different contract.",
    },
    code: goCode(
      "func reverseString(s string) string {",
      "    runes := []rune(s)",
      "    for left, right := 0, len(runes)-1; left < right; left, right = left+1, right-1 {",
      "        runes[left], runes[right] = runes[right], runes[left]",
      "    }",
      "    return string(runes)",
      "}",
    ),
    table: "| Unit reversed | Preserves | Typical use |\n|---|---|---|\n| Byte | Exact bytes only | ASCII or binary contract |\n| Rune | Unicode code points | Most interview text problems |\n| Grapheme cluster | Displayed characters | User-facing text editor behaviour |",
  },

  "count-words-go": {
    label: "word counting in Go",
    alt: "a manual text scanner",
    questions: [
      "How do you count word frequencies in Go?",
      "When should you use strings.Fields for word counting?",
      "Why do punctuation and case produce surprising word counts?",
      "How do strings.Fields, bufio.Scanner, and a manual parser differ?",
      "How would you count words in a large file without loading it all?",
    ],
    definition: "A word-frequency counter tokenizes text and increments a map entry for each token. The tokenization rule decides what the program considers a word.",
    mechanism: "`strings.Fields` splits an in-memory string around runs of Unicode whitespace. A loop over the returned words updates `map[string]int` with `counts[word]++`.",
    use: "Use `strings.Fields` when whitespace-separated tokens are the actual requirement and the text already fits comfortably in memory. It is small, Unicode-aware for spaces, and easy to test.",
    example: "`go Go go` produces separate keys unless the program first applies a chosen case rule. `go, go` also keeps `go,` distinct because `Fields` does not remove punctuation.",
    mistake: "Calling whitespace splitting a general word parser hides important rules. Case, punctuation, apostrophes, hyphens, and Unicode categories can all change the expected token.",
    contrast: "`strings.Fields` handles an in-memory value, `bufio.Scanner` reads tokens incrementally, and a manual or `FieldsFunc` tokenizer gives precise separator rules. Scanner also has a configurable token-size limit.",
    boundary: "There is no universal word definition. The application must state its language, normalization, punctuation, memory, and maximum-token requirements before choosing a tokenizer.",
    repair: "Write representative tokenization examples first, normalize deliberately, stream large inputs, increase Scanner's buffer when valid tokens can be large, and always check `scanner.Err()`.",
    scenario: "For missing or extra counts, inspect the exact tokens rather than only the final map. For large files, watch memory use, Scanner errors, and whether a final partial token is handled.",
    quick: ["Tokenization defines a word", "`Fields` splits on Unicode whitespace", "A map stores frequencies", "Punctuation is not removed automatically", "Stream large input and check scanner errors"],
    deepByMode: {
      fundamentals: "Counting is linear in the amount of text under expected map behaviour, but correctness belongs to the tokenizer. The frequency map is only the second stage. Keeping tokenization and counting as separate functions makes rules easier to test and change.",
      usage: "`Fields` is ideal for command-like whitespace tokens and simple exercises. Search indexing, natural-language text, source code, and CSV all have different grammars. Using a familiar helper is valuable only when its documented split rule matches the question.",
      mistake: "Lowercasing can combine `Go` and `go`, while trimming punctuation can combine `go` and `go,`; neither choice is universally correct. A count discrepancy should be reduced to the smallest input that produces two unexpected tokens, then fixed in the tokenizer rather than patched in the map.",
      comparison: "A string helper sees the whole input at once. Scanner offers bounded incremental processing and a default word split function, while `FieldsFunc` offers custom separators for an in-memory string. A hand-written state machine is justified only when token rules cannot be expressed clearly by those tools.",
      scenario: "Streaming keeps memory related to unique words rather than file size, although a vocabulary with millions of unique tokens can still make the map large. Scanner must be checked for errors and configured if a legal token may exceed its buffer. Very large counts may also need a wider integer type.",
    },
    code: goCode(
      "func wordCounts(text string) map[string]int {",
      "    counts := make(map[string]int)",
      "    for _, word := range strings.Fields(text) {",
      "        counts[strings.ToLower(word)]++",
      "    }",
      "    return counts",
      "}",
    ),
    table: "| Tool | Input style | Split rule | Important limit |\n|---|---|---|---|\n| `strings.Fields` | Whole string | Unicode whitespace | Holds tokens in memory |\n| `strings.FieldsFunc` | Whole string | Custom rune separator | Separator-only model |\n| `bufio.Scanner` | Stream | Configurable split function | Token buffer must be sized |",
  },

  comparisons: {
    label: "choosing a Go coding approach",
    alt: "a more complex algorithm",
    questions: [
      "Should a Go string algorithm work with bytes or runes?",
      "When is a frequency map better than sorting?",
      "Why is iteration usually preferred to naive recursion?",
      "How do hash lookup and nested loops trade time for space?",
      "When should a Go solution use the standard library instead of manual code?",
    ],
    definition: "A good Go interview solution chooses a representation and algorithm from the input contract. Bytes, runes, maps, sorting, iteration, and library helpers each provide different costs and guarantees.",
    mechanism: "Start with the required unit of data, expected input size, output, and memory limit. Those facts narrow the valid approaches before syntax or personal preference matters.",
    use: "Prefer the simplest approach that meets the real constraints and preserves the required data. Explain the invariant that keeps it correct and calculate both time and extra space.",
    example: "a Unicode palindrome needs runes, while a Two Sum search on unsorted indexes benefits from a map. An ASCII protocol token and a tiny input can justify different choices.",
    mistake: "Memorizing one fastest-looking pattern and using it everywhere can corrupt text, lose indexes, allocate needlessly, or add complexity without a measured benefit.",
    contrast: "Library functions reduce code and communicate intent; manual algorithms expose control when the required rule differs. Neither is automatically faster or clearer without the input contract.",
    boundary: "Expected O(1) map lookup is not a promise about every operation, Unicode code points are not always displayed characters, and a lower Big-O result can still have a larger constant cost on tiny inputs.",
    repair: "State the input guarantee, write a small counterexample for the rejected approach, and test the chosen invariant at its boundary before optimizing.",
    scenario: "When a solution is correct but unsuitable, measure the dimension that matters: invalid encoding, repeated work, allocation, lost order, or growth with input size.",
    quick: ["Representation follows the input contract", "Big-O describes growth, not every cost", "Maps trade space for expected lookup speed", "Library code is often clearer", "An invariant explains correctness"],
    deepByMode: {
      fundamentals: "Byte operations preserve encoded bytes and are correct for byte-oriented data; rune operations decode Unicode code points and are usually expected for text exercises. The choice changes indexing, length, and reversal behaviour. State the supported unit before describing complexity because `len(s)` counts bytes, not runes.",
      usage: "A frequency map is strong when counts or direct lookup are central and the alphabet is not tiny. Sorting is attractive when ordered output is also needed or when avoiding a hash table matters. Counting is expected linear time with space for distinct values; comparison sorting is normally O(n log n).",
      mistake: "Direct recursion mirrors many mathematical definitions but can repeat subproblems and grow the call stack. Iteration is preferred when states form a simple forward chain. Recursion remains natural for nested structures such as trees, where the shape itself supplies the smaller subproblems.",
      comparison: "Nested loops spend little extra memory but compare many pairs. A hash map records earlier information so later work becomes an expected constant-time lookup. This is a deliberate time-space trade: O(n²) time with O(1) auxiliary space versus expected O(n) time with O(n) space.",
      scenario: "Use standard packages such as `strings`, `sort`, `slices`, and `bufio` when their documented semantics match the problem. Manual code is appropriate for a different comparison rule, streaming state, or an invariant the helper does not express. Tests should protect semantics before benchmarks guide optimization.",
    },
    codeByMode: {
      fundamentals: goCode("for _, r := range text {", "    fmt.Printf(\"%c\\n\", r) // decoded rune, not a byte", "}"),
      usage: goCode("counts := map[rune]int{}", "for _, r := range text { counts[r]++ }"),
      mistake: goCode("a, b := 0, 1", "for i := 0; i < n; i++ { a, b = b, a+b }"),
      comparison: goCode("seen := map[int]struct{}{}", "for _, n := range nums {", "    if _, ok := seen[target-n]; ok { return true }", "    seen[n] = struct{}{}", "}"),
      scenario: goCode("words := strings.Fields(text)", "slices.Sort(words)"),
    },
    table: "| Decision | First question | Common choice |\n|---|---|---|\n| Bytes or runes | Is input bytes or human text? | Runes for code-point text |\n| Map or sort | Need direct lookup/counts or order? | Map for lookup, sort for order |\n| Loop or recursion | Linear state or nested structure? | Loop for linear state |\n| Library or manual | Do helper semantics match exactly? | Library when they do |",
  },
});

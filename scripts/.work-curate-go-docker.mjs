import { curateModule } from "./.work-curate-go-shells-lib.mjs";

curateModule("docker-go-basics", {
  "dockerfile-for-go-basics": {
    label: "a Dockerfile for a Go application",
    alt: "building and running the binary directly on the host",
    definition: "A Dockerfile is a repeatable recipe for building an image. For a Go service it normally defines a build environment, downloads modules, compiles the command, and places the resulting binary plus required runtime files into a small final image.",
    mechanism: "Each instruction creates build state that Docker can cache. Copying `go.mod` and `go.sum` before source code lets dependency download stay cached when only application files change; a later `COPY` brings in the source for compilation.",
    use: "Use a Dockerfile when the service needs a consistent, distributable runtime image for CI, testing, or deployment. Pin important base-image versions, keep the build context small, and make runtime configuration external to the image.",
    example: "a builder based on the official Go image can run `go mod download` and `go build`, then a second stage copies `/out/server` into a minimal runtime image. The deployed image does not need the compiler or module cache.",
    mistake: "Common mistakes include copying the whole repository before downloading modules, storing secrets in `ENV` or build arguments, running as root without need, using an unpinned moving base, and forgetting certificates or CGO libraries required at runtime.",
    contrast: "A host build is quick for local work but depends on the host's tools and libraries. A Dockerfile records the environment and produces an image that can move between compatible container hosts, at the cost of image maintenance and container-specific debugging.",
    boundary: "A container shares the host kernel; it is not a virtual machine. A tiny `scratch` image only works when the binary and copied files are truly self-contained, including CA certificates, time-zone data, and any dynamic libraries the program uses.",
    repair: "Inspect build layers and the final filesystem, use `.dockerignore`, separate dependency and source copies, build for the target platform, run as a non-root user, scan the image, and start the binary directly so it receives signals.",
    scenario: "When an image builds but the container will not start, inspect the entrypoint, target architecture, executable permissions, linked libraries, and runtime files. Compare the builder output with the path copied into the final stage.",
    quick: ["A Dockerfile produces an image", "Layer order controls cache reuse", "Build tools need not exist in the final image", "Secrets must not be baked into layers", "Minimal images still need required runtime files"],
    deep: "A useful Go image has two contracts. The build contract says which source, module graph, Go version, target operating system, and architecture produce the executable. The runtime contract says which user, files, certificates, ports, environment values, and signals the executable expects. Multi-stage builds let these contracts use different base images. Build caching works best when stable inputs appear before frequently changed source files. Image size matters, but correctness and patchability matter more than chasing the smallest possible number. Minimal images provide fewer tools for an attacker and fewer packages to update, yet they can also make diagnosis harder and omit files the service assumes exist. The final command should normally use exec form and run the Go process as PID 1 so termination reaches it. The service must then handle shutdown and stop accepting work before the container's grace period ends.",
    code: "~~~dockerfile\nFROM golang:1.25 AS build\nWORKDIR /src\nCOPY go.mod go.sum ./\nRUN go mod download\nCOPY . .\nRUN CGO_ENABLED=0 go build -trimpath -o /out/server ./cmd/server\n\nFROM gcr.io/distroless/static-debian12:nonroot\nCOPY --from=build /out/server /server\nUSER nonroot:nonroot\nENTRYPOINT [\"/server\"]\n~~~",
    table: "| Stage | Contains | Purpose |\n|---|---|---|\n| Builder | Go toolchain, source, module cache | Compile and test |\n| Runtime | Binary and required runtime files | Run the service |\n| Image config | User, entrypoint, defaults | Define the process contract |",
  },
  "go-multi-stage-build-basics": {
    label: "a multi-stage Docker build for Go",
    alt: "a single-stage image containing the Go toolchain",
    definition: "A multi-stage Dockerfile uses one stage to compile a Go program and another to run it. `COPY --from=build` transfers only selected artifacts, so compilers, source files, and build caches do not have to ship in production.",
    mechanism: "Stages have separate filesystems and may use different base images. Docker builds the dependency graph needed by the selected target; the final stage receives only files explicitly copied from earlier stages.",
    use: "Use multiple stages when build dependencies are larger or more privileged than the runtime needs. This is a natural fit for Go because many services can be compiled into one executable, sometimes with CGO disabled.",
    example: "the first stage compiles `./cmd/api` to `/out/api`; the final stage copies that file and CA certificates, selects a non-root user, and launches `/api`. Source code and the Go compiler remain outside the runtime image.",
    mistake: "The usual mistake is assuming every Go binary is static. Enabling CGO can introduce dynamic-library requirements, and even a static binary may need certificates or time-zone data. Another mistake is invalidating the module cache on every source edit.",
    contrast: "A single-stage image is simpler to inspect and can be convenient during development, but it includes build tools and source that production does not need. Multi-stage builds reduce the runtime contents while keeping the full build environment reproducible.",
    boundary: "Smaller does not automatically mean safer or easier to operate. The chosen runtime base still needs security updates, a usable non-root identity, and every file the program reads. Platform and CGO settings must match the destination.",
    repair: "Check the executable with tools such as `file` or `ldd` in the builder, verify certificates and user IDs in the runtime stage, use cache-friendly copy order, and run the final image in CI on its target platform.",
    scenario: "For a `no such file or directory` error when the binary visibly exists, check its architecture and dynamic loader rather than only its path. A binary built for the wrong platform or linked to a missing loader produces this confusing symptom.",
    quick: ["Build and runtime stages have separate filesystems", "Only explicit artifacts cross stages", "CGO changes runtime library needs", "CA certificates are data, not part of the Go binary", "Test the final stage, not only the builder"],
    deep: "Multi-stage construction reduces the gap between a rich compiler environment and a focused runtime. The builder may contain compilers, source, Git metadata, and caches. The production stage needs only what the process reads or executes. This separation also prevents accidental reliance on a tool that exists during compilation but not after deployment. Go's static-linking story depends on the program: pure-Go packages can often build with `CGO_ENABLED=0`, while database drivers or native libraries may require CGO and matching system libraries. Cross-compilation adds another dimension because `GOOS` and `GOARCH` must match the runtime platform. BuildKit cache mounts can speed repeated module or build-cache use without copying those caches into the image. The reliable test is therefore not image size alone. Start the final image, exercise TLS, time zones, file access, health endpoints, and graceful shutdown under the same platform and security settings used in deployment.",
    code: "~~~dockerfile\nFROM golang:1.25 AS build\nWORKDIR /src\nCOPY go.mod go.sum ./\nRUN go mod download\nCOPY . .\nRUN CGO_ENABLED=0 GOOS=linux go build -o /out/api ./cmd/api\n\nFROM scratch\nCOPY --from=build /etc/ssl/certs/ca-certificates.crt /etc/ssl/certs/\nCOPY --from=build /out/api /api\nENTRYPOINT [\"/api\"]\n~~~",
    flow: "```mermaid\nflowchart LR\n  A[Source + modules] --> B[Builder stage]\n  B --> C[Compiled binary]\n  C --> D[Runtime stage]\n  B -. toolchain and cache stay behind .-> E[Not shipped]\n```",
    table: "| Concern | Builder stage | Runtime stage |\n|---|---|---|\n| Compiler | Required | Usually absent |\n| Source | Required | Usually absent |\n| Binary | Produced | Required |\n| Certificates/libraries | Build-dependent | Required when program uses them |",
  },
  "docker-run-go-binary": {
    label: "running a Go binary in Docker",
    alt: "starting the binary as a host process",
    definition: "`docker run` creates and starts a container from an image. The image supplies the filesystem and default command, while flags select runtime settings such as published ports, environment values, mounted data, resource limits, and the container name.",
    mechanism: "The Go process runs inside the container's namespaces and uses the host kernel. An exposed image port is documentation; `-p 8080:8080` publishes a host port to the container port so traffic can reach the server.",
    use: "Run a Go image when testing the exact packaged artifact or deploying it on a container host. Bind the server to a container-reachable address such as `:8080`, provide configuration at runtime, and set a restart or orchestration policy outside the binary.",
    example: "`docker run --rm -p 8080:8080 --env-file .env api:1.4` starts the image, maps host port 8080, and removes the stopped container. The service must listen on port 8080 inside the container.",
    mistake: "Frequent mistakes are binding the server only to `127.0.0.1` inside the container, confusing `EXPOSE` with publishing, putting secrets directly in command history, using shell form that swallows signals, and expecting container files to survive removal.",
    contrast: "A host process has direct access to host paths and tools. A container applies an image-defined filesystem and isolated runtime settings, which improves consistency but requires explicit networking, storage, configuration, and observability.",
    boundary: "Published ports may be reachable beyond localhost depending on the bind address and firewall. Containers are not a secret store, durable disk by default, or a replacement for graceful application shutdown.",
    repair: "Inspect `docker ps`, logs, port mappings, environment, mounts, health, and exit status; run the image command interactively when appropriate; verify the server address; and send a stop signal to test that the Go process exits cleanly.",
    scenario: "If the service works inside the container but not from the host, check the listening address, container port, published host port, protocol, and firewall in that order. If it exits immediately, inspect its logs and configured command.",
    quick: ["An image is the template; a container is a running instance", "`-p` publishes a port", "The service must listen on the container port", "Runtime files disappear unless stored in a mount", "PID 1 must receive and handle termination"],
    deep: "Running the image turns static build content into a process with a network and storage boundary. Docker chooses a writable container layer, applies mounts and environment values, creates network interfaces, and starts the configured entrypoint. Port publishing has two numbers: the host-side port clients use and the container-side port where the server listens. Binding the host side to `127.0.0.1` can limit local exposure, while an unspecified host address may publish more broadly. Container names and restart settings belong to operations, not application identity. Logs should normally go to standard output and error so the runtime can collect them. State that must survive a container replacement belongs in a named volume or external service. Shutdown in Go follows the same process lifecycle: receive `SIGTERM`, stop accepting new requests, let in-flight work finish within a deadline, and then exit.",
    code: "~~~sh\ndocker run --rm \\\n  --name catalog-api \\\n  --publish 127.0.0.1:8080:8080 \\\n  --env-file .env \\\n  catalog-api:1.4.0\n~~~",
    table: "| Setting | Meaning | Common confusion |\n|---|---|---|\n| Image name/tag | Artifact to instantiate | A tag is mutable unless a digest is used |\n| `-p host:container` | Network publication | `EXPOSE` alone does not publish |\n| `--env-file` | Runtime configuration | It is not encryption |\n| Volume or bind mount | External persistent/shared path | Container writable layer is disposable |",
  },
  "environment-variables-docker": {
    label: "Docker environment configuration for Go",
    alt: "baking configuration into the image",
    definition: "Environment variables are key-value strings supplied to a container process. A Go program reads them at startup with `os.LookupEnv` or a configuration library, validates them, converts types, and stores a typed configuration object.",
    mechanism: "Docker can set variables with image `ENV`, `docker run -e`, `--env-file`, Compose configuration, or an orchestrator. Later runtime values can override image defaults, but the exact precedence depends on the tool used to start the container.",
    use: "Use environment variables for deployment-specific values such as addresses, ports, feature settings, and references to mounted secrets. Parse once at startup and fail with a clear message when a required value is absent or invalid.",
    example: "`PORT=8080` arrives as text. Startup code parses it with `strconv.Atoi`, checks the valid range, and builds `:8080`; it should not silently turn a malformed value into port zero.",
    mistake: "Problems arise when code cannot distinguish missing from empty values, reads variables throughout business logic, logs secrets, trusts malformed numbers, or uses Dockerfile `ENV` and build arguments for credentials that remain visible in image metadata or layers.",
    contrast: "Runtime configuration lets one image move through environments without rebuilding. Hard-coded or baked configuration ties the artifact to one deployment and can expose sensitive or environment-specific data in the image history.",
    boundary: "Environment variables are visible to the process and often to users who can inspect the container; they are not encryption. Large structured configuration and secret material may be better supplied through controlled files or a secret manager.",
    repair: "Use `LookupEnv`, validate required names and formats during startup, redact sensitive values, document the configuration contract, separate defaults from required settings, and test missing, empty, malformed, and valid cases.",
    scenario: "When configuration differs between environments, inspect the effective container settings and the application's parsed startup summary without printing secrets. Check spelling, precedence, quoting in env files, and whether the process was restarted after values changed.",
    quick: ["Environment values are strings", "Missing and empty can be different states", "Parse and validate once at startup", "Runtime values keep the image reusable", "Environment storage is not a secret-protection mechanism"],
    deep: "External configuration separates an immutable application artifact from values that differ by deployment. That boundary works only if the application has a clear schema for its environment: required keys, defaults, allowed ranges, and relationships between values. `os.Getenv` returns an empty string for both missing and explicitly empty variables, whereas `os.LookupEnv` keeps those cases separate. Parsing should produce a typed configuration before the server begins accepting traffic, so an invalid duration or URL becomes a startup failure rather than a late request bug. Docker offers several places to define values, which makes precedence important during diagnosis. The image should contain safe defaults at most; deployment tools should supply environment-specific values. Secrets require extra care because anyone with sufficient container inspection access may see environment metadata. Mounted secret files or a platform secret service can offer better rotation and access control, but the Go application must still handle errors and avoid logging the contents.",
    code: "~~~go\nfunc loadConfig() (Config, error) {\n    raw, ok := os.LookupEnv(\"PORT\")\n    if !ok { return Config{}, errors.New(\"PORT is required\") }\n    port, err := strconv.Atoi(raw)\n    if err != nil || port < 1 || port > 65535 {\n        return Config{}, fmt.Errorf(\"PORT must be 1..65535\")\n    }\n    return Config{Port: port}, nil\n}\n~~~",
    table: "| Source | Good use | Security note |\n|---|---|---|\n| Dockerfile `ENV` | Non-sensitive safe default | Stored with image configuration |\n| `--env-file` / Compose | Local or deployment values | Protect the file itself |\n| Mounted secret file | Sensitive material | Avoid copying into image |\n| Secret manager | Central control and rotation | Requires identity and availability design |",
  },
  "docker-compose-basics": {
    label: "Docker Compose for a Go service",
    alt: "separate docker run commands",
    definition: "Docker Compose describes a group of containers, networks, volumes, and runtime settings in one YAML file. It is useful for running a Go service with dependencies such as PostgreSQL or Redis as a repeatable local stack.",
    mechanism: "Compose creates a default network where services can reach each other by service name. `depends_on` controls creation order and can use health conditions in supported forms, but simple ordering alone does not prove that an application dependency is ready.",
    use: "Use Compose for local development, integration tests, demos, and small single-host setups where one command should reproduce the service topology. Keep production orchestration requirements separate unless Compose is deliberately the deployment tool.",
    example: "an `api` service can use `DATABASE_URL=postgres://...@db:5432/app` and depend on a `db` service. Inside the network the hostname is `db`, not `localhost`, because localhost refers to the current container.",
    mistake: "Common mistakes are using host `localhost` for another service, assuming `depends_on` means ready, storing real credentials in the file, mounting over files built into the image, and expecting Compose to provide multi-host scheduling or rolling deployment.",
    contrast: "Several `docker run` commands can create the same containers but spread the topology across shell history. Compose makes relationships reviewable and repeatable, while a cluster orchestrator adds scheduling, rollout, service discovery, and recovery across machines.",
    boundary: "Compose does not make databases stateless or guarantee application readiness. Persistent data needs volumes, startup needs retries or health-aware conditions, and secrets and production availability need an explicit operational design.",
    repair: "Inspect the rendered Compose configuration, service logs, network names, health states, volumes, and environment values. Add application retries with bounded backoff and ensure service-to-service URLs use Compose DNS names.",
    scenario: "If the API cannot reach the database, run a DNS or connection check from the API container, verify it uses `db:5432`, inspect database health and credentials, and distinguish network refusal from authentication or schema errors.",
    quick: ["Compose describes a multi-container application", "Service names become DNS names on the network", "Container localhost is not another service", "Start order is not always readiness", "Volumes preserve selected data outside container layers"],
    deep: "Compose turns a set of runtime commands into versioned application topology. A service definition selects an image or build, command, environment, ports, mounts, networks, and health settings. Containers on the same Compose network discover each other through service names, while published ports are needed only for access from outside that network. This distinction prevents a common error: the database is `db:5432` from the API container even if a different host port is published for a desktop client. Startup is a distributed-systems concern even on one laptop. A database process may have started before it can accept authenticated queries or before migrations complete. Health conditions can improve ordering, but the application should still handle temporary dependency failures according to its requirements. Named volumes preserve database files when containers are replaced. Bind mounts are useful for source code but can hide paths placed in an image, so their effect should be deliberate.",
    code: "~~~yaml\nservices:\n  api:\n    build: .\n    ports: [\"8080:8080\"]\n    environment:\n      DATABASE_URL: ${DATABASE_URL}\n    depends_on:\n      db:\n        condition: service_healthy\n  db:\n    image: postgres:17\n    environment:\n      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}\n    healthcheck:\n      test: [\"CMD-SHELL\", \"pg_isready -U postgres\"]\n    volumes: [\"db-data:/var/lib/postgresql/data\"]\nvolumes:\n  db-data: {}\n~~~",
    flow: "```mermaid\nflowchart LR\n  H[Host browser] -- published 8080 --> A[api:8080]\n  A -- db:5432 on Compose network --> D[PostgreSQL]\n  D --- V[(named volume)]\n```",
    table: "| Address used from | API address | Database address |\n|---|---|---|\n| Host machine | `localhost:8080` | Published host port, if configured |\n| API container | Its own `localhost:8080` | `db:5432` |\n| Database container | `api:8080` | Its own `localhost:5432` |",
  },
  "comparisons": {
    label: "containerized Go deployment",
    alt: "a direct host or virtual-machine deployment",
    definition: "Containerized deployment packages a Go binary with a defined filesystem and process configuration as an image. Direct deployment copies or builds the binary on a host and relies more heavily on that host's installed files and service manager.",
    mechanism: "A container process still runs on the host kernel but receives isolated namespaces, resource controls, image layers, and explicit mounts and networks. A host process uses the machine's normal filesystem and process environment directly.",
    use: "Containers fit teams that want the same artifact in CI and deployment, image-based rollbacks, or an orchestrated service platform. Direct binaries remain an excellent choice for simple services where a host service manager and a small release process meet the requirement.",
    example: "the same `catalog-api@sha256:...` image can be tested and promoted, while configuration changes at runtime. A direct release might copy `catalog-api` to a server and let systemd manage its user, environment file, restart, and logs.",
    mistake: "A common mistake is assuming containers provide a full security boundary or make operations automatic. Teams may ignore image patching, persistent data, signal handling, resource limits, and network exposure because the service appears packaged.",
    contrast: "Containers strengthen artifact consistency and integrate well with registries and orchestrators; direct binaries have fewer layers and simpler host-level inspection. Neither choice fixes application design, security, scaling, or observability by itself.",
    boundary: "The right unit is the whole operating model: number of services, deployment frequency, platform skills, rollback needs, isolation, and cost. Adding Kubernetes to one rarely changed binary can create more work than value.",
    repair: "Make the process contract explicit in either model: non-root identity, configuration, network listeners, durable state, health, resource limits, logs, shutdown, upgrades, and rollback. Test the packaged artifact rather than only local source execution.",
    scenario: "For environment-only failures, compare the artifact digest or binary checksum, configuration, user permissions, network path, files, architecture, and signals between the working and failing hosts. The first difference narrows the deployment layer at fault.",
    questions: ["What changes when a Go service is deployed in a container?", "When should a Go team choose containers for deployment?", "Which assumptions make containerized Go services fail in production?", "What is the difference between a Go container and a directly deployed binary?", "How do you investigate a Go service that fails only in its container?"],
    quick: ["A container packages runtime filesystem and process settings", "Containers share the host kernel", "Direct binaries can be simpler for small deployments", "Artifact consistency is the main container benefit", "Operations and security still require deliberate work"],
    deep: "A Go executable is already a strong deployment unit, which makes the container decision more nuanced than it is for runtimes with large host dependencies. An image adds a filesystem snapshot, metadata, distribution through a registry, and a standard contract for container platforms. Direct deployment can provide equally reliable restarts, users, environment files, and logs through a service manager, with less tooling. Containers become especially valuable when many services share one delivery platform or when immutable artifacts and automated rollouts are established practices. They also add responsibilities: base images need updates, registries and provenance matter, container networking differs from host networking, and state must live outside replaceable layers. Security comes from several controls together—minimal contents, non-root execution, capabilities, read-only paths, resource limits, and host hardening—not from the word container. The decision should reduce total delivery and operating risk rather than follow a trend.",
    code: "~~~text\nDirect:    source -> go build -> binary -> service manager -> host process\nContainer: source -> image build -> registry -> container runtime -> host process\n~~~",
    table: "| Concern | Container image | Direct binary |\n|---|---|---|\n| Artifact | Binary plus runtime filesystem/config | Binary plus release files |\n| Isolation | Namespaces and resource controls | Host user/process controls |\n| Distribution | Registry and digest | Package, object store, or copy |\n| Operations | Runtime or orchestrator | Service manager and host tooling |",
  },
});

import { curateModule } from "./.work-curate-go-shells-lib.mjs";

curateModule("database-basics-go", {
  "database-sql-package": {
    label: "the database/sql package",
    alt: "a driver-specific database API",
    definition: "`database/sql` is Go's standard interface for relational databases. It gives applications common types for database handles, queries, rows, statements, and transactions, while a separate driver performs the database-specific network work.",
    mechanism: "An `*sql.DB` is a long-lived handle that manages a pool of underlying connections. `sql.Open` may only validate its arguments, so startup code normally calls `PingContext` when it must prove the database is reachable.",
    use: "Use `database/sql` when an application needs SQL with direct control over queries, scanning, transactions, timeouts, and pool settings. Create one shared `*sql.DB` for the process instead of opening a handle for every request.",
    example: "a service can call `db.QueryRowContext(ctx, query, id).Scan(&user.Name)` to read one user. The context places a deadline on the call, and `Scan` makes the expected result shape explicit.",
    mistake: "A frequent mistake is treating `*sql.DB` as one connection and repeatedly opening and closing it. Another is assuming `sql.Open` has already connected, or forgetting to close a `Rows` result.",
    contrast: "`database/sql` is portable across drivers and keeps SQL visible; a driver-specific API may expose special database features and types. The standard package is usually the simpler boundary unless the application genuinely needs those extensions.",
    boundary: "The package does not include a database driver, generate SQL, or map rows to structs automatically. Query text, argument placeholders, transaction boundaries, null handling, and connection limits remain application decisions.",
    repair: "Keep one configured handle, verify it with `PingContext`, pass contexts to database calls, close `Rows`, check `rows.Err()`, and classify expected results such as `sql.ErrNoRows` separately from infrastructure failures.",
    scenario: "When database calls hang or fail, first check the context deadline, returned error, pool statistics, and whether every `Rows`, `Stmt`, or transaction is being completed. Then run the same parameterized query against the expected database and schema.",
    quick: ["`*sql.DB` represents a pooled database handle, not one connection", "A driver must be registered separately", "`sql.Open` may not contact the server", "Use context-aware query methods", "Close rows and inspect their final error"],
    deep: "The standard package separates the application's SQL workflow from the wire protocol used by PostgreSQL, MySQL, SQLite, or another database. A driver registers itself, and the application opens a handle with the driver's name and its data-source settings. The handle is safe for concurrent use and obtains or reuses physical connections as work arrives. A query that returns rows owns resources until those rows are closed. A transaction temporarily binds its work to one connection so its statements share the same database session. This model explains several practical rules: keep the handle for the application's lifetime, configure pool limits deliberately, use placeholders for values, carry request cancellation through `Context`, and handle `NULL` with nullable Go types or SQL expressions. Errors are part of the data contract; no row, malformed SQL, a canceled deadline, and a lost connection do not mean the same thing.",
    code: "~~~go\nfunc findUser(ctx context.Context, db *sql.DB, id int64) (User, error) {\n    const q = `SELECT id, name FROM users WHERE id = $1`\n    var u User\n    err := db.QueryRowContext(ctx, q, id).Scan(&u.ID, &u.Name)\n    if errors.Is(err, sql.ErrNoRows) {\n        return User{}, ErrUserNotFound\n    }\n    return u, err\n}\n~~~",
    table: "| Value | Real meaning | Lifetime |\n|---|---|---|\n| `*sql.DB` || Concurrent handle and connection pool | Usually the whole process |\n| `*sql.Conn` | One reserved connection | Close after the session work |\n| `*sql.Tx` | Transaction bound to one connection | Until commit or rollback |\n| `*sql.Rows` | Cursor over a result set | Until exhausted or closed |",
  },
  "connection-pooling-go": {
    label: "SQL connection pooling in Go",
    alt: "opening a new connection for every operation",
    definition: "A connection pool keeps a controlled set of reusable database connections behind `*sql.DB`. Callers borrow a connection for an operation and the package returns it to the idle pool when the query, rows, or transaction is finished.",
    mechanism: "`SetMaxOpenConns` limits open connections, `SetMaxIdleConns` limits idle ones, and lifetime settings retire old connections. When every allowed connection is busy, later calls wait until one becomes available or their context ends.",
    use: "Configure a pool when database access is shared by concurrent requests. Its size should respect the database's total connection budget, the number of application instances, query time, and expected traffic rather than copying one universal number.",
    example: "ten service replicas with `MaxOpenConns(20)` can ask the database for roughly two hundred connections. If the database only permits one hundred, each process must receive a smaller budget or the deployment must change.",
    mistake: "The most damaging mistakes are leaving rows or transactions open, setting an unlimited pool under heavy traffic, and giving every replica the database's entire connection allowance. These lead to waiting, timeouts, or database exhaustion.",
    contrast: "A pool reuses established connections and applies back-pressure; opening a fresh connection for every query adds handshake cost and can create an uncontrolled connection storm. A dedicated `*sql.Conn` is reserved only when session state really matters.",
    boundary: "A larger pool does not automatically make slow queries faster. It can increase contention inside the database. Pool settings must be measured together with query latency, database capacity, replica count, and `DB.Stats()` wait data.",
    repair: "Close resources promptly, ensure every transaction commits or rolls back, add context deadlines, inspect `OpenConnections`, `InUse`, `Idle`, `WaitCount`, and `WaitDuration`, and adjust one limit at a time under representative load.",
    scenario: "For rising database latency, compare request concurrency with pool statistics. A high wait count suggests callers cannot obtain connections; many idle connections point elsewhere. Also check long transactions, leaked rows, slow queries, and database-side connection limits.",
    quick: ["`*sql.DB` owns the pool", "Busy operations borrow connections", "Pool limits create back-pressure", "Every replica consumes part of the database budget", "Leaks often look like pool starvation"],
    deep: "Pooling solves two different problems: establishing a database connection is expensive, and the database can safely serve only a limited number at once. Go's database handle coordinates both. It can open connections as demand rises, retain a smaller idle set for reuse, and make callers wait once the open limit is reached. Waiting is not always a bug; it protects the database from unbounded concurrency. The useful signals are therefore both application-side and database-side. Pool statistics show demand and waiting, while database metrics show active sessions, locks, query time, and resource pressure. Connection lifetime can help rotate credentials or avoid stale network paths, but setting it too low creates needless reconnects. A sound configuration divides the available database budget across replicas, leaves room for maintenance and other clients, and is verified under realistic query latency rather than a synthetic empty query.",
    code: "~~~go\ndb.SetMaxOpenConns(20)\ndb.SetMaxIdleConns(10)\ndb.SetConnMaxLifetime(30 * time.Minute)\ndb.SetConnMaxIdleTime(5 * time.Minute)\n\nctx, cancel := context.WithTimeout(context.Background(), time.Second)\ndefer cancel()\nif err := db.PingContext(ctx); err != nil {\n    return err\n}\n~~~",
    table: "| Symptom | Pool evidence | First investigation |\n|---|---|---|\n| Calls wait before SQL runs | `WaitCount` and `WaitDuration` rise | Busy/leaked connections or limit too small |\n| Database rejects clients | Open connections near server limit | Per-replica budgets |\n| Frequent reconnects | Short connection lifetime | Lifetime and network policy |\n| Plenty of idle connections | `Idle` stays high | Query or database, not pool starvation |",
  },
  "executing-queries-go": {
    label: "executing and scanning SQL in Go",
    alt: "an ORM that maps query results automatically",
    definition: "Go uses `ExecContext` for statements that do not return rows, `QueryContext` for a result set, and `QueryRowContext` for one expected row. `Scan` copies selected columns into destination variables in the same order.",
    mechanism: "Query arguments travel separately from SQL text through placeholders. A multi-row query returns `*sql.Rows`; code loops with `Next`, scans each current row, closes the cursor, and checks `rows.Err()` for errors that happened during iteration.",
    use: "Use direct queries when SQL shape, joins, selected columns, and database behaviour should remain explicit. Select only needed columns and scan into types that correctly represent database values, including nullable columns.",
    example: "`SELECT id, email FROM users WHERE active = $1` can pass `true` as an argument, then scan each row into `int64` and `string`. A nullable nickname needs `sql.NullString`, a pointer, or a query that supplies a non-null value.",
    mistake: "Common errors are using `Exec` for a `SELECT`, forgetting `rows.Close` or `rows.Err`, scanning columns in the wrong order, using the wrong placeholder style, and treating `sql.ErrNoRows` as an unexpected server failure.",
    contrast: "Direct `database/sql` code keeps the exact query and conversion visible. An ORM can remove repetitive mapping and relationship code, but it can also hide generated SQL and make unusual queries harder to reason about.",
    boundary: "Placeholder syntax is driver-specific, and arguments can replace values but not trusted SQL identifiers such as table or column names. A query returning many rows also needs limits or streaming logic so memory and response time remain controlled.",
    repair: "Match the method to the result shape, list columns explicitly, keep scan order beside the query, close and check row iterators, handle no-row results deliberately, and test the query against realistic nulls and multiple rows.",
    scenario: "For wrong scan results, compare the `SELECT` list with every `Scan` destination from left to right. For intermittent errors, inspect the context, `rows.Err`, connection state, and whether iteration stops early without closing the result.",
    quick: ["`ExecContext` returns no rows", "`QueryRowContext` models at most one row", "`QueryContext` requires iteration and closing", "Scan order follows SELECT order", "Nullable database values need nullable Go destinations"],
    deep: "A database query has two contracts. The SQL contract defines which rows and columns may come back; the Go contract defines how those values enter typed variables. Keeping the `SELECT` list explicit makes that boundary reviewable. For one row, `QueryRowContext` delays any query error until `Scan`, including `sql.ErrNoRows`. For several rows, `Next` advances the cursor and `Scan` reads only the current row. An error can occur after some rows have been returned, which is why the final `rows.Err()` check matters. Context cancellation bounds waiting and network work, but the exact driver and database determine how cancellation is carried out. Values belong in query arguments. If a user chooses a sort column, the program must map that choice to a small allow-list of trusted identifiers instead of trying to bind the name as a parameter.",
    code: "~~~go\nrows, err := db.QueryContext(ctx,\n    `SELECT id, email FROM users WHERE active = $1 ORDER BY id`, true)\nif err != nil { return err }\ndefer rows.Close()\n\nfor rows.Next() {\n    var u User\n    if err := rows.Scan(&u.ID, &u.Email); err != nil { return err }\n    users = append(users, u)\n}\nreturn rows.Err()\n~~~",
    table: "| Method | Expected result | Important cleanup/error |\n|---|---|---|\n| `ExecContext` | No row set | Check `Result` only if needed |\n| `QueryRowContext` | Zero or one row | Error appears from `Scan` |\n| `QueryContext` | Zero or many rows | Close rows and check `rows.Err()` |",
  },
  "prepared-statements-go": {
    label: "prepared statements and SQL parameters",
    alt: "building SQL by joining input strings",
    definition: "A prepared statement separates SQL structure from data values. In Go, placeholders stay in the query while values are supplied as arguments to `ExecContext`, `QueryContext`, a prepared `*sql.Stmt`, or transaction methods.",
    mechanism: "The driver encodes each argument according to the database protocol instead of inserting its text into the SQL grammar. `db.PrepareContext` can create a reusable statement, although ordinary parameterized calls may already be prepared internally by the driver.",
    use: "Parameterize every untrusted value and use a reusable prepared statement only when its lifecycle and repeated execution provide a measured benefit. Keep dynamic identifiers behind an allow-list because placeholders represent values, not SQL keywords or column names.",
    example: "a login lookup should use `WHERE email = $1` with the email as an argument. The text `x' OR true --` then remains one string value instead of becoming part of the WHERE clause.",
    mistake: "The dangerous mistake is formatting or concatenating request data into SQL. Another is trying to parameterize a table name, or preparing statements repeatedly inside a hot loop without closing or reusing them.",
    contrast: "Parameterized SQL preserves the statement's grammar regardless of the value. String construction mixes code and data, creates injection risk, complicates quoting, and should be limited to trusted fragments selected by application code.",
    boundary: "Parameters do not validate business meaning, authorize access, make a query fast, or protect identifiers assembled into SQL. They solve the code-versus-data boundary for values; the rest still needs validation and access control.",
    repair: "Move every value into the argument list, map dynamic sort or table choices to fixed trusted strings, close explicit statements, use the placeholder syntax required by the driver, and add a test containing quotes and SQL-looking text.",
    scenario: "When a query behaves differently for special characters, log the query template separately from safely redacted argument metadata and inspect how it was built. Replace interpolation with arguments, then test ordinary, quoted, empty, and hostile-looking values.",
    quick: ["SQL text and values are separate channels", "Placeholders represent values, not identifiers", "Parameters prevent values changing SQL grammar", "Preparation is not the same as input validation", "Explicit statements have a lifecycle and must be closed"],
    deep: "SQL injection occurs when data is allowed to alter the program represented by a query. Parameters prevent that by keeping the query structure fixed and transmitting each value separately. This remains useful even for values produced inside the service because it avoids manual escaping and type ambiguity. An explicit prepared statement is a performance and lifecycle tool, not the security requirement by itself: passing arguments to a normal context-aware query is still parameterized. Driver placeholder forms differ, such as `$1` or `?`, so portable code cannot assume one spelling. Identifiers are different because they are part of SQL grammar. If a report accepts a sort key, translate `created` to a literal trusted fragment such as `created_at`; never paste the raw request value. Finally, parameterization does not decide which rows a user is allowed to access. Authorization predicates and tenant boundaries must still be included and tested.",
    code: "~~~go\nconst lookup = `SELECT id FROM users WHERE email = $1`\nvar id int64\nerr := db.QueryRowContext(ctx, lookup, email).Scan(&id)\n\nallowedSort := map[string]string{\n    \"created\": \"created_at\",\n    \"name\": \"display_name\",\n}\ncolumn, ok := allowedSort[requestedSort]\nif !ok { return ErrInvalidSort }\nquery := `SELECT id FROM users ORDER BY ` + column // trusted map value\n~~~",
    table: "| Input part | Placeholder? | Safe handling |\n|---|---|---|\n| Email, ID, date, amount | Yes | Pass as a query argument |\n| Column or table name | No | Select from a fixed allow-list |\n| ASC/DESC direction | No | Map a small enum to a literal |\n| User authorization | Not solved by parameters | Add and test access predicates |",
  },
  "transactions-go-basics": {
    label: "SQL transactions in Go",
    alt: "independent autocommit statements",
    definition: "A SQL transaction groups statements into one database unit of work. In Go, `BeginTx` returns `*sql.Tx`; statements run through that value until `Commit` makes the changes durable or `Rollback` abandons them.",
    mechanism: "The transaction is bound to one database connection. Reads and writes inside it see the database's transaction rules and isolation level, so using `db.QueryContext` halfway through would run outside that transaction.",
    use: "Use a transaction when several database changes must succeed or fail together, or when a read-modify-write decision needs a defined isolation boundary. Keep the transaction short and avoid slow network calls while it holds database resources.",
    example: "a money transfer debits one account and credits another. Both updates must run through the same `*sql.Tx`; if the credit fails, rollback prevents the database from keeping only the debit.",
    mistake: "Typical mistakes are forgetting rollback on an early return, ignoring the commit error, mixing `db` and `tx` calls, and assuming a transaction automatically prevents every race regardless of isolation level and query design.",
    contrast: "Autocommit is simpler for one independent statement. A transaction adds atomicity across related statements and a consistent database session, but it occupies a connection longer and can introduce lock waits or serialization conflicts.",
    boundary: "A database transaction does not automatically roll back an email, HTTP call, or message already sent to another system. Cross-system workflows need patterns such as an outbox, idempotency, compensation, or a carefully designed distributed protocol.",
    repair: "Call `BeginTx`, defer a rollback as a safe no-op after success, perform every database operation through `tx`, handle each error, and return the result of `Commit`. Add concurrency tests for invariants, not only a happy-path unit test.",
    scenario: "For partial updates, find the transaction boundary and trace which receiver—`db` or `tx`—ran each statement. Inspect every return path, the isolation level, lock or serialization errors, and whether the commit result was checked.",
    quick: ["`*sql.Tx` binds work to one connection", "Commit and rollback end the transaction", "Every transactional query must use `tx`", "Isolation behaviour comes from the database and options", "External side effects need a separate consistency design"],
    deep: "Atomicity is the central promise: either the database accepts all changes in the unit or none of them. Go does not infer that unit from function boundaries; code must begin a transaction and send each relevant statement through the returned `*sql.Tx`. A deferred rollback is a convenient safety net because rollback after a successful commit returns an error but causes no database change. Commit itself can fail, so success is not known until its result is checked. Isolation is separate from atomicity. Concurrent transactions may still observe or conflict with one another according to the database and requested level. Correct code protects a business invariant—for example, total transferred money—using suitable statements, constraints, locks, or retry logic. The transaction should also remain local to database work. Holding it open while calling another service increases lock time and still cannot make that remote side effect atomic.",
    code: "~~~go\nfunc transfer(ctx context.Context, db *sql.DB, from, to int64, amount int64) (err error) {\n    tx, err := db.BeginTx(ctx, nil)\n    if err != nil { return err }\n    defer tx.Rollback()\n\n    if _, err = tx.ExecContext(ctx, debitSQL, amount, from); err != nil { return err }\n    if _, err = tx.ExecContext(ctx, creditSQL, amount, to); err != nil { return err }\n    return tx.Commit()\n}\n~~~",
    flow: "```mermaid\nflowchart LR\n  A[BeginTx] --> B[Debit through tx]\n  B --> C{Credit succeeds?}\n  C -- No --> D[Rollback]\n  C -- Yes --> E[Commit]\n```",
    table: "| Work | Autocommit | Transaction |\n|---|---|---|\n| One independent update | Usually enough | Often unnecessary |\n| Related debit and credit | Can leave partial state | Commits or rolls back together |\n| Remote API plus database | Still not atomic | Requires an additional workflow pattern |",
  },
  "gorm-basics-go": {
    label: "GORM in a Go service",
    alt: "writing SQL directly with database/sql",
    definition: "GORM is a Go ORM that maps structs and model relationships to database operations. It provides query builders, conventions, associations, hooks, and migration helpers on top of a SQL database driver.",
    mechanism: "A `*gorm.DB` builds statements through chained methods and scans results into structs. Zero values, selected fields, association loading, transaction handles, and error values affect the generated SQL, so developers still need to inspect what runs.",
    use: "GORM is useful for routine CRUD and relationship-heavy application code where its conventions reduce repeated mapping. Direct SQL remains valuable for complex reports, database-specific features, carefully tuned queries, or teams that want every statement explicit.",
    example: "`db.WithContext(ctx).Where(\"email = ?\", email).First(&user)` binds the email as a value and returns `gorm.ErrRecordNotFound` when no record matches. Debug or dry-run logging can show the generated statement.",
    mistake: "Common mistakes include ignoring `result.Error`, causing N+1 queries through repeated association loads, assuming `AutoMigrate` is a complete production migration plan, and using struct conditions without noticing that zero-valued fields may be omitted.",
    contrast: "GORM trades some SQL visibility for faster model-oriented CRUD. `database/sql` requires more scanning and query code but makes statement shape, selected columns, and database behaviour direct and predictable.",
    boundary: "An ORM does not remove the need to understand SQL, indexes, transactions, constraints, connection pools, and query plans. Production schema changes may need versioned migration tools and review beyond GORM's automatic migration helper.",
    repair: "Check every returned error, enable SQL observation in development, preload relationships deliberately, measure query counts, use explicit maps or selected fields for zero values, and place schema changes in a reviewed migration workflow.",
    scenario: "When a GORM endpoint is slow or returns surprising data, capture the generated SQL and arguments, count statements, compare selected fields with the model, check preload behaviour, and run the SQL with an execution plan.",
    quick: ["GORM maps Go models to SQL operations", "Generated SQL still needs review", "Always check `result.Error`", "Associations can create N+1 queries", "AutoMigrate is not every team's full production migration strategy"],
    deep: "An ORM changes the level at which application code expresses database work, but it does not change what the database executes. GORM reads model metadata and chained query options, produces SQL, sends values through a driver, and maps returned columns back into fields. That convenience is strongest for standard create, read, update, and delete flows. It becomes less helpful when the query is the important design artifact, such as a complex aggregate or database-specific operation. Model behaviour also has rules worth testing: updates may skip zero values depending on the API used, associations can be loaded lazily through extra application calls, and missing records have a distinct error. Transactions must use the transaction-scoped handle just as `database/sql` code uses `*sql.Tx`. A healthy GORM codebase therefore keeps query logging or tracing available, watches statement counts, uses database constraints, and permits direct SQL where it is clearer.",
    code: "~~~go\nvar user User\nresult := db.WithContext(ctx).\n    Preload(\"Roles\").\n    Where(\"email = ?\", email).\n    First(&user)\n\nif errors.Is(result.Error, gorm.ErrRecordNotFound) {\n    return User{}, ErrUserNotFound\n}\nreturn user, result.Error\n~~~",
    table: "| Need | GORM | Direct SQL |\n|---|---|---|\n| Routine model CRUD | Less mapping code | More explicit code |\n| Exact complex query | Possible, but abstraction may add noise | Query is fully visible |\n| Relationships | Association helpers and preload | Explicit joins or several queries |\n| Database knowledge | Still required | Required directly |",
  },
});

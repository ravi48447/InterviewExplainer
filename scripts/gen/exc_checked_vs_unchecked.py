import os, sys
sys.path.insert(0, os.path.dirname(__file__))
from _apply import apply

F = "content/java-backend-fresher/java-exceptions/checked-vs-unchecked/complete-qa.json"

UPDATES = {
"checked-vs-unchecked-001": {
  "layout_type": "comparison-arena",
  "direct_answer": "**Checked exceptions** (direct `Exception` subclasses like `IOException`, `SQLException`) must be caught or declared with `throws` — the **compiler enforces** it, modelling recoverable external failures. **Unchecked exceptions** (`RuntimeException` subclasses like `NullPointerException`) need no declaration and usually signal **bugs**. The line is drawn at `RuntimeException`: anything under it is unchecked, everything else under `Exception` is checked.",
  "interviewer_intent": {
    "testing": "Whether you can define the compiler-enforced distinction and place exceptions correctly under RuntimeException or not",
    "common_mistake": "Thinking the difference is about when they occur, or that unchecked means less serious",
    "to_stand_out": "Frame it as intent: checked = recoverable external failure you should plan for; unchecked = a programming bug to fix"
  },
  "sections": [
    {"type":"overview","title":"The Line Is Drawn at RuntimeException","content":"Every exception under `Exception` is **checked** *unless* it descends from **`RuntimeException`**, in which case it's **unchecked**. That single boundary is the whole rule.\n\n**Checked exceptions** — `IOException`, `SQLException` — model recoverable external failures: a file is missing, a query failed. The compiler **forces** you to either `catch` them or declare `throws`, so you can't ignore them by accident. **Unchecked exceptions** — `NullPointerException`, `IllegalArgumentException` — usually mean a **bug in the code**; the compiler stays out of your way so you don't wrap every line in try-catch. It's about *intent and the compiler*, not about *when* the exception happens (all happen at runtime)."},
    {"type":"comparison_table","title":"Checked vs Unchecked","content":"| Aspect | Checked | Unchecked |\n|---|---|---|\n| Parent | `Exception` (not `RuntimeException`) | `RuntimeException` |\n| Compiler enforcement | Must `catch` or declare `throws` | None |\n| Models | Recoverable external failure | Programming bug / misuse |\n| Examples | `IOException`, `SQLException` | `NullPointerException`, `IllegalArgumentException` |\n| Typical response | Handle / retry | Fix the code |"},
    {"type":"concept_map","title":"Two Categories at a Glance","content":"blue|Checked|~Compiler forces handling|IOException, SQLException|external & recoverable|catch or throws\nemerald|Unchecked|~No compiler enforcement|RuntimeException subclasses|usually a bug|fix the code\namber|The boundary|~RuntimeException is the line|under it = unchecked|elsewhere under Exception = checked|Error is also unchecked\nrose|Name trap|~Not about timing|all happen at runtime|'checked' = compile-time check|severity isn't the point"},
    {"type":"before_code","title":"Checked: compiler forces you to handle it","content":"```java\nString read(String path) {\n    return Files.readString(Path.of(path));  // compile error: unhandled IOException\n}\n```"},
    {"type":"after_code","title":"Unchecked needs nothing; checked needs catch or throws","content":"```java\n// Unchecked: no throws needed, compiler doesn't complain\nint half(int n) {\n    if (n < 0) throw new IllegalArgumentException(\"n must be >= 0\");\n    return n / 2;\n}\n\n// Checked: must declare (or catch)\nString read(String path) throws IOException {\n    return Files.readString(Path.of(path));\n}\n```\n*The checked `IOException` needs `throws`; the unchecked `IllegalArgumentException` needs nothing.*"},
    {"type":"key_points","title":"Key Points","content":"- **Checked** = under `Exception` but not `RuntimeException`; compiler forces `catch`/`throws`.\n- **Unchecked** = `RuntimeException` (and `Error`); no compiler enforcement.\n- **Checked models recoverable external failures**; **unchecked usually means a bug.**\n- **The boundary is `RuntimeException`** — that's the rule in one sentence.\n- **It's not about timing or severity** — every exception is thrown at runtime."},
    {"type":"speakable_answer","title":"How to Answer This Verbally","content":"The cleanest way to define it is by where the line is drawn in the hierarchy. Everything under Exception is checked, except for the things that descend from RuntimeException, which are unchecked. So RuntimeException is the single boundary, and that's basically the whole rule. Checked exceptions are things like I O exception and S Q L exception, and they model recoverable external failures — a file's missing, the network dropped, a query failed. The compiler forces you to deal with them: you either catch them or you declare them with a throws clause. The point is that you can't accidentally ignore them, because the build won't pass. Unchecked exceptions are the RuntimeException subclasses, like NullPointerException or IllegalArgumentException, and they usually represent a bug in the code — you forgot a null check, you passed an invalid argument. The compiler deliberately stays out of your way there, because if it forced you to catch a NullPointerException on every line, your code would be unreadable, and the right fix for a bug isn't to catch it everywhere, it's to correct the logic. The two misconceptions I'd clear up are, first, that this is about timing — people hear runtime and think it's about when the exception happens, but every exception happens at runtime; checked just means it's checked at compile time. And second, that unchecked means less serious — that's not it either; a NullPointerException can absolutely crash your app. It's about intent and compiler enforcement: checked is a recoverable external condition you should plan for, unchecked is a programming error you should fix."}
  ],
  "hook": "There's exactly one class in the hierarchy that decides whether an exception is checked or unchecked. Name it and the whole distinction clicks."
},

"checked-vs-unchecked-002": {
  "layout_type": "concept-explainer",
  "direct_answer": "The most common checked exceptions are **`IOException`** (and subtypes like `FileNotFoundException`), **`SQLException`** (JDBC), **`InterruptedException`** (thread interruption), **`ClassNotFoundException`** (reflection/class loading), and **`ParseException`**. They all extend `Exception` (not `RuntimeException`), so the compiler forces you to `catch` or declare them — they model recoverable external/environmental failures.",
  "interviewer_intent": {
    "testing": "Whether you can name real checked exceptions and recognise the pattern (external/environmental failures)",
    "common_mistake": "Listing RuntimeExceptions (like NullPointerException) as checked, or not knowing InterruptedException is checked",
    "to_stand_out": "Point out the common thread: they all represent things outside your code's control where recovery is plausible"
  },
  "sections": [
    {"type":"overview","title":"The Checked Exceptions You'll Actually Meet","content":"Checked exceptions are direct subclasses of `Exception` that don't extend `RuntimeException`, so the compiler forces you to handle or declare them. The ones you'll meet most often: **`IOException`** (file/stream/network I/O, with subtypes like `FileNotFoundException`), **`SQLException`** (every JDBC call), **`InterruptedException`** (a blocked thread was interrupted), **`ClassNotFoundException`** (reflection / dynamic class loading), and **`ParseException`** (e.g., `SimpleDateFormat.parse`).\n\nThe common thread is that they all represent **external or environmental conditions** outside your code's direct control — exactly the situations where recovering (retry, fall back, inform the user) is plausible, which is why the compiler insists you plan for them."},
    {"type":"concept_map","title":"Common Checked Exceptions","content":"blue|IOException|~File / stream / network|FileNotFoundException|EOFException|retry or report\nemerald|SQLException|~Every JDBC call|connection / query failure|carries SQL error code|rollback / retry\namber|InterruptedException|~Blocked thread interrupted|sleep / wait / join|restore interrupt flag|stop gracefully\nviolet|ClassNotFoundException|~Reflection / loading|Class.forName|missing class on path|fail clearly\nrose|ParseException|~Text parsing|SimpleDateFormat.parse|bad input format|prompt for valid input"},
    {"type":"code_example","title":"Each One in Context","content":"```java\n// IOException (checked) -- reading a file\nString cfg = Files.readString(Path.of(\"app.conf\"));        // throws IOException\n\n// SQLException (checked) -- JDBC\ntry (Connection c = ds.getConnection()) { /* ... */ }       // throws SQLException\n\n// InterruptedException (checked) -- blocking call\ntry {\n    Thread.sleep(1000);\n} catch (InterruptedException e) {\n    Thread.currentThread().interrupt();   // restore the interrupt flag\n}\n\n// ClassNotFoundException (checked) -- reflection\nClass<?> c = Class.forName(\"com.example.Plugin\");           // throws ClassNotFoundException\n```\n*All five are checked because they model conditions outside your code where recovery makes sense.*"},
    {"type":"common_mistakes","title":"Common Mistakes","content":"- **Calling `NullPointerException`/`IllegalArgumentException` checked** — they're unchecked (`RuntimeException`).\n- **Not knowing `InterruptedException` is checked** — and forgetting to restore the interrupt flag.\n- **Swallowing `SQLException`** with an empty catch instead of rolling back/logging.\n- **Assuming all I/O is one type** — `FileNotFoundException` is a specific `IOException` subtype."},
    {"type":"key_points","title":"Key Points","content":"- **`IOException`, `SQLException`, `InterruptedException`, `ClassNotFoundException`, `ParseException`** are the common checked ones.\n- **They extend `Exception`, not `RuntimeException`** — compiler forces `catch`/`throws`.\n- **Common thread: external/environmental failures** where recovery is plausible.\n- **`FileNotFoundException` is a subtype of `IOException`.**\n- **`InterruptedException` requires restoring the interrupt flag** when caught."},
    {"type":"speakable_answer","title":"How to Answer This Verbally","content":"The checked exceptions you run into most often are I O exception, S Q L exception, InterruptedException, ClassNotFoundException, and ParseException. I O exception covers anything to do with files, streams, or network I O, and it has more specific subtypes like FileNotFoundException. S Q L exception comes up on basically every J D B C call — opening a connection, running a query — and it carries the database's error code. InterruptedException is the one people forget is checked; it's thrown when a thread that's blocked, say in sleep or wait or join, gets interrupted, and the important detail is that when you catch it you should usually restore the interrupt flag by calling Thread dot current thread dot interrupt. ClassNotFoundException shows up with reflection and dynamic class loading, like Class dot forName when the class isn't on the classpath. And ParseException comes from text parsing, the classic being SimpleDateFormat dot parse on a badly formatted string. The thing that ties all of these together — and this is the insight that makes a good answer — is that they all represent external or environmental conditions that are outside your code's direct control. A file might not exist, a database might be down, a thread might be interrupted, a class might be missing, the input might be malformed. Those are exactly the situations where it's reasonable to try to recover — retry, fall back, or tell the user — which is why the language designers made them checked and forced you to plan for them. The mistake to avoid is listing things like NullPointerException or IllegalArgumentException as checked. Those are RuntimeException subclasses, so they're unchecked, because they signal bugs rather than recoverable external failures."}
  ],
  "hook": "Quick-fire: name five checked exceptions. If InterruptedException isn't on your list, you've probably been swallowing it wrong."
},

"checked-vs-unchecked-004": {
  "layout_type": "concept-explainer",
  "direct_answer": "The code **won't compile**. For a checked exception, the compiler enforces 'catch-or-declare': you must either wrap the call in a `try`/`catch` or add `throws` to the method signature. Skip both and you get a compile error like *'unreported exception IOException; must be caught or declared to be thrown'*. Unchecked exceptions have no such requirement.",
  "interviewer_intent": {
    "testing": "Whether you know the catch-or-declare rule is enforced at compile time, not runtime",
    "common_mistake": "Thinking an unhandled checked exception is a runtime error rather than a compile-time failure",
    "to_stand_out": "Name the exact compiler message and contrast with unchecked, which compiles without any handling"
  },
  "sections": [
    {"type":"overview","title":"Catch-or-Declare Is a Compile-Time Rule","content":"If a method calls something that throws a **checked** exception, the compiler enforces the **catch-or-declare** rule: you must either handle it in a `try`/`catch`, or propagate it by adding `throws` to your method's signature. Do neither and the program **fails to compile** — you never reach runtime. The message reads something like *'unreported exception IOException; must be caught or declared to be thrown.'*\n\nThis is the entire practical difference of 'checked': the compiler refuses to let you silently ignore it. **Unchecked** exceptions are exempt — code that might throw a `NullPointerException` compiles with no handling at all."},
    {"type":"concept_map","title":"Catch-or-Declare","content":"rose|Do nothing|~Compile error|'unreported exception'|build fails|never runs\nblue|Catch it|~try / catch|handle here|recover or log|compiles\nemerald|Declare it|~add throws|propagate to caller|caller must then handle|compiles\namber|Unchecked|~No requirement|RuntimeException|compiles with nothing|no enforcement"},
    {"type":"before_code","title":"Wrong: checked exception ignored -> won't compile","content":"```java\nvoid load() {\n    // IOException is checked and neither caught nor declared\n    String s = Files.readString(Path.of(\"a.txt\"));\n}\n// error: unreported exception java.io.IOException;\n//        must be caught or declared to be thrown\n```"},
    {"type":"after_code","title":"Right: catch it, or declare it","content":"```java\n// Option A -- handle it here\nvoid load() {\n    try {\n        String s = Files.readString(Path.of(\"a.txt\"));\n    } catch (IOException e) {\n        log.error(\"could not read\", e);\n    }\n}\n\n// Option B -- declare it and let the caller handle it\nvoid load() throws IOException {\n    String s = Files.readString(Path.of(\"a.txt\"));\n}\n```\n*Either option satisfies catch-or-declare; doing neither is a compile error.*"},
    {"type":"common_mistakes","title":"Common Mistakes","content":"- **Expecting a runtime error** — it's a *compile-time* failure; the program won't build.\n- **Adding an empty `catch`** just to compile — that silently swallows real failures.\n- **Declaring `throws Exception`** to make it go away — declare the specific type instead.\n- **Confusing it with unchecked** — `RuntimeException` code compiles with zero handling."},
    {"type":"key_points","title":"Key Points","content":"- **Unhandled checked exception = compile error**, not a runtime error.\n- **Catch-or-declare**: wrap in `try`/`catch` *or* add `throws`.\n- **Compiler message**: 'unreported exception … must be caught or declared'.\n- **Unchecked exceptions are exempt** — no handling required to compile.\n- **Don't 'fix' it with an empty catch** — handle or propagate meaningfully."},
    {"type":"speakable_answer","title":"How to Answer This Verbally","content":"If you don't handle a checked exception, the code simply won't compile — and that's the key point, it's a compile-time failure, not something that blows up at runtime. The compiler enforces what's called the catch-or-declare rule. Whenever you call something that can throw a checked exception, like reading a file which throws I O exception, you have two choices. Either you catch it, by wrapping the call in a try-catch block and handling it there, or you declare it, by adding throws I O exception to your method's signature so that the responsibility passes up to whoever calls your method. If you do neither, the build fails with a message along the lines of unreported exception I O exception, must be caught or declared to be thrown. So you literally can't run the program until you deal with it one way or another. That compile-time enforcement is the entire practical meaning of the word checked — the compiler won't let you silently ignore it. Compare that to unchecked exceptions, the RuntimeException subclasses: code that might throw a NullPointerException compiles perfectly fine with no handling at all, because the compiler doesn't enforce anything for those. A couple of mistakes I'd warn against: don't just slap an empty catch block on it to make the compiler happy, because that swallows real failures silently. And don't reach for throws Exception to make it go away — declare the specific exception type instead, so the contract stays honest and callers know exactly what they're dealing with."}
  ],
  "hook": "Forget to handle a checked exception and your code never even runs. Why? Because the failure happens somewhere most freshers don't expect."
},

"checked-vs-unchecked-003": {
  "layout_type": "concept-explainer",
  "direct_answer": "Frameworks like **Spring** favor unchecked exceptions because checked exceptions force `throws`/`try`-`catch` boilerplate up every layer, leak low-level details into high-level code, and break clean APIs (especially lambdas/streams, which can't throw checked exceptions). Spring wraps JDBC's checked `SQLException` into its unchecked `DataAccessException` hierarchy so your service code stays clean and decoupled.",
  "interviewer_intent": {
    "testing": "Whether you understand the practical downsides of checked exceptions that pushed frameworks toward unchecked",
    "common_mistake": "Thinking unchecked is just 'lazier', missing that checked exceptions don't compose with lambdas/streams and leak across layers",
    "to_stand_out": "Mention Spring's DataAccessException translating SQLException, and that functional interfaces can't propagate checked exceptions"
  },
  "sections": [
    {"type":"overview","title":"Why Modern Frameworks Lean Unchecked","content":"Checked exceptions sound disciplined, but at scale they cause real friction, which is why **Spring** and most modern frameworks prefer unchecked. Three reasons: (1) **boilerplate** — a checked exception must be declared or caught at *every* layer it passes through, cluttering signatures; (2) **leaky abstraction** — `throws SQLException` forces your high-level service to know about JDBC; (3) **they don't compose** — lambdas and Stream operations use functional interfaces that **can't throw checked exceptions**, so checked exceptions break functional-style code.\n\nSpring's answer is **exception translation**: it catches JDBC's checked `SQLException` and rethrows it as an **unchecked `DataAccessException`**. Your repository and service code stays clean, decoupled, and lambda-friendly."},
    {"type":"concept_map","title":"Why Unchecked Wins in Frameworks","content":"rose|Boilerplate|~throws up every layer|signatures clutter|propagation noise|hard to refactor\namber|Leaky abstraction|~throws SQLException|service knows JDBC|layers coupled|breaks encapsulation\nblue|No lambda support|~Functional interfaces|Runnable/Function can't|throw checked exceptions|streams break\nemerald|Spring's fix|~DataAccessException|unchecked wrapper|translates SQLException|clean service code"},
    {"type":"before_code","title":"Without unchecked: checked exceptions clutter every layer","content":"```java\n// Each layer must declare or catch the checked SQLException\nUser findUser(Long id) throws SQLException { return repo.find(id); }      // repo\nUser getUser(Long id)  throws SQLException { return findUser(id); }       // service\n// And this won't even compile -- lambdas can't throw checked exceptions:\nids.forEach(id -> repo.find(id));   // error: unhandled SQLException\n```"},
    {"type":"after_code","title":"With Spring's unchecked DataAccessException","content":"```java\n// Spring translates SQLException -> unchecked DataAccessException\nUser findUser(Long id) { return jdbcTemplate.queryForObject(SQL, mapper, id); }\nUser getUser(Long id)  { return findUser(id); }   // no throws clause anywhere\n\n// Lambdas/streams compose cleanly now:\nids.forEach(id -> findUser(id));   // compiles -- no checked exception\n```\n*Unchecked exceptions keep signatures clean, decouple layers, and work inside lambdas and streams.*"},
    {"type":"common_mistakes","title":"Common Mistakes","content":"- **Thinking unchecked is just 'lazy'** — it's a deliberate API design choice for composability.\n- **Forgetting lambdas can't throw checked exceptions** — the real reason streams force unchecked.\n- **Letting `SQLException` leak into services** instead of translating at the repository boundary.\n- **Over-correcting** — genuinely recoverable, caller-actionable failures can still be checked."},
    {"type":"key_points","title":"Key Points","content":"- **Checked exceptions add `throws`/`catch` boilerplate** at every layer.\n- **They leak low-level details** (`SQLException`) into high-level code.\n- **Lambdas/streams can't throw checked exceptions** — checked breaks functional style.\n- **Spring translates `SQLException` → unchecked `DataAccessException`.**\n- **Result: clean signatures, decoupled layers, lambda-friendly code.**"},
    {"type":"speakable_answer","title":"How to Answer This Verbally","content":"Frameworks like Spring lean heavily toward unchecked exceptions, and there are three practical reasons that all show up once a codebase gets big. The first is boilerplate. A checked exception has to be either caught or declared at every single layer it passes through. So if your data layer throws S Q L exception, your repository declares throws, your service declares throws, and it ripples all the way up, cluttering signatures with propagation noise. The second is leaky abstraction. The moment your service method says throws S Q L exception, your high-level business code is now coupled to the fact that you're using J D B C underneath. That breaks encapsulation — the upper layers shouldn't have to know or care about the persistence technology. The third reason, and this is the one that really forced the issue in modern Java, is that checked exceptions don't compose with lambdas and streams. Functional interfaces like Runnable, Function, and the ones the Stream API uses are not declared to throw checked exceptions, so if you try to call a method that throws a checked exception inside a lambda, it won't compile. That makes checked exceptions actively painful in functional-style code. Spring's solution is exception translation. It catches J D B C's checked S Q L exception and rethrows it as an unchecked DataAccessException, which sits in its own hierarchy. So your repository and service code has no throws clauses, stays decoupled from J D B C, and works cleanly inside lambdas and streams. The nuance I'd add is that this doesn't mean checked exceptions are always wrong — for a genuinely recoverable failure where the caller can take a specific action, checked can still be the right call. But for framework-level plumbing, unchecked keeps everything clean."}
  ],
  "hook": "Spring buried checked exceptions on purpose — and one of the reasons is that they literally don't fit inside a lambda. Here's the full story."
},

"checked-vs-unchecked-005": {
  "layout_type": "concept-explainer",
  "direct_answer": "Catch the checked exception and `throw` a `RuntimeException` (or a custom unchecked subclass) that **takes the original as its cause**: `catch (IOException e) { throw new UncheckedIOException(e); }`. The cause keeps the full stack trace. The JDK even provides `UncheckedIOException` for exactly this. Wrapping removes the `throws` burden from callers while preserving the root error.",
  "interviewer_intent": {
    "testing": "Whether you can convert a checked exception to unchecked correctly, preserving the cause",
    "common_mistake": "Wrapping without passing the cause, or swallowing the checked exception instead of wrapping it",
    "to_stand_out": "Mention UncheckedIOException as the built-in example and that this is the standard technique for lambdas/streams"
  },
  "sections": [
    {"type":"overview","title":"Wrap the Checked Exception in an Unchecked One","content":"To turn a checked exception into an unchecked one, you `catch` it and `throw` a `RuntimeException` (or a custom subclass) that carries the original as its **cause**. The constructor form `new RuntimeException(message, e)` preserves the entire chain so `getCause()` and the stack trace still point at the real failure.\n\nThe JDK ships a purpose-built example: **`UncheckedIOException`**, which wraps an `IOException`. This technique is the standard way to call checked-throwing methods inside **lambdas and streams**, which can't propagate checked exceptions. The cardinal rule: **always pass the cause** — wrapping without it discards the root stack trace."},
    {"type":"concept_map","title":"Wrapping Checked -> Unchecked","content":"emerald|The pattern|~catch then throw unchecked|new RuntimeException(msg, e)|cause preserved|callers freed of throws\nblue|Built-in helper|~UncheckedIOException|wraps IOException|standard JDK type|getCause() returns it\namber|Use case|~Inside lambdas/streams|can't throw checked|wrap to propagate|functional-friendly\nrose|The rule|~Always pass the cause|new X(msg) -> trace lost|new X(msg, e) -> kept|never swallow"},
    {"type":"before_code","title":"Wrong: swallow, or wrap without the cause","content":"```java\ntry {\n    return Files.readString(path);\n} catch (IOException e) {\n    // (a) swallowed -> caller has no idea it failed\n    return \"\";\n    // (b) or: throw new RuntimeException(\"read failed\");  // cause dropped!\n}\n```"},
    {"type":"after_code","title":"Right: wrap with the cause (or use UncheckedIOException)","content":"```java\n// Custom wrap, cause preserved\nString read(Path path) {\n    try {\n        return Files.readString(path);\n    } catch (IOException e) {\n        throw new UncheckedIOException(e);   // JDK type, keeps the cause\n    }\n}\n\n// Now it composes inside a stream (which can't throw checked exceptions):\npaths.stream().map(this::read).forEach(System.out::println);\n```\n*`UncheckedIOException` carries the original `IOException`; `getCause()` returns it for debugging.*"},
    {"type":"common_mistakes","title":"Common Mistakes","content":"- **Wrapping without the cause** — `new RuntimeException(msg)` loses the root stack trace.\n- **Swallowing instead of wrapping** — returning a default hides the failure entirely.\n- **Reinventing `UncheckedIOException`** — the JDK already provides it for `IOException`.\n- **Wrapping everything reflexively** — only wrap when removing the checked burden genuinely helps."},
    {"type":"key_points","title":"Key Points","content":"- **Catch the checked exception, throw an unchecked one carrying the cause.**\n- **`new RuntimeException(msg, e)`** — always pass `e` as the cause.\n- **`UncheckedIOException`** is the JDK's built-in wrapper for `IOException`.\n- **Essential for lambdas/streams**, which can't propagate checked exceptions.\n- **Never swallow** the checked exception in the process."},
    {"type":"speakable_answer","title":"How to Answer This Verbally","content":"The way you wrap a checked exception in an unchecked one is to catch the checked exception and then throw a RuntimeException, or a custom subclass of it, passing the original exception in as the cause. So the pattern is catch I O exception e, then throw new RuntimeException, with the message and e as the second argument. That second argument is the crucial part, because it preserves the cause, which means the full stack trace and the real root failure are still available through getCause. The JDK actually gives you a ready-made example of this called UncheckedIOException, whose entire purpose is to wrap a checked I O exception as an unchecked one. So in a lot of code you'll just catch the I O exception and throw a new UncheckedIOException with the original. Why would you do this at all? The biggest reason in modern Java is lambdas and streams. The functional interfaces used by the Stream API can't throw checked exceptions, so if you want to call something like Files dot readString inside a map operation, you have to wrap the checked I O exception into an unchecked one, otherwise it won't compile. Wrapping lets the exception propagate cleanly out of the stream. The one rule you absolutely must not break is to always pass the cause. If you write throw new RuntimeException with just a message and forget the original exception, you've thrown away the stack trace and made the failure undebuggable. And you should never just swallow the checked exception by returning a default value, because then the failure disappears silently. So: catch, wrap in an unchecked exception, always chain the cause, and lean on UncheckedIOException when it fits."}
  ],
  "hook": "You need to call a file-reading method inside a stream, but it throws a checked exception. There's a one-line idiom for that — and a one-word mistake that ruins it."
},

"checked-vs-unchecked-006": {
  "layout_type": "concept-explainer",
  "direct_answer": "**`InterruptedException`** is checked because interruption is a **cooperative, recoverable** signal that a blocked thread (`sleep`, `wait`, `join`, blocking queue ops) *should stop what it's doing*. Making it checked forces you to consciously decide how to respond. The correct response is to **restore the interrupt flag** (`Thread.currentThread().interrupt()`) and exit gracefully — not to swallow it.",
  "interviewer_intent": {
    "testing": "Whether you understand interruption as a cooperative cancellation signal and the correct handling pattern",
    "common_mistake": "Swallowing InterruptedException with an empty catch, destroying the interrupt status",
    "to_stand_out": "Explain that catching it clears the interrupt flag, so you must restore it (or rethrow) so callers can also react"
  },
  "sections": [
    {"type":"overview","title":"Interruption Is a Request, Not a Kill","content":"**`InterruptedException`** is thrown by blocking calls — `Thread.sleep()`, `Object.wait()`, `Thread.join()`, blocking queue operations — when another thread **interrupts** the blocked one. Interruption in Java is **cooperative**: it's a polite *request* to stop, not a forced kill. The thread gets to decide how to respond.\n\nIt's **checked** precisely to force that decision — the compiler makes you write a handler rather than ignore a cancellation signal. The catch is subtle: when the exception is thrown, the JVM **clears** the thread's interrupt flag. So the correct handling is to **restore it** with `Thread.currentThread().interrupt()` (or rethrow `InterruptedException`) so code higher up can also notice the interruption and shut down cleanly."},
    {"type":"concept_map","title":"Handling InterruptedException","content":"blue|Cooperative signal|~A request to stop|sleep/wait/join throw it|thread decides|not a forced kill\namber|Why checked|~Forces a decision|compiler-enforced handler|can't ignore cancellation|recoverable by design\nrose|Catch clears the flag|~Interrupt status reset|swallowing hides it|callers never know|the classic bug\nemerald|Correct response|~Restore or rethrow|Thread.currentThread().interrupt()|then exit gracefully|propagate the signal"},
    {"type":"before_code","title":"Wrong: swallowing it destroys the interrupt signal","content":"```java\nwhile (running) {\n    try {\n        Thread.sleep(1000);\n    } catch (InterruptedException e) {\n        // empty catch: interrupt flag already cleared, signal lost,\n        // loop keeps running -- the thread can never be cancelled\n    }\n}\n```"},
    {"type":"after_code","title":"Right: restore the flag and stop gracefully","content":"```java\nwhile (running) {\n    try {\n        Thread.sleep(1000);\n    } catch (InterruptedException e) {\n        Thread.currentThread().interrupt();  // restore the cleared flag\n        break;                               // exit the loop cleanly\n    }\n}\n// Higher-level code checking isInterrupted() can now also react.\n```\n*Catching clears the interrupt flag — restore it so the cancellation signal isn't lost.*"},
    {"type":"common_mistakes","title":"Common Mistakes","content":"- **Empty catch** — swallows the signal and clears the flag; the thread becomes uncancellable.\n- **Forgetting the flag is cleared** when `InterruptedException` is thrown.\n- **Not restoring or rethrowing** — callers can't tell the thread was interrupted.\n- **Treating interruption as a fatal error** — it's a cooperative shutdown request."},
    {"type":"key_points","title":"Key Points","content":"- **Interruption is cooperative** — a request to stop, not a forced kill.\n- **It's checked to force a conscious decision** on cancellation.\n- **Catching `InterruptedException` clears the interrupt flag.**\n- **Restore it** with `Thread.currentThread().interrupt()` or rethrow.\n- **Never swallow it** with an empty catch — the thread becomes uncancellable."},
    {"type":"speakable_answer","title":"How to Answer This Verbally","content":"InterruptedException is checked because interruption in Java is a cooperative, recoverable signal, and the language designers wanted to force you to consciously decide how to respond to it. It's thrown by blocking calls — Thread dot sleep, Object dot wait, join, blocking queue operations — when another thread interrupts the thread that's blocked. The key mental model is that interruption is a polite request to stop, not a forced kill. Java deliberately has no safe way to forcibly terminate a thread, so instead it sends this cooperative signal, and the receiving thread gets to decide how and when to wind down. Making the exception checked means the compiler won't let you just ignore that cancellation request — you have to write a handler. Now here's the subtle part that interviewers love. When InterruptedException is thrown, the JVM clears the thread's interrupt flag as a side effect. So if you catch it and do nothing — an empty catch block — you've not only swallowed the exception, you've also wiped out the interrupt status, which means any code higher up that checks isInterrupted will think everything is fine, and the thread effectively can't be cancelled anymore. That's the classic bug. The correct handling is one of two things: either rethrow the InterruptedException so it propagates, or, if you're catching it, immediately restore the flag by calling Thread dot current thread dot interrupt, and then break out and let the thread shut down gracefully. That way the cancellation signal isn't lost and the rest of the system can react to it. So the short version is: it's checked to force a decision, catching it clears the flag, and you must restore the flag or rethrow rather than swallow it."}
  ],
  "hook": "Catch InterruptedException with an empty block and you've just made your thread impossible to cancel. Here's why that one line matters so much."
}
}

apply(F, UPDATES)

{
  "topic": "Arrays Basics",
  "topicSlug": "arrays-basics",
  "questions": [
    {
      "id": "arrays-basics-001",
      "slug": "array-declaration-and-initialization-in-java",
      "question": "How do you declare and initialize an array in Java, and what are the default values?",
      "title": "Array Declaration and Initialization in Java",
      "direct_answer": "Declare an array with `Type[] name`. Create it with `new Type[length]`, or supply values in an initializer such as `int[] scores = {90, 80}`. A newly created array is fixed in length and its elements receive defaults: numeric zero, `false`, the null character for `char`, and `null` for reference elements. `array.length` is a field.",
      "layout_type": "concept-explainer",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 5,
      "last_updated": "2026-06-07",
      "interviewer_intent": {
        "testing": "Whether you know the three declaration styles, understand how Java allocates array memory on the heap, and know that arrays have fixed size and auto-filled default values.",
        "common_mistake": "Confusing the declaration `int[] arr` (no memory yet) with initialization `new int[5]` (memory allocated). Also assuming elements are random garbage values — Java always zero-fills.",
        "to_stand_out": "Mention that the array reference lives on the stack while the actual block of elements lives on the heap — and that `arr.length` is a field, not a method, so no parentheses."
      },
      "company_tags": [
        "tcs",
        "infosys",
        "wipro",
        "cognizant",
        "accenture"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "The One Line That Secretly Does Two Different Things",
            "content": "An **array** stores a fixed number of values of one component type. `int[] scores;` declares a variable that can refer to an integer array; it does not create an array. `scores = new int[5];` creates five elements, and `int[] scores = {90, 80};` creates and fills an array in one expression.\n\nJava initializes every element of a newly created array. Numeric elements start at zero, `boolean` at `false`, `char` at `\\u0000`, and reference elements at `null`. The length cannot change after creation. Valid indexes run from `0` through `length - 1`; an invalid index compiles but throws `ArrayIndexOutOfBoundsException` when executed.\n\nThe language specifies reference behavior, initialization, and bounds checking. It does not require a local reference to be stored in a particular stack slot or expose a numeric memory address, so those details are not needed to understand arrays."
          },
          {
            "type": "flow_diagram",
            "title": "What Happens in Memory When You Write `int[] scores = new int[5]`",
            "content": "```mermaid\nflowchart LR\n    D[\"int[] scores\"] -->|\"can hold an array reference\"| N[\"null before assignment\"]\n    C[\"new int[5]\"] --> A[\"array with indexes 0..4\"]\n    A --> Z[\"initial values: 0, 0, 0, 0, 0\"]\n    D -->|\"after assignment\"| A\n```\n\nThe useful model is a variable referring to one array object; Java does not expose the object's numeric address."
          },
          {
            "type": "code_example",
            "title": "Three Ways to Declare and Initialize — Plus Default Values in Action",
            "content": "Each declaration style fits a different situation, and the snippet below also proves the two facts interviewers probe: Java zero-fills automatically, and `length` is a field. Run it as a plain Java 17 console app.\n\n```java\npublic class ArrayDemo {\n    public static void main(String[] args) {\n\n        // Style 1: declare size, fill later\n        int[] scores = new int[5];\n        System.out.println(scores[0]); // 0  — Java zero-fills automatically\n\n        scores[0] = 95;\n        scores[1] = 72;\n        // scores[2], [3], [4] remain 0\n\n        // Style 2: declare and initialize with values in one line\n        String[] days = {\"Mon\", \"Tue\", \"Wed\", \"Thu\", \"Fri\"};\n        System.out.println(days[0]); // Mon\n\n        // Style 3: new keyword with explicit values (useful when size is computed)\n        double[] prices = new double[]{9.99, 14.50, 3.75};\n\n        // Length is a field — no parentheses\n        System.out.println(scores.length); // 5\n        // System.out.println(scores.length()); // COMPILE ERROR — common mistake\n\n        // Default for object arrays is null, not empty string\n        String[] names = new String[3];\n        System.out.println(names[0]); // null\n    }\n}\n```\n\nNotice the two traps in one place: `scores.length()` would refuse to compile, and `names[0]` is `null` rather than an empty string — object arrays default to `null`, not to a \"blank\" value."
          },
          {
            "type": "comparison_table",
            "title": "Default Values by Element Type",
            "content": "| Array element type | Default value | Example declaration |\n|---|---|---|\n| `byte`, `short`, `int`, `long` | `0` | `int[] arr = new int[5]` |\n| `float`, `double` | `0.0` | `double[] arr = new double[3]` |\n| `boolean` | `false` | `boolean[] flags = new boolean[4]` |\n| `char` | `'\\u0000'` (null char) | `char[] chars = new char[3]` |\n| Any object (`String`, `Object`, etc.) | `null` | `String[] names = new String[3]` |"
          },
          {
            "type": "when_to_use",
            "title": "When a Plain Array Is the Right Call",
            "content": "**Reach for a plain array** when the size is known and fixed the moment you create it — a 7-day week, the three RGB channels of a pixel, a chessboard's `8x8` grid. The fixed size is a feature here: it documents your intent, and Java provides constant-time indexed access; the language does not require one exposed physical-memory layout.\n\n**Which initialization style you pick depends on what you know:**\n\n- Use the literal `{1, 2, 3}` when you already have the values in hand\n- Use `new int[n]` when you know only the count and will fill the slots in a loop\n- Prefer `new type[]{...}` when you want to build a fresh array inline and pass it straight into a method call\n\nThe moment the count is unknown or changes as the program runs — reading lines from a file, collecting user clicks — **stop fighting the fixed size and switch to `ArrayList`**. Manually copying into a bigger array every time you outgrow the old one is exactly the work `ArrayList` was created to do for you."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Writing `arr.length()`** — `length` is a field on an array, not a method, so the parentheses are a compile error. Only `String` uses `length()`; arrays use `arr.length`.\n- **Expecting garbage values** — beginners coming from C sometimes add code to \"clear\" a new array first. Java already zero-fills every slot (`0`, `false`, `null`), so that work is wasted.\n- **Indexing an array you only declared** — a field left as `int[] arr;` is `null`, and `arr[0] = 1` throws `NullPointerException`, not an empty-array error. You must assign with `new` (or a literal) before indexing.\n- **Reading index `length`** — a 5-element array's last valid index is `4`; `arr[arr.length]` always throws `ArrayIndexOutOfBoundsException` because indices run `0` to `length - 1`."
          },
          {
            "type": "key_points",
            "title": "Quick revision",
            "content": "- **Declare**: `int[] values;` creates a reference variable, not an array.\n- **Create**: `new int[5]` makes a fixed-length array with five elements.\n- **Initialize directly**: `int[] values = {1, 2, 3};`.\n- **Defaults**: numeric zero, `false`, `\\u0000`, or `null` depending on the component type.\n- **Bounds**: indexes are `0` to `length - 1`; an invalid index fails at runtime."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I declare an array with the component type and brackets, such as `int[] scores`, and create it with `new int[5]` or an initializer like `{90, 80}`. The array length is fixed once it is created, and I read it through the `length` field.\n\nJava gives new elements default values: zero for numeric types, false for boolean, the null character for char, and null for references. Valid indexes are zero through `length - 1`. Accessing index `length` is not a compile-time error; it throws `ArrayIndexOutOfBoundsException` at runtime.",
            "answerSize": "compact"
          }
        ]
      },
      "followup_questions": [
        "What is ArrayIndexOutOfBoundsException and how do you prevent it?",
        "What is the difference between an array and an ArrayList in Java?",
        "How does Java store arrays in memory — stack vs heap?"
      ],
      "seo": {
        "metaTitle": "Array Declaration and Initialization in Java — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How do you declare and initialize an array in Java, and what are the default values? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1,
      "speakable_v2": {
        "archetype": "B",
        "pillar": "P01",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "simple Java 17 console app with a main method",
        "familiarity_anchors": [
          "array",
          "int[]",
          "index"
        ],
        "hook": "Writing `int[] x = new int[5]` looks like one step. It's really two — a stack reference, then a heap block, joined by the equals sign.",
        "beats": [
          {
            "kind": "what_each_is",
            "layout": "grouped_paragraphs",
            "groups": [
              {
                "heading": "Declaration",
                "text": "`int[] scores` makes a **reference variable** on the stack. Right then, it's pointing at nothing."
              },
              {
                "heading": "Initialization",
                "text": "`new int[5]` reserves five slots on the **heap** and hands back their address. The equals sign stores it."
              }
            ]
          },
          {
            "kind": "differences",
            "layout": "mini_table",
            "columns": [
              "Declaration",
              "Initialization"
            ],
            "rows": [
              {
                "axis": "What it makes",
                "values": [
                  "a reference variable",
                  "the real array block"
                ]
              },
              {
                "axis": "Where it lives",
                "values": [
                  "a slot on the stack",
                  "a block on the heap"
                ]
              },
              {
                "axis": "State right after",
                "values": [
                  "points at null",
                  "five zero-filled slots"
                ]
              }
            ]
          },
          {
            "kind": "when_to_pick",
            "layout": "bullets",
            "items": [
              "Use `new int[5]` when you'll fill the slots later in a loop.",
              "Use the literal `{1, 2, 3}` when you've already got the values.",
              "Use `new int[]{...}` when you're building one inline for a method call."
            ]
          },
          {
            "kind": "tiny_example",
            "layout": "paragraph",
            "text": "In a Java 17 console app, `int[] scores = new int[5]` gives you five zeros. Then `scores[0] = 95` sets the first. Here's the **trap** beginners miss — a fresh `String[]` holds nulls, never garbage."
          }
        ],
        "cap": "So declaration's a stack reference, and initialization's the heap block. The size locks the moment you build it.",
        "followup_handoff": [
          "What is the difference between an array and an ArrayList in Java?",
          "How does Java store arrays in memory — stack vs heap?"
        ],
        "tts_overrides": {
          "int[] x = new int[5]": "int array x equals new int array of size five",
          "int[] scores = new int[5]": "int array scores equals new int array of size five",
          "scores[0] = 95": "scores at zero equals ninety five",
          "new int[]{...}": "new int array with values inline",
          "int[] scores": "int array scores",
          "new int[5]": "new int array of size five",
          "{1, 2, 3}": "one, two, three",
          "String[]": "string array"
        },
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "arrays-basics-005",
      "slug": "array-vs-arraylist-in-java",
      "question": "What is the difference between an array and an ArrayList in Java?",
      "title": "Array vs ArrayList in Java",
      "direct_answer": "An array has a fixed length and can store either primitives or references. `ArrayList<E>` is a resizable `List` backed by an array and stores reference types, so primitive values use wrappers such as `Integer`. Arrays fit fixed-size or primitive data; `ArrayList` fits a collection whose size changes and needs `List` operations. Choose from the required behavior, not a blanket performance rule.",
      "layout_type": "concept-explainer",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 5,
      "last_updated": "2026-06-07",
      "interviewer_intent": {
        "testing": "Whether you understand the fixed-size constraint of arrays vs dynamic resizing of ArrayList, and know when to choose one over the other.",
        "common_mistake": "Saying ArrayList is always better — arrays are faster for indexed access and are the right tool when the size is known upfront (e.g. a 12-month calendar, a 3-channel RGB value).",
        "to_stand_out": "Mention that ArrayList internally uses an array and doubles its capacity when full — so it is not magic, just an array with automatic resize logic wrapped around it."
      },
      "company_tags": [
        "tcs",
        "infosys",
        "wipro",
        "cognizant",
        "amazon",
        "flipkart"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "The Real Question Isn't Which Is Better — It's Whether the Size Changes",
            "content": "An array and an `ArrayList` both hold a sequence of values that you reach by position. **The one difference that drives everything else** is simple: an array's size is fixed the moment you create it, while an `ArrayList` grows and shrinks as you go.\n\nWhy does `ArrayList` exist at all? Because **real programs often don't know up front how many items they'll have**. An array fixes its size at birth — `int[] scores = new int[5]` reserves exactly five slots, permanently. Need a sixth? You'd have to allocate a bigger array and copy everything across by hand. There is no `add()`.\n\n`ArrayList` takes that chore off your hands. `ArrayList<Integer> scores = new ArrayList<>()` starts with room for a few elements and **grows itself** when you exceed that. The mechanism is not magic: internally it holds a plain array, and when that array fills, it allocates a new one about **1.5× larger** (the formula is `oldCapacity + oldCapacity >> 1`), copies the elements over, and swaps it in. You just call `add()` and never see it happen.\n\nThat convenience costs two things, and interviewers listen for them:\n\n- `ArrayList` stores **objects only**, so an `int` becomes a boxed `Integer` with a little memory and speed overhead — you cannot write `ArrayList<int>`\n- you read elements with `get(i)` instead of raw `[]`, and ask its length with `size()` (a method) instead of `length` (a field)\n\nThe gotcha to avoid is the reflex answer that \"`ArrayList` is just better.\" It isn't — for a known, fixed count of primitives, like a 12-month total or an RGB triple, an array is smaller and faster. So the rule is concrete: **fixed count and primitives, use an array; a count that changes at runtime, use an `ArrayList`**."
          },
          {
            "type": "comparison_table",
            "title": "Array vs ArrayList — Side by Side",
            "content": "| Feature | Array | ArrayList |\n|---|---|---|\n| Size | Fixed at creation — cannot change | Dynamic — grows automatically |\n| Syntax | `int[] arr = new int[5]` | `ArrayList<Integer> list = new ArrayList<>()` |\n| Primitives | Supports `int`, `double`, etc. directly | Objects only — `int` must be `Integer` |\n| Access | `arr[0]` | `list.get(0)` |\n| Add element | Not possible (fixed) | `list.add(value)` |\n| Remove element | Not possible (fixed) | `list.remove(index)` |\n| Length / size | `arr.length` (field) | `list.size()` (method) |\n| Performance | Faster — direct memory block | Slightly slower — object + boxing overhead |\n| Use when | Size known upfront, primitives needed | Size changes at runtime, need add/remove |"
          },
          {
            "type": "code_example",
            "title": "Same Task — Array vs ArrayList",
            "content": "Watching the same job done both ways makes the trade-off tangible: the array hits a wall at its fixed size, while the `ArrayList` just keeps accepting elements. Run it as a Java 17 console app.\n\n```java\nimport java.util.ArrayList;\n\npublic class ArrayVsArrayList {\n    public static void main(String[] args) {\n\n        // ARRAY — fixed size, direct primitive storage\n        int[] topScores = new int[3];\n        topScores[0] = 95;\n        topScores[1] = 88;\n        topScores[2] = 76;\n        // topScores[3] = 70;  // compiles, but throws ArrayIndexOutOfBoundsException at runtime\n        System.out.println(topScores.length); // 3\n\n        // ARRAYLIST — dynamic, grows as needed\n        ArrayList<Integer> playerScores = new ArrayList<>();\n        playerScores.add(95);\n        playerScores.add(88);\n        playerScores.add(76);\n        playerScores.add(70);  // no problem — ArrayList resizes automatically\n        playerScores.remove(0); // removes the first element (95)\n        System.out.println(playerScores.size()); // 3\n        System.out.println(playerScores.get(0)); // 88\n    }\n}\n```\n\nThe array rejects the fourth element at runtime with an index exception; the `ArrayList` absorbs it and even lets you remove from the middle — that flexibility is the whole reason it exists, and primitive values such as `int` are boxed as `Integer` elements."
          },
          {
            "type": "flow_diagram",
            "title": "Which One to Choose?",
            "content": "A simple decision tree for picking between array and ArrayList:\n\n```mermaid\nflowchart TD\n    A[\"Need to store a sequence\"] --> B{\"Is the size fixed<br/>and known upfront?\"}\n    B -->|\"Yes\"| C{\"Need primitives<br/>int, double, etc.?\"}\n    C -->|\"Yes\"| D[\"Use array<br/>int[] arr = new int[n]\"]\n    C -->|\"No\"| E[\"Array or ArrayList<br/>both work\"]\n    B -->|\"No — size<br/>changes at runtime\"| F[\"Use ArrayList<br/>list.add() / list.remove()\"]\n```"
          },
          {
            "type": "when_to_use",
            "title": "Choosing Between Them in Practice",
            "content": "**Default to an array** when the size is genuinely fixed and known up front — a 12-month total, a fixed-size buffer, the `8x8` of a chessboard — or when you're storing large volumes of primitives and want to avoid boxing overhead. Arrays are also the natural choice in tight numeric loops where primitive storage and indexed access help a measured hot path.\n\n**Default to `ArrayList`** when the collection needs to grow or use the Collections API, especially when you don't know how many elements you'll end up with:\n\n- reading rows from a database\n- accumulating events\n- building a result whose length depends on the input\n\nThe convenience of `add()`, `remove()`, and `contains()` plus automatic growth often outweighs the extra bookkeeping in ordinary application code.\n\nA useful tell: if you find yourself manually tracking a `count` variable alongside an array and reallocating a bigger one whenever it fills, **you've reinvented `ArrayList` by hand** — just use it."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Mixing up `length` and `size()`** — arrays use the field `arr.length`; `ArrayList` uses the method `list.size()`. Calling `arr.size()` or `list.length` is a compile error and a classic exam trap.\n- **Trying `ArrayList<int>`** — generics only accept reference types, so it must be `ArrayList<Integer>`. Autoboxing converts the `int`, but storing millions of boxed `Integer`s costs far more memory than an `int[]`.\n- **Array covariance bites at runtime** — `Object[] arr = new String[3]; arr[0] = 42;` compiles fine but throws `ArrayStoreException` at runtime, because the real array is a `String[]`. Generics close this hole: a `List<String>` simply won't let you add an `Integer` at compile time.\n- **`list.remove(2)` on an `ArrayList<Integer>`** — this removes the element at *index* 2, not the value `2`. To delete the value you must call `list.remove(Integer.valueOf(2))`."
          },
          {
            "type": "key_points",
            "title": "Quick revision",
            "content": "- **Fixed vs dynamic**: arrays are fixed-size forever; `ArrayList` resizes itself by allocating a new internal array about 1.5× larger and copying over.\n- **Primitives vs objects**: arrays support `int`, `double`, etc. directly; `ArrayList` needs wrapper types (`Integer`, `Double`).\n- **API richness**: `ArrayList` gives you `add()`, `remove()`, `contains()`, `indexOf()` — arrays give you nothing but `[]` access.\n- **`length` vs `size()`**: arrays use `arr.length` (field); `ArrayList` uses `list.size()` (method) — mixing these up is a common mistake.\n- **Under the hood**: `ArrayList` is just an array with auto-resize logic — indexed access (`get(i)`) is still O(1)."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I'd frame it as **one decision: does the number of elements change while the program runs?** An array locks its size the moment you create it — `new int[5]` is five slots forever — and it stores primitives directly, which keeps it compact and fast for indexed access. An `ArrayList` is built for the opposite case: it grows on demand, so you just call `add()` and it handles the resizing.\n\nThe point I'd make to show I actually understand it is that **an `ArrayList` isn't magic** — internally it's a plain array, and when it fills up it allocates a new one about 1.5 times larger, copies the elements across, and swaps it in.\n\nThe trade-offs fall out of that: it **holds objects only**, so an `int` gets boxed into an `Integer` with some overhead, and you read elements with `get()` and ask its length with `size()`, a method, rather than the `length` field — mixing those two up is a classic slip.\n\nThe reflex answer that `ArrayList` is just better is **the trap**; for a fixed count of primitives like a 12-month total, an array is smaller and faster. So my rule is simple — **fixed size and primitives, use an array; a size that changes at runtime, use an `ArrayList`**.",
            "answerSize": "compact"
          }
        ]
      },
      "followup_questions": [
        "How does ArrayList resize itself internally?",
        "What is autoboxing in Java and when does it happen?",
        "What is the time complexity of ArrayList.get() vs add()?"
      ],
      "seo": {
        "metaTitle": "Array vs ArrayList in Java — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is the difference between an array and an ArrayList in Java? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 2,
      "speakable_v2": {
        "archetype": "B",
        "pillar": "P01",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "simple Java 17 console app with a main method",
        "familiarity_anchors": [
          "array",
          "ArrayList",
          "index"
        ],
        "hook": "Pick by answering one thing — does the count change while the program runs? Everything else falls out of that.",
        "beats": [
          {
            "kind": "what_each_is",
            "layout": "grouped_paragraphs",
            "groups": [
              {
                "heading": "Array",
                "text": "Its size **locks** at creation. It stores primitives directly, and you read it with `arr[i]` and `arr.length`."
              },
              {
                "heading": "ArrayList",
                "text": "It grows on demand and holds objects only. It's contiguous inside too, so cache locality favours ArrayList."
              }
            ]
          },
          {
            "kind": "differences",
            "layout": "mini_table",
            "columns": [
              "Array",
              "ArrayList"
            ],
            "rows": [
              {
                "axis": "Size",
                "values": [
                  "fixed forever",
                  "grows ~1.5x when full"
                ]
              },
              {
                "axis": "Holds",
                "values": [
                  "primitives directly",
                  "objects only, boxed"
                ]
              },
              {
                "axis": "Length",
                "values": [
                  "arr.length, a field",
                  "list.size(), a method"
                ]
              }
            ]
          },
          {
            "kind": "when_to_pick",
            "layout": "bullets",
            "items": [
              "Use an array when the size is fixed and you're storing primitives, like a 12-month total.",
              "Use an ArrayList when the count changes at runtime, like rows read from a file.",
              "Hand-rolling a resize on an array? You've just reinvented ArrayList."
            ]
          },
          {
            "kind": "tiny_example",
            "layout": "paragraph",
            "text": "In a Java 17 app, `int[] top = new int[3]` rejects a fourth value, while `new ArrayList<Integer>()` keeps taking `add()`. The **trap** candidates miss? ArrayList isn't magic — it's a wrapped array."
          }
        ],
        "cap": "Array's fixed, fast, primitives. ArrayList's dynamic, objects, rich API — and it wraps an array inside.",
        "followup_handoff": [
          "How does ArrayList resize itself internally?",
          "What is autoboxing in Java and when does it happen?"
        ],
        "tts_overrides": {
          "int[] top = new int[3]": "int array top equals new int array of size three",
          "new ArrayList<Integer>()": "new array list of Integer",
          "arr[i]": "arr at i",
          "arr.length": "arr length",
          "list.size()": "list size",
          "add()": "add"
        },
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "arrays-basics-006",
      "slug": "passing-arrays-to-methods-in-java-reference-semantics",
      "question": "How do you pass an array to a method in Java — is it pass-by-value or pass-by-reference?",
      "title": "Passing Arrays to Methods in Java",
      "direct_answer": "Java is always pass-by-value. For an array parameter, the copied value is an array reference, so the caller and method initially refer to the same array. The method can change that array's elements. Reassigning the parameter changes only the method's local copy of the reference and does not redirect the caller's variable.",
      "layout_type": "concept-explainer",
      "difficulty": "medium",
      "importance": "high",
      "reading_time_minutes": 5,
      "last_updated": "2026-06-07",
      "interviewer_intent": {
        "testing": "Whether you understand Java's pass-by-value semantics and why arrays behave like they are passed by reference even though they technically are not.",
        "common_mistake": "Saying Java passes arrays by reference. Java is ALWAYS pass-by-value. The confusion is that the value happens to be a reference value, so the method can reach the same heap object through it.",
        "to_stand_out": "Distinguish the two scenarios clearly: modifying arr[0] inside a method DOES affect the caller's array (same heap object), but doing arr = new int[]{...} inside a method does NOT affect the caller (you replaced your local copy of the reference, not the original)."
      },
      "company_tags": [
        "tcs",
        "infosys",
        "wipro",
        "cognizant",
        "amazon",
        "microsoft"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "The Reason Arrays 'Feel' Pass-By-Reference When They Aren't",
            "content": "Java passes every argument **by value**. An array variable holds a reference to an array object, so a method call copies that reference value into its parameter. The two variables are separate, but they initially refer to the same array.\n\nThat explains the two outcomes. `items[0] = 99` changes the shared array object, so the caller sees it. `items = new int[]{1, 2}` changes only the parameter variable, so the caller still refers to the original array.\n\nCalling this 'pass-by-reference' is incorrect: the method cannot reassign the caller's variable. Java references are language-level values; code should not describe them as exposed pointers or numeric reference valuees."
          },
          {
            "type": "flow_diagram",
            "title": "Two Stack Slots, One Heap Array",
            "content": "```mermaid\nflowchart LR\n    C[\"caller variable: scores\"] --> A[\"one int[] object\"]\n    P[\"method parameter: arr<br/>copied reference value\"] --> A\n    P -->|\"arr[0] = 99 changes\"| A\n    P -. \"arr = new int[] changes only arr\" .-> B[\"another array\"]\n```\n\nBoth variables initially refer to one array, but the variables themselves are independent."
          },
          {
            "type": "code_example",
            "title": "Modification Works — Reassignment Does Not Escape",
            "content": "These two methods look almost identical but behave completely differently, and running them is the fastest way to internalise the rule. Try it as a Java 17 console app and watch the two print statements.\n\n```java\npublic class ArrayPassDemo {\n\n    // Modifies elements — the change IS visible to the caller\n    static void doubleAll(int[] arr) {\n        for (int i = 0; i < arr.length; i++) {\n            arr[i] = arr[i] * 2; // reaches through the copied reference to the shared array\n        }\n    }\n\n    // Reassigns the parameter — the caller's variable is NOT affected\n    static void tryReplace(int[] arr) {\n        arr = new int[]{1, 2, 3}; // only changes the local copy of the reference\n        // the caller's original array is completely untouched\n    }\n\n    public static void main(String[] args) {\n        int[] scores = {90, 85, 78};\n\n        doubleAll(scores);\n        System.out.println(scores[0]); // 180 — change visible, same heap object\n\n        tryReplace(scores);\n        System.out.println(scores[0]); // 180 — unchanged, reassignment did not escape\n    }\n}\n```\n\nThe contrast is the whole answer: `doubleAll` reaches through the shared array reference and the change sticks, while `tryReplace` only rebinds its local copy of the address, so the caller's `scores` never moves."
          },
          {
            "type": "when_to_use",
            "title": "Mutate In Place or Return a New Array?",
            "content": "Because a method can change the caller's array through the shared reference, you have two honest designs and should pick deliberately. **Mutating in place** — sorting, scaling, or filling the array you were handed — is fine when the caller expects it and the array is large enough that copying would be wasteful. `Arrays.sort(arr)` works exactly this way.\n\n**Return a brand-new array** when the caller should keep its original intact. Build a fresh array inside the method, fill it, and `return` it. Don't try to swap it in by reassigning the parameter — as we saw, that change never escapes the method.\n\n**When in doubt, prefer not mutating a caller's data.** A method that quietly rearranges an array you passed in is a classic source of \"why did my data change?\" bugs. Defensive copying with `arr.clone()` or `Arrays.copyOf` costs a little memory but removes a whole category of surprises."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Calling it pass-by-reference** — the word interviewers wait for is \"value\". Java copies the reference *by value*; it never passes the variable itself.\n- **Expecting reassignment to escape** — `arr = new int[]{...}` inside a method looks like it should replace the caller's array, but it only rebinds the local parameter; the caller is untouched.\n- **Forgetting that shared state cuts both ways** — a helper that sorts or zeroes the array in place silently mutates the caller's data. If the caller still needs the original, pass `arr.clone()` or `Arrays.copyOf(arr, arr.length)`.\n- **Assuming a method can \"return\" through the parameter** — to give the caller a *different* array you must `return` it and reassign at the call site; writing `arr = ...` inside the method accomplishes nothing visible outside."
          },
          {
            "type": "key_points",
            "title": "Quick revision",
            "content": "- **Java uses pass-by-value** for every argument.\n- **Array argument**: the copied value is a reference to the same array.\n- **Element change**: visible to the caller because both references reach one object.\n- **Parameter reassignment**: local only; the caller's variable is unchanged.\n- **Not pass-by-reference**: the method cannot replace the caller's variable."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "Java always passes arguments by value. With an array, Java copies the array reference into the parameter, so both variables initially refer to the same array. That is why changing `arr[0]` inside the method is visible to the caller.\n\nIf the method assigns `arr = new int[]{1, 2}`, only its local parameter changes; the caller still has its original reference. That final test is why this is pass-by-value rather than pass-by-reference.",
            "answerSize": "compact"
          }
        ]
      },
      "followup_questions": [
        "Is Java pass-by-value or pass-by-reference for primitive types?",
        "What happens when you pass a String to a method in Java?",
        "How would you write a method that returns a new array instead of modifying the original?"
      ],
      "seo": {
        "metaTitle": "Passing Arrays to Methods in Java — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How do you pass an array to a method in Java — is it pass-by-value or pass-by-reference? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 3,
      "speakable_v2": {
        "archetype": "B",
        "pillar": "P01",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "simple Java 17 console app with a main method",
        "familiarity_anchors": [
          "array",
          "reference",
          "method"
        ],
        "hook": "Saying arrays pass by reference is the **trap**. Java copies the address, and you'll spot the difference the moment you reassign.",
        "beats": [
          {
            "kind": "what_each_is",
            "layout": "grouped_paragraphs",
            "groups": [
              {
                "heading": "Modify an element",
                "text": "`arr[i] = x` follows the **shared** address to the same heap array. So the caller sees it."
              },
              {
                "heading": "Reassign the parameter",
                "text": "`arr = new int[]{...}` only overwrites the method's local copy. So the caller's untouched."
              }
            ]
          },
          {
            "kind": "differences",
            "layout": "mini_table",
            "columns": [
              "Modify element",
              "Reassign param"
            ],
            "rows": [
              {
                "axis": "What changes",
                "values": [
                  "the shared heap data",
                  "only the local address"
                ]
              },
              {
                "axis": "Caller sees it",
                "values": [
                  "yes",
                  "no"
                ]
              },
              {
                "axis": "Why",
                "values": [
                  "same array on heap",
                  "address copied by value"
                ]
              }
            ]
          },
          {
            "kind": "when_to_pick",
            "layout": "bullets",
            "items": [
              "Mutate in place when the caller expects it, like `Arrays.sort(arr)`.",
              "Return a fresh array when the caller should keep the original intact.",
              "Don't trust a reassign to escape the method — it never does."
            ]
          },
          {
            "kind": "tiny_example",
            "layout": "paragraph",
            "text": "Pass `int[] s = {90, 85, 78}` to a method that doubles each element, and back in main `s[0]` reads 180. But reassigning `arr` inside leaves it at 180. The **gotcha**? That's pass-by-value, not by-reference."
          }
        ],
        "cap": "A copy of the address means shared data. Modify elements and the caller sees it. Reassign and it doesn't.",
        "followup_handoff": [
          "Is Java pass-by-value or pass-by-reference for primitive types?",
          "What happens when you pass a String to a method in Java?"
        ],
        "tts_overrides": {
          "int[] s = {90, 85, 78}": "int array s holds ninety, eighty five, seventy eight",
          "arr = new int[]{...}": "arr equals a new int array",
          "arr[i] = x": "arr at i equals x",
          "Arrays.sort(arr)": "arrays dot sort of arr",
          "s[0]": "s at zero"
        },
        "speakable_status": "pending_review"
      }
    }
  ]
}

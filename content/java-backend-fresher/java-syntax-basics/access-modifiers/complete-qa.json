{
  "topic": "Access Modifiers",
  "topicSlug": "access-modifiers",
  "questions": [
    {
      "id": "access-modifiers-001",
      "slug": "java-access-modifiers-private-default-protected-public",
      "question": "What are the four access modifiers in Java and what does each allow?",
      "title": "Java Access Modifiers — private, default, protected, public",
      "direct_answer": "Java has four access levels: `private` (inside the declaring top-level class and its nested members), package-private or default (same package), `protected` (same package plus limited access from subclasses in other packages), and `public` (wherever the declaring type is accessible). Top-level classes can be only `public` or package-private.",
      "layout_type": "concept-explainer",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 4,
      "last_updated": "2026-06-07",
      "interviewer_intent": {
        "testing": "Understanding of encapsulation fundamentals and how Java enforces information hiding at compile time",
        "common_mistake": "Confusing protected with default — protected crosses package boundaries through inheritance, default never does",
        "to_stand_out": "Explain the least-privilege principle: always start with private and relax only when necessary, explaining the design rationale"
      },
      "company_tags": [
        "tcs",
        "infosys",
        "wipro",
        "amazon",
        "microsoft"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Access Modifiers: Controlling Who Can See Your Code",
            "content": "An **access level** controls which source code may name a class or member. Java provides four levels. `private` keeps a member inside its declaring top-level class and nested classes. Writing no modifier gives **package-private** access to code in the same package. `protected` includes same-package access and also lets a subclass in another package use the inherited member. `public` is the widest level, although the enclosing type must also be accessible.\n\nThere is one important `protected` detail. In a different package, subclass code cannot use the protected instance member through an arbitrary superclass object. It can use it through itself or through an expression whose type is that subclass (or one of its subclasses). This keeps protected access tied to inheritance.\n\nAccess modifiers support encapsulation, but they are not a security boundary. Start with the narrowest access the design needs. Also remember that a top-level class can be only `public` or package-private; `private` and `protected` are available for members and nested types."
          },
          {
            "type": "flow_diagram",
            "title": "Each Level Is a Superset of the One Above It",
            "content": "Think of the four modifiers as expanding rings of trust. Each level reaches everyone the previous level reached, then adds one more group on top — `private` trusts only the declaring class, while `public` trusts the entire world.\n\n```mermaid\nflowchart TD\n    PRI[\"private — same class only\"]\n    DEF[\"default / package-private — same package\"]\n    PRO[\"protected — same package + subclasses anywhere\"]\n    PUB[\"public — everywhere\"]\n\n    PRI --> DEF\n    DEF --> PRO\n    PRO --> PUB\n\n    PRI --- N1[\"Only the declaring class\"]\n    DEF --- N2[\"Any class in the same package\"]\n    PRO --- N3[\"Same package + subclasses in other packages via inheritance\"]\n    PUB --- N4[\"All classes in all packages\"]\n```\n\nThe arrows run from more restrictive to less restrictive. The one jump that surprises people is `default` to `protected`: it does not just loosen things slightly, it punches a hole through the package boundary specifically for subclasses."
          },
          {
            "type": "comparison_table",
            "title": "The Visibility Matrix You Can Reconstruct From Memory",
            "content": "| Access level | Same class | Same package | Subclass in another package | Unrelated code elsewhere |\n|---|---:|---:|---:|---:|\n| `private` | Yes | No | No | No |\n| package-private | Yes | Yes | No | No |\n| `protected` | Yes | Yes | Yes, through the subclass access rule | No |\n| `public` | Yes | Yes | Yes | Yes, if the containing type is accessible |\n\nFor top-level classes, only `public` and package-private are legal."
          },
          {
            "type": "code_example",
            "title": "Reading the Modifiers as a Statement of Trust",
            "content": "Seeing all four modifiers in one class makes the spectrum concrete. Each field below carries the narrowest access it can tolerate, and the methods expose only what the outside world legitimately needs.\n\n```java\npackage com.company.model;\n\npublic class BankAccount {\n    private double balance;       // only BankAccount can access\n    String accountNumber;          // default: same package only\n    protected String ownerName;    // same package + subclasses anywhere\n    public String bankName;        // accessible everywhere\n\n    public void deposit(double amount) {  // public API\n        if (amount > 0) balance += amount; // only this class touches balance\n    }\n\n    protected double getBalance() {       // subclasses can call this\n        return balance;\n    }\n}\n```\n\nNotice the logic behind each choice: `balance` is the most sensitive value so it is `private`, while `bankName` is shared freely as `public`. The modifier on every line is really a one-word statement of who is allowed to depend on that member."
          },
          {
            "type": "when_to_use",
            "title": "Pick the Narrowest Level the Design Can Tolerate",
            "content": "- Use **`private`** for internal state and helper methods — your default 90% of the time (password hashes, internal calculations).\n- Use **`default`** (`package-private`) for a tightly related group of classes in one package that collaborate but shouldn't be visible outside (internal DTOs between a service and repository).\n- Use **`protected`** when designing for inheritance — fields or methods subclasses override or access (template methods, framework lifecycle hooks).\n- Use **`public`** only for the official API — methods external code should call, or widely shared constants."
          },
          {
            "type": "common_mistakes",
            "title": "The Traps That Turn This Easy Question Into a Wrong Answer",
            "content": "- **Reading \"default\" as more open than `protected`** — default actually *blocks* subclasses in other packages, while `protected` lets them in; getting this backwards is exactly the trap interviewers set.\n- **Treating package-private as the same as `public`** — a default member is invisible outside its package, so code in another module that used it as if it were public will not even compile.\n- **Making fields `public` for convenience** — once external code reads `account.balance` directly, you can never add validation or change the representation without breaking every caller.\n- **Assuming `private` is only \"partly\" restrictive** — private is absolute; not even a subclass in the same file can see it, so reaching for `protected` when you meant `private` quietly widens your public surface."
          },
          {
            "type": "key_points",
            "title": "Quick revision",
            "content": "- **`private`**: within the declaring top-level class and its nested members.\n- **Package-private**: no keyword; accessible within the same package.\n- **`protected`**: same package, plus inheritance-based access for subclasses in other packages.\n- **`public`**: accessible wherever the containing type is accessible.\n- **Top-level types**: only `public` or package-private."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "Java has four access levels. `private` keeps a member inside its declaring class, package-private—written with no keyword—opens it to the package, `protected` adds inheritance-based access for subclasses in other packages, and `public` is the widest level.\n\nThe useful nuance is `protected`: a subclass in another package can use the inherited member through its own subclass context, not through any random instance of the superclass. I also distinguish members from top-level classes, because a top-level class can be only public or package-private. I normally begin with the narrowest access that callers actually need.",
            "answerSize": "compact"
          }
        ]
      },
      "followup_questions": [
        "What is the default access modifier in Java?",
        "Can a subclass in a different package access a protected method?",
        "Why should class fields generally be private?"
      ],
      "seo": {
        "metaTitle": "Java Access Modifiers — private, default, protected, public — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What are the four access modifiers in Java and what does each allow? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1,
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P01",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "BankAccount with private balance, protected getBalance, and public deposit",
        "familiarity_anchors": [
          "public",
          "private",
          "package"
        ],
        "hook": "Picture clearance levels in a building. Public is the open lobby. Private is the locked vault only the class itself can open.",
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "Java's got four access levels. They say who can reach a class, a field, or a method. The keyword sits right in front of the member. The compiler checks it, so access gets locked in at compile time."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "Without these levels, any line anywhere could reach in and rewrite a value. Picture a bank balance that any code could set to minus a million. That's a bug waiting to happen. Controlling who touches what sits at the heart of **encapsulation**."
          },
          {
            "kind": "parts_or_states",
            "label": "The four levels",
            "layout": "bullets",
            "items": [
              "**private** keeps a member to the same class — nothing else can touch it.",
              "**default**, with no keyword, opens it to the whole package.",
              "**protected** does what default does, plus subclasses in any package.",
              "**public** drops every limit, so any class anywhere can reach it."
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "Take a `BankAccount`. I'd keep its balance private, so only the class itself reads it. A getBalance method can stay protected, letting subclasses read it. The deposit method goes public as the real API. Here's the **non-obvious** part. Protected reaches subclasses across a package boundary, while default never leaves its own package."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Reading default as stricter than protected. Across packages it's actually the more open one.",
              "Making everything public for convenience, which quietly breaks encapsulation."
            ]
          }
        ],
        "cap": "So reach for the tightest modifier that still works. Start private, and open up only when there's a real reason.",
        "followup_handoff": [
          "Can you elaborate on the least-privilege principle?",
          "What would you say if asked why fields should be private?"
        ],
        "tts_overrides": {
          "BankAccount": "bank account",
          "getBalance": "get balance",
          "deposit": "deposit",
          "private": "private",
          "public": "public",
          "protected": "protected",
          "default": "default"
        },
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "access-modifiers-002",
      "slug": "private-fields-with-getters-and-setters-in-java",
      "question": "Why should class fields be private in Java, and what is the role of getters and setters?",
      "title": "Private Fields with Getters and Setters in Java",
      "direct_answer": "Making fields **`private`** enforces **encapsulation** — it stops external code from modifying state in unexpected ways. **Getters** give controlled read access; **setters** validate data before accepting a change. This protects data integrity and lets the internal implementation change without breaking callers.",
      "layout_type": "concept-explainer",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 4,
      "last_updated": "2026-06-07",
      "interviewer_intent": {
        "testing": "Whether you understand encapsulation as a design principle, not just a syntax rule to memorize",
        "common_mistake": "Generating getters and setters for every field automatically without thinking about whether each field should be mutable",
        "to_stand_out": "Mention validation in setters as the real benefit, and explain that immutable fields should have getters only — no setter"
      },
      "company_tags": [
        "tcs",
        "infosys",
        "wipro",
        "cognizant",
        "amazon"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Private Fields and Accessor Methods: The Core of Encapsulation",
            "content": "A **field** is a variable that lives inside an object and holds its data — a user's age, an account balance, an email address. Making a field `private` means only the class it belongs to can read or change it directly; code in other classes cannot.\n\nWhy bother locking it down? Because a `public` field is an **open door**. Any code anywhere could write `user.age = -500`, and the object would sit there in a broken state with nothing to stop it. Controlling how data changes is the whole point of **encapsulation** — one of the core ideas of object-oriented programming.\n\nSo if the field is `private`, how does outside code work with it? Through two small methods you write and control. A **getter** is a method that returns the field's value — the read side. A **setter** is a method that takes a new value and assigns it — the write side. The trick is that a setter can check the value first. If someone passes -500 for an age, the setter throws an error before that number ever reaches the field. That **validation** step is the real reason setters exist, not the assignment line itself.\n\nThink of the object like a **vending machine**. You cannot reach inside and grab whatever you want; you press a button and the machine decides what to hand back. Getters and setters are those buttons — controlled doorways instead of open shelves.\n\nThe interview-level point is that **not every field deserves a setter**. If a value should never change after the object is built — an ID, a creation date — give it a getter and no setter at all, which keeps it effectively read-only.\n\nThe **subtle gotcha** is a getter that returns a mutable object directly, like `return this.items;`. That hands callers your internal list, and they can change it behind your back, quietly undoing all the protection — so you return a copy or an unmodifiable view instead."
          },
          {
            "type": "flow_diagram",
            "title": "Every Write Goes Through the Validation Gate",
            "content": "This diagram shows how external code interacts with a private field. Direct access is blocked. All reads go through the getter; all writes go through the setter's validation gate.\n\n```mermaid\nflowchart TD\n    EXT[\"External code (another class)\"] -->|\"getAge()\"| GET[\"Getter returns current value\"]\n    EXT -->|\"setAge(value)\"| SET[\"Setter method\"]\n    SET --> VAL{\"Is value valid?\"}\n    VAL -->|\"Yes (0-150)\"| FIELD[\"private int age\"]\n    VAL -->|\"No (negative or over 150)\"| ERR[\"throw IllegalArgumentException\"]\n    GET --> FIELD\n    EXT -. blocked .-> FIELD\n```\n\nThe dashed arrow from external code to the private field is blocked at compile time. The only way in or out is through the getter and setter methods — and the setter forces every incoming value past the validity check first."
          },
          {
            "type": "before_code",
            "title": "Wrong — A Setter That Skips Validation Is Just a Public Field",
            "content": "```java\npublic class User {\n    private int age;\n    public void setAge(int age) { this.age = age; } // no checks — barely better than public\n}\n```"
          },
          {
            "type": "after_code",
            "title": "Right — The Setter Earns Its Keep by Rejecting Bad Data",
            "content": "```java\npublic class User {\n    private int age;\n    public void setAge(int age) {\n        if (age < 0 || age > 150)\n            throw new IllegalArgumentException(\"Age must be 0-150\");\n        this.age = age; // only valid values reach the field\n    }\n}\n```\nThe validation is the whole point of a setter — without it, a private field plus a pass-through setter is effectively public with extra typing."
          },
          {
            "type": "code_example",
            "title": "Accessors That Validate and Normalize in One Step",
            "content": "A realistic accessor does two jobs at once — it guards the value and it cleans it up. This `Account` rejects blank usernames and malformed emails, and it also normalizes whatever it keeps.\n\n```java\npublic class Account {\n    private String username;\n    private String email;\n\n    public String getUsername() { return username; }\n\n    public void setUsername(String username) {\n        if (username == null || username.isBlank())\n            throw new IllegalArgumentException(\"Username required\");\n        this.username = username.trim();      // normalize\n    }\n\n    public void setEmail(String email) {\n        if (!email.contains(\"@\"))\n            throw new IllegalArgumentException(\"Invalid email\");\n        this.email = email.toLowerCase();     // normalize\n    }\n}\n```\n\nBecause callers can never write the field directly, every value that lands in `username` or `email` is already trimmed and lower-cased. That guarantee is only possible when the field is private and the setter is the single doorway in."
          },
          {
            "type": "when_to_use",
            "title": "Not Every Field Deserves a Setter",
            "content": "- Use `private` fields with getters and setters for **mutable domain objects** where callers read or modify state but you want control — a `User` whose email is validated before acceptance.\n- Use **getter-only** (no setter) for **immutable fields** set once in the constructor — a `userId` or `createdAt`.\n- Use **neither** for truly internal data — a lazily computed cache.\n- Prefer **Java records** (Java 14+) for simple immutable data carriers — they generate component accessors and final fields; the record is shallowly immutable, so a mutable component can still change."
          },
          {
            "type": "common_mistakes",
            "title": "The Boilerplate Habits That Quietly Break Encapsulation",
            "content": "- **Auto-generating a getter and setter for every field** — fields meant to be set once at construction end up with a setter they never needed, throwing away immutability for no reason.\n- **Writing setters with no validation** — `this.age = age;` with no checks is barely better than a public field; the validation is the entire reason the setter exists.\n- **Returning the live reference from a getter** — `return this.items;` hands callers your internal `List`, and they can mutate it behind your back; return a copy or an unmodifiable view instead.\n- **Confusing `private` with secure** — private is a compile-time visibility rule, not security; reflection can still reach the field, so this is about clean design and data integrity, not hiding secrets."
          },
          {
            "type": "key_points",
            "title": "Quick revision",
            "content": "- **Encapsulation** — private fields hide state; methods control access.\n- **Validation** — the primary benefit of setters is checking new values.\n- **Getter-only** — for immutable fields, provide a getter and no setter.\n- **No accessors** — for internal-only data like caches.\n- **Flexibility** — changing the field type won't break callers if signatures stay."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The reason fields go `private` is that a `public` field is an **open door** — anyone can set `age` to minus a hundred and the object is instantly broken with nothing to stop it. `private` means the only way in is a method I control.\n\nThat's what a setter really is: **not the assignment, but the validation gate** in front of it, rejecting garbage before it reaches the field. The getter is the read side, letting callers see the value without handing them a reference they can mutate — which also lets me change how the field is stored later without breaking anyone.\n\nThe point I'd stress is that **not every field needs a setter**; if something shouldn't change after construction, like an ID, I give it a getter only. A setter that just assigns with no checks buys almost nothing over a `public` field.\n\nAnd for simple immutable data, I'd reach for a **record** instead of hand-writing all of it.",
            "answerSize": "compact"
          }
        ]
      },
      "followup_questions": [
        "What is the JavaBeans convention for getters and setters?",
        "Should every private field have both a getter and a setter?",
        "What happens if you make a field public?"
      ],
      "seo": {
        "metaTitle": "Private Fields with Getters and Setters in Java — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for Why should class fields be private in Java, and what is the role of getters and setters? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 2,
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P01",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "User with a private age field validated through setAge",
        "familiarity_anchors": [
          "private",
          "getter",
          "setter"
        ],
        "hook": "A private field sits behind a counter. People ask for it through methods, instead of grabbing it themselves.",
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "Making fields private enforces **encapsulation**. Outside code can't reach in and rewrite an object's state directly. The only way in runs through methods the class controls."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "A public field acts like an open door. Any line could set an age to minus a hundred, and the object's instantly broken. Routing every change through a method means we check the value first."
          },
          {
            "kind": "parts_or_states",
            "label": "The two accessors",
            "layout": "bullets",
            "items": [
              "A **getter** gives controlled read access. It can format or compute before returning.",
              "A **setter** validates the new value first. Bad data gets rejected before it lands."
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "Take a private age field. The setAge method checks the value sits between zero and a hundred fifty, then throws if it doesn't. So nobody forces the object into a bad state. That validation gate's the real payoff. It also lets me restyle how age gets stored later, and no caller breaks."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Writing a getter and setter for every field by default, even one that should never change.",
              "The **sharp edge**: a getter that hands back the live list lets callers mutate your state, so return a copy."
            ]
          }
        ],
        "cap": "So a field that should never change gets a getter and no setter. The validation, not the assignment, makes setters worth writing.",
        "followup_handoff": [
          "Can you elaborate on validation in setters?",
          "What would you say if asked whether every field needs a setter?"
        ],
        "tts_overrides": {
          "setAge": "set age",
          "getter": "getter",
          "setter": "setter",
          "private": "private"
        },
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "access-modifiers-005",
      "slug": "protected-vs-default-access-in-java",
      "question": "What is the difference between protected and default (package-private) access in Java?",
      "title": "protected vs default Access in Java",
      "direct_answer": "Package-private access (no keyword) stops at the package boundary. `protected` also allows same-package access, and it gives a subclass in another package inheritance-based access. In that cross-package case, the subclass cannot access the protected instance member through an arbitrary superclass reference; it must use its own inherited view or a reference of the subclass type.",
      "layout_type": "comparison-arena",
      "difficulty": "medium",
      "importance": "medium",
      "reading_time_minutes": 4,
      "last_updated": "2026-06-07",
      "interviewer_intent": {
        "testing": "Precise understanding of the protected vs default subtlety — a common interview trap question",
        "common_mistake": "Thinking default allows subclasses in other packages, or that protected is more restrictive than default",
        "to_stand_out": "Give a concrete cross-package inheritance example showing protected access works but default fails"
      },
      "company_tags": [
        "amazon",
        "microsoft",
        "tcs",
        "infosys",
        "flipkart"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "protected vs default: The One Difference That Trips Everyone Up",
            "content": "Package-private and `protected` look the same to code in the declaring package: either member is accessible there. The difference appears when a subclass is declared in another package. A package-private member is not accessible across that boundary. A protected member is accessible to the subclass as part of its inherited API.\n\nThat does not make protected a general 'all subclasses anywhere' permission. Suppose `Dog` in another package extends `Animal`. Code inside `Dog` may use `this.age` or `anotherDog.age`, but it may not read `someAnimal.age` merely because the code happens to be inside a subclass. Java keeps this access connected to the subclass relationship.\n\nUse package-private when cooperating classes should stay inside one package. Use `protected` sparingly when subclasses genuinely need an extension point; a protected method is often safer than exposing mutable fields."
          },
          {
            "type": "flow_diagram",
            "title": "protected Follows the Inheritance Link Across the Package Line",
            "content": "This diagram shows two packages side by side with a subclass in the other package. Notice that `protected` reaches across the boundary through the inheritance arrow, while default stops at the package edge.\n\n```mermaid\nflowchart TD\n    subgraph PKG1[\"com.company.model (base package)\"]\n        ANIMAL[\"Animal class\"]\n        DEF[\"void breathe() — default, no keyword\"]\n        PRO[\"protected void eat()\"]\n    end\n\n    subgraph PKG2[\"com.company.pets (other package)\"]\n        DOG[\"Dog extends Animal\"]\n    end\n\n    DOG -->|\"extends\"| ANIMAL\n    DOG -->|\"eat() — OK, protected crosses packages\"| PRO\n    DOG -. \"breathe() — COMPILE ERROR, default blocked\" .-> DEF\n```\n\nThe solid arrow shows that `Dog` can call `eat()` because it is `protected`. The dashed arrow is blocked at compile time — `breathe()` is default and `Dog` lives in a different package."
          },
          {
            "type": "comparison_table",
            "title": "Same Inside a Package, Opposite Across One",
            "content": "| Case | Package-private | `protected` |\n|---|---:|---:|\n| Code in the same package | Yes | Yes |\n| Subclass in another package, using `this.member` | No | Yes |\n| Subclass in another package, using another instance of that subclass | No | Yes |\n| Subclass in another package, using an arbitrary superclass instance | No | No |\n| Unrelated code in another package | No | No |"
          },
          {
            "type": "before_code",
            "title": "The Base Class That Sets Up the Trap",
            "content": "```java\n// package com.company.model\npackage com.company.model;\npublic class Animal {\n    void breathe() {}        // default (package-private)\n    protected void eat() {}  // protected\n}\n```"
          },
          {
            "type": "after_code",
            "title": "One Call Compiles, the Other Doesn't — and That's the Answer",
            "content": "```java\n// package com.company.pets — a DIFFERENT package\npackage com.company.pets;\nimport com.company.model.Animal;\n\nclass Dog extends Animal {\n    void test() {\n        eat();       // OK — protected crosses packages via inheritance\n        // breathe(); // COMPILE ERROR — default is blocked across packages\n    }\n}\n```\nThe single line that compiles versus the one that doesn't is the whole interview answer: `eat()` is reachable because `protected` follows the inheritance link across packages, while `breathe()` is not because default never leaves its own package."
          },
          {
            "type": "when_to_use",
            "title": "Default for Package Cohesion, protected for Extensibility",
            "content": "Use **`default`** (`package-private`) for a group of classes collaborating in one package with no planned inheritance — internal helpers, package-scoped DTOs. It says \"this is an implementation detail of our package.\"\n\nUse **`protected`** when designing a class hierarchy where subclasses in other packages must access fields or methods to override or extend — template methods, lifecycle hooks, configuration fields. If you aren't designing for inheritance, `default` is safer and keeps the surface smaller."
          },
          {
            "type": "common_mistakes",
            "title": "The Backwards Assumptions That Sink This Trap Question",
            "content": "- **Assuming `protected` means package-only** — protected always includes same-package access *and* reaches subclasses in any package; treating it as package-scoped is the single most common mistake here.\n- **Believing default lets subclasses in other packages access members** — inheritance does not override the package boundary for default; the cross-package subclass simply won't compile.\n- **Ranking `protected` as stricter than default** — for cross-package subclasses, `protected` is actually the *more* permissive of the two, the opposite of what the name suggests.\n- **Using `protected` with no inheritance in mind** — it invites outsiders to subclass your class just to reach its members; default keeps the surface tighter when extension was never a goal."
          },
          {
            "type": "key_points",
            "title": "Quick revision",
            "content": "- **Package-private**: no modifier; accessible only in the same package.\n- **`protected`**: same-package access plus limited cross-package subclass access.\n- **Cross-package rule**: a subclass cannot use a protected instance member through an arbitrary superclass reference.\n- **Design use**: package collaboration versus a deliberate subclass extension point.\n- **Prefer methods over protected mutable fields** when exposing behavior to subclasses."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "Package-private means exactly what its name says: code in the same package can use the member, and code outside cannot. `protected` includes that package access and adds an inheritance path for a subclass in another package.\n\nThe detail I would mention is that cross-package protected access is tied to the subclass object. A `Dog` subclass can use the inherited member through `this` or another `Dog`, but not through an arbitrary `Animal` reference. That is the real difference, not simply saying 'protected works everywhere in subclasses.'",
            "answerSize": "compact"
          }
        ]
      },
      "followup_questions": [
        "Can a subclass in a different package access a default member of its parent?",
        "When would you choose protected over public?",
        "What is the package-private access level used for in framework design?"
      ],
      "seo": {
        "metaTitle": "protected vs default Access in Java — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is the difference between protected and default (package-private) access in Java? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 3,
      "speakable_v2": {
        "archetype": "B",
        "pillar": "P01",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Animal with a default breathe and a protected eat, and Dog extending it from another package",
        "familiarity_anchors": [
          "protected",
          "package",
          "subclass"
        ],
        "hook": "Inside one package these two look identical. The difference shows up when a subclass lives in another package.",
        "beats": [
          {
            "kind": "what_each_is",
            "layout": "paragraph",
            "text": "Default means no keyword at all. It allows access inside the same package only. Inheritance doesn't change that. Protected keeps the same-package access. Then it adds subclasses in any package."
          },
          {
            "kind": "differences",
            "layout": "mini_table",
            "columns": [
              "default",
              "protected"
            ],
            "rows": [
              {
                "axis": "Same package",
                "values": [
                  "any class reaches it",
                  "any class reaches it"
                ]
              },
              {
                "axis": "Cross-package subclass",
                "values": [
                  "blocked",
                  "allowed via inheritance"
                ]
              },
              {
                "axis": "Design intent",
                "values": [
                  "package cohesion",
                  "built for extension"
                ]
              }
            ]
          },
          {
            "kind": "when_to_pick",
            "layout": "paragraph",
            "text": "Reach for protected when you're building for extension. Subclasses elsewhere can then read or override a member. Pick default when classes just work together in one package. It keeps the surface small."
          },
          {
            "kind": "tiny_example",
            "layout": "paragraph",
            "text": "Say Animal sits in one package. Dog extends it from another. Dog can call a protected eat. But a default breathe won't compile. Here's the **trap** candidates miss. Protected sounds stricter, yet across packages it's the looser one."
          }
        ],
        "cap": "So the one rule: protected crosses the package line through inheritance. Default never does.",
        "followup_handoff": [
          "Can you elaborate on the cross-package example?",
          "What would you say if asked when to choose protected over public?"
        ],
        "tts_overrides": {
          "Animal": "Animal",
          "Dog": "Dog",
          "protected": "protected",
          "default": "default"
        },
        "speakable_status": "pending_review"
      }
    }
  ]
}

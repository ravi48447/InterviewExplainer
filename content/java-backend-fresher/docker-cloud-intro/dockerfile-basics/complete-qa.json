{
  "topic": "Dockerfile Basics",
  "module": "docker-cloud-intro",
  "domain": "java-backend-fresher",
  "last_updated": "2026-05-31",
  "questions": [
    {
      "id": "dockerfile-basics-001",
      "question": "What is a Dockerfile and what are the key instructions?",
      "difficulty": "easy",
      "company_tags": [
        "tcs",
        "infosys",
        "wipro",
        "amazon"
      ],
      "interviewer_intent": "Check ability to write a basic Dockerfile for a Spring Boot app",
      "direct_answer": "A Dockerfile is a text file with instructions to build a Docker image. Key instructions: FROM (base image), WORKDIR (set working directory), COPY (add files), RUN (execute commands), EXPOSE (document port), ENTRYPOINT (command to run).",
      "answer_sections": [
        {
          "type": "code",
          "language": "dockerfile",
          "content": "FROM eclipse-temurin:17-jre\nWORKDIR /app\nCOPY target/my-app.jar app.jar\nEXPOSE 8080\nENTRYPOINT [\"java\", \"-jar\", \"app.jar\"]"
        },
        {
          "type": "comparison_table",
          "headers": [
            "Instruction",
            "Purpose",
            "Example"
          ],
          "rows": [
            [
              "FROM",
              "Base image to start from",
              "FROM eclipse-temurin:17-jre"
            ],
            [
              "WORKDIR",
              "Set working directory",
              "WORKDIR /app"
            ],
            [
              "COPY",
              "Copy files into image",
              "COPY target/app.jar app.jar"
            ],
            [
              "RUN",
              "Execute command at build time",
              "RUN chmod +x app.jar"
            ],
            [
              "EXPOSE",
              "Document which port app uses",
              "EXPOSE 8080"
            ],
            [
              "ENV",
              "Set environment variable",
              "ENV SPRING_PROFILES_ACTIVE=prod"
            ],
            [
              "ENTRYPOINT",
              "Command when container starts",
              "ENTRYPOINT [\"java\",\"-jar\",\"app.jar\"]"
            ]
          ]
        },
        {
          "type": "speakable_answer",
          "content": "A Dockerfile is the recipe for building a Docker image. For a Spring Boot app, a minimal Dockerfile is 5 lines: FROM tells Docker which base image to use \u2014 I use eclipse-temurin:17-jre which gives me Java 17's JRE without the full JDK. WORKDIR sets the directory inside the container where we work. COPY brings in the fat JAR that Maven built. EXPOSE documents the port (it doesn't actually open it \u2014 that's docker run -p). ENTRYPOINT is the command that runs when a container starts. Docker then builds each instruction as a layer."
        }
      ],
      "follow_up_questions": [
        "What is the difference between CMD and ENTRYPOINT in Dockerfile?",
        "What is a .dockerignore file?"
      ],
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P10",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Spring Boot app packaged as a Docker image",
        "familiarity_anchors": ["Docker", "Dockerfile", "Spring Boot JAR"],
        "hook": "A Dockerfile is a five-line recipe that turns your Spring Boot JAR into something any server in the world can run.",
        "beats": [
          {
            "kind": "what_it_is",
            "layout": "paragraph",
            "text": "A Dockerfile is a plain text file with instructions that Docker reads top-to-bottom to build an image. Each instruction becomes a layer. The result is a portable, self-contained image."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "FROM eclipse-temurin:17-jre — base image with Java 17 JRE already installed",
              "WORKDIR /app — sets the working directory inside the container",
              "COPY target/my-app.jar app.jar — copies the built JAR into the image",
              "EXPOSE 8080 — documents the port (does NOT publish it)",
              "ENTRYPOINT [\"java\", \"-jar\", \"app.jar\"] — the command that runs when the container starts"
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "Notice we use eclipse-temurin:17-jre, not the full JDK image. JRE is enough to run a compiled JAR and results in a much smaller image than bundling the entire JDK."
          },
          {
            "kind": "pitfalls",
            "layout": "paragraph",
            "text": "candidates miss: EXPOSE does not actually open the port. It's documentation. You make the port accessible with -p 8080:8080 in docker run, not EXPOSE."
          }
        ],
        "cap": "Five instructions is all you need for a working Spring Boot Docker image. Everything else is optimization — multi-stage builds, layer caching, smaller base images.",
        "followup_handoff": [
          "What is the difference between CMD and ENTRYPOINT in a Dockerfile?",
          "What should you put in a .dockerignore file?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "dockerfile-basics-002",
      "question": "What is the difference between ENTRYPOINT and CMD in a Dockerfile?",
      "difficulty": "medium",
      "company_tags": [
        "amazon",
        "microsoft",
        "flipkart",
        "tcs"
      ],
      "interviewer_intent": "Check understanding of how container startup commands are configured",
      "direct_answer": "ENTRYPOINT defines the main executable that always runs. CMD provides default arguments to ENTRYPOINT (or the default command if no ENTRYPOINT is set). CMD can be overridden at docker run; ENTRYPOINT cannot (without --entrypoint flag).",
      "answer_sections": [
        {
          "type": "code",
          "language": "dockerfile",
          "content": "# ENTRYPOINT fixed, CMD provides default args\nENTRYPOINT [\"java\", \"-jar\", \"app.jar\"]\nCMD [\"--spring.profiles.active=prod\"]\n\n# docker run myapp          \u2192 java -jar app.jar --spring.profiles.active=prod\n# docker run myapp --spring.profiles.active=dev  \u2192 CMD overridden\n\n# Spring Boot standard: just ENTRYPOINT\nENTRYPOINT [\"java\", \"-jar\", \"app.jar\"]"
        },
        {
          "type": "speakable_answer",
          "content": "ENTRYPOINT is the fixed command that always executes when the container starts \u2014 in our case 'java -jar app.jar'. CMD provides default arguments to ENTRYPOINT that can be overridden when you run the container. So ENTRYPOINT [\"java\",\"-jar\",\"app.jar\"] with CMD [\"--server.port=9090\"] means the default is port 9090, but you can override that with docker run myapp --server.port=8080. For most Spring Boot apps I just use ENTRYPOINT without CMD, and pass environment variables like -e SPRING_PROFILES_ACTIVE=prod to configure the app."
        }
      ],
      "follow_up_questions": [
        "What is the exec form vs shell form in ENTRYPOINT?",
        "How do you pass JVM flags like -Xmx in a Docker ENTRYPOINT?"
      ],
      "speakable_v2": {
        "archetype": "B",
        "pillar": "P10",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Spring Boot app with configurable startup command",
        "familiarity_anchors": ["Dockerfile", "ENTRYPOINT", "CMD"],
        "hook": "ENTRYPOINT and CMD look similar but they play different roles — one is the fixed executable, the other is the default argument you can swap at runtime.",
        "beats": [
          {
            "kind": "what_each_is",
            "layout": "grouped_paragraphs",
            "groups": [
              {
                "heading": "ENTRYPOINT",
                "text": "Defines the fixed executable that always runs when the container starts. For a Spring Boot app this is always java -jar app.jar. It cannot be overridden by arguments to docker run."
              },
              {
                "heading": "CMD",
                "text": "Provides default arguments to ENTRYPOINT, or the default command if no ENTRYPOINT is set. CMD can be completely replaced by passing arguments to docker run."
              }
            ]
          },
          {
            "kind": "tiny_example",
            "layout": "paragraph",
            "text": "ENTRYPOINT [\"java\",\"-jar\",\"app.jar\"] with CMD [\"--spring.profiles.active=prod\"] means prod is the default. Run docker run myapp --spring.profiles.active=dev and CMD is replaced — but the java -jar part stays fixed."
          },
          {
            "kind": "when_to_pick",
            "layout": "paragraph",
            "text": "For most Spring Boot apps, use just ENTRYPOINT and pass configuration via -e environment variables. That's cleaner and avoids confusion about what CMD is overriding."
          }
        ],
        "cap": "sharp edge: if you use the shell form — ENTRYPOINT java -jar app.jar — Java runs as a child process of sh, so Docker signals like SIGTERM won't reach it cleanly. Always use the exec form with square brackets.",
        "followup_handoff": [
          "What is the exec form versus shell form in ENTRYPOINT?",
          "How do you pass JVM flags like -Xmx in a Docker ENTRYPOINT?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "dockerfile-basics-003",
      "question": "What is a .dockerignore file and why is it important?",
      "difficulty": "easy",
      "company_tags": [
        "tcs",
        "infosys",
        "wipro",
        "amazon"
      ],
      "interviewer_intent": "Check awareness of excluding unnecessary files from the Docker build context",
      "direct_answer": ".dockerignore excludes files from the Docker build context, reducing image size and build time. For Spring Boot: exclude target/ class files, node_modules, .git, and IDE files.",
      "answer_sections": [
        {
          "type": "code",
          "language": "bash",
          "content": "# .dockerignore\ntarget/          # except we COPY target/*.jar explicitly\n!target/*.jar    # but keep JARs\n.git/            # git history not needed in image\n.idea/           # IDE files\n*.md             # documentation\nsrc/             # source code (not needed if JAR is pre-built)\nDockerfile       # don't include the Dockerfile itself"
        },
        {
          "type": "key_points",
          "items": [
            "Docker sends entire build context to Docker daemon before building",
            ".dockerignore prevents sending large/unnecessary files",
            "Reduces build context size \u2192 faster build start",
            "Prevents accidentally including secrets in the image",
            "Similar syntax to .gitignore with !/path for negation (include exceptions)"
          ]
        },
        {
          "type": "speakable_answer",
          "content": "When you run docker build, Docker sends the entire current directory to the Docker daemon as a build context. Without .dockerignore, this includes your .git directory with all history, IDE config files, and potentially the whole target/ directory full of compiled files. That can be hundreds of megabytes. .dockerignore works like .gitignore \u2014 you list patterns to exclude. For Spring Boot, I exclude everything except the target/*.jar that I need to COPY. This makes builds faster and images smaller. More importantly, it prevents accidentally including secret files like .env in the build context."
        }
      ],
      "follow_up_questions": [
        "How do you include a specific file while excluding its parent directory in .dockerignore?",
        "What are the security implications of including .env in a Docker image?"
      ],
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P10",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Spring Boot app packaged as a Docker image",
        "familiarity_anchors": ["Docker build", ".gitignore", "build context"],
        "hook": "Without .dockerignore, docker build can silently send hundreds of megabytes — including your secrets — to the Docker daemon before a single layer is built.",
        "beats": [
          {
            "kind": "what_it_is",
            "layout": "paragraph",
            "text": "When you run docker build, Docker packages your entire current directory and sends it to the Docker daemon as a build context. This happens before any layer is built. .dockerignore tells Docker what to leave out — using the same syntax as .gitignore."
          },
          {
            "kind": "why_it_exists",
            "layout": "paragraph",
            "text": "For a Spring Boot project, the .git directory alone can be tens of megabytes. The target/ folder after a Maven build is even larger. None of that belongs in the image."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "Exclude .git/ — git history is never needed in a running container",
              "Exclude .idea/ and other IDE files",
              "Exclude src/ if you're copying the pre-built JAR only",
              "Use !target/*.jar to exclude target/ but keep the JAR you need to COPY"
            ]
          },
          {
            "kind": "pitfalls",
            "layout": "paragraph",
            "text": "production trap: accidentally including a .env file in the build context means those secrets end up in the image. Anyone with docker history access or image pull access can read them."
          }
        ],
        "cap": ".dockerignore is a small file with an outsized impact on both build speed and security. Add it to every project alongside the Dockerfile.",
        "followup_handoff": [
          "How do you include a specific file while excluding its parent directory in .dockerignore?",
          "What are the security risks of baking secrets into a Docker image?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "dockerfile-basics-004",
      "question": "How do you pass environment variables to a Spring Boot app running in Docker?",
      "difficulty": "easy",
      "company_tags": [
        "amazon",
        "microsoft",
        "flipkart",
        "wipro"
      ],
      "interviewer_intent": "Check practical knowledge of runtime configuration in containers",
      "direct_answer": "Use -e flag in docker run or environment section in docker-compose.yml. Spring Boot reads environment variables and overrides application.properties values (SPRING_DATASOURCE_URL overrides spring.datasource.url).",
      "answer_sections": [
        {
          "type": "code",
          "language": "bash",
          "content": "# docker run with env vars\ndocker run -p 8080:8080 \\\n  -e SPRING_PROFILES_ACTIVE=prod \\\n  -e SPRING_DATASOURCE_URL=jdbc:postgresql://db:5432/prod \\\n  -e SPRING_DATASOURCE_PASSWORD=secret \\\n  myapp:latest\n\n# Or reference from host env file\ndocker run --env-file .env myapp:latest"
        },
        {
          "type": "key_points",
          "items": [
            "Spring Boot converts env vars to properties (dots become underscores)",
            "SPRING_DATASOURCE_URL \u2192 spring.datasource.url (auto-converted)",
            "Environment variables override application.properties values",
            "Never hardcode secrets in Dockerfile or docker-compose.yml",
            "Use Docker secrets or K8s secrets for production credentials"
          ]
        },
        {
          "type": "speakable_answer",
          "content": "Spring Boot's environment variable support is perfect for Docker. Environment variables override application.properties, so I can have a generic image and configure it at runtime. The naming convention: properties like spring.datasource.url become SPRING_DATASOURCE_URL as environment variables \u2014 dots become underscores and everything is uppercase. I pass them with -e when doing docker run, or in the environment section of docker-compose.yml for local development. For production, you'd use Docker secrets or Kubernetes secrets rather than plain text env vars, since container inspection can reveal environment variables."
        }
      ],
      "follow_up_questions": [
        "What is the 12-factor app methodology and how does it relate to env var configuration?",
        "Why should you not put secrets directly in environment variables in production?"
      ],
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P10",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Spring Boot app with environment-specific database config",
        "familiarity_anchors": ["Spring Boot", "application.properties", "environment variables"],
        "hook": "One Docker image running in dev, staging, and production — environment variables are how you make a single image behave differently in each place.",
        "beats": [
          {
            "kind": "what_it_is",
            "layout": "paragraph",
            "text": "Spring Boot automatically reads environment variables and uses them to override application.properties values. The naming is automatic: dots become underscores and letters go uppercase. So spring.datasource.url in properties becomes SPRING_DATASOURCE_URL as an env var."
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "Pass -e SPRING_PROFILES_ACTIVE=prod and -e SPRING_DATASOURCE_URL=jdbc:postgresql://prod-db:5432/app at docker run time. The same image that ran with an H2 in-memory database in dev now connects to your PostgreSQL in production."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "docker run -e KEY=VALUE: pass a single environment variable",
              "docker run --env-file .env: load all variables from a file",
              "docker-compose.yml environment section: define per-service variables"
            ]
          },
          {
            "kind": "pitfalls",
            "layout": "paragraph",
            "text": "non-obvious: never hardcode database passwords in your Dockerfile ENV instructions. ENV values in a Dockerfile are baked into the image and visible to anyone who runs docker inspect. Pass secrets at runtime, and for production use Docker secrets or Kubernetes secrets instead of plain env vars."
          }
        ],
        "cap": "The pattern — generic image, runtime configuration via env vars — is the 12-factor app principle. Master it and your Spring Boot apps will deploy cleanly to any environment.",
        "followup_handoff": [
          "What is the 12-factor app methodology?",
          "How do you manage secrets securely in Kubernetes instead of plain environment variables?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "jbf-docker-dockerfile-basics-005",
      "slug": "dockerfile-env-workdir-expose-instructions",
      "question": "What do the ENV, WORKDIR, and EXPOSE instructions do in a Dockerfile?",
      "title": "ENV, WORKDIR, and EXPOSE in Dockerfile",
      "direct_answer": "WORKDIR sets the working directory inside the container; ENV sets environment variables available at runtime; EXPOSE documents which port the application listens on but does not actually publish it.",
      "layout_type": "default",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 2,
      "last_updated": "2025-01-01",
      "interviewer_intent": {
        "testing": "Dockerfile literacy \u2014 these instructions appear in every real Dockerfile",
        "common_mistake": "Thinking EXPOSE actually publishes/opens the port \u2014 it's documentation only; -p flag in docker run publishes",
        "to_stand_out": "Explain that ENV values set in Dockerfile can be overridden at runtime with docker run -e \u2014 good for configuration"
      },
      "company_tags": [
        "amazon",
        "tcs",
        "infosys"
      ],
      "answer": {
        "sections": [
          {
            "type": "speakable_answer",
            "title": "How to Answer",
            "content": "Let me go through each one. WORKDIR sets the current directory inside the container for subsequent instructions. If I write 'WORKDIR /app', then all COPY and RUN commands after it work relative to /app. It also becomes the default directory when someone runs a shell inside the container. It's cleaner than using 'cd' commands in RUN instructions. ENV sets environment variables: 'ENV SPRING_PROFILES_ACTIVE=prod'. These are available both during the build and when the container runs. ENV values in a Dockerfile can be overridden at runtime with 'docker run -e SPRING_PROFILES_ACTIVE=dev'. This is the standard way to inject configuration that differs between environments. EXPOSE is documentation only. 'EXPOSE 8080' tells people reading the Dockerfile that the application listens on port 8080. It does NOT actually publish the port or make it accessible from outside the container. To actually access the container's port, I need 'docker run -p 8080:8080 myapp'. EXPOSE is still worth including for documentation and because some tools use it."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": [
              "WORKDIR: sets working directory for subsequent RUN/COPY/CMD instructions",
              "ENV: sets environment variables available during build and at runtime",
              "ENV values overridable at runtime: docker run -e SPRING_PROFILES_ACTIVE=dev",
              "EXPOSE: documentation only \u2014 does NOT actually publish the port",
              "Port publishing: docker run -p hostPort:containerPort \u2014 this is what makes it accessible"
            ]
          }
        ]
      },
      "followup_questions": [
        "What is the ARG instruction in Dockerfile and how does it differ from ENV?",
        "How do you pass secrets to a Docker container securely?"
      ],
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P10",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Spring Boot app packaged as a Docker image",
        "familiarity_anchors": ["Dockerfile", "WORKDIR", "ENV", "EXPOSE"],
        "hook": "Three Dockerfile instructions you'll see in every real project — and one of them is purely documentation that does absolutely nothing to the running container.",
        "beats": [
          {
            "kind": "parts_or_states",
            "layout": "grouped_paragraphs",
            "groups": [
              {
                "heading": "WORKDIR",
                "text": "Sets the current directory inside the container for all instructions that follow. WORKDIR /app means COPY and RUN commands work relative to /app. It's cleaner than using cd in RUN instructions."
              },
              {
                "heading": "ENV",
                "text": "Sets environment variables available both during build and at runtime. ENV SPRING_PROFILES_ACTIVE=prod bakes a default profile into the image, but you can override it with docker run -e SPRING_PROFILES_ACTIVE=dev."
              },
              {
                "heading": "EXPOSE",
                "text": "Documents which port the application listens on. That's all it does. EXPOSE 8080 does not open port 8080. Port publishing happens at runtime with docker run -p 8080:8080."
              }
            ]
          },
          {
            "kind": "pitfalls",
            "layout": "paragraph",
            "text": "candidates miss: the most common misconception is that EXPOSE actually opens the port. It doesn't. Think of it as a comment for humans and tooling. The -p flag in docker run is what makes the port reachable."
          }
        ],
        "cap": "WORKDIR keeps your container tidy, ENV lets you configure at runtime, and EXPOSE communicates intent without doing anything. Know which one is which and you'll never confuse an interviewer.",
        "followup_handoff": [
          "What is the ARG instruction and how does it differ from ENV?",
          "How do you pass secrets securely to a Docker container?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      },
      "seo": {
        "metaTitle": "ENV WORKDIR EXPOSE Dockerfile Java Interview Answer",
        "metaDescription": "ENV, WORKDIR, and EXPOSE instructions in Dockerfile explained \u2014 environment variables, working directory, port documentation."
      },
      "order": 5
    }
  ]
}
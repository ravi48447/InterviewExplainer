[
  {
    "id": "method-level-security-002",
    "slug": "preauthorize-vs-secured-in-spring-security",
    "question": "What is the difference between @PreAuthorize and @Secured?",
    "title": "@PreAuthorize vs @Secured in Spring Security",
    "direct_answer": "@PreAuthorize evaluates a SpEL expression, so it can combine authority checks, method arguments, and calls to authorization beans. @Secured accepts a list of authority strings and cannot read method arguments or express compound SpEL rules. With @EnableMethodSecurity, pre/post annotations are enabled by default, while @Secured must be enabled with securedEnabled=true.",
    "layout_type": "comparison-arena",
    "difficulty": "easy",
    "importance": "medium",
    "reading_time_minutes": 2,
    "last_updated": "2026-06-04",
    "interviewer_intent": {
      "testing": "Ability to select the appropriate annotation based on authorization complexity requirements and understanding of their different capabilities",
      "common_mistake": "Forgetting to enable @Secured, or mixing hasRole with exact authority strings without checking the application's configured role prefix",
      "to_stand_out": "Demonstrate a practical @PreAuthorize example with parameter-based authorization that @Secured couldn't handle, showing why expression language matters"
    },
    "company_tags": [
      "tcs",
      "infosys",
      "amazon",
      "wipro"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "Two Approaches to Method Security with Different Power Levels",
          "content": "Both **@PreAuthorize** and **@Secured** protect a method before it runs, but they express rules differently. @Secured accepts one or more exact authority strings; access is allowed when the current Authentication contains a matching authority. @PreAuthorize evaluates a SpEL expression, so it can combine authority checks, read method arguments for ownership rules, inspect Authentication, or call a Spring authorization bean.\n\nThe setup also differs. With @EnableMethodSecurity, pre/post annotations are enabled by default. To use @Secured, add securedEnabled=true. Choose the simpler authority list when that is the project's established style, and use an expression when the rule genuinely needs more context."
        },
        {
          "type": "concept_map",
          "title": "Capability Gap at a Glance",
          "content": "amber|@Secured|~simple|accepts authority strings|ROLE_ is used by the default role convention|no parameter expressions\nemerald|@PreAuthorize|~expression based|uses SpEL|reads #parameters and calls beans|fits role and ownership checks\nblue|@EnableMethodSecurity defaults|~setup|pre/post annotations enabled|@Secured needs securedEnabled=true|choose one clear project style"
        },
        {
          "type": "comparison_table",
          "title": "@PreAuthorize vs @Secured Capabilities",
          "content": {
            "headers": [
              "Feature",
              "@PreAuthorize",
              "@Secured"
            ],
            "rows": [
              [
                "Expression Language",
                "SpEL expressions with full logic",
                "Authority strings; no SpEL"
              ],
              [
                "Method Parameters",
                "Can access with #paramName",
                "Cannot access parameters"
              ],
              [
                "Logical Operators",
                "AND, OR, NOT supported",
                "Not supported, multiple roles are OR"
              ],
              [
                "Role Prefix",
                "hasRole() adds the configured default prefix",
                "Matches the exact configured authority string"
              ],
              [
                "Custom Logic",
                "Can call bean methods via @beanName",
                "Cannot invoke custom logic"
              ]
            ]
          }
        },
        {
          "type": "before_code",
          "title": "@Secured Cannot Express Ownership",
          "content": "```java\n// @Secured compares exact authority strings; it cannot read method arguments.\n@Secured(\"ROLE_ADMIN\") // ROLE_ is the default convention in this example\npublic Order getOrder(Long orderId, Long userId) {\n    return orderRepository.findById(orderId).orElseThrow();\n}\n// This annotation cannot express \"the owner or an admin\" from userId.\n```\nThe stored authority must match the annotation value. ROLE_ is a common default convention, not an unchangeable rule of the annotation."
        },
        {
          "type": "after_code",
          "title": "@PreAuthorize Reads Parameters via SpEL",
          "content": "```java\n// @PreAuthorize uses SpEL: combine roles, read #params, add an admin override.\n@PreAuthorize(\"#userId == authentication.principal.id or hasRole('ADMIN')\")\npublic Order getOrder(Long orderId, Long userId) { // hasRole adds ROLE_ for you\n    return orderRepository.findById(orderId).orElseThrow();\n}\n```\nOwnership checks, logical operators, and bean calls are all possible with @PreAuthorize."
        },
        {
          "type": "when_to_use",
          "title": "Choosing Between the Two Annotations",
          "content": "Use **@PreAuthorize** for parameter-based ownership checks, combined conditions, or calls to an authorization bean. For example, a user editing only their own profile needs a parameter expression that @Secured cannot express.\n\nUse **@Secured** when an existing codebase intentionally uses simple authority strings and enables it with securedEnabled=true. Do not choose between them using an assumed performance difference; choose the annotation that expresses the rule clearly and keep the project style consistent."
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **ROLE prefix confusion**: @Secured compares exact authority strings, while hasRole adds the configured default role prefix. Check the actual GrantedAuthority values and prefix configuration.\n- **Using @Secured for ownership**: @Secured cannot inspect a method argument, so use @PreAuthorize or an authorization bean for an owner rule.\n- **Forgetting activation**: @Secured requires @EnableMethodSecurity(securedEnabled = true) with the current defaults.\n- **Mixing styles without a reason**: a consistent project convention makes authorization rules easier to review."
        },
        {
          "type": "key_points",
          "title": "Quick revision",
          "content": [
            "**@PreAuthorize power**: supports SpEL expressions, parameter access, logical operators, and custom bean methods for complex authorization",
            "**@Secured simplicity**: accepts authority strings but cannot express parameter-based or compound SpEL conditions",
            "**Parameter-based auth**: @PreAuthorize can check if hash-userId equals authentication dot principal dot id, @Secured cannot access parameters at all",
            "**Role prefix handling**: hasRole() adds the configured default prefix, while @Secured compares the authority string supplied to it",
            "**Activation**: @EnableMethodSecurity enables pre/post annotations by default; @Secured needs securedEnabled=true"
          ]
        },
        {
          "type": "speakable_answer",
          "title": "Interview answer",
          "content": "@PreAuthorize evaluates a SpEL expression, while @Secured accepts exact authority strings. That makes @PreAuthorize suitable for a rule such as an admin or the owner identified by a method argument. @Secured cannot read that argument, but it is enough for a simple authority check. Prefix behavior depends on the expression and configuration: hasRole adds the configured default prefix, whereas @Secured compares the string written in the annotation. With @EnableMethodSecurity, pre/post annotations are available by default; @Secured needs securedEnabled=true. I would choose the form that clearly expresses the rule and keep the project's authorization style consistent.",
          "answerSize": "compact"
        }
      ]
    },
    "followup_questions": [
      "How do you enable @Secured annotation support in Spring Security 6?",
      "What is @RolesAllowed and how does it compare to @Secured?",
      "What SpEL functions are available in @PreAuthorize?"
    ],
    "seo": {
      "metaTitle": "@PreAuthorize vs @Secured in Spring Security | Interview Guide",
      "metaDescription": "Compare @PreAuthorize and @Secured in Spring Security — SpEL vs role strings, method parameter access, ROLE_ prefix, when to use each."
    },
    "order": 2,
    "speakable_v2": {
      "archetype": "B",
      "pillar": "P02",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API with JWT-based authentication",
      "familiarity_anchors": [
        "Spring Security",
        "@PreAuthorize",
        "@Secured"
      ],
      "hook": "@PreAuthorize uses SpEL, while @Secured accepts authority strings and must be enabled explicitly with the current method-security defaults.",
      "beats": [
        {
          "layout": "paragraph",
          "content": "@Secured accepts one or more exact authority strings. It cannot write SpEL conditions or reference method parameters. ROLE_ is the common default role convention, but the important point is that the annotation value must match the application's GrantedAuthority value."
        },
        {
          "layout": "paragraph",
          "content": "@PreAuthorize takes a SpEL expression, which gives you full flexibility. Role checks without the prefix via hasRole, combining conditions with AND and OR, accessing method arguments with #paramName, comparing to the current user's identity — all possible."
        },
        {
          "layout": "mini_table",
          "content": {
            "headers": [
              "Feature",
              "@PreAuthorize",
              "@Secured"
            ],
            "rows": [
              [
                "Expression language",
                "SpEL — full flexibility",
                "Authority strings only"
              ],
              [
                "Method parameter access",
                "Yes — #paramName",
                "No"
              ],
              [
                "ROLE_ prefix",
                "hasRole adds the configured prefix",
                "Uses the exact annotation value"
              ],
              [
                "Enabled by default in Spring 6",
                "Yes",
                "No — needs securedEnabled=true"
              ]
            ]
          }
        },
        {
          "layout": "paragraph",
          "content": "With the current method-security defaults, @EnableMethodSecurity enables pre/post annotations such as @PreAuthorize, but @Secured remains disabled. Enable it explicitly with @EnableMethodSecurity(securedEnabled = true)."
        }
      ],
      "cap": "Use @PreAuthorize when the rule needs an expression or method data. @Secured can express an exact authority check, but it needs explicit activation with current method-security defaults.",
      "followup_handoff": [
        "@RolesAllowed is another role-oriented option from Jakarta annotations; it also needs the corresponding JSR-250 support enabled.",
        "We can also look at the SpEL functions available in @PreAuthorize in more detail."
      ],
      "tts_overrides": {},
      "speakable_status": "pending_review"
    }
  },
  {
    "id": "method-level-security-001",
    "slug": "method-level-security-in-spring-security-preauthorize",
    "question": "What is method-level security in Spring Security?",
    "title": "Method-Level Security in Spring Security — @PreAuthorize",
    "direct_answer": "Method-level security in Spring Security applies access control directly on service or controller methods using annotations like @PreAuthorize, @PostAuthorize, and @Secured. You enable it by adding @EnableMethodSecurity to your configuration class. It provides fine-grained authorization that evaluates at method invocation time, checking roles, permissions, or complex conditions before or after the method executes.",
    "layout_type": "concept-explainer",
    "difficulty": "medium",
    "importance": "high",
    "reading_time_minutes": 3,
    "last_updated": "2026-06-04",
    "interviewer_intent": {
      "testing": "Understanding of fine-grained authorization beyond URL-pattern security and awareness of annotation-driven security configuration",
      "common_mistake": "Forgetting that @EnableMethodSecurity is required to activate method security annotations, or thinking URL security alone is sufficient for all authorization needs",
      "to_stand_out": "Show a @PreAuthorize example with SpEL expressions that access method parameters, demonstrating how to implement owner-only access control"
    },
    "company_tags": [
      "amazon",
      "microsoft",
      "tcs",
      "infosys"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "Fine-Grained Authorization at the Method Level",
          "content": "Method-level security lets you protect individual methods in your Spring application with authorization rules that go beyond simple URL patterns.\n\nThink of it like room-level access control in a building — URL security is the front door guard checking if you're allowed in the building, while method security is the lock on each office door checking if you're specifically allowed in that room. You annotate methods with **@PreAuthorize**, **@PostAuthorize**, or **@Secured** to define who can call them.\n\nThe real power comes from using **SpEL expressions** (Spring Expression Language) in these annotations, which let you write complex conditions like checking if the logged-in user owns the data they're trying to access. This security layer activates when Spring creates proxies around your beans, intercepting method calls to evaluate authorization rules before the actual method code runs."
        },
        {
          "type": "concept_map",
          "title": "Method Security Building Blocks",
          "content": "blue|@EnableMethodSecurity|~the switch|Put it on a @Configuration class|Activates the security annotations|Without it, annotations are ignored\nemerald|@PreAuthorize|~before the call|Evaluates SpEL before the method runs|Reads roles and #parameters|Throws AccessDeniedException if false\nviolet|@PostAuthorize|~after the call|Inspects the returnObject|For decisions based on loaded data|Method already executed\namber|AOP proxy|~how it works|Spring wraps the bean in a proxy|Self-invocation bypasses it|Call through injected dependencies"
        },
        {
          "type": "code_example",
          "title": "Method Security in Action with Owner-Based Access",
          "content": "@Configuration\n@EnableMethodSecurity  // Required to activate method security annotations\npublic class SecurityConfig {\n}\n\n@Service\npublic class OrderService {\n    \n    @PreAuthorize(\"hasRole('ADMIN')\")\n    public List<Order> getAllOrders() {\n        return orderRepository.findAll();\n    }\n    \n    // Only order owner or admin can view\n    @PreAuthorize(\"#userId == authentication.principal.id or hasRole('ADMIN')\")\n    public Order getOrder(Long orderId, Long userId) {\n        return orderRepository.findById(orderId)\n            .orElseThrow(() -> new OrderNotFoundException(orderId));\n    }\n    \n    @PreAuthorize(\"hasAuthority('ORDER_WRITE')\")\n    public Order createOrder(Order order) {\n        order.setUserId(SecurityContextHolder.getContext().getAuthentication().getName());\n        return orderRepository.save(order);\n    }\n}"
        },
        {
          "type": "when_to_use",
          "title": "When Method-Level Security Makes Sense",
          "content": "Use method-level security when you need **data-level authorization** — for example, ensuring users can only view or modify their own orders, not someone else's by changing the ID in the URL. It's essential for **service layer protection** where methods might be called from multiple entry points like REST controllers, scheduled jobs, or internal service calls — URL security won't catch those.\n\nApply it when you need **complex authorization logic** that depends on method parameters, database state, or relationships between entities, like allowing users to edit comments only if they're the author or an admin."
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **Missing @EnableMethodSecurity**: Freshers often add @PreAuthorize annotations but forget the @EnableMethodSecurity on their configuration class, then wonder why security isn't working — the annotations do nothing without this enabler.\n- **Self-invocation bypass**: Calling a secured method from another method in the same class bypasses the security proxy, so the authorization check never runs — always call secured methods through injected dependencies.\n- **Over-relying on URL security**: Protecting only controller endpoints leaves service methods exposed to internal calls or scheduled tasks, allowing unauthorized access if someone finds another way to invoke the method."
        },
        {
          "type": "key_points",
          "title": "Quick revision",
          "content": [
            "**@EnableMethodSecurity**: required configuration annotation to activate method security, placed on any @Configuration class",
            "**@PreAuthorize**: evaluates SpEL expression before method execution, most flexible option supporting parameter access and complex logic",
            "**@PostAuthorize**: checks authorization after method executes, useful when you need to inspect the return value before allowing access",
            "**SpEL expressions**: powerful authorization conditions like hasRole('ADMIN'), #userId == authentication.principal.id, or logical combinations",
            "**Proxy-based**: Spring creates AOP proxies that intercept method calls to evaluate security rules, which is why self-invocation doesn't work"
          ]
        },
        {
          "type": "speakable_answer",
          "title": "Interview answer",
          "content": "Method-level security in Spring Security lets you protect individual methods using annotations like at-PreAuthorize right on your service or controller methods. It's like adding a lock to each room in a building, not just the front door. You enable it by putting at-EnableMethodSecurity on your configuration class — that's a step freshers often forget. The real power is using SpEL expressions in the annotations. For example, you can write at-PreAuthorize with hash-userId equals authentication dot principal dot id to make sure users can only access their own data. This is critical because URL security alone doesn't catch cases where someone changes an ID in the request to view another user's order. The way it works is Spring creates proxies around your beans that intercept method calls and check the authorization rules before letting the actual method run. One thing to watch out for is self-invocation — if you call a secured method from another method in the same class, the security check gets bypassed because you're not going through the proxy. Always call secured methods through injected dependencies. In production, you'd use method security on your service layer to enforce business rules like only order owners or admins can cancel orders, giving you fine-grained control that URL patterns can't provide.",
          "answerSize": "compact"
        }
      ]
    },
    "followup_questions": [
      "What is the difference between @PreAuthorize and @Secured?",
      "How do you access method parameters in a @PreAuthorize expression?",
      "What is @EnableMethodSecurity and what annotation did it replace?"
    ],
    "seo": {
      "metaTitle": "Method-Level Security in Spring Security | Java Fresher Interview",
      "metaDescription": "Learn method-level security in Spring Security — @EnableMethodSecurity, @PreAuthorize, @PostAuthorize, @Secured, SpEL expressions, parameter access."
    },
    "order": 1,
    "speakable_v2": {
      "archetype": "A",
      "pillar": "P02",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API with JWT-based authentication",
      "familiarity_anchors": [
        "Spring Security",
        "@PreAuthorize",
        "method security"
      ],
      "hook": "Request rules protect HTTP entry points; method security can express rules at a proxied service boundary, including checks based on method arguments.",
      "beats": [
        {
          "layout": "paragraph",
          "content": "Method-level security lets you place access rules on Spring bean methods. In the usual annotation-based setup, @EnableMethodSecurity registers the interceptors that enforce @PreAuthorize and related annotations. Without those advisors, merely writing the annotation does not enforce the rule."
        },
        {
          "layout": "paragraph",
          "content": "@PreAuthorize is the main annotation. You give it a SpEL expression, and Spring Security evaluates it before the method runs. If the expression returns false, it throws AccessDeniedException immediately — the method never executes."
        },
        {
          "layout": "paragraph",
          "content": "The real power comes from SpEL. You can do more than role checks: @PreAuthorize('#userId == authentication.principal.id') lets a user access only their own data. @PreAuthorize('hasRole(ADMIN) or #userId == authentication.principal.id') adds an admin override. URL security can't express either of those patterns."
        },
        {
          "layout": "paragraph",
          "content": "Current configuration uses @EnableMethodSecurity. It supersedes the older @EnableGlobalMethodSecurity and has different defaults, so migration code should follow the Spring Security version in use rather than assuming the old annotation behaves identically."
        }
      ],
      "cap": "Enable with @EnableMethodSecurity, annotate service methods with @PreAuthorize, write SpEL. Three steps for data-level security that URL rules can't provide.",
      "followup_handoff": [
        "@PostAuthorize is the companion annotation — it checks authorization after the method runs, with access to the return value.",
        "The difference between @PreAuthorize and the older @Secured is worth knowing — especially since @Secured needs extra setup in Spring 6."
      ],
      "tts_overrides": {},
      "speakable_status": "pending_review"
    }
  },
  {
    "id": "method-level-security-003",
    "slug": "spel-in-preauthorize-hasrole-hasauthority-isauthenticated-principal-access",
    "question": "What SpEL expressions can you use in @PreAuthorize?",
    "title": "SpEL in @PreAuthorize — hasRole, hasAuthority, isAuthenticated, Principal Access",
    "direct_answer": "Common SpEL expressions in @PreAuthorize include hasRole('ADMIN') for role checks, hasAuthority('ORDER_WRITE') for permission checks, isAuthenticated() to verify login status, #userId == authentication.principal.id for parameter-based ownership checks, and logical operators like hasRole('ADMIN') or hasRole('MANAGER') for combining conditions.",
    "layout_type": "concept-explainer",
    "difficulty": "medium",
    "importance": "medium",
    "reading_time_minutes": 2,
    "last_updated": "2026-06-04",
    "interviewer_intent": {
      "testing": "Practical knowledge of writing effective @PreAuthorize expressions for real-world authorization scenarios",
      "common_mistake": "Not knowing you can access method parameters with hash prefix or authentication.principal properties, limiting authorization to only role checks",
      "to_stand_out": "Show the owner-only access pattern with hash-userId equals authentication dot principal dot id and explain how it prevents users from accessing other users' data"
    },
    "company_tags": [
      "amazon",
      "infosys",
      "tcs",
      "cognizant"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "SpEL Expression Language for Authorization Rules",
          "content": "**SpEL (Spring Expression Language)** in @PreAuthorize lets you write authorization conditions that go far beyond simple role checks. You can access the **authentication object** to get the logged-in user's details, reference **method parameters** using the hash prefix, call **Spring beans** to implement custom logic, and combine conditions with **logical operators** like and, or, and not.\n\nThe most common patterns are role-based access with **hasRole()**, permission-based access with **hasAuthority()**, and ownership checks by comparing parameter values to the current user's ID. Think of SpEL as a mini programming language inside your annotation — you can write if-this-and-that-or-something-else logic right where you declare the security rule. The expressions evaluate at runtime when the method is called, with full access to the current security context and method invocation details."
        },
        {
          "type": "concept_map",
          "title": "SpEL Toolbox for @PreAuthorize",
          "content": "blue|Role / permission|~built-ins|hasRole('ADMIN') adds ROLE_|hasAuthority('ORDER_WRITE') exact match|hasAnyRole for multiple roles\nemerald|Identity|~current user|isAuthenticated() checks login|authentication.name is the username|authentication.principal is your UserDetails\nviolet|Parameters|~data ownership|#userId references the method arg|#userId == authentication.principal.id|Forgetting the # is a runtime error\namber|Combine|~logic|and, or, not with parentheses|@beanName.method() for custom logic|(A or B) and C style rules"
        },
        {
          "type": "code_example",
          "title": "Common SpEL Patterns in Production Code",
          "content": "@Service\npublic class UserService {\n    \n    // Simple role check\n    @PreAuthorize(\"hasRole('ADMIN')\")\n    public List<User> getAllUsers() {\n        return userRepository.findAll();\n    }\n    \n    // Permission-based (more granular than roles)\n    @PreAuthorize(\"hasAuthority('USER_DELETE')\")\n    public void deleteUser(Long userId) {\n        userRepository.deleteById(userId);\n    }\n    \n    // Owner-only access - parameter compared to logged-in user\n    @PreAuthorize(\"#userId == authentication.principal.id or hasRole('ADMIN')\")\n    public User updateProfile(Long userId, UserUpdateRequest request) {\n        var user = userRepository.findById(userId).orElseThrow();\n        user.setEmail(request.email());\n        return userRepository.save(user);\n    }\n    \n    // Combining multiple conditions\n    @PreAuthorize(\"isAuthenticated() and (#order.userId == authentication.principal.id or hasRole('SUPPORT'))\")\n    public void cancelOrder(Order order) {\n        order.setStatus(OrderStatus.CANCELLED);\n        orderRepository.save(order);\n    }\n}"
        },
        {
          "type": "when_to_use",
          "title": "Real-World SpEL Expression Scenarios",
          "content": "Use **hasRole()** or **hasAnyRole()** when authorization depends on user roles like ADMIN, USER, or MANAGER — this is your baseline protection for admin-only operations.\n\nUse **hasAuthority()** for fine-grained permissions when you have a permission system like ORDER_WRITE, USER_DELETE, or REPORT_VIEW that's more specific than broad roles.\n\nUse **parameter-based expressions** like hash-userId equals authentication dot principal dot id whenever you need owner-only access — for example, users editing their own profiles, viewing their own orders, or deleting their own comments.\n\nUse **logical operators** to combine conditions when access depends on multiple factors, like allowing both the content owner and moderators to delete posts."
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **Forgetting the hash prefix**: Writing userId instead of hash-userId in the expression means you're referencing a nonexistent variable instead of the method parameter, causing a SpEL evaluation error at runtime.\n- **Wrong principal property**: Assuming authentication dot principal dot id when your UserDetails implementation uses authentication dot principal dot userId or authentication dot name — you need to match your actual principal object structure.\n- **String comparison errors**: Comparing hash-userId equals authentication dot principal dot id when userId is Long but principal dot id is String causes type mismatch — ensure both sides have the same type or convert explicitly."
        },
        {
          "type": "key_points",
          "title": "Quick revision",
          "content": [
            "**hasRole('ADMIN')**: checks for ROLE_ADMIN with the default role prefix; use the role name expected by the configured prefix",
            "**hasAuthority('PERMISSION')**: checks for specific permission string without automatic prefix, more granular than roles",
            "**isAuthenticated()**: returns true if user is logged in (not anonymous), useful as a baseline check before other conditions",
            "**#paramName**: accesses method parameter by name using hash prefix, enables ownership and relationship-based authorization",
            "**authentication.principal**: the logged-in user object (usually UserDetails implementation) containing id, username, authorities"
          ]
        },
        {
          "type": "speakable_answer",
          "title": "Interview answer",
          "content": "The most common SpEL expressions in at-PreAuthorize are hasRole for checking user roles and hasAuthority for checking specific permissions. For example, hasRole ADMIN checks if the user has admin role, and hasAuthority ORDER underscore WRITE checks for a specific permission. The authentication object gives you access to the logged-in user's details, so you can write authentication dot principal dot id to get their user ID. The really powerful pattern is combining that with method parameters using the hash prefix. Let's say you have a method updateProfile that takes a userId parameter. You can write at-PreAuthorize hash-userId equals authentication dot principal dot id to ensure users can only update their own profile. That's the owner-only access pattern, and it's super important for data security because it prevents users from changing the ID in the URL to access someone else's data. You can combine conditions with logical operators too. For example, hasRole ADMIN or hasRole MANAGER lets either role through. Or you can write hash-userId equals authentication dot principal dot id or hasRole ADMIN to allow both owners and admins. The isAuthenticated check is useful when you just need to verify someone's logged in, without caring about their specific roles. One thing freshers miss is that you can call Spring beans in expressions. If you have a custom authorization bean, you can write at-mySecurityService dot canAccessOrder hash-orderId to delegate complex logic. Just remember the hash prefix is only for method parameters — if you forget it, SpEL won't find the variable and you'll get a runtime error.",
          "answerSize": "standard"
        }
      ]
    },
    "followup_questions": [
      "What is the difference between hasRole() and hasAuthority() in SpEL?",
      "How do you access a custom field on your UserDetails in @PreAuthorize?",
      "What does @PostAuthorize('returnObject.owner == authentication.name') do?"
    ],
    "seo": {
      "metaTitle": "SpEL in @PreAuthorize — hasRole, authentication, #param | Interview",
      "metaDescription": "Learn SpEL expressions in @PreAuthorize — hasRole, hasAuthority, isAuthenticated, #param access, authentication.principal, and owner-only access pattern."
    },
    "order": 3,
    "speakable_v2": {
      "archetype": "A",
      "pillar": "P02",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API with JWT-based authentication",
      "familiarity_anchors": [
        "Spring Security",
        "@PreAuthorize",
        "SpEL"
      ],
      "hook": "The #paramName syntax in @PreAuthorize is what makes data-level security possible — it's the difference between 'only admins' and 'only the owner or an admin'.",
      "beats": [
        {
          "layout": "paragraph",
          "content": "The basic SpEL functions are hasRole, which checks for a role with the ROLE_ prefix added automatically, hasAuthority for an exact authority string match, and isAuthenticated to verify the user is logged in."
        },
        {
          "layout": "paragraph",
          "content": "For data ownership checks, use #paramName to reference a method parameter. The expression #userId == authentication.principal.id compares the argument with the principal. An admin override can combine it with hasRole('ADMIN') using or."
        },
        {
          "layout": "paragraph",
          "content": "The authentication object gives you access to the current user's full Authentication. authentication.name returns the username. authentication.principal returns your UserDetails object, where you can access custom fields like id or email if your UserDetails implementation has them."
        },
        {
          "layout": "paragraph",
          "content": "#paramName requires Spring Security to discover that parameter name. Compile with -parameters or use a supported parameter-name annotation where needed, especially across interfaces. Test the expression because a missing name fails at runtime."
        }
      ],
      "cap": "hasRole for roles, hasAuthority for exact permissions, #paramName for method argument access, authentication.principal for the current user's object. Combine with AND and OR for complex rules.",
      "followup_handoff": [
        "@PostAuthorize has returnObject — a companion variable that gives you access to the method's return value after execution.",
        "We can also look at @PreFilter and @PostFilter for filtering collection parameters and return values."
      ],
      "tts_overrides": {},
      "speakable_status": "pending_review"
    }
  },
  {
    "id": "method-level-security-004",
    "slug": "url-based-vs-method-level-security-in-spring-when-to",
    "question": "What is the difference between URL-based and method-level security?",
    "title": "URL-Based vs Method-Level Security in Spring — When to Use Each",
    "direct_answer": "URL rules protect requests as they pass through the security filter chain. Method security protects calls that pass through a Spring AOP proxy and can use method arguments or returned data in its decision. Using both gives a useful outer and inner boundary, but proxy rules still matter: a self-invocation inside the same bean normally bypasses the method-security interceptor.",
    "layout_type": "comparison-arena",
    "difficulty": "medium",
    "importance": "high",
    "reading_time_minutes": 3,
    "last_updated": "2026-06-04",
    "interviewer_intent": {
      "testing": "Architectural understanding of security layers and when to apply each type of protection for defense in depth",
      "common_mistake": "Relying solely on URL security and missing data-level authorization, allowing users to access others' data by changing IDs in requests",
      "to_stand_out": "Explain defense in depth strategy where URL security provides baseline protection and method security adds data-level authorization, using both layers together"
    },
    "company_tags": [
      "amazon",
      "microsoft",
      "infosys",
      "tcs"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "Two Layers of Protection with Different Scopes",
          "content": "**Request authorization** runs in the servlet security filter chain and commonly protects HTTP areas by request matcher, path, and method. **Method authorization** runs through Spring method interceptors around proxied bean calls and can inspect method arguments or returned values.\n\nThey can be used together, but neither is universally required in every application. A service-boundary rule is useful when the same operation has several entry points or needs an ownership check. It applies only when the call goes through the secured Spring proxy and an appropriate SecurityContext is available. Self-invocation bypasses a proxy, and scheduled or message-driven work does not automatically have a user Authentication."
        },
        {
          "type": "concept_map",
          "title": "Two Security Layers",
          "content": "blue|Request authorization|~HTTP boundary|configured in SecurityFilterChain|matches servlet requests|runs before the controller\nemerald|Method authorization|~proxied bean boundary|@PreAuthorize on methods|reads arguments and return values|requires a proxy and SecurityContext\namber|Layering|~use where valuable|request rules reject early|method rules express business access|test each real call path"
        },
        {
          "type": "comparison_table",
          "title": "URL-Based vs Method-Level Security",
          "content": {
            "headers": [
              "Aspect",
              "URL-Based Security",
              "Method-Level Security"
            ],
            "rows": [
              [
                "Scope",
                "HTTP endpoints by path pattern",
                "Individual methods by annotation"
              ],
              [
                "Granularity",
                "Coarse - all /admin endpoints",
                "Fine - specific parameter checks"
              ],
              [
                "When Applied",
                "Filter chain before controller",
                "Method invocation via AOP proxy"
              ],
              [
                "Works For",
                "HTTP requests only",
                "Calls that cross the secured Spring proxy"
              ],
              [
                "Authorization Logic",
                "Role and path pattern matching",
                "SpEL with parameters and custom beans"
              ]
            ]
          }
        },
        {
          "type": "before_code",
          "title": "URL Security Alone Misses Ownership",
          "content": "```java\n// URL security only sees the path — any logged-in user can hit any order ID.\n@Bean\nSecurityFilterChain chain(HttpSecurity http) throws Exception {\n    return http\n        .authorizeHttpRequests(auth -> auth\n            .requestMatchers(\"/api/orders/**\").authenticated()) // path only\n        .build();\n}\n// GET /api/orders/999 succeeds even if order 999 belongs to someone else.\n```\nThe filter cannot tell whose data the ID refers to."
        },
        {
          "type": "after_code",
          "title": "Method Security Adds the Ownership Check",
          "content": "```java\n// The ownership rule runs when callers invoke this bean through its Spring proxy.\n@Service\npublic class OrderService {\n    @PostAuthorize(\"returnObject.userId == authentication.principal.id\")\n    public Order getOrder(Long orderId) {\n        return orderRepository.findById(orderId).orElseThrow();\n    }\n}\n```\nFor a proxied call with a user Authentication, the returned order is released only when its userId matches the principal."
        },
        {
          "type": "when_to_use",
          "title": "Applying Both Layers for Defense in Depth",
          "content": "Use **request authorization** to establish HTTP-level rules, such as requiring authentication for /api/** and an admin authority for /api/admin/**. It rejects requests before controller work begins.\n\nUse **method authorization** when a proxied service method is a useful business boundary or the rule depends on method arguments or returned data. Ownership can also be enforced directly in a repository query or authorization service.\n\nUsing both layers can provide defense in depth, but avoid duplicating complex policy in two places. Choose clear boundaries, account for proxy behavior, and test every supported entry path."
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **Missing ownership checks**: authentication alone does not prove a user may read every object ID. Enforce ownership in a query, authorization service, or tested method rule.\n- **Assuming every internal call is intercepted**: self-invocation and objects created outside Spring can bypass proxy-based method security.\n- **Assuming scheduled work has a user**: a scheduled job or message listener needs an explicit system identity or a separate trusted workflow; a servlet SecurityContext does not appear automatically.\n- **Duplicating rules without ownership**: if the same policy exists at two layers, keep its source clear and test both allowed and denied paths."
        },
        {
          "type": "key_points",
          "title": "Quick revision",
          "content": [
            "**URL security layer**: SecurityFilterChain configuration protecting endpoints by HTTP path patterns, operates in servlet filter chain",
            "**Method security layer**: @PreAuthorize annotations on service methods, operates via Spring AOP proxies intercepting method calls",
            "**Defense in depth**: using both layers together where URL security provides perimeter and method security enforces business rules",
            "**Invocation scope**: request security protects servlet requests; method rules apply to calls through the secured proxy with a usable SecurityContext",
            "**Granularity difference**: URL patterns match paths like /api/admin/**, methods can check if #userId equals authentication dot principal dot id"
          ]
        },
        {
          "type": "speakable_answer",
          "title": "Interview answer",
          "content": "Request authorization works in the servlet filter chain and rejects HTTP requests before the controller. Method authorization works through interceptors on Spring bean proxies and can use arguments or returned data, which makes it useful for ownership rules. For example, a request rule can require authentication for `/api/orders/**`, while `@PreAuthorize` can call `orderAuth.canRead(#id, authentication)` on the proxied service method that loads one order. Method security is not automatic for every Java call. Self-invocation bypasses the proxy, and scheduled jobs or message listeners do not automatically carry a user's SecurityContext. Choose the boundaries that match the architecture, make system identities explicit, and test each real entry path.",
          "answerSize": "standard"
        }
      ]
    },
    "followup_questions": [
      "When would you use method security instead of URL security?",
      "What is defense in depth in security?",
      "Does method security work for scheduled jobs?"
    ],
    "seo": {
      "metaTitle": "URL Security vs Method Security in Spring | Java Fresher Interview",
      "metaDescription": "Learn when to use URL-based vs method-level security in Spring — coarse-grained routing vs fine-grained data access, defense in depth, @PreAuthorize vs SecurityFilterChain."
    },
    "order": 4,
    "speakable_v2": {
      "archetype": "B",
      "pillar": "P02",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API with JWT-based authentication",
      "familiarity_anchors": [
        "Spring Security",
        "URL security",
        "method security"
      ],
      "hook": "Request security and method security protect different boundaries; the architecture decides whether one or both are useful.",
      "beats": [
        {
          "layout": "paragraph",
          "content": "Request authorization runs in the servlet filter chain and commonly matches paths and HTTP methods for coarse rules. Custom request authorization can inspect more request data, but it usually does not own the service's loaded domain object."
        },
        {
          "layout": "paragraph",
          "content": "Method security runs through Spring interceptors on a proxied bean. It can check roles, object ownership, arguments, and return values. The caller still needs to cross the proxy, and non-HTTP work needs an explicit SecurityContext or a separately trusted path."
        },
        {
          "layout": "paragraph",
          "content": "One design uses both layers: request security requires a user for /api/users/**, while a proxied service rule checks ownership. Another design may enforce ownership in the database query or an authorization service. Keep one clear source for the business rule."
        },
        {
          "layout": "paragraph",
          "content": "A service rule can protect more than one entry point, but it does not cover self-invocation or code outside the secured proxy. Review scheduled, message-driven, and internal callers explicitly instead of assuming one annotation covers every path."
        }
      ],
      "cap": "Use request rules for the HTTP boundary and method rules where a proxied service boundary adds value; neither replaces an explicit ownership policy.",
      "followup_handoff": [
        "Defense in depth as a security principle is worth elaborating on if the interviewer asks about general security approach.",
        "We can also discuss where Spring Security's AOP proxy applies — only on Spring beans, not on direct method calls within the same class."
      ],
      "tts_overrides": {},
      "speakable_status": "pending_review"
    }
  },
  {
    "id": "method-level-security-005",
    "slug": "postauthorize-in-spring-security-post-method-authorization",
    "question": "What is @PostAuthorize in Spring Security?",
    "title": "@PostAuthorize in Spring Security — Post-Method Authorization",
    "direct_answer": "@PostAuthorize evaluates a security expression after the method executes, allowing access to the method's return value through the returnObject variable in SpEL. It's used when authorization depends on the returned data, such as verifying that a loaded document belongs to the current user. If the expression evaluates to false, Spring throws AccessDeniedException even though the method already ran.",
    "layout_type": "concept-explainer",
    "difficulty": "medium",
    "importance": "medium",
    "reading_time_minutes": 2,
    "last_updated": "2026-06-04",
    "interviewer_intent": {
      "testing": "Knowledge of post-execution authorization and understanding when authorization logic depends on method return values rather than just input parameters",
      "common_mistake": "Not realizing @PostAuthorize exists and manually checking ownership after loading data in service code, or not understanding the performance implication that the method executes before the authorization check",
      "to_stand_out": "Explain the performance trade-off that the database query executes before the authorization check, so use @PostAuthorize only when authorization truly depends on return values, not when you can check parameters with @PreAuthorize"
    },
    "company_tags": [
      "amazon",
      "infosys",
      "tcs",
      "cognizant"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "Post-Execution Authorization in Spring Security",
          "content": "**@PostAuthorize** is a method-level security annotation that checks authorization AFTER the method completes execution. Unlike **@PreAuthorize** which runs before the method, @PostAuthorize has access to the **returnObject** variable in its SpEL expression, letting you make security decisions based on what the method returns. This is crucial for **ownership-based authorization** where you need to verify that the returned entity belongs to the current user. For example, when loading a document by ID, you can't know who owns it until after the database query runs. The trade-off is that the method executes fully—including database queries—before the security check happens. If authorization fails, Spring throws **AccessDeniedException** and the return value is discarded, but the work is already done.\n\nThink of it like a bouncer checking your ticket AFTER you've already walked into the venue—if it's invalid, you get kicked out, but you were briefly inside."
        },
        {
          "type": "flow_diagram",
          "title": "@PreAuthorize vs @PostAuthorize Timing",
          "content": "```mermaid\nflowchart LR\n  A[Caller invokes method] --> B{@PreAuthorize<br/>check BEFORE}\n  B -->|denied| X[AccessDenied<br/>method never runs]\n  B -->|allowed| C[Method executes<br/>returns object]\n  C --> D{@PostAuthorize<br/>check AFTER<br/>on returnObject}\n  D -->|denied| Y[AccessDenied<br/>result discarded]\n  D -->|allowed| E[Return to caller]\n```"
        },
        {
          "type": "code_example",
          "title": "Document Service with Ownership Verification",
          "content": "@Service\npublic class DocumentService {\n    private final DocumentRepository documents;\n\n    public DocumentService(DocumentRepository documents) {\n        this.documents = documents;\n    }\n\n    @PostAuthorize(\"returnObject.ownerId == authentication.principal.userId\")\n    public Document getDocument(Long documentId) {\n        return documents.findById(documentId)\n            .orElseThrow(() -> new DocumentNotFoundException(documentId));\n    }\n\n    @PostAuthorize(\"returnObject.department.teamLead == authentication.name\")\n    public ProjectReport getReport(Long reportId) {\n        return documents.findReportById(reportId);\n    }\n}\n\n// For collections, prefer a repository query that returns only rows the user may see\n// instead of loading an unrestricted list and trying to authorize it afterward."
        },
        {
          "type": "when_to_use",
          "title": "When to Use @PostAuthorize",
          "content": "Use **@PostAuthorize** when authorization depends on data loaded from the database that you can't determine from input parameters alone. Typical scenarios include **ownership verification** where you need to check if document.ownerId matches the current user, **role-based access to specific entities** where a manager can only see reports from their department after loading the full report object, and **field-level security** where authorization depends on nested properties like checking if user is the teamLead of the returned project's department. Don't use it when you can make the decision from method parameters—use @PreAuthorize instead to avoid unnecessary database queries. Also avoid it for lists where you're filtering results; use data-level filtering in the query instead."
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **Using @PostAuthorize when @PreAuthorize would work**: if inputs are enough to decide, reject before doing the work.\n- **Ignoring side effects**: the method has already run; database rollback depends on transaction boundaries, and external effects may not be reversible.\n- **Forgetting null or container return types**: an expression written for Document does not automatically work for null, Optional<Document>, or a collection. Match and test the actual return type.\n- **Loading unrestricted collections**: prefer a query that returns only authorized rows rather than filtering or rejecting a large result afterward."
        },
        {
          "type": "key_points",
          "title": "Quick revision",
          "content": [
            "**@PostAuthorize**: Runs AFTER method execution, has access to return value via returnObject in SpEL",
            "**returnObject variable**: References the actual returned object in the security expression, works with POJOs, records, and collections",
            "**Performance trade-off**: Database queries and business logic execute before authorization check happens",
            "**AccessDeniedException**: Thrown when expression evaluates to false, even though method already completed",
            "**Ownership patterns**: Primary use case is verifying returned entity belongs to current user when you can't check from parameters"
          ]
        },
        {
          "type": "speakable_answer",
          "title": "Interview answer",
          "content": "At-PostAuthorize is a Spring Security annotation that checks authorization after the method runs. The key difference from at-PreAuthorize is it has access to what the method returns through a variable called returnObject. You'd use this when your authorization decision depends on data you just loaded from the database. A common example is ownership checks. Let's say you're loading a document by ID. You don't know who owns that document until after you query the database and get the actual document object back. So you'd write at-PostAuthorize with an expression like returnObject dot ownerId equals equals authentication dot principal dot userId. If that evaluates to false, Spring throws an AccessDeniedException. The important thing to understand is the performance implication. The method already executed completely, including the database query. So if the user isn't authorized, you've wasted that query. That's why you should prefer at-PreAuthorize whenever you can make the decision from the method parameters instead of the return value. Only use at-PostAuthorize when authorization truly requires looking at the returned data.",
          "answerSize": "compact"
        }
      ]
    },
    "followup_questions": [
      "What is @PostFilter in Spring Security?",
      "What are the performance implications of @PostAuthorize vs @PreAuthorize?",
      "Can @PostAuthorize prevent side effects from already-executed methods?"
    ],
    "seo": {
      "metaTitle": "@PostAuthorize in Spring Security | Java Fresher Interview Guide",
      "metaDescription": "Learn @PostAuthorize in Spring Security — post-method authorization, returnObject access, ownership verification, @PostFilter, and performance implications."
    },
    "order": 5,
    "speakable_v2": {
      "archetype": "A",
      "pillar": "P02",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API with JWT-based authentication",
      "familiarity_anchors": [
        "Spring Security",
        "@PostAuthorize",
        "method security"
      ],
      "hook": "@PostAuthorize solves an authorization problem @PreAuthorize can't — when you don't know if access is allowed until after you've fetched the data.",
      "beats": [
        {
          "layout": "paragraph",
          "content": "@PostAuthorize evaluates its expression after the method runs. It exposes the return value through a returnObject variable in the SpEL expression. The classic case: loading a document and then checking that the owner field on the returned object matches the current user."
        },
        {
          "layout": "paragraph",
          "content": "The difference from @PreAuthorize is timing. @PostAuthorize('returnObject.ownerUsername == authentication.name') on a getDocument method means: load the document, then check ownership. If the check fails, Spring throws AccessDeniedException — but the method already ran and the database query already happened."
        },
        {
          "layout": "paragraph",
          "content": "Because the method executes first, its work occurs before the decision. Prefer a pre-execution check for writes when possible. An AccessDeniedException may roll back transactional database work depending on the transaction boundary, but external calls or already committed work may not be reversible."
        }
      ],
      "cap": "@PostAuthorize is for when you need the returned object to make the authorization decision. Use @PreAuthorize whenever you can — it prevents execution entirely, including any side effects.",
      "followup_handoff": [
        "@PostFilter filters collection return values — it removes elements from the returned list where the expression is false.",
        "We can also look at the performance implications in detail and when a manual ownership check in service code is actually cleaner."
      ],
      "tts_overrides": {},
      "speakable_status": "pending_review"
    }
  },
  {
    "id": "method-level-security-006",
    "slug": "testing-method-level-security-withmockuser-and-mockmvc",
    "question": "How do you test method-level security in Spring Boot?",
    "title": "Testing Method-Level Security — @WithMockUser and MockMvc",
    "direct_answer": "Use a Spring test with method security enabled and call the proxied bean under identities such as @WithMockUser. Verify both allowed results and denied calls, usually as AccessDeniedException at the service boundary or 403 through MockMvc. A plain unit test can test authorization logic directly, but it does not prove that Spring's method-security proxy and annotation are wired.",
    "layout_type": "concept-explainer",
    "difficulty": "medium",
    "importance": "medium",
    "reading_time_minutes": 3,
    "last_updated": "2026-06-04",
    "interviewer_intent": {
      "testing": "Practical knowledge of security testing and understanding that security rules must be validated with tests, not just assumed to work",
      "common_mistake": "Writing only happy-path tests without verifying that unauthorized users are properly blocked, or forgetting to test security rules at all",
      "to_stand_out": "Show both positive tests (authorized user succeeds) and negative tests (unauthorized user gets AccessDeniedException), demonstrating comprehensive security validation"
    },
    "company_tags": [
      "amazon",
      "infosys",
      "tcs",
      "microsoft"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "Security Testing Fundamentals in Spring Boot",
          "content": "Testing method-level security means verifying that your **@PreAuthorize**, **@Secured**, and **@RolesAllowed** annotations actually prevent unauthorized access. Spring Security provides **@WithMockUser** to simulate authenticated users in tests without setting up real authentication. This annotation creates a fake SecurityContext with a specified username, roles, and authorities. You write **positive tests** to verify authorized users can call the method successfully, and **negative tests** to verify unauthorized users get **AccessDeniedException**. For controller tests, use **MockMvc** with @WithMockUser to test the full request-response cycle including security filters. For service layer tests, inject the service directly and call methods—Spring's test context applies the security rules.\n\nThe key insight is that security isn't just about getting the syntax right; you must prove with tests that your rules work, especially the deny cases that block unauthorized access."
        },
        {
          "type": "concept_map",
          "title": "Testing Secured Methods",
          "content": "blue|@WithMockUser|~fake an authenticated user|username + roles|@WithMockUser(roles=\"ADMIN\")|Runs the SecurityContext for the test\nemerald|Assert allowed|~has the role|Call passes @PreAuthorize|Method runs normally\nrose|Assert denied|~wrong/no role|Expect AccessDeniedException|MockMvc -> 403 Forbidden"
        },
        {
          "type": "code_example",
          "title": "Complete Security Test Suite for Admin Endpoint",
          "content": "@SpringBootTest\n@AutoConfigureMockMvc\nclass OrderControllerSecurityTest {\n    @Autowired\n    private MockMvc mockMvc;\n\n    @Test\n    @WithMockUser(roles = \"ADMIN\")\n    void adminCanDeleteOrder() throws Exception {\n        mockMvc.perform(delete(\"/api/orders/123\"))\n            .andExpect(status().isNoContent());\n    }\n\n    @Test\n    @WithMockUser(roles = \"USER\")\n    void userCannotDeleteOrder() throws Exception {\n        mockMvc.perform(delete(\"/api/orders/123\"))\n            .andExpect(status().isForbidden());\n    }\n\n    @Test\n    void unauthenticatedUserCannotDeleteOrder() throws Exception {\n        // This API's configured AuthenticationEntryPoint returns 401.\n        mockMvc.perform(delete(\"/api/orders/123\"))\n            .andExpect(status().isUnauthorized());\n    }\n}"
        },
        {
          "type": "when_to_use",
          "title": "Testing Strategies for Different Security Layers",
          "content": "Use **MockMvc with @WithMockUser** to test the HTTP path, filter chain, exception handling, and proxied service call together. The expected unauthenticated result depends on the configured AuthenticationEntryPoint: an API may return 401, while form login may redirect.\n\nUse a focused **Spring test that injects the proxied service** to test method authorization without the HTTP layer. That is not a plain unit test: a directly constructed service object has no Spring security proxy, so its annotation is not enforced. Test an allowed identity, a denied authenticated identity, and missing or anonymous authentication according to the configured policy. Test custom authorization logic separately as ordinary unit-tested Java where possible."
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **Only testing happy paths**: Freshers write tests verifying admins can delete orders but forget to test that regular users can't, leaving the authorization rules unvalidated.\n- **Forgetting @EnableMethodSecurity**: Tests pass because security isn't enabled in the test context, giving false confidence—always verify method security is active in your test configuration.\n- **Wrong assertion expectations**: Testing that unauthorized access returns status OK instead of Forbidden, or not using assertThrows when directly testing services—you need to verify AccessDeniedException is actually thrown.\n- **Not testing anonymous users**: Only testing authenticated users with wrong roles misses the case where no authentication exists at all."
        },
        {
          "type": "key_points",
          "title": "Quick revision",
          "content": [
            "**@WithMockUser**: Creates fake authenticated user in test SecurityContext with specified username and roles",
            "**MockMvc testing**: Tests full request-response cycle through security filters for controller endpoints",
            "**Proxied service test**: inject the bean from a Spring context; a directly constructed object does not apply annotation advice",
            "**Positive and negative tests**: Verify authorized users succeed AND unauthorized users are blocked with AccessDeniedException",
            "**Access scenarios**: test allowed, denied authenticated, and unauthenticated behavior with the response expected from the configured entry point"
          ]
        },
        {
          "type": "speakable_answer",
          "title": "Interview answer",
          "content": "Use spring-security-test support such as @WithMockUser to place an Authentication in the test SecurityContext. Call the service through a bean injected from a Spring test context so the method-security proxy is exercised, and verify an allowed and a denied identity. A plain object created with new does not enforce @PreAuthorize. For the HTTP boundary, MockMvc can verify the filter chain, exception translation, and service rule together. An authenticated denial commonly becomes 403. The unauthenticated result comes from the configured entry point: a bearer API often returns 401, while form login may redirect. Keep separate tests for the authorization rule itself when it is implemented in an ordinary Java component.",
          "answerSize": "compact"
        }
      ]
    },
    "followup_questions": [
      "What is @WithMockUser and how does it work under the hood?",
      "What is the difference between @WithMockUser and @WithUserDetails?",
      "How do you test security in MockMvc controller tests?"
    ],
    "seo": {
      "metaTitle": "Testing Method-Level Security — @WithMockUser | Spring Interview",
      "metaDescription": "Learn to test method-level security in Spring Boot — @WithMockUser, @WithAnonymousUser, AccessDeniedException assertions, MockMvc security tests."
    },
    "order": 6,
    "speakable_v2": {
      "archetype": "A",
      "pillar": "P02",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API with JWT-based authentication",
      "familiarity_anchors": [
        "Spring Security",
        "@WithMockUser",
        "security testing"
      ],
      "hook": "Testing security means testing both the success path and the rejection path — most developers test that authorized users get in, but forget to test that unauthorized users get kicked out.",
      "beats": [
        {
          "layout": "paragraph",
          "content": "@WithMockUser is an annotation from spring-security-test that creates a mock Authentication in the SecurityContext before your test runs. By default it creates a user with username 'user' and role USER. You specify @WithMockUser(roles='ADMIN') to simulate an admin."
        },
        {
          "layout": "paragraph",
          "content": "To test authorization properly, you need two test cases per secured method. First, the success case: the correct role runs the method without throwing. Second, the rejection case: the wrong role triggers AccessDeniedException. An unauthenticated call throws AuthenticationCredentialsNotFoundException instead."
        },
        {
          "layout": "bullets",
          "content": [
            "@WithMockUser — creates a mock user with specified username and roles",
            "@WithAnonymousUser — simulates an unauthenticated request",
            "@WithUserDetails('alice') — loads the real user through your UserDetailsService",
            "AccessDeniedException — what to expect when authenticated user lacks the required role",
            "AuthenticationCredentialsNotFoundException — what to expect when no authentication exists at all"
          ]
        },
        {
          "layout": "paragraph",
          "content": "Add spring-security-test as a test dependency. @SpringBootTest is one way to create the advised bean, but it is not mandatory; a focused Spring test context that imports the service and @EnableMethodSecurity configuration can also exercise the proxy."
        }
      ],
      "cap": "Always test both the authorized path and the rejection path. @WithMockUser for role simulation, assertThrows for AccessDeniedException — that's a complete security test.",
      "followup_handoff": [
        "@WithUserDetails is more realistic than @WithMockUser — it loads the actual user from your database, which catches authorization bugs that mock users miss.",
        "MockMvc security tests for controllers use springSecurity() in the setup to apply the full filter chain."
      ],
      "tts_overrides": {},
      "speakable_status": "pending_review"
    }
  }
]

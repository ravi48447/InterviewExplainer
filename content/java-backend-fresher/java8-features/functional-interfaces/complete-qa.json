{
  "topic": "Functional Interfaces",
  "topicSlug": "functional-interfaces",
  "questions": [
    {
      "id": "functional-interfaces-001",
      "slug": "functional-interface-in-java-8",
      "question": "What is a functional interface in Java?",
      "title": "Functional Interface in Java 8",
      "direct_answer": "A functional interface has exactly one abstract method (SAM — Single Abstract Method). It can have multiple default and static methods. The @FunctionalInterface annotation verifies this at compile time. Functional interfaces are the target type for lambda expressions.",
      "layout_type": "concept-explainer",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-04",
      "interviewer_intent": {
        "testing": "Foundational Java 8 knowledge — functional interfaces are the contract for lambdas",
        "common_mistake": "Thinking @FunctionalInterface creates the constraint — the annotation only validates it",
        "to_stand_out": "Name the four key built-in functional interfaces: Predicate, Function, Consumer, Supplier"
      },
      "company_tags": [
        "tcs",
        "infosys",
        "wipro",
        "amazon",
        "microsoft"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "What Makes an Interface Functional",
            "content": "A **functional interface** is any interface with exactly one abstract method. This single abstract method (SAM) is the key — it's what allows Java to convert a lambda expression into an implementation of that interface.\n\nThink of it like a contract with one specific job: the lambda provides the implementation for that one method, and Java handles the rest. The interface can have as many **default methods** (with bodies) and **static methods** as you want — those don't count toward the SAM rule. The **@FunctionalInterface** annotation is optional but recommended: it tells the compiler to enforce the one-abstract-method rule, preventing you from accidentally adding a second abstract method later. Without functional interfaces, lambdas wouldn't have a type to target. They're the bridge between Java's object-oriented type system and the functional programming style introduced in Java 8."
          },
          {
            "type": "concept_map",
            "title": "Functional Interface at a Glance",
            "content": "amber|Functional interface|~exactly one abstract method|the SAM|target type for a lambda|@FunctionalInterface optional\nblue|Allowed extras|~default methods OK|static methods OK|Object methods don't count|equals / hashCode / toString\nemerald|Examples|~Runnable, Callable|Comparator, Predicate|Function, Supplier|your own custom SAM"
          },
          {
            "type": "code_example",
            "title": "Custom Functional Interface with Lambda Usage",
            "content": "@FunctionalInterface\npublic interface OrderValidator {\n    boolean isValid(Order order);\n    \n    // Default methods are allowed\n    default OrderValidator and(OrderValidator other) {\n        return order -> this.isValid(order) && other.isValid(order);\n    }\n}\n\nrecord Order(Long id, double amount, String status) {}\n\npublic class OrderService {\n    public List<Order> filterOrders(List<Order> orders, OrderValidator validator) {\n        return orders.stream()\n            .filter(validator::isValid)  // lambda target\n            .toList();\n    }\n    \n    public static void main(String[] args) {\n        var service = new OrderService();\n        var orders = List.of(new Order(1L, 100.0, \"PENDING\"));\n        \n        // Lambda expression implements the single abstract method\n        var valid = service.filterOrders(orders, order -> order.amount() > 50);\n    }\n}"
          },
          {
            "type": "when_to_use",
            "title": "When to Use Functional Interfaces",
            "content": "Use functional interfaces when you need to **pass behavior as a parameter** — like validation logic to a filter method, or a transformation rule to a mapper. They're essential for **Stream API operations** where you pass lambdas to filter(), map(), or forEach(). Create **custom functional interfaces** when the built-in ones (Predicate, Function, Consumer, Supplier) don't match your method signature — for example, if you need a method that takes three parameters or throws checked exceptions.\n\nUse them in **event handling** where a listener needs to react to domain events. They're the foundation for making your code more flexible and testable by treating operations as first-class values."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Adding a second abstract method**: Freshers often add another abstract method to an interface marked @FunctionalInterface, breaking the SAM rule. The compiler catches this if you use the annotation, but without it you'll get cryptic lambda compilation errors.\n- **Confusing default methods with abstract methods**: Default methods have implementations (the default keyword) so they don't count toward the one-abstract-method limit. Freshers sometimes think any method in the interface breaks the functional interface rule.\n- **Forgetting the annotation**: While @FunctionalInterface is optional, skipping it means you won't get a compile-time error if someone later adds a second abstract method, breaking all the lambdas that use your interface."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Single Abstract Method (SAM)**: exactly one abstract method defines the functional interface contract\n- **@FunctionalInterface**: optional annotation that enforces the SAM rule at compile time\n- **Default and static methods**: allowed in unlimited quantity — they don't count as abstract methods\n- **Lambda target type**: functional interfaces are what lambdas compile to — the lambda implements the single abstract method\n- **Built-in interfaces**: Java provides Predicate, Function, Consumer, Supplier for common patterns\n- **Method references**: functional interfaces enable :: syntax like String::toLowerCase\n- **Pre-Java 8 examples**: Runnable and Callable were functional interfaces before the term existed"
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer",
            "content": "A functional interface is an interface with exactly one abstract method. That's the key rule. The single abstract method is what allows you to use a lambda expression as an implementation of that interface. So when you write something like orders.filter with a lambda that takes an order and returns a boolean, that lambda is implementing a functional interface — in this case, Predicate. Now, the interface can also have default methods and static methods, as many as you want. Those don't count toward the rule because they already have implementations. The at-FunctionalInterface annotation is optional, but it's a good practice. It tells the compiler to check that you really do have exactly one abstract method. If you accidentally add a second one later, you'll get a compile error instead of breaking all your lambdas. Java provides four main built-in functional interfaces: Predicate for testing, Function for transforming, Consumer for side effects, and Supplier for producing values. But you can create your own when those don't fit. One mistake freshers make is thinking the annotation is required. It's not. Any interface with one abstract method works. Runnable is a functional interface and it doesn't have the annotation. The annotation just helps catch mistakes early."
          }
        ]
      },
      "followup_questions": [
        "What is the difference between Predicate and Function?",
        "Can a functional interface have default methods?",
        "What is @FunctionalInterface and is it required?"
      ],
      "seo": {
        "metaTitle": "Functional Interface Java 8 | Java Interview Questions for Freshers",
        "metaDescription": "What is a functional interface in Java 8? Single abstract method, @FunctionalInterface, Predicate, Function, Consumer, Supplier explained."
      },
      "order": 1,
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P01",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Java 17 app using Java 8 streams and lambdas",
        "familiarity_anchors": [
          "interfaces",
          "lambdas",
          "Predicate",
          "Function"
        ],
        "hook": "A functional interface is the contract that makes lambdas possible. Without it, the compiler doesn't know what you're implementing.",
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "A functional interface is an interface with exactly one abstract method — sometimes called a SAM, which stands for Single Abstract Method. That one method is what a lambda implements. The interface can also have default and static methods — those don't count against the one-abstract-method rule."
          },
          {
            "kind": "annotation",
            "layout": "paragraph",
            "text": "The @FunctionalInterface annotation is optional. It tells the compiler to verify that the interface really does have only one abstract method. If someone adds a second abstract method by mistake, the annotation causes a compile error. It's basically a safety net."
          },
          {
            "kind": "built_ins",
            "layout": "bullets",
            "items": [
              "Predicate takes a value and returns boolean — used in stream filter()",
              "Function takes a value and returns a different value — used in stream map()",
              "Consumer takes a value and returns nothing — used in stream forEach()",
              "Supplier takes nothing and returns a value — used in Optional.orElseGet()"
            ]
          },
          {
            "kind": "depth_marker",
            "layout": "paragraph",
            "text": "candidates miss: the @FunctionalInterface annotation does NOT create the functional interface — any interface with one abstract method is already functional. The annotation just validates it. Runnable has been a functional interface since Java 1 — it just didn't get the annotation until Java 8."
          }
        ],
        "cap": "A functional interface has exactly one abstract method — that's what makes a lambda valid. Know the four built-ins: Predicate, Function, Consumer, Supplier.",
        "followup_handoff": [
          "Can you walk me through the difference between Predicate and Function with a stream example?",
          "What happens if you put @FunctionalInterface on an interface with two abstract methods?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "functional-interfaces-002",
      "slug": "predicate-vs-function-vs-consumer-vs-supplier",
      "question": "What is the difference between Predicate, Function, Consumer, and Supplier?",
      "title": "Predicate vs Function vs Consumer vs Supplier",
      "direct_answer": "Predicate takes T and returns boolean for testing. Function takes T and returns R for transformation. Consumer takes T and returns nothing for side effects. Supplier takes nothing and returns T for production. These four patterns cover the fundamental operations: test, transform, act, and produce.",
      "layout_type": "comparison-arena",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-04",
      "interviewer_intent": {
        "testing": "Whether the candidate can name and distinguish the four key functional interfaces",
        "common_mistake": "Confusing Function and Consumer — Function returns something, Consumer does not",
        "to_stand_out": "Give a concrete real-world use case for each from Stream operations"
      },
      "company_tags": [
        "tcs",
        "infosys",
        "amazon",
        "microsoft",
        "flipkart"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "The Four Core Functional Interface Patterns",
            "content": "Java 8 provides four fundamental functional interfaces that cover nearly every operation you'll need. **Predicate** tests a condition — it takes an input and returns true or false, perfect for filtering. **Function** transforms data — it takes one type and returns another, like converting a string to its length. **Consumer** performs side effects — it takes an input and returns nothing, used when you want to do something with a value (like printing or saving to a database). **Supplier** produces values — it takes no input and returns something, useful for lazy initialization or factory patterns. These aren't random choices — they map to the four things you can do with data: test it, transform it, act on it, or create it.\n\nUnderstanding these four makes Stream API operations intuitive because almost every stream method uses one of them."
          },
          {
            "type": "concept_map",
            "title": "The Four Core Functional Interfaces",
            "content": "amber|Predicate<T>|~test(T) -> boolean|filtering conditions|and / or / negate|stream filter\nblue|Function<T,R>|~apply(T) -> R|transform / map|andThen / compose|stream map\nemerald|Consumer<T>|~accept(T) -> void|side effects|andThen chain|forEach\nviolet|Supplier<T>|~get() -> T|lazy value / factory|takes no input|orElseGet"
          },
          {
            "type": "code_example",
            "title": "Predicate, Function, Consumer, Supplier in code",
            "content": "```java\nimport java.util.function.*;\n\npublic class CoreFunctionalInterfaces {\n    public static void main(String[] args) {\n        Predicate<Integer> isEven = n -> n % 2 == 0;    // T -> boolean\n        Function<Integer, Integer> square = n -> n * n; // T -> R\n        Consumer<String> printer = System.out::println; // T -> void\n        Supplier<String> greeting = () -> \"hello\";      // () -> T\n\n        System.out.println(isEven.test(4));   // true\n        System.out.println(square.apply(5));  // 25\n        printer.accept(greeting.get());       // hello\n    }\n}\n```"
          },
          {
            "type": "comparison_table",
            "title": "Four Core Functional Interfaces Compared",
            "content": "| Interface | Signature | Purpose | Stream Example |\n|---|---|---|---|\n| Predicate<T> | T → boolean | Test/filter data | filter(order -> order.amount() > 100) |\n| Function<T,R> | T → R | Transform data | map(user -> user.email()) |\n| Consumer<T> | T → void | Perform side effect | forEach(order -> logger.info(order)) |\n| Supplier<T> | () → T | Produce/create data | generate(() -> UUID.randomUUID()) |"
          },
          {
            "type": "when_to_use",
            "title": "When to Use Each Interface",
            "content": "Use **Predicate** when you need to filter or validate — deciding which elements to keep in a collection or whether an input meets criteria.\n\nUse **Function** when you need to transform data types or extract properties — like mapping User objects to their email addresses or converting strings to integers.\n\nUse **Consumer** when you need to perform actions without returning anything — logging, saving to a database, sending notifications, or updating UI.\n\nUse **Supplier** when you need lazy initialization — deferring expensive object creation until actually needed, or providing default values in Optional.orElseGet(). In the Stream API: filter() takes Predicate, map() takes Function, forEach() takes Consumer, and generate() takes Supplier."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Using Function when Consumer is correct**: Freshers often write lambdas that ignore the return value or return null, when they should use Consumer. If you're not returning anything meaningful, you need Consumer not Function.\n- **Confusing input/output patterns**: Mixing up which interface takes what — especially confusing Supplier (no input) with Consumer (no output). The names help: Supplier supplies something, Consumer consumes something.\n- **Not using method references**: Writing user -> user.getName() instead of User::getName when a simple method reference works. While both compile, the method reference is cleaner and shows you understand the connection between functional interfaces and existing methods."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Predicate<T>**: test() method returns boolean — used for filtering and validation logic\n- **Function<T,R>**: apply() method transforms T to R — used for mapping and data transformation\n- **Consumer<T>**: accept() method returns void — used for side effects like logging or persistence\n- **Supplier<T>**: get() method produces T — used for lazy initialization and factory patterns\n- **BiFunction and BiPredicate**: two-argument versions exist for operations needing two inputs\n- **Primitive specializations**: IntPredicate, LongFunction avoid boxing for primitive types\n- **Method composition**: these interfaces have default methods like and(), andThen(), compose() for chaining"
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer",
            "content": "These four interfaces represent the four things you can do with data. Predicate is for testing — it takes an input and returns true or false. You use it in filter operations, like filtering orders where the amount is greater than a hundred. Function is for transformation — it takes one type and returns another type. You use it in map operations, like mapping a User to their email address. Consumer is for side effects — it takes an input but returns nothing. You use it when you want to do something with each element, like logging it or saving it to a database. That's forEach. Supplier is for producing values — it takes no input and returns something. You use it for lazy initialization, like generating UUIDs only when needed. The key difference freshers miss is between Function and Consumer. Function returns something, Consumer doesn't. If you're not using the return value, you probably need Consumer not Function. In the Stream API, you'll see these everywhere: filter uses Predicate, map uses Function, forEach uses Consumer, and generate uses Supplier. Understanding these four makes the entire Streams API make sense."
          }
        ]
      },
      "followup_questions": [
        "What is BiFunction and when would you use it?",
        "What is UnaryOperator?",
        "How do you chain Predicates in Java 8?"
      ],
      "seo": {
        "metaTitle": "Predicate vs Function vs Consumer vs Supplier Java | Java Interview Questions for Freshers",
        "metaDescription": "Difference between Predicate, Function, Consumer, and Supplier in Java 8 — signatures, use cases, and Stream operations for freshers."
      },
      "order": 2,
      "speakable_v2": {
        "archetype": "B",
        "pillar": "P01",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Java 17 app using Java 8 streams and lambdas",
        "familiarity_anchors": [
          "Predicate",
          "Function",
          "Consumer",
          "Supplier"
        ],
        "hook": "These four interfaces come up in almost every Java 8 interview. The trick is connecting each one to its stream operation.",
        "beats": [
          {
            "kind": "each_is",
            "layout": "mini_table",
            "columns": [
              "axis",
              "Input → Output",
              "Stream use"
            ],
            "rows": [
              {
                "axis": "Predicate",
                "values": [
                  "T → boolean",
                  "filter()"
                ]
              },
              {
                "axis": "Function",
                "values": [
                  "T → R",
                  "map()"
                ]
              },
              {
                "axis": "Consumer",
                "values": [
                  "T → void",
                  "forEach()"
                ]
              },
              {
                "axis": "Supplier",
                "values": [
                  "() → T",
                  "orElseGet(), generate()"
                ]
              }
            ]
          },
          {
            "kind": "examples",
            "layout": "paragraph",
            "text": "Predicate is your filter logic — it takes a value and says yes or no. Function is your transformer — it maps a User to a String name, for example. Consumer is for side effects — it takes a value and does something but returns nothing, like printing. Supplier creates a value on demand — it's the lazy factory you pass to orElseGet so the default only runs when needed."
          },
          {
            "kind": "depth_marker",
            "layout": "paragraph",
            "text": "candidates miss: Function and Consumer look similar but are fundamentally different. Function returns something — the pipeline can continue. Consumer returns void — it's a dead end, used at the terminal stage. Confusing them gives you confusing generics errors."
          }
        ],
        "cap": "Predicate tests, Function transforms, Consumer consumes with side effects, Supplier produces — connect each to filter, map, forEach, and orElseGet respectively.",
        "followup_handoff": [
          "What's a BiFunction and when would you reach for that instead of Function?",
          "How do you combine two Predicates using and() or or()?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "functional-interfaces-003",
      "slug": "functionalinterface-annotation-in-java",
      "question": "What is @FunctionalInterface annotation and is it required?",
      "title": "@FunctionalInterface Annotation in Java",
      "direct_answer": "@FunctionalInterface is an optional annotation that tells the compiler to verify the interface has exactly one abstract method. It is NOT required — any interface with one abstract method can be used as a functional interface. The annotation just prevents accidental addition of a second abstract method.",
      "layout_type": "concept-explainer",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-04",
      "interviewer_intent": {
        "testing": "Understanding that @FunctionalInterface is optional but recommended",
        "common_mistake": "Thinking an interface MUST have @FunctionalInterface to be used as a lambda target",
        "to_stand_out": "Mention that Runnable and Callable work as functional interfaces without @FunctionalInterface on them"
      },
      "company_tags": [
        "tcs",
        "infosys",
        "amazon",
        "microsoft"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "The Optional Safety Net for Functional Interfaces",
            "content": "The **@FunctionalInterface** annotation is a compiler directive that validates an interface follows the functional interface contract — exactly one abstract method.\n\nThink of it like @Override: it doesn't change what your code does, but it prevents mistakes. If you mark an interface with @FunctionalInterface and then add a second abstract method, the compiler immediately stops you with an error. Without the annotation, the interface would silently stop being a functional interface, and all your lambda expressions using it would break with confusing errors. The annotation is **completely optional** — Runnable, Callable, and Comparator are all functional interfaces but were created before Java 8, so they don't have the annotation. Any interface with exactly one abstract method can be used as a lambda target, annotated or not. The annotation is documentation and validation, not a requirement."
          },
          {
            "type": "concept_map",
            "title": "@FunctionalInterface at a Glance",
            "content": "amber|@FunctionalInterface|~compiler safety check|fails if 0 or 2+ abstract methods|documents intent|optional, not required\nblue|Works without it|~lambda still compiles|the annotation only guards|protects against future edits|self-documenting\nemerald|Doesn't count|~default methods|static methods|public Object methods|only the abstract SAM matters"
          },
          {
            "type": "code_example",
            "title": "Functional Interface With and Without Annotation",
            "content": "// WITHOUT annotation - still works as functional interface\ninterface PaymentProcessor {\n    void processPayment(double amount);\n}\n\n// WITH annotation - compiler enforces the rule\n@FunctionalInterface\ninterface OrderProcessor {\n    void processOrder(Order order);\n    \n    // Adding this would cause compile error with @FunctionalInterface\n    // void cancelOrder(Order order);\n    \n    // Default methods are allowed - don't count as abstract\n    default void logProcessing(Order order) {\n        System.out.println(\"Processing: \" + order.id());\n    }\n}\n\nrecord Order(Long id, double amount) {}\n\npublic class PaymentService {\n    public void executePayment(double amount, PaymentProcessor processor) {\n        processor.processPayment(amount);  // Lambda works here\n    }\n    \n    public static void main(String[] args) {\n        var service = new PaymentService();\n        // Both work with lambdas\n        service.executePayment(100.0, amt -> System.out.println(\"Paid: \" + amt));\n    }\n}"
          },
          {
            "type": "when_to_use",
            "title": "When to Use @FunctionalInterface",
            "content": "Use @FunctionalInterface when you're **designing an interface explicitly for lambda usage** — it signals your intent to other developers and catches mistakes early. Always use it for **custom functional interfaces in your codebase** where maintainability matters — if someone adds a second abstract method six months later, the compiler will catch it immediately instead of breaking all the call sites.\n\nUse it for **API design** where you're providing interfaces for clients to implement with lambdas — the annotation makes the contract explicit. Skip it only for **legacy interfaces** that happen to be functional but weren't designed as such. The annotation is especially valuable in **team environments** where multiple developers modify interfaces — it's a guard rail that prevents accidental breaking changes."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Thinking it's required**: Freshers often believe an interface won't work with lambdas unless it has @FunctionalInterface. They'll see Runnable.run() used with lambdas and wonder why it works without the annotation. The annotation is validation, not a requirement.\n- **Adding it to interfaces with multiple abstract methods**: Some freshers annotate every interface with @FunctionalInterface, then get confused by compiler errors. If your interface naturally needs two abstract methods, it's not functional and shouldn't be annotated.\n- **Forgetting default methods don't count**: Freshers sometimes hesitate to add a default method to a @FunctionalInterface, thinking it will break the annotation. Default methods have implementations, so they don't count toward the one-abstract-method rule."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Optional annotation**: any interface with one abstract method is functional, with or without the annotation\n- **Compile-time validation**: the annotation makes the compiler enforce the single-abstract-method rule\n- **Intent documentation**: it signals to developers that this interface is designed for lambda usage\n- **Legacy interfaces**: Runnable and Callable are functional interfaces without the annotation\n- **Default methods allowed**: you can add as many default methods as needed without breaking the annotation\n- **Static methods allowed**: static methods in the interface also don't count toward the abstract method limit\n- **Best practice**: always use @FunctionalInterface for custom interfaces designed for lambdas"
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer",
            "content": "The at-FunctionalInterface annotation is optional. That's the most important thing to understand. Any interface with exactly one abstract method can be used as a functional interface, whether you annotate it or not. So what does the annotation do? It tells the compiler to check that your interface really does have exactly one abstract method. If you mark an interface with at-FunctionalInterface and then accidentally add a second abstract method, the compiler will give you an error immediately. Without the annotation, the interface would just silently stop being functional, and you'd get confusing errors where you try to use lambdas with it. Think of it like at-Override. It doesn't change how your code works, but it catches mistakes. Runnable is a great example. It's been a functional interface since Java 1.0, long before the annotation existed. It has one abstract method, run, so you can use it with lambdas even though it doesn't have the annotation. The same with Callable. Should you use the annotation? Yes, for custom interfaces you're designing for lambdas. It makes your intent clear and prevents future mistakes. But don't think it's required. It's a safety net, not a requirement."
          }
        ]
      },
      "followup_questions": [
        "Does Runnable have @FunctionalInterface?",
        "What happens if you add @FunctionalInterface to an interface with two abstract methods?",
        "Can an interface with no abstract methods be a functional interface?"
      ],
      "seo": {
        "metaTitle": "@FunctionalInterface Annotation Java | Java Interview Questions for Freshers",
        "metaDescription": "Is @FunctionalInterface required in Java? Optional annotation that validates one abstract method — explained with examples for fresher interviews."
      },
      "order": 3,
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P01",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Java 17 app using Java 8 streams and lambdas",
        "familiarity_anchors": [
          "annotations",
          "Runnable",
          "compiler"
        ],
        "hook": "A lot of freshers think you must put @FunctionalInterface on an interface before using it as a lambda target. That's not true — here's what the annotation actually does.",
        "beats": [
          {
            "kind": "what_it_does",
            "layout": "paragraph",
            "text": "@FunctionalInterface is a compile-time validation annotation. It tells the compiler to verify that the interface has exactly one abstract method. If someone accidentally adds a second abstract method to the interface, the compiler flags an error right away."
          },
          {
            "kind": "not_required",
            "layout": "paragraph",
            "text": "It's not required. Any interface with exactly one abstract method is already a functional interface — the annotation just validates that. Runnable has been a valid lambda target since Java 8 even though it doesn't have @FunctionalInterface on it."
          },
          {
            "kind": "recommendation",
            "layout": "paragraph",
            "text": "Best practice: always add @FunctionalInterface when you're designing an interface to be used as a lambda target. It documents your intent clearly and protects against accidental breakage if someone later adds a method to the interface."
          },
          {
            "kind": "depth_marker",
            "layout": "paragraph",
            "text": "gotcha: if you put @FunctionalInterface on an interface that has two abstract methods, you get a compile error on the annotation line itself — not on the method that was added. This is how you'd know someone broke a functional interface contract."
          }
        ],
        "cap": "@FunctionalInterface is optional — it validates your design but doesn't create anything. Any single-abstract-method interface works as a lambda target regardless.",
        "followup_handoff": [
          "Runnable doesn't have @FunctionalInterface — how do we know it works as a lambda target?",
          "Can an interface with default methods and one abstract method still be functional?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "functional-interfaces-004",
      "slug": "composing-predicates-in-java-8",
      "question": "How do you compose two Predicates in Java 8?",
      "title": "Composing Predicates in Java 8",
      "direct_answer": "Predicate has default methods and(), or(), and negate() for composition. p1.and(p2) creates a new Predicate that is true only when both are true. p1.or(p2) is true when either is true. p.negate() inverts the Predicate. These compose predicates without nesting lambdas.",
      "layout_type": "concept-explainer",
      "difficulty": "medium",
      "importance": "medium",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-04",
      "interviewer_intent": {
        "testing": "Practical Java 8 Predicate composition — shows real usage beyond just definitions",
        "common_mistake": "Using && inside a lambda instead of and() method — works but misses the composition approach",
        "to_stand_out": "Show how composed predicates make stream filters more readable and reusable"
      },
      "company_tags": [
        "amazon",
        "microsoft",
        "flipkart",
        "tcs"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Building Complex Filters Through Composition",
            "content": "**Predicate composition** lets you build complex filtering logic from simple, reusable pieces. Instead of writing one giant lambda with nested conditions, you create small predicates and combine them with **and()**, **or()**, and **negate()** methods.\n\nThink of it like building with LEGO blocks: each predicate is a single block testing one condition, and composition methods snap them together. The and() method creates a new predicate that's true only when both predicates are true (logical AND). The or() method creates one that's true when either is true (logical OR). The negate() method inverts a predicate, turning true to false and vice versa. The power is in **reusability** — you can define predicates once and combine them in different ways across your codebase. This makes filters more readable, testable, and maintainable than inline lambdas with complex boolean expressions."
          },
          {
            "type": "concept_map",
            "title": "Composing Predicates",
            "content": "amber|and()|~both must be true|short-circuits|p1.and(p2)|AND logic\nblue|or()|~either may be true|short-circuits|p1.or(p2)|OR logic\nemerald|negate()|~flips the result|p.negate()|NOT logic|reuse a condition"
          },
          {
            "type": "code_example",
            "title": "Composing Predicates for Order Filtering",
            "content": "record Order(Long id, double amount, String status, String customerType) {}\n\npublic class OrderFilter {\n    // Define reusable predicates\n    private static final Predicate<Order> isHighValue = \n        order -> order.amount() > 1000;\n    \n    private static final Predicate<Order> isPending = \n        order -> \"PENDING\".equals(order.status());\n    \n    private static final Predicate<Order> isPremiumCustomer = \n        order -> \"PREMIUM\".equals(order.customerType());\n    \n    public List<Order> filterOrders(List<Order> orders) {\n        // Compose predicates for complex logic\n        var urgentOrders = isHighValue.and(isPending);\n        var priorityOrders = urgentOrders.or(isPremiumCustomer);\n        var nonPriorityOrders = priorityOrders.negate();\n        \n        return orders.stream()\n            .filter(priorityOrders)  // Readable, reusable\n            .toList();\n    }\n    \n    public static void main(String[] args) {\n        var filter = new OrderFilter();\n        var orders = List.of(\n            new Order(1L, 1500.0, \"PENDING\", \"REGULAR\"),\n            new Order(2L, 500.0, \"COMPLETE\", \"PREMIUM\")\n        );\n        var results = filter.filterOrders(orders);\n    }\n}"
          },
          {
            "type": "when_to_use",
            "title": "When to Use Predicate Composition",
            "content": "Use composition when you have **multiple filtering conditions that vary across use cases** — like filtering orders by status, amount, and customer type where different screens need different combinations.\n\nUse it to **avoid duplicating complex logic** — define each condition once as a named predicate, then compose them instead of copying the same lambda everywhere.\n\nUse it for **building dynamic filters** where users select which conditions to apply — you can conditionally add predicates based on user input.\n\nUse it to make **stream filters more readable** — filter(isHighValue.and(isPending)) is clearer than filter(o -> o.amount() > 1000 && \"PENDING\".equals(o.status())).\n\nUse composition when **testing filters** — you can unit-test each predicate independently, then trust that composition works correctly."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Using boolean operators instead of methods**: Freshers write order -> isHighValue(order) && isPending(order) inside a lambda instead of using isHighValue.and(isPending). Both work, but the method-based approach allows reusing composed predicates.\n- **Not extracting reusable predicates**: Freshers inline all logic in stream filters, repeating the same conditions. Extract conditions into named predicates so you can reuse and compose them.\n- **Confusing and() with logical AND**: The and() method returns a NEW predicate — it doesn't modify the existing one. Freshers sometimes think isHighValue.and(isPending) changes isHighValue. It doesn't. You must use the returned composed predicate."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **and() method**: returns a new Predicate that is true only when both predicates are true\n- **or() method**: returns a new Predicate that is true when either predicate is true\n- **negate() method**: returns a new Predicate that inverts the boolean result\n- **Immutable composition**: composition methods return new predicates without modifying originals\n- **Reusability**: define predicates once and compose them in multiple filters\n- **Readability**: composed predicates with names are clearer than complex lambda expressions\n- **Testability**: you can unit-test individual predicates before composing them"
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer",
            "content": "You compose predicates using three methods: and, or, and negate. Let's say you have two predicates. One checks if an order amount is over a thousand. Another checks if the status is pending. If you want a predicate that's true only when both conditions are met, you use the and method. So isHighValue.and(isPending) gives you a new predicate that returns true only when the order is high value AND pending. The or method works the same way but for either-or logic. If you want to invert a predicate, use negate. So isPending.negate() gives you a predicate for non-pending orders. The key thing is these methods return a NEW predicate. They don't modify the original ones. Why is this better than just using double ampersand inside a lambda? Because you can define each condition once as a named predicate and reuse it across your codebase. Instead of copying the same lambda logic everywhere, you compose predicates. It makes your stream filters way more readable. Instead of filter with a giant lambda full of boolean logic, you write filter with a composed predicate that has a meaningful name. And you can test each predicate independently. Common mistake: freshers write the boolean logic inside lambdas instead of using composition. It works, but you miss the reusability."
          }
        ]
      },
      "followup_questions": [
        "What is the difference between Predicate.and() and writing && in a lambda?",
        "How does Function.andThen() work?",
        "What is Predicate.not() and when was it added?"
      ],
      "seo": {
        "metaTitle": "Predicate Composition Java 8 | Java Interview Questions for Freshers",
        "metaDescription": "How to compose Predicates in Java 8 using and(), or(), negate()? Reusable predicate combination for stream filter operations."
      },
      "order": 4,
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P01",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Java 17 app using Java 8 streams and lambdas",
        "familiarity_anchors": [
          "Predicate",
          "stream filter",
          "logical operators"
        ],
        "hook": "Instead of cramming all your filter conditions into one lambda, you can compose separate Predicates — and that makes them reusable across your whole codebase.",
        "beats": [
          {
            "kind": "methods",
            "layout": "bullets",
            "items": [
              "and() — both predicates must return true, short-circuits on first false",
              "or() — either predicate must return true, short-circuits on first true",
              "negate() — inverts the predicate, true becomes false and vice versa"
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "You define each condition as its own Predicate, then combine them. notEmpty.and(longEnough) gives a new Predicate that's only true when both hold. You can chain as many as you need. The composed predicate is just another Predicate — pass it anywhere a Predicate is expected."
          },
          {
            "kind": "reusability",
            "layout": "paragraph",
            "text": "The real benefit is reuse. Define a notEmpty Predicate once. Define a startsWithA Predicate once. Combine them differently in different parts of your code. Versus embedding the full inline lambda everywhere — that's duplication."
          },
          {
            "kind": "depth_marker",
            "layout": "paragraph",
            "text": "non-obvious: Predicate.and() is not the same as writing and-and inside a lambda. Both work, but and() creates a composed Predicate object you can store, reuse, and test in isolation. The inline lambda is a one-off that you can't reuse without copying."
          }
        ],
        "cap": "Predicate.and(), or(), and negate() let you build complex filter conditions from simple reusable parts rather than cramming everything into one lambda.",
        "followup_handoff": [
          "Does Function have similar composition methods?",
          "What's Predicate.not() and when was it added?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "functional-interfaces-005",
      "slug": "function-andthen-vs-function-compose",
      "question": "What is Function.andThen() vs Function.compose() in Java?",
      "title": "Function.andThen() vs Function.compose()",
      "direct_answer": "f.andThen(g) means apply f first, then apply g to the result (left to right: f then g). f.compose(g) means apply g first, then apply f to the result (right to left: g then f). andThen is more intuitive for sequential thinking.",
      "layout_type": "comparison-arena",
      "difficulty": "medium",
      "importance": "medium",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-04",
      "interviewer_intent": {
        "testing": "Awareness of Function composition methods — shows depth of Java 8 knowledge",
        "common_mistake": "Getting the order backwards for compose()",
        "to_stand_out": "Show with types: f: A→B, g: B→C, f.andThen(g) gives A→C"
      },
      "company_tags": [
        "amazon",
        "microsoft",
        "flipkart"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Two Directions for Function Composition",
            "content": "Function composition lets you chain transformations, but Java gives you two ways to think about the order: **andThen()** and **compose()**.\n\nThe difference is which function runs first. With **andThen()**, you're saying \"do this function, THEN do that function\" — it reads left to right, matching how you naturally think about sequences. If f converts a string to uppercase and g counts the length, then f.andThen(g) first uppercases, then counts. With **compose()**, you're thinking backwards — if you have a function f that operates on type B, and you compose it with g that produces B from A, the result is a function from A to whatever f produces. It's right-to-left: apply g first to get B, then apply f. Most developers prefer andThen because it matches sequential thinking, but compose is useful when you're building pipelines where you want to specify the final transformation first."
          },
          {
            "type": "concept_map",
            "title": "andThen vs compose",
            "content": "amber|andThen(g)|~this first, then g|f.andThen(g) = g(f(x))|left to right|reads naturally\nblue|compose(g)|~g first, then this|f.compose(g) = f(g(x))|right to left|pre-process the input\nemerald|Tip|~same result, order differs|pick by readability|chain multiple andThen|each returns a new Function"
          },
          {
            "type": "code_example",
            "title": "andThen vs compose, same functions",
            "content": "```java\nimport java.util.function.Function;\n\npublic class AndThenVsCompose {\n    public static void main(String[] args) {\n        Function<Integer, Integer> times2 = x -> x * 2;\n        Function<Integer, Integer> plus3  = x -> x + 3;\n\n        // andThen: times2 first, then plus3 -> (5*2)+3\n        System.out.println(times2.andThen(plus3).apply(5)); // 13\n\n        // compose: plus3 first, then times2 -> (5+3)*2\n        System.out.println(times2.compose(plus3).apply(5)); // 16\n    }\n}\n```"
          },
          {
            "type": "comparison_table",
            "title": "andThen vs compose",
            "content": "| Aspect | andThen() | compose() |\n|---|---|---|\n| Execution order | f.andThen(g): f first, g second | f.compose(g): g first, f second |\n| Reading direction | Left to right (f → g) | Right to left (g → f) |\n| Mental model | Do f, THEN do g | Before f, do g |\n| Type flow | A → B (f), B → C (g), result: A → C | B → C (f), A → B (g), result: A → C |\n| Common usage | Sequential pipeline building | Wrapping with preprocessing |"
          },
          {
            "type": "when_to_use",
            "title": "When to Use Each Composition Method",
            "content": "Use **andThen()** when you're building a **sequential transformation pipeline** where each step naturally follows the previous one — like validating input, then transforming it, then formatting the result. It's the intuitive choice for most scenarios because it reads like English: \"do this, then do that.\" Use **compose()** when you're **adding preprocessing to an existing function** — if you have a function that operates on cleaned data, compose it with a sanitization function so the cleaning happens automatically before the main logic.\n\nUse compose when you're thinking about **what needs to happen before** the current function runs. In practice, andThen is more commonly used because left-to-right thinking is more natural for sequential transformations, but compose is useful in functional programming patterns where you build complex functions by wrapping simpler ones."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Getting compose order backwards**: Freshers see f.compose(g) and think f runs first because it's the object. But g runs first — compose means \"before doing f, do g.\" This backwards thinking is the most common source of bugs.\n- **Using compose when andThen is clearer**: Freshers sometimes use compose trying to seem advanced, but it makes the code harder to read. If you're thinking sequentially, use andThen.\n- **Not understanding the type constraints**: With f.andThen(g), the output type of f must match the input type of g. Freshers sometimes try to chain incompatible functions and get confused by the compiler errors."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **andThen(g)**: applies this function first, then applies g to the result — left to right execution\n- **compose(g)**: applies g first, then applies this function to the result — right to left execution\n- **Type matching**: for f.andThen(g), f's return type must match g's parameter type\n- **Immutability**: both methods return a NEW function without modifying the original functions\n- **Chaining**: you can chain multiple compositions like f.andThen(g).andThen(h)\n- **Left-to-right preference**: andThen is more intuitive for most developers and most use cases\n- **Mathematical notation**: compose matches mathematical function composition notation f∘g means f(g(x))"
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer",
            "content": "The difference is which function runs first. With andThen, you apply the first function, THEN apply the second to the result. With compose, you apply the second function first, then apply the first to that result. It's about reading direction. Let me give you a concrete example. Say you have a function that trims whitespace from a string, and another that converts to uppercase. If you do trim.andThen(toUpperCase), it trims first, then uppercases the trimmed result. That's left to right — do trim, then do uppercase. If you do toUpperCase.compose(trim), trim runs first, then the result gets uppercased. That's right to left — before uppercasing, trim. Most people prefer andThen because it matches how you think about sequences: do this, then do that. Compose is useful when you're thinking about prerequisites — like what needs to happen before the main function runs. The types matter too. For trim.andThen(toUpperCase), trim returns a string and toUpperCase takes a string, so the types line up. If they don't match, it won't compile. Common mistake: freshers think the object in f.compose(g) runs first. It doesn't. The parameter g runs first. That's what compose means — before doing f, compose it with g."
          }
        ]
      },
      "followup_questions": [
        "What is the mathematical definition of function composition?",
        "Can you chain more than two Functions with andThen?",
        "What types must align when composing Functions?"
      ],
      "seo": {
        "metaTitle": "Function andThen vs compose Java 8 | Java Interview Questions for Freshers",
        "metaDescription": "Difference between Function.andThen() and Function.compose() in Java 8 — execution order, type alignment, and when to use each."
      },
      "order": 5,
      "speakable_v2": {
        "archetype": "B",
        "pillar": "P01",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Java 17 app using Java 8 streams and lambdas",
        "familiarity_anchors": [
          "Function interface",
          "method chaining",
          "pipelines"
        ],
        "hook": "andThen and compose do the same thing — chain two functions — but in opposite orders. Getting them mixed up gives you type errors that are hard to debug.",
        "beats": [
          {
            "kind": "each_is",
            "layout": "grouped_paragraphs",
            "groups": [
              {
                "heading": "andThen",
                "text": "f.andThen(g) means: run f first, then pass the result to g. This is left-to-right order — the same order you'd read a pipeline. Most developers find this natural. f is a String-to-Integer function, g is Integer-to-String, so f.andThen(g) gives you String-to-String."
              },
              {
                "heading": "compose",
                "text": "f.compose(g) means: run g first, then pass the result to f. This is right-to-left order — it matches the mathematical notation for function composition. Less intuitive for most Java developers. In practice it's rarely used."
              }
            ]
          },
          {
            "kind": "depth_marker",
            "layout": "paragraph",
            "text": "gotcha: the types must align. If f goes from A to B, and g goes from B to C, then f.andThen(g) works and gives you A to C. But f.compose(g) would require g to go from A to A so that f can then process it. Mix up the order and the type checker catches you — but the error message isn't always obvious."
          }
        ],
        "cap": "andThen is left-to-right and natural for pipelines; compose is right-to-left and mathematical — use andThen in practice.",
        "followup_handoff": [
          "Can you chain more than two functions with andThen?",
          "What types need to align when you're composing two Functions together?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "functional-interfaces-006",
      "slug": "custom-functional-interface-in-java",
      "question": "How do you create a custom functional interface in Java?",
      "title": "Custom Functional Interface in Java",
      "direct_answer": "Define an interface with exactly one abstract method and annotate it with @FunctionalInterface. Choose the method signature to match what your lambda will do. Custom functional interfaces are needed when none of the built-in ones (Predicate, Function, Consumer, Supplier) match your signature.",
      "layout_type": "recipe-builder",
      "difficulty": "medium",
      "importance": "medium",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-04",
      "interviewer_intent": {
        "testing": "Ability to create reusable functional interfaces for domain-specific operations",
        "common_mistake": "Creating a custom functional interface when Function<T,R> or BiFunction would already work",
        "to_stand_out": "Show a practical example: a functional interface that throws checked exceptions"
      },
      "company_tags": [
        "amazon",
        "microsoft",
        "flipkart",
        "tcs"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "When Built-In Interfaces Aren't Enough",
            "content": "Creating a **custom functional interface** is straightforward: define an interface with exactly one abstract method, and annotate it with **@FunctionalInterface** for compile-time validation.\n\nThe key decision is choosing the right **method signature** — the parameter types and return type should match exactly what your domain operation needs. While Java provides Predicate, Function, Consumer, and Supplier for common patterns, you need a custom interface when you have **domain-specific requirements** that don't fit those four: multiple parameters (more than two), **checked exceptions** in the signature, or a **semantically meaningful name** that makes your code self-documenting. For example, a PaymentProcessor is clearer than Function<Payment, Receipt> — the custom interface makes the intent explicit. Custom functional interfaces also let you add **default methods** for composition logic specific to your domain."
          },
          {
            "type": "concept_map",
            "title": "Building a Custom Functional Interface",
            "content": "amber|Define|~one abstract method|add @FunctionalInterface|generics allowed|may declare throws\nblue|Why custom|~need checked exceptions|domain-specific name|more than two params|clearer intent\nemerald|Use|~assign a lambda to it|pass it as a parameter|add default combinators|reuse across calls"
          },
          {
            "type": "code_example",
            "title": "Custom Functional Interface with Checked Exceptions",
            "content": "@FunctionalInterface\npublic interface DataValidator<T> {\n    // Single abstract method that can throw checked exception\n    ValidationResult validate(T data) throws ValidationException;\n    \n    // Default method for composition\n    default DataValidator<T> and(DataValidator<T> other) {\n        return data -> {\n            var firstResult = this.validate(data);\n            if (!firstResult.isValid()) return firstResult;\n            return other.validate(data);\n        };\n    }\n}\n\nrecord ValidationResult(boolean isValid, String message) {}\nclass ValidationException extends Exception {\n    public ValidationException(String message) { super(message); }\n}\n\nrecord UserRegistration(String email, String password, int age) {}\n\npublic class RegistrationService {\n    private final DataValidator<UserRegistration> emailValidator = \n        reg -> reg.email().contains(\"@\") \n            ? new ValidationResult(true, \"Valid\") \n            : new ValidationResult(false, \"Invalid email\");\n    \n    private final DataValidator<UserRegistration> ageValidator = \n        reg -> reg.age() >= 18 \n            ? new ValidationResult(true, \"Valid\") \n            : new ValidationResult(false, \"Must be 18+\");\n    \n    public void register(UserRegistration reg) throws ValidationException {\n        var combinedValidator = emailValidator.and(ageValidator);\n        var result = combinedValidator.validate(reg);\n        if (!result.isValid()) throw new ValidationException(result.message());\n    }\n}"
          },
          {
            "type": "when_to_use",
            "title": "When to Create Custom Functional Interfaces",
            "content": "Create a custom functional interface when you need **checked exceptions in the signature** — the built-in interfaces don't throw checked exceptions, so if your operation can throw IOException or SQLException, you need a custom interface. Create one when you need **more than two parameters** — BiFunction handles two, but if your operation needs three or more inputs, a custom interface is required. Create one for **domain clarity** — OrderProcessor.process(Order) is more self-documenting than Function<Order,Receipt>.\n\nUse a custom interface when you want **domain-specific default methods** for composition — like a custom combinator method that makes sense for your business logic. Create one when you're designing a **callback API** where the name matters for understanding what the callback does — like EventListener or NotificationHandler."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Creating custom interfaces unnecessarily**: Freshers create a custom interface when Function<Order,String> would work fine. If the built-in interfaces fit, use them — they're well-known and integrate seamlessly with Stream API. Custom interfaces should add value, not just wrap existing patterns.\n- **Not using @FunctionalInterface**: Freshers skip the annotation thinking it's optional. While technically true, without it you won't get compile-time enforcement. Someone can accidentally add a second abstract method later, breaking all lambdas.\n- **Poor naming**: Naming your interface Processor or Handler is too generic. The name should clearly communicate what the operation does — OrderValidator is better than Validator, EmailSender is better than Sender."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Single abstract method**: the core requirement — define exactly one abstract method with your needed signature\n- **@FunctionalInterface annotation**: always use it for custom interfaces to get compile-time validation\n- **Checked exceptions**: custom interfaces let you declare checked exceptions in the method signature\n- **Multiple parameters**: use custom interfaces when you need more than two parameters (BiFunction limit)\n- **Domain semantics**: custom names like OrderProcessor make code more self-documenting than generic Function\n- **Default methods**: add domain-specific composition or utility methods as defaults\n- **Fallback to built-ins**: if Predicate, Function, Consumer, or Supplier fit, use them instead of custom"
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer",
            "content": "Creating a custom functional interface is simple. You define an interface with exactly one abstract method, and you annotate it with at-FunctionalInterface. The method signature should match what your operation needs — the parameter types and return type. When should you create a custom one instead of using the built-in ones? First, when you need checked exceptions. The standard functional interfaces like Function and Predicate don't declare checked exceptions. So if your operation can throw IOException or SQLException, you need a custom interface with throws in the signature. Second, when you need more than two parameters. Function takes one, BiFunction takes two. If you need three or more, you need a custom interface. Third, for domain clarity. Writing OrderProcessor.process is way clearer than Function apply. The custom name makes your code self-documenting. You can also add default methods to your custom interface for domain-specific composition logic. Like a custom combiner method that makes sense for your business rules. Common mistake: creating custom interfaces when the built-ins would work. If your operation takes one input and returns a result, that's Function. Don't create a custom interface just to wrap it. Use custom interfaces when they add real value — checked exceptions, multiple params, or meaningful names."
          }
        ]
      },
      "followup_questions": [
        "What is the difference between Function and UnaryOperator?",
        "When would you use IntFunction instead of Function?",
        "How do you handle checked exceptions in stream lambdas?"
      ],
      "seo": {
        "metaTitle": "Custom Functional Interface Java | Java Interview Questions for Freshers",
        "metaDescription": "How to create custom functional interfaces in Java 8 — when to use them, checked exception support, and domain-specific naming."
      },
      "order": 6,
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P01",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Java 17 app using Java 8 streams and lambdas",
        "familiarity_anchors": [
          "functional interfaces",
          "checked exceptions",
          "domain naming"
        ],
        "hook": "The built-in functional interfaces cover most cases, but there are two situations where you need to create your own: checked exceptions and domain-specific naming.",
        "beats": [
          {
            "kind": "how_to",
            "layout": "paragraph",
            "text": "Creating a custom functional interface is simple: define an interface with one abstract method and add @FunctionalInterface. The method signature becomes the contract your lambdas must match. That's all there is to it structurally."
          },
          {
            "kind": "when_to_create",
            "layout": "bullets",
            "items": [
              "When you need a lambda to throw a checked exception — built-in interfaces don't declare throws",
              "When you want a meaningful domain name — PasswordEncoder is clearer than Function of String to String",
              "When you need three or more input parameters — BiFunction is the most built-in offers",
              "When building a library API that should document intent clearly"
            ]
          },
          {
            "kind": "depth_marker",
            "layout": "paragraph",
            "text": "sharp edge: don't create a custom functional interface when an existing one works. If your method takes T and returns R, that's Function. If it returns boolean, that's Predicate. Reinventing the wheel creates friction for anyone using your code who expects the standard interfaces."
          }
        ],
        "cap": "Create a custom functional interface when you need checked exception support, meaningful domain names, or signatures that don't match the built-in options.",
        "followup_handoff": [
          "What's the difference between Function and UnaryOperator — when do you reach for each?",
          "How do you handle the checked exception problem inside a stream pipeline?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "functional-interfaces-007",
      "slug": "primitive-functional-interfaces-in-java-8",
      "question": "What are primitive functional interfaces like IntPredicate and LongFunction?",
      "title": "Primitive Functional Interfaces in Java 8",
      "direct_answer": "Java provides specialized primitive functional interfaces like IntPredicate, IntFunction, ToIntFunction, LongConsumer to avoid autoboxing overhead. When working with streams of primitives (IntStream, LongStream), these avoid wrapping primitives in Integer or Long wrapper objects, improving performance.",
      "layout_type": "concept-explainer",
      "difficulty": "medium",
      "importance": "low",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-04",
      "interviewer_intent": {
        "testing": "Awareness of performance considerations in Java 8 functional programming",
        "common_mistake": "Using Function<Integer, Integer> when IntUnaryOperator would avoid boxing",
        "to_stand_out": "Explain the autoboxing cost and how primitive streams (IntStream) use these automatically"
      },
      "company_tags": [
        "amazon",
        "microsoft",
        "flipkart"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Performance-Optimized Functional Interfaces for Primitives",
            "content": "**Primitive functional interfaces** are specialized versions of the standard functional interfaces that work directly with int, long, and double types instead of their wrapper classes.\n\nThe problem they solve is **autoboxing overhead** — every time you use Function<Integer,Integer>, Java has to wrap primitive ints in Integer objects and unwrap them back. For operations processing thousands or millions of numbers, this creates massive garbage collection pressure. Java provides specialized interfaces like **IntPredicate** (tests an int), **IntFunction** (transforms an int to an object), **ToIntFunction** (transforms an object to an int), **IntUnaryOperator** (int to int transformation), and similar versions for long and double. When you use **IntStream**, **LongStream**, or **DoubleStream**, these primitive streams automatically use the specialized interfaces. This isn't premature optimization — it's the standard way to process primitive data efficiently in streams."
          },
          {
            "type": "concept_map",
            "title": "Primitive Functional Interfaces",
            "content": "amber|Why they exist|~avoid autoboxing|no wrapper object churn|less GC pressure|faster numeric loops\nblue|Families|~IntPredicate / IntFunction|IntUnaryOperator|IntBinaryOperator|long and double variants\nemerald|To-primitive|~ToIntFunction<T>|ToLongFunction<T>|the double-valued variant|feed mapToInt and friends"
          },
          {
            "type": "code_example",
            "title": "Primitive Functional Interfaces in Action",
            "content": "import java.util.function.*;\nimport java.util.stream.IntStream;\n\nrecord Order(Long id, int quantity, double price) {}\n\npublic class PrimitiveInterfaceExample {\n    public static void main(String[] args) {\n        var orders = List.of(\n            new Order(1L, 10, 99.99),\n            new Order(2L, 5, 49.99),\n            new Order(3L, 15, 149.99)\n        );\n        \n        // BAD: Boxing overhead with Function<Integer,Integer>\n        Function<Integer, Integer> doubleQuantityBoxed = q -> q * 2;\n        \n        // GOOD: No boxing with IntUnaryOperator\n        IntUnaryOperator doubleQuantity = q -> q * 2;\n        \n        // ToIntFunction extracts primitive from object\n        ToIntFunction<Order> getQuantity = Order::quantity;\n        \n        // IntPredicate tests primitive values\n        IntPredicate isHighQuantity = q -> q > 10;\n        \n        // IntStream uses primitive interfaces automatically\n        var totalQuantity = orders.stream()\n            .mapToInt(getQuantity)  // Returns IntStream\n            .filter(isHighQuantity)  // Uses IntPredicate internally\n            .map(doubleQuantity)     // Uses IntUnaryOperator\n            .sum();                  // No boxing needed\n        \n        System.out.println(\"Total: \" + totalQuantity);\n    }\n}"
          },
          {
            "type": "when_to_use",
            "title": "When to Use Primitive Functional Interfaces",
            "content": "Use primitive functional interfaces when working with **IntStream, LongStream, or DoubleStream** — these streams are designed for primitives and their operations expect primitive functional interfaces.\n\nUse them when you're **processing large collections of numbers** where boxing overhead would be significant — financial calculations, data analytics, or processing sensor data.\n\nUse **ToIntFunction, ToLongFunction**, and the double-valued counterpart when extracting primitive values from objects in mapToInt and mapToLong operations.\n\nUse **IntUnaryOperator, LongUnaryOperator, DoubleUnaryOperator** for primitive-to-primitive transformations that should avoid boxing.\n\nUse **IntBinaryOperator** (and long/double versions) for reduction operations like sum or product that combine two primitive values. Don't use primitive interfaces when you're working with small datasets or when the operation already involves objects — the complexity isn't worth it for negligible performance gain."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Using boxed types with primitive streams**: Freshers write IntStream.map with a Function<Integer,Integer> instead of IntUnaryOperator, forcing boxing. The stream API methods are overloaded to accept primitive interfaces — use them.\n- **Not knowing the naming patterns**: The names follow patterns like IntPredicate (int input, boolean output), IntFunction (int input, object output), ToIntFunction (object input, int output). Freshers get confused by the names because there are many combinations.\n- **Premature optimization**: Freshers sometimes use primitive interfaces everywhere thinking it's always better. If you're working with a Stream<Order> and only need the quantity once, converting to IntStream isn't worth the readability cost. Use them when performance actually matters."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Autoboxing overhead**: standard Function<Integer,Integer> wraps primitives in objects, creating garbage\n- **IntStream integration**: primitive streams (IntStream, LongStream, DoubleStream) use specialized interfaces automatically\n- **IntPredicate**: test an int value, returns boolean — used in IntStream.filter()\n- **IntUnaryOperator**: transform int to int — used in IntStream.map()\n- **ToIntFunction**: extract int from object — used in Stream.mapToInt()\n- **Performance impact**: significant for large datasets, negligible for small operations\n- **Three primitive types**: Java provides these interfaces for int, long, and double only"
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer",
            "content": "Primitive functional interfaces are specialized versions that work directly with int, long, and double instead of their wrapper classes. The reason they exist is performance. When you use Function with Integer, every time you pass or return an int, Java has to wrap it in an Integer object. That's called autoboxing. For small operations it doesn't matter, but if you're processing thousands of numbers in a stream, all that boxing creates tons of short-lived objects that the garbage collector has to clean up. Java provides interfaces like IntPredicate for testing an int, IntUnaryOperator for transforming an int to another int, and ToIntFunction for extracting an int from an object. When you use IntStream, these are used automatically. For example, IntStream.filter expects an IntPredicate, not a Predicate with Integer. The naming can be confusing because there are many combinations. Int-Predicate means int input, boolean output. Int-Function means int input, object output. To-Int-Function means object input, int output. You use these when working with primitive streams or when you're processing large amounts of numeric data. For everyday operations with small datasets, the standard functional interfaces are fine. Don't reach for primitive interfaces everywhere — use them when performance actually matters. Common mistake: using Function with Integer when IntUnaryOperator would avoid the boxing overhead. The stream methods are overloaded to accept the primitive versions, so use them."
          }
        ]
      },
      "followup_questions": [
        "What is the difference between Stream<Integer> and IntStream?",
        "When would autoboxing cause performance issues?",
        "How do you convert an IntStream to a Stream<Integer>?"
      ],
      "seo": {
        "metaTitle": "Primitive Functional Interfaces Java 8 | Java Interview Questions for Freshers",
        "metaDescription": "What are IntPredicate, IntFunction, ToIntFunction in Java 8? Primitive functional interfaces to avoid autoboxing overhead explained."
      },
      "order": 7,
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P01",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Java 17 app using Java 8 streams and lambdas",
        "familiarity_anchors": [
          "autoboxing",
          "IntStream",
          "performance"
        ],
        "hook": "When you use Function of Integer to Integer with a million numbers, you're boxing every single int into an object. Primitive functional interfaces eliminate that entirely.",
        "beats": [
          {
            "kind": "problem",
            "layout": "paragraph",
            "text": "Every time you use Function of Integer to Integer, each primitive int gets wrapped in an Integer object. That's object allocation and garbage collection pressure for every element in your pipeline. With a small list it doesn't matter. With millions of numbers it adds up."
          },
          {
            "kind": "solution",
            "layout": "paragraph",
            "text": "Java provides primitive-specialized versions of all the main functional interfaces. IntPredicate is int to boolean — no boxing. IntUnaryOperator is int to int — no boxing. ToIntFunction takes a T and returns a plain int. The whole family exists for int, long, and double."
          },
          {
            "kind": "when_it_matters",
            "layout": "paragraph",
            "text": "When you use IntStream instead of Stream of Integer, all the operations automatically use these primitive interfaces internally. So just choosing IntStream over Stream gets you the performance benefit without having to think about which primitive interface to pick."
          },
          {
            "kind": "depth_marker",
            "layout": "paragraph",
            "text": "non-obvious: for a typical web application processing a list of 50 users, the boxing overhead is invisible. Primitive interfaces matter for batch processing, numerical algorithms, or any pipeline with millions of elements. Don't prematurely optimize — profile first."
          }
        ],
        "cap": "Primitive functional interfaces like IntPredicate and IntUnaryOperator eliminate autoboxing overhead — use them or IntStream when processing large volumes of numbers.",
        "followup_handoff": [
          "What's the difference between Stream of Integer and IntStream?",
          "How do you convert back from IntStream to a regular Stream?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    }
  ]
}

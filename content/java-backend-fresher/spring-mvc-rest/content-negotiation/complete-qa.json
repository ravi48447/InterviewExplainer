[
  {
    "id": "content-negotiation-001",
    "slug": "content-negotiation-in-spring-mvc-accept-and-content-type-headers",
    "question": "What is content negotiation in Spring MVC?",
    "title": "Content Negotiation in Spring MVC — Accept and Content-Type Headers",
    "direct_answer": "Content negotiation is the process where the client and server agree on the format of data exchanged. The client sends Accept: application/json to indicate what it wants in the response. The server selects the appropriate HttpMessageConverter (Jackson for JSON, JAXB for XML) to produce that format.",
    "layout_type": "concept-explainer",
    "difficulty": "medium",
    "importance": "medium",
    "reading_time_minutes": 3,
    "last_updated": "2026-06-04",
    "interviewer_intent": {
      "testing": "Understanding of HTTP content negotiation mechanism and how Spring MVC implements it",
      "common_mistake": "Confusing Content-Type (request body format) with Accept (desired response format) or thinking Spring always returns JSON automatically",
      "to_stand_out": "Explain both directions: Accept controls response format, Content-Type describes request body format, and show how Spring uses HttpMessageConverter to bridge them"
    },
    "company_tags": [
      "amazon",
      "infosys",
      "tcs",
      "microsoft"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "Content Negotiation: Letting Clients Choose Response Format",
          "content": "**Content negotiation** is how a REST API decides what format to send back to the client — JSON, XML, plain text, or even CSV. The client puts its preference in the **Accept** header of the HTTP request (e.g. Accept: application/json). Spring MVC reads that header, checks which **HttpMessageConverter** beans can produce that format, and picks the right one.\n\nThink of it like ordering food: you tell the restaurant 'I want Italian,' and they send you pasta instead of sushi. The restaurant (Spring) has multiple chefs (converters) — Jackson for JSON, JAXB for XML — and picks the right one based on your preference. This mechanism is built into the **DispatcherServlet** and happens before your controller method even runs. If no converter matches the Accept header, Spring returns a **406 Not Acceptable** status code. The reverse happens for incoming requests: the **Content-Type** header tells Spring which converter to use to parse the request body into a Java object."
        },
        {
          "type": "concept_map",
          "title": "Content Negotiation at a Glance",
          "content": "amber|Content negotiation|~Agree on the format|client + server|via HTTP headers|JSON, XML, etc.\nblue|Accept header|~What the client wants|Accept: application/json|server picks a converter|drives response type\nemerald|Spring's role|~Strategy + converters|matches Accept to converter|first supported wins|auto-configured\nrose|Trap|~No matching converter|returns 406|add the format dependency|JSON is the Boot default"
        },
        {
          "type": "code_example",
          "title": "Content Negotiation with Multiple Formats",
          "content": "@RestController\n@RequestMapping(\"/api/orders\")\npublic class OrderController {\n\n    @GetMapping(\"/{id}\")\n    public Order getOrder(@PathVariable Long id) {\n        // Client sends Accept: application/json → Jackson converts Order to JSON\n        // Client sends Accept: application/xml → JAXB converts Order to XML\n        return new Order(id, \"Laptop\", 1299.99);\n    }\n\n    @PostMapping(consumes = MediaType.APPLICATION_JSON_VALUE)\n    public ResponseEntity<Order> createOrder(@RequestBody Order order) {\n        // Content-Type: application/json tells Spring to use Jackson to parse body\n        var saved = orderService.save(order);\n        return ResponseEntity.status(HttpStatus.CREATED).body(saved);\n    }\n}\n\nrecord Order(Long id, String item, Double price) {}"
        },
        {
          "type": "when_to_use",
          "title": "When Content Negotiation Matters",
          "content": "Use content negotiation when you're building a public REST API that serves multiple client types. A web browser might prefer HTML, a mobile app wants JSON, and an enterprise system needs XML. Without negotiation, you'd need three separate endpoints. It's also critical when you expose the same data model to legacy systems (XML) and modern frontends (JSON). In microservices, negotiation lets you add CSV export endpoints without changing existing JSON contracts. For internal-only APIs with a single client, you can skip negotiation and just return JSON everywhere — but knowing how it works shows you understand HTTP fundamentals."
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **Mixing up Accept and Content-Type**: Freshers often think Content-Type controls both request and response. Accept is what the client wants back; Content-Type describes what the client is sending. Sending Accept: application/xml but reading Content-Type in code leads to bugs.\n- **Assuming JSON is always default**: If Jackson isn't on the classpath or no converter is registered, Spring won't return JSON. You'll get 406 errors. Always check spring-boot-starter-web includes Jackson.\n- **Ignoring 406 responses**: When you see 406 Not Acceptable in Postman, it means the Accept header doesn't match any produces value. Freshers debug the controller logic instead of checking the HTTP headers first."
        },
        {
          "type": "key_points",
          "title": "Key Points",
          "content": [
            "**Accept header**: client tells server what response format it prefers (e.g. application/json)",
            "**Content-Type header**: describes the format of the request body being sent to the server",
            "**HttpMessageConverter**: Spring interface with implementations like MappingJackson2HttpMessageConverter for JSON, Jaxb2RootElementHttpMessageConverter for XML",
            "**406 Not Acceptable**: HTTP status returned when no converter can produce the format in the Accept header",
            "**DispatcherServlet**: core Spring MVC component that performs content negotiation before routing to the controller",
            "**@RestController**: automatically enables content negotiation by using HttpMessageConverters for response serialization",
            "**MediaType constants**: Spring provides APPLICATION_JSON_VALUE, APPLICATION_XML_VALUE to avoid hardcoded strings"
          ]
        },
        {
          "type": "speakable_answer",
          "title": "How to Answer",
          "content": "Content negotiation is how the client and server agree on what format to use for the response. The client sends an Accept header like application slash json, and Spring picks the right converter to serialize the response. So if I return an Order object from my controller, and the client says it wants JSON, Spring uses Jackson to convert that Order into JSON. If the client wants XML, Spring uses JAXB instead. It's the same Java object but different output formats. The key thing is that this happens automatically in Spring MVC. The DispatcherServlet checks the Accept header, looks at which HttpMessageConverters are registered, and picks one that matches. If no converter can handle the format, Spring sends back a 406 Not Acceptable status. One mistake I see is confusing Content-Type with Accept. Content-Type describes the request body you're sending, like when you POST JSON data. Accept describes what you want back in the response. They work in opposite directions. In Spring Boot, JSON support is already there because the starter includes Jackson, so most of the time you don't think about it. But knowing how it works under the hood shows you understand HTTP and how Spring handles different data formats."
        }
      ]
    },
    "followup_questions": [
      "What is the difference between Accept and Content-Type headers?",
      "What status code does Spring return if it can't produce the requested Accept type?",
      "How do you add XML support to a Spring Boot REST API?"
    ],
    "seo": {
      "metaTitle": "Content Negotiation in Spring MVC | Java Fresher Interview",
      "metaDescription": "Learn content negotiation in Spring MVC — Accept vs Content-Type headers, HttpMessageConverter selection, JSON vs XML, 406 and 415 status codes."
    },
    "order": 1,
    "speakable_v2": {
      "question_id": "content-negotiation-001",
      "archetype": "A",
      "pillar": "P04",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API serving a mobile client",
      "familiarity_anchors": [
        "Spring MVC",
        "REST",
        "controllers"
      ],
      "hook": "Content negotiation is the HTTP handshake where the client says what format it wants and the server decides if it can deliver.",
      "beats": [
        {
          "type": "paragraph",
          "label": "what_it_is",
          "content": "Content negotiation is the process where a client and server agree on the data format. The client sends an Accept header saying what format it wants in the response. Spring looks through its registered HttpMessageConverters to find one that can produce that format."
        },
        {
          "type": "paragraph",
          "label": "why_it_exists",
          "content": "There are two directions to this. The Accept header controls what format comes back in the response — the server selects a converter to serialize the output. The Content-Type header describes what format the client is sending in the request body — the server selects a converter to deserialize the input."
        },
        {
          "type": "bullets",
          "label": "parts_or_states",
          "content": [
            "Accept: application/json: client wants JSON back — Spring uses Jackson.",
            "Accept: application/xml: client wants XML — Spring needs jackson-dataformat-xml.",
            "Content-Type: application/json: client is sending JSON — Jackson deserializes it.",
            "406 Not Acceptable: no registered converter can produce the requested format.",
            "415 Unsupported Media Type: no registered converter can read the incoming Content-Type."
          ]
        },
        {
          "type": "paragraph",
          "label": "pitfalls",
          "content": "candidates miss: If the client doesn't send an Accept header, Spring defaults to JSON when Jackson is configured. Many developers don't know content negotiation is even happening because JSON just works by default. The mechanism only becomes visible when clients request a different format or send an unsupported Content-Type."
        }
      ],
      "cap": "Use the produces attribute on your mapping annotations to explicitly declare what formats an endpoint supports, rather than relying on global negotiation silently.",
      "followup_handoff": [
        "What is the difference between Accept and Content-Type headers?",
        "How do you add XML support to a Spring Boot REST API?"
      ],
      "tts_overrides": {
        "HttpMessageConverter": "Http Message Converter",
        "HttpMessageConverters": "Http Message Converters",
        "Accept": "Accept header",
        "Content-Type": "Content Type header",
        "jackson-dataformat-xml": "jackson dataformat xml",
        "406": "four oh six",
        "415": "four fifteen",
        "JSON": "J-S-O-N",
        "XML": "X-M-L",
        "HTTP": "H-T-T-P"
      },
      "speakable_status": "pending_review"
    }
  },
  {
    "id": "content-negotiation-002",
    "slug": "produces-attribute-in-getmapping-restricting-response-content-type",
    "question": "What is the produces attribute in @GetMapping?",
    "title": "produces Attribute in @GetMapping — Restricting Response Content Type",
    "direct_answer": "The produces attribute on mapping annotations like @GetMapping restricts which response content types this endpoint can produce. If the client's Accept header doesn't match any produces value, Spring returns 406 Not Acceptable before even reaching the controller method.",
    "layout_type": "concept-explainer",
    "difficulty": "easy",
    "importance": "medium",
    "reading_time_minutes": 2,
    "last_updated": "2026-06-04",
    "interviewer_intent": {
      "testing": "Knowledge of endpoint-level content type constraints and how they interact with Accept headers",
      "common_mistake": "Not knowing produces exists or thinking Spring automatically supports all formats without configuration",
      "to_stand_out": "Show both produces and consumes together on a POST endpoint to demonstrate understanding of bidirectional constraints"
    },
    "company_tags": [
      "tcs",
      "infosys",
      "wipro",
      "accenture"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "Constraining Endpoint Response Formats with produces",
          "content": "The **produces** attribute is a filter that tells Spring 'this endpoint can only return these specific content types.' When a request comes in, Spring checks the client's **Accept** header against the produces list before routing to your controller method. If there's no match, Spring immediately returns **406 Not Acceptable** and your code never runs.\n\nThis is useful when you have endpoints that only support JSON but not XML, or when you want to create separate endpoints for different formats of the same resource. The produces value must match a **MediaType** constant like application/json or application/xml. You can list multiple types: produces = {MediaType.APPLICATION_JSON_VALUE, MediaType.APPLICATION_XML_VALUE}. This early filtering saves CPU cycles by rejecting incompatible requests before deserialization or business logic runs. Without produces, Spring tries all registered converters, which can cause unexpected behavior if multiple converters claim they can handle the same format."
        },
        {
          "type": "concept_map",
          "title": "The produces Attribute",
          "content": "amber|produces|~Restricts output type|on @GetMapping|produces = APPLICATION_JSON|narrows the route\nblue|Matching|~Against Accept header|client must accept it|else 406 Not Acceptable|content negotiation\nemerald|Use for|~Multiple formats|JSON vs XML endpoints|same path, different produces|version via media type\nrose|Trap|~Mismatch with Accept|gives 406|client sends wrong Accept|align both sides"
        },
        {
          "type": "code_example",
          "title": "Using produces and consumes Together",
          "content": "@RestController\n@RequestMapping(\"/api/products\")\npublic class ProductController {\n\n    @GetMapping(value = \"/{id}\", produces = MediaType.APPLICATION_JSON_VALUE)\n    public Product getProductJson(@PathVariable Long id) {\n        // Only accepts requests with Accept: application/json\n        return productService.findById(id);\n    }\n\n    @GetMapping(value = \"/{id}/xml\", produces = MediaType.APPLICATION_XML_VALUE)\n    public Product getProductXml(@PathVariable Long id) {\n        // Only accepts requests with Accept: application/xml\n        return productService.findById(id);\n    }\n\n    @PostMapping(\n        consumes = MediaType.APPLICATION_JSON_VALUE,\n        produces = MediaType.APPLICATION_JSON_VALUE\n    )\n    public ResponseEntity<Product> createProduct(@RequestBody Product product) {\n        // consumes: only accepts JSON request body (checks Content-Type)\n        // produces: only returns JSON response (checks Accept)\n        var saved = productService.save(product);\n        return ResponseEntity.status(HttpStatus.CREATED).body(saved);\n    }\n}"
        },
        {
          "type": "comparison_table",
          "title": "produces vs consumes Attributes",
          "content": {
            "headers": [
              "Aspect",
              "produces",
              "consumes"
            ],
            "rows": [
              [
                "Controls",
                "Response format",
                "Request body format"
              ],
              [
                "Checks header",
                "Accept header",
                "Content-Type header"
              ],
              [
                "Used on",
                "All mapping annotations",
                "POST, PUT, PATCH only"
              ],
              [
                "Mismatch status",
                "406 Not Acceptable",
                "415 Unsupported Media Type"
              ],
              [
                "Example value",
                "application/json",
                "application/json"
              ]
            ]
          }
        },
        {
          "type": "when_to_use",
          "title": "When to Use produces",
          "content": "Use produces when you want explicit control over response formats. If your API only supports JSON and you don't want to handle XML requests, add produces = MediaType.APPLICATION_JSON_VALUE to prevent 406 errors from being confusing. It's also useful when you have multiple endpoints serving the same resource in different formats — one for JSON at /api/user/123, another for CSV at /api/user/123/export with produces = text/csv. In versioned APIs, you can use custom media types like application/vnd.company.v2+json in produces to route to different controller methods based on version. For public APIs, produces documents what formats each endpoint supports, making it easier for clients to integrate. Skip produces if you're building an internal API where all consumers use JSON and you want Spring's default behavior."
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **Using hardcoded strings instead of constants**: Writing produces = \"application/json\" instead of MediaType.APPLICATION_JSON_VALUE. The constant prevents typos and makes refactoring easier.\n- **Forgetting produces only filters GET/HEAD**: For POST endpoints, you need both consumes (for request body) and produces (for response). Freshers often set only produces and wonder why their POST fails with 415 when sending JSON.\n- **Not understanding 406 vs 415**: 406 means the Accept header doesn't match produces. 415 means the Content-Type doesn't match consumes. Freshers see these status codes and think it's a Spring bug instead of a header mismatch.\n- **Overly restrictive produces on all endpoints**: Setting produces on every endpoint when you only return JSON creates maintenance overhead. Use it selectively where you need format constraints."
        },
        {
          "type": "key_points",
          "title": "Key Points",
          "content": [
            "**produces attribute**: restricts which content types an endpoint can return based on the Accept header",
            "**Early filtering**: Spring checks produces before routing to the controller method, returning 406 if no match",
            "**Multiple formats**: produces accepts an array like {APPLICATION_JSON_VALUE, APPLICATION_XML_VALUE} for flexible endpoints",
            "**consumes counterpart**: consumes filters incoming request body formats by checking Content-Type header",
            "**406 Not Acceptable**: status code returned when Accept header doesn't match any produces value",
            "**MediaType constants**: use APPLICATION_JSON_VALUE, TEXT_PLAIN_VALUE instead of hardcoded strings",
            "**Method-level specificity**: produces on a method overrides class-level @RequestMapping produces"
          ]
        },
        {
          "type": "speakable_answer",
          "title": "How to Answer",
          "content": "The produces attribute tells Spring what content types this endpoint can return. So if I write produces equals MediaType dot APPLICATION_JSON_VALUE on a GetMapping, that endpoint will only respond to requests where the Accept header is application slash json. If a client sends Accept colon application slash xml, Spring returns a 406 Not Acceptable status before my controller method even runs. This is useful when I want to be explicit about formats. For example, I might have one endpoint that returns JSON and another that exports the same data as CSV. I'd use different produces values to route requests correctly. The opposite of produces is consumes, which filters the request body format. So on a PostMapping, I'd use consumes to say 'only accept JSON' and produces to say 'only return JSON.' One thing that trips people up is confusing 406 and 415 errors. 406 means the Accept header doesn't match produces. 415 means the Content-Type doesn't match consumes. They're checking different headers in different directions. In real projects, I'd use produces when I need format control or when I'm exposing multiple formats of the same resource. For simple JSON APIs, I usually skip it and let Spring use the default converters."
        }
      ]
    },
    "followup_questions": [
      "What is the difference between produces and consumes in Spring MVC?",
      "What HTTP status is returned when produces doesn't match the Accept header?",
      "How do you implement content-type-based API versioning in Spring?"
    ],
    "seo": {
      "metaTitle": "produces and consumes in Spring MVC | Content Negotiation Interview",
      "metaDescription": "Learn produces and consumes attributes in Spring MVC mapping annotations — restricting response and request content types, 406 and 415 status codes."
    },
    "order": 2,
    "speakable_v2": {
      "question_id": "content-negotiation-002",
      "archetype": "B",
      "pillar": "P04",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API serving a mobile client",
      "familiarity_anchors": [
        "Spring MVC",
        "REST",
        "controllers"
      ],
      "hook": "produces and consumes are how you declare your endpoint's content type contract explicitly — instead of leaving it to Spring's defaults.",
      "beats": [
        {
          "type": "paragraph",
          "label": "what_each_is",
          "content": "produces restricts what response formats this endpoint can output. Spring checks it against the client's Accept header. If they don't match, the request is rejected with 406 before your method is even called."
        },
        {
          "type": "paragraph",
          "label": "what_each_is",
          "content": "consumes restricts what request body formats this endpoint will accept. Spring checks it against the client's Content-Type header. If they don't match, the request is rejected with 415 before your method is called."
        },
        {
          "type": "mini_table",
          "label": "differences",
          "headers": [
            "Attribute",
            "Checked against",
            "Error when no match"
          ],
          "rows": [
            [
              "produces",
              "Client's Accept header",
              "406 Not Acceptable"
            ],
            [
              "consumes",
              "Client's Content-Type header",
              "415 Unsupported Media Type"
            ]
          ]
        },
        {
          "type": "paragraph",
          "label": "when_to_pick",
          "content": "Use MediaType constants instead of raw strings — MediaType.APPLICATION_JSON_VALUE instead of the string 'application/json'. Constants avoid typos and give IDE autocomplete. Multiple formats are supported: an array of values in produces means the endpoint can serve both JSON and XML."
        },
        {
          "type": "paragraph",
          "label": "tiny_example",
          "content": "non-obvious: produces and consumes can be set at the class level on @RequestMapping to apply to all methods in a controller. You'd typically do this if your entire controller is JSON-only, rather than repeating it on every method."
        }
      ],
      "cap": "Without specifying produces, Spring uses global content negotiation with all registered converters. Specifying produces makes the contract explicit and the API self-documenting.",
      "followup_handoff": [
        "What HTTP status is returned when produces doesn't match the Accept header?",
        "How do you implement content-type-based API versioning in Spring?"
      ],
      "tts_overrides": {
        "produces": "produces attribute",
        "consumes": "consumes attribute",
        "Accept": "Accept header",
        "Content-Type": "Content Type header",
        "MediaType": "Media Type",
        "APPLICATION_JSON_VALUE": "APPLICATION JSON VALUE",
        "406": "four oh six",
        "415": "four fifteen",
        "@RequestMapping": "at-RequestMapping",
        "JSON": "J-S-O-N",
        "XML": "X-M-L"
      },
      "speakable_status": "pending_review"
    }
  },
  {
    "id": "content-negotiation-003",
    "slug": "spring-boot-json-auto-configuration-jackson-and-objectmapper",
    "question": "How does Spring Boot auto-configure JSON support?",
    "title": "Spring Boot JSON Auto-Configuration — Jackson and ObjectMapper",
    "direct_answer": "Spring Boot auto-configures JSON support through JacksonAutoConfiguration when jackson-databind is on the classpath. It creates a default ObjectMapper bean, registers MappingJackson2HttpMessageConverter, and sets application/json as the default produced content type for RestController methods.",
    "layout_type": "concept-explainer",
    "difficulty": "medium",
    "importance": "medium",
    "reading_time_minutes": 2,
    "last_updated": "2026-06-04",
    "interviewer_intent": {
      "testing": "Understanding of Spring Boot auto-configuration for JSON and how Jackson is integrated",
      "common_mistake": "Not knowing that spring-boot-starter-web already includes Jackson or trying to manually configure ObjectMapper without understanding auto-configuration",
      "to_stand_out": "Show how to customize ObjectMapper with application.properties or a custom Bean to override defaults"
    },
    "company_tags": [
      "tcs",
      "infosys",
      "amazon",
      "wipro"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "How Spring Boot Wires Up JSON Without Configuration",
          "content": "Spring Boot's **auto-configuration** for JSON happens through the **JacksonAutoConfiguration** class, which runs when **jackson-databind** is detected on the classpath. Since **spring-boot-starter-web** includes Jackson by default, you get JSON support out of the box without any XML or properties files.\n\nSpring Boot creates an **ObjectMapper** bean configured with sensible defaults: date serialization as ISO-8601 timestamps, ignoring unknown properties during deserialization, and proper handling of Java 8 time types through the JavaTimeModule. It then registers **MappingJackson2HttpMessageConverter** as a converter bean, which tells the DispatcherServlet 'I can convert Java objects to and from application/json.' When you return an object from a **@RestController** method, Spring checks the Accept header, sees application/json, finds this converter, and uses the ObjectMapper to serialize your object. This entire setup is conditional — if you add your own ObjectMapper bean, Spring Boot backs off and uses yours instead. This 'opinionated defaults with easy override' pattern is core to Spring Boot's philosophy."
        },
        {
          "type": "concept_map",
          "title": "Spring Boot JSON Auto-Configuration",
          "content": "amber|JacksonAutoConfiguration|~Triggers on classpath|jackson-databind present|creates an ObjectMapper bean|zero config\nblue|Converter|~MappingJackson2HttpMessageConverter|registered automatically|JSON read + write|for all controllers\nemerald|Customize|~Define an ObjectMapper bean|or application.properties|spring.jackson.*|dates, null handling\nrose|Trap|~Two ObjectMappers|custom backs off default|@ConditionalOnMissingBean|don't duplicate"
        },
        {
          "type": "code_example",
          "title": "Customizing Jackson ObjectMapper",
          "content": "@Configuration\npublic class JacksonConfig {\n\n    @Bean\n    public ObjectMapper objectMapper() {\n        var mapper = new ObjectMapper();\n        // Include non-null fields only (don't serialize null values)\n        mapper.setSerializationInclusion(JsonInclude.Include.NON_NULL);\n        // Use snake_case for JSON field names instead of camelCase\n        mapper.setPropertyNamingStrategy(PropertyNamingStrategies.SNAKE_CASE);\n        // Write dates as \"2024-01-15\" instead of timestamps\n        mapper.registerModule(new JavaTimeModule());\n        mapper.disable(SerializationFeature.WRITE_DATES_AS_TIMESTAMPS);\n        return mapper;\n    }\n}\n\n@RestController\nclass UserController {\n    @GetMapping(\"/api/users/{id}\")\n    public User getUser(@PathVariable Long id) {\n        // This User will be serialized using our custom ObjectMapper\n        return new User(id, \"john_doe\", null); // null email won't appear in JSON\n    }\n}\n\nrecord User(Long id, String username, String email) {}"
        },
        {
          "type": "when_to_use",
          "title": "When to Customize Jackson",
          "content": "Use the default auto-configured ObjectMapper for most internal APIs where you control both client and server. Customize it when you need specific JSON formatting: snake_case field names for Python clients, excluding null fields to reduce payload size, or formatting dates as strings instead of timestamps for readability. If you're integrating with a legacy system that expects specific JSON structure, create a custom ObjectMapper bean. For APIs with strict JSON-LD or HAL requirements, register custom serializers. In microservices with shared DTOs, you might define a custom ObjectMapper in a shared library to ensure consistent JSON across all services. If you only need per-field customization, use Jackson annotations like @JsonProperty or @JsonFormat on your model classes instead of modifying the global ObjectMapper."
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **Manually adding Jackson dependency**: Freshers add jackson-databind to pom.xml when spring-boot-starter-web already includes it. This can cause version conflicts. Always check what your starter includes first.\n- **Not understanding auto-configuration override**: Creating a Bean named objectMapper but not realizing Spring Boot's default is still active because they misspelled the method or used wrong scope. The custom bean must match the name and return type exactly.\n- **Using @Autowired ObjectMapper in config**: Trying to inject ObjectMapper in the same @Configuration class that creates it causes circular dependencies. Define it as a @Bean method, don't inject it.\n- **Ignoring application.properties**: Spring Boot lets you configure common ObjectMapper settings via properties like spring.jackson.default-property-inclusion=non_null. Freshers write a whole @Bean when a single property would work."
        },
        {
          "type": "key_points",
          "title": "Key Points",
          "content": [
            "**JacksonAutoConfiguration**: Spring Boot class that auto-configures Jackson when jackson-databind is on classpath",
            "**spring-boot-starter-web**: includes jackson-databind by default, so no manual dependency needed",
            "**ObjectMapper bean**: central Jackson object that handles Java-to-JSON serialization and deserialization",
            "**MappingJackson2HttpMessageConverter**: Spring converter that uses ObjectMapper to process application/json requests and responses",
            "**Customization via Bean**: define your own ObjectMapper @Bean to override default configuration",
            "**application.properties**: configure common settings like spring.jackson.serialization.indent-output=true for pretty-printing",
            "**Conditional auto-configuration**: Spring Boot backs off if you define your own ObjectMapper, following the opinionated defaults with easy override pattern"
          ]
        },
        {
          "type": "speakable_answer",
          "title": "How to Answer",
          "content": "Spring Boot auto-configures JSON support through a class called JacksonAutoConfiguration. When Spring Boot starts, it checks if Jackson is on the classpath. Since spring-boot-starter-web includes jackson-databind by default, it always finds it. Then Spring Boot creates an ObjectMapper bean with sensible defaults like handling Java 8 dates correctly and ignoring unknown JSON fields during deserialization. It also registers MappingJackson2HttpMessageConverter, which is the bridge between Spring MVC and Jackson. When I return an object from a RestController method, that converter uses the ObjectMapper to turn my Java object into JSON. The cool thing is I don't have to configure any of this. It just works out of the box. If I need to customize it, I can create my own ObjectMapper bean in a Configuration class. For example, I might want to exclude null fields from JSON or use snake_case instead of camelCase for field names. Spring Boot sees my custom bean and uses that instead of the default. I can also configure some things through application dot properties, like spring dot jackson dot default-property-inclusion equals non_null. One mistake people make is adding Jackson to their dependencies manually when it's already there. Another is not knowing that defining a custom ObjectMapper overrides all the defaults, so you have to re-enable features you want to keep. Understanding this auto-configuration shows you know how Spring Boot reduces boilerplate without hiding too much magic."
        }
      ]
    },
    "followup_questions": [
      "How do you configure Jackson to serialize Java 8 LocalDateTime as ISO-8601 string?",
      "What does fail-on-unknown-properties=false do in Jackson?",
      "What is the difference between Jackson2ObjectMapperBuilderCustomizer and defining an ObjectMapper bean?"
    ],
    "seo": {
      "metaTitle": "Spring Boot JSON Auto-Configuration — Jackson | Interview Guide",
      "metaDescription": "Learn how Spring Boot auto-configures Jackson for JSON — JacksonAutoConfiguration, ObjectMapper customization, properties configuration, JavaTimeModule."
    },
    "order": 3,
    "speakable_v2": {
      "question_id": "content-negotiation-003",
      "archetype": "A",
      "pillar": "P04",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API serving a mobile client",
      "familiarity_anchors": [
        "Spring MVC",
        "REST",
        "controllers"
      ],
      "hook": "spring-boot-starter-web already includes Jackson — Spring Boot wires it up without a single line of configuration from you.",
      "beats": [
        {
          "type": "paragraph",
          "label": "what_it_is",
          "content": "When spring-boot-starter-web is on the classpath, Spring Boot's JacksonAutoConfiguration activates. It creates a default ObjectMapper bean, registers MappingJackson2HttpMessageConverter, and makes JSON the default response format for all @RestController methods."
        },
        {
          "type": "paragraph",
          "label": "why_it_exists",
          "content": "You can customize the ObjectMapper through application.properties without writing any code. Setting spring.jackson.serialization.write-dates-as-timestamps=false makes dates serialize as ISO strings. Setting spring.jackson.default-property-inclusion=NON_NULL skips null fields in every response."
        },
        {
          "type": "bullets",
          "label": "parts_or_states",
          "content": [
            "application.properties: configure global ObjectMapper behavior — indent output, date format, null handling.",
            "@Bean ObjectMapper: define your own bean for full control — Spring Boot backs off and uses yours.",
            "Jackson2ObjectMapperBuilderCustomizer: add to the auto-configured settings without replacing them entirely.",
            "@JsonIgnoreProperties(ignoreUnknown=true): on a DTO class — silently ignore unknown fields during deserialization."
          ]
        },
        {
          "type": "paragraph",
          "label": "pitfalls",
          "content": "sharp edge: Defining an @Bean ObjectMapper replaces the entire auto-configured one. You lose any customizations Boot applied. For most cases, Jackson2ObjectMapperBuilderCustomizer is the safer choice — it applies on top of the auto-configuration rather than replacing it."
        }
      ],
      "cap": "JavaTimeModule handles Java 8 LocalDate and LocalDateTime serialization. Spring Boot usually auto-registers it — verify by testing a date field in your response.",
      "followup_handoff": [
        "How do you configure Jackson to serialize LocalDateTime as ISO-8601 string?",
        "What is the difference between Jackson2ObjectMapperBuilderCustomizer and defining an ObjectMapper bean?"
      ],
      "tts_overrides": {
        "spring-boot-starter-web": "spring boot starter web",
        "JacksonAutoConfiguration": "Jackson Auto Configuration",
        "ObjectMapper": "Object Mapper",
        "MappingJackson2HttpMessageConverter": "Mapping Jackson 2 Http Message Converter",
        "@RestController": "at-RestController",
        "Jackson2ObjectMapperBuilderCustomizer": "Jackson 2 Object Mapper Builder Customizer",
        "@JsonIgnoreProperties": "at-JsonIgnoreProperties",
        "JavaTimeModule": "Java Time Module",
        "LocalDate": "Local Date",
        "LocalDateTime": "Local Date Time",
        "DTO": "D-T-O",
        "JSON": "J-S-O-N",
        "ISO": "I-S-O"
      },
      "speakable_status": "pending_review"
    }
  },
  {
    "id": "content-negotiation-004",
    "slug": "mediatype-in-spring-mvc-content-type-constants",
    "question": "What is MediaType in Spring MVC?",
    "title": "MediaType in Spring MVC — Content Type Constants",
    "direct_answer": "MediaType is a Spring class containing constants for MIME type strings used in produces, consumes, and Content-Type headers. Key constants include APPLICATION_JSON_VALUE for application/json, APPLICATION_XML_VALUE, TEXT_PLAIN_VALUE, and MULTIPART_FORM_DATA_VALUE. Using these constants prevents typos and makes code more maintainable.",
    "layout_type": "concept-explainer",
    "difficulty": "easy",
    "importance": "medium",
    "reading_time_minutes": 2,
    "last_updated": "2026-06-04",
    "interviewer_intent": {
      "testing": "Awareness of MediaType constants as best practice over hardcoded strings",
      "common_mistake": "Using hardcoded strings like application slash json instead of MediaType.APPLICATION_JSON_VALUE everywhere",
      "to_stand_out": "Mention MediaType can be parsed, compared, and checked for compatibility using methods like isCompatibleWith"
    },
    "company_tags": [
      "tcs",
      "infosys",
      "wipro",
      "accenture"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "MediaType: Type-Safe MIME Type Constants",
          "content": "**MediaType** is a Spring Framework utility class in the org.springframework.http package that provides constants for **MIME types** (also called media types or content types). Instead of writing the string \"application/json\" everywhere, you use **MediaType.APPLICATION_JSON_VALUE**.\n\nThis prevents typos like \"applicaton/json\" that would cause 406 or 415 errors at runtime. MediaType has two forms: the constant itself (e.g. MediaType.APPLICATION_JSON) which is a MediaType object, and the _VALUE suffix (e.g. MediaType.APPLICATION_JSON_VALUE) which is the raw string \"application/json\". For annotations like @GetMapping(produces = ...), you use the _VALUE form because annotations require compile-time constants. The MediaType class also has methods for parsing media type strings, comparing types, and checking compatibility. For example, MediaType.APPLICATION_JSON.isCompatibleWith(MediaType.ALL) returns true. This is useful when writing custom logic that needs to handle multiple formats. Using MediaType constants is a **Spring best practice** that improves code readability and catches errors at compile time."
        },
        {
          "type": "concept_map",
          "title": "MediaType Constants",
          "content": "amber|MediaType|~MIME type constants|APPLICATION_JSON_VALUE|for produces/consumes|avoids typos\nblue|_VALUE suffix|~String vs object|..._VALUE is the String|use in annotations|MediaType object elsewhere\nemerald|Common ones|~JSON, XML, plain|APPLICATION_JSON|APPLICATION_XML|TEXT_PLAIN\nrose|Trap|~Hardcoding strings|\"aplication/json\" typo|use the constant|compile-safe"
        },
        {
          "type": "code_example",
          "title": "Using MediaType Constants in Controllers",
          "content": "@RestController\n@RequestMapping(\"/api/files\")\npublic class FileController {\n\n    @GetMapping(value = \"/{id}\", produces = MediaType.APPLICATION_JSON_VALUE)\n    public FileMetadata getFileInfo(@PathVariable String id) {\n        return new FileMetadata(id, \"document.pdf\", 2048000L);\n    }\n\n    @PostMapping(\n        value = \"/upload\",\n        consumes = MediaType.MULTIPART_FORM_DATA_VALUE,\n        produces = MediaType.APPLICATION_JSON_VALUE\n    )\n    public ResponseEntity<FileMetadata> uploadFile(\n        @RequestParam(\"file\") MultipartFile file\n    ) {\n        // consumes checks Content-Type: multipart/form-data\n        var metadata = fileService.save(file);\n        return ResponseEntity.ok(metadata);\n    }\n\n    @GetMapping(value = \"/{id}/download\", produces = MediaType.APPLICATION_OCTET_STREAM_VALUE)\n    public ResponseEntity<byte[]> downloadFile(@PathVariable String id) {\n        var fileData = fileService.getFileBytes(id);\n        return ResponseEntity.ok()\n            .header(\"Content-Disposition\", \"attachment; filename=\\\"file.pdf\\\"\")\n            .body(fileData);\n    }\n}\n\nrecord FileMetadata(String id, String filename, Long size) {}"
        },
        {
          "type": "when_to_use",
          "title": "When MediaType Constants Matter",
          "content": "Use MediaType constants in every @RequestMapping, @GetMapping, @PostMapping annotation where you specify produces or consumes. This catches typos at compile time instead of runtime. When building file upload endpoints, use MediaType.MULTIPART_FORM_DATA_VALUE. For file downloads, use MediaType.APPLICATION_OCTET_STREAM_VALUE. If you're exposing CSV exports, use MediaType.TEXT_PLAIN_VALUE or define custom types. In custom HttpMessageConverter implementations, use the MediaType object form to check compatibility: if (mediaType.isCompatibleWith(MediaType.APPLICATION_JSON)) { ... }. For APIs that return different formats based on complex logic, parse the Accept header into MediaType objects and compare them programmatically. In tests, use MediaType constants in MockMvc: mockMvc.perform(get(...).accept(MediaType.APPLICATION_JSON)). This makes tests more readable and refactoring safer."
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **Using the wrong constant form**: Writing produces = MediaType.APPLICATION_JSON (the object) instead of MediaType.APPLICATION_JSON_VALUE (the string). Annotations need compile-time string constants, so you get a compilation error.\n- **Hardcoding strings**: Writing produces = \"application/json\" everywhere. One typo like \"applicaiton/json\" breaks the endpoint at runtime with 406 errors. MediaType constants prevent this.\n- **Not knowing common types**: Freshers often don't know MediaType.MULTIPART_FORM_DATA_VALUE exists for file uploads or MediaType.TEXT_HTML_VALUE for HTML responses. They hardcode these and wonder why their code looks inconsistent.\n- **Ignoring isCompatibleWith**: When writing custom logic, freshers do string equality checks like mediaType.equals(\"application/json\") instead of using MediaType's built-in compatibility methods that handle wildcards like application slash star."
        },
        {
          "type": "key_points",
          "title": "Key Points",
          "content": [
            "**MediaType class**: Spring utility in org.springframework.http providing constants for MIME types",
            "**_VALUE suffix**: string constant form used in annotations like APPLICATION_JSON_VALUE for produces and consumes",
            "**Object form**: MediaType.APPLICATION_JSON is a MediaType object with methods for parsing and comparison",
            "**Common constants**: APPLICATION_JSON_VALUE, APPLICATION_XML_VALUE, TEXT_PLAIN_VALUE, MULTIPART_FORM_DATA_VALUE, APPLICATION_OCTET_STREAM_VALUE",
            "**Compile-time safety**: using constants catches typos at compile time instead of runtime 406/415 errors",
            "**isCompatibleWith method**: checks if one MediaType is compatible with another, handling wildcards like application slash star",
            "**Best practice**: always use MediaType constants instead of hardcoded strings for maintainability"
          ]
        },
        {
          "type": "speakable_answer",
          "title": "How to Answer",
          "content": "MediaType is a Spring class that holds constants for MIME types, which are the format strings like application slash json or text slash html. Instead of writing the string directly, I use MediaType dot APPLICATION_JSON_VALUE. The reason is it prevents typos. If I write 'application slash jason' by mistake, my endpoint returns 406 errors and it's hard to debug. With the constant, I get a compile error immediately if I mistype it. There are two forms of each constant. The one with _VALUE at the end like APPLICATION_JSON_VALUE is a string, and that's what I use in annotations like GetMapping produces. The one without _VALUE like APPLICATION_JSON is a MediaType object, and that has methods for comparing and checking compatibility. For example, if I'm writing custom logic that needs to check if a request is JSON, I can use isCompatibleWith to handle wildcards. Common constants I use are APPLICATION_JSON_VALUE for REST APIs, MULTIPART_FORM_DATA_VALUE for file uploads, and APPLICATION_OCTET_STREAM_VALUE for binary file downloads. A mistake I've seen is people using the wrong form in annotations. They write produces equals MediaType dot APPLICATION_JSON without the _VALUE, and it doesn't compile because annotations need string literals. Using MediaType constants is just a best practice. It makes code more readable and safer because you're not scattering string literals everywhere. In a real project, every produces and consumes should use these constants."
        }
      ]
    },
    "followup_questions": [
      "What is the difference between MediaType.APPLICATION_JSON and MediaType.APPLICATION_JSON_VALUE?",
      "What MediaType do you use for file uploads in Spring MVC?",
      "How do you set the Content-Type header on an HttpHeaders object?"
    ],
    "seo": {
      "metaTitle": "MediaType in Spring MVC — Content Type Constants | Interview",
      "metaDescription": "Learn MediaType constants in Spring MVC — APPLICATION_JSON_VALUE, MULTIPART_FORM_DATA_VALUE, APPLICATION_XML_VALUE, and MediaType object vs _VALUE String."
    },
    "order": 4,
    "speakable_v2": {
      "question_id": "content-negotiation-004",
      "archetype": "A",
      "pillar": "P04",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API serving a mobile client",
      "familiarity_anchors": [
        "Spring MVC",
        "REST",
        "controllers"
      ],
      "hook": "MediaType constants are the type-safe way to reference MIME types — no more typos in 'application/json'.",
      "beats": [
        {
          "type": "paragraph",
          "label": "what_it_is",
          "content": "MediaType is a Spring class that provides constants for MIME type strings. Instead of hardcoding the string 'application/json' in your annotations — which can be misspelled — you use MediaType.APPLICATION_JSON_VALUE."
        },
        {
          "type": "paragraph",
          "label": "parts_or_states",
          "content": "There are two forms to know. The VALUE variant — like APPLICATION_JSON_VALUE — is a plain String constant. Use it in annotation attributes like produces and consumes. The non-VALUE variant — like APPLICATION_JSON — is a MediaType object. Use it when calling methods that take a MediaType parameter, like HttpHeaders.setContentType."
        },
        {
          "type": "bullets",
          "label": "parts_or_states",
          "content": [
            "APPLICATION_JSON_VALUE: for REST API responses — the most common.",
            "APPLICATION_XML_VALUE: for XML responses.",
            "MULTIPART_FORM_DATA_VALUE: for file upload endpoints.",
            "APPLICATION_OCTET_STREAM_VALUE: for binary file downloads.",
            "TEXT_PLAIN_VALUE: for plain text responses."
          ]
        },
        {
          "type": "paragraph",
          "label": "pitfalls",
          "content": "non-obvious: A common mistake is using APPLICATION_JSON where APPLICATION_JSON_VALUE is needed and vice versa. If you see a compile error that a String was expected but a MediaType was provided, you need the _VALUE variant. If you see that a MediaType was expected but a String was provided, drop the _VALUE suffix."
        }
      ],
      "cap": "Using constants over string literals is a small thing that prevents bugs and makes code searchable — refactoring tools can find all usages of APPLICATION_JSON_VALUE, but not all hardcoded strings.",
      "followup_handoff": [
        "What is the difference between MediaType.APPLICATION_JSON and MediaType.APPLICATION_JSON_VALUE?",
        "What MediaType do you use for file uploads in Spring MVC?"
      ],
      "tts_overrides": {
        "MediaType": "Media Type",
        "APPLICATION_JSON_VALUE": "APPLICATION JSON VALUE",
        "APPLICATION_JSON": "APPLICATION JSON",
        "APPLICATION_XML_VALUE": "APPLICATION XML VALUE",
        "MULTIPART_FORM_DATA_VALUE": "MULTIPART FORM DATA VALUE",
        "APPLICATION_OCTET_STREAM_VALUE": "APPLICATION OCTET STREAM VALUE",
        "TEXT_PLAIN_VALUE": "TEXT PLAIN VALUE",
        "HttpHeaders": "Http Headers",
        "setContentType": "set Content Type",
        "MIME": "M-I-M-E",
        "produces": "produces attribute",
        "consumes": "consumes attribute"
      },
      "speakable_status": "pending_review"
    }
  },
  {
    "id": "content-negotiation-005",
    "slug": "content-type-vs-accept-http-headers-request-and-response-format",
    "question": "What is the difference between Content-Type and Accept HTTP headers?",
    "title": "Content-Type vs Accept HTTP Headers — Request and Response Format",
    "direct_answer": "Content-Type describes the format of the request body being sent to the server. Accept tells the server what response format the client prefers. Example: POST /orders with Content-Type: application/json means you're sending JSON data, while Accept: application/xml means you want the response in XML format.",
    "layout_type": "comparison-arena",
    "difficulty": "easy",
    "importance": "high",
    "reading_time_minutes": 2,
    "last_updated": "2026-06-04",
    "interviewer_intent": {
      "testing": "Understanding of HTTP content negotiation mechanics and request/response header directionality",
      "common_mistake": "Confusing Content-Type as controlling response format or thinking Accept applies to the request body",
      "to_stand_out": "Explain the bidirectional nature: Content-Type flows with the body (request or response), Accept is a client preference declaration"
    },
    "company_tags": [
      "amazon",
      "microsoft",
      "tcs",
      "infosys"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "Request vs Response Format Headers",
          "content": "**Content-Type** and **Accept** are HTTP headers that work in opposite directions during client-server communication. Think of them like shipping labels: **Content-Type** is the label on the package you're sending (\"this box contains JSON\"), while **Accept** is your wishlist for what you want to receive back (\"please send me XML\").\n\nContent-Type appears on both requests and responses—it always describes the format of the **body** in that message. Accept only appears in requests—it's the client telling the server \"I prefer this format for your response.\" In a Spring Boot REST API, when you POST data, Content-Type tells Spring how to deserialize your request body. When Spring sends a response, it checks the Accept header to decide which format to serialize into. These headers enable **content negotiation**, where the same endpoint can serve multiple formats (JSON, XML, HTML) based on what the client requests."
        },
        {
          "type": "concept_map",
          "title": "Content-Type vs Accept",
          "content": "amber|Content-Type|~Format of the body sent|on request and response|\"this is what I'm sending\"|drives @RequestBody read\nblue|Accept|~Format client wants back|on the request|\"send me this format\"|drives the response converter\nemerald|Together|~POST with both|Content-Type: json (body)|Accept: json (response)|full negotiation\nrose|Trap|~Confusing the two|Content-Type for response|Accept for request body|keep directions straight"
        },
        {
          "type": "comparison_table",
          "title": "Content-Type vs Accept Headers",
          "content": {
            "headers": [
              "Aspect",
              "Content-Type",
              "Accept"
            ],
            "rows": [
              [
                "Direction",
                "Describes body being sent",
                "Requests format wanted back"
              ],
              [
                "Appears in",
                "Requests with body, all responses",
                "Requests only"
              ],
              [
                "Example value",
                "application/json",
                "application/xml, application/json"
              ],
              [
                "Spring usage",
                "Selects HttpMessageConverter for deserialization",
                "Selects HttpMessageConverter for serialization"
              ],
              [
                "Multiple values",
                "Single value only",
                "Comma-separated list with priorities"
              ]
            ]
          }
        },
        {
          "type": "code_example",
          "title": "Headers in Action with RestTemplate",
          "content": "var restTemplate = new RestTemplate();\nvar headers = new HttpHeaders();\n\n// Content-Type: what format am I sending?\nheaders.setContentType(MediaType.APPLICATION_JSON);\n\n// Accept: what format do I want back?\nheaders.setAccept(List.of(MediaType.APPLICATION_XML));\n\nvar order = new Order(null, \"Laptop\", 1299.99);\nvar request = new HttpEntity<>(order, headers);\n\n// Sends JSON (Content-Type), expects XML back (Accept)\nvar response = restTemplate.postForEntity(\n    \"http://localhost:8080/api/orders\",\n    request,\n    Order.class\n);\n\n// Server sees Content-Type: application/json -> deserializes request as JSON\n// Server sees Accept: application/xml -> serializes response as XML"
        },
        {
          "type": "when_to_use",
          "title": "When These Headers Matter",
          "content": "Use **Content-Type** when your request has a body (POST, PUT, PATCH)—without it, Spring can't deserialize your data and will return 415 Unsupported Media Type.\n\nUse **Accept** when you need a specific response format from an API that supports multiple formats—if you omit it, the server picks a default (usually JSON in Spring Boot). In microservice communication, always set both: Content-Type ensures your service correctly interprets incoming data, Accept ensures you get responses in the format your service expects. When building a mobile app that prefers JSON and a web app that prefers HTML from the same backend, the Accept header lets each client get the right format without changing the server endpoint."
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **Using Content-Type to request response format**: Freshers often think setting Content-Type: application/xml will make the server return XML. Content-Type only describes what you're sending, not what you want back—that's Accept's job.\n- **Forgetting Content-Type on POST requests**: Sending JSON data without Content-Type: application/json causes 415 errors because Spring doesn't know how to parse the body. The header isn't optional when there's a body.\n- **Expecting Accept to affect request parsing**: Setting Accept: application/xml doesn't change how Spring reads your request body—it only affects the response. If you send JSON, you still need Content-Type: application/json regardless of your Accept value."
        },
        {
          "type": "key_points",
          "title": "Key Points",
          "content": [
            "**Content-Type**: describes the format of the message body being sent (request or response)",
            "**Accept**: client declares preferred response formats, can include multiple values with quality weights",
            "**415 Unsupported Media Type**: returned when server can't handle the Content-Type you sent",
            "**406 Not Acceptable**: returned when server can't produce any format listed in Accept header",
            "**Content negotiation**: Spring uses Accept header to select the right HttpMessageConverter for serialization",
            "**MediaType class**: Spring's enum for standard MIME types like APPLICATION_JSON, APPLICATION_XML",
            "**produces/consumes**: @RequestMapping attributes that restrict which Content-Type (consumes) and Accept (produces) values a controller method handles"
          ]
        },
        {
          "type": "speakable_answer",
          "title": "How to Answer",
          "content": "Content-Type and Accept work in opposite directions. Content-Type describes the format of the body you're sending to the server—like when you POST JSON data to create a new order, you set Content-Type to application slash json so Spring knows how to deserialize it. Accept is your wishlist for what format you want the response in. If you want XML back, you set Accept to application slash xml. The key thing is Content-Type travels with the body in both directions. When the server sends a response, it includes its own Content-Type header describing that response body. Accept only appears in the request—it's the client telling the server its preferences. Here's a concrete example: you POST to slash api slash users with Content-Type application slash json because you're sending JSON, and Accept application slash xml because you want the created user resource back in XML format. A common mistake freshers make is thinking Content-Type controls the response format. It doesn't—Accept does that. If you forget Content-Type on a POST request with a body, Spring returns 415 Unsupported Media Type because it doesn't know how to parse your data. In Spring Boot, these headers drive content negotiation, where the framework automatically picks the right message converter based on what you sent and what you want back."
        }
      ]
    },
    "followup_questions": [
      "What happens if the client sends a request without a Content-Type header to a POST endpoint?",
      "How does the server set the Content-Type in the response?",
      "What is the difference between consumes and produces in @PostMapping and these headers?"
    ],
    "seo": {
      "metaTitle": "Content-Type vs Accept Headers in HTTP | Java Fresher Interview",
      "metaDescription": "Learn the difference between Content-Type and Accept HTTP headers — request body format vs desired response format, Spring MVC consumes/produces mapping."
    },
    "order": 5,
    "speakable_v2": {
      "question_id": "content-negotiation-005",
      "archetype": "B",
      "pillar": "P04",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API serving a mobile client",
      "familiarity_anchors": [
        "Spring MVC",
        "REST",
        "controllers"
      ],
      "hook": "Content-Type and Accept are both HTTP headers about data format — but they point in opposite directions.",
      "beats": [
        {
          "type": "paragraph",
          "label": "what_each_is",
          "content": "Content-Type describes the format of the data being sent right now. The client sets it on a POST or PUT request body to say 'this is JSON I'm sending you'. The server sets it on the response to say 'this is JSON I'm sending back'."
        },
        {
          "type": "paragraph",
          "label": "what_each_is",
          "content": "Accept is a preference declaration. The client sets it to say 'this is the format I want in your response'. The server uses it to pick the right serializer."
        },
        {
          "type": "mini_table",
          "label": "differences",
          "headers": [
            "Header",
            "Set by",
            "Describes",
            "Spring uses it for"
          ],
          "rows": [
            [
              "Content-Type",
              "Client (request) / Server (response)",
              "The body being sent",
              "@RequestBody deserialization"
            ],
            [
              "Accept",
              "Client",
              "Desired response format",
              "@ResponseBody serialization"
            ]
          ]
        },
        {
          "type": "paragraph",
          "label": "when_to_pick",
          "content": "In a typical POST: Content-Type: application/json tells Spring to deserialize the request body with Jackson. Accept: application/json tells Spring to serialize the response with Jackson. They often match, but they don't have to — a client could send XML and want JSON back."
        },
        {
          "type": "paragraph",
          "label": "tiny_example",
          "content": "gotcha: If a client sends a POST with a JSON body but forgets to set Content-Type: application/json, Spring can't determine the format and may return 415 Unsupported Media Type. This is one of the most common mistakes when testing APIs with curl or Postman for the first time."
        }
      ],
      "cap": "In Spring: consumes checks the incoming Content-Type. produces checks the incoming Accept header. The directions mirror the headers.",
      "followup_handoff": [
        "What happens if the client sends a POST request without a Content-Type header?",
        "How does the server set the Content-Type in the response?"
      ],
      "tts_overrides": {
        "Content-Type": "Content Type",
        "Accept": "Accept header",
        "@RequestBody": "at-RequestBody",
        "@ResponseBody": "at-ResponseBody",
        "consumes": "consumes attribute",
        "produces": "produces attribute",
        "415": "four fifteen",
        "JSON": "J-S-O-N",
        "XML": "X-M-L",
        "HTTP": "H-T-T-P",
        "curl": "curl command"
      },
      "speakable_status": "pending_review"
    }
  }
]

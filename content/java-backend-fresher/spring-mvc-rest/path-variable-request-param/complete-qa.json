[
  {
    "id": "path-variable-request-param-001",
    "slug": "path-variable-request-param-001",
    "question": "What is @PathVariable in Spring MVC?",
    "title": "@PathVariable in Spring MVC — Extracting URL Path Segments",
    "direct_answer": "**`@PathVariable`** extracts a value from a URL path template variable. In `@GetMapping(\"/users/{id}\")`, the `{id}` part is extracted from the actual URL and bound to the annotated parameter. Used for resource identification in REST APIs.",
    "layout_type": "default",
    "difficulty": "easy",
    "importance": "high",
    "reading_time_minutes": 2,
    "last_updated": "2026-05-31",
    "interviewer_intent": {
      "testing": "Core REST URL parameter extraction knowledge",
      "common_mistake": "Using @RequestParam for resource ID instead of @PathVariable — breaks REST conventions",
      "to_stand_out": "Explain REST URL convention: path variables for identity, query params for filtering"
    },
    "company_tags": [
      "tcs",
      "infosys",
      "wipro",
      "amazon"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "@PathVariable Usage",
          "content": "```java\n// GET /api/users/42\n@GetMapping(\"/users/{id}\")\npublic User getUser(@PathVariable Long id) {\n    return userService.findById(id);\n}\n\n// Multiple path variables: GET /api/users/42/orders/7\n@GetMapping(\"/users/{userId}/orders/{orderId}\")\npublic Order getOrder(\n    @PathVariable Long userId,\n    @PathVariable Long orderId\n) { ... }\n\n// Rename: when variable name differs from template\n@GetMapping(\"/products/{product-id}\")\npublic Product get(@PathVariable(\"product-id\") Long productId) { ... }\n```"
        },
        {
          "type": "key_points",
          "title": "@PathVariable Details",
          "content": "- **Syntax**: `{variableName}` in URL template, `@PathVariable Type variableName` in parameter\n- **Name matching**: Parameter name must match template variable name OR use `@PathVariable(\"name\")`\n- **Type conversion**: Spring automatically converts String segment to `Long`, `Integer`, `UUID`, etc.\n- **Multiple**: Multiple `{vars}` in URL → multiple @PathVariable params\n- **Optional**: `@PathVariable(required=false)` with `Optional<T>` for optional path variables\n- **REST convention**: Use PathVariable for resource identity (`/users/{id}`, `/orders/{orderId}`)\n- **vs. @RequestParam**: PathVariable from URL path; RequestParam from `?key=value` query string\n- **Regex**: `@GetMapping(\"/users/{id:[0-9]+}\")` — constrain path variable to numeric only"
        },
        {
          "type": "speakable_answer",
          "title": "How to Answer in an Interview",
          "content": "@PathVariable extracts a value embedded in the URL path itself. You define a template in the @GetMapping URL with curly braces — like /users/{id} — and Spring takes whatever is at that position in the actual request URL and injects it into the annotated parameter. Spring also handles type conversion automatically, so if your parameter is Long, Spring converts the String segment to a Long for you. The REST convention is to use path variables for resource identity — the ID that uniquely identifies which resource you're operating on. Query parameters are for optional filtering and pagination. So /users/42 is cleaner than /users?id=42 in REST style."
        }
      ]
    },
    "followup_questions": [
      "What is the difference between @PathVariable and @RequestParam?",
      "How do you add regex constraint to a @PathVariable?",
      "What happens if the path variable cannot be converted to the parameter type?"
    ],
    "seo": {
      "metaTitle": "@PathVariable in Spring MVC | Java Fresher Interview Guide",
      "metaDescription": "Learn @PathVariable in Spring MVC — URL path template extraction, type conversion, multiple variables, REST URL design conventions."
    },
    "order": 1,
    "speakable_v2": {
      "question_id": "path-variable-request-param-001",
      "archetype": "A",
      "pillar": "P04",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API serving a mobile client",
      "familiarity_anchors": [
        "Spring MVC",
        "REST",
        "controllers"
      ],
      "hook": "@PathVariable turns the dynamic parts of your URL into Java method parameters — no manual string parsing needed.",
      "beats": [
        {
          "type": "paragraph",
          "label": "what_it_is",
          "content": "@PathVariable extracts a value embedded in the URL path itself. You define a template in your mapping annotation with curly braces — like /users/{id} — and Spring takes whatever is at that position in the actual request URL and injects it into the annotated parameter."
        },
        {
          "type": "paragraph",
          "label": "why_it_exists",
          "content": "Spring also handles type conversion automatically. If your parameter is Long, Spring converts the String segment to a Long. If it can't convert — like someone sends /users/abc and you expect a number — Spring returns 400 Bad Request."
        },
        {
          "type": "bullets",
          "label": "parts_or_states",
          "content": [
            "Single variable: /users/{id} with @PathVariable Long id.",
            "Multiple variables: /users/{userId}/orders/{orderId} with two @PathVariable parameters.",
            "Name mismatch: if the URL template uses a hyphen like {product-id}, use @PathVariable(\"product-id\") Long productId.",
            "Regex constraint: /users/{id:[0-9]+} restricts the segment to digits only."
          ]
        },
        {
          "type": "paragraph",
          "label": "pitfalls",
          "content": "candidates miss: The parameter name in your Java method must match the template variable name exactly, unless you specify the name explicitly in the annotation. A mismatch causes a 500 error at runtime, not a compile-time warning."
        }
      ],
      "cap": "REST convention: use @PathVariable for resource identity — the thing that says which specific resource. Use @RequestParam for options and filters.",
      "followup_handoff": [
        "What is the difference between @PathVariable and @RequestParam?",
        "How do you add a regex constraint to a @PathVariable?"
      ],
      "tts_overrides": {
        "@PathVariable": "at-PathVariable",
        "@RequestParam": "at-RequestParam",
        "400": "four hundred",
        "500": "five hundred",
        "Long": "Long type",
        "URL": "U-R-L",
        "HTTP": "H-T-T-P"
      },
      "speakable_status": "pending_review"
    }
  },
  {
    "id": "path-variable-request-param-002",
    "slug": "path-variable-request-param-002",
    "question": "What is @RequestParam in Spring MVC and when do you use it?",
    "title": "@RequestParam in Spring MVC — Query String Parameter Binding",
    "direct_answer": "**`@RequestParam`** extracts parameters from the URL query string (after `?`). Example: `/users?status=ACTIVE&page=0`. Use it for optional filters, pagination, and search parameters. Supports `required=false` and `defaultValue` for optional params.",
    "layout_type": "default",
    "difficulty": "easy",
    "importance": "high",
    "reading_time_minutes": 2,
    "last_updated": "2026-05-31",
    "interviewer_intent": {
      "testing": "Query string parameter binding — used in every list/search endpoint",
      "common_mistake": "Making all params required without defaultValue — breaks client code when params are omitted",
      "to_stand_out": "Show pagination example with required=false and defaultValue"
    },
    "company_tags": [
      "tcs",
      "infosys",
      "wipro",
      "amazon"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "@RequestParam Usage",
          "content": "```java\n// GET /users?status=ACTIVE&page=0&size=10\n@GetMapping(\"/users\")\npublic Page<User> getUsers(\n    @RequestParam String status,\n    @RequestParam(defaultValue = \"0\") int page,\n    @RequestParam(defaultValue = \"10\") int size\n) { ... }\n\n// Optional param:\n@GetMapping(\"/search\")\npublic List<User> search(\n    @RequestParam(required = false) String name,\n    @RequestParam(required = false) String email\n) { ... }\n\n// All params as map:\n@GetMapping(\"/data\")\npublic Data get(@RequestParam Map<String, String> allParams) { ... }\n```"
        },
        {
          "type": "key_points",
          "title": "@RequestParam Details",
          "content": "- **Required by default**: Missing required param → 400 Bad Request\n- **`required=false`**: Make param optional — Spring injects `null` if missing\n- **`defaultValue`**: Sets a default when param is absent; implicitly makes it optional\n- **Type conversion**: Spring converts String to `int`, `Long`, `Boolean`, etc.\n- **`@RequestParam Map<String, String>`**: Capture all query params as a map\n- **List binding**: `@RequestParam List<String> ids` for repeated params: `?ids=1&ids=2&ids=3`\n- **Form data**: `@RequestParam` also works for `application/x-www-form-urlencoded` body (HTML form submissions)\n- **Use for**: Filtering, sorting, pagination, search — optional aspects of the request"
        },
        {
          "type": "speakable_answer",
          "title": "How to Answer in an Interview",
          "content": "@RequestParam binds query string parameters to method parameters. These are the key-value pairs after the question mark in the URL. It's the right choice for filters, pagination parameters, and search terms — things that are optional additions to a request. By default params are required and Spring returns 400 if they're missing. Use required=false to make them optional, or provide a defaultValue which also makes them optional implicitly. A common pattern is pagination: page and size params with sensible defaults like 0 and 10. @RequestParam also works for HTML form submissions, not just JSON APIs. The big difference from @PathVariable is that path variables are for identity — which specific resource — and query params are for how you want to retrieve it."
        }
      ]
    },
    "followup_questions": [
      "What happens if a required @RequestParam is missing?",
      "How do you bind a list of values from the query string with @RequestParam?",
      "What is the difference between required=false and providing a defaultValue?"
    ],
    "seo": {
      "metaTitle": "@RequestParam in Spring MVC | Java Fresher Interview Guide",
      "metaDescription": "Learn @RequestParam in Spring MVC — query string binding, required=false, defaultValue, list params, pagination pattern, and difference from @PathVariable."
    },
    "order": 2,
    "speakable_v2": {
      "question_id": "path-variable-request-param-002",
      "archetype": "A",
      "pillar": "P04",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API serving a mobile client",
      "familiarity_anchors": [
        "Spring MVC",
        "REST",
        "controllers"
      ],
      "hook": "@RequestParam is how you read the ?key=value pairs from the URL — the filtering and pagination parameters every list endpoint needs.",
      "beats": [
        {
          "type": "paragraph",
          "label": "what_it_is",
          "content": "@RequestParam binds query string parameters to method parameters. These are the key-value pairs after the question mark in the URL — like /users?status=ACTIVE&page=0&size=10."
        },
        {
          "type": "paragraph",
          "label": "why_it_exists",
          "content": "By default, every @RequestParam is required. If the client doesn't include it, Spring returns 400. For optional parameters, you have two options: set required=false to get null when the parameter is absent, or provide a defaultValue to get a fallback."
        },
        {
          "type": "bullets",
          "label": "parts_or_states",
          "content": [
            "defaultValue: use a string default when the param is absent — also implicitly makes the param optional.",
            "required=false: Spring injects null. Use Integer not int — primitives can't hold null.",
            "Map<String, String>: inject all query params at once, useful when you don't know the keys upfront.",
            "Also works for HTML form submissions with application/x-www-form-urlencoded content type."
          ]
        },
        {
          "type": "paragraph",
          "label": "pitfalls",
          "content": "non-obvious: If you use required=false with a primitive type like int, Spring throws a NullPointerException when the parameter is absent because it tries to unbox null to int. Always use the wrapper type Integer when making numeric params optional."
        }
      ],
      "cap": "The standard pagination pattern: @RequestParam(defaultValue='0') int page, @RequestParam(defaultValue='20') int size. Clients can omit them and get sensible defaults.",
      "followup_handoff": [
        "What happens if a required @RequestParam is missing?",
        "How do you bind a list of values from the query string?"
      ],
      "tts_overrides": {
        "@RequestParam": "at-RequestParam",
        "required=false": "required equals false",
        "defaultValue": "default Value",
        "Integer": "Integer wrapper type",
        "application/x-www-form-urlencoded": "application x-www-form-urlencoded",
        "400": "four hundred",
        "URL": "U-R-L"
      },
      "speakable_status": "pending_review"
    }
  },
  {
    "id": "path-variable-request-param-003",
    "slug": "path-variable-request-param-003",
    "question": "What is the difference between @PathVariable and @RequestParam?",
    "title": "@PathVariable vs @RequestParam — URL Path vs Query String",
    "direct_answer": "**`@PathVariable`** extracts values from the **URL path** (e.g., `/users/42` where `42` is the ID). **`@RequestParam`** extracts values from the **query string** (e.g., `/users?status=ACTIVE`). PathVariable is for resource identity; RequestParam is for optional filters/options.",
    "layout_type": "default",
    "difficulty": "easy",
    "importance": "high",
    "reading_time_minutes": 2,
    "last_updated": "2026-05-31",
    "interviewer_intent": {
      "testing": "Common interview comparison — shows REST design understanding",
      "common_mistake": "Using @RequestParam for IDs that should be @PathVariable — not RESTful",
      "to_stand_out": "Give the REST design rule: path for nouns/identity, query for adjectives/filtering"
    },
    "company_tags": [
      "amazon",
      "microsoft",
      "tcs",
      "infosys"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "Side-by-Side Comparison",
          "content": "```java\n// @PathVariable — identity: GET /api/users/42/orders/7\n@GetMapping(\"/users/{userId}/orders/{orderId}\")\npublic Order getOrder(\n    @PathVariable Long userId,      // from path\n    @PathVariable Long orderId      // from path\n) { ... }\n\n// @RequestParam — options: GET /api/users?status=ACTIVE&page=0&sort=name\n@GetMapping(\"/users\")\npublic Page<User> getUsers(\n    @RequestParam(defaultValue = \"ACTIVE\") String status,  // from query string\n    @RequestParam(defaultValue = \"0\") int page              // from query string\n) { ... }\n```"
        },
        {
          "type": "key_points",
          "title": "When to Use Each",
          "content": "| Aspect | @PathVariable | @RequestParam |\n|--------|---------------|----------------|\n| Location | URL path `/users/{id}` | Query string `/users?id=1` |\n| Purpose | Resource identity | Filtering, pagination, options |\n| Optional | Unusual | Common (required=false) |\n| Required | Usually yes | Often no |\n| REST style | `/users/42` ✓ | `/users?id=42` ✗ (for identity) |\n| Multiple | Nested resources | Multiple filter params |\n\n**Rule**: If you're identifying WHICH resource, use @PathVariable. If you're describing HOW to retrieve it, use @RequestParam."
        },
        {
          "type": "speakable_answer",
          "title": "How to Answer in an Interview",
          "content": "The simple rule is: @PathVariable is for identity, @RequestParam is for options. When you want to identify a specific resource — user 42, order 7 — that goes in the path: /users/42/orders/7. When you want to filter or control how you retrieve resources — give me active users, page 2, sorted by name — those go in the query string as @RequestParam values. This follows REST design principles: the path identifies the resource hierarchy, and query parameters refine the operation. So /users/42 means 'get me user 42 specifically', while /users?status=ACTIVE means 'get me all users that are active'. Mixing these up — like /users?id=42 for a single user — works but is not considered clean REST design."
        }
      ]
    },
    "followup_questions": [
      "When would you use @PathVariable for something other than an ID?",
      "How do you design a REST URL for a nested resource (user's orders)?",
      "Can you use both @PathVariable and @RequestParam in the same endpoint?"
    ],
    "seo": {
      "metaTitle": "@PathVariable vs @RequestParam in Spring | Interview Guide",
      "metaDescription": "Understand @PathVariable vs @RequestParam in Spring MVC — URL path vs query string, REST design convention, resource identity vs filtering comparison."
    },
    "order": 3,
    "speakable_v2": {
      "question_id": "path-variable-request-param-003",
      "archetype": "B",
      "pillar": "P04",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API serving a mobile client",
      "familiarity_anchors": [
        "Spring MVC",
        "REST",
        "controllers"
      ],
      "hook": "@PathVariable and @RequestParam both get data from the URL — but they live in completely different parts of it.",
      "beats": [
        {
          "type": "paragraph",
          "label": "what_each_is",
          "content": "@PathVariable reads from the URL path itself — the segments before any question mark. You define the position using curly braces in the mapping template, like /users/{id}. The value is part of the resource address."
        },
        {
          "type": "paragraph",
          "label": "what_each_is",
          "content": "@RequestParam reads from the query string — everything after the question mark. Like /users?status=ACTIVE&page=0. These are modifiers: how to filter, sort, or paginate the result."
        },
        {
          "type": "mini_table",
          "label": "differences",
          "headers": [
            "",
            "@PathVariable",
            "@RequestParam"
          ],
          "rows": [
            [
              "Location",
              "URL path /users/{id}",
              "Query string ?status=ACTIVE"
            ],
            [
              "Purpose",
              "Which resource",
              "How to retrieve it"
            ],
            [
              "Optional",
              "Unusual",
              "Common"
            ],
            [
              "REST convention",
              "Resource identity",
              "Filtering / pagination"
            ]
          ]
        },
        {
          "type": "paragraph",
          "label": "when_to_pick",
          "content": "Use @PathVariable when you're identifying a specific resource: GET /users/42 means user 42 specifically. Use @RequestParam when you're describing criteria: GET /users?status=ACTIVE means all active users. A single endpoint can use both — GET /users/{id}/orders?status=PENDING gets pending orders for a specific user."
        },
        {
          "type": "paragraph",
          "label": "tiny_example",
          "content": "gotcha: /users?id=42 works technically but breaks REST conventions. Interviewers notice this. Resource IDs belong in the path, not the query string."
        }
      ],
      "cap": "Path for nouns, query for adjectives. The path identifies the resource; the query string describes how you want it.",
      "followup_handoff": [
        "How do you design a REST URL for a nested resource like a user's orders?",
        "Can you use both @PathVariable and @RequestParam in the same endpoint?"
      ],
      "tts_overrides": {
        "@PathVariable": "at-PathVariable",
        "@RequestParam": "at-RequestParam",
        "URL": "U-R-L",
        "REST": "R-E-S-T",
        "HTTP": "H-T-T-P"
      },
      "speakable_status": "pending_review"
    }
  },
  {
    "id": "path-variable-request-param-004",
    "slug": "path-variable-request-param-004",
    "question": "How do you make @RequestParam optional with a default value?",
    "title": "Optional @RequestParam with defaultValue in Spring MVC",
    "direct_answer": "Set **`required=false`** to make a `@RequestParam` optional (Spring injects `null` if missing). Set **`defaultValue=\"value\"`** to provide a fallback value when the parameter is absent — this also implicitly makes the parameter optional.",
    "layout_type": "default",
    "difficulty": "easy",
    "importance": "medium",
    "reading_time_minutes": 2,
    "last_updated": "2026-05-31",
    "interviewer_intent": {
      "testing": "Practical knowledge for building flexible API endpoints",
      "common_mistake": "Required params without defaults — clients must always provide all params or get 400 errors",
      "to_stand_out": "Show the pagination pattern as a concrete example of defaultValue in practice"
    },
    "company_tags": [
      "tcs",
      "infosys",
      "wipro",
      "accenture"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "Optional Params and Defaults",
          "content": "```java\n// required=false → null if missing\n@GetMapping(\"/users\")\npublic List<User> getUsers(\n    @RequestParam(required = false) String nameFilter\n) {\n    if (nameFilter == null) return userService.findAll();\n    return userService.findByName(nameFilter);\n}\n\n// defaultValue → use fallback if missing\n@GetMapping(\"/users\")\npublic Page<User> getUsers(\n    @RequestParam(defaultValue = \"0\") int page,\n    @RequestParam(defaultValue = \"20\") int size,\n    @RequestParam(defaultValue = \"id\") String sortBy\n) {\n    return userService.findAll(PageRequest.of(page, size, Sort.by(sortBy)));\n}\n```"
        },
        {
          "type": "key_points",
          "title": "Optional Param Options",
          "content": "- **`required=false`**: Parameter is optional; `null` injected if absent; use `Integer` not `int` (int can't be null)\n- **`defaultValue`**: String value used when param absent; automatically makes param optional; parsed to the parameter type\n- **`defaultValue` vs `required=false`**: `defaultValue` gives a fallback; `required=false` gives null\n- **`Optional<String>` parameter type**: Java Optional also works — `Optional.empty()` when absent\n- **Primitive types**: `int`, `boolean` etc. can't be null — use `Integer`, `Boolean` with `required=false`, or provide `defaultValue`\n- **Best practice for pagination**: Always provide defaults (`page=0`, `size=20`) — never make clients required to send these\n- **Max size guard**: Consider validating `@Max(100)` on `size` to prevent `?size=999999` attacks"
        },
        {
          "type": "speakable_answer",
          "title": "How to Answer in an Interview",
          "content": "To make a @RequestParam optional, you have two approaches. First, set required=false — Spring injects null if the parameter is missing from the URL. For primitive types like int, you'd need to use the wrapper Integer since primitives can't be null. Second, provide a defaultValue — Spring uses that string value when the parameter is absent and parses it to the parameter type. The defaultValue approach is cleaner for pagination: defaultValue='0' for page and defaultValue='20' for size means clients can omit them and get sensible defaults. Both approaches effectively make the parameter optional — the difference is whether you get null or a default value when it's missing."
        }
      ]
    },
    "followup_questions": [
      "Why should you use Integer instead of int when @RequestParam is required=false?",
      "What is the difference between required=false and providing a defaultValue?",
      "How do you validate a @RequestParam value (e.g., size must be <= 100)?"
    ],
    "seo": {
      "metaTitle": "Optional @RequestParam with defaultValue | Spring MVC Interview",
      "metaDescription": "Learn how to make @RequestParam optional in Spring MVC — required=false, defaultValue, Integer vs int, and pagination best practices."
    },
    "order": 4,
    "speakable_v2": {
      "question_id": "path-variable-request-param-004",
      "archetype": "A",
      "pillar": "P04",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API serving a mobile client",
      "familiarity_anchors": [
        "Spring MVC",
        "REST",
        "controllers"
      ],
      "hook": "Making query parameters optional is how you build APIs that work with and without those parameters — defaultValue is the clean way.",
      "beats": [
        {
          "type": "paragraph",
          "label": "what_it_is",
          "content": "There are two ways to make a @RequestParam optional. Set required=false and Spring injects null when the parameter is absent. Or set defaultValue with a string value and Spring uses that as the fallback."
        },
        {
          "type": "paragraph",
          "label": "why_it_exists",
          "content": "defaultValue is usually the cleaner choice because it means you never have to check for null in your method body. The parameter always has a value — either from the client or from the default. Pagination especially benefits from this."
        },
        {
          "type": "bullets",
          "label": "parts_or_states",
          "content": [
            "defaultValue='0': page parameter defaults to the first page if not supplied.",
            "required=false with Integer: nameFilter is null when absent — check before using.",
            "Optional<String>: Java Optional works too — Optional.empty() when the parameter is absent.",
            "Both approaches make the parameter optional — the difference is null vs a default value."
          ]
        },
        {
          "type": "paragraph",
          "label": "pitfalls",
          "content": "sharp edge: defaultValue is always a String, even if your parameter type is int or Long. Spring converts it. But if your defaultValue string can't be parsed to the parameter type — like defaultValue='none' for an int parameter — you'll get a TypeMismatchException at request time, not startup."
        }
      ],
      "cap": "Validate the size parameter with @Max to prevent clients from requesting thousands of records in one call — ?size=99999 is a real attack vector.",
      "followup_handoff": [
        "Why should you use Integer instead of int when @RequestParam is required=false?",
        "How do you validate a @RequestParam value like size <= 100?"
      ],
      "tts_overrides": {
        "@RequestParam": "at-RequestParam",
        "required=false": "required equals false",
        "defaultValue": "default Value",
        "Optional": "Java Optional",
        "TypeMismatchException": "Type Mismatch Exception",
        "@Max": "at-Max",
        "Integer": "Integer wrapper type",
        "null": "null value"
      },
      "speakable_status": "pending_review"
    }
  },
  {
    "id": "path-variable-request-param-005",
    "slug": "path-variable-request-param-005",
    "question": "How do you design RESTful URLs with path variables?",
    "title": "RESTful URL Design with Path Variables — Best Practices",
    "direct_answer": "RESTful URLs use **nouns** (not verbs), **hierarchical path variables** for resource identity, and **HTTP methods** for actions. Example: `GET /users/{id}`, `POST /users`, `DELETE /users/{id}/orders/{orderId}`. The path variables identify which resource; the HTTP method defines the action.",
    "layout_type": "default",
    "difficulty": "medium",
    "importance": "medium",
    "reading_time_minutes": 3,
    "last_updated": "2026-05-31",
    "interviewer_intent": {
      "testing": "REST URL design knowledge — shows thinking beyond just annotations",
      "common_mistake": "Using verbs in URLs (/getUser, /deleteOrder) — REST uses HTTP methods for actions, not URL verbs",
      "to_stand_out": "Show the full CRUD URL design for a resource with proper methods"
    },
    "company_tags": [
      "amazon",
      "microsoft",
      "infosys",
      "tcs"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "RESTful URL Patterns",
          "content": "```\n# CRUD for users:\nGET    /api/users           → list all users\nPOST   /api/users           → create new user\nGET    /api/users/{id}      → get user by ID\nPUT    /api/users/{id}      → full update user\nPATCH  /api/users/{id}      → partial update user\nDELETE /api/users/{id}      → delete user\n\n# Nested resource:\nGET    /api/users/{userId}/orders           → user's orders\nGET    /api/users/{userId}/orders/{orderId} → specific order\nPOST   /api/users/{userId}/orders           → create order for user\n```"
        },
        {
          "type": "key_points",
          "title": "URL Design Rules",
          "content": "- **Nouns, not verbs**: `/users` not `/getUsers`; `/users/{id}` not `/deleteUser/{id}`\n- **HTTP method = action**: GET = read, POST = create, PUT/PATCH = update, DELETE = delete\n- **Plural nouns**: `/users` (plural) not `/user`\n- **Lowercase and hyphenated**: `/api/user-profiles` not `/api/userProfiles`\n- **Hierarchical nesting**: `/users/{id}/orders` for sub-resources (but keep nesting shallow)\n- **No deep nesting**: `/users/{uId}/orders/{oId}/items/{iId}/details` is too deep\n- **Query params for filtering**: `/users?status=ACTIVE&role=ADMIN` not `/activeAdminUsers`\n- **Versioning**: `/api/v1/users` — version in path or header\n- **Consistent casing**: Stick to one style throughout the API"
        },
        {
          "type": "speakable_answer",
          "title": "How to Answer in an Interview",
          "content": "RESTful URL design follows a few key principles. Use nouns, not verbs — the URL identifies a resource, not an action. So /users not /getUsers, /users/42 not /getUserById/42. The HTTP method tells you what action to take: GET reads, POST creates, PUT or PATCH updates, DELETE deletes. Use plural nouns for collections: /users not /user. For nested resources like a user's orders, use hierarchical paths: /users/42/orders. Query parameters are for filtering and pagination — /users?status=active&page=0 — not for identifying the resource. Try to keep nesting to two levels maximum — if you go deeper, consider whether you really need the full hierarchy or can access the child resource directly."
        }
      ]
    },
    "followup_questions": [
      "Why is /getUsers a bad RESTful URL?",
      "How deep should URL nesting go in REST API design?",
      "How do you represent filtering in a REST URL?"
    ],
    "seo": {
      "metaTitle": "RESTful URL Design with Path Variables | Spring Java Interview",
      "metaDescription": "Learn REST URL design best practices — nouns not verbs, hierarchical resources, nested path variables, plural nouns, and HTTP method conventions."
    },
    "order": 5,
    "speakable_v2": {
      "question_id": "path-variable-request-param-005",
      "archetype": "A",
      "pillar": "P04",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API serving a mobile client",
      "familiarity_anchors": [
        "Spring MVC",
        "REST",
        "controllers"
      ],
      "hook": "RESTful URL design has a simple core rule: nouns in the path, verbs in the HTTP method. That one rule eliminates most bad URL patterns.",
      "beats": [
        {
          "type": "paragraph",
          "label": "what_it_is",
          "content": "RESTful URL design uses the URL to identify a resource and the HTTP method to express the action. The path tells you what you're operating on. GET, POST, PUT, PATCH, and DELETE tell you what you're doing to it."
        },
        {
          "type": "bullets",
          "label": "parts_or_states",
          "content": [
            "GET /users — list all users.",
            "POST /users — create a new user.",
            "GET /users/{id} — get one user.",
            "PUT /users/{id} — replace a user.",
            "DELETE /users/{id} — remove a user.",
            "GET /users/{userId}/orders — list a user's orders."
          ]
        },
        {
          "type": "paragraph",
          "label": "why_it_exists",
          "content": "Use plural nouns: /users not /user. Use lowercase and hyphens for multi-word paths: /user-profiles not /userProfiles. Query parameters are for filtering and pagination — /users?status=active — not for defining a different resource."
        },
        {
          "type": "paragraph",
          "label": "pitfalls",
          "content": "candidates miss: /getUser, /deleteOrder, /createProduct are all anti-patterns. They put the action in the URL instead of the HTTP method. Interviewers from companies with public APIs — Amazon, Microsoft — specifically test this because it's a signal of REST maturity."
        }
      ],
      "cap": "Keep nesting to two levels maximum. /users/{id}/orders is clean. /users/{uId}/orders/{oId}/items/{iId}/details is not — flatten it.",
      "followup_handoff": [
        "Why is /getUsers a bad RESTful URL?",
        "How deep should URL nesting go in REST API design?"
      ],
      "tts_overrides": {
        "HTTP": "H-T-T-P",
        "REST": "R-E-S-T",
        "RESTful": "REST-ful",
        "URL": "U-R-L",
        "/users": "slash users",
        "/getUser": "slash get User",
        "/deleteOrder": "slash delete Order",
        "GET": "G-E-T",
        "POST": "P-O-S-T",
        "PUT": "P-U-T",
        "DELETE": "D-E-L-E-T-E"
      },
      "speakable_status": "pending_review"
    }
  },
  {
    "id": "path-variable-request-param-006",
    "slug": "path-variable-request-param-006",
    "question": "What is @RequestParam with a List type in Spring MVC?",
    "title": "@RequestParam List — Multi-Value Query Parameters in Spring",
    "direct_answer": "Declaring `@RequestParam List<String> ids` allows clients to pass multiple values for the same parameter: `/items?ids=1&ids=2&ids=3`. Spring automatically binds all `ids` values into the List. Also supports comma-separated format with `@RequestParam(\"ids\") String[] ids`.",
    "layout_type": "default",
    "difficulty": "easy",
    "importance": "medium",
    "reading_time_minutes": 2,
    "last_updated": "2026-05-31",
    "interviewer_intent": {
      "testing": "Knowledge of multi-value query parameter binding",
      "common_mistake": "Not knowing @RequestParam can bind to List — trying to manually parse a comma-separated string",
      "to_stand_out": "Show both repeated param style (?ids=1&ids=2) and comma-separated style (?ids=1,2,3)"
    },
    "company_tags": [
      "tcs",
      "infosys",
      "wipro",
      "accenture"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "Multi-Value @RequestParam",
          "content": "```java\n// Repeated params: GET /items?ids=1&ids=2&ids=3\n@GetMapping(\"/items\")\npublic List<Item> getItems(\n    @RequestParam List<Long> ids\n) {\n    return itemService.findAllById(ids);\n}\n\n// Comma-separated: GET /items?ids=1,2,3 (parse manually or use String[])\n@GetMapping(\"/filter\")\npublic List<Item> filter(\n    @RequestParam(required = false) List<String> statuses,\n    @RequestParam(required = false) List<String> categories\n) { ... }\n```"
        },
        {
          "type": "key_points",
          "title": "List @RequestParam Details",
          "content": "- **Repeated params**: `?ids=1&ids=2&ids=3` → `@RequestParam List<Long> ids` → `[1, 2, 3]`\n- **`String[]`**: Works too — `@RequestParam String[] ids`\n- **`List<Long>`**: Spring handles type conversion for each element\n- **`required=false`**: Returns empty list `[]` by default when param absent (add `defaultValue` if needed)\n- **Comma-separated**: Spring 5+ auto-splits `?tags=java,spring` into a list if using `List<String>`\n- **`@RequestParam MultiValueMap`**: Get all params including multi-value params as a map\n- **Use case**: Batch lookups (`?ids=1&ids=2`), multi-select filters (`?status=ACTIVE&status=PENDING`)\n- **Limit client input**: Validate list size to prevent very long lists from being passed"
        },
        {
          "type": "speakable_answer",
          "title": "How to Answer in an Interview",
          "content": "You can bind multiple query parameter values with the same name to a List by declaring @RequestParam with a List type. If a client sends ?ids=1&ids=2&ids=3, Spring collects all the values for that parameter name into the List automatically. This is really useful for batch operations — like fetching multiple items by ID in a single request. Spring also handles type conversion for each element, so @RequestParam List<Long> works if the values are all numeric. For multi-select filters — where the user can filter by multiple statuses — you'd do the same: declare @RequestParam List<String> statuses and the client passes ?status=ACTIVE&status=PENDING."
        }
      ]
    },
    "followup_questions": [
      "What is the difference between repeated params and comma-separated params in URLs?",
      "How do you handle an empty list when @RequestParam List is required=false?",
      "What is MultiValueMap in Spring MVC?"
    ],
    "seo": {
      "metaTitle": "@RequestParam List in Spring MVC | Multi-Value Query Params",
      "metaDescription": "Learn how to bind multi-value query parameters to List with @RequestParam in Spring MVC — repeated params, comma-separated values, type conversion."
    },
    "order": 6,
    "speakable_v2": {
      "question_id": "path-variable-request-param-006",
      "archetype": "A",
      "pillar": "P04",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API serving a mobile client",
      "familiarity_anchors": [
        "Spring MVC",
        "REST",
        "controllers"
      ],
      "hook": "When a client needs to pass multiple values for the same parameter, @RequestParam with a List handles it in one line.",
      "beats": [
        {
          "type": "paragraph",
          "label": "what_it_is",
          "content": "Declaring @RequestParam with a List type lets clients pass the same parameter name multiple times. If a client sends ?ids=1&ids=2&ids=3, Spring collects all three values into the List automatically and converts each element to the right type."
        },
        {
          "type": "paragraph",
          "label": "why_it_exists",
          "content": "This is the clean way to handle batch lookups — like fetching multiple items by ID in a single request. Without it, you'd receive a single string with all values and have to split it manually."
        },
        {
          "type": "bullets",
          "label": "parts_or_states",
          "content": [
            "List<Long> ids: Spring collects all ?ids values and converts each to Long.",
            "String[] ids: arrays work too — same behavior.",
            "required=false with a List: returns an empty list when the parameter is absent.",
            "Spring 5 and later also auto-splits comma-separated values like ?tags=java,spring into a list."
          ]
        },
        {
          "type": "paragraph",
          "label": "pitfalls",
          "content": "production trap: An unguarded List<Long> ids with no size limit means a client can send ?ids=1&ids=2&ids=3... for thousands of IDs in one request. Validate the list size or set a maximum to prevent oversized queries from hitting your database."
        }
      ],
      "cap": "For multi-select filters — like filtering by multiple statuses — @RequestParam List<String> statuses with ?status=ACTIVE&status=PENDING is the idiomatic Spring approach.",
      "followup_handoff": [
        "What is the difference between repeated params and comma-separated params in URLs?",
        "What is MultiValueMap in Spring MVC?"
      ],
      "tts_overrides": {
        "@RequestParam": "at-RequestParam",
        "List<Long>": "List of Long",
        "List<String>": "List of String",
        "String[]": "String array",
        "URL": "U-R-L",
        "Long": "Long type"
      },
      "speakable_status": "pending_review"
    }
  }
]

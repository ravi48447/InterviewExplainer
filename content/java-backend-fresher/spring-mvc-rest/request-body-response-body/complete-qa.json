[
  {
    "id": "request-body-response-body-001",
    "slug": "requestbody-in-spring-rest-json-to-java-object-deserialization",
    "question": "What does @RequestBody do in Spring REST?",
    "title": "@RequestBody in Spring REST — JSON to Java Object Deserialization",
    "direct_answer": "@RequestBody tells Spring MVC to read the HTTP request body into a method parameter using an HttpMessageConverter. JSON with Jackson is common, but the annotation is not JSON-only: text, XML, bytes, forms, and custom media types can use other converters. Content-Type identifies the representation sent by the client.",
    "layout_type": "concept-explainer",
    "difficulty": "easy",
    "importance": "high",
    "reading_time_minutes": 2,
    "last_updated": "2026-09-08",
    "interviewer_intent": {
      "testing": "Core request-body annotation; use it when the endpoint actually accepts a converted HTTP body",
      "common_mistake": "Forgetting to send Content-Type: application/json header when calling the API — causes 415 Unsupported Media Type",
      "to_stand_out": "Mention @Valid with @RequestBody for automatic Bean Validation"
    },
    "company_tags": [
      "tcs",
      "infosys",
      "wipro",
      "amazon"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "What @RequestBody Reads",
          "content": "Body conversion happens after a handler is selected. The selected converter must be able to read both the request media type and target Java type. A 415 response usually means no supported request representation matched; malformed JSON normally becomes an unreadable-message error; Bean Validation failures occur after successful deserialization.\n\nUse a request DTO rather than binding an entity directly. That gives the HTTP contract its own fields and validation without exposing persistence details."
        },
        {
          "type": "key_points",
          "title": "Quick revision",
          "content": [
            "Content-Type selects a compatible reader together with the Java parameter type.",
            "JSON usually uses MappingJackson2HttpMessageConverter when Jackson is configured.",
            "@Valid performs Bean Validation after conversion.",
            "Malformed input, unsupported media types, and validation errors are different failures."
          ]
        },
        {
          "type": "speakable_answer",
          "title": "Interview answer",
          "content": "For @PostMapping(consumes = application/json), a JSON request can be converted into CreateOrderRequest through the Jackson converter. Spring does not literally promise to call ObjectMapper.readValue in one particular way; the converter owns that implementation. A text/plain endpoint can bind the body to String without Jackson. If the client sends XML to a JSON-only endpoint, handler/converter selection fails rather than silently treating it as JSON.",
          "answerSize": "compact"
        },
        {
          "type": "concept_map",
          "title": "@RequestBody at a Glance",
          "content": "amber|@RequestBody|~Body to Java object|JSON deserialized|via Jackson converter|needs Content-Type application/json\nblue|How|~HttpMessageConverter|reads the request body|ObjectMapper maps fields|into your DTO\nemerald|Use for|~POST/PUT payloads|create/update resources|structured input|not query params\nrose|Trap|~Missing Content-Type|415 Unsupported Media Type|or an empty body|add the header"
        },
        {
          "type": "code_example",
          "title": "Creating a User with @RequestBody",
          "content": "@RestController\n@RequestMapping(\"/api/users\")\npublic class UserController {\n\n    @PostMapping\n    public ResponseEntity<User> createUser(@RequestBody User user) {\n        // user object is already populated from JSON request body\n        var saved = userService.save(user);\n        return ResponseEntity.status(HttpStatus.CREATED).body(saved);\n    }\n}\n\nrecord User(String name, String email, int age) {}\n\n// Client sends: POST /api/users with body {\"name\":\"Alice\",\"email\":\"alice@example.com\",\"age\":25}\n// Spring deserializes JSON into User record automatically\n// If Content-Type header is missing, you get 415 Unsupported Media Type"
        },
        {
          "type": "when_to_use",
          "title": "When to Use @RequestBody",
          "content": "Use @RequestBody when your API endpoint needs to accept structured data from the client. **POST endpoints** creating new resources — the client sends the full object as JSON in the request body. **PUT endpoints** updating existing resources — the client sends updated fields as JSON. **PATCH endpoints** for partial updates — send only changed fields. Don't use @RequestBody for GET or DELETE methods — those use @PathVariable or @RequestParam instead. Also skip it for simple form submissions with a few fields — use @RequestParam for those. The rule: if the client needs to send complex nested data (objects with multiple fields, arrays, nested objects), use @RequestBody."
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **Missing Content-Type header**: Freshers test with Postman or curl but forget to set Content-Type: application/json. Spring returns 415 Unsupported Media Type because it doesn't know the body is JSON. Always set this header when sending JSON.\n- **Using @RequestBody with GET**: GET requests have no body by design. Freshers sometimes add @RequestBody to GET methods, which doesn't work — use @RequestParam or @PathVariable instead.\n- **Not handling deserialization failures**: If the JSON structure doesn't match the Java class (wrong field names, type mismatches), Jackson throws an exception. Freshers don't catch this — the client sees a 400 error with no useful message. Use @ControllerAdvice to handle HttpMessageNotReadableException and return a clear error response."
        }
      ]
    },
    "followup_questions": [
      "What happens if the client doesn't set Content-Type: application/json with @RequestBody?",
      "How do you validate a @RequestBody in Spring Boot?",
      "What is the difference between @Valid and @Validated?"
    ],
    "seo": {
      "metaTitle": "@RequestBody in Spring REST | Java Fresher Interview Guide",
      "metaDescription": "Learn @RequestBody in Spring REST — JSON to Java deserialization, Content-Type requirement, @Valid validation, and common errors explained."
    },
    "order": 1,
    "speakable_v2": {
      "question_id": "request-body-response-body-001",
      "archetype": "A",
      "pillar": "P04",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API serving a mobile client",
      "familiarity_anchors": [
        "Spring MVC",
        "REST",
        "controllers"
      ],
      "hook": "@RequestBody is what turns incoming JSON bytes into a Java object you can work with — one annotation does the whole job.",
      "beats": [
        {
          "type": "paragraph",
          "label": "what_it_is",
          "content": "@RequestBody tells Spring to read the HTTP request body and deserialize it into the annotated Java parameter. Jackson's ObjectMapper does the actual work — it reads the JSON and populates your Java object's fields."
        },
        {
          "type": "paragraph",
          "label": "why_it_exists",
          "content": "Without @RequestBody, you'd have to manually read the request input stream and parse JSON yourself. With it, you just declare the type you want and Spring delivers a populated object."
        },
        {
          "type": "bullets",
          "label": "parts_or_states",
          "content": [
            "The client must send Content-Type: application/json — without it, Spring returns 415 Unsupported Media Type.",
            "Add @Valid before @RequestBody to trigger Bean Validation automatically on the deserialized object.",
            "If validation fails, Spring returns 400 Bad Request without your method body ever running.",
            "required=false makes the body optional — useful when the body might be absent."
          ]
        },
        {
          "type": "paragraph",
          "label": "pitfalls",
          "content": "sharp edge: If Jackson can't parse the incoming JSON to the target type — wrong field name, wrong type — it throws HttpMessageNotReadableException and returns 400. The error message tells you exactly which field failed, but only if you log it. In production, many teams swallow this into a generic error response and lose the detail."
        }
      ],
      "cap": "The @Valid + @RequestBody combination is the standard pattern for POST and PUT endpoints — it handles both deserialization and validation in one shot.",
      "followup_handoff": [
        "How do you validate a @RequestBody in Spring Boot?",
        "What is the difference between @Valid and @Validated?"
      ],
      "tts_overrides": {
        "400": "four hundred",
        "415": "four fifteen",
        "@RequestBody": "@RequestBody",
        "@Valid": "@Valid",
        "ObjectMapper": "Object Mapper",
        "HttpMessageNotReadableException": "Http Message Not Readable Exception",
        "Content-Type": "Content Type",
        "JSON": "J-S-O-N",
        "HTTP": "H-T-T-P"
      },
      "speakable_status": "pending"
    }
  },
  {
    "id": "request-body-response-body-002",
    "slug": "responsebody-in-spring-mvc-java-object-to-json-serialization",
    "question": "What does @ResponseBody do in Spring MVC?",
    "title": "@ResponseBody in Spring MVC — Java Object to JSON Serialization",
    "direct_answer": "@ResponseBody tells Spring MVC to write a handler return value to the HTTP response through an HttpMessageConverter instead of resolving it as a view name. JSON with Jackson is one possible representation, not the definition. @RestController applies @ResponseBody semantics to its methods automatically.",
    "layout_type": "concept-explainer",
    "difficulty": "easy",
    "importance": "high",
    "reading_time_minutes": 2,
    "last_updated": "2026-09-08",
    "interviewer_intent": {
      "testing": "Understanding of serialization direction and @ResponseBody's role",
      "common_mistake": "Adding @ResponseBody explicitly to @RestController methods — it's already included",
      "to_stand_out": "Explain that @RestController = @Controller + @ResponseBody as a meta-annotation"
    },
    "company_tags": [
      "tcs",
      "infosys",
      "wipro",
      "amazon"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "What @ResponseBody Writes",
          "content": "@ResponseBody affects return-value handling only. Request mapping, argument binding, validation, and exception handling still follow the normal MVC pipeline.\n\nThe response Content-Type should describe what the converter actually writes. Avoid manually converting every DTO to a JSON String: returning the object lets Spring select the converter and negotiate the representation, and avoids accidentally labelling JSON text as plain text."
        },
        {
          "type": "key_points",
          "title": "Quick revision",
          "content": [
            "Response-body handling bypasses view-name resolution.",
            "Converter selection uses the return type and negotiated media type.",
            "A String can become text; a DTO can become JSON or XML when converters support it.",
            "ResponseEntity adds status and headers while using the same body-conversion path."
          ]
        },
        {
          "type": "speakable_answer",
          "title": "Interview answer",
          "content": "Returning an OrderDto from a response-body method commonly produces JSON because the web starter supplies Jackson support. Returning byte[] or Resource can use a different converter. A produces condition and Accept header can narrow the response media type. If no converter can write the chosen type, the annotation cannot make serialization succeed on its own.",
          "answerSize": "compact"
        },
        {
          "type": "concept_map",
          "title": "@ResponseBody at a Glance",
          "content": "amber|@ResponseBody|~Return value to body|object to JSON|skips ViewResolver|implicit in @RestController\nblue|How|~HttpMessageConverter|serializes the return|writes to response body|Jackson by default\nemerald|Use for|~REST responses|DTOs, lists, records|data not HTML|@RestController everywhere\nrose|Trap|~On @Controller returning String|without it = view name|with it = raw body|@RestController avoids this"
        },
        {
          "type": "code_example",
          "title": "Returning JSON with @ResponseBody",
          "content": "@Controller\n@RequestMapping(\"/api/products\")\npublic class ProductController {\n\n    @GetMapping(\"/{id}\")\n    @ResponseBody // explicitly needed because this is @Controller, not @RestController\n    public Product getProduct(@PathVariable Long id) {\n        return productService.findById(id);\n        // Spring serializes Product object to JSON automatically\n    }\n}\n\n// Better: use @RestController so @ResponseBody is implicit\n@RestController\n@RequestMapping(\"/api/orders\")\npublic class OrderController {\n\n    @GetMapping\n    public List<Order> getAllOrders() {\n        return orderService.findAll(); // no @ResponseBody needed, it's implicit\n    }\n}\n\nrecord Product(Long id, String name, double price) {}\nrecord Order(Long id, String customer, List<String> items) {}"
        },
        {
          "type": "when_to_use",
          "title": "When to Use @ResponseBody",
          "content": "Use @ResponseBody when you need to return JSON data directly from a controller method, not an HTML view. **REST API endpoints** — every GET, POST, PUT, DELETE that returns JSON needs this (implicitly via @RestController or explicitly with @ResponseBody). **AJAX handlers** — when a web page makes an asynchronous request and expects JSON back, not a full page reload. **Mixed controllers** — if you have one controller handling both HTML views and JSON endpoints, add @ResponseBody to the JSON methods only. Don't use @ResponseBody if your method returns a view name String for template rendering — that's traditional Spring MVC behavior. In practice, almost all REST APIs today use @RestController instead of manually adding @ResponseBody to every method."
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **Adding @ResponseBody to @RestController methods**: Freshers see @ResponseBody in tutorials and add it explicitly even when using @RestController. It's redundant — @RestController already includes @ResponseBody on every method.\n- **Returning String thinking it will be JSON**: If you return a plain String like \"success\" from a @ResponseBody method, Spring sends exactly that string (not JSON-encoded). Freshers expect {\"message\":\"success\"} but get the raw text. Return an object or record instead.\n- **Mixing view names with @ResponseBody**: In a @Controller with @ResponseBody on some methods, freshers forget which mode they're in. A method without @ResponseBody tries to find a template named after the returned String, leading to template not found errors. Keep REST endpoints in @RestController classes separate from view controllers."
        }
      ]
    },
    "followup_questions": [
      "What is the difference between returning a String from @Controller and @RestController?",
      "What happens when a @RestController method returns null?",
      "How do you return both JSON and HTML from the same controller class?"
    ],
    "seo": {
      "metaTitle": "@ResponseBody in Spring MVC | Java Fresher Interview Guide",
      "metaDescription": "Learn @ResponseBody in Spring MVC — Java to JSON serialization, implicit in @RestController, selective use in @Controller, and Content-Type header."
    },
    "order": 2,
    "speakable_v2": {
      "question_id": "request-body-response-body-002",
      "archetype": "A",
      "pillar": "P04",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API serving a mobile client",
      "familiarity_anchors": [
        "Spring MVC",
        "REST",
        "controllers"
      ],
      "hook": "@ResponseBody flips the direction — instead of going to a template, your return value goes straight to the response as JSON.",
      "beats": [
        {
          "type": "paragraph",
          "label": "what_it_is",
          "content": "@ResponseBody tells Spring that a method's return value should be written directly into the HTTP response body, not treated as a view name. Jackson serializes the return object to JSON and Spring sets Content-Type to application/json."
        },
        {
          "type": "paragraph",
          "label": "why_it_exists",
          "content": "In a traditional @Controller, return values are view names — Spring looks for a template file. @ResponseBody overrides that behavior for specific methods. @RestController puts @ResponseBody on every method in the class implicitly."
        },
        {
          "type": "paragraph",
          "label": "example",
          "content": "The only time you'd add @ResponseBody explicitly is in a @Controller class that mostly returns HTML pages, but needs one specific method to return JSON. You're mixing view-based and REST endpoints in the same class."
        },
        {
          "type": "paragraph",
          "label": "pitfalls",
          "content": "candidates miss: If a @RestController method returns null, Spring writes an empty body with status 200 — not a 404. If a method returns void, same thing: 200 with no body. Returning null is rarely the right behavior; use ResponseEntity.notFound() or throw a proper exception instead."
        }
      ],
      "cap": "@RequestBody and @ResponseBody are mirrors: one deserializes incoming JSON, the other serializes outgoing objects. Together they define the contract of your REST endpoint.",
      "followup_handoff": [
        "What happens when a @RestController method returns null?",
        "How do you return both JSON and HTML from the same controller class?"
      ],
      "tts_overrides": {
        "200": "two hundred",
        "404": "four oh four",
        "@ResponseBody": "@ResponseBody",
        "@RequestBody": "@RequestBody",
        "@Controller": "@Controller",
        "@RestController": "@RestController",
        "ResponseEntity": "Response Entity",
        "Content-Type": "Content Type",
        "JSON": "J-S-O-N",
        "HTTP": "H-T-T-P"
      },
      "speakable_status": "pending"
    }
  },
  {
    "id": "request-body-response-body-004",
    "slug": "requestheader-and-cookievalue-in-spring-mvc-header-and-cookie-extraction",
    "question": "What is @RequestHeader and @CookieValue in Spring MVC?",
    "title": "@RequestHeader and @CookieValue in Spring MVC — Header and Cookie Extraction",
    "direct_answer": "@RequestHeader binds one header value, a collection, or the full header map to a controller argument. @CookieValue binds a named HTTP cookie value. required=false, Optional, or defaultValue can represent absence, but a default should be used only when it has real domain meaning. Authentication is normally handled by Spring Security filters and SecurityContext rather than manually parsing credentials in every controller.",
    "layout_type": "concept-explainer",
    "difficulty": "easy",
    "importance": "medium",
    "reading_time_minutes": 2,
    "last_updated": "2026-09-08",
    "interviewer_intent": {
      "testing": "Knowledge of additional Spring MVC parameter binding options",
      "common_mistake": "Manually parsing HttpServletRequest for header values instead of using @RequestHeader",
      "to_stand_out": "Mention that @RequestHeader can inject all headers as Map<String, String>"
    },
    "company_tags": [
      "tcs",
      "infosys",
      "wipro",
      "accenture"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "Binding Request Headers and Cookies",
          "content": "Spring converts header/cookie text to supported Java types through its conversion system. Multiple header values and comma handling need the target type and HTTP rules considered. Cookie decoding does not by itself make a value trusted; validate and authorize any value that affects protected behavior."
        },
        {
          "type": "key_points",
          "title": "Quick revision",
          "content": [
            "HTTP header names are case-insensitive.",
            "Do not mark a required protocol precondition optional merely to avoid an error.",
            "Cookies still need Secure, HttpOnly, SameSite, domain/path, expiry, and CSRF considerations.",
            "Use trusted tracing/security infrastructure for correlation and identity where available."
          ]
        },
        {
          "type": "speakable_answer",
          "title": "Interview answer",
          "content": "For example, @RequestHeader(name=\"If-Match\", required=false) String etag can support an optional conditional update, while @CookieValue(name=\"theme\", defaultValue=\"light\") can supply a harmless display default. A bearer token should be authenticated by Spring Security, after which the controller reads the authenticated principal rather than the raw Authorization header.",
          "answerSize": "compact"
        },
        {
          "type": "concept_map",
          "title": "@RequestHeader and @CookieValue",
          "content": "amber|@RequestHeader|~Reads a header|Authorization, User-Agent|into a parameter|required by default\nblue|@CookieValue|~Reads a cookie|session token, prefs|by cookie name|same options\nemerald|Options|~required + defaultValue|required=false for optional|defaultValue fallback|avoid missing errors\nrose|Trap|~Header missing|400 if required|use required=false|or a default value"
        },
        {
          "type": "code_example",
          "title": "Using @RequestHeader and @CookieValue",
          "content": "@RestController\n@RequestMapping(\"/api/users\")\npublic class UserController {\n\n    @GetMapping(\"/profile\")\n    public ResponseEntity<UserProfile> getProfile(\n        @RequestHeader(\"Authorization\") String authHeader,\n        @RequestHeader(value = \"X-Request-ID\", required = false, defaultValue = \"unknown\") String requestId,\n        @CookieValue(value = \"session-id\", required = false) String sessionId\n    ) {\n        var token = authHeader.replace(\"Bearer \", \"\"); // extract JWT token\n        var profile = userService.getProfile(token);\n        return ResponseEntity.ok(profile);\n    }\n\n    @GetMapping(\"/debug\")\n    public Map<String, String> debugHeaders(@RequestHeader Map<String, String> headers) {\n        return headers; // returns all request headers as JSON\n    }\n}\n\nrecord UserProfile(String username, String email) {}"
        },
        {
          "type": "when_to_use",
          "title": "When to Use @RequestHeader and @CookieValue",
          "content": "Use **@RequestHeader** when your API needs to read metadata sent by the client. **Authentication endpoints** — extract the Authorization header containing a JWT or API key. **Correlation IDs** — read X-Request-ID or X-Correlation-ID headers for distributed tracing across services. **API versioning** — some APIs use Accept or custom headers like X-API-Version to select the response format or behavior.\n\nUse **@CookieValue** when you need to read session identifiers or user preferences stored in cookies. **Session-based authentication** — extract JSESSIONID or a custom session cookie. **Feature flags** — read a cookie that tracks which features are enabled for this user. Don't use these annotations if you need to access all headers or cookies repeatedly — inject the full Map instead. Also avoid them for body data — use @RequestBody for that."
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **Manually parsing HttpServletRequest**: Freshers inject HttpServletRequest and call request.getHeader(\"Authorization\") instead of using at-RequestHeader. This is verbose, harder to test, and not idiomatic Spring. Use the annotation.\n- **Not handling missing headers**: Freshers don't set required=false when a header is optional. If the client doesn't send it, Spring throws an exception and returns a 400 error. For optional metadata, always add required=false and a defaultValue.\n- **Case sensitivity confusion**: HTTP headers are case-insensitive per the spec, but freshers sometimes worry about exact casing. Spring normalizes header names, so \"Authorization\", \"authorization\", and \"AUTHORIZATION\" all work. Focus on consistency in your code, not exact casing.\n- **Reading cookies manually**: Freshers parse the Cookie header as a String and split it. Use at-CookieValue instead — Spring extracts and decodes the cookie automatically."
        }
      ]
    },
    "followup_questions": [
      "How do you extract a JWT token from the Authorization header in Spring?",
      "What is the difference between @RequestHeader and HttpServletRequest.getHeader()?",
      "How do you inject all HTTP headers as a map in Spring MVC?"
    ],
    "seo": {
      "metaTitle": "@RequestHeader and @CookieValue in Spring MVC | Fresher Interview",
      "metaDescription": "Learn @RequestHeader and @CookieValue in Spring MVC — extracting HTTP headers and cookies, optional headers with defaultValue, Map injection."
    },
    "order": 4,
    "speakable_v2": {
      "question_id": "request-body-response-body-004",
      "archetype": "A",
      "pillar": "P04",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API serving a mobile client",
      "familiarity_anchors": [
        "Spring MVC",
        "REST",
        "controllers"
      ],
      "hook": "@RequestHeader saves you from digging into HttpServletRequest just to read one header value.",
      "beats": [
        {
          "type": "paragraph",
          "label": "what_it_is",
          "content": "@RequestHeader extracts a specific HTTP request header into a method parameter. @CookieValue does the same for cookie values. Spring binds them by name automatically."
        },
        {
          "type": "paragraph",
          "label": "why_it_exists",
          "content": "The alternative is injecting HttpServletRequest and calling getHeader manually. @RequestHeader is cleaner, testable, and declarative — the method signature documents exactly which headers the endpoint depends on."
        },
        {
          "type": "bullets",
          "label": "parts_or_states",
          "content": [
            "required=false with defaultValue: makes the header optional and provides a fallback.",
            "Map<String, String> with @RequestHeader: injects all headers at once.",
            "HTTP headers are case-insensitive — Spring normalizes casing automatically.",
            "@CookieValue follows the same pattern for cookie names."
          ]
        },
        {
          "type": "paragraph",
          "label": "pitfalls",
          "content": "gotcha: If a required header is missing, Spring returns 400 Bad Request with a MissingRequestHeaderException. If you expect the header might not always be present — like an optional correlation ID — mark it required=false so missing headers don't break the request."
        }
      ],
      "cap": "Common real-world use: extract the Authorization Bearer token from the Authorization header for manual validation before Spring Security is in place.",
      "followup_handoff": [
        "How do you extract a JWT token from the Authorization header in Spring?",
        "How do you inject all HTTP headers as a map in Spring MVC?"
      ],
      "tts_overrides": {
        "400": "four hundred",
        "@RequestHeader": "@RequestHeader",
        "@CookieValue": "@CookieValue",
        "HttpServletRequest": "Http Servlet Request",
        "MissingRequestHeaderException": "Missing Request Header Exception",
        "HTTP": "H-T-T-P",
        "JWT": "J-W-T"
      },
      "speakable_status": "pending"
    }
  },
  {
    "id": "request-body-response-body-005",
    "slug": "requestparam-vs-requestbody-query-string-vs-json-body",
    "question": "What is the difference between @RequestParam and @RequestBody?",
    "title": "@RequestParam vs @RequestBody — Query String vs JSON Body",
    "direct_answer": "@RequestParam binds request parameters such as query-string or form values. @RequestBody reads the request body through an HttpMessageConverter. The body is not necessarily JSON and query parameters are not limited to GET. Choose from the API contract: filters/options often fit query parameters, while a structured create/update command commonly belongs in the body.",
    "layout_type": "comparison-arena",
    "difficulty": "easy",
    "importance": "high",
    "reading_time_minutes": 2,
    "last_updated": "2026-09-08",
    "interviewer_intent": {
      "testing": "Understanding where HTTP request data lives and how to extract it in Spring MVC",
      "common_mistake": "Trying to use @RequestParam to capture complex JSON objects instead of @RequestBody",
      "to_stand_out": "Explain the HTTP fundamentals: query params are visible in URL and limited to simple values; request body carries structured data and isn't visible in browser address bar"
    },
    "company_tags": [
      "tcs",
      "infosys",
      "wipro",
      "amazon"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "@RequestParam and @RequestBody Read Different Parts of HTTP",
          "content": "HTTP does not make @RequestBody synonymous with POST/PUT or JSON. Some methods/clients/proxies have conventions and interoperability limits around bodies, so design for the ecosystem. Query strings also have practical length limits imposed by clients and intermediaries rather than one universal browser number.\n\nValidate both sources and define which wins if the same concept appears twice. Avoid duplicated parameters in path, query, and body unless their roles are distinct.\n\n**Example.** POST /orders?notify=true can bind notify with @RequestParam and the JSON order fields with @RequestBody CreateOrderRequest. Query parameters are suitable for small modifiers; the body carries the structured representation."
        },
        {
          "type": "key_points",
          "title": "Quick revision",
          "content": [
            "Query: /users?page=2&size=10 -> @RequestParam.",
            "Body with Content-Type application/json -> @RequestBody DTO through Jackson when configured.",
            "A request can use both, for example path identity + query option + JSON command body.",
            "Do not put secrets in URLs because URLs are commonly logged and cached/history-visible."
          ]
        },
        {
          "type": "speakable_answer",
          "title": "Interview answer",
          "content": "POST /orders?dryRun=true can bind dryRun with @RequestParam and the order command with @RequestBody. Spring uses Content-Type to choose how to read the body. text/plain can bind String; application/json can bind a DTO through Jackson. A long nested object is clearer in a body, but simple query filters are easier to cache, link, and inspect in the URL.",
          "answerSize": "compact"
        },
        {
          "type": "concept_map",
          "title": "@RequestParam vs @RequestBody",
          "content": "amber|@RequestParam|~From the query string|/users?page=2|small scalar values|GET filters\nblue|@RequestBody|~From the request body|JSON payload|structured objects|POST/PUT\nemerald|Rule|~Params for simple inputs|body for resources|one body per request|many params allowed\nrose|Trap|~@RequestBody on GET|GET has no body|use @RequestParam|or switch to POST"
        },
        {
          "type": "code_example",
          "title": "Side-by-Side Comparison in a Controller",
          "content": "@RestController\n@RequestMapping(\"/api/products\")\npublic class ProductController {\n\n    // @RequestParam: extracts from query string\n    @GetMapping\n    public List<Product> search(\n            @RequestParam String category,\n            @RequestParam(defaultValue = \"0\") int page,\n            @RequestParam(defaultValue = \"20\") int size) {\n        // Called via: GET /api/products?category=electronics&page=0&size=20\n        return productService.findByCategory(category, page, size);\n    }\n\n    // @RequestBody: extracts from request body (JSON payload)\n    @PostMapping\n    public Product create(@RequestBody CreateProductRequest request) {\n        // Called via: POST /api/products with JSON body:\n        // {\"name\":\"Laptop\",\"price\":999.99,\"category\":\"electronics\"}\n        return productService.create(request);\n    }\n}\n\nrecord CreateProductRequest(String name, double price, String category) {}"
        },
        {
          "type": "when_to_use",
          "title": "Decision Rules for Choosing Between Them",
          "content": "Use **@RequestParam** when you need simple filtering, pagination, or optional parameters in GET requests — scenarios where the client is refining a query (e.g., `/orders?status=shipped&date=2024-01-15`). It's perfect for values that users might want to bookmark or share via URL.\n\nUse **@RequestBody** when you're creating or updating resources with POST/PUT/PATCH and need to send complex structured data like nested objects or arrays (e.g., creating an order with multiple line items). If your data has more than 3-4 fields or includes nested structures, it belongs in the body. Query params have URL length limits (~2000 chars in most browsers) and can't represent hierarchies well, while the body can handle megabytes of JSON with deep nesting."
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **Using @RequestParam for complex objects**: Freshers sometimes try to pass a whole User object as query params like `?name=John&email=john@example.com&age=25`. This works for tiny objects but becomes unmaintainable and hits URL length limits. Complex objects should go in @RequestBody as JSON.\n- **Forgetting @RequestParam is required by default**: If a query param is missing, Spring throws an error unless you add `required = false` or provide a `defaultValue`. Freshers often forget this and get confusing 400 Bad Request errors.\n- **Using @RequestBody with GET requests**: HTTP GET requests typically don't have a body (some clients ignore it). If you need to send complex criteria for a search, either use POST for the search endpoint or flatten the params into query strings with multiple @RequestParam annotations."
        }
      ]
    },
    "followup_questions": [
      "Can a GET request have a @RequestBody?",
      "How do you handle both @RequestParam and @RequestBody in the same POST endpoint?",
      "What is @ModelAttribute and how does it differ from @RequestBody?"
    ],
    "seo": {
      "metaTitle": "@RequestParam vs @RequestBody in Spring | Fresher Interview",
      "metaDescription": "Understand the difference between @RequestParam (query string) and @RequestBody (JSON body) in Spring MVC — when to use each with GET and POST examples."
    },
    "order": 5,
    "speakable_v2": {
      "question_id": "request-body-response-body-005",
      "archetype": "B",
      "pillar": "P04",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API serving a mobile client",
      "familiarity_anchors": [
        "Spring MVC",
        "REST",
        "controllers"
      ],
      "hook": "@RequestParam and @RequestBody both get data from the client — but from completely different places.",
      "beats": [
        {
          "type": "paragraph",
          "label": "what_each_is",
          "content": "@RequestParam reads from the URL query string — those key-value pairs after the question mark. Something like /users?page=0&size=10&status=ACTIVE. Simple primitive types and strings."
        },
        {
          "type": "paragraph",
          "label": "what_each_is",
          "content": "@RequestBody reads from the HTTP request body — typically a JSON payload. Used for complex objects in POST and PUT requests. The client sends JSON, Jackson deserializes it into your Java object."
        },
        {
          "type": "mini_table",
          "label": "differences",
          "headers": [
            "",
            "@RequestParam",
            "@RequestBody"
          ],
          "rows": [
            [
              "Data source",
              "URL query string",
              "HTTP request body"
            ],
            [
              "Data type",
              "Primitives, strings",
              "Complex objects (JSON)"
            ],
            [
              "Used with",
              "GET, filtering",
              "POST, PUT, PATCH"
            ],
            [
              "Content-Type needed",
              "No",
              "application/json"
            ]
          ]
        },
        {
          "type": "paragraph",
          "label": "when_to_pick",
          "content": "Use @RequestParam for filtering, sorting, and pagination parameters in GET requests. Use @RequestBody for the main payload in create and update operations. You can use both in the same method — for example, a POST with a JSON body and a ?dryRun=true query parameter."
        },
        {
          "type": "paragraph",
          "label": "tiny_example",
          "content": "candidates miss: GET requests technically can have a body, but it's against REST conventions and many proxies and clients strip it. Never use @RequestBody on a GET endpoint in practice."
        }
      ],
      "cap": "A simple rule: GET requests use @RequestParam and @PathVariable. POST and PUT requests use @RequestBody for the main data.",
      "followup_handoff": [
        "Can a GET request have a @RequestBody?",
        "What is @ModelAttribute and how does it differ from @RequestBody?"
      ],
      "tts_overrides": {
        "@RequestParam": "@RequestParam",
        "@RequestBody": "@RequestBody",
        "@PathVariable": "@PathVariable",
        "Content-Type": "Content Type",
        "HTTP": "H-T-T-P",
        "JSON": "J-S-O-N",
        "URL": "U-R-L"
      },
      "speakable_status": "pending"
    }
  },
  {
    "id": "request-body-response-body-003",
    "slug": "validating-requestbody-in-spring-boot-valid-and-bean-validation",
    "question": "How do you validate a @RequestBody in Spring Boot?",
    "title": "Validating @RequestBody in Spring Boot — @Valid and Bean Validation",
    "direct_answer": "Put Jakarta Bean Validation constraints on the request DTO and add @Valid or @Validated at the controller boundary. Spring first uses an HttpMessageConverter to deserialize the body and then validates the created object. Invalid method arguments normally raise MethodArgumentNotValidException; the default or custom exception handling decides the exact 400 response body.",
    "layout_type": "recipe-builder",
    "difficulty": "medium",
    "importance": "high",
    "reading_time_minutes": 3,
    "last_updated": "2026-09-08",
    "interviewer_intent": {
      "testing": "Input validation knowledge — critical for any production API",
      "common_mistake": "Manually checking fields in service layer — use Bean Validation annotations instead",
      "to_stand_out": "Show how to customize the 400 error response using @ControllerAdvice to catch MethodArgumentNotValidException"
    },
    "company_tags": [
      "amazon",
      "tcs",
      "infosys",
      "microsoft"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "Validating a Request Body",
          "content": "Malformed JSON can fail during conversion before Bean Validation, while valid JSON with invalid field values reaches validation. Spring Boot commonly maps these failures to 400, but exposing detailed field errors requires a suitable error handler/configuration. Constraints on nested values require @Valid on the nested property/collection elements where appropriate.\n\n**Example.** A blank @NotBlank name produces a validation violation after JSON conversion, while malformed JSON fails earlier during message conversion."
        },
        {
          "type": "key_points",
          "title": "Quick revision",
          "content": [
            "Use @NotNull for presence and @NotBlank for nonblank text.",
            "@Email checks email-like syntax but usually allows null; combine constraints when presence is required.",
            "Use validation groups only for a real operation-specific rule.",
            "Keep cross-record/domain rules in an appropriate custom constraint or service boundary."
          ]
        },
        {
          "type": "speakable_answer",
          "title": "Interview answer",
          "content": "For example, record CreateUser(@NotBlank String name, @NotBlank @Email String email, @Min(18) int age) can be accepted as @Valid @RequestBody CreateUser request. An empty name and malformed email create field violations before the method body runs. A duplicate email still needs a database/domain check because Bean Validation cannot make a race-free uniqueness guarantee.",
          "answerSize": "compact"
        },
        {
          "type": "concept_map",
          "title": "Validating a @RequestBody",
          "content": "amber|@Valid|~Triggers validation|before controller body runs|on the @RequestBody DTO|JSR-380 Bean Validation\nblue|Constraints|~On DTO fields|@NotNull, @Size, @Email|@Min, @Pattern|declarative rules\nemerald|On failure|~MethodArgumentNotValidException|maps to 400|handle via @ControllerAdvice|return field errors\nrose|Trap|~Forgetting @Valid|constraints ignored|invalid data accepted|always annotate the param"
        },
        {
          "type": "code_example",
          "title": "Validating Request Body with @Valid",
          "content": "@RestController\n@RequestMapping(\"/api/users\")\npublic class UserController {\n\n    @PostMapping\n    public ResponseEntity<User> createUser(@Valid @RequestBody CreateUserRequest request) {\n        var user = userService.create(request);\n        return ResponseEntity.status(HttpStatus.CREATED).body(user);\n    }\n}\n\nrecord CreateUserRequest(\n    @NotBlank(message = \"Name is required\") String name,\n    @Email(message = \"Invalid email format\") String email,\n    @Min(value = 18, message = \"Must be at least 18\") int age\n) {}\n\n// If client sends {\"name\":\"\",\"email\":\"bad-email\",\"age\":15}:\n// Spring validates, finds 3 violations, throws MethodArgumentNotValidException\n// Returns 400 with error details before createUser method runs\n\nrecord User(Long id, String name, String email, int age) {}"
        },
        {
          "type": "when_to_use",
          "title": "When to Use @Valid with @RequestBody",
          "content": "Use @Valid with @RequestBody on **every API endpoint that accepts user input**. POST endpoints creating resources — validate that required fields are present and correctly formatted. PUT endpoints for updates — ensure the replacement data meets all constraints. PATCH endpoints for partial updates — validate only the fields being changed. The pattern is universal: if an endpoint takes a complex object from the client, add @Valid to catch malformed data immediately. Don't rely on database constraints alone — they trigger too late, after your business logic has run. Also don't manually validate in service methods — that scatters validation logic across your codebase. Keep validation rules colocated with the DTO using Bean Validation annotations, then trigger them with @Valid in the controller."
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **Validating in service layer instead of controller**: Freshers write if (user.getEmail() == null) throw exception logic in services. This is tedious and error-prone. Use Bean Validation annotations on the DTO and @Valid in the controller — validation happens automatically before service code runs.\n- **Forgetting @Valid annotation**: You add validation annotations like at-NotNull to DTO fields but forget at-Valid on the @RequestBody parameter. Spring deserializes the object but never validates it, so invalid data reaches your service layer. Always pair Bean Validation annotations with at-Valid.\n- **Not customizing error responses**: The default 400 response Spring returns is verbose and exposes internal field names. Freshers leave it as-is. Production APIs need clean error responses. Use at-ControllerAdvice with an at-ExceptionHandler for MethodArgumentNotValidException to return a structured error response with only the field name and user-friendly message."
        }
      ]
    },
    "followup_questions": [
      "What exception does @Valid throw when validation fails?",
      "How do you return a custom error response when @Valid fails?",
      "What is the difference between @NotNull, @NotEmpty, and @NotBlank?"
    ],
    "seo": {
      "metaTitle": "Validating @RequestBody with @Valid in Spring Boot | Interview",
      "metaDescription": "Learn how to validate @RequestBody in Spring Boot — @Valid, Bean Validation annotations @NotBlank @Email @Min @Max, MethodArgumentNotValidException handling."
    },
    "order": 3,
    "speakable_v2": {
      "question_id": "request-body-response-body-003",
      "archetype": "A",
      "pillar": "P04",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API serving a mobile client",
      "familiarity_anchors": [
        "Spring MVC",
        "REST",
        "controllers"
      ],
      "hook": "Adding @Valid before @RequestBody turns your controller into a validator — no if-null checks, no boilerplate.",
      "beats": [
        {
          "type": "paragraph",
          "label": "what_it_is",
          "content": "When you add @Valid before @RequestBody, Spring runs Bean Validation on the deserialized object before your method body runs. If any constraint fails, Spring automatically returns 400 Bad Request with error details."
        },
        {
          "type": "paragraph",
          "label": "why_it_exists",
          "content": "You annotate your DTO fields with validation constraints. @NotBlank for required strings, @Email for email format, @Min and @Max for number ranges, @Size for length. When the request comes in, Spring checks all of them in one pass."
        },
        {
          "type": "bullets",
          "label": "parts_or_states",
          "content": [
            "@NotNull: field must not be null.",
            "@NotBlank: string must not be null, empty, or whitespace.",
            "@Email: string must be a valid email format.",
            "@Min and @Max: number must be within the given range.",
            "@Size(min, max): string or collection must be within the length range.",
            "@Pattern(regexp): string must match a custom regular expression."
          ]
        },
        {
          "type": "paragraph",
          "label": "pitfalls",
          "content": "non-obvious: The default 400 response from Spring is a MethodArgumentNotValidException with a nested error structure. Most production teams add a @ControllerAdvice to catch this exception and return a clean, consistent error JSON — like a list of field names and their error messages. Without that, the default error body can be verbose and inconsistent."
        }
      ],
      "cap": "Add spring-boot-starter-validation to your pom.xml or build.gradle — without it, the @Valid annotation is present but the constraints are silently ignored.",
      "followup_handoff": [
        "How do you return a custom error response when @Valid fails?",
        "What is the difference between @NotNull, @NotEmpty, and @NotBlank?"
      ],
      "tts_overrides": {
        "400": "four hundred",
        "@Valid": "@Valid",
        "@RequestBody": "@RequestBody",
        "@NotBlank": "@NotBlank",
        "@NotNull": "@NotNull",
        "@NotEmpty": "@NotEmpty",
        "@Email": "@Email",
        "@Min": "@Min",
        "@Max": "@Max",
        "@Size": "@Size",
        "@Pattern": "@Pattern",
        "@ControllerAdvice": "@ControllerAdvice",
        "MethodArgumentNotValidException": "Method Argument Not Valid Exception",
        "spring-boot-starter-validation": "spring boot starter validation",
        "JSON": "J-S-O-N",
        "DTO": "D-T-O"
      },
      "speakable_status": "pending"
    }
  },
  {
    "id": "request-body-response-body-006",
    "slug": "dto-in-spring-rest-data-transfer-object-best-practice",
    "question": "What is a DTO and why should you use it in REST APIs?",
    "title": "DTO in Spring REST — Data Transfer Object Best Practice",
    "direct_answer": "A DTO is a Java value used to carry a request or response shape without exposing the persistence/domain object as the wire contract. It can include only the fields for one use case, carry validation, and keep API evolution separate from entity mapping. DTOs are especially valuable at public, security-sensitive, or independently versioned boundaries; they are a design choice rather than a rule that every tiny internal endpoint needs a distinct class per operation.",
    "layout_type": "concept-explainer",
    "difficulty": "medium",
    "importance": "high",
    "reading_time_minutes": 3,
    "last_updated": "2026-09-08",
    "interviewer_intent": {
      "testing": "Understanding API design best practices and the dangers of exposing domain entities directly",
      "common_mistake": "Returning JPA @Entity classes directly from controllers, which exposes internal DB structure and causes lazy-loading serialization errors",
      "to_stand_out": "Mention specific risks: exposing passwords or sensitive fields, triggering N+1 queries from lazy collections, coupling API clients to your database schema changes"
    },
    "company_tags": [
      "amazon",
      "microsoft",
      "infosys",
      "tcs"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "DTOs Define an API Representation",
          "content": "DTOs do not make data safe by themselves. Validate input, enforce authorization, and define serialization carefully. Returning entities can be reasonable in a narrow prototype, but it couples HTTP to persistence and becomes risky as relationships, proxies, sensitive fields, or versioning needs grow.\n\n**Example.** CreateUserRequest can accept a password, while UserResponse deliberately omits passwordHash and persistence-only audit fields."
        },
        {
          "type": "key_points",
          "title": "Quick revision",
          "content": [
            "Request and response needs often differ, especially around ids and secrets.",
            "Map only intended fields; never expose password hashes or internal relations.",
            "Avoid lazy-loading and recursion surprises from serializing JPA entities directly.",
            "Balance explicit contracts against needless one-field wrapper types in controlled internal code."
          ]
        },
        {
          "type": "speakable_answer",
          "title": "Interview answer",
          "content": "For example, CreateUserRequest can contain name, email, and a plaintext password accepted over TLS, while UserResponse contains id, name, and email but never passwordHash. Mapping can be explicit Java code or a tested mapper such as MapStruct. The entity can later gain audit columns without silently changing the response JSON.",
          "answerSize": "standard"
        },
        {
          "type": "concept_map",
          "title": "Why Use a DTO",
          "content": "amber|DTO|~Data Transfer Object|API input/output shape|only the needed fields|decoupled from entity\nblue|Why|~Hide internals|no password leaks|stable API contract|evolve DB freely\nemerald|Mapping|~Entity to DTO|manual or MapStruct|in the service layer|both directions\nrose|Trap|~Exposing a JPA entity|leaks columns + relations|lazy-load issues|always map to a DTO"
        },
        {
          "type": "code_example",
          "title": "DTO Pattern in a Spring REST Controller",
          "content": "@RestController\n@RequestMapping(\"/api/users\")\npublic class UserController {\n    private final UserService userService;\n\n    // Input DTO: only fields needed to create a user\n    @PostMapping\n    public UserResponse createUser(@RequestBody CreateUserRequest request) {\n        var user = userService.create(request);\n        return toResponse(user); // Convert entity to output DTO\n    }\n\n    // Output DTO: exclude sensitive fields, include computed fields\n    @GetMapping(\"/{id}\")\n    public UserResponse getUser(@PathVariable Long id) {\n        var user = userService.findById(id);\n        return toResponse(user);\n    }\n\n    private UserResponse toResponse(User user) {\n        return new UserResponse(user.getId(), user.getName(), user.getEmail());\n    }\n}\n\n// Input DTO: what the client sends\nrecord CreateUserRequest(String name, String email, String password) {}\n\n// Output DTO: what the API returns (no password!)\nrecord UserResponse(Long id, String name, String email) {}"
        },
        {
          "type": "when_to_use",
          "title": "When to Introduce DTOs",
          "content": "Always use DTOs in **production REST APIs** — even for simple CRUD operations. The cost of creating a DTO class is tiny compared to the cost of accidentally exposing a password field or breaking API clients when you refactor your database schema.\n\nUse separate DTOs for **input and output** — a `CreateUserRequest` might require `password`, but `UserResponse` should never include it.\n\nUse separate DTOs for **different operations** — `UserSummary` for list endpoints might have fewer fields than `UserDetail` for single-resource endpoints, reducing payload size. When you have **complex entities with bidirectional relationships**, DTOs are non-negotiable — returning a User entity with a `List<Order>` where each Order has a `User user` field creates circular references that break Jackson serialization unless you add @JsonIgnore annotations, which pollutes your domain model with API concerns. If you're building a quick internal prototype with no security concerns, you might skip DTOs temporarily, but they should be the first refactoring before any real usage."
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **Returning JPA entities directly from controllers**: Freshers often do `return userRepository.findById(id).orElseThrow()` straight from the controller. This exposes every field in the entity, including `passwordHash`, `version`, `createdBy`, and triggers lazy-loading exceptions if Jackson tries to serialize a `@OneToMany` collection. Always map to a DTO before returning.\n- **Creating one DTO for everything**: Having a single `UserDTO` used for create, update, list, and detail responses defeats the purpose. Each operation should have its own DTO with exactly the fields needed — `CreateUserRequest`, `UpdateUserRequest`, `UserSummary`, `UserDetail`.\n- **Manual mapping hell**: Writing `new UserResponse(user.getId(), user.getName(), ...)` everywhere is tedious. Use a mapping library like MapStruct (compile-time) or ModelMapper (runtime) to automate conversion, or put the mapping logic in a dedicated mapper class.\n- **Not validating input DTOs**: Just because you're using a DTO doesn't mean the data is safe — add Bean Validation annotations like @NotNull, @Email, @Size to input DTOs so Spring validates them automatically when paired with @Valid."
        }
      ]
    },
    "followup_questions": [
      "What is MapStruct and how does it help with DTO mapping?",
      "What are the risks of returning JPA entities directly from REST controllers?",
      "What is the difference between a Request DTO and a Response DTO?"
    ],
    "seo": {
      "metaTitle": "DTO in Spring REST APIs | Java Backend Fresher Interview",
      "metaDescription": "Learn why DTOs are essential in Spring REST APIs — security, lazy loading issues, decoupling from DB schema, and risks of returning JPA entities directly."
    },
    "order": 6,
    "speakable_v2": {
      "question_id": "request-body-response-body-006",
      "archetype": "A",
      "pillar": "P04",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API serving a mobile client",
      "familiarity_anchors": [
        "Spring MVC",
        "REST",
        "controllers"
      ],
      "hook": "Returning your JPA entity directly from a REST controller works — until it causes a security leak or a serialization crash.",
      "beats": [
        {
          "type": "paragraph",
          "label": "what_it_is",
          "content": "A DTO — Data Transfer Object — is a plain Java class designed specifically for API input or output. It contains only the fields the API needs, nothing more. You map between your entity and DTO explicitly."
        },
        {
          "type": "paragraph",
          "label": "why_it_exists",
          "content": "Your JPA entity maps to your database schema. Your API contract is a different concern. Tying them together means a column rename in the database breaks your API, and adding an internal field to the entity exposes it in every response."
        },
        {
          "type": "bullets",
          "label": "parts_or_states",
          "content": [
            "Security: a User entity might have a passwordHash field. A DTO exposes only name and email.",
            "Lazy loading: Jackson serializes entity fields eagerly. Lazy collections hit the database after the transaction closes, causing LazyInitializationException.",
            "Circular references: bidirectional JPA relationships cause infinite recursion during serialization.",
            "Decoupling: rename a database column without changing your API contract."
          ]
        },
        {
          "type": "paragraph",
          "label": "pitfalls",
          "content": "production trap: The LazyInitializationException and infinite recursion bugs from returning entities directly are the two most common Jackson errors in fresher codebases. Interviewers know this and ask about it to filter candidates who've only done toy projects."
        }
      ],
      "cap": "Use separate request and response DTOs. MapStruct generates the mapping code at compile time with zero reflection overhead.",
      "followup_handoff": [
        "What are the risks of returning JPA entities directly from REST controllers?",
        "What is MapStruct and how does it help with DTO mapping?"
      ],
      "tts_overrides": {
        "DTO": "D-T-O",
        "DTOs": "D-T-Os",
        "JPA": "J-P-A",
        "LazyInitializationException": "Lazy Initialization Exception",
        "MapStruct": "Map Struct",
        "JSON": "J-S-O-N",
        "REST": "R-E-S-T",
        "@OneToMany": "@OneToMany",
        "@ManyToOne": "@ManyToOne"
      },
      "speakable_status": "pending"
    }
  }
]

[
  {
    "id": "api-design-basics-001",
    "slug": "api-design-basics-001",
    "question": "What are the principles of good REST API URI design?",
    "title": "REST URI Design Principles",
    "direct_answer": "Good REST URIs use nouns not verbs, plural resource names, lowercase with hyphens, and nested hierarchies for relationships. Avoid verbs in URLs — the HTTP method is the verb. Keep URIs predictable and consistent.",
    "layout_type": "default",
    "difficulty": "basic",
    "importance": "high",
    "reading_time_minutes": 3,
    "last_updated": "2025-01-15",
    "interviewer_intent": {
      "testing": "REST API design maturity and consistency principles",
      "common_mistake": "Using verbs in URIs: /getUser, /createOrder, /deleteProduct",
      "to_stand_out": "Show awareness of tradeoffs like when to use query params vs path params"
    },
    "company_tags": [
      "startup",
      "product",
      "enterprise"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "URI Design Rules",
          "content": "REST URIs identify resources — they're addresses, not commands. The HTTP method provides the action. Well-designed URIs are predictable: a developer can guess /users/{id}/orders without reading docs. Inconsistent URIs (sometimes plural, sometimes singular, mixed cases) create friction."
        },
        {
          "type": "key_points",
          "title": "URI Design Rules",
          "content": [
            "Use nouns: /users not /getUsers",
            "Plural for collections: /users, /orders, /products",
            "Singular for single resources: /users/{id}",
            "Lowercase with hyphens: /user-profiles not /UserProfiles or /user_profiles",
            "Nested for relationships: /users/{id}/orders, /orders/{id}/items",
            "Query params for filtering/sorting/pagination: /users?role=admin&page=0&size=20",
            "Avoid deep nesting: /users/{id}/orders/{id}/items/{id} — prefer /items?orderId={id}",
            "Version in path or header: /api/v1/users"
          ]
        },
        {
          "type": "speakable_answer",
          "title": "How to Say It in an Interview",
          "content": "The key URI design principles: use nouns not verbs — /users not /getUsers, since GET is already the verb. Use plural nouns for collections. Use lowercase with hyphens for multi-word names. Nest resources to show relationships: /users/123/orders for a user's orders. Use query parameters for filtering, sorting, and pagination. I avoid deep nesting beyond two levels — beyond /users/{id}/orders it gets unwieldy. And always version your API, either in the URL like /api/v1 or via headers."
        }
      ]
    },
    "followup_questions": [
      "How would you design URIs for a blog API with posts, comments, and tags?",
      "When do you use path params vs query params?",
      "How do you handle actions that don't map to CRUD?"
    ],
    "seo": {
      "metaTitle": "REST API URI Design Principles | Java Backend Interview",
      "metaDescription": "Learn REST URI design rules: nouns, plural, nested resources, versioning. Java fresher prep."
    },
    "order": 1,
    "speakable_v2": {
      "archetype": "A",
      "pillar": "P04",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API for a todo app with GET, POST, PUT, DELETE endpoints",
      "familiarity_anchors": ["noun vs verb", "plural collection", "query params"],
      "hook": "Good URI design is about predictability — a developer should be able to guess your API without reading the docs.",
      "beats": [
        {
          "kind": "definition",
          "layout": "paragraph",
          "text": "REST URIs identify resources — they're addresses, not commands. The HTTP method supplies the action. Well-designed URIs follow consistent conventions so every endpoint in your API behaves like every other one."
        },
        {
          "kind": "why_exists",
          "layout": "paragraph",
          "text": "Inconsistent URIs — sometimes plural, sometimes singular, sometimes with verbs — create friction. Developers have to read docs for every endpoint. Consistent URIs mean they guess correctly the first time."
        },
        {
          "kind": "parts_or_states",
          "layout": "bullets",
          "items": [
            "Nouns not verbs: /todos not /getTodos — the HTTP GET is already the verb",
            "Plural for collections: /todos not /todo — GET /todos returns many, POST /todos creates one",
            "Path params for resource identity: /todos/42 — 42 identifies a specific resource",
            "Query params for filters, sort, pagination: /todos?done=false&sort=dueDate&page=0&size=20",
            "Nested for ownership: /users/5/todos — todos belonging to user 5; don't go deeper than two levels",
            "Lowercase with hyphens: /todo-items not /TodoItems or /todo_items"
          ]
        },
        {
          "kind": "example",
          "layout": "paragraph",
          "text": "Todo app: GET /todos (list), POST /todos (create), GET /todos/42 (read one), PUT /todos/42 (replace), PATCH /todos/42 (update), DELETE /todos/42 (remove). Want todos for user 5: GET /users/5/todos. Want incomplete todos sorted by due date: GET /todos?done=false&sort=dueDate. Spring Boot: @GetMapping(\"/todos/{id}\") with @PathVariable Long id, @GetMapping(\"/todos\") with @RequestParam(required=false) Boolean done. candidates miss: /api/v1/getTodo — the 'get' is redundant. GET is already the verb. /getTodo is RPC-style. The URI /todos/42 with GET is REST-style."
        },
        {
          "kind": "pitfalls",
          "layout": "bullets",
          "items": [
            "Verbs in URIs like /getTodo, /createTodo — GET and POST are the verbs; URIs should be nouns",
            "Deeply nested paths like /users/5/todos/42/comments/7 — beyond two levels, use query params instead: /comments?todoId=42"
          ]
        }
      ],
      "cap": "Nouns not verbs. Plural collections. Path params for identity, query params for filters. Lowercase hyphens. Nest only one level deep. Consistent = guessable.",
      "followup_handoff": ["How do you design a URI for an action that doesn't map to CRUD, like sending a password reset?", "When would you use path params versus query params?"],
      "tts_overrides": {},
      "speakable_status": "pending_review"
    }
  },
  {
    "id": "api-design-basics-002",
    "slug": "api-design-basics-002",
    "question": "How do you version a REST API and what are the tradeoffs?",
    "title": "REST API Versioning Strategies",
    "direct_answer": "Common approaches: URI versioning (/api/v1/users), request header versioning (Accept: application/vnd.api.v1+json), and query parameter versioning (/users?version=1). URI versioning is simplest and most visible; header versioning is cleaner but harder to test.",
    "layout_type": "default",
    "difficulty": "intermediate",
    "importance": "high",
    "reading_time_minutes": 3,
    "last_updated": "2025-01-15",
    "interviewer_intent": {
      "testing": "API lifecycle management awareness — a sign of production experience",
      "common_mistake": "Not knowing that breaking changes require versioning",
      "to_stand_out": "Explain what constitutes a breaking change vs a non-breaking change"
    },
    "company_tags": [
      "product",
      "enterprise"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "Why API Versioning Matters",
          "content": "APIs evolve. Removing a field, changing a data type, or renaming an endpoint breaks existing clients. Versioning lets you release v2 while keeping v1 running for old clients. Without versioning, any change risks breaking production clients you can't control."
        },
        {
          "type": "key_points",
          "title": "Versioning Approaches",
          "content": [
            "URI versioning: /api/v1/users — most common, easy to test in browser",
            "Header versioning: Accept: application/vnd.myapi.v1+json — clean URIs, harder to test",
            "Query param: /users?version=1 — easy but mixes versioning with query logic",
            "Breaking changes: removing fields, changing types, renaming endpoints → requires new version",
            "Non-breaking: adding optional fields, adding new endpoints → can add to same version",
            "Deprecation: announce v1 end-of-life, give clients migration time",
            "Spring Boot: separate controller packages per version or use @RequestMapping(path = '/v1')"
          ]
        },
        {
          "type": "speakable_answer",
          "title": "How to Say It in an Interview",
          "content": "API versioning is essential for evolving APIs without breaking clients. I prefer URI versioning — /api/v1/users — because it's obvious, easy to test in a browser, and works with every HTTP client without configuration. Header versioning is architecturally cleaner but harder to test and document. The key rule: add a new version only for breaking changes. Breaking changes are things like removing a field, changing a field's type, or renaming an endpoint. Adding new optional fields is non-breaking. In Spring Boot, I structure versioned controllers with /v1 and /v2 packages, keeping both running until v1 is deprecated."
        }
      ]
    },
    "followup_questions": [
      "What is a breaking change in an API?",
      "How long should you support a deprecated API version?",
      "How do you handle versioning in Spring Boot?"
    ],
    "seo": {
      "metaTitle": "REST API Versioning Strategies | Java Backend Interview",
      "metaDescription": "Learn URI, header, and query parameter versioning for REST APIs. Java fresher interview prep."
    },
    "order": 2,
    "speakable_v2": {
      "archetype": "A",
      "pillar": "P04",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API for a todo app with GET, POST, PUT, DELETE endpoints",
      "familiarity_anchors": ["breaking change", "URI versioning", "deprecation"],
      "hook": "APIs evolve — versioning is how you evolve without breaking the clients you can't control.",
      "beats": [
        {
          "kind": "definition",
          "layout": "paragraph",
          "text": "API versioning lets you release a new version of your API with breaking changes while keeping the old version running for existing clients. A breaking change is any change that causes existing client code to fail — removing a field, changing a field's type, renaming an endpoint."
        },
        {
          "kind": "why_exists",
          "layout": "paragraph",
          "text": "Mobile apps can't be force-updated. Third-party clients are outside your control. Without versioning, any breaking change in your API breaks everyone simultaneously. Versioning gives you the freedom to evolve."
        },
        {
          "kind": "parts_or_states",
          "layout": "bullets",
          "items": [
            "URI versioning: /api/v1/todos and /api/v2/todos — most common, easy to test in a browser, obvious in logs",
            "Header versioning: Accept: application/vnd.todoapp.v1+json — cleaner URIs but harder to test and document",
            "Query param: /todos?version=1 — simple but mixes versioning with business query params",
            "Breaking changes require a new version: removing fields, changing field types, renaming resources",
            "Non-breaking changes don't need a new version: adding optional fields, adding new endpoints"
          ]
        },
        {
          "kind": "example",
          "layout": "paragraph",
          "text": "Todo app v1: GET /api/v1/todos returns {id, title, done}. For v2 we change done (boolean) to status (enum: TODO, IN_PROGRESS, DONE) — that's a breaking change. We launch /api/v2/todos with the new shape and keep /api/v1/todos running. Mobile clients on v1 keep working. New clients use v2. In Spring Boot: create separate @RestController classes annotated with @RequestMapping('/api/v1/todos') and @RequestMapping('/api/v2/todos'), or use separate packages v1/ and v2/. After 3-6 months, deprecate v1 — add a Deprecation response header and document the sunset date. non-obvious: adding a required field to a POST body is also a breaking change even though you're not removing anything — old clients that don't send the new field will fail."
        },
        {
          "kind": "pitfalls",
          "layout": "bullets",
          "items": [
            "Making breaking changes without versioning — you'll break existing clients immediately",
            "Forgetting that adding required fields is a breaking change — old clients won't send them"
          ]
        }
      ],
      "cap": "URI versioning is the standard: /api/v1, /api/v2. Breaking changes = new version. Non-breaking changes = same version. Always plan for deprecation with a transition window.",
      "followup_handoff": ["What exactly counts as a breaking change in a REST API?", "How long should you support a deprecated API version?"],
      "tts_overrides": {},
      "speakable_status": "pending_review"
    }
  },
  {
    "id": "api-design-basics-003",
    "slug": "api-design-basics-003",
    "question": "How do you design REST API pagination?",
    "title": "REST API Pagination Design",
    "direct_answer": "Use offset-based or cursor-based pagination. Offset pagination uses page and size query params. Cursor pagination uses a cursor token for consistent results in large or frequently changing datasets. Return pagination metadata (total, page, size) in the response.",
    "layout_type": "default",
    "difficulty": "intermediate",
    "importance": "high",
    "reading_time_minutes": 3,
    "last_updated": "2025-01-15",
    "interviewer_intent": {
      "testing": "Practical API design knowledge for handling large datasets",
      "common_mistake": "Not returning total count or using OFFSET queries on large tables",
      "to_stand_out": "Explain why cursor pagination is better for large datasets and real-time feeds"
    },
    "company_tags": [
      "product",
      "startup",
      "enterprise"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "Pagination Approaches",
          "content": "Without pagination, GET /users on a million-user database returns everything — crashing clients and servers. Pagination returns data in pages. Offset pagination is simpler but has issues with large offsets and changing data. Cursor pagination is better for infinite scroll and real-time feeds."
        },
        {
          "type": "key_points",
          "title": "Implementation Patterns",
          "content": [
            "Offset pagination: GET /users?page=2&size=20 → OFFSET 40 LIMIT 20",
            "Response: {data: [...], page: 2, size: 20, total: 1000, totalPages: 50}",
            "Cursor pagination: GET /users?cursor=abc123&size=20 → WHERE id > cursor_value LIMIT 20",
            "Spring Boot: Pageable parameter with @PageableDefault in controller, Page<T> return type",
            "Spring Data: findAll(Pageable pageable) returns Page<T>",
            "Offset pagination problem: OFFSET 1000000 is slow; data shifts as new records added",
            "Cursor pagination: consistent performance regardless of page depth"
          ]
        },
        {
          "type": "speakable_answer",
          "title": "How to Say It in an Interview",
          "content": "For pagination, I use Spring Data's Pageable which handles everything cleanly. The controller accepts ?page=0&size=20, Spring converts it to a Pageable object, and I pass it to the repository's findAll(pageable) which returns a Page object with data, total count, and page info. For large tables, offset pagination gets slow at high page numbers because the database scans all skipped rows. Cursor-based pagination avoids this by using WHERE id > lastSeenId LIMIT 20 — consistent speed regardless of depth. I'd use cursor pagination for feeds or any dataset over a million records."
        }
      ]
    },
    "followup_questions": [
      "What is the difference between offset and cursor pagination?",
      "How does Spring Data Pageable work?",
      "What metadata should a paginated response include?"
    ],
    "seo": {
      "metaTitle": "REST API Pagination Design | Java Backend Interview",
      "metaDescription": "Learn offset and cursor pagination for REST APIs with Spring Boot. Java fresher interview prep."
    },
    "order": 3,
    "speakable_v2": {
      "archetype": "B",
      "pillar": "P04",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API for a todo app with GET, POST, PUT, DELETE endpoints",
      "familiarity_anchors": ["Spring Data Pageable", "offset", "cursor"],
      "hook": "Never return an unbounded list — pagination is the first thing you add to any list endpoint.",
      "beats": [
        {
          "kind": "what_each_is",
          "layout": "grouped_paragraphs",
          "groups": [
            {
              "label": "Offset pagination",
              "text": "Client requests a page number and page size. Server uses SQL OFFSET and LIMIT. GET /todos?page=2&size=20 returns records 41-60. Simple to implement and understand."
            },
            {
              "label": "Cursor pagination",
              "text": "Client receives an opaque cursor token with each response. Next request includes the cursor. Server uses WHERE id > cursor_value LIMIT 20. Consistent performance regardless of dataset size."
            }
          ]
        },
        {
          "kind": "differences",
          "layout": "mini_table",
          "columns": ["Aspect", "Offset", "Cursor"],
          "rows": [
            {"axis": "Implementation", "values": ["Simple — page + size", "Moderate — opaque cursor token"]},
            {"axis": "Performance at depth", "values": ["Slow — OFFSET 1M scans 1M rows", "Fast — indexed seek regardless of depth"]},
            {"axis": "Consistent results", "values": ["Items shift if data changes mid-page", "Stable — based on sort key snapshot"]}
          ]
        },
        {
          "kind": "when_to_pick",
          "layout": "bullets",
          "items": [
            "Offset pagination: admin lists, search results, small datasets — page numbers are visible and intuitive",
            "Cursor pagination: feeds, infinite scroll, large datasets over a million records, real-time data that changes frequently"
          ]
        },
        {
          "kind": "tiny_example",
          "layout": "paragraph",
          "text": "Todo app: GET /todos?page=0&size=20&sort=createdAt,desc. Spring Boot: controller accepts Pageable pageable via @PageableDefault, service calls todoRepository.findAll(pageable), returns Page with {content:[...], totalElements:150, totalPages:8, number:0, size:20}. In Spring Data JPA, findAll(Pageable) is built in — zero SQL to write. non-obvious: OFFSET on large tables is slow because the database scans and discards every row before the offset. OFFSET 100000 scans 100000 rows just to throw them away. Cursor-based avoids this entirely with a WHERE id > lastId index scan."
        }
      ],
      "cap": "Offset pagination = page+size, simple, slow at depth. Cursor pagination = opaque token, fast always, good for feeds. Spring Data Pageable handles offset pagination automatically.",
      "followup_handoff": ["How does Spring Data Pageable work and what does Page return?", "Why is SQL OFFSET slow on large tables?"],
      "tts_overrides": {},
      "speakable_status": "pending_review"
    }
  },
  {
    "id": "api-design-basics-004",
    "slug": "api-design-basics-004",
    "question": "What is HATEOAS and why don't most APIs implement it?",
    "title": "HATEOAS in REST APIs",
    "direct_answer": "HATEOAS (Hypermedia as the Engine of Application State) is a REST constraint where responses include links to related actions. A GET /orders/1 response includes links for cancel, update, and return. Most APIs skip it because the added complexity rarely justifies the benefit.",
    "layout_type": "default",
    "difficulty": "advanced",
    "importance": "low",
    "reading_time_minutes": 3,
    "last_updated": "2025-01-15",
    "interviewer_intent": {
      "testing": "Depth of REST knowledge and ability to evaluate theoretical vs practical tradeoffs",
      "common_mistake": "Thinking all REST APIs must implement HATEOAS",
      "to_stand_out": "Show awareness of Spring HATEOAS library while explaining why real projects skip it"
    },
    "company_tags": [
      "enterprise"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "What HATEOAS Looks Like",
          "content": "A HATEOAS response for GET /orders/123 might include: { id: 123, status: 'pending', _links: { self: '/orders/123', cancel: '/orders/123/cancel', update: '/orders/123' } }. The client doesn't need to know URLs upfront — the API tells it what's possible based on current state."
        },
        {
          "type": "key_points",
          "title": "Why It's Rarely Used",
          "content": [
            "Theoretical benefit: client needs no URL knowledge, API self-documents",
            "Reality: clients hardcode URLs anyway for performance; HATEOAS adds response size",
            "Implementation complexity: generating state-dependent links is non-trivial",
            "Spring HATEOAS: spring-boot-starter-hateoas provides RepresentationModel",
            "Netflix, Amazon, GitHub REST APIs: don't use HATEOAS",
            "When HATEOAS helps: public APIs with complex state machines, workflow APIs"
          ]
        },
        {
          "type": "speakable_answer",
          "title": "How to Say It in an Interview",
          "content": "HATEOAS is theoretically elegant — the API response tells you what actions are possible based on the current resource state. But in practice, almost no major API implements it because the benefits don't justify the complexity. Clients — mobile apps, SPAs — hardcode URLs anyway for simplicity. The API changes state-aware links every request, adding payload size. Spring does have a spring-boot-starter-hateoas if you need it. I'd honestly say: know what HATEOAS is for interviews, but don't expect to implement it in real projects."
        }
      ]
    },
    "followup_questions": [
      "What is the HAL (Hypertext Application Language) format?",
      "When would HATEOAS actually add value?",
      "How does Spring HATEOAS work?"
    ],
    "seo": {
      "metaTitle": "HATEOAS REST Constraint | Java Backend Interview",
      "metaDescription": "Learn HATEOAS, why it's rarely implemented, and Spring HATEOAS library. Java fresher prep."
    },
    "order": 4,
    "speakable_v2": {
      "archetype": "A",
      "pillar": "P04",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API for a todo app with GET, POST, PUT, DELETE endpoints",
      "familiarity_anchors": ["hypermedia", "Spring HATEOAS", "self-documenting API"],
      "hook": "HATEOAS is theoretically elegant and practically rare — know what it is, know why it's skipped, and you'll nail this question.",
      "beats": [
        {
          "kind": "definition",
          "layout": "paragraph",
          "text": "HATEOAS stands for Hypermedia as the Engine of Application State. It's the fourth sub-constraint of REST's Uniform Interface. A HATEOAS-compliant API response includes not just data but also links to the actions the client can take next, based on the resource's current state."
        },
        {
          "kind": "why_exists",
          "layout": "paragraph",
          "text": "The theoretical benefit: clients don't need to hardcode URLs. The API tells them what's possible. If a todo is already done, the response omits the mark-done link. If it's not done, the link appears. Clients follow the API's guidance rather than knowing its URL structure upfront."
        },
        {
          "kind": "parts_or_states",
          "layout": "bullets",
          "items": [
            "Each response includes a _links block with URIs for related actions",
            "Links are state-dependent: a cancelled order response includes a view link but not a cancel link",
            "Spring HATEOAS: spring-boot-starter-hateoas provides RepresentationModel and WebMvcLinkBuilder",
            "HAL (Hypertext Application Language): the most common HATEOAS format — _links with self, related, and action URIs"
          ]
        },
        {
          "kind": "example",
          "layout": "paragraph",
          "text": "Todo HATEOAS response: {id:42, title:Buy milk, done:false, _links:{self:{href:/todos/42}, mark-done:{href:/todos/42, method:PATCH}, delete:{href:/todos/42, method:DELETE}}}. Once done is true, the mark-done link disappears. In Spring Boot with Spring HATEOAS: extend RepresentationModel in your DTO and use WebMvcLinkBuilder to add links. Roy Fielding has said that APIs without HATEOAS aren't really REST. In practice: GitHub, Stripe, Twitter — none of them implement HATEOAS. The complexity of generating state-aware links for every response outweighs the benefit for typical CRUD APIs. production trap: thinking you must implement HATEOAS to build a RESTful API. You don't. Know what it is, acknowledge why it's skipped in practice."
        },
        {
          "kind": "pitfalls",
          "layout": "bullets",
          "items": [
            "Thinking HATEOAS is required for REST — it's part of Fielding's definition but almost no production API implements it",
            "Confusing HATEOAS links with pagination links — pagination next/prev links are a practical navigation feature, not strictly HATEOAS"
          ]
        }
      ],
      "cap": "HATEOAS = API responses include links to possible next actions. Theoretically self-documenting. Practically skipped by almost every real API because the complexity doesn't pay off.",
      "followup_handoff": ["When would HATEOAS actually add value in a real application?", "What is the HAL format and how does Spring HATEOAS support it?"],
      "tts_overrides": {},
      "speakable_status": "pending_review"
    }
  },
  {
    "id": "api-design-basics-005",
    "slug": "api-design-basics-005",
    "question": "How do you design API error responses for good developer experience?",
    "title": "API Error Response Design",
    "direct_answer": "Good API errors include: appropriate HTTP status code, machine-readable error code, human-readable message, and field-level details for validation errors. Follow RFC 9457 Problem Details format or a consistent custom structure.",
    "layout_type": "default",
    "difficulty": "intermediate",
    "importance": "high",
    "reading_time_minutes": 3,
    "last_updated": "2025-01-15",
    "interviewer_intent": {
      "testing": "API design maturity and developer empathy",
      "common_mistake": "Returning only HTTP status code with no body, or returning inconsistent error formats",
      "to_stand_out": "Show RFC 9457 Problem Details and the importance of not leaking internal details"
    },
    "company_tags": [
      "product",
      "enterprise"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "What Makes a Good Error Response",
          "content": "A good error response helps the developer fix the problem without guessing. It tells them: what went wrong (status code), why (message), where (field-level details for validation), and potentially how to fix it (documentation link). Never expose stack traces, internal server details, or database column names."
        },
        {
          "type": "key_points",
          "title": "Error Response Structure",
          "content": [
            "HTTP status code: correct 4xx or 5xx",
            "type: URI identifying error type (RFC 9457)",
            "title: short human-readable summary",
            "detail: specific explanation for this instance",
            "errors: array of field-level errors for validation",
            "timestamp, traceId: for debugging, correlating with server logs",
            "Never include: stack traces, SQL errors, internal class names",
            "Spring Boot 3 ProblemDetail implements RFC 9457 automatically"
          ]
        },
        {
          "type": "speakable_answer",
          "title": "How to Say It in an Interview",
          "content": "Good error responses follow a consistent structure so API consumers can handle them programmatically. I use the RFC 9457 Problem Details format in Spring Boot 3: it includes a type URI, title, detail, and status. For validation errors, I include an errors array with field name, rejected value, and message so the client knows exactly which fields failed and why. Critical rule: never expose internal details in error responses — no stack traces, no SQL error messages, no class names. Log those server-side for debugging. The client gets a clean, safe error message."
        }
      ]
    },
    "followup_questions": [
      "What is RFC 9457 Problem Details?",
      "How do you add a traceId to error responses for debugging?",
      "How do you implement consistent error handling with @ControllerAdvice?"
    ],
    "seo": {
      "metaTitle": "REST API Error Response Design | Java Backend Interview",
      "metaDescription": "Design good API error responses with RFC 9457 Problem Details. Spring Boot Java fresher prep."
    },
    "order": 5,
    "speakable_v2": {
      "archetype": "A",
      "pillar": "P04",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 REST API for a todo app with GET, POST, PUT, DELETE endpoints",
      "familiarity_anchors": ["RFC 9457", "ProblemDetail", "traceId"],
      "hook": "A good error response tells the developer what went wrong, why, and where — without leaking anything about your internal implementation.",
      "beats": [
        {
          "kind": "definition",
          "layout": "paragraph",
          "text": "API error response design is about giving developers the information they need to understand and fix the problem. Every error response should have: the right HTTP status code, a machine-readable error type, a human-readable message, and (for validation) field-level details."
        },
        {
          "kind": "why_exists",
          "layout": "paragraph",
          "text": "A status code alone isn't enough — a 422 just says 'invalid'. An error body with field:title, message:must not be blank tells the developer exactly what to fix. Inconsistent error formats force every client to write custom parsing for each endpoint."
        },
        {
          "kind": "parts_or_states",
          "layout": "bullets",
          "items": [
            "Correct HTTP status code: 400/422 for validation, 404 for not found, 409 for conflict, 500 for server error",
            "type: a URI identifying the error class (RFC 9457 convention)",
            "title: short stable summary — 'Validation Failed', 'Resource Not Found'",
            "detail: specific explanation for this instance — 'Todo with id 42 does not exist'",
            "errors array for validation: [{field:title, message:must not be blank, rejectedValue:null}]",
            "traceId: a correlation ID for matching the error to your server logs",
            "Never include: stack traces, SQL error messages, internal class names"
          ]
        },
        {
          "kind": "example",
          "layout": "paragraph",
          "text": "POST /todos with empty title returns 422 with: {type:/errors/validation-failed, title:Validation Failed, status:422, detail:Request validation failed, errors:[{field:title, message:must not be blank}], traceId:abc-123}. The mobile app reads errors[0].field to highlight the title input. In Spring Boot 3, ProblemDetail implements RFC 9457 — extend ResponseEntityExceptionHandler and override handleMethodArgumentNotValid. For custom exceptions, use @ControllerAdvice with @ExceptionHandler that returns ProblemDetail. non-obvious: include a traceId in every error response. When a client reports error reference abc-123, you search your logs for that ID and immediately find the stack trace. Without it, debugging is a needle-in-haystack exercise."
        },
        {
          "kind": "pitfalls",
          "layout": "bullets",
          "items": [
            "Returning only the status code with no body — a 422 tells the client something failed but not what; always include a descriptive body",
            "Including stack traces in error responses — log them server-side, never send them to clients; they reveal implementation details to attackers"
          ]
        }
      ],
      "cap": "Good error response = correct status code + type + title + detail + field-level errors + traceId. Never expose stack traces. Spring Boot 3 ProblemDetail follows RFC 9457 out of the box.",
      "followup_handoff": ["How do you implement RFC 9457 ProblemDetail in Spring Boot 3?", "How do you add a traceId to error responses for distributed debugging?"],
      "tts_overrides": {},
      "speakable_status": "pending_review"
    }
  }
]

{
  "topic": "REST Controller Basics",
  "topicSlug": "rest-controller-basics",
  "questions": [
    {
      "id": "rest-controller-basics-001",
      "slug": "rest-controller-basics-001",
      "question": "What is @RestController in Spring Boot?",
      "title": "@RestController annotation explained",
      "direct_answer": "@RestController is a combination of @Controller and @ResponseBody. @Controller marks a class as a Spring MVC controller. @ResponseBody tells Spring to write the return value directly to the HTTP response body (as JSON by default) instead of looking for a view template. @RestController saves you from adding @ResponseBody to every method.",
      "layout_type": "explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 2,
      "last_updated": "2026-05-30",
      "interviewer_intent": "One of the most common Spring Boot interview questions — verify the candidate knows what the annotation does.",
      "company_tags": [
        "tcs",
        "infosys",
        "wipro",
        "cognizant",
        "amazon",
        "flipkart"
      ],
      "answer": {
        "sections": [
          {
            "type": "verbal",
            "content": "Think of @RestController as a convenience shortcut. Before it existed in Spring 4, if you were building a REST API you'd put @Controller on the class and @ResponseBody on every method. @ResponseBody is the key piece — it tells Spring 'don't look for a JSP or Thymeleaf template, just serialize this object to JSON and write it to the response.' @RestController combines both into one annotation at the class level. That's all it does. Behind the scenes, Jackson converts your returned objects to JSON automatically."
          },
          {
            "type": "code",
            "language": "java",
            "content": "@RestController\n@RequestMapping(\"/users\")\npublic class UserController {\n    @GetMapping(\"/{id}\")\n    public User getUser(@PathVariable Long id) {\n        return userService.findById(id); // Jackson serializes to JSON\n    }\n}\n// Equivalent to:\n@Controller\npublic class UserController {\n    @GetMapping(\"/{id}\")\n    @ResponseBody  // needed on every method\n    public User getUser(@PathVariable Long id) { ... }\n}"
          },
          {
            "type": "key_point",
            "content": "@RestController = @Controller + @ResponseBody. Return objects become JSON automatically via Jackson."
          }
        ]
      },
      "speakable_v2": {
        "pillar": "P02",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "A",
        "depth_marker": "shortcut-explained",
        "hook": "@RestController is just two annotations merged into one — and knowing which two tells you exactly what it does.",
        "beat_layout": "paragraph",
        "beats": [
          {
            "type": "paragraph",
            "text": "@RestController combines @Controller and @ResponseBody into one class-level annotation. @Controller tells Spring MVC this class handles HTTP requests. @ResponseBody tells Spring to serialize the return value directly to the HTTP response as JSON — don't look for a view template."
          },
          {
            "type": "paragraph",
            "text": "Before @RestController existed, you had to add @ResponseBody to every single method in a REST controller. @RestController applies it at the class level so you write it once. In a Spring Boot 3.2 REST API application, Jackson is already on the classpath via starter-web, so it handles the Java-to-JSON conversion automatically. You just return objects."
          }
        ],
        "familiarity_anchors": ["@Controller", "@ResponseBody", "Jackson", "JSON serialization"],
        "standard_example": "Spring Boot 3.2 REST API application"
      },
      "followup_questions": [
        "When would you use @Controller instead of @RestController?",
        "What converts Java objects to JSON in Spring Boot?"
      ],
      "seo": {
        "meta_title": "@RestController Spring Boot Explained | InterviewExplainer",
        "meta_description": "Understand @RestController in Spring Boot — how it combines @Controller and @ResponseBody for REST APIs.",
        "keywords": [
          "restcontroller spring boot",
          "spring boot rest api",
          "@restcontroller vs @controller"
        ]
      },
      "order": 1
    },
    {
      "id": "rest-controller-basics-002",
      "slug": "rest-controller-basics-002",
      "question": "What is the difference between @GetMapping, @PostMapping, @PutMapping, @DeleteMapping?",
      "title": "HTTP method mapping annotations in Spring Boot",
      "direct_answer": "@GetMapping maps GET requests (read). @PostMapping maps POST requests (create). @PutMapping maps PUT requests (replace/update). @DeleteMapping maps DELETE requests (delete). All are shortcuts for @RequestMapping(method = RequestMethod.GET/POST/etc.). Use the specific annotations — they communicate intent and are shorter.",
      "layout_type": "explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 2,
      "last_updated": "2026-05-30",
      "interviewer_intent": "Basic REST API knowledge — verify the candidate maps HTTP verbs to CRUD operations.",
      "company_tags": [
        "tcs",
        "infosys",
        "wipro",
        "cognizant",
        "amazon"
      ],
      "answer": {
        "sections": [
          {
            "type": "verbal",
            "content": "These are composed annotations — each one is @RequestMapping with a specific HTTP method pre-set. The reason they exist is readability: @GetMapping('/users') is much clearer than @RequestMapping(value='/users', method=RequestMethod.GET). In REST API design, the convention is: GET for reading data, POST for creating new resources, PUT for replacing an existing resource, DELETE for removing. PATCH is for partial updates. There's also @PatchMapping. Use these specific annotations consistently — they make your API's intent obvious."
          },
          {
            "type": "code",
            "language": "java",
            "content": "@RestController\n@RequestMapping(\"/users\")\npublic class UserController {\n    @GetMapping          // GET /users\n    public List<User> getAll() { ... }\n    @GetMapping(\"/{id}\") // GET /users/1\n    public User getOne(@PathVariable Long id) { ... }\n    @PostMapping         // POST /users\n    public User create(@RequestBody User user) { ... }\n    @DeleteMapping(\"/{id}\") // DELETE /users/1\n    public void delete(@PathVariable Long id) { ... }\n}"
          },
          {
            "type": "key_point",
            "content": "All four are shortcuts for @RequestMapping with a specific method. Always prefer the specific annotation."
          }
        ]
      },
      "speakable_v2": {
        "pillar": "P02",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "A",
        "depth_marker": "http-verbs",
        "hook": "Each HTTP verb has its own mapping annotation — and each maps to a specific CRUD operation in REST design.",
        "beat_layout": "mini_table",
        "beats": [
          {
            "type": "paragraph",
            "text": "All four are shortcuts for @RequestMapping with a specific HTTP method pre-set. They exist purely for readability. Here's the standard REST mapping used in a Spring Boot 3.2 REST API application."
          },
          {
            "type": "mini_table",
            "headers": ["Annotation", "HTTP verb", "REST operation"],
            "rows": [
              ["@GetMapping", "GET", "Read — fetch a resource or list"],
              ["@PostMapping", "POST", "Create — add a new resource"],
              ["@PutMapping", "PUT", "Replace — update an existing resource fully"],
              ["@DeleteMapping", "DELETE", "Remove — delete a resource"],
              ["@PatchMapping", "PATCH", "Partial update — modify specific fields"]
            ]
          },
          {
            "type": "paragraph",
            "text": "Always use the specific annotation — @GetMapping is far clearer than @RequestMapping(method = RequestMethod.GET). They communicate intent in one glance."
          }
        ],
        "familiarity_anchors": ["@GetMapping", "@PostMapping", "@PutMapping", "@DeleteMapping"],
        "standard_example": "Spring Boot 3.2 REST API application"
      },
      "followup_questions": [
        "What is the difference between PUT and PATCH?",
        "What HTTP status code should POST return?"
      ],
      "seo": {
        "meta_title": "@GetMapping @PostMapping @PutMapping @DeleteMapping Spring Boot | InterviewExplainer",
        "meta_description": "Understand Spring Boot's HTTP method mapping annotations — @GetMapping, @PostMapping, @PutMapping, @DeleteMapping.",
        "keywords": [
          "spring boot getmapping postmapping",
          "spring boot rest api annotations",
          "requestmapping spring boot"
        ]
      },
      "order": 2
    },
    {
      "id": "rest-controller-basics-003",
      "slug": "rest-controller-basics-003",
      "question": "What is the difference between @PathVariable and @RequestParam?",
      "title": "@PathVariable vs @RequestParam in Spring Boot",
      "direct_answer": "@PathVariable extracts a value from the URL path segment: /users/{id} → @PathVariable Long id. @RequestParam extracts a value from the query string: /users?status=active → @RequestParam String status. Path variables identify a resource; query parameters filter or modify the request.",
      "layout_type": "comparison",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 2,
      "last_updated": "2026-05-30",
      "interviewer_intent": "Extremely common question — verify the candidate knows both annotations and when to use each.",
      "company_tags": [
        "tcs",
        "infosys",
        "wipro",
        "cognizant",
        "amazon",
        "flipkart"
      ],
      "answer": {
        "sections": [
          {
            "type": "verbal",
            "content": "The difference is where in the URL the value comes from. @PathVariable reads from the path itself — the curly brace placeholder in the URL pattern. So /users/42 maps to @PathVariable Long id = 42. Path variables are used to identify a specific resource. @RequestParam reads from the query string — the part after the question mark. So /users?status=active&page=2 gives you @RequestParam String status = 'active'. Query params are used for filtering, sorting, pagination. A good mental model: path variable = which resource, query param = how to filter or shape the response."
          },
          {
            "type": "code",
            "language": "java",
            "content": "// GET /users/42\n@GetMapping(\"/{id}\")\npublic User getUser(@PathVariable Long id) { ... }\n\n// GET /users?status=active&page=0\n@GetMapping\npublic List<User> getUsers(\n    @RequestParam String status,\n    @RequestParam(defaultValue = \"0\") int page) { ... }"
          },
          {
            "type": "table",
            "headers": [
              "Annotation",
              "URL example",
              "Use for"
            ],
            "rows": [
              [
                "@PathVariable",
                "/users/{id}",
                "Identifying a specific resource"
              ],
              [
                "@RequestParam",
                "/users?status=active",
                "Filtering, pagination, optional params"
              ]
            ]
          }
        ]
      },
      "speakable_v2": {
        "pillar": "P02",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "B",
        "depth_marker": "url-extraction",
        "hook": "Both extract values from the URL — but from different parts. Path variable is which resource, query param is how to filter it.",
        "beat_layout": "mini_table",
        "beats": [
          {
            "type": "paragraph",
            "text": "The difference is where in the URL the value comes from."
          },
          {
            "type": "mini_table",
            "headers": ["Annotation", "Where it reads from", "Use for"],
            "rows": [
              ["@PathVariable", "URL path segment — /users/{id}", "Identifying a specific resource"],
              ["@RequestParam", "Query string — /users?status=active", "Filtering, pagination, optional modifiers"]
            ]
          },
          {
            "type": "paragraph",
            "text": "Mental model that works: path variable answers 'which resource?' — GET /users/42 fetches user 42. Query parameter answers 'how should the response be shaped?' — GET /users?status=active&page=1 filters and paginates the list. In a Spring Boot 3.2 REST API application, you'll use both in the same controller all the time."
          }
        ],
        "familiarity_anchors": ["@PathVariable", "@RequestParam", "query string", "URL path"],
        "standard_example": "Spring Boot 3.2 REST API application"
      },
      "followup_questions": [
        "How do you make a @RequestParam optional?",
        "What is @RequestBody?"
      ],
      "seo": {
        "meta_title": "@PathVariable vs @RequestParam Spring Boot | InterviewExplainer",
        "meta_description": "Understand the difference between @PathVariable and @RequestParam in Spring Boot REST controllers.",
        "keywords": [
          "pathvariable vs requestparam spring boot",
          "spring boot url parameters",
          "@pathvariable spring boot"
        ]
      },
      "order": 3
    },
    {
      "id": "rest-controller-basics-004",
      "slug": "rest-controller-basics-004",
      "question": "What is @RequestBody and how does it work?",
      "title": "@RequestBody in Spring Boot REST controllers",
      "direct_answer": "@RequestBody tells Spring to read the HTTP request body and deserialize it into a Java object using Jackson. It is used with POST, PUT, and PATCH methods that send JSON in the request body. Jackson automatically maps JSON field names to Java field names.",
      "layout_type": "explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 2,
      "last_updated": "2026-05-30",
      "interviewer_intent": "Core REST API building block — verify the candidate knows how POST request bodies are handled.",
      "company_tags": [
        "tcs",
        "wipro",
        "cognizant",
        "amazon",
        "infosys"
      ],
      "answer": {
        "sections": [
          {
            "type": "verbal",
            "content": "When a client sends a POST request with a JSON body, Spring needs to turn that JSON string into a Java object. @RequestBody triggers that conversion. Jackson reads the JSON, looks at the field names, and maps them to the corresponding Java object fields. If the client sends {\"name\": \"Alice\", \"email\": \"alice@example.com\"}, Jackson creates a CreateUserRequest object with those fields set. If Jackson can't deserialize the JSON — wrong format, missing required field — it throws an HttpMessageNotReadableException and Spring returns a 400. Combined with @Valid, you get both format checking and constraint checking."
          },
          {
            "type": "code",
            "language": "java",
            "content": "@PostMapping\n@ResponseStatus(HttpStatus.CREATED)  // returns 201\npublic User createUser(@Valid @RequestBody CreateUserRequest req) {\n    return userService.create(req);\n}\n\npublic class CreateUserRequest {\n    @NotBlank\n    private String name;\n    @Email\n    private String email;\n    // getters, setters\n}"
          },
          {
            "type": "key_point",
            "content": "@RequestBody = Jackson deserializes JSON to Java object. Combine with @Valid for input validation."
          }
        ]
      },
      "speakable_v2": {
        "pillar": "P02",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "A",
        "depth_marker": "deserialization",
        "hook": "@RequestBody is the bridge between the JSON in the request body and your Java object — Jackson handles the conversion.",
        "beat_layout": "paragraph",
        "beats": [
          {
            "type": "paragraph",
            "text": "When a client POSTs a JSON body, Spring needs to turn that JSON string into a Java object. @RequestBody triggers that. Jackson reads the JSON field names and maps them to matching Java fields on your DTO. If the client sends name and email, Jackson creates a CreateUserRequest with those fields populated — no manual parsing needed."
          },
          {
            "type": "paragraph",
            "text": "The power move is combining @RequestBody with @Valid. @RequestBody handles format parsing — if the JSON is malformed, it fails with a 400. @Valid then validates the constraints declared on the DTO fields (@NotBlank, @Email). In a Spring Boot 3.2 REST API application, using both annotations on a @PostMapping parameter gives you format safety and business rule validation in one line."
          }
        ],
        "familiarity_anchors": ["@RequestBody", "Jackson", "@Valid", "DTO deserialization"],
        "standard_example": "Spring Boot 3.2 REST API application"
      },
      "followup_questions": [
        "What happens if the request Content-Type is not application/json?",
        "What is @ResponseBody?"
      ],
      "seo": {
        "meta_title": "@RequestBody Spring Boot REST | InterviewExplainer",
        "meta_description": "Understand @RequestBody in Spring Boot — how Jackson deserializes the HTTP request body into a Java object.",
        "keywords": [
          "requestbody spring boot",
          "spring boot post request body",
          "jackson deserialize spring boot"
        ]
      },
      "order": 4
    },
    {
      "id": "rest-controller-basics-005",
      "slug": "rest-controller-basics-005",
      "question": "How do you return different HTTP status codes in Spring Boot?",
      "title": "HTTP status codes in Spring Boot — ResponseEntity and @ResponseStatus",
      "direct_answer": "Two approaches: @ResponseStatus(HttpStatus.CREATED) on a method sets a fixed status code. ResponseEntity<T> as the return type lets you set status, headers, and body dynamically — useful when the status depends on the result. ResponseEntity is more flexible; @ResponseStatus is simpler for fixed codes.",
      "layout_type": "comparison",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 2,
      "last_updated": "2026-05-30",
      "interviewer_intent": "Proper HTTP status codes matter for REST API design — verify the candidate knows how to control them.",
      "company_tags": [
        "tcs",
        "wipro",
        "amazon",
        "flipkart",
        "infosys"
      ],
      "answer": {
        "sections": [
          {
            "type": "verbal",
            "content": "By default, Spring returns 200 OK for successful methods. But good REST APIs are more precise. A POST that creates a resource should return 201 Created. A successful DELETE with no body should return 204 No Content. For fixed status codes, just add @ResponseStatus to the method. For dynamic cases — like returning 200 if found, or 404 if not — use ResponseEntity as your return type and build it with ResponseEntity.ok(user) or ResponseEntity.notFound().build(). ResponseEntity is the most expressive option."
          },
          {
            "type": "code",
            "language": "java",
            "content": "@PostMapping\n@ResponseStatus(HttpStatus.CREATED)  // always 201\npublic User create(@RequestBody User user) { ... }\n\n@GetMapping(\"/{id}\")\npublic ResponseEntity<User> get(@PathVariable Long id) {\n    return userService.findById(id)\n        .map(ResponseEntity::ok)              // 200 if found\n        .orElse(ResponseEntity.notFound().build()); // 404 if not\n}"
          },
          {
            "type": "key_point",
            "content": "ResponseEntity for dynamic status codes. @ResponseStatus for fixed codes. ResponseEntity is more flexible."
          }
        ]
      },
      "speakable_v2": {
        "pillar": "P02",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "B",
        "depth_marker": "status-control",
        "hook": "Spring defaults to 200 OK. To return 201, 204, or a dynamic 404, you need to take control of the status code.",
        "beat_layout": "paragraph",
        "beats": [
          {
            "type": "paragraph",
            "text": "Two ways to control the HTTP status code in a Spring Boot 3.2 REST API application. For a fixed status that's always the same, add @ResponseStatus on the method — @ResponseStatus(HttpStatus.CREATED) makes a POST return 201 every time, @ResponseStatus(HttpStatus.NO_CONTENT) makes a DELETE return 204."
          },
          {
            "type": "paragraph",
            "text": "For dynamic status codes that depend on the result, use ResponseEntity as the return type. ResponseEntity.ok(user) returns 200 with the user body. ResponseEntity.notFound().build() returns 404 with no body. You can chain these with Optional.map() to return 200 if the record exists and 404 if it doesn't — in a single line. ResponseEntity also lets you set custom response headers when needed."
          }
        ],
        "familiarity_anchors": ["@ResponseStatus", "ResponseEntity", "HttpStatus.CREATED", "404"],
        "standard_example": "Spring Boot 3.2 REST API application"
      },
      "followup_questions": [
        "What is the difference between 201 and 200?",
        "How do you return custom headers in ResponseEntity?"
      ],
      "seo": {
        "meta_title": "Spring Boot HTTP Status Codes ResponseEntity | InterviewExplainer",
        "meta_description": "Learn how to return different HTTP status codes in Spring Boot using @ResponseStatus and ResponseEntity.",
        "keywords": [
          "spring boot response status",
          "responseentity spring boot",
          "spring boot 201 created"
        ]
      },
      "order": 5
    },
    {
      "id": "rest-controller-basics-006",
      "slug": "rest-controller-basics-006",
      "question": "What is @ControllerAdvice and how do you handle exceptions globally in Spring Boot?",
      "title": "@ControllerAdvice for global exception handling",
      "direct_answer": "@ControllerAdvice combined with @ExceptionHandler lets you handle exceptions across all controllers in one place. Without it, every controller method would need its own try-catch. @ControllerAdvice is an interceptor that catches specified exceptions and maps them to HTTP responses — for example catching ResourceNotFoundException and returning 404.",
      "layout_type": "explanation",
      "difficulty": "medium",
      "importance": "high",
      "reading_time_minutes": 2,
      "last_updated": "2026-05-30",
      "interviewer_intent": "Exception handling is a real-world concern — tests whether the candidate goes beyond basic CRUD controllers.",
      "company_tags": [
        "amazon",
        "flipkart",
        "infosys",
        "wipro"
      ],
      "answer": {
        "sections": [
          {
            "type": "verbal",
            "content": "Without global exception handling, every controller method needs to catch exceptions and convert them to error responses. That's repetitive and inconsistent. @ControllerAdvice centralizes this. You create one class, annotate it, and define @ExceptionHandler methods. Each handler method catches a specific exception type and returns an appropriate response. So when any controller throws a ResourceNotFoundException, your handler catches it and returns a 404 with a structured error body — same format across all endpoints. This is how you build consistent error responses in a Spring Boot app."
          },
          {
            "type": "code",
            "language": "java",
            "content": "@RestControllerAdvice\npublic class GlobalExceptionHandler {\n    @ExceptionHandler(ResourceNotFoundException.class)\n    @ResponseStatus(HttpStatus.NOT_FOUND)\n    public ErrorResponse handleNotFound(ResourceNotFoundException ex) {\n        return new ErrorResponse(\"NOT_FOUND\", ex.getMessage());\n    }\n    @ExceptionHandler(MethodArgumentNotValidException.class)\n    @ResponseStatus(HttpStatus.BAD_REQUEST)\n    public ErrorResponse handleValidation(MethodArgumentNotValidException ex) {\n        return new ErrorResponse(\"VALIDATION_ERROR\", ex.getMessage());\n    }\n}"
          },
          {
            "type": "key_point",
            "content": "@RestControllerAdvice = @ControllerAdvice + @ResponseBody. Use it for a central error-handling class."
          }
        ]
      },
      "speakable_v2": {
        "pillar": "P02",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "A",
        "depth_marker": "centralized-errors",
        "hook": "Without @ControllerAdvice, every controller method needs its own try-catch. With it, you handle all exceptions in one place.",
        "beat_layout": "paragraph",
        "beats": [
          {
            "type": "paragraph",
            "text": "@ControllerAdvice is a class-level annotation that makes a class an interceptor for exceptions thrown by any controller in the application. You add @ExceptionHandler methods inside it — each one handles a specific exception type. When UserController throws a ResourceNotFoundException, your advice class catches it, runs your handler method, and returns the structured error response you define."
          },
          {
            "type": "paragraph",
            "text": "In a Spring Boot 3.2 REST API application, use @RestControllerAdvice instead — it combines @ControllerAdvice and @ResponseBody so your error response objects serialize to JSON automatically. One class handles all your exception-to-HTTP-response mappings: ResourceNotFoundException becomes 404, MethodArgumentNotValidException becomes 400, any unhandled RuntimeException becomes 500. Consistent error format across the entire API."
          }
        ],
        "familiarity_anchors": ["@ControllerAdvice", "@ExceptionHandler", "@RestControllerAdvice", "global error handling"],
        "standard_example": "Spring Boot 3.2 REST API application"
      },
      "followup_questions": [
        "What is Problem Details (RFC 9457)?",
        "How is @ExceptionHandler different from @ControllerAdvice?"
      ],
      "seo": {
        "meta_title": "@ControllerAdvice Global Exception Handling Spring Boot | InterviewExplainer",
        "meta_description": "Learn how to use @ControllerAdvice and @ExceptionHandler for global exception handling in Spring Boot.",
        "keywords": [
          "controlleradvice spring boot",
          "global exception handling spring boot",
          "spring boot error handling"
        ]
      },
      "order": 6
    },
    {
      "id": "rest-controller-basics-007",
      "slug": "rest-controller-basics-007",
      "question": "What is @RequestMapping at the class level used for?",
      "title": "@RequestMapping class-level base path",
      "direct_answer": "@RequestMapping on a controller class sets a base path prefix for all methods in that class. If the class has @RequestMapping(\"/users\") and a method has @GetMapping(\"/{id}\"), the full path is /users/{id}. This avoids repeating the base path on every method and groups related endpoints.",
      "layout_type": "explanation",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 1,
      "last_updated": "2026-05-30",
      "interviewer_intent": "Basic Spring MVC knowledge — verify the candidate understands URL composition from class + method levels.",
      "company_tags": [
        "tcs",
        "wipro",
        "cognizant"
      ],
      "answer": {
        "sections": [
          {
            "type": "verbal",
            "content": "Putting @RequestMapping on the class sets a root path for all the methods inside. Without it, you'd have to write /users/ in every @GetMapping, @PostMapping, etc. With it on the class, all the method-level mappings are relative paths that get combined. @GetMapping('/{id}') on a class with @RequestMapping('/users') becomes GET /users/{id}. This is the standard way to group endpoints — one controller, one resource, one class-level @RequestMapping."
          },
          {
            "type": "code",
            "language": "java",
            "content": "@RestController\n@RequestMapping(\"/api/v1/users\")  // base path for all methods\npublic class UserController {\n    @GetMapping          // GET /api/v1/users\n    public List<User> getAll() { ... }\n    @GetMapping(\"/{id}\") // GET /api/v1/users/42\n    public User getOne(@PathVariable Long id) { ... }\n    @PostMapping         // POST /api/v1/users\n    public User create(@RequestBody User u) { ... }\n}"
          },
          {
            "type": "key_point",
            "content": "Class-level @RequestMapping = base URL. Method-level mapping = relative path. Final URL = base + method path."
          }
        ]
      },
      "speakable_v2": {
        "pillar": "P02",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "A",
        "depth_marker": "url-composition",
        "hook": "Class-level @RequestMapping sets a base path so you don't repeat /users on every single method.",
        "beat_layout": "paragraph",
        "beats": [
          {
            "type": "paragraph",
            "text": "@RequestMapping on the class sets a root path that all method-level mappings are relative to. Add @RequestMapping('/users') to the class, then @GetMapping('/{id}') on a method, and the full URL becomes GET /users/{id}. Without the class-level mapping, you'd have to write /users/{id} on every method annotation — repetitive and error-prone."
          },
          {
            "type": "paragraph",
            "text": "The standard pattern in a Spring Boot 3.2 REST API application: one controller class per resource, with the resource base path on the class. UserController has @RequestMapping('/api/v1/users'). OrderController has @RequestMapping('/api/v1/orders'). Methods then only declare the relative path — /{id}, /search, whatever they handle. URL versioning also happens here — bump the class-level path to /api/v2/users when you need to version an endpoint."
          }
        ],
        "familiarity_anchors": ["@RequestMapping", "base path", "URL composition", "API versioning"],
        "standard_example": "Spring Boot 3.2 REST API application"
      },
      "followup_questions": [
        "How do you version your REST API URL?",
        "What is the difference between @RequestMapping and @GetMapping?"
      ],
      "seo": {
        "meta_title": "@RequestMapping Class Level Spring Boot | InterviewExplainer",
        "meta_description": "Learn how @RequestMapping at the class level sets a base URL path for all methods in a Spring Boot controller.",
        "keywords": [
          "requestmapping class level spring boot",
          "spring boot controller base path",
          "spring boot url mapping"
        ]
      },
      "order": 7
    }
  ]
}

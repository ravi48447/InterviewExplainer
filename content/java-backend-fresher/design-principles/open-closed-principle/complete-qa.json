[
  {
    "id": "ocp-001",
    "slug": "open-closed-principle-ocp-open-for-extension-closed-for-modification",
    "question": "What is the Open/Closed Principle?",
    "title": "Open/Closed Principle (OCP) — open for extension, closed for modification",
    "direct_answer": "The Open/Closed Principle says a software element should provide stable extension points for expected kinds of change. New variants can often be added through an interface, strategy, handler, or configuration without rewriting the central algorithm. It is a design goal, not a ban on editing existing code.",
    "layout_type": "concept-explainer",
    "difficulty": "easy",
    "importance": "high",
    "reading_time_minutes": 2,
    "company_tags": [
      "amazon",
      "flipkart",
      "microsoft",
      "infosys"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "Understanding Open/Closed Principle",
          "content": "The **Open/Closed Principle** asks a design to be open to a planned kind of extension while keeping its stable core closed to repeated editing. It is impossible to make code closed to every future change, so the important step is identifying a real variation point.\n\nFor example, a checkout flow may be stable while payment providers vary. The flow can call a `PaymentGateway` contract, and new gateway adapters can implement it. Registration or configuration may still change; the benefit is that provider-specific rules do not accumulate inside the checkout algorithm.\n\nOCP does not forbid bug fixes, refactoring, migrations, or requirement changes. It also does not justify an interface for every class. Add an extension point when repeated or expected variation makes it valuable, and keep the design simple when the set of cases is small and deliberately closed."
        },
        {
          "type": "concept_map",
          "title": "At a Glance",
          "content": "amber|Open / Closed|~OCP core rule|open for extension|closed for modification|add code, don't edit tested code\nblue|Mechanism|~polymorphism|interface + new implementations|the Strategy pattern\nemerald|Violation|~switch on type|new type → edit the method|retest every path\nviolet|Payoff|~stability|new feature = new class|existing code untouched"
        },
        {
          "type": "code_example",
          "title": "OCP with Payment Processing",
          "content": "// Define the abstraction - closed for modification\ninterface PaymentProcessor {\n    void processPayment(double amount);\n}\n\n// First implementation\nclass CreditCardProcessor implements PaymentProcessor {\n    public void processPayment(double amount) {\n        System.out.println(\"Processing credit card payment: $\" + amount);\n        // Credit card-specific logic\n    }\n}\n\n// Second implementation - extension without modifying existing code\nclass PayPalProcessor implements PaymentProcessor {\n    public void processPayment(double amount) {\n        System.out.println(\"Processing PayPal payment: $\" + amount);\n        // PayPal-specific logic\n    }\n}\n\n@Service\nclass OrderService {\n    private final PaymentProcessor processor;\n    \n    // Depends on abstraction, not concrete type\n    public OrderService(PaymentProcessor processor) {\n        this.processor = processor;\n    }\n    \n    public void checkout(double total) {\n        processor.processPayment(total);  // Works with any implementation\n    }\n}"
        },
        {
          "type": "when_to_use",
          "title": "When to Apply OCP",
          "content": "Apply OCP when you anticipate variation in behavior. **Use it for payment gateways** where your app might support CreditCard today, PayPal tomorrow, and Stripe next month — the OrderService shouldn't need updates for each new gateway. **Use it for notification systems** where you might send emails now but add SMS and push notifications later — the NotificationService remains unchanged. **Use it for discount rules** in e-commerce where business logic changes frequently — new discount strategies become new classes, not edits to existing discount code. **Don't over-apply it to stable code** — if a User entity's validation logic hasn't changed in years, abstracting it prematurely adds complexity without benefit. The key question: is this behavior likely to vary or expand? If yes, design for extension."
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **Over-engineering simple cases**: Freshers see OCP and immediately create interfaces for everything, even a simple DateFormatter that will never have variants. Start simple, refactor to OCP when variation appears.\n- **Mixing abstraction levels**: Creating an interface but then checking instanceof inside the consuming class defeats the purpose — that's still modification when you add types. Let polymorphism handle the branching.\n- **Ignoring legitimate modifications**: Treating OCP as absolute law and refusing to fix bugs or refactor because it requires touching existing code. OCP applies to feature additions, not all code changes. Bug fixes always modify code, and that's fine."
        },
        {
          "type": "key_points",
          "title": "Quick revision",
          "content": [
            "OCP provides stable extension points for expected kinds of change.",
            "A new variant can often be added without rewriting the central algorithm.",
            "Interfaces, strategies, handlers, decorators, and plugins are possible tools, not requirements.",
            "Registration, configuration, tests, or the contract itself may still need modification.",
            "OCP is a design goal; bug fixes and changed requirements still edit existing code."
          ]
        },
        {
          "type": "speakable_answer",
          "title": "Interview answer",
          "content": "The Open/Closed Principle means designing a stable part of the system so an expected variation can be added through an extension point rather than repeatedly editing the central logic. 'Closed' is relative to that chosen kind of change, not to every future requirement.\n\nFor example, a checkout service can depend on a `PaymentGateway` interface. Card and UPI adapters implement the contract, while the checkout flow continues to validate the cart, request payment, and record the result. A new provider mainly adds an adapter; configuration and tests may still be updated to register and verify it.\n\nOCP does not mean never modifying working code, and it does not require an abstraction before variation exists. Bug fixes, refactoring, security changes, and a changed contract all require edits. The practical goal is to isolate a real variation so additions have a smaller, safer change surface.",
          "answerSize": "compact"
        }
      ]
    },
    "followup_questions": [
      "Which design patterns help implement OCP?",
      "How does polymorphism enable OCP?"
    ],
    "seo": {
      "title": "Open Closed Principle OCP Java example | SOLID principles",
      "description": "Learn the Open/Closed Principle with Java examples — open for extension, closed for modification using interfaces.",
      "keywords": [
        "open closed principle",
        "OCP Java",
        "SOLID OCP example"
      ]
    },
    "last_updated": "2026-09-08"
  },
  {
    "id": "ocp-002",
    "slug": "implementing-ocp-with-interfaces-and-polymorphism-in-java",
    "question": "How do you implement OCP in Java using interfaces?",
    "title": "Implementing OCP with interfaces and polymorphism in Java",
    "direct_answer": "Use an interface when a real behavior varies, make the client call that interface, and place each variant in its own implementation. Adding a variant then leaves the client algorithm unchanged, although composition-root registration, configuration, and tests may still change. An interface alone does not guarantee OCP if the client still switches on concrete types.",
    "layout_type": "concept-explainer",
    "difficulty": "medium",
    "importance": "high",
    "reading_time_minutes": 2,
    "company_tags": [
      "amazon",
      "tcs",
      "wipro",
      "microsoft"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "Implementing OCP Through Interfaces",
          "content": "To apply OCP with Java interfaces, first identify the behavior that changes. Put only that behavior behind a contract, let the stable client depend on it, and select an implementation at the application's composition root.\n\nA `DiscountPolicy` can expose `discountFor(Order)`. `SeasonalDiscount` and `MemberDiscount` implement it, while `PricingService` applies whichever policy it receives. Adding a policy does not require another branch inside `PricingService`. The application still needs a deliberate way to select or register the policy; Spring injection does not make runtime selection automatic.\n\nThe design fails if the client performs `instanceof` checks, switches on implementation names, or the interface changes for every new case. The abstraction must match stable client needs, and the extension should preserve its contract."
        },
        {
          "type": "concept_map",
          "title": "At a Glance",
          "content": "amber|Define a Contract|~interface|PaymentMethod.process()|callers depend on the abstraction\nblue|Add Implementations|~extension points|CreditCard, PayPal, ApplePay|each in its own class\nemerald|Wire by Polymorphism|~runtime dispatch|inject the interface|no switch on type\nviolet|Result|~closed core|new method = new class only"
        },
        {
          "type": "code_example",
          "title": "OCP with Discount Strategies",
          "content": "// The abstraction - defines the contract\ninterface DiscountStrategy {\n    double applyDiscount(double amount);\n}\n\n// Implementation 1: Percentage discount\nclass PercentageDiscount implements DiscountStrategy {\n    private final double percentage;\n    \n    public PercentageDiscount(double percentage) {\n        this.percentage = percentage;\n    }\n    \n    public double applyDiscount(double amount) {\n        return amount * (1 - percentage / 100);\n    }\n}\n\n// Implementation 2: Fixed amount discount\nclass FixedAmountDiscount implements DiscountStrategy {\n    private final double discountAmount;\n    \n    public FixedAmountDiscount(double discountAmount) {\n        this.discountAmount = discountAmount;\n    }\n    \n    public double applyDiscount(double amount) {\n        return Math.max(0, amount - discountAmount);\n    }\n}\n\n@Service\nclass PricingService {\n    private final DiscountStrategy discountStrategy;\n    \n    // Injected by Spring - depends on interface only\n    public PricingService(DiscountStrategy discountStrategy) {\n        this.discountStrategy = discountStrategy;\n    }\n    \n    public double calculateFinalPrice(double basePrice) {\n        return discountStrategy.applyDiscount(basePrice);\n    }\n}\n\n// Adding new strategy later - zero changes to PricingService\nclass BuyOneGetOneDiscount implements DiscountStrategy {\n    public double applyDiscount(double amount) {\n        return amount * 0.5;  // 50% off for BOGO\n    }\n}"
        },
        {
          "type": "when_to_use",
          "title": "When to Use Interface-Based OCP",
          "content": "Use this pattern when behavior varies across contexts or will expand over time. **Use it for external integrations** like payment gateways, shipping providers, or cloud storage — you'll likely add providers, and each has different APIs. Create a unified interface and wrap each provider. **Use it for business rules that change** like pricing strategies, validation rules, or workflow steps — business requirements evolve, and new rules shouldn't modify existing rule code. **Use it for cross-cutting concerns with variations** like logging (console, file, remote), authentication (JWT, OAuth, session), or caching (in-memory, Redis, database). Each variant becomes an implementation. **Don't use it for single-implementation scenarios** — if you have a TaxCalculator and there's no realistic chance of multiple tax calculation strategies, an interface adds ceremony without benefit. Wait until the second implementation appears."
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **Creating anemic interfaces**: Defining an interface with just one or two methods but no clear abstraction purpose. Interfaces should represent meaningful concepts, not just mirror one class.\n- **Hardcoding types somewhere else**: Creating the interface but then using instanceof checks or a factory that hardcodes specific implementations. The whole point is avoiding type checks.\n- **Not using dependency injection**: Manually instantiating implementations inside the service defeats OCP. Use constructor injection and let Spring or a factory provide the implementation.\n- **Interface explosion**: Creating interfaces for every class reflexively. Only abstract things that vary or will vary."
        },
        {
          "type": "key_points",
          "title": "Quick revision",
          "content": [
            "Put a real variation behind a small, stable interface.",
            "Make the client call the interface rather than switch on concrete types.",
            "Add each variant as a separate implementation that preserves the contract.",
            "Selection or registration belongs at a composition boundary and may still change.",
            "Do not create an interface when there is no useful variation or boundary."
          ]
        },
        {
          "type": "speakable_answer",
          "title": "Interview answer",
          "content": "A common Java implementation of OCP is to isolate a behavior that genuinely varies behind an interface. The stable class depends on that contract, and each variant implements it.\n\nFor example, `PricingService` can receive a `DiscountPolicy` with `discountFor(Order)`. `SeasonalDiscount` and `MemberDiscount` contain their own rules. Adding `FirstOrderDiscount` adds a class and tests without adding another type check to `PricingService`. The composition root or Spring configuration still selects which policy applies.\n\nThe interface is useful only if its contract stays stable. If the client still uses `instanceof`, or every new variant forces another method into the interface, the change has not been isolated. OCP reduces changes to stable logic; it does not promise zero changes or automatic runtime switching.",
          "answerSize": "compact"
        }
      ]
    },
    "followup_questions": [
      "What is the Strategy design pattern?",
      "How does Spring Boot wiring support OCP?"
    ],
    "seo": {
      "title": "How to implement Open Closed Principle with interfaces in Java",
      "description": "Learn how to apply OCP in Java using interfaces and polymorphism with Spring Boot dependency injection examples.",
      "keywords": [
        "OCP Java interface",
        "open closed principle implementation",
        "SOLID Java interfaces"
      ]
    },
    "last_updated": "2026-09-08"
  },
  {
    "id": "ocp-003",
    "slug": "if-else-chains-as-ocp-violations",
    "question": "What is the problem with long if-else chains in terms of OCP?",
    "title": "If-else chains as OCP violations",
    "direct_answer": "A long conditional can violate OCP when it repeatedly selects behavior by type and must be edited for every new variant. That is a signal to consider polymorphism, a handler registry, or data-driven rules. Conditionals are not inherently wrong: a small, stable, exhaustive decision can be clearer than an abstraction.",
    "layout_type": "concept-explainer",
    "difficulty": "medium",
    "importance": "high",
    "reading_time_minutes": 2,
    "company_tags": [
      "tcs",
      "cognizant",
      "flipkart",
      "infosys"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "The Type-Switching Antipattern",
          "content": "An `if` or `switch` becomes an OCP concern when it is the central list of changing variants. If every new shipping method requires editing the same calculator, tests for old branches and new branches share one growing change point.\n\nA `ShippingCost` strategy or a registry from method code to handler can isolate those algorithms. The caller chooses a handler and invokes one contract; a new independently developed method adds a handler and registration rather than another calculation branch.\n\nNot every conditional needs polymorphism. Null checks, validation, state-machine transitions, and a small closed enum often belong in direct control flow. The decision depends on change pressure, ownership, complexity, and whether each branch represents an independent behavior."
        },
        {
          "type": "concept_map",
          "title": "At a Glance",
          "content": "amber|The Anti-Pattern|~type switch|if/else on enum or instanceof|grows with every new type\nblue|Why It Hurts|~OCP break|new type → edit and retest the method|shotgun surgery\nemerald|The Fix|~polymorphism|interface + one class per type|a map or DI picks the impl\nviolet|Bonus|~cleaner code|no giant method|each variant isolated"
        },
        {
          "type": "code_example",
          "title": "Refactoring Type-Switch to Polymorphism",
          "content": "// BEFORE: Type-switching antipattern (OCP violation)\nclass ShippingCalculatorBad {\n    public double calculateCost(String method, double weight) {\n        if (method.equals(\"GROUND\")) {\n            return weight * 1.5;\n        } else if (method.equals(\"EXPRESS\")) {\n            return weight * 3.0 + 10;\n        } else if (method.equals(\"OVERNIGHT\")) {\n            return weight * 5.0 + 20;\n        }\n        throw new IllegalArgumentException(\"Unknown method: \" + method);\n        // Adding AIR_FREIGHT means modifying this method\n    }\n}\n\n// AFTER: Polymorphic approach (OCP compliant)\ninterface ShippingMethod {\n    double calculateCost(double weight);\n}\n\nclass GroundShipping implements ShippingMethod {\n    public double calculateCost(double weight) {\n        return weight * 1.5;\n    }\n}\n\nclass ExpressShipping implements ShippingMethod {\n    public double calculateCost(double weight) {\n        return weight * 3.0 + 10;\n    }\n}\n\nclass OvernightShipping implements ShippingMethod {\n    public double calculateCost(double weight) {\n        return weight * 5.0 + 20;\n    }\n}\n\n@Service\nclass ShippingCalculatorGood {\n    public double calculateCost(ShippingMethod method, double weight) {\n        return method.calculateCost(weight);  // No if-else needed\n    }\n    // Adding AirFreightShipping requires zero changes here\n}"
        },
        {
          "type": "when_to_use",
          "title": "When If-Else Becomes a Problem",
          "content": "Not all if-else is bad — **simple conditionals are fine**. The problem emerges when if-else switches on types to select behavior variants. **Refactor when you hit three or more branches** checking types or enums. At that point, polymorphism becomes clearer than conditionals. **Refactor when you add branches frequently** — if business requirements mean adding new payment types, shipping methods, or discount rules regularly, the if-else becomes a choke point. **Refactor when branches contain complex logic** — if each branch is 10+ lines of domain logic, extracting to separate classes improves readability even before extension happens. **Keep if-else for simple control flow** — checking null, validating input, or choosing between two straightforward paths doesn't need polymorphism. The rule: if-else for conditions, polymorphism for type-based behavior variants."
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **Refactoring every switch into classes**: a small closed decision can be clearer as direct control flow.\n- **Changing syntax without changing the change boundary**: replacing `if` with `switch` does not isolate independently evolving algorithms.\n- **Creating strategies while retaining concrete type checks in the client**: selection belongs at a deliberate boundary.\n- **Ignoring evidence**: use repeated additions, ownership, and branch complexity to justify the abstraction."
        },
        {
          "type": "key_points",
          "title": "Quick revision",
          "content": [
            "A conditional is an OCP smell when every new behavior variant edits the same branch list.",
            "Polymorphism or a handler registry can isolate independently changing algorithms.",
            "A small, stable, exhaustive decision may be clearer as a switch.",
            "Validation and state transitions are not automatically OCP violations.",
            "Refactor in response to real change pressure, not simply line count."
          ]
        },
        {
          "type": "speakable_answer",
          "title": "Interview answer",
          "content": "A long `if-else` or `switch` is an OCP problem when it acts as a catalogue of behavior variants and must be modified whenever a new variant is added. A shipping calculator that contains all ground, express, and freight algorithms in one switch has that change pattern.\n\nThose algorithms can implement a `ShippingCost` contract and be selected by a registry or composition root. The calculator then calls the selected handler without knowing its concrete type. A new carrier adds a handler and registration, while existing calculations remain isolated.\n\nThe presence of a conditional alone is not a violation. A short switch over a deliberately closed enum, input validation, or explicit state-machine logic may be the simplest and safest design. The signal is repeated change and independent behavior, not a rule that every switch must become classes.",
          "answerSize": "compact"
        }
      ]
    },
    "followup_questions": [
      "What is the type-switching antipattern?",
      "How do you refactor an if-else chain to be OCP-compliant?"
    ],
    "seo": {
      "title": "If-else chains as OCP violations | Open Closed Principle Java",
      "description": "Understand why long if-else type-checking chains violate the Open/Closed Principle and how polymorphism fixes it.",
      "keywords": [
        "if-else OCP violation",
        "open closed principle if-else",
        "polymorphism replace if-else"
      ]
    },
    "last_updated": "2026-09-08"
  },
  {
    "id": "ocp-004",
    "slug": "design-patterns-that-implement-the-open-closed-principle",
    "question": "Which design patterns are commonly used to achieve OCP?",
    "title": "Design patterns that implement the Open/Closed Principle",
    "direct_answer": "Strategy, Decorator, Template Method, Factory Method, Chain of Responsibility, and plugin or handler registries can support OCP by creating explicit variation points. No pattern guarantees OCP: a central factory switch or an unstable interface can still require modification for every new case.",
    "layout_type": "concept-explainer",
    "difficulty": "medium",
    "importance": "medium",
    "reading_time_minutes": 2,
    "company_tags": [
      "amazon",
      "microsoft",
      "flipkart",
      "wipro"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "Design Patterns That Enable OCP",
          "content": "Several patterns can create a stable place for variation:\n\n- **Strategy** moves an algorithm behind a contract.\n- **Decorator** wraps an object with the same contract and adds behavior around delegation.\n- **Template Method** fixes an algorithm outline while subclasses customize defined steps.\n- **Factory Method** lets subclasses or providers decide what product to create.\n- **Chain of Responsibility** adds handlers that share a request contract.\n- A **registry or plugin mechanism** discovers handlers without placing every concrete type in the caller.\n\nThe pattern name is not proof of OCP. A simple factory with a growing switch still changes for every product. A strategy interface that gains a new method for every variant is not stable. Judge whether the expected extension can be added while preserving the central contract and existing behavior."
        },
        {
          "type": "concept_map",
          "title": "At a Glance",
          "content": "amber|Strategy|~swap algorithms|interface + impls|inject the chosen one\nblue|Template Method|~fixed skeleton|abstract steps overridden|parent closed, child extends\nemerald|Decorator|~add behavior|wrap, don't modify|stack responsibilities\nviolet|Factory|~hide creation|new type → register, not edit callers"
        },
        {
          "type": "code_example",
          "title": "Strategy and Decorator Together",
          "content": "// Strategy pattern: swap sorting algorithms\ninterface SortStrategy {\n    void sort(List<Integer> numbers);\n}\n\nclass QuickSort implements SortStrategy {\n    public void sort(List<Integer> numbers) {\n        Collections.sort(numbers);  // Using built-in quick sort\n    }\n}\n\nclass BubbleSort implements SortStrategy {\n    public void sort(List<Integer> numbers) {\n        // Bubble sort implementation\n    }\n}\n\n// Decorator pattern: add logging to any strategy\nclass LoggingSortDecorator implements SortStrategy {\n    private final SortStrategy wrapped;\n    \n    public LoggingSortDecorator(SortStrategy wrapped) {\n        this.wrapped = wrapped;\n    }\n    \n    public void sort(List<Integer> numbers) {\n        System.out.println(\"Sorting started with \" + numbers.size() + \" items\");\n        wrapped.sort(numbers);\n        System.out.println(\"Sorting completed\");\n    }\n}\n\n@Service\nclass DataProcessor {\n    private final SortStrategy sortStrategy;\n    \n    public DataProcessor(SortStrategy sortStrategy) {\n        this.sortStrategy = sortStrategy;\n    }\n    \n    public void processData(List<Integer> data) {\n        sortStrategy.sort(data);  // Extends via strategy, decorates via wrapper\n    }\n}\n\n// Usage: new DataProcessor(new LoggingSortDecorator(new QuickSort()))"
        },
        {
          "type": "when_to_use",
          "title": "Choosing the Right Pattern",
          "content": "Use **Strategy** when you have multiple algorithms or behaviors that are interchangeable at runtime — sorting algorithms, validation rules, pricing calculations. Strategy is the most common OCP pattern.\n\nUse **Template Method** when you have a multi-step process where most steps are fixed but one or two vary — database transaction handling where connection setup is standard but query logic varies, or file parsers where reading and closing are fixed but parsing varies by format.\n\nUse **Decorator** when you need to add optional features to objects dynamically — adding logging, caching, or retry logic to existing services without modifying them. Decorators stack, so you can layer multiple behaviors.\n\nUse **Factory** when object creation logic is complex or when the type to create depends on runtime conditions — creating different notification senders based on user preferences, or different report generators based on file format. Factories centralize creation logic, keeping client code clean."
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **Listing patterns without explaining the extension point**: name the stable contract and what new code can be added.\n- **Calling every factory extensible**: a central product switch still changes for each new case.\n- **Confusing callback templates with subclass-only Template Method**: explain the actual mechanism used by the library.\n- **Treating a security filter chain as Decorator by default**: chains and decorators have different object relationships.\n- **Adding indirection without real variation**: patterns have a maintenance cost too."
        },
        {
          "type": "key_points",
          "title": "Quick revision",
          "content": [
            "Strategy isolates interchangeable algorithms behind one contract.",
            "Decorator adds responsibilities by wrapping and delegating through the same contract.",
            "Template Method exposes controlled subclass extension steps.",
            "Factory Method, handler chains, and registries can move creation or selection out of clients.",
            "A pattern supports OCP only when its extension contract remains stable."
          ]
        },
        {
          "type": "speakable_answer",
          "title": "Interview answer",
          "content": "Patterns commonly used for OCP include Strategy for interchangeable algorithms, Decorator for adding behavior around the same interface, Template Method for controlled subclass hooks, Factory Method for extensible creation, and Chain of Responsibility or registries for adding handlers.\n\nFor example, a request pipeline can hold a list of `RequestHandler` implementations. Each handler decides whether and how to process a request. A new audit handler can be registered without adding another branch to the pipeline. Java I/O decorators similarly wrap a `Reader` to add buffering while preserving the `Reader` contract.\n\nThe pattern must be implemented carefully. A factory containing a central switch still changes for every product, and an interface that changes for every new variant is not a stable extension point. Patterns offer structures for OCP; the actual test is whether the expected change stays localized and preserves existing contracts.",
          "answerSize": "compact"
        }
      ]
    },
    "followup_questions": [
      "What is the Strategy design pattern?",
      "How is Decorator different from inheritance?"
    ],
    "seo": {
      "title": "Design patterns for Open Closed Principle — Strategy Template Decorator",
      "description": "Learn which design patterns implement the Open/Closed Principle: Strategy, Template Method, Decorator, and Factory.",
      "keywords": [
        "OCP design patterns",
        "strategy pattern OCP",
        "open closed principle patterns"
      ]
    },
    "last_updated": "2026-09-08"
  },
  {
    "id": "ocp-005",
    "slug": "ocp-and-code-modification-what-is-actually-closed",
    "question": "Does OCP mean you should never modify existing code?",
    "title": "OCP and code modification — what is actually closed?",
    "direct_answer": "No. OCP does not mean never touch existing code. It means stable, well-tested code should not need to change just to add new features. Bug fixes, refactoring, and performance improvements still require modifying code. The key is: adding a new feature should ideally be done by adding new code, not by editing already-working code.",
    "layout_type": "concept-explainer",
    "difficulty": "medium",
    "importance": "medium",
    "reading_time_minutes": 2,
    "company_tags": [
      "microsoft",
      "amazon",
      "tcs",
      "cognizant"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "OCP as a Design Guideline, Not a Law",
          "content": "The Open/Closed Principle is often misunderstood as an absolute rule: never modify existing code. That interpretation is **too rigid** and leads to over-engineering or analysis paralysis.\n\nThe true meaning: **stable, working code should not require changes to accommodate new features**. The emphasis is on feature additions, not all modifications. **Bug fixes** always modify code — that's their purpose. **Refactoring** for clarity or performance improvements modifies code — that's healthy maintenance. **API changes** when requirements shift fundamentally will modify code — that's adapting to reality. OCP is about **minimizing risky edits** to battle-tested classes when extending functionality. It's a **design goal**, not a constraint. The principle guides you to build **extensible architectures** where new features slot in as new components rather than requiring surgery on existing ones. Think of OCP as **making extension easy and modification unnecessary**, not making modification forbidden. A well-designed system lets most features be added by writing new classes, but pragmatism accepts that some changes require touching old code — and that's okay when done carefully."
        },
        {
          "type": "concept_map",
          "title": "At a Glance",
          "content": "amber|The Nuance|~not literal|OCP is not 'never touch code'|bug fixes still edit code\nblue|What's Closed|~stable behavior|don't edit working logic for new variants\nemerald|What's Open|~new behavior|added via new classes or extensions\nviolet|Judgment|~apply where it varies|don't over-abstract stable code (YAGNI)"
        },
        {
          "type": "code_example",
          "title": "Legitimate Modifications to Existing Code",
          "content": "// Original implementation\n@Service\nclass UserService {\n    private final UserRepository repository;\n    \n    public UserService(UserRepository repository) {\n        this.repository = repository;\n    }\n    \n    public void registerUser(String email, String password) {\n        var user = new User(email, password);\n        repository.save(user);\n    }\n}\n\n// Scenario 1: BUG FIX - modify existing code (OCP doesn't apply)\nclass UserService {\n    public void registerUser(String email, String password) {\n        if (email == null || email.isBlank()) {\n            throw new IllegalArgumentException(\"Email required\");\n        }\n        var user = new User(email, password);\n        repository.save(user);\n    }\n}\n\n// Scenario 2: REFACTORING - modify for clarity (OCP doesn't apply)\nclass UserService {\n    public void registerUser(String email, String password) {\n        validateEmail(email);\n        var user = new User(email, password);\n        repository.save(user);\n    }\n    \n    private void validateEmail(String email) {\n        if (email == null || email.isBlank()) {\n            throw new IllegalArgumentException(\"Email required\");\n        }\n    }\n}\n\n// Scenario 3: FEATURE ADDITION - extend via new class (OCP applies)\ninterface UserRegistrationListener {\n    void onUserRegistered(User user);\n}\n\nclass EmailVerificationListener implements UserRegistrationListener {\n    public void onUserRegistered(User user) {\n        // Send verification email - new behavior, zero changes to UserService\n    }\n}"
        },
        {
          "type": "when_to_use",
          "title": "When to Modify vs When to Extend",
          "content": "**Modify existing code** when fixing bugs — incorrect logic must be corrected in place. **Modify for security patches** — vulnerabilities require immediate fixes regardless of OCP. **Modify during refactoring** — improving code structure, extracting methods, renaming for clarity all touch existing files. **Modify when requirements fundamentally change** — if the business model shifts and old assumptions are invalid, rewriting is appropriate. **Extend with new code** when adding optional features — new payment methods, new report formats, new notification channels should be new classes if the design supports it. **Extend when adding variations** — new discount types, new validators, new strategies fit the extension model. The decision rule: if you're fixing or fundamentally changing what exists, modify. If you're adding alternatives or new capabilities alongside existing ones, extend. OCP primarily guides the second case."
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **Treating OCP as absolute**: Freshers read OCP and become afraid to touch any existing code, even for legitimate bug fixes. This leads to convoluted workarounds instead of straightforward fixes. Remember: OCP is about feature additions, not prohibiting all modifications.\n- **Over-engineering everything**: Creating interfaces and abstract base classes for every class preemptively to satisfy OCP, even when no variation exists or is planned. This adds complexity without benefit. Apply OCP when variation is expected, not by default.\n- **Not modifying when you should**: Continuing to hack around poorly designed code with more extensions when the real solution is refactoring the original design. Sometimes the best path is to modify the base design to make it truly extensible."
        },
        {
          "type": "key_points",
          "title": "Quick revision",
          "content": [
            "**OCP is a guideline**: it shapes design decisions but is not an absolute prohibition on modifying code",
            "**Bug fixes modify code**: correcting defects requires changing the defective code, OCP does not apply to bug fixes",
            "**Refactoring modifies code**: improving structure, performance, or readability involves touching existing code appropriately",
            "**Feature additions should extend**: the core OCP use case is adding capabilities without editing stable classes",
            "**Stability is the goal**: minimize risky changes to well-tested code, not eliminate all changes"
          ]
        },
        {
          "type": "speakable_answer",
          "title": "Interview answer",
          "content": "No, OCP definitely doesn't mean you never modify existing code. That's a common misunderstanding. What OCP actually says is that stable, well-tested code shouldn't need changes just to add new features. But there are plenty of legitimate reasons to modify code. Bug fixes always require modifying code — that's how you fix bugs. If your UserService has a logic error, you edit it, you don't try to extend around it. Refactoring is another case. If you're improving code readability or extracting methods for clarity, that modifies existing files, and that's healthy maintenance. Performance optimizations often modify existing code too. Security patches require immediate changes regardless of design principles. OCP is really about feature additions. When you're adding a new payment method or a new discount type, that should happen by creating new classes, not by editing the existing payment processor. The principle is about making your design extensible so that most new features don't require touching stable code. It's a design goal, not a rigid rule. The key is judgment. If you're fixing something broken or fundamentally changing requirements, modify the code. If you're adding a variant of existing behavior and the design supports it, extend with new classes. OCP guides you to prefer extension where feasible, but it doesn't handcuff you.",
          "answerSize": "compact"
        }
      ]
    },
    "followup_questions": [
      "What is the difference between modifying and extending in OCP?",
      "When is it acceptable to violate OCP?"
    ],
    "seo": {
      "title": "Does OCP mean never modify code? Open Closed Principle clarified",
      "description": "Clarify whether Open/Closed Principle means you can never modify existing code — OCP is about extensibility, not prohibition.",
      "keywords": [
        "OCP never modify code",
        "open closed principle modification",
        "OCP clarification"
      ]
    },
    "last_updated": "2026-09-08"
  }
]

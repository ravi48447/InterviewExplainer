{
  "topic": "DDL vs DML vs DQL",
  "topicSlug": "ddl-dml-dql",
  "questions": [
    {
      "id": "ddl-dml-dql-001",
      "slug": "ddl-vs-dml-vs-dql-in-sql",
      "question": "What is the difference between DDL, DML, and DQL in SQL?",
      "title": "DDL vs DML vs DQL in SQL",
      "direct_answer": "SQL commands are grouped by purpose: **DDL** (Data Definition Language) defines structure — `CREATE`, `ALTER`, `DROP`, `TRUNCATE`; **DML** (Data Manipulation Language) changes data — `INSERT`, `UPDATE`, `DELETE`; **DQL** (Data Query Language) reads data — `SELECT`. DDL auto-commits; DML is transactional.",
      "layout_type": "sql-playground",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-04",
      "interviewer_intent": {
        "testing": "Whether you can categorize SQL commands and know DDL auto-commits while DML is transactional.",
        "common_mistake": "Lumping all SQL together and assuming everything can be rolled back.",
        "to_stand_out": "Mention DCL (GRANT/REVOKE) and TCL (COMMIT/ROLLBACK) as the other two families."
      },
      "company_tags": [
        "tcs",
        "infosys",
        "wipro",
        "amazon"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "DDL vs DML vs DQL",
            "content": "SQL statements are organized into families by what they do. **DDL** — *Data Definition Language* — defines and changes **structure**: `CREATE`, `ALTER`, `DROP`, `TRUNCATE`. **DML** — *Data Manipulation Language* — changes the **data inside** tables: `INSERT`, `UPDATE`, `DELETE`. **DQL** — *Data Query Language* — **reads** data: `SELECT`.\n\nA crucial behavioural difference: **DDL auto-commits** (its changes are permanent immediately and end any open transaction), while **DML is transactional** — you can `COMMIT` or `ROLLBACK` it. (Two more families exist: **DCL** `GRANT/REVOKE`, **TCL** `COMMIT/ROLLBACK`.)"
          },
          {
            "type": "concept_map",
            "title": "SQL Command Families",
            "content": "blue|DDL|~defines structure|CREATE ALTER DROP TRUNCATE|auto-commits|changes the schema\nemerald|DML|~changes data|INSERT UPDATE DELETE|transactional|rollbackable\namber|DQL|~reads data|SELECT|no data change|the R in CRUD\nrose|DCL / TCL|~security + txn|GRANT/REVOKE (DCL)|COMMIT/ROLLBACK (TCL)|round out the families"
          },
          {
            "type": "code_example",
            "title": "One statement from each family",
            "content": "```sql\n-- DDL: define structure (auto-commits)\nCREATE TABLE products (id INT PRIMARY KEY, name VARCHAR(80));\n\n-- DML: change data (transactional)\nINSERT INTO products VALUES (1, 'Phone');\nUPDATE products SET name = 'Smartphone' WHERE id = 1;\n\n-- DQL: read data\nSELECT id, name FROM products;\n```\n\nSample output (SELECT):\n\n| id | name |\n|---|---|\n| 1 | Smartphone |"
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Assuming DDL is rollbackable**: in most databases DDL auto-commits and can't be undone.\n- **Mixing DDL inside a transaction**: a DDL statement often commits the pending DML before it.\n- **Calling SELECT 'DML'**: SELECT is DQL (it reads, doesn't change data).\n- **Forgetting DCL/TCL exist**: GRANT/REVOKE and COMMIT/ROLLBACK are separate families."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **DDL** defines structure: CREATE, ALTER, DROP, TRUNCATE.\n- **DML** changes data: INSERT, UPDATE, DELETE.\n- **DQL** reads data: SELECT.\n- **DDL auto-commits**; **DML is transactional** (COMMIT/ROLLBACK).\n- **DCL** (GRANT/REVOKE) and **TCL** (COMMIT/ROLLBACK) are the other families."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "SQL commands are grouped into families based on what they do, and knowing the categories helps you reason about behaviour. DDL, data definition language, is about structure: CREATE to make a table, ALTER to change it, DROP to remove it, and TRUNCATE to empty it. DML, data manipulation language, is about the data inside tables: INSERT to add rows, UPDATE to change them, and DELETE to remove them. DQL, data query language, is about reading data, and that's essentially just SELECT, the R in CRUD. The behavioural difference that interviewers love is around transactions. DDL statements auto-commit, meaning their effect is permanent immediately and they typically commit any pending work, so you can't roll a CREATE or DROP back in most databases. DML, on the other hand, is transactional: you can wrap inserts, updates, and deletes in a transaction and either commit them or roll them back. There are two more families worth naming to show depth: DCL, data control language, which is GRANT and REVOKE for permissions, and TCL, transaction control language, which is COMMIT and ROLLBACK. So the quick mental map is DDL for structure, DML for data changes, DQL for reads, plus DCL and TCL for security and transactions."
          }
        ]
      },
      "followup_questions": [
        "Why do DDL statements auto-commit?",
        "Which SQL family does SELECT belong to?",
        "What are DCL and TCL commands?"
      ],
      "seo": {
        "metaTitle": "DDL vs DML vs DQL in SQL | Java Fresher Interview",
        "metaDescription": "Learn the difference between DDL, DML, and DQL in SQL with examples. Essential Java fresher interview question covering CREATE, INSERT, SELECT commands."
      },
      "order": 1,
      "speakable_v2": {
        "archetype": "B",
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Spring Boot app with JPA over a relational database",
        "familiarity_anchors": [
          "SQL",
          "databases",
          "queries"
        ],
        "hook": "DDL builds the table, DML fills it, DQL reads it — and only one of those three you can't roll back.",
        "beats": [
          {
            "layout": "paragraph",
            "content": "SQL commands are grouped by what they do. DDL — Data Definition Language — defines the database structure using CREATE, ALTER, DROP, and TRUNCATE. DML — Data Manipulation Language — works with the actual data using INSERT, UPDATE, and DELETE. DQL — Data Query Language — retrieves data and is essentially SELECT."
          },
          {
            "layout": "mini_table",
            "content": {
              "headers": [
                "Category",
                "Commands",
                "What it does"
              ],
              "rows": [
                [
                  "DDL",
                  "CREATE, ALTER, DROP, TRUNCATE",
                  "Defines database structure"
                ],
                [
                  "DML",
                  "INSERT, UPDATE, DELETE",
                  "Changes data in tables"
                ],
                [
                  "DQL",
                  "SELECT",
                  "Reads and queries data"
                ],
                [
                  "TCL",
                  "COMMIT, ROLLBACK, SAVEPOINT",
                  "Manages transactions"
                ]
              ]
            }
          },
          {
            "layout": "paragraph",
            "content": "sharp edge: DDL commands auto-commit in most databases like MySQL and Oracle. That means you cannot rollback a DROP TABLE — the change is permanent the moment you run it. DML commands are transactional and can be rolled back."
          }
        ],
        "cap": "DDL shapes structure, DML changes data, DQL reads it — and DDL auto-commits while DML participates in transactions.",
        "followup_handoff": [
          "Can you rollback a DROP TABLE statement?",
          "What is the difference between DELETE and TRUNCATE?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "ddl-dml-dql-002",
      "slug": "ddl-commands-in-sql-create-alter-drop-truncate",
      "question": "What are the main DDL commands and what does each do?",
      "title": "DDL Commands in SQL — CREATE, ALTER, DROP, TRUNCATE",
      "direct_answer": "The main **DDL** commands are **CREATE** (make a new table/index/database), **ALTER** (modify an existing object — add/drop/change columns), **DROP** (delete an object entirely), and **TRUNCATE** (remove all rows but keep the table). They define and change schema, and they auto-commit.",
      "layout_type": "sql-playground",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-04",
      "interviewer_intent": {
        "testing": "Whether you know the four core DDL commands and what each does.",
        "common_mistake": "Confusing TRUNCATE (DDL, empties table) with DELETE (DML, removes rows).",
        "to_stand_out": "Note ALTER variants (ADD/DROP/MODIFY COLUMN) and that DDL auto-commits."
      },
      "company_tags": [
        "tcs",
        "infosys",
        "cognizant",
        "wipro"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "The Core DDL Commands",
            "content": "**DDL** defines and changes database **structure**, and these four do the heavy lifting:\n\n- **`CREATE`** — make a new object (table, index, view, database).\n- **`ALTER`** — modify an existing object: `ADD COLUMN`, `DROP COLUMN`, `MODIFY COLUMN`, add constraints.\n- **`DROP`** — delete an object entirely (structure + data).\n- **`TRUNCATE`** — remove **all rows** fast while keeping the table's structure.\n\nAll of them **auto-commit**, so their effects are immediate and not easily undone."
          },
          {
            "type": "concept_map",
            "title": "DDL Commands at a Glance",
            "content": "blue|CREATE|~make new object|table/index/view|defines structure|\nemerald|ALTER|~modify object|ADD/DROP/MODIFY column|add constraints|evolves schema\nrose|DROP|~delete object|structure + data gone|irreversible-ish|\namber|TRUNCATE|~empty table fast|keeps structure|resets identity|auto-commits"
          },
          {
            "type": "code_example",
            "title": "CREATE, ALTER, TRUNCATE, DROP",
            "content": "```sql\nCREATE TABLE customers (id INT PRIMARY KEY, name VARCHAR(80));\n\nALTER TABLE customers ADD COLUMN email VARCHAR(120);   -- add a column\nALTER TABLE customers MODIFY COLUMN name VARCHAR(120); -- change a type\n\nTRUNCATE TABLE customers;  -- remove all rows, keep the table\nDROP TABLE customers;      -- remove the table entirely\n```\n\n*`CREATE`/`ALTER` shape the schema; `TRUNCATE` empties; `DROP` removes the object.*"
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **TRUNCATE vs DELETE**: TRUNCATE is DDL and empties the whole table; DELETE is DML and can filter rows.\n- **Expecting rollback**: DDL auto-commits — a wrong DROP usually needs a restore.\n- **ALTER on huge tables**: some ALTERs rewrite/lock the table — plan for it in production.\n- **DROP vs TRUNCATE confusion**: DROP removes the table; TRUNCATE keeps an empty one."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **CREATE** makes a new object.\n- **ALTER** modifies an existing object (add/drop/change columns).\n- **DROP** deletes an object entirely.\n- **TRUNCATE** empties a table but keeps its structure.\n- **All DDL auto-commits** — effects are immediate."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "The core DDL commands are CREATE, ALTER, DROP, and TRUNCATE, and together they define and evolve your database structure. CREATE makes a new object, most commonly a table, but also indexes, views, or databases. ALTER modifies an existing object; for a table that means adding a column, dropping a column, modifying a column's type, or adding and removing constraints, so ALTER is how your schema evolves over time. DROP deletes an object entirely, including both its structure and its data, so after dropping a table it no longer exists. TRUNCATE is the one people often confuse with DELETE: TRUNCATE is DDL and it removes all the rows from a table very quickly while keeping the table's structure intact, and it usually resets the auto-increment counter. DELETE, by contrast, is DML, can use a WHERE clause to remove specific rows, and is transactional. A key behaviour across all four DDL commands is that they auto-commit, so their effects are immediate and you generally can't roll them back; an accidental DROP usually means restoring from a backup. One production caution: ALTER on a very large table can rewrite or lock the table, so I plan those changes carefully."
          }
        ]
      },
      "followup_questions": [
        "What is the difference between TRUNCATE and DELETE?",
        "Can you roll back an ALTER TABLE?",
        "What does ALTER TABLE let you change?"
      ],
      "seo": {
        "metaTitle": "DDL Commands in SQL: CREATE ALTER DROP TRUNCATE | Java Interview",
        "metaDescription": "Understand DDL commands CREATE, ALTER, DROP, TRUNCATE with examples. Key Java backend fresher interview topic on SQL Data Definition Language."
      },
      "order": 2,
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Spring Boot app with JPA over a relational database",
        "familiarity_anchors": [
          "SQL",
          "databases",
          "queries"
        ],
        "hook": "CREATE, ALTER, DROP, TRUNCATE shape your schema — and every one of them commits the moment you run it, no undo button.",
        "beats": [
          {
            "layout": "paragraph",
            "content": "DDL commands define and change the database structure. CREATE builds new tables, databases, indexes, or views. ALTER modifies an existing table — adding a column, changing a data type, or adding a constraint. DROP permanently removes a table along with all its data and structure. TRUNCATE removes all rows from a table but leaves the structure intact and resets the auto-increment counter."
          },
          {
            "layout": "bullets",
            "content": [
              "TRUNCATE is DDL, not DML — even though it removes rows",
              "All DDL commands auto-commit in MySQL and Oracle — no rollback possible",
              "ALTER TABLE operations like adding a column on a large table can lock the table for a significant time in production"
            ]
          }
        ],
        "cap": "CREATE, ALTER, DROP, TRUNCATE — all auto-commit and cannot be rolled back in MySQL.",
        "followup_handoff": [
          "What is the difference between TRUNCATE and DELETE?",
          "Can you ALTER a table that has data in it?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "ddl-dml-dql-003",
      "slug": "delete-vs-truncate-vs-drop-in-sql-2",
      "question": "What is the difference between DELETE, TRUNCATE, and DROP in SQL?",
      "title": "DELETE vs TRUNCATE vs DROP in SQL",
      "direct_answer": "**DELETE** (DML) removes rows, supports `WHERE`, is logged and rollbackable. **TRUNCATE** (DDL) empties the whole table fast, resets identity, and usually auto-commits. **DROP** (DDL) removes the table itself — structure and all. Same removal theme, very different scope and recoverability.",
      "layout_type": "comparison-arena",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-04",
      "interviewer_intent": {
        "testing": "Whether you can rank DELETE/TRUNCATE/DROP by scope and recoverability.",
        "common_mistake": "Treating TRUNCATE like DELETE (expecting WHERE or rollback).",
        "to_stand_out": "Note DELETE is DML (transactional); TRUNCATE/DROP are DDL (auto-commit)."
      },
      "company_tags": [
        "amazon",
        "microsoft",
        "tcs",
        "infosys"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "DELETE vs TRUNCATE vs DROP",
            "content": "Three ways to remove data, escalating in scope. **`DELETE`** is **DML**: removes rows (with optional `WHERE`), logs each, and can be rolled back in a transaction — but is slower per row. **`TRUNCATE`** is **DDL**: empties the *entire* table fast, resets auto-increment, and usually auto-commits (hard to undo). **`DROP`** is **DDL**: removes the **table object itself** — columns, indexes, data, all gone."
          },
          {
            "type": "concept_map",
            "title": "DELETE / TRUNCATE / DROP",
            "content": "blue|DELETE|~rows, DML|WHERE allowed|logged + rollbackable|slower per row\nemerald|TRUNCATE|~whole table, DDL|no WHERE, fast|resets identity|auto-commits\nrose|DROP|~table itself, DDL|structure gone|indexes/constraints gone|auto-commits\namber|Recover|~know the risk|DELETE: undo in txn|TRUNCATE/DROP: restore from backup|"
          },
          {
            "type": "comparison_table",
            "title": "Comparison",
            "content": "| Aspect | DELETE | TRUNCATE | DROP |\n|---|---|---|---|\n| Family | DML | DDL | DDL |\n| WHERE | yes | no | n/a |\n| Speed | slow | fast | fast |\n| Rollback | yes (txn) | usually no | usually no |\n| Table remains | yes | yes (empty) | no |"
          },
          {
            "type": "before_code",
            "title": "DELETE: selective and reversible",
            "content": "```sql\nBEGIN;\nDELETE FROM logs WHERE created < '2025-01-01';  -- only old rows\nROLLBACK;  -- undone: DELETE is transactional\n```"
          },
          {
            "type": "after_code",
            "title": "TRUNCATE / DROP: wholesale and structural",
            "content": "```sql\nTRUNCATE TABLE logs;   -- empties fast, resets identity, auto-commits\nDROP TABLE logs;       -- removes the table entirely\n```\n\n*DELETE for specific, recoverable removal; TRUNCATE to wipe fast; DROP to delete the table.*"
          },
          {
            "type": "when_to_use",
            "title": "When to Use Which",
            "content": "Use **DELETE** to remove *specific* rows or when you need transactional safety. Use **TRUNCATE** to clear an entire table quickly (staging/reload) with no WHERE or rollback needed. Use **DROP** when the table should no longer exist at all."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **DELETE** = DML, row-level, `WHERE`-able, rollbackable.\n- **TRUNCATE** = DDL, fast full wipe, resets identity, auto-commits.\n- **DROP** = DDL, removes the table and its structure.\n- **TRUNCATE/DROP** generally can't be rolled back.\n- **Pick by scope**: rows vs whole table vs the table object."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "DELETE, TRUNCATE, and DROP all remove data, but they escalate in scope and differ in recoverability. DELETE is a DML command that removes rows. You can target specific rows with a WHERE clause, each removed row is logged, and because it's transactional you can roll it back inside a transaction. The trade-off is that row-by-row deletion is slower on big tables. TRUNCATE is a DDL command that empties the entire table in one fast operation, with no WHERE clause, and it resets the auto-increment counter. In most databases it auto-commits, so you generally can't undo it. DROP is also DDL, and it's the most drastic: it removes the table object itself, including its structure, columns, indexes, and constraints, not just the rows. So I choose based on scope. If I need to remove certain rows, or want the safety of rolling back, I use DELETE. If I want to wipe an entire table quickly, like resetting a staging table before a reload, I use TRUNCATE. If the table should genuinely cease to exist, I use DROP. And because TRUNCATE and DROP usually can't be rolled back, I make sure there's a backup before running them in production."
          }
        ]
      },
      "followup_questions": [
        "Why is TRUNCATE faster than DELETE?",
        "Which of these auto-commit and why?",
        "Does DROP remove indexes and constraints too?"
      ],
      "seo": {
        "metaTitle": "DELETE vs TRUNCATE vs DROP in SQL | Java Fresher Interview",
        "metaDescription": "Compare DELETE, TRUNCATE, and DROP SQL commands with key differences. Essential Java backend interview question on data removal operations."
      },
      "order": 3,
      "speakable_v2": {
        "archetype": "B",
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Spring Boot app with JPA over a relational database",
        "familiarity_anchors": [
          "SQL",
          "databases",
          "queries"
        ],
        "hook": "Same verb, three blast radii: DELETE picks off rows, TRUNCATE empties the table, DROP deletes the table itself — and only DELETE undoes.",
        "beats": [
          {
            "layout": "paragraph",
            "content": "DELETE is DML — it removes specific rows using a WHERE clause and is transactional, meaning you can roll it back. It logs each deleted row, which makes it slower but recoverable. TRUNCATE is DDL — it removes all rows at once, is faster because it does not log individual rows, resets the auto-increment counter, and cannot be rolled back. DROP removes the entire table including its structure — both the data and the schema definition are gone."
          },
          {
            "layout": "mini_table",
            "content": {
              "headers": [
                "Command",
                "Category",
                "Rollback?",
                "WHERE clause?"
              ],
              "rows": [
                [
                  "DELETE",
                  "DML",
                  "Yes",
                  "Yes"
                ],
                [
                  "TRUNCATE",
                  "DDL",
                  "No",
                  "No"
                ],
                [
                  "DROP",
                  "DDL",
                  "No",
                  "No"
                ]
              ]
            }
          },
          {
            "layout": "paragraph",
            "content": "gotcha: in MySQL, TRUNCATE on a table referenced by foreign keys will fail. DELETE can still work in that case because it can handle constraint checks row by row. Also, TRUNCATE fires no DELETE triggers — if your application relies on triggers for audit logging, a TRUNCATE will silently bypass them."
          }
        ],
        "cap": "DELETE is surgical and reversible; TRUNCATE is fast and permanent; DROP removes everything including the schema.",
        "followup_handoff": [
          "Why is TRUNCATE faster than DELETE?",
          "What happens to foreign key constraints when you TRUNCATE a table?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "ddl-dml-dql-004",
      "slug": "dml-commands-in-sql-insert-update-delete",
      "question": "What are DML commands in SQL and how are they used?",
      "title": "DML Commands in SQL — INSERT, UPDATE, DELETE",
      "direct_answer": "**DML** (Data Manipulation Language) changes the data inside tables: **INSERT** adds rows, **UPDATE** modifies existing rows, **DELETE** removes rows. DML is **transactional** — you can wrap statements in a transaction and `COMMIT` or `ROLLBACK`. Always use a `WHERE` with UPDATE/DELETE to avoid changing every row.",
      "layout_type": "sql-playground",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-04",
      "interviewer_intent": {
        "testing": "Whether you know the DML commands and that they're transactional.",
        "common_mistake": "Running UPDATE/DELETE without WHERE and changing every row.",
        "to_stand_out": "Mention wrapping multi-statement DML in a transaction for atomicity."
      },
      "company_tags": [
        "tcs",
        "wipro",
        "cognizant",
        "flipkart"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "What DML Commands Do",
            "content": "**DML** — *Data Manipulation Language* — is how you change the **data** stored in tables. The three commands: **`INSERT`** adds new rows, **`UPDATE`** modifies values in existing rows, and **`DELETE`** removes rows.\n\nDML is **transactional**: a sequence of DML statements can be grouped and either committed together or rolled back as a unit, which is how you keep multi-step changes **atomic**. The single most important habit: **always include a `WHERE`** on `UPDATE` and `DELETE`, or you'll change/remove *every* row."
          },
          {
            "type": "concept_map",
            "title": "DML at a Glance",
            "content": "emerald|INSERT|~add rows|INSERT INTO ... VALUES|or INSERT ... SELECT|new data\nblue|UPDATE|~modify rows|SET col = value|needs WHERE!|changes existing data\nrose|DELETE|~remove rows|with WHERE|else wipes table|removes data\namber|Transactional|~atomic changes|BEGIN ... COMMIT|or ROLLBACK|all-or-nothing"
          },
          {
            "type": "code_example",
            "title": "INSERT, UPDATE, DELETE in a transaction",
            "content": "```sql\nBEGIN;\nINSERT INTO accounts (id, balance) VALUES (1, 1000);\nUPDATE accounts SET balance = balance - 100 WHERE id = 1;  -- WHERE!\nDELETE FROM accounts WHERE balance < 0;                    -- WHERE!\nCOMMIT;   -- all three changes become permanent together\n```\n\n*Wrapping the statements makes them atomic — ROLLBACK would undo all three.*"
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **UPDATE/DELETE without WHERE**: changes or deletes every row — a classic disaster.\n- **Forgetting to COMMIT**: changes aren't durable until committed (auto-commit varies by client).\n- **Partial multi-step changes**: not using a transaction can leave data half-updated.\n- **Mass updates without batching**: huge UPDATE/DELETE can lock rows and bloat logs."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **DML** = INSERT, UPDATE, DELETE — changes table data.\n- **Transactional**: COMMIT to keep, ROLLBACK to undo.\n- **Always use WHERE** with UPDATE/DELETE.\n- **Group multi-step changes** in a transaction for atomicity.\n- **Batch** very large modifications to avoid long locks."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "DML, data manipulation language, is how you change the actual data inside your tables, and it has three commands. INSERT adds new rows, either with explicit VALUES or by selecting from another query. UPDATE modifies values in existing rows using a SET clause. And DELETE removes rows. The most important habit with UPDATE and DELETE is to always include a WHERE clause, because if you forget it, UPDATE changes every row in the table and DELETE removes every row, which is one of the classic ways people cause a production disaster. The other defining property of DML is that it's transactional. You can wrap a sequence of DML statements in a transaction with BEGIN and then either COMMIT to make them all permanent or ROLLBACK to undo them all. This is what keeps multi-step changes atomic; for example, transferring money by subtracting from one account and adding to another should either both happen or neither, and a transaction guarantees that. A couple more practical points: changes aren't durable until they're committed, and client auto-commit settings vary, so I'm explicit; and for very large updates or deletes I batch the work to avoid holding long locks and bloating the transaction log."
          }
        ]
      },
      "followup_questions": [
        "Why must you use WHERE with UPDATE and DELETE?",
        "How do transactions make DML atomic?",
        "What is the difference between DML and DDL?"
      ],
      "seo": {
        "metaTitle": "DML Commands in SQL: INSERT UPDATE DELETE | Java Interview",
        "metaDescription": "Learn DML commands INSERT, UPDATE, DELETE with examples and transaction behavior. Java backend fresher interview preparation on SQL data manipulation."
      },
      "order": 4,
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Spring Boot app with JPA over a relational database",
        "familiarity_anchors": [
          "SQL",
          "databases",
          "queries"
        ],
        "hook": "DML changes your data — and the scariest two words in SQL are UPDATE and DELETE without a WHERE.",
        "beats": [
          {
            "layout": "paragraph",
            "content": "INSERT adds new rows to a table. UPDATE modifies existing rows. DELETE removes rows. All three are transactional — they participate in transactions and can be rolled back if something goes wrong. They are the opposite of DDL in that sense."
          },
          {
            "layout": "paragraph",
            "content": "production trap: UPDATE and DELETE without a WHERE clause affect every single row in the table. An UPDATE with no WHERE sets every row to the same value. A DELETE with no WHERE empties the table. In MySQL, running in safe mode prevents this, but it is not enabled everywhere. The habit to build is always writing your WHERE clause first, then the rest of the statement."
          },
          {
            "layout": "bullets",
            "content": [
              "INSERT can add multiple rows in one statement: INSERT INTO table VALUES (1), (2), (3)",
              "DML commands wrap naturally in Spring's @Transactional — a runtime exception triggers an automatic rollback"
            ]
          }
        ],
        "cap": "DML is transactional and rollback-able — always include WHERE in UPDATE and DELETE or you change every row.",
        "followup_handoff": [
          "What happens if you run UPDATE without a WHERE clause?",
          "What is the difference between DML and DDL regarding transactions?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "ddl-dml-dql-005",
      "slug": "tcl-in-sql-commit-rollback-savepoint",
      "question": "What is TCL in SQL and why is it important?",
      "title": "TCL in SQL — COMMIT, ROLLBACK, SAVEPOINT",
      "direct_answer": "**TCL** (Transaction Control Language) manages transactions: **COMMIT** makes changes permanent, **ROLLBACK** undoes them, and **SAVEPOINT** marks a point to partially roll back to. TCL gives DML its **atomicity** — a group of changes succeeds together or not at all.",
      "layout_type": "sql-playground",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-04",
      "interviewer_intent": {
        "testing": "Whether you know TCL commands and that they control DML transactions (ACID atomicity).",
        "common_mistake": "Forgetting that DDL auto-commits, so TCL can't undo a DROP/CREATE.",
        "to_stand_out": "Tie TCL to ACID and explain SAVEPOINT for partial rollback."
      },
      "company_tags": [
        "amazon",
        "microsoft",
        "infosys",
        "tcs"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "What TCL Is and Why It Matters",
            "content": "**TCL** — *Transaction Control Language* — controls **transactions**, the unit that makes a group of DML changes **atomic**. The commands: **`COMMIT`** makes all changes in the transaction permanent; **`ROLLBACK`** discards them; **`SAVEPOINT`** sets a named marker you can roll back to without undoing the whole transaction.\n\nTCL is what delivers the **A** in **ACID** (Atomicity): money transfers, multi-row updates, and order placement either complete fully or not at all. Note: **DDL auto-commits**, so TCL can't undo a `DROP` or `CREATE`."
          },
          {
            "type": "concept_map",
            "title": "TCL at a Glance",
            "content": "emerald|COMMIT|~make permanent|ends transaction|changes durable|\nrose|ROLLBACK|~undo changes|back to start|or to a SAVEPOINT|\nblue|SAVEPOINT|~partial marker|roll back part|keep earlier work|nested control\namber|Why|~ACID atomicity|all-or-nothing|protects multi-step DML|DDL auto-commits anyway"
          },
          {
            "type": "code_example",
            "title": "COMMIT, ROLLBACK, SAVEPOINT",
            "content": "```sql\nBEGIN;\nUPDATE accounts SET balance = balance - 100 WHERE id = 1;\nSAVEPOINT after_debit;                       -- marker\n\nUPDATE accounts SET balance = balance + 100 WHERE id = 2;\n-- something wrong with the credit? undo only the credit:\nROLLBACK TO after_debit;\n\nUPDATE accounts SET balance = balance + 100 WHERE id = 3;\nCOMMIT;   -- debit + credit-to-3 become permanent together\n```\n\n*`SAVEPOINT` enables partial rollback; `COMMIT` finalizes the whole transaction.*"
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Expecting to ROLLBACK DDL**: DDL auto-commits — you can't undo a DROP/ALTER with ROLLBACK.\n- **Forgetting to COMMIT**: work stays uncommitted (and may be lost on disconnect).\n- **Long-running transactions**: holding locks too long blocks other users.\n- **Misusing SAVEPOINT**: it's for partial undo within one transaction, not a substitute for COMMIT."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **TCL** = COMMIT, ROLLBACK, SAVEPOINT.\n- **COMMIT** makes changes permanent; **ROLLBACK** undoes them.\n- **SAVEPOINT** allows partial rollback within a transaction.\n- **TCL provides atomicity** — the A in ACID.\n- **DDL auto-commits**, so TCL can't undo it."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "TCL, transaction control language, is the set of commands that manage transactions, and a transaction is the unit that makes a group of changes atomic. There are three main commands. COMMIT makes all the changes in the current transaction permanent and ends the transaction. ROLLBACK discards the changes and returns the data to how it was at the start of the transaction. And SAVEPOINT sets a named marker inside a transaction, so you can roll back just to that point without throwing away everything you've done. TCL is what delivers the A in ACID, atomicity, which means a set of related changes either all succeed or all fail together. The textbook example is a money transfer: you debit one account and credit another, and you wrap both in a transaction so that if the credit fails, the debit is rolled back too and money is never lost. SAVEPOINT is useful for more granular control, letting you undo part of a transaction while keeping earlier work. The important caveat to mention is that DDL auto-commits, so TCL can't help you there; you cannot roll back a DROP or a CREATE with ROLLBACK. And practically, I keep transactions short, because holding locks for a long time blocks other users."
          }
        ]
      },
      "followup_questions": [
        "What is the difference between COMMIT and ROLLBACK?",
        "What is a SAVEPOINT used for?",
        "Why can't you roll back a DDL statement?"
      ],
      "seo": {
        "metaTitle": "TCL in SQL: COMMIT ROLLBACK SAVEPOINT | Java Fresher Interview",
        "metaDescription": "Learn TCL commands COMMIT, ROLLBACK, SAVEPOINT with examples. Understand SQL transaction control for Java backend fresher interviews."
      },
      "order": 5,
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Spring Boot app with JPA over a relational database",
        "familiarity_anchors": [
          "SQL",
          "databases",
          "queries"
        ],
        "hook": "TCL is the safety net under your data changes — COMMIT keeps them, ROLLBACK erases them, and SAVEPOINT lets you undo just part.",
        "beats": [
          {
            "layout": "paragraph",
            "content": "TCL stands for Transaction Control Language. COMMIT makes your DML changes permanent — once committed, the data is saved. ROLLBACK undoes all uncommitted changes since the last commit. SAVEPOINT lets you create a named checkpoint so you can rollback to a specific point without undoing everything from the start of the transaction."
          },
          {
            "layout": "paragraph",
            "content": "A practical example: imagine inserting a payment record and then updating a balance. You can set a SAVEPOINT after the insert. If the balance update fails validation, you rollback to the savepoint, keeping the insert but undoing the bad update, then handle the error before committing."
          },
          {
            "layout": "bullets",
            "content": [
              "TCL only works with DML — DDL auto-commits and ignores transaction boundaries",
              "Spring @Transactional handles COMMIT and ROLLBACK automatically based on whether a runtime exception is thrown"
            ]
          }
        ],
        "cap": "COMMIT saves, ROLLBACK undoes, SAVEPOINT marks a partial restore point — TCL only applies to DML, never to DDL.",
        "followup_handoff": [
          "Why can't you rollback a DDL command?",
          "How does Spring @Transactional relate to TCL?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "ddl-dml-dql-006",
      "slug": "why-ddl-statements-auto-commit-in-sql",
      "question": "Why do DDL statements auto-commit in most databases?",
      "title": "Why DDL Statements Auto-Commit in SQL",
      "direct_answer": "**DDL statements auto-commit** because they change the database's **structure (schema)**, which can't sit half-applied inside a transaction. Most databases implicitly commit any pending work before and after a DDL statement, so a `CREATE`/`ALTER`/`DROP` is permanent immediately and ends the current transaction.",
      "layout_type": "sql-playground",
      "difficulty": "medium",
      "importance": "medium",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-04",
      "interviewer_intent": {
        "testing": "Whether you understand why DDL auto-commits and its impact on open transactions.",
        "common_mistake": "Assuming a DDL inside a transaction can be rolled back with the DML around it.",
        "to_stand_out": "Note this is engine-dependent (some DBs support transactional DDL) and that DDL commits pending DML."
      },
      "company_tags": [
        "amazon",
        "microsoft",
        "flipkart",
        "tcs"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Why DDL Auto-Commits",
            "content": "**DDL** changes the database's **structure** — tables, columns, indexes. In most engines (MySQL, Oracle), a DDL statement triggers an **implicit COMMIT**: any pending transaction is committed *before* the DDL runs, the DDL itself is committed, and a new transaction begins after. So a `CREATE`, `ALTER`, or `DROP` is **permanent immediately** and you **can't roll it back**.\n\nThe reason: schema changes are hard to represent as reversible row-level operations, and other sessions need a consistent view of the structure. (Some engines, like PostgreSQL, *do* support transactional DDL — it's engine-dependent.)"
          },
          {
            "type": "concept_map",
            "title": "DDL Auto-Commit at a Glance",
            "content": "blue|What happens|~implicit COMMIT|commits pending DML first|then runs DDL|then commits DDL\nrose|Consequence|~no rollback|CREATE/ALTER/DROP permanent|ends your transaction|backup before risky DDL\namber|Why|~schema not row-level|hard to reverse cleanly|other sessions need consistency|\nemerald|Engine note|~not universal|MySQL/Oracle auto-commit|PostgreSQL transactional DDL|verify your DB"
          },
          {
            "type": "code_example",
            "title": "DDL commits the DML around it",
            "content": "```sql\nBEGIN;\nINSERT INTO logs VALUES (1, 'start');   -- pending DML\n\nCREATE TABLE temp_report (id INT);       -- DDL: implicit COMMIT here!\n-- the INSERT above is now COMMITTED and can no longer be rolled back\n\nROLLBACK;   -- too late for the INSERT; only affects work AFTER the DDL\n```\n\n*In MySQL/Oracle the DDL silently commits the earlier INSERT — a common surprise.*"
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Expecting rollback of DDL**: a wrong DROP/ALTER usually needs a restore.\n- **Mixing DDL into a DML transaction**: the DDL commits your pending DML unexpectedly.\n- **Assuming all engines behave alike**: PostgreSQL supports transactional DDL; MySQL/Oracle don't.\n- **No backup before risky schema changes**: there's no ROLLBACK to save you."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **DDL auto-commits** via an implicit COMMIT in most engines.\n- **It commits pending DML** before running.\n- **`CREATE`/`ALTER`/`DROP`** are permanent immediately.\n- **Can't be rolled back** in MySQL/Oracle.\n- **Engine-dependent**: PostgreSQL supports transactional DDL."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "DDL statements auto-commit because they change the database's structure, the schema, and a schema change can't sensibly sit half-applied inside a transaction. In most engines like MySQL and Oracle, running a DDL statement triggers an implicit commit: the database first commits any pending transaction you have open, then runs the DDL, then commits that too and starts a fresh transaction. The practical consequences are twofold. First, a CREATE, ALTER, or DROP is permanent the moment it runs and you can't roll it back, so an accidental DROP usually means restoring from a backup. Second, and this surprises a lot of people, a DDL statement will silently commit the DML you ran just before it. So if you insert some rows and then create a table in the same session, those inserts are now committed and a later ROLLBACK won't undo them. The underlying reason is that schema changes are hard to express as reversible row-level operations, and other sessions need a consistent view of the table structure, so the engine doesn't keep them pending. The one nuance worth mentioning is that this is engine-dependent: PostgreSQL actually supports transactional DDL, where you can roll back a CREATE or ALTER, whereas MySQL and Oracle do not. So I always check the database before assuming."
          }
        ]
      },
      "followup_questions": [
        "Does DDL commit pending DML statements?",
        "Which databases support transactional DDL?",
        "Can you roll back a CREATE TABLE in MySQL?"
      ],
      "seo": {
        "metaTitle": "Why DDL Auto-Commits in SQL | Java Backend Interview Questions",
        "metaDescription": "Understand why DDL statements auto-commit in MySQL and Oracle but not PostgreSQL. Java backend fresher interview prep on SQL transaction behavior."
      },
      "order": 6,
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Spring Boot app with JPA over a relational database",
        "familiarity_anchors": [
          "SQL",
          "databases",
          "queries"
        ],
        "hook": "Run a CREATE TABLE mid-transaction in MySQL and it quietly commits your earlier INSERTs too — DDL's implicit commit catches everyone once.",
        "beats": [
          {
            "layout": "paragraph",
            "content": "When you run a DDL statement, the database modifies its own schema metadata — the system catalog that describes what tables exist, what columns they have, and how everything is structured. Most databases, including MySQL and Oracle, treat these structural changes as immediately permanent. They issue an implicit commit before and after the DDL statement, which means any DML you had open in the same transaction also gets committed at that moment."
          },
          {
            "layout": "paragraph",
            "content": "non-obvious: PostgreSQL is the notable exception. It supports transactional DDL — you can run CREATE TABLE or DROP TABLE inside a transaction and roll the whole thing back. This makes schema migrations safer in PostgreSQL because a failed migration can roll back to the previous state cleanly."
          },
          {
            "layout": "bullets",
            "content": [
              "MySQL and Oracle — DDL auto-commits, cannot be rolled back",
              "PostgreSQL — DDL is transactional, can be rolled back",
              "Running DDL inside a transaction in MySQL commits your pending DML as a side effect"
            ]
          }
        ],
        "cap": "DDL auto-commits in MySQL to protect schema consistency — PostgreSQL is the exception where DDL can be rolled back.",
        "followup_handoff": [
          "Which databases support transactional DDL?",
          "What is the risk of running DDL in the middle of a transaction in MySQL?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    }
  ]
}

{
  "topic": "WHERE Clause in SQL",
  "topicSlug": "where-clause",
  "questions": [
    {
      "id": "where-clause-001",
      "slug": "where-clause-in-sql-filtering-rows",
      "question": "What is the WHERE clause in SQL and how does it filter rows?",
      "title": "WHERE Clause in SQL — Filtering Rows",
      "direct_answer": "The **WHERE** clause filters rows *before* grouping and selection, keeping only rows where the condition is **true** (not false, not unknown). It runs right after FROM/JOIN, supports `=, <>, <, >, BETWEEN, IN, LIKE, IS NULL`, and combines conditions with `AND`/`OR`.",
      "layout_type": "sql-playground",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-04",
      "interviewer_intent": {
        "testing": "Whether you know WHERE filters individual rows before GROUP BY and uses three-valued logic.",
        "common_mistake": "Putting aggregate conditions in WHERE, or expecting NULL rows to pass a normal comparison.",
        "to_stand_out": "Note WHERE filters rows while HAVING filters groups, and that sargable predicates use indexes."
      },
      "company_tags": [
        "tcs",
        "infosys",
        "wipro",
        "cognizant"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "How WHERE Filters Rows",
            "content": "The **WHERE** clause keeps only the rows whose condition evaluates to **true**. Rows where the condition is *false* or *unknown* (NULL comparisons) are dropped. It runs early — right after FROM and JOIN, **before** GROUP BY, HAVING, and SELECT — so it filters individual rows, never groups.\n\nYou build conditions with comparison operators (`=`, `<>`, `<`, `>`), range/set operators (`BETWEEN`, `IN`), pattern matching (`LIKE`), NULL tests (`IS NULL`), and combine them with `AND`/`OR`."
          },
          {
            "type": "concept_map",
            "title": "WHERE at a Glance",
            "content": "blue|Role|~filter rows early|runs after FROM/JOIN|before GROUP BY|row-level only\nemerald|Operators|~rich condition set|= <> < > BETWEEN IN|LIKE for patterns|IS NULL for missing\namber|Three-valued logic|~true/false/unknown|only TRUE rows kept|NULL compare = unknown|so NULL rows drop\nrose|vs HAVING|~rows vs groups|WHERE filters rows|HAVING filters groups|no aggregates in WHERE"
          },
          {
            "type": "code_example",
            "title": "WHERE with several operators",
            "content": "```sql\nSELECT name, city, age\nFROM customers\nWHERE age BETWEEN 18 AND 30\n  AND city IN ('Pune', 'Delhi')\n  AND phone IS NOT NULL;\n```\n\nSample output:\n\n| name | city | age |\n|---|---|---|\n| Neha | Delhi | 27 |\n| Karan | Pune | 22 |\n\n*Only adults 18–30, in Pune or Delhi, with a phone on file.*"
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Aggregates in WHERE**: `WHERE COUNT(*) > 5` fails — use HAVING.\n- **`= NULL`**: never matches; use `IS NULL`.\n- **Wrapping the column in a function**: `WHERE YEAR(created) = 2026` can't use an index — prefer a range like `created >= '2026-01-01'`.\n- **AND/OR precedence**: `a=1 OR a=2 AND b=3` binds AND first; use parentheses."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **WHERE filters rows** before GROUP BY/SELECT.\n- **Keeps only TRUE rows**; unknown (NULL) comparisons drop out.\n- **Use `IS NULL`** for missing values, never `=`.\n- **WHERE vs HAVING**: rows vs groups; no aggregates in WHERE.\n- **Keep predicates sargable** (no functions on the column) so indexes apply."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "The WHERE clause filters which rows make it into your result. It runs early in execution, right after FROM and any joins, and well before GROUP BY and SELECT, so it works on individual rows, not on groups. A row only survives if its condition is true; if the condition is false, or unknown because of a NULL comparison, the row is dropped. You build conditions with the usual operators: equals, not equals, less than, greater than, plus handy ones like BETWEEN for ranges, IN for a set of values, LIKE for pattern matching, and IS NULL for missing data. You combine them with AND and OR, and it's worth remembering AND binds tighter than OR, so use parentheses when mixing them. Two traps I keep in mind. First, you can't put aggregate conditions like COUNT greater than five in WHERE, because grouping hasn't happened yet; that belongs in HAVING. Second, for performance, avoid wrapping the column in a function, like YEAR of a date column, because that prevents the database from using an index. Instead I write a range condition on the raw column, which stays index-friendly."
          }
        ]
      },
      "followup_questions": [
        "What is the difference between WHERE and HAVING?",
        "Why does a function on a column prevent index usage?",
        "How does three-valued logic affect WHERE?"
      ],
      "seo": {
        "metaTitle": "WHERE Clause in SQL | Java Fresher Interview Questions",
        "metaDescription": "Learn how the SQL WHERE clause filters rows with comparison operators, BETWEEN, IN, LIKE, and IS NULL. Java backend fresher interview prep."
      },
      "order": 1,
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Spring Boot app with JPA over a relational database",
        "familiarity_anchors": [
          "SQL",
          "databases",
          "queries"
        ],
        "hook": "WHERE runs almost first and only lets TRUE rows through — which is exactly why aggregates and NULLs behave the way they do here.",
        "beats": [
          {
            "layout": "paragraph",
            "content": "WHERE filters rows from a table — only rows where the condition evaluates to TRUE make it into the result. It supports comparison operators like equals, not-equals, greater than, and less than, as well as BETWEEN for ranges, IN for checking a value against a list, LIKE for pattern matching, and IS NULL for null checks."
          },
          {
            "layout": "bullets",
            "content": [
              "AND and OR combine conditions — AND has higher precedence, so use parentheses to be explicit",
              "WHERE col equals NULL never works — always use IS NULL for null checks",
              "WHERE runs before SELECT, so column aliases defined in SELECT are not available here"
            ]
          },
          {
            "layout": "paragraph",
            "content": "non-obvious: WHERE executes before GROUP BY, which is why you cannot use aggregate functions like COUNT inside a WHERE clause. Aggregate conditions belong in HAVING, which runs after the grouping."
          }
        ],
        "cap": "WHERE filters individual rows before anything else runs — and IS NULL is the only correct way to check for missing values.",
        "followup_handoff": [
          "What is the difference between WHERE and HAVING?",
          "What is the precedence of AND vs OR in SQL?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "where-clause-002",
      "slug": "like-operator-in-sql-pattern-matching",
      "question": "How does the LIKE operator work in SQL?",
      "title": "LIKE Operator in SQL — Pattern Matching",
      "direct_answer": "**LIKE** does pattern matching with two wildcards: **`%`** matches any sequence of characters (including none) and **`_`** matches exactly one character. A leading wildcard (`'%term'`) prevents normal index use, making such searches slow on large tables.",
      "layout_type": "sql-playground",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-04",
      "interviewer_intent": {
        "testing": "Whether you know % vs _ and the leading-wildcard performance trap.",
        "common_mistake": "Using LIKE '%x%' on big tables and expecting it to use an index.",
        "to_stand_out": "Mention case sensitivity differences and full-text search as the scalable alternative."
      },
      "company_tags": [
        "tcs",
        "infosys",
        "wipro",
        "amazon"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "How LIKE Pattern Matching Works",
            "content": "**LIKE** matches text against a pattern using two wildcards: **`%`** stands for any run of characters (including zero), and **`_`** stands for exactly one character. `'A%'` finds anything starting with A; `'_at'` finds three-letter words ending in *at* (cat, bat).\n\nThe key performance fact: a pattern with a **leading wildcard** (`'%son'`) can't use a normal B-tree index, so the database scans every row. Prefix patterns (`'Ander%'`) can use an index. For heavy text search, use a **full-text index** instead."
          },
          {
            "type": "concept_map",
            "title": "LIKE at a Glance",
            "content": "blue|Wildcards|~two of them|% = any length|_ = exactly one char|combine freely\nemerald|Index-friendly|~prefix only|'abc%' uses index|leading % does not|so '%abc%' = full scan\namber|Case|~db-dependent|MySQL often case-insensitive|Postgres case-sensitive|use ILIKE/LOWER()\nrose|Scale|~LIKE doesn't scale|use full-text search|or a search engine|for big text columns"
          },
          {
            "type": "code_example",
            "title": "LIKE patterns and a sample result",
            "content": "```sql\nSELECT name FROM customers WHERE name LIKE 'A%';      -- starts with A\nSELECT name FROM customers WHERE name LIKE '%n';      -- ends with n\nSELECT code FROM products  WHERE code LIKE 'X_9';     -- X, any char, 9\n```\n\nSample output (first query):\n\n| name |\n|---|\n| Anita |\n| Arjun |\n\n*`A%` matches any name beginning with A and can use an index on `name`.*"
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Leading wildcard at scale**: `LIKE '%error%'` forces a full table scan — use full-text search for large text.\n- **Assuming case behaviour**: LIKE is case-sensitive in some databases, not others; normalize with `LOWER()` or `ILIKE` (Postgres).\n- **Forgetting to escape**: a literal `%` or `_` in data needs an `ESCAPE` clause.\n- **Using LIKE for exact match**: use `=` when there's no wildcard — it's clearer and faster."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **`%`** = any sequence; **`_`** = exactly one character.\n- **Prefix patterns** (`'abc%'`) can use an index.\n- **Leading wildcard** (`'%abc'`) forces a full scan.\n- **Case sensitivity** is database-specific (`ILIKE`, `LOWER()`).\n- **Full-text search** beats LIKE for large text columns."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "LIKE is SQL's pattern-matching operator, and it has two wildcards. The percent sign matches any sequence of characters, including none at all, and the underscore matches exactly one character. So name LIKE A percent finds every name starting with A, and code LIKE X underscore nine matches a three-character code that starts with X and ends with nine with any single character in the middle. The thing interviewers really want to hear is the performance angle. A pattern that starts with a literal prefix, like Ander percent, can use a normal index because the database knows where to start in the sorted index. But a pattern with a leading wildcard, like percent son, cannot use that index, so the database has to scan every single row. On a large table that's a real problem. So for big text columns and search-style features, I reach for a full-text index instead of LIKE, or a dedicated search engine. Two more things to watch: case sensitivity depends on the database, so I normalize with LOWER or use ILIKE in Postgres when I need case-insensitive matching, and if the data itself contains a percent or underscore I use an ESCAPE clause so it's treated literally."
          }
        ]
      },
      "followup_questions": [
        "Why does a leading wildcard prevent index usage?",
        "What is full-text search and when do you use it?",
        "Is LIKE case-sensitive in MySQL vs PostgreSQL?"
      ],
      "seo": {
        "metaTitle": "LIKE Operator in SQL Pattern Matching | Java Interview Prep",
        "metaDescription": "Learn SQL LIKE operator with % and _ wildcards and examples. Java backend fresher interview question on pattern matching in WHERE clauses."
      },
      "order": 2,
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Spring Boot app with JPA over a relational database",
        "familiarity_anchors": [
          "SQL",
          "databases",
          "queries"
        ],
        "hook": "LIKE 'A%' flies on an index, but LIKE '%A' scans your whole table — the wildcard's position decides everything.",
        "beats": [
          {
            "layout": "paragraph",
            "content": "LIKE does pattern matching in a WHERE clause. The percent sign matches zero or more characters, and the underscore matches exactly one character. LIKE 'J%' matches anything starting with J. LIKE '%@gmail.com' matches any email ending with that domain. LIKE '_a%' means any name whose second character is the letter a."
          },
          {
            "layout": "bullets",
            "content": [
              "NOT LIKE excludes all rows that match the pattern",
              "In MySQL, LIKE is case-insensitive by default; PostgreSQL is case-sensitive",
              "For complex patterns, MySQL supports REGEXP as a more powerful alternative"
            ]
          },
          {
            "layout": "paragraph",
            "content": "production trap: a leading percent like LIKE percent-java-percent forces a full table scan because the database cannot use a B-tree index when it does not know what the string starts with. LIKE 'java%' with the percent only at the end can still use the index as a prefix search."
          }
        ],
        "cap": "Percent for any sequence, underscore for one character — and never lead with percent if you care about index performance.",
        "followup_handoff": [
          "Why does LIKE '%text' cause a full table scan?",
          "How is LIKE different from REGEXP in MySQL?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "where-clause-003",
      "slug": "between-operator-in-sql",
      "question": "What is the BETWEEN operator in SQL and when do you use it?",
      "title": "BETWEEN Operator in SQL",
      "direct_answer": "**BETWEEN x AND y** is shorthand for `>= x AND <= y` — it is **inclusive** on both ends. It works for numbers, dates, and text, and is most useful for range filters. For dates with a time component, watch the inclusive upper bound.",
      "layout_type": "sql-playground",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 2,
      "last_updated": "2026-06-04",
      "interviewer_intent": {
        "testing": "Whether you know BETWEEN is inclusive on both bounds.",
        "common_mistake": "Assuming BETWEEN is exclusive, or using it on datetimes and missing same-day rows.",
        "to_stand_out": "Show the half-open date pattern (>= start AND < nextDay) to avoid timestamp edge cases."
      },
      "company_tags": [
        "tcs",
        "wipro",
        "infosys",
        "cognizant"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "How BETWEEN Works",
            "content": "**`BETWEEN x AND y`** filters values in a range and is **inclusive** of both endpoints — it's exactly `>= x AND <= y`. It reads cleanly for numbers (`price BETWEEN 100 AND 500`), dates (`order_date BETWEEN '2026-01-01' AND '2026-01-31'`), and even text.\n\nThe gotcha is datetimes: `BETWEEN '2026-01-01' AND '2026-01-31'` excludes rows timestamped later on Jan 31, because midnight is the implied time. The safe pattern is a **half-open range**: `>= '2026-01-01' AND < '2026-02-01'`."
          },
          {
            "type": "concept_map",
            "title": "BETWEEN at a Glance",
            "content": "blue|Meaning|~inclusive range|= >= x AND <= y|both ends included|reads clearly\nemerald|Works on|~many types|numbers|dates|text\namber|Datetime trap|~midnight cutoff|end date excludes later times|use < nextDay|half-open range\nrose|NOT BETWEEN|~outside the range|excludes the bounds too|good for exclusions|combine with AND/OR"
          },
          {
            "type": "code_example",
            "title": "BETWEEN vs the safe date range",
            "content": "```sql\n-- inclusive numeric range\nSELECT name, price FROM products WHERE price BETWEEN 100 AND 500;\n\n-- date-only column: BETWEEN is fine and inclusive\nSELECT id FROM orders WHERE order_date BETWEEN '2026-01-01' AND '2026-01-31';\n\n-- datetime column: use a half-open range to catch all of Jan 31\nSELECT id FROM orders WHERE created_at >= '2026-01-01' AND created_at < '2026-02-01';\n```\n\nSample output (first query):\n\n| name | price |\n|---|---|\n| Cable | 199 |\n| Stand | 499 |"
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Thinking BETWEEN is exclusive**: it includes both endpoints.\n- **BETWEEN on datetimes**: the upper bound at midnight silently drops same-day afternoon rows — use `< nextDay`.\n- **Reversed bounds**: `BETWEEN 500 AND 100` returns nothing; the low value must come first.\n- **NOT BETWEEN with NULL**: NULL values never satisfy BETWEEN or NOT BETWEEN."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **BETWEEN is inclusive** on both ends (`>= x AND <= y`).\n- **Low bound first**, or the range matches nothing.\n- **Datetime trap**: prefer half-open `>= start AND < nextDay`.\n- **Works** on numbers, dates, and text.\n- **NOT BETWEEN** excludes the range, NULLs never match either way."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "BETWEEN is a readable way to write a range filter, and the most important thing to know is that it's inclusive on both ends. So price BETWEEN one hundred and five hundred is exactly the same as price greater than or equal to one hundred and less than or equal to five hundred; both the one hundred and the five hundred are included. It works on numbers, dates, and text. The classic trap is datetimes. If I write order date BETWEEN January first and January thirty-first on a column that stores a full timestamp, the upper bound is treated as midnight at the start of the thirty-first, so any order placed later that day gets silently excluded. To avoid that I use a half-open range instead: created at greater than or equal to January first AND created at less than February first. That cleanly captures everything in January regardless of time. A couple of smaller points: the low value has to come first, because BETWEEN five hundred and one hundred matches nothing, and NULL values never satisfy BETWEEN or NOT BETWEEN, because any comparison with NULL is unknown."
          }
        ]
      },
      "followup_questions": [
        "Why is BETWEEN risky with datetime columns?",
        "How do you write an inclusive vs exclusive range?",
        "Does BETWEEN use an index?"
      ],
      "seo": {
        "metaTitle": "BETWEEN Operator in SQL | Java Fresher Interview Questions",
        "metaDescription": "Understand the SQL BETWEEN operator for range filtering with numbers and dates. Java backend fresher interview question on WHERE clause operators."
      },
      "order": 3,
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Spring Boot app with JPA over a relational database",
        "familiarity_anchors": [
          "SQL",
          "databases",
          "queries"
        ],
        "hook": "BETWEEN includes both ends — which quietly bites you on timestamp columns where the last day's afternoon rows vanish.",
        "beats": [
          {
            "layout": "paragraph",
            "content": "BETWEEN is shorthand for a range filter. WHERE salary BETWEEN 50000 AND 80000 means salary is greater than or equal to 50000 and less than or equal to 80000. Both endpoints are included — that is the part most people need confirming. NOT BETWEEN excludes the range instead."
          },
          {
            "layout": "bullets",
            "content": [
              "Works with numbers, dates, and strings — not just numeric columns",
              "For strings, BETWEEN uses alphabetical ordering"
            ]
          },
          {
            "layout": "paragraph",
            "content": "gotcha: when filtering timestamp columns with BETWEEN on date-only values, records from the last date after midnight can be missed. If the column stores time components, be explicit about the upper bound — for example, use less than the next day rather than BETWEEN on the date string."
          }
        ],
        "cap": "BETWEEN is inclusive on both ends and works on dates and strings too — watch out for timestamp precision at the upper bound.",
        "followup_handoff": [
          "Is BETWEEN inclusive or exclusive?",
          "How would you filter rows outside a range?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "where-clause-004",
      "slug": "in-operator-in-sql-vs-multiple-or-conditions",
      "question": "What is the IN operator in SQL and how is it different from OR?",
      "title": "IN Operator in SQL vs Multiple OR Conditions",
      "direct_answer": "**IN (a, b, c)** tests whether a value matches any item in a list — a concise replacement for a chain of `OR` equalities. It's cleaner and the optimizer treats it as a set lookup. Caution: `NOT IN` with a NULL in the list returns no rows.",
      "layout_type": "sql-playground",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-04",
      "interviewer_intent": {
        "testing": "Whether you know IN equals an OR chain and the NOT IN + NULL pitfall.",
        "common_mistake": "Using NOT IN against a subquery that can return NULL, silently getting zero rows.",
        "to_stand_out": "Prefer NOT EXISTS over NOT IN when the subquery may yield NULLs."
      },
      "company_tags": [
        "amazon",
        "microsoft",
        "tcs",
        "flipkart"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "How IN Works vs OR",
            "content": "**`IN`** checks membership in a list: `city IN ('Pune','Delhi')` is shorthand for `city = 'Pune' OR city = 'Delhi'`. It's easier to read and the optimizer can treat it as a set lookup. `IN` can also take a **subquery**: `id IN (SELECT customer_id FROM orders)`.\n\nThe dangerous edge is **`NOT IN` with NULL**: if the list (or subquery) contains a NULL, `NOT IN` evaluates to unknown for every row and returns **nothing**. Use **`NOT EXISTS`** when NULLs are possible."
          },
          {
            "type": "concept_map",
            "title": "IN at a Glance",
            "content": "blue|Meaning|~membership test|IN (a,b,c)|= a OR = b OR = c|cleaner than OR chain\nemerald|Subquery form|~dynamic list|IN (SELECT ...)|good for set filters|optimizer-friendly\nrose|NOT IN + NULL|~silent zero rows|one NULL breaks it|all rows become unknown|use NOT EXISTS instead\namber|vs OR|~readability|fewer typos|same result for equality|index still usable"
          },
          {
            "type": "code_example",
            "title": "IN vs OR, and the NOT IN trap",
            "content": "```sql\n-- IN list (equivalent to the OR chain below it)\nSELECT name FROM customers WHERE city IN ('Pune', 'Delhi', 'Mumbai');\n-- same as: WHERE city = 'Pune' OR city = 'Delhi' OR city = 'Mumbai'\n\n-- IN with a subquery\nSELECT name FROM customers\nWHERE id IN (SELECT customer_id FROM orders WHERE total > 1000);\n```\n\nSample output (first query):\n\n| name |\n|---|\n| Neha |\n| Ravi |\n\n*Note: if the subquery could return NULL, prefer `EXISTS`/`NOT EXISTS` over `IN`/`NOT IN`.*"
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **`NOT IN` with NULLs**: a single NULL in the list returns zero rows — use `NOT EXISTS`.\n- **Huge IN lists**: thousands of literals can be slow; use a join or temp table.\n- **IN vs EXISTS confusion**: `EXISTS` often wins when the subquery is correlated and large.\n- **Type mismatch**: mixing strings and numbers in the list can cause implicit casts and skip the index."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **`IN`** = OR chain of equality checks, but cleaner.\n- **Accepts a subquery** for dynamic membership.\n- **`NOT IN` + NULL** silently returns no rows.\n- **Prefer `NOT EXISTS`** when NULLs are possible.\n- **Very large lists** are better expressed as a join."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "IN tests whether a value is in a list, and it's really just a cleaner way to write a chain of OR equalities. So city IN Pune, Delhi, Mumbai is exactly the same as city equals Pune OR city equals Delhi OR city equals Mumbai. It's more readable, less error-prone, and the optimizer can treat it as a set lookup. IN can also take a subquery, like id IN select customer_id from orders, which is handy for filtering by membership in another table. Now the part that catches people: NOT IN combined with NULL. If the list or the subquery contains even one NULL value, NOT IN evaluates to unknown for every row and the query returns zero rows, which looks like a bug but is actually defined behaviour from three-valued logic. So whenever the subquery could produce NULLs, I use NOT EXISTS instead, which handles NULLs correctly. A couple of smaller tips: extremely large IN lists with thousands of literals can be slow, so a join or a temp table is usually better, and I keep the list values the same type as the column so the database can still use an index instead of doing implicit casts."
          }
        ]
      },
      "followup_questions": [
        "What is the difference between IN and EXISTS?",
        "Why does NOT IN fail with NULL values?",
        "When is a JOIN better than an IN subquery?"
      ],
      "seo": {
        "metaTitle": "IN Operator in SQL | Java Backend Fresher Interview",
        "metaDescription": "Learn SQL IN operator with value lists and subqueries vs multiple OR conditions. Java fresher interview question on WHERE clause filtering."
      },
      "order": 4,
      "speakable_v2": {
        "archetype": "B",
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Spring Boot app with JPA over a relational database",
        "familiarity_anchors": [
          "SQL",
          "databases",
          "queries"
        ],
        "hook": "IN is just a tidy OR chain — until you write NOT IN against a NULL and your query mysteriously returns nothing.",
        "beats": [
          {
            "layout": "paragraph",
            "content": "IN checks whether a column value matches any item in a list. WHERE department IN IT, HR, Finance is equivalent to three OR conditions joined together — just cleaner to read and easier to extend. IN also works with a subquery, letting you filter rows dynamically based on another query's results."
          },
          {
            "layout": "mini_table",
            "content": {
              "headers": [
                "Feature",
                "IN",
                "Multiple OR"
              ],
              "rows": [
                [
                  "Readability",
                  "Clean for many values",
                  "Gets messy fast"
                ],
                [
                  "Subquery support",
                  "Yes — IN (SELECT ...)",
                  "No"
                ],
                [
                  "NULL handling",
                  "Tricky with NOT IN",
                  "Straightforward"
                ]
              ]
            }
          },
          {
            "layout": "paragraph",
            "content": "sharp edge: NOT IN with a subquery breaks silently if the subquery returns any NULL. Because NULL compared to anything is UNKNOWN, the NOT IN condition evaluates to UNKNOWN for every row and returns no results at all. Use NOT EXISTS instead when the subquery might include nulls."
          }
        ],
        "cap": "Use IN for clean list matching and subqueries — but switch to NOT EXISTS when nulls might appear in the comparison set.",
        "followup_handoff": [
          "What happens with NOT IN when the list contains NULL?",
          "When would you use EXISTS instead of IN?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "where-clause-005",
      "slug": "and-vs-or-operator-precedence-in-sql-where",
      "question": "What is the difference between AND and OR in SQL WHERE clause?",
      "title": "AND vs OR Operator Precedence in SQL WHERE",
      "direct_answer": "**AND** requires *all* conditions to be true; **OR** requires *at least one*. AND has **higher precedence** than OR, so `a OR b AND c` is parsed as `a OR (b AND c)`. Use parentheses to make combined logic explicit.",
      "layout_type": "sql-playground",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-04",
      "interviewer_intent": {
        "testing": "Whether you understand AND/OR precedence and how to parenthesize mixed logic.",
        "common_mistake": "Mixing AND and OR without parentheses and getting unexpected rows.",
        "to_stand_out": "Mention three-valued logic: AND/OR with NULL can yield unknown."
      },
      "company_tags": [
        "tcs",
        "infosys",
        "amazon",
        "wipro"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "AND vs OR and Precedence",
            "content": "**`AND`** keeps a row only when *every* joined condition is true. **`OR`** keeps a row when *any* condition is true. The subtlety is **precedence**: AND binds tighter than OR, so `status='A' OR status='B' AND city='Pune'` actually means `status='A' OR (status='B' AND city='Pune')` — probably not what you intended.\n\nAlways wrap OR groups in parentheses to control evaluation. With NULLs, remember the third truth value: `TRUE OR NULL` is true, but `FALSE OR NULL` is unknown."
          },
          {
            "type": "concept_map",
            "title": "AND / OR at a Glance",
            "content": "blue|AND|~all must be true|narrows results|higher precedence|evaluated first\nemerald|OR|~at least one true|widens results|lower precedence|wrap in parentheses\namber|Precedence|~AND before OR|a OR b AND c|= a OR (b AND c)|use ( ) to be explicit\nrose|With NULL|~three-valued|TRUE OR NULL = true|FALSE AND NULL = false|else unknown"
          },
          {
            "type": "before_code",
            "title": "Ambiguous: no parentheses",
            "content": "```sql\n-- intent: VIP or premium customers, but only in Pune\nSELECT name FROM customers\nWHERE tier = 'VIP' OR tier = 'PREMIUM' AND city = 'Pune';\n-- parsed as: tier='VIP' OR (tier='PREMIUM' AND city='Pune')\n-- BUG: every VIP from ANY city is returned\n```"
          },
          {
            "type": "after_code",
            "title": "Explicit: parentheses fix it",
            "content": "```sql\nSELECT name FROM customers\nWHERE (tier = 'VIP' OR tier = 'PREMIUM') AND city = 'Pune';\n-- now: must be in Pune AND (VIP or PREMIUM)\n```\n\n*Parentheses make the OR group bind before the AND — the result now matches the intent.*"
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **No parentheses around OR**: AND runs first, so you get rows you didn't intend.\n- **Assuming left-to-right**: precedence, not order, decides grouping.\n- **Forgetting NULL logic**: a condition on a NULL column may be unknown, dropping the row.\n- **Over-nesting**: deeply nested AND/OR is hard to read — sometimes `IN` or a CASE is clearer."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **AND** = all true; **OR** = at least one true.\n- **AND has higher precedence** than OR.\n- **`a OR b AND c`** means `a OR (b AND c)`.\n- **Parenthesize OR groups** to encode real intent.\n- **NULL** can make AND/OR yield unknown (three-valued logic)."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "AND and OR combine conditions, but they behave differently and the precedence between them trips people up. AND requires every condition to be true, so it narrows your results, while OR only needs one condition to be true, so it widens them. The crucial rule is that AND has higher precedence than OR, which means AND is evaluated first. So if I write tier equals VIP OR tier equals premium AND city equals Pune, SQL actually reads it as tier equals VIP OR, in parentheses, tier equals premium and city equals Pune. The effect is that every VIP from any city slips through, which is almost never what you meant. The fix is parentheses: wrap the OR group so it becomes, in parentheses tier equals VIP or premium, AND city equals Pune. Now the city filter applies to both tiers. My habit is to always parenthesize whenever I mix AND and OR, even when precedence would technically give the right answer, because it makes intent obvious to the next reader. And one more subtlety from three-valued logic: combining with a NULL can produce unknown, so true OR NULL is true, but false OR NULL is unknown, which can quietly drop rows."
          }
        ]
      },
      "followup_questions": [
        "What is the operator precedence in a SQL WHERE clause?",
        "How do NULLs affect AND and OR?",
        "When is IN cleaner than a chain of ORs?"
      ],
      "seo": {
        "metaTitle": "AND vs OR Operator Precedence in SQL | Java Interview Questions",
        "metaDescription": "Understand AND and OR operator precedence in SQL WHERE clauses with examples. Java backend fresher interview question on logical operators."
      },
      "order": 5,
      "speakable_v2": {
        "archetype": "B",
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Spring Boot app with JPA over a relational database",
        "familiarity_anchors": [
          "SQL",
          "databases",
          "queries"
        ],
        "hook": "`a OR b AND c` doesn't mean what it looks like — AND silently runs first, so a missing pair of parentheses changes your whole result set.",
        "beats": [
          {
            "layout": "paragraph",
            "content": "AND requires all conditions to be true — it makes your filter more restrictive. OR requires at least one condition to be true — it broadens the filter. The trap is operator precedence: AND is evaluated before OR, just like multiplication before addition in math."
          },
          {
            "layout": "paragraph",
            "content": "candidates miss: this query looks like it filters both departments with the same salary threshold — but it does not. WHERE department equals HR OR department equals IT AND salary is greater than 60000. SQL reads this as HR, or any IT employee with salary over 60000. To get HR and IT employees both filtered by salary, you must wrap the OR in parentheses: WHERE open-paren department equals HR OR department equals IT close-paren AND salary greater than 60000."
          },
          {
            "layout": "bullets",
            "content": [
              "Precedence order from highest to lowest: NOT, then AND, then OR",
              "Always use parentheses when mixing AND and OR — it makes intent explicit and prevents bugs"
            ]
          }
        ],
        "cap": "AND binds tighter than OR — when you mix them, always add parentheses to make your intent clear.",
        "followup_handoff": [
          "What is the operator precedence for NOT, AND, and OR?",
          "How would you get employees from IT or HR with salary above 60000?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "where-clause-006",
      "slug": "where-vs-having-in-sql",
      "question": "What is the difference between WHERE and HAVING in SQL?",
      "title": "WHERE vs HAVING in SQL",
      "direct_answer": "**WHERE** filters **individual rows** before grouping; **HAVING** filters **groups** after `GROUP BY`. WHERE can't use aggregates (groups don't exist yet); HAVING can. Filtering in WHERE first is faster because fewer rows reach the grouping step.",
      "layout_type": "comparison-arena",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-04",
      "interviewer_intent": {
        "testing": "Whether you know WHERE filters rows pre-grouping and HAVING filters groups post-grouping.",
        "common_mistake": "Putting COUNT()/SUM() in WHERE, or doing row filtering in HAVING and hurting performance.",
        "to_stand_out": "Explain filter-early: WHERE shrinks input to GROUP BY, so prefer it for non-aggregate conditions."
      },
      "company_tags": [
        "amazon",
        "microsoft",
        "tcs",
        "infosys"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "WHERE vs HAVING",
            "content": "Both filter, but at different stages. **`WHERE`** runs *before* `GROUP BY`, so it filters **individual rows** and cannot reference aggregates. **`HAVING`** runs *after* `GROUP BY`, so it filters **groups** and is the only place an aggregate condition like `COUNT(*) > 5` is legal.\n\nFor performance, push every non-aggregate condition into WHERE: it shrinks the data *before* grouping, so the database groups fewer rows. Use HAVING only for conditions on aggregated values."
          },
          {
            "type": "concept_map",
            "title": "WHERE vs HAVING at a Glance",
            "content": "blue|WHERE|~filters rows|runs before GROUP BY|no aggregates allowed|index-friendly\nemerald|HAVING|~filters groups|runs after GROUP BY|aggregates allowed|works on grouped data\namber|Performance|~filter early|WHERE shrinks input|fewer rows to group|prefer WHERE\nrose|Rule|~choose by intent|row condition -> WHERE|group condition -> HAVING|if it fits WHERE, use it"
          },
          {
            "type": "comparison_table",
            "title": "WHERE vs HAVING",
            "content": "| Aspect | WHERE | HAVING |\n|---|---|---|\n| Runs | Before GROUP BY | After GROUP BY |\n| Filters | Individual rows | Groups |\n| Aggregates? | Not allowed | Allowed (`COUNT`, `SUM`...) |\n| Performance | Filters early, faster | Filters late, on grouped data |\n| Typical use | `age > 18` | `COUNT(*) > 5` |"
          },
          {
            "type": "before_code",
            "title": "Wrong: aggregate in WHERE",
            "content": "```sql\n-- ERROR: aggregates don't exist during WHERE\nSELECT department, COUNT(*) AS headcount\nFROM employees\nWHERE COUNT(*) > 5\nGROUP BY department;\n```"
          },
          {
            "type": "after_code",
            "title": "Right: row filter in WHERE, group filter in HAVING",
            "content": "```sql\nSELECT department, COUNT(*) AS headcount\nFROM employees\nWHERE active = true            -- row filter, runs first (fast)\nGROUP BY department\nHAVING COUNT(*) > 5;           -- group filter, runs after grouping\n```\n\nSample output:\n\n| department | headcount |\n|---|---|\n| Tech | 12 |\n| Sales | 7 |"
          },
          {
            "type": "when_to_use",
            "title": "When to Use Which",
            "content": "Use **WHERE** for any condition on a raw column value — dates, status, IDs — because it filters before grouping and is index-friendly.\n\nUse **HAVING** only when the condition involves an aggregate of a group, like *departments with more than 5 employees* or *customers whose total spend exceeds 10,000*. If a condition could go in either, put it in WHERE."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **WHERE** filters rows before GROUP BY; **HAVING** filters groups after.\n- **Aggregates** are legal only in HAVING.\n- **Filter early** in WHERE for performance.\n- **HAVING** is for conditions on `COUNT/SUM/AVG` per group.\n- **If it can go in WHERE, put it in WHERE.**"
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "WHERE and HAVING both filter, but they act at different stages of query execution, and that's the whole answer. WHERE runs before GROUP BY, so it filters individual rows, and because grouping hasn't happened yet, you cannot use aggregate functions in WHERE. HAVING runs after GROUP BY, so it filters whole groups, and it's the only place a condition like COUNT greater than five is legal. A simple example: to find departments with more than five active employees, I filter the rows first with WHERE active equals true, then group by department, then filter the groups with HAVING COUNT greater than five. The performance angle is what makes you stand out. Always push non-aggregate conditions into WHERE, because WHERE shrinks the data before grouping, so the database has fewer rows to group, which is faster. If you instead did that filtering in HAVING, the database would group everything first and then throw groups away, doing more work. So my rule of thumb is: if a condition can be expressed in WHERE, it belongs in WHERE; reserve HAVING strictly for conditions on aggregated values like counts, sums, and averages per group."
          }
        ]
      },
      "followup_questions": [
        "Why can't you use COUNT() in a WHERE clause?",
        "Which is faster for non-aggregate filters, WHERE or HAVING?",
        "Can a query use both WHERE and HAVING together?"
      ],
      "seo": {
        "metaTitle": "WHERE vs HAVING in SQL | Java Backend Fresher Interview",
        "metaDescription": "Understand the difference between WHERE and HAVING in SQL with examples. Key Java fresher interview question on filtering rows vs groups."
      },
      "order": 6,
      "speakable_v2": {
        "archetype": "B",
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Spring Boot app with JPA over a relational database",
        "familiarity_anchors": [
          "SQL",
          "databases",
          "queries"
        ],
        "hook": "WHERE filters rows, HAVING filters groups — and putting the right filter in the right place is both a correctness and a speed decision.",
        "beats": [
          {
            "layout": "paragraph",
            "content": "WHERE filters individual rows before any grouping happens. HAVING filters groups after GROUP BY is applied. The key consequence: WHERE cannot use aggregate functions like COUNT or SUM because those haven't been calculated yet. HAVING can, because it runs after aggregation."
          },
          {
            "layout": "mini_table",
            "content": {
              "headers": [
                "Feature",
                "WHERE",
                "HAVING"
              ],
              "rows": [
                [
                  "Runs",
                  "Before GROUP BY",
                  "After GROUP BY"
                ],
                [
                  "Filters",
                  "Individual rows",
                  "Groups"
                ],
                [
                  "Aggregates allowed",
                  "No",
                  "Yes"
                ]
              ]
            }
          },
          {
            "layout": "paragraph",
            "content": "A practical example: to find departments where all salaries are above 30000 and the department has at least 5 employees, put the salary condition in WHERE to cut rows early, then use HAVING COUNT star greater than or equal to 5 to filter the resulting groups. WHERE is also more efficient — it reduces the data set before the expensive aggregation step."
          }
        ],
        "cap": "WHERE filters rows before grouping, HAVING filters groups after — put aggregate conditions in HAVING, not WHERE.",
        "followup_handoff": [
          "Can you use HAVING without GROUP BY?",
          "Why is WHERE more efficient than HAVING for row-level filtering?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    }
  ]
}

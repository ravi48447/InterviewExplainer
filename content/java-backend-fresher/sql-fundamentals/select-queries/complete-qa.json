{
  "topic": "SELECT Queries in SQL",
  "topicSlug": "select-queries",
  "questions": [
    {
      "id": "select-queries-001",
      "slug": "basic-select-statement-syntax-in-sql",
      "question": "What is the basic syntax of a SELECT statement in SQL?",
      "title": "Basic SELECT Statement Syntax in SQL",
      "direct_answer": "**SELECT** retrieves rows from one or more tables. The written form is `SELECT columns FROM table WHERE condition GROUP BY ... HAVING ... ORDER BY ... LIMIT n`, but SQL's **logical execution order** is FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT. Prefer naming explicit columns over `SELECT *` in real code.",
      "layout_type": "sql-playground",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-04",
      "interviewer_intent": {
        "testing": "Whether you know SELECT clause order and that logical execution order differs from written order.",
        "common_mistake": "Defaulting to SELECT * everywhere, and assuming clauses run in the order they are typed.",
        "to_stand_out": "Explain why a SELECT alias works in ORDER BY but not in WHERE — because SELECT runs after WHERE."
      },
      "company_tags": [
        "tcs",
        "infosys",
        "wipro",
        "cognizant"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Understanding the SELECT Statement",
            "content": "The **SELECT statement** is SQL's primary read operation — the *R* in CRUD. You name the **columns** you want, the **table** (`FROM`), an optional **`WHERE`** filter, **`ORDER BY`** to sort, and **`LIMIT`** to cap rows.\n\nThe trap most freshers miss: the order you *write* clauses is not the order the database *runs* them. SQL's **logical execution order** is `FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT`. That single fact explains why a column alias created in `SELECT` is usable in `ORDER BY` (which runs later) but not in `WHERE` (which runs earlier)."
          },
          {
            "type": "concept_map",
            "title": "SELECT at a Glance",
            "content": "blue|SELECT|~which columns to return|name columns, avoid *|aliases via AS|runs after WHERE\nemerald|FROM / WHERE|~source + row filter|FROM picks the table|WHERE filters rows|runs before SELECT\namber|ORDER BY / LIMIT|~sort + cap|ASC default, DESC reverse|LIMIT caps rows|needs ORDER BY to be stable\nviolet|Execution order|~logical, not written|FROM→WHERE→SELECT|then ORDER BY→LIMIT|alias works in ORDER BY only"
          },
          {
            "type": "code_example",
            "title": "SELECT with WHERE, ORDER BY, LIMIT",
            "content": "```sql\n-- customers table\nSELECT name, city, age\nFROM customers\nWHERE age >= 18\nORDER BY age DESC\nLIMIT 3;\n```\n\nSample output:\n\n| name | city | age |\n|---|---|---|\n| Anita | Pune | 41 |\n| Ravi | Mumbai | 33 |\n| Neha | Delhi | 27 |\n\n*Only the named columns come back, filtered to adults, sorted oldest first, capped at three rows.*"
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **`SELECT *` in production**: pulls every column even when you need two — wastes network/memory and breaks when the schema changes. Name the columns.\n- **`LIMIT` without `ORDER BY`**: row order is undefined, so the same `LIMIT 10` can return different rows on each run.\n- **Using a SELECT alias in WHERE**: `SELECT price*qty AS total ... WHERE total > 100` fails — WHERE runs before SELECT computes `total`.\n- **Assuming written order = run order**: it does not; FROM is always first."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Shape**: `SELECT cols FROM t WHERE ... ORDER BY ... LIMIT n`.\n- **Logical order**: FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT.\n- **Name columns** instead of `*` to cut data transfer and avoid surprises.\n- **`ORDER BY` before `LIMIT`** for deterministic, repeatable results.\n- **Aliases** (`AS`) are visible in ORDER BY, never in WHERE."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "The SELECT statement is the core read command in SQL. You name the columns you want, the table after FROM, a WHERE clause to filter rows, ORDER BY to sort, and LIMIT to cap how many rows come back. So to get the names and cities of adult customers, oldest first, top three, I'd write SELECT name, city, age FROM customers WHERE age greater than or equal to eighteen ORDER BY age descending LIMIT three. The thing that tripped me up early is that SQL doesn't run clauses in the order you type them. The logical execution order is FROM first to pick the table, then WHERE to filter, then GROUP BY and HAVING, then SELECT to compute the output columns, then ORDER BY, and finally LIMIT. That explains a classic confusion: you can use a column alias from SELECT inside ORDER BY because ORDER BY runs after SELECT, but you cannot use that alias in WHERE because WHERE runs first. Two habits I keep: name the exact columns instead of SELECT star so I'm not shipping data nobody asked for, and always pair LIMIT with ORDER BY, otherwise the database is free to return rows in any order and pagination becomes inconsistent."
          }
        ]
      },
      "followup_questions": [
        "What is the difference between WHERE and HAVING?",
        "How do you implement pagination in SQL?",
        "Why should you avoid SELECT * in production?"
      ],
      "seo": {
        "metaTitle": "Basic SELECT Statement Syntax in SQL | Java Interview Prep",
        "metaDescription": "Learn SELECT statement syntax with WHERE, ORDER BY, LIMIT examples. Java backend fresher interview preparation on SQL query fundamentals."
      },
      "order": 1,
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Spring Boot app with JPA over a relational database",
        "familiarity_anchors": [
          "SQL",
          "databases",
          "queries"
        ],
        "hook": "Everyone writes SELECT first — but the database runs it almost last, and that one fact answers half of SQL's trick questions.",
        "beats": [
          {
            "layout": "paragraph",
            "content": "A SELECT statement has a handful of clauses: SELECT names the columns, FROM names the table, WHERE filters rows, ORDER BY sorts them, and LIMIT caps how many come back. You can use SELECT star to grab every column, but in production you should always name only the columns you need — fetching unused data wastes network and memory."
          },
          {
            "layout": "bullets",
            "content": [
              "AS gives a column a readable alias in the result",
              "DISTINCT removes duplicate rows from the output",
              "LIMIT n OFFSET m is the standard way to paginate results"
            ]
          },
          {
            "layout": "paragraph",
            "content": "non-obvious: SQL does not execute in the order you write it. FROM is processed before SELECT, which is why you cannot use a column alias from SELECT inside a WHERE clause — WHERE runs before SELECT has even computed that alias."
          }
        ],
        "cap": "Name the columns you need, filter with WHERE, sort with ORDER BY, and remember that FROM runs before SELECT.",
        "followup_handoff": [
          "What is the difference between WHERE and HAVING?",
          "How do you implement pagination in SQL?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "select-queries-002",
      "slug": "distinct-in-sql-select-remove-duplicate-rows",
      "question": "How does DISTINCT work in SQL SELECT queries?",
      "title": "DISTINCT in SQL SELECT — Remove Duplicate Rows",
      "direct_answer": "**DISTINCT** removes duplicate rows from the result set. With multiple columns it deduplicates the **entire row combination**, not just the first column. `COUNT(DISTINCT col)` counts unique non-NULL values, and DISTINCT treats all NULLs as equal.",
      "layout_type": "sql-playground",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-04",
      "interviewer_intent": {
        "testing": "Whether you know DISTINCT applies to the whole selected row, not a single column.",
        "common_mistake": "Thinking SELECT DISTINCT a, b deduplicates only column a.",
        "to_stand_out": "Mention DISTINCT's sort/hash cost on big tables and that GROUP BY can be a faster alternative."
      },
      "company_tags": [
        "tcs",
        "infosys",
        "amazon",
        "flipkart"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "How DISTINCT Eliminates Duplicates",
            "content": "**DISTINCT** tells SQL to return only unique rows. `SELECT DISTINCT city FROM customers` returns each city once even if 500 customers share it.\n\nThe crucial part: with several columns, DISTINCT looks at the **whole row**. `SELECT DISTINCT city, age` returns unique *(city, age)* pairs — so *(Mumbai, 25)* and *(Mumbai, 30)* are both kept. It also has a cost: the database must **sort or hash** every row to find duplicates, which is expensive on large unindexed tables."
          },
          {
            "type": "concept_map",
            "title": "DISTINCT at a Glance",
            "content": "blue|Whole-row rule|~dedupes the row, not a column|DISTINCT a,b = unique pairs|not just first column|order of columns irrelevant\nemerald|In aggregates|~count unique values|COUNT(DISTINCT col)|ignores NULLs|one value per group\namber|NULL handling|~all NULLs are equal|many NULL rows collapse|to one NULL|unlike = comparisons\nrose|Cost|~sort or hash needed|slow on big tables|GROUP BY often faster|index helps"
          },
          {
            "type": "code_example",
            "title": "DISTINCT on one and many columns",
            "content": "```sql\n-- unique cities\nSELECT DISTINCT city FROM customers ORDER BY city;\n\n-- unique (category, city) pairs\nSELECT DISTINCT category, city FROM orders;\n\n-- count unique customers who ordered\nSELECT COUNT(DISTINCT customer_id) AS unique_buyers FROM orders;\n```\n\nSample output (last query):\n\n| unique_buyers |\n|---|\n| 142 |\n\n*The 500 order rows collapse to 142 distinct customers.*"
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Expecting per-column dedupe**: `SELECT DISTINCT city, name` gives unique city+name pairs, so a city repeats when names differ.\n- **DISTINCT as a band-aid**: slapping it on to hide duplicate rows from a bad JOIN masks the real bug and slows the query.\n- **Forgetting DISTINCT inside COUNT**: `COUNT(customer_id)` counts rows; `COUNT(DISTINCT customer_id)` counts unique customers.\n- **Ignoring cost**: DISTINCT on a 10M-row table forces a full sort — consider GROUP BY or an index."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Row-level**: DISTINCT dedupes the entire selected row combination.\n- **`COUNT(DISTINCT col)`** counts unique non-NULL values.\n- **NULLs**: all NULLs are treated as one value by DISTINCT.\n- **Cost**: needs a sort/hash — expensive on large tables.\n- **Alternative**: GROUP BY can produce the same result, sometimes faster."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "DISTINCT removes duplicate rows from a result set. If I write SELECT DISTINCT city FROM customers, each city comes back exactly once even if hundreds of people live there. The key thing interviewers probe is what happens with multiple columns. DISTINCT looks at the entire row, not just the first column. So SELECT DISTINCT city, age returns unique combinations of city and age together, which means Mumbai with age twenty-five and Mumbai with age thirty are both kept because the pairs differ. DISTINCT also shows up inside aggregates: COUNT DISTINCT customer_id counts how many unique customers placed orders, ignoring repeats. One mistake I watch for is using DISTINCT as a quick fix when a join accidentally multiplies rows. That just hides the real problem and makes the query slower, because the database has to sort or hash every row to find duplicates. On large tables without a supporting index that's a real performance hit, so often GROUP BY or fixing the join is the better move. Last detail worth knowing: DISTINCT treats all NULLs as equal, so many NULL rows collapse into a single NULL in the output, which is different from how the equals operator treats NULL."
          }
        ]
      },
      "followup_questions": [
        "What is the difference between DISTINCT and GROUP BY?",
        "How does DISTINCT handle NULL values?",
        "What is the performance impact of using DISTINCT?"
      ],
      "seo": {
        "metaTitle": "DISTINCT in SQL SELECT Queries | Java Fresher Interview",
        "metaDescription": "Understand how DISTINCT works in SQL with single and multiple columns. Java backend fresher interview question on removing duplicate rows in SELECT queries."
      },
      "order": 2,
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Spring Boot app with JPA over a relational database",
        "familiarity_anchors": [
          "SQL",
          "databases",
          "queries"
        ],
        "hook": "DISTINCT looks trivial, yet most candidates get the multi-column behaviour wrong — it dedupes whole rows, not single columns.",
        "beats": [
          {
            "layout": "paragraph",
            "content": "DISTINCT removes duplicate rows from the result set. When you write SELECT DISTINCT department FROM employees, you get each department name exactly once. That part everyone knows."
          },
          {
            "layout": "paragraph",
            "content": "candidates miss: when you select multiple columns with DISTINCT, it applies to the entire row — not just the first column. SELECT DISTINCT department, job_title returns unique combinations of both columns together. Two rows with the same department but different job titles are both kept."
          },
          {
            "layout": "bullets",
            "content": [
              "COUNT(DISTINCT column) counts unique non-null values inside an aggregate",
              "DISTINCT treats all NULL values as equal, so only one NULL appears in the result",
              "DISTINCT requires an internal sort or hash to find duplicates — it has a performance cost on large tables"
            ]
          }
        ],
        "cap": "DISTINCT deduplicates whole rows, not individual columns — and you can use it inside COUNT to count unique values.",
        "followup_handoff": [
          "What is the difference between DISTINCT and GROUP BY?",
          "How does DISTINCT handle NULL values?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "select-queries-003",
      "slug": "handling-null-values-in-sql-select",
      "question": "How do you handle NULL values in SQL SELECT queries?",
      "title": "Handling NULL Values in SQL SELECT",
      "direct_answer": "**NULL** means unknown or missing — not zero, not an empty string. You can't test it with `=`; use **`IS NULL`** / **`IS NOT NULL`**. SQL uses **three-valued logic** (true, false, unknown), and `COALESCE(col, default)` substitutes a fallback for NULL.",
      "layout_type": "sql-playground",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-04",
      "interviewer_intent": {
        "testing": "Whether you know NULL needs IS NULL and that NULL is never equal to anything, even another NULL.",
        "common_mistake": "Writing WHERE col = NULL, which always returns zero rows.",
        "to_stand_out": "Explain three-valued logic and that aggregates ignore NULL except COUNT(*)."
      },
      "company_tags": [
        "amazon",
        "microsoft",
        "tcs",
        "infosys"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Understanding NULL in SQL",
            "content": "**NULL** represents the absence of a value — genuinely unknown data, not `0` and not `''`. Because of that, normal equality breaks: `WHERE salary = NULL` returns no rows ever, since NULL is never equal to anything, **not even another NULL**.\n\nSQL uses **three-valued logic**: a comparison is true, false, or *unknown*, and unknown rows are dropped by WHERE. To test for missing data you must use **`IS NULL`** or **`IS NOT NULL`**, and **`COALESCE`** lets you swap a NULL for a default in the output."
          },
          {
            "type": "concept_map",
            "title": "NULL at a Glance",
            "content": "rose|What NULL is|~unknown / missing|not zero|not empty string|NULL != NULL\nblue|How to test|~special operators|IS NULL|IS NOT NULL|never use =\nemerald|COALESCE|~fallback value|COALESCE(col, 0)|first non-NULL wins|cleans output\namber|Aggregates|~mostly skip NULL|SUM/AVG ignore NULL|COUNT(col) skips NULL|COUNT(*) counts all rows"
          },
          {
            "type": "code_example",
            "title": "Testing and replacing NULLs",
            "content": "```sql\n-- employees with no phone\nSELECT name FROM employees WHERE phone IS NULL;\n\n-- replace NULL bonus with 0 in the output\nSELECT name, COALESCE(bonus, 0) AS bonus FROM employees;\n```\n\nSample output (second query):\n\n| name | bonus |\n|---|---|\n| Asha | 5000 |\n| Karan | 0 |\n\n*Karan's `bonus` is NULL in the table; COALESCE shows 0 instead of a blank.*"
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **`WHERE col = NULL`**: never matches — use `IS NULL`.\n- **NULL = empty string**: they're different; `''` is a known empty value, NULL is unknown.\n- **Forgetting aggregate behaviour**: `AVG(salary)` skips NULLs, so the average can differ from what you expect.\n- **Primitive fields in JPA**: map nullable columns to `Integer`, not `int`, or NULL silently becomes 0."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **NULL = unknown**, distinct from 0 and ''.\n- **Test with `IS NULL` / `IS NOT NULL`**, never `=`.\n- **NULL != NULL** — that's why a special operator exists.\n- **`COALESCE(col, default)`** returns the first non-NULL value.\n- **Aggregates** ignore NULL except `COUNT(*)`."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "In SQL, NULL means a value is unknown or missing. It is not zero and it is not an empty string. The big consequence is that you cannot compare NULL with the equals operator. If I write WHERE salary equals NULL, that returns zero rows even when salaries are missing, because NULL is never equal to anything, not even another NULL. That comes from SQL's three-valued logic, where a comparison can be true, false, or unknown, and unknown rows get filtered out. To actually find missing data I use IS NULL or IS NOT NULL. So to find employees with no phone number I write WHERE phone IS NULL. When I want to clean up the output, I use COALESCE, which returns the first non-null value in a list. COALESCE of bonus and zero shows zero wherever bonus is missing, which is handy for reports and API responses. One subtlety that surprises people is how aggregates behave: SUM and AVG and COUNT of a column all ignore NULLs, but COUNT star counts every row including the NULL ones. And in a Spring Boot entity, a nullable column should map to a wrapper type like Integer rather than a primitive int, otherwise a missing value quietly turns into zero and you lose the fact that it was unknown."
          }
        ]
      },
      "followup_questions": [
        "Why does WHERE col = NULL not work in SQL?",
        "What is the difference between COALESCE and IFNULL?",
        "How do NULL values affect COUNT in SQL?"
      ],
      "seo": {
        "metaTitle": "Handling NULL Values in SQL | Java Backend Interview Questions",
        "metaDescription": "Learn how to handle NULL values in SQL with IS NULL, COALESCE examples. Common Java fresher interview question on SQL NULL behavior."
      },
      "order": 3,
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Spring Boot app with JPA over a relational database",
        "familiarity_anchors": [
          "SQL",
          "databases",
          "queries"
        ],
        "hook": "The golden NULL rule is two words — IS NULL — because in SQL, NULL isn't even equal to itself.",
        "beats": [
          {
            "layout": "paragraph",
            "content": "NULL in SQL means unknown or missing — it is not zero and it is not an empty string. The key rule is that you cannot compare NULL using equals. Writing WHERE manager_id = NULL will never return any rows, because any comparison with NULL using equals produces UNKNOWN, not TRUE. Use IS NULL or IS NOT NULL instead."
          },
          {
            "layout": "paragraph",
            "content": "sharp edge: NULL is not equal to NULL. That is not a typo — NULL compared to NULL is still UNKNOWN, which is why IS NULL exists as a special operator. This surprises a lot of candidates."
          },
          {
            "layout": "bullets",
            "content": [
              "COALESCE(col, default) returns the first non-null value — great for replacing NULLs in output",
              "COUNT(star) counts all rows including nulls; COUNT(column) skips null values in that column",
              "Aggregate functions like SUM and AVG automatically ignore null values"
            ]
          }
        ],
        "cap": "Check for NULL with IS NULL, never with equals — and use COALESCE when you want a fallback value.",
        "followup_handoff": [
          "What is the difference between COALESCE and IFNULL?",
          "How do NULL values affect COUNT in SQL?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "select-queries-005",
      "slug": "order-by-clause-in-sql-sorting-query-results",
      "question": "What is the ORDER BY clause and how does it work?",
      "title": "ORDER BY Clause in SQL — Sorting Query Results",
      "direct_answer": "**ORDER BY** sorts the result set. **ASC** (ascending) is the default; **DESC** reverses it. With several columns the leftmost is the primary sort and the next breaks ties. Because ORDER BY runs after SELECT, it can reference SELECT aliases.",
      "layout_type": "sql-playground",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 2,
      "last_updated": "2026-06-04",
      "interviewer_intent": {
        "testing": "Whether you understand multi-column sort priority and that ORDER BY runs after SELECT.",
        "common_mistake": "Expecting ORDER BY a DESC, b to group by b, or using LIMIT without ORDER BY.",
        "to_stand_out": "Mention NULLS FIRST/LAST and sorting by column position or alias."
      },
      "company_tags": [
        "tcs",
        "infosys",
        "wipro",
        "cognizant"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "How ORDER BY Sorts Results",
            "content": "**ORDER BY** controls the order rows come back in — without it, the database returns rows in *no guaranteed order*. You give one or more **sort keys** and an optional direction: **ASC** (default, A→Z, low→high) or **DESC** (reverse).\n\nMulti-column sorting works left to right, like sorting a deck first by suit, then by rank: the first column is the primary key, the second breaks ties. Since ORDER BY runs *after* SELECT, you can sort by a column **alias** defined in SELECT."
          },
          {
            "type": "concept_map",
            "title": "ORDER BY at a Glance",
            "content": "blue|Direction|~ASC default, DESC reverse|ASC = low to high|DESC = high to low|per-column direction\nemerald|Multi-column|~left = highest priority|first sorts globally|next breaks ties|order matters\namber|Aliases|~runs after SELECT|alias usable here|not usable in WHERE|can sort by position too\nrose|With LIMIT|~stability needed|no ORDER = random pages|NULLS FIRST/LAST|controls NULL spot"
          },
          {
            "type": "code_example",
            "title": "Single and multi-column sorting",
            "content": "```sql\n-- department A→Z, then highest paid first within each\nSELECT name, department, salary\nFROM employees\nORDER BY department ASC, salary DESC;\n```\n\nSample output:\n\n| name | department | salary |\n|---|---|---|\n| Meera | Finance | 92000 |\n| Sam | Finance | 70000 |\n| Ravi | Tech | 120000 |\n| Anil | Tech | 95000 |\n\n*Finance before Tech alphabetically; inside each, salary descends.*"
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **LIMIT without ORDER BY**: pages become non-deterministic; the same page can show different rows.\n- **Wrong priority**: `ORDER BY salary DESC, department` sorts by salary first, using department only as a tiebreaker — not what you usually want.\n- **Sorting unindexed columns on huge tables**: forces an expensive in-memory sort.\n- **Assuming NULL position**: NULL ordering varies by database; use `NULLS FIRST`/`NULLS LAST` when it matters."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **ASC is default**, DESC reverses; direction is per column.\n- **Leftmost column wins**; later columns break ties.\n- **Alias-friendly**: ORDER BY runs after SELECT.\n- **Pagination needs ORDER BY** for stable pages.\n- **NULLS FIRST/LAST** (Postgres) controls where NULLs land."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "ORDER BY controls the sequence rows come back in. Without it the database can return rows in any order, so you should never rely on the default. By default ORDER BY sorts ascending, which is low to high or A to Z, and you add DESC when you want the reverse. The interesting part is multi-column sorting. If I write ORDER BY department, salary descending, it first sorts everyone by department alphabetically, and then within each department it sorts by salary highest first. The left-to-right priority is the key idea: the first column is the primary sort and each later column only breaks ties. A common mistake is forgetting ORDER BY when paginating with LIMIT and OFFSET; without a stable sort, page two can show different rows each time you load it. Another thing worth mentioning is that ORDER BY runs late in execution, after SELECT, so unlike WHERE it can reference a column alias you defined in the SELECT list, and you can even sort by column position. In production I always check whether the sort columns are indexed, because sorting a few million rows without an index means loading everything into memory to sort, which is slow."
          }
        ]
      },
      "followup_questions": [
        "How does ORDER BY handle NULL values?",
        "What is the execution order of SQL clauses?",
        "Can you ORDER BY a column that is not in the SELECT list?"
      ],
      "seo": {
        "metaTitle": "ORDER BY Clause in SQL | Java Fresher Interview Questions",
        "metaDescription": "Learn how ORDER BY works in SQL with ASC, DESC, and multi-column sorting examples. Java backend fresher interview prep on sorting query results."
      },
      "order": 5,
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Spring Boot app with JPA over a relational database",
        "familiarity_anchors": [
          "SQL",
          "databases",
          "queries"
        ],
        "hook": "The deck-of-cards rule — first column is the suit, second is the rank — is what separates a confident ORDER BY answer from a vague one.",
        "beats": [
          {
            "layout": "paragraph",
            "content": "ORDER BY sorts query results. ASC is the default sort direction so you only need to write it explicitly when you want to be clear — DESC is what you add when you want highest first. You can sort by multiple columns separated by commas. The first column has the highest priority, and the next column only matters when two rows tie on the first."
          },
          {
            "layout": "paragraph",
            "content": "For example, ORDER BY department ASC, salary DESC sorts employees alphabetically by department, and within the same department shows the highest earners first."
          },
          {
            "layout": "bullets",
            "content": [
              "ORDER BY is processed after WHERE but before LIMIT — filter first, sort second, then cap the count",
              "NULL values sort last by default in ascending order in most databases",
              "You can ORDER BY column aliases defined in SELECT — unlike WHERE, ORDER BY runs after SELECT"
            ]
          }
        ],
        "cap": "First column sets the main sort, subsequent columns break ties — and ORDER BY runs after SELECT so aliases work there.",
        "followup_handoff": [
          "How does ORDER BY handle NULL values?",
          "Can you ORDER BY a column that is not in the SELECT list?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "select-queries-004",
      "slug": "sql-pagination-with-limit-and-offset",
      "question": "How do you implement pagination in SQL?",
      "title": "SQL Pagination with LIMIT and OFFSET",
      "direct_answer": "**Pagination** splits results into pages with **LIMIT** (page size) and **OFFSET** (rows to skip). The formula is `OFFSET = (page - 1) * pageSize`. You **must** add `ORDER BY` for stable pages, and large offsets get slow because skipped rows are still scanned — cursor (keyset) pagination avoids that.",
      "layout_type": "sql-playground",
      "difficulty": "medium",
      "importance": "high",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-04",
      "interviewer_intent": {
        "testing": "Whether you can compute OFFSET and know pagination needs a stable sort.",
        "common_mistake": "Omitting ORDER BY, or large-OFFSET performance ignorance.",
        "to_stand_out": "Explain keyset/cursor pagination (WHERE id > lastSeen) for deep pages."
      },
      "company_tags": [
        "amazon",
        "flipkart",
        "microsoft",
        "tcs"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Pagination with LIMIT and OFFSET",
            "content": "**Pagination** shows a manageable chunk of a large result instead of everything at once. **`LIMIT`** sets the page size; **`OFFSET`** skips rows to reach the page. The formula is `OFFSET = (page - 1) * pageSize` — page 3 at 10 per page skips 20 and takes 10.\n\nTwo non-negotiables: include **`ORDER BY`** or pages are non-deterministic, and know that **large offsets are slow** because the database still scans and discards every skipped row. For deep pagination, **keyset (cursor) pagination** with `WHERE id > lastSeenId` is far faster."
          },
          {
            "type": "concept_map",
            "title": "Pagination at a Glance",
            "content": "blue|LIMIT / OFFSET|~page size + skip|LIMIT n = page size|OFFSET = (page-1)*n|page 3@10 = OFFSET 20\nemerald|ORDER BY|~mandatory|stable page order|else rows repeat/skip|sort on a unique key\nrose|Large offsets|~scan all skipped|OFFSET 100000 is slow|cost grows with page|don't deep-paginate\nviolet|Keyset/cursor|~fast deep pages|WHERE id > lastSeen|no row skipping|great for infinite scroll"
          },
          {
            "type": "code_example",
            "title": "Offset pagination vs keyset pagination",
            "content": "```sql\n-- page 3, 10 per page (offset pagination)\nSELECT id, name, price\nFROM products\nORDER BY id\nLIMIT 10 OFFSET 20;\n\n-- keyset: next page after the last id you saw (fast even deep)\nSELECT id, name, price\nFROM products\nWHERE id > 1040\nORDER BY id\nLIMIT 10;\n```\n\nSample output (first query):\n\n| id | name | price |\n|---|---|---|\n| 21 | Mouse | 499 |\n| 22 | Cable | 199 |\n| 23 | Stand | 899 |\n\n*Rows 1–20 are skipped; the next 10 are returned in id order.*"
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **No ORDER BY**: the same page shows different rows on refresh.\n- **Off-by-one OFFSET**: with 1-based pages it's `(page-1)*size`, not `page*size`.\n- **Deep offsets**: `OFFSET 100000` scans and throws away 100k rows — use keyset pagination.\n- **No total count**: return total rows/pages so the UI can render controls (Spring's `Page` does this)."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **LIMIT** = page size, **OFFSET** = rows to skip.\n- **`OFFSET = (page - 1) * pageSize`** for 1-based pages.\n- **ORDER BY is mandatory** for stable pagination.\n- **Large offsets are slow** — prefer keyset/cursor pagination.\n- **Spring Data `Pageable`** generates LIMIT/OFFSET plus total count."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "Pagination breaks a big result into pages using LIMIT and OFFSET. LIMIT is the page size, how many rows you want, and OFFSET is how many rows to skip to reach the page. The formula is offset equals page number minus one, times page size. So page three at ten per page skips twenty rows and takes the next ten, which is LIMIT ten OFFSET twenty. The single most important rule is to always include ORDER BY, ideally on a unique column. Without a stable sort the database can return rows in any order, so page two might show items you already saw on page one, or skip some entirely. In Spring Boot I'd lean on Spring Data's Pageable and PageRequest, which generate the LIMIT and OFFSET for me and also give me the total count so the frontend can render page controls. The catch with offset pagination is performance on deep pages: if a user jumps to page five thousand, the database still scans and discards every row before that page. For deep pagination or infinite scroll I switch to keyset, also called cursor pagination, where instead of an offset I filter WHERE id greater than the last id I saw and order by id. That skips nothing, so it stays fast no matter how deep you go."
          }
        ]
      },
      "followup_questions": [
        "What is cursor-based pagination and when would you use it?",
        "Why must you include ORDER BY in pagination queries?",
        "How does Spring Data JPA handle pagination?"
      ],
      "seo": {
        "metaTitle": "SQL Pagination with LIMIT and OFFSET | Java Interview Prep",
        "metaDescription": "Learn SQL pagination using LIMIT and OFFSET with examples and best practices. Java backend fresher interview question on paginating query results."
      },
      "order": 4,
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Spring Boot app with JPA over a relational database",
        "familiarity_anchors": [
          "SQL",
          "databases",
          "queries"
        ],
        "hook": "LIMIT and OFFSET get you started, but the hidden trap is the deep page that scans a hundred thousand rows just to throw them away.",
        "beats": [
          {
            "layout": "paragraph",
            "content": "Pagination in SQL uses LIMIT to control how many rows per page and OFFSET to control how many rows to skip. The formula is offset equals page number minus one, multiplied by page size. Page 1 is LIMIT 10 OFFSET 0, page 2 is LIMIT 10 OFFSET 10, and so on."
          },
          {
            "layout": "bullets",
            "content": [
              "Always include ORDER BY when paginating — without it, the row order is undefined and results can differ between pages",
              "In Spring Data JPA, the Pageable interface handles LIMIT and OFFSET for you automatically"
            ]
          },
          {
            "layout": "paragraph",
            "content": "production trap: LIMIT/OFFSET becomes slow on large tables. For page 500 with 10 rows per page, the database still scans and discards 4990 rows to get to the ones you want. Cursor-based pagination — using WHERE id is greater than the last seen id — avoids that scan entirely."
          }
        ],
        "cap": "Use LIMIT and OFFSET for pagination, always with ORDER BY, and know that large offsets hurt performance at scale.",
        "followup_handoff": [
          "What is cursor-based pagination and when would you use it?",
          "How does Spring Data JPA handle pagination?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    },
    {
      "id": "select-queries-006",
      "slug": "sql-query-execution-order-from-to-select",
      "question": "What is the SQL query execution order?",
      "title": "SQL Query Execution Order — FROM to SELECT",
      "direct_answer": "SQL clauses run in a fixed **logical order**: FROM → (JOIN) → WHERE → GROUP BY → HAVING → SELECT → DISTINCT → ORDER BY → LIMIT — not the order you write them. This is why a SELECT alias can't be used in WHERE, and why aggregates belong in HAVING, not WHERE.",
      "layout_type": "sql-playground",
      "difficulty": "medium",
      "importance": "high",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-04",
      "interviewer_intent": {
        "testing": "Whether you know the logical processing order and its consequences.",
        "common_mistake": "Using a SELECT alias in WHERE, or putting COUNT()/SUM() in WHERE instead of HAVING.",
        "to_stand_out": "Distinguish logical order (what's legal) from physical order (what the optimizer actually does)."
      },
      "company_tags": [
        "amazon",
        "microsoft",
        "flipkart",
        "tcs"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "SQL Logical Execution Order",
            "content": "SQL does **not** execute clauses in written order. The **logical order** is `FROM → JOIN → WHERE → GROUP BY → HAVING → SELECT → DISTINCT → ORDER BY → LIMIT`. Each step can only see what earlier steps produced.\n\nThat explains three classic gotchas: a **SELECT alias can't be used in WHERE** (SELECT runs after WHERE), **aggregates go in HAVING not WHERE** (grouping hasn't happened during WHERE), and **ORDER BY can use SELECT aliases** (it runs after SELECT)."
          },
          {
            "type": "flow_diagram",
            "title": "Execution Order Flow",
            "content": "```mermaid\nflowchart TB\n  A[FROM / JOIN<br/>pick + combine tables] --> B[WHERE<br/>filter rows]\n  B --> C[GROUP BY<br/>form groups]\n  C --> D[HAVING<br/>filter groups]\n  D --> E[SELECT<br/>compute columns + aliases]\n  E --> F[DISTINCT<br/>dedupe]\n  F --> G[ORDER BY<br/>sort, aliases OK]\n  G --> H[LIMIT<br/>cap rows]\n```"
          },
          {
            "type": "code_example",
            "title": "Legal vs illegal references",
            "content": "```sql\n-- LEGAL: ORDER BY uses the alias 'total' (ORDER BY runs after SELECT)\nSELECT id, quantity * price AS total\nFROM orders\nWHERE status = 'PENDING'\nORDER BY total DESC;\n\n-- ILLEGAL: alias in WHERE (WHERE runs before SELECT)\n-- SELECT id, quantity * price AS total FROM orders WHERE total > 1000;\n\n-- LEGAL: aggregate filter belongs in HAVING, after GROUP BY\nSELECT customer_id, COUNT(*) AS orders\nFROM orders\nGROUP BY customer_id\nHAVING COUNT(*) > 5;\n```\n\nSample output (last query):\n\n| customer_id | orders |\n|---|---|\n| 7 | 9 |\n| 12 | 6 |"
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Alias in WHERE**: repeat the expression instead (`WHERE quantity*price > 1000`).\n- **Aggregate in WHERE**: `WHERE COUNT(*) > 5` fails — use `HAVING` after GROUP BY.\n- **Filtering late**: put row filters in WHERE (runs before grouping) so fewer rows are grouped — faster than HAVING.\n- **Confusing logical vs physical**: the optimizer may reorder physically, but must produce the logically-correct result."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Order**: FROM → WHERE → GROUP BY → HAVING → SELECT → ORDER BY → LIMIT.\n- **FROM first**, SELECT computed late — that's why aliases fail in WHERE.\n- **Aggregates** need HAVING because groups don't exist during WHERE.\n- **ORDER BY** runs after SELECT, so it can use aliases.\n- **Filter early** in WHERE for performance, not in HAVING."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "SQL does not run clauses in the order you write them. The logical execution order is FROM first, then WHERE, then GROUP BY, then HAVING, then SELECT, then ORDER BY, and finally LIMIT. Even though you type SELECT at the top, the database processes FROM first to identify the tables, applies WHERE to filter individual rows, groups them with GROUP BY, filters those groups with HAVING, and only then computes the SELECT columns. This single fact explains a bunch of confusing behaviour. You cannot use a column alias from SELECT inside WHERE, because WHERE runs before SELECT has created the alias; you have to repeat the expression. For the same reason, aggregate functions like COUNT and SUM go in HAVING, not WHERE, because grouping has not happened yet when WHERE runs. That is exactly why HAVING exists. The good news is ORDER BY runs after SELECT, so it can reference aliases freely. A practical tip that falls out of this: filter with WHERE whenever you can rather than HAVING, because WHERE runs earlier and shrinks the data before grouping, which is faster. And one nuance for senior interviewers: this is the logical order that defines what is legal; the optimizer may physically execute steps in a different sequence as long as it returns the same result."
          }
        ]
      },
      "followup_questions": [
        "Why can't you use a SELECT alias in a WHERE clause?",
        "What is the difference between WHERE and HAVING?",
        "When does JOIN happen in the SQL execution order?"
      ],
      "seo": {
        "metaTitle": "SQL Query Execution Order Explained | Java Backend Interview",
        "metaDescription": "Learn the SQL execution order: FROM, WHERE, GROUP BY, HAVING, SELECT, ORDER BY, LIMIT. Critical Java fresher interview topic for writing correct SQL."
      },
      "order": 6,
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "Spring Boot app with JPA over a relational database",
        "familiarity_anchors": [
          "SQL",
          "databases",
          "queries"
        ],
        "hook": "Knowing FROM runs first and SELECT runs almost last unlocks the answer to a dozen other SQL questions at once.",
        "beats": [
          {
            "layout": "paragraph",
            "content": "SQL clauses execute in a different order than you write them. The logical order is: FROM to identify the data source, WHERE to filter individual rows, GROUP BY to group them, HAVING to filter the groups, SELECT to compute the output columns, ORDER BY to sort, and finally LIMIT to cap the result."
          },
          {
            "layout": "mini_table",
            "content": {
              "headers": [
                "Step",
                "Clause",
                "What it does"
              ],
              "rows": [
                [
                  "1",
                  "FROM",
                  "Identifies tables and joins"
                ],
                [
                  "2",
                  "WHERE",
                  "Filters individual rows"
                ],
                [
                  "3",
                  "GROUP BY / HAVING",
                  "Groups rows, then filters groups"
                ],
                [
                  "4",
                  "SELECT",
                  "Computes columns and aliases"
                ],
                [
                  "5",
                  "ORDER BY / LIMIT",
                  "Sorts and caps the output"
                ]
              ]
            }
          },
          {
            "layout": "paragraph",
            "content": "gotcha: this order explains two common errors. You cannot use a SELECT alias in WHERE because SELECT has not run yet when WHERE executes. And you cannot put aggregate functions like COUNT in WHERE — aggregation has not happened yet either. That is exactly why HAVING exists."
          }
        ],
        "cap": "FROM runs first, SELECT runs fifth — that single fact explains why aliases fail in WHERE and why aggregates need HAVING.",
        "followup_handoff": [
          "Why can't you use a SELECT alias in a WHERE clause?",
          "What is the difference between WHERE and HAVING?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review"
      }
    }
  ]
}

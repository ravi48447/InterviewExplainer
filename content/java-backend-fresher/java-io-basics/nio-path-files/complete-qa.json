{
  "questions": [
    {
      "id": "jbf-io-nio-path-files-001",
      "slug": "path-and-paths-get",
      "question": "What is java.nio.file.Path and how do you create one?",
      "title": "Path and Paths.get()",
      "direct_answer": "Path is an interface representing a file system path; create instances using Path.of() (Java 11+) or Paths.get() (Java 7+), which accept a string path or URI.",
      "layout_type": "default",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 2,
      "last_updated": "2025-01-01",
      "interviewer_intent": {
        "testing": "Whether the candidate knows the modern NIO2 path creation API",
        "common_mistake": "Using new File('path') instead of Path.of('path') in modern Java code",
        "to_stand_out": "Mention that Path is an interface—different implementations exist for different file systems (local, zip, in-memory)"
      },
      "company_tags": [
        "amazon",
        "tcs",
        "infosys"
      ],
      "answer": {
        "sections": [
          {
            "type": "speakable_answer",
            "title": "How to Answer",
            "content": "java.nio.file.Path is an interface introduced in Java 7 as part of the NIO2 API. Unlike java.io.File which is a class, Path is an interface, which means there can be different implementations—the default implementation for the local file system, but also implementations for zip file systems, in-memory file systems, or remote file systems through custom providers. To create a Path, you have two main options. In Java 7+: Paths.get('/home/user/data.txt') or Paths.get('/home/user', 'data.txt')—you can pass multiple segments and they're joined with the OS separator. In Java 11+: Path.of('/home/user/data.txt')—a static factory method added directly to the interface, more idiomatic. Both produce equivalent objects. You can also resolve relative paths: basePath.resolve('subdir/file.txt') appends the relative path to the base. Path.toFile() converts back to java.io.File if needed for legacy APIs. Unlike File, Path doesn't make any OS calls just from being created—it's just a representation of a path string."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": [
              "Path is an interface (not a class like File)—enables different file system implementations",
              "Paths.get('path', 'subpath') (Java 7+) or Path.of('path') (Java 11+)",
              "path.resolve('relative/path') appends relative path to base path",
              "path.toFile() converts to legacy java.io.File when needed",
              "Creating a Path makes no OS calls—no existence check",
              "Path.getFileName(), getParent(), getRoot() for path components"
            ]
          }
        ]
      },
      "speakable_v2": {
        "pillar": "P01",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "A",
        "depth_marker": "foundation",
        "hook": "Path is the modern Java 11 way to represent a file location — no OS calls, just a typed path.",
        "beats": [
          {
            "layout": "paragraph",
            "content": "Path is an interface, not a class like the old File. That means different implementations can exist for local file systems, zip files, or in-memory file systems. In practice, Path.of() is all you need for everyday work — pass one string or multiple segments and Java joins them with the correct OS separator."
          },
          {
            "layout": "bullets",
            "heading": "Two ways to create a Path",
            "items": [
              "Path.of(\"config/app.yml\") — the modern way, Java 11+",
              "Paths.get(\"config\", \"app.yml\") — the Java 7+ way, same result",
              "path.resolve(\"subdir\") — append a relative segment to an existing path",
              "path.toFile() — convert back to File when a legacy API demands it"
            ]
          },
          {
            "layout": "paragraph",
            "content": "One thing worth saying in an interview: creating a Path makes zero OS calls. It's just a path string wrapper. The path doesn't need to exist yet. Existence checks happen only when you pass it to Files.exists() or a read operation."
          }
        ],
        "familiarity_anchor": "Path.of is like writing down an address — you can write it without leaving your seat, and the destination doesn't have to exist yet.",
        "standard_example": "Java 17 app reading configuration files and writing logs"
      },
      "followup_questions": [
        "What is the difference between Path.resolve() and Path.resolveSibling()?",
        "How do you convert a java.io.File to a java.nio.file.Path?"
      ],
      "seo": {
        "metaTitle": "java.nio.file.Path and Paths.get() Explained | Java NIO2",
        "metaDescription": "Learn what Java NIO2 Path is, how to create paths using Paths.get() and Path.of(), path resolution, and conversion from legacy java.io.File."
      },
      "order": 1
    },
    {
      "id": "jbf-io-nio-path-files-002",
      "slug": "files-utility-class-operations",
      "question": "What are the most important methods in the java.nio.file.Files utility class?",
      "title": "Files Utility Class Key Methods",
      "direct_answer": "Key Files methods include: readAllBytes/readAllLines/readString for reading, writeString/write for writing, copy/move for file operations, createDirectory/createDirectories for directories, exists/isFile/isDirectory for checks, and delete for removal.",
      "layout_type": "default",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 2,
      "last_updated": "2025-01-01",
      "interviewer_intent": {
        "testing": "Familiarity with the modern Java file operation API",
        "common_mistake": "Not knowing Files exists and implementing verbose manual IO for simple operations",
        "to_stand_out": "Mention Files.copy() with StandardCopyOption.REPLACE_EXISTING and StandardCopyOption.COPY_ATTRIBUTES options"
      },
      "company_tags": [
        "amazon",
        "tcs",
        "infosys",
        "cognizant"
      ],
      "answer": {
        "sections": [
          {
            "type": "speakable_answer",
            "title": "How to Answer",
            "content": "Files is a utility class with static methods for all common file operations—think of it as the Swiss Army knife of Java file IO. For reading: Files.readAllBytes(path) for binary, Files.readAllLines(path) for all text lines as List<String>, Files.readString(path) for all text as a single String (Java 11+). For writing: Files.writeString(path, string) or Files.write(path, bytes/lines). For file operations: Files.copy(source, target) to copy files, Files.move(source, target) to move/rename—both accept copy options like StandardCopyOption.REPLACE_EXISTING. For directory creation: Files.createDirectory(path) for one level, Files.createDirectories(path) for full path. For checking: Files.exists(path), Files.isFile(path), Files.isDirectory(path), Files.size(path). For deletion: Files.delete(path) throws exception if it fails, or Files.deleteIfExists(path). For streaming: Files.lines(path) for lazy line-by-line reading, Files.list(path) for directory listing, Files.walk(path) for recursive traversal. Every method throws IOException on failure—proper error handling unlike the old boolean-return File methods."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": [
              "Read: readAllBytes(), readAllLines(), readString() (Java 11+), lines() (lazy stream)",
              "Write: writeString(), write(path, bytes), write(path, lines)",
              "File ops: copy(), move() with StandardCopyOption options",
              "Directories: createDirectory(), createDirectories()",
              "Checks: exists(), isFile(), isDirectory(), size()",
              "Delete: delete() throws on failure, deleteIfExists() returns boolean",
              "All methods throw IOException—proper error handling"
            ]
          }
        ]
      },
      "speakable_v2": {
        "pillar": "P01",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "A",
        "depth_marker": "foundation",
        "hook": "The Files class is the Swiss Army knife of Java file I/O — one-liners for everything.",
        "beats": [
          {
            "layout": "grouped_paragraphs",
            "groups": [
              {
                "heading": "Reading",
                "content": "Files.readString(path) reads the whole file as a String. Files.readAllLines(path) gives you a List of lines. Files.lines(path) gives you a lazy stream for large files."
              },
              {
                "heading": "Writing",
                "content": "Files.writeString(path, text) is the one-liner for writing text. Pass StandardOpenOption.APPEND to add to an existing file instead of overwriting."
              },
              {
                "heading": "File operations",
                "content": "Files.copy, Files.move, Files.delete, Files.createDirectories — all throw real IOExceptions with meaningful messages, unlike the old File methods that silently return false."
              }
            ]
          },
          {
            "layout": "paragraph",
            "content": "Every method in Files throws IOException when something goes wrong. That's the key upgrade over the legacy File class — you get a proper error message instead of a silent boolean false."
          }
        ],
        "familiarity_anchor": "Files is to file I/O what Collections is to lists — one utility class that handles everything.",
        "standard_example": "Java 17 app reading configuration files and writing logs"
      },
      "followup_questions": [
        "What copy options does Files.copy() support?",
        "What is the difference between Files.delete() and Files.deleteIfExists()?"
      ],
      "seo": {
        "metaTitle": "Java Files Utility Class Key Methods | NIO2 Interview Guide",
        "metaDescription": "Learn the most important methods in java.nio.file.Files: reading, writing, copying, moving, directory creation, and why they're better than java.io.File methods."
      },
      "order": 2
    },
    {
      "id": "jbf-io-nio-path-files-003",
      "slug": "files-walk-directory-traversal",
      "question": "How do you traverse a directory recursively using Files.walk() in Java?",
      "title": "Directory Traversal with Files.walk()",
      "direct_answer": "Files.walk(path) returns a depth-first Stream<Path> of all files and directories starting from the given path; filter with stream operations and use try-with-resources to close the stream and release the directory handle.",
      "layout_type": "default",
      "difficulty": "medium",
      "importance": "medium",
      "reading_time_minutes": 2,
      "last_updated": "2025-01-01",
      "interviewer_intent": {
        "testing": "Practical NIO2 knowledge for directory operations",
        "common_mistake": "Not closing the stream returned by Files.walk()—it holds an open directory handle",
        "to_stand_out": "Mention Files.walk(path, maxDepth) to limit traversal depth and Files.find() for combined walk+filter"
      },
      "company_tags": [
        "amazon",
        "tcs"
      ],
      "answer": {
        "sections": [
          {
            "type": "speakable_answer",
            "title": "How to Answer",
            "content": "Files.walk(path) returns a lazy Stream<Path> that performs depth-first traversal of the directory tree starting at the given path, including the path itself. The stream yields directories and files mixed in depth-first order. A common pattern is to filter for only files and process them: try (Stream<Path> paths = Files.walk(baseDir)) { paths.filter(Files::isRegularFile).filter(p -> p.toString().endsWith('.txt')).forEach(this::processFile); }. The try-with-resources is critical—Files.walk() holds open directory handles for each level of traversal and they must be released. To limit depth: Files.walk(path, 2) only goes 2 levels deep. Files.find(path, maxDepth, BiPredicate) is an alternative that combines the walk with a filter predicate, and can be more efficient because it doesn't yield paths that don't match. For just direct children without recursion, use Files.list(path). To find all files of a certain extension recursively, Files.walk combined with stream filtering is the idiomatic NIO2 approach."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": [
              "Files.walk(path): lazy Stream<Path>, depth-first, includes starting path itself",
              "MUST use try-with-resources—stream holds open directory handles",
              "Filter: .filter(Files::isRegularFile) to exclude directories",
              "Limit depth: Files.walk(path, maxDepth)",
              "Files.list(path): direct children only (no recursion), also needs try-with-resources",
              "Files.find(path, depth, BiPredicate): combined walk+filter, potentially more efficient"
            ]
          }
        ]
      },
      "speakable_v2": {
        "pillar": "P01",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "E",
        "depth_marker": "how-it-works",
        "hook": "Files.walk gives you every file in a directory tree as a lazy stream — just remember to close it.",
        "beats": [
          {
            "layout": "paragraph",
            "content": "Files.walk(path) returns a depth-first Stream of Path objects for every file and directory under the given root, including the root itself. It's lazy — entries are read from disk as you consume them. Chain stream operations to filter: filter(Files::isRegularFile) to skip directories, filter to match an extension, then forEach or collect."
          },
          {
            "layout": "bullets",
            "heading": "Things to get right",
            "items": [
              "Always use try-with-resources — Files.walk holds open directory handles that must close",
              "Files.walk(path, maxDepth) — limits how deep it recurses, useful to avoid scanning huge trees",
              "Files.list(path) — use this instead when you only need immediate children",
              "Files.find(path, depth, predicate) — combines walk and filter, more efficient for large trees"
            ]
          },
          {
            "layout": "paragraph",
            "content": "For recursive delete, walk the tree in reverse order so children come before parents: Files.walk(path).sorted(Comparator.reverseOrder()).forEach(Files::delete)."
          }
        ],
        "familiarity_anchor": "Files.walk is like find in the terminal — it digs through every subfolder and hands you each path one at a time.",
        "standard_example": "Java 17 app reading configuration files and writing logs"
      },
      "followup_questions": [
        "How do you count all .java files in a project directory recursively?",
        "What is the difference between Files.walk() and Files.find()?"
      ],
      "seo": {
        "metaTitle": "Files.walk() Directory Traversal in Java | NIO2 Interview",
        "metaDescription": "Learn how to use Java Files.walk() for recursive directory traversal, filtering files, limiting depth, and the importance of try-with-resources."
      },
      "order": 3
    },
    {
      "id": "jbf-io-nio-path-files-004",
      "slug": "path-operations-resolve-relativize",
      "question": "How do Path.resolve(), relativize(), and normalize() work?",
      "title": "Path.resolve(), relativize(), normalize()",
      "direct_answer": "resolve() appends a path to another; relativize() computes the relative path from one path to another; normalize() removes redundant . and .. elements from a path.",
      "layout_type": "default",
      "difficulty": "medium",
      "importance": "medium",
      "reading_time_minutes": 2,
      "last_updated": "2025-01-01",
      "interviewer_intent": {
        "testing": "Practical Path manipulation API knowledge",
        "common_mistake": "Trying to build paths with string concatenation instead of using resolve()",
        "to_stand_out": "Show a concrete use case for relativize()—generating relative links in file-based content systems"
      },
      "company_tags": [
        "amazon",
        "tcs"
      ],
      "answer": {
        "sections": [
          {
            "type": "speakable_answer",
            "title": "How to Answer",
            "content": "These three methods cover the most common path manipulation operations. resolve() appends a path: Path.of('/home/user').resolve('documents/file.txt') gives /home/user/documents/file.txt. If you resolve an absolute path onto another path, the result is just the absolute path—resolve('/etc/config') gives /etc/config regardless of base. relativize() is the inverse: it computes the relative path from one path to another. Path.of('/a/b').relativize(Path.of('/a/b/c/d')) gives c/d—the relative path to walk from /a/b to /a/b/c/d. Useful for generating relative links or relative import paths. normalize() removes redundant elements: Path.of('/home/user/../user/documents').normalize() gives /home/user/documents. It doesn't make system calls—it just operates on the string representation. For the real resolved path including symlinks, use toRealPath() which does make system calls. A common security pattern: resolve() a user-provided path onto a base directory, then check that normalize().startsWith(baseDir) to prevent directory traversal attacks."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": [
              "resolve(other): append relative path to base path",
              "resolve(absolute): returns the absolute path (not appended)",
              "relativize(target): compute relative path from this path to target",
              "normalize(): remove . and .. components from path string",
              "normalize() is string-only—no OS calls; toRealPath() resolves symlinks with OS call",
              "Security: resolve + normalize + startsWith check prevents directory traversal"
            ]
          }
        ]
      },
      "speakable_v2": {
        "pillar": "P01",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "A",
        "depth_marker": "nuance",
        "hook": "resolve, relativize, and normalize are the three path-math methods — each solves a different puzzle.",
        "beats": [
          {
            "layout": "mini_table",
            "heading": "What each method does",
            "columns": ["Method", "What it does", "Example result"],
            "rows": [
              ["resolve(\"sub/file\")", "Appends a relative path to a base", "/home/user/sub/file"],
              ["relativize(other)", "Computes relative path from this to other", "sub/file"],
              ["normalize()", "Removes . and .. from the path string", "/home/user/docs"]
            ]
          },
          {
            "layout": "paragraph",
            "content": "normalize() works purely on the string — no OS calls, no symlink resolution. If you need the actual canonical path including symlinks, use toRealPath() which does make an OS call and requires the path to exist."
          },
          {
            "layout": "paragraph",
            "content": "Security bonus: when accepting a file path from user input, resolve it onto a safe base directory, then call normalize, and check that the result still starts with the base. That pattern prevents directory traversal attacks where an attacker passes paths containing dots-dots to escape the intended directory."
          }
        ],
        "familiarity_anchor": "resolve is like combining two addresses; relativize is finding directions from A to B; normalize removes the pointless detours.",
        "standard_example": "Java 17 app reading configuration files and writing logs"
      },
      "followup_questions": [
        "What is the difference between Path.normalize() and Path.toRealPath()?",
        "How do you prevent directory traversal attacks using Path?"
      ],
      "seo": {
        "metaTitle": "Path.resolve() relativize() normalize() in Java NIO2",
        "metaDescription": "Learn how Java NIO2 Path.resolve(), relativize(), and normalize() work for path manipulation, with security implications for directory traversal prevention."
      },
      "order": 4
    },
    {
      "id": "jbf-io-nio-path-files-005",
      "slug": "watchservice-file-monitoring",
      "question": "What is WatchService in Java NIO2 and what is it used for?",
      "title": "WatchService for File System Monitoring",
      "direct_answer": "WatchService is a NIO2 API that monitors a directory for changes (file created, modified, deleted) and delivers events; used in applications that react to file system changes like hot-reload configuration.",
      "layout_type": "default",
      "difficulty": "medium",
      "importance": "medium",
      "reading_time_minutes": 2,
      "last_updated": "2025-01-01",
      "interviewer_intent": {
        "testing": "Awareness of a NIO2 feature with no java.io.File equivalent",
        "common_mistake": "Not knowing WatchService exists and suggesting polling with Files.exists() in a loop",
        "to_stand_out": "Mention Spring Boot's DevTools uses WatchService internally for hot-reload, and configuration libraries like Typesafe Config"
      },
      "company_tags": [
        "amazon",
        "microsoft"
      ],
      "answer": {
        "sections": [
          {
            "type": "speakable_answer",
            "title": "How to Answer",
            "content": "WatchService is a NIO2 API introduced in Java 7 that lets you register a directory to receive notifications when files inside it are created, modified, or deleted. The API works by registering a Path with the WatchService for specific events: dir.register(watchService, StandardWatchEventKinds.ENTRY_CREATE, ENTRY_MODIFY, ENTRY_DELETE). You then poll the WatchService in a loop: WatchKey key = watchService.take() blocks until an event occurs, then you iterate over key.pollEvents() to get the individual events. Each event has a kind (created/modified/deleted) and a context (the filename). After processing, you reset the key so it can receive more events. Real-world uses: Spring Boot DevTools watches your classpath for changes and hot-reloads classes when source is compiled. Configuration libraries watch for config file changes and reload without restart. File upload processors watch an inbox directory for new files. IDE plugins watch source files for external changes. The WatchService uses OS-native file watching APIs (inotify on Linux, FSEvents on macOS) so it's efficient—not polling."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": [
              "WatchService: register directory → receive events for ENTRY_CREATE, ENTRY_MODIFY, ENTRY_DELETE",
              "dir.register(watchService, StandardWatchEventKinds.*) to subscribe",
              "watchService.take(): blocks until event occurs",
              "key.pollEvents(): get individual file change events",
              "Must call key.reset() to receive future events on same key",
              "Uses OS-native notifications (inotify, FSEvents)—not polling",
              "Used by: Spring DevTools, config reload libraries, file inbox processors"
            ]
          }
        ]
      },
      "speakable_v2": {
        "pillar": "P01",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "A",
        "depth_marker": "when-to-use",
        "hook": "WatchService lets Java react to file changes without polling — the OS taps your app on the shoulder.",
        "beats": [
          {
            "layout": "paragraph",
            "content": "WatchService registers a directory with the OS and blocks until a file change event arrives. You get notified when files are created, modified, or deleted inside that directory. Under the hood it uses OS-native APIs — inotify on Linux, FSEvents on macOS — so there's no polling loop burning CPU."
          },
          {
            "layout": "bullets",
            "heading": "How the API works, step by step",
            "items": [
              "Register a directory path with the WatchService for specific event kinds",
              "Call watchService.take() — it blocks until an event fires",
              "Call key.pollEvents() to get the individual file change events",
              "Call key.reset() after processing, or the key stops receiving future events"
            ]
          },
          {
            "layout": "paragraph",
            "content": "You've already used this indirectly: Spring Boot DevTools uses WatchService to detect recompiled class files and trigger hot-reload. Configuration libraries like Typesafe Config use it to reload application.conf when the file changes. Knowing it exists is the interview differentiator — most candidates suggest a polling loop."
          }
        ],
        "familiarity_anchor": "WatchService is like a doorbell for your file system — instead of constantly checking if someone's there, you wait to be notified.",
        "standard_example": "Java 17 app reading configuration files and writing logs"
      },
      "followup_questions": [
        "How does Spring Boot DevTools use WatchService?",
        "What is the difference between WatchService.take() and WatchService.poll()?"
      ],
      "seo": {
        "metaTitle": "Java NIO2 WatchService for File System Monitoring | Interview",
        "metaDescription": "Learn how Java WatchService monitors directories for file changes, how to use it, and real-world applications like Spring DevTools and config hot-reload."
      },
      "order": 5
    }
  ]
}

[
  {
    "id": "crud-repository-001",
    "slug": "crudrepository-in-spring-data-jpa",
    "question": "What is CrudRepository in Spring Data and what methods does it provide?",
    "title": "CrudRepository in Spring Data JPA",
    "direct_answer": "**CrudRepository<T, ID>** is Spring Data's base interface providing the core CRUD methods: `save()`, `findById()`, `findAll()`, `count()`, `delete()`, `existsById()`. You only declare the interface — Spring **generates the implementation at runtime** (a proxy backed by `SimpleJpaRepository`), so no DAO boilerplate. `findById` returns `Optional<T>` and `findAll` returns `Iterable<T>`.",
    "layout_type": "concept-explainer",
    "difficulty": "easy",
    "importance": "high",
    "reading_time_minutes": 3,
    "last_updated": "2026-06-04",
    "interviewer_intent": {
      "testing": "Whether you understand the repository abstraction and that Spring auto-generates the implementation from an interface",
      "common_mistake": "Writing a CustomerRepositoryImpl class by hand, or confusing CrudRepository with JpaRepository",
      "to_stand_out": "Mention runtime proxy generation backed by SimpleJpaRepository, the Optional return of findById, and the Iterable return of findAll"
    },
    "company_tags": [
      "amazon",
      "tcs",
      "infosys",
      "cognizant"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "CrudRepository: The Foundation of Spring Data",
          "content": "**`CrudRepository`** is Spring Data's base interface providing the essential **CRUD (Create, Read, Update, Delete)** operations for any entity. Instead of writing JDBC/DAO boilerplate per table, you declare an interface extending `CrudRepository<Entity, ID>` and Spring generates a working implementation at runtime.\n\nThat's **repository abstraction**: Spring translates calls like `save()` or `findById()` into SQL through JPA and Hibernate. You get `save()` (insert/update), `findById()` (by primary key, returns `Optional`), `findAll()` (returns `Iterable`), `delete()`, `count()`, and `existsById()`. The interface is the contract; Spring provides the execution."
        },
        {
          "type": "concept_map",
          "title": "CrudRepository Methods at a Glance",
          "content": "amber|save(entity)|~Insert or update|new id → INSERT|existing id → UPDATE|one method, both jobs\nblue|findById(id)|~Read by PK|returns Optional<T>|never null|forces null-safety\nemerald|findAll()|~Read all|returns Iterable<T>|not a List|convert or use JpaRepository\nviolet|delete / deleteById|~Remove|by entity or id|silent if missing|no exception on absent id\ncyan|count / existsById|~Helpers|count() = long|existsById = boolean|cheap checks"
        },
        {
          "type": "code_example",
          "title": "Declaring and Using a CrudRepository",
          "content": "```java\n// Spring Boot 3.2 + Java 17\n@Entity\n@Table(name = \"customers\")\npublic class Customer {\n    @Id @GeneratedValue(strategy = GenerationType.IDENTITY)\n    private Long id;\n    private String name;\n    private String email;\n    // getters/setters\n}\n\n// No implementation needed — Spring generates it at runtime\npublic interface CustomerRepository extends CrudRepository<Customer, Long> { }\n\n@Service\npublic class CustomerService {\n    private final CustomerRepository repository;\n    CustomerService(CustomerRepository r) { this.repository = r; }\n\n    public Customer create(String name, String email) {\n        var c = new Customer();\n        c.setName(name); c.setEmail(email);\n        return repository.save(c);            // INSERT (id was null)\n    }\n\n    public Optional<Customer> get(Long id) {\n        return repository.findById(id);       // SELECT by PK → Optional\n    }\n\n    public void remove(Long id) {\n        repository.deleteById(id);            // DELETE\n    }\n}\n```"
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **Writing an `Impl` class** — `CustomerRepositoryImpl` causes bean conflicts; Spring generates the implementation for you.\n- **Confusing it with JpaRepository** — `CrudRepository` has no pagination/sorting or `flush()`; for those, extend `JpaRepository`.\n- **Wrong ID generic** — `CrudRepository<Customer, String>` when `@Id` is `Long` causes runtime errors; the second type must match the id type.\n- **Expecting a `List`** — `findAll()` returns `Iterable<T>`; convert it or use `JpaRepository` for `List`."
        },
        {
          "type": "key_points",
          "title": "Key Points",
          "content": "- **`CrudRepository<T, ID>`** — T is the entity, ID is the primary-key type; declare the interface only.\n- **`save()`** inserts when id is null, updates when id exists.\n- **`findById()` returns `Optional<T>`** — forces null-safe handling.\n- **`findAll()` returns `Iterable<T>`**, not `List`.\n- **Implementation is generated at runtime** via a proxy backed by `SimpleJpaRepository`."
        },
        {
          "type": "speakable_answer",
          "title": "How to Answer This Verbally",
          "content": "CrudRepository is Spring Data's core interface that gives you the basic database operations without writing any code. I just create an interface that extends CrudRepository and specify the entity type and the id type, and Spring does the rest. It provides save for inserting or updating, findById to fetch by primary key, findAll to read everything, delete to remove records, and helpers like count and existsById. The interesting part is what happens at startup. Spring scans my interfaces, sees they extend CrudRepository, and generates a concrete implementation in memory using a proxy. That proxy is backed by a class called SimpleJpaRepository, which holds the real Hibernate logic, and it gets registered as a bean so I can inject it anywhere. I never write a repository implementation myself. For example, if I have a Customer entity with a Long id, I write interface CustomerRepository extends CrudRepository of Customer and Long, and immediately I can call save and findById. Two details worth knowing: findById returns an Optional so I'm forced to handle the missing case safely, and findAll returns an Iterable rather than a List. If I need pagination, sorting, or batch operations, I'd upgrade to JpaRepository, which extends CrudRepository and adds more methods and a List return type."
        }
      ]
    },
    "followup_questions": [
      "What is the difference between CrudRepository and JpaRepository?",
      "Why does findById return Optional?",
      "How does Spring Data generate the repository implementation?"
    ],
    "seo": {
      "metaTitle": "CrudRepository Spring Data JPA | Java Interview Questions for Freshers",
      "metaDescription": "Learn CrudRepository in Spring Data JPA — available CRUD methods, how Spring generates implementations, and save() insert vs update behavior."
    },
    "order": 1,
    "speakable_v2": {
      "archetype": "A",
      "pillar": "P02",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 app with Hibernate and PostgreSQL",
      "familiarity_anchors": [
        "JPA",
        "Hibernate",
        "database"
      ],
      "hook": "You write an interface with zero methods inside it, and Spring hands you a fully working data layer — that's CrudRepository.",
      "beats": [
        {
          "kind": "what_it_is",
          "layout": "paragraph",
          "text": "CrudRepository is Spring Data's base interface that provides the core CRUD operations: save, findById, findAll, count, delete, and existsById. You declare the interface and Spring generates the implementation at runtime."
        },
        {
          "kind": "parts_or_states",
          "layout": "bullets",
          "items": [
            "save inserts or updates, findById returns an Optional, findAll returns an Iterable",
            "Spring generates a proxy backed by SimpleJpaRepository and registers it as a bean"
          ]
        },
        {
          "kind": "example",
          "layout": "paragraph",
          "text": "For a Customer with a Long id, I write an interface extending CrudRepository of Customer and Long, and I can immediately call save and findById."
        },
        {
          "kind": "pitfalls",
          "layout": "bullets",
          "items": [
            "Writing your own implementation class, which causes bean conflicts",
            "Confusing CrudRepository with JpaRepository, which adds paging and batch methods"
          ]
        }
      ],
      "cap": " Spring generates the implementation automatically at runtime.",
      "followup_handoff": [
        "Can you explain how Spring generates the implementation?",
        "What would you say if asked why findById returns Optional?"
      ],
      "tts_overrides": {},
      "speakable_status": "pending_review"
    }
  },
  {
    "id": "crud-repository-004",
    "slug": "findbyid-return-type-optional-in-spring-data",
    "question": "What does findById return and why does it return Optional?",
    "title": "findById Return Type: Optional in Spring Data",
    "direct_answer": "`findById()` returns **`Optional<T>`**, not `T` or `null`. This makes absence explicit at compile time and prevents **NullPointerException**. Handle it with **`orElseThrow()`** (best for REST → 404), **`orElse(default)`**, or **`map()`**. Never call **`.get()`** unguarded — it throws `NoSuchElementException`, which is just as bad as an NPE.",
    "layout_type": "concept-explainer",
    "difficulty": "easy",
    "importance": "high",
    "reading_time_minutes": 3,
    "last_updated": "2026-06-04",
    "interviewer_intent": {
      "testing": "Whether you understand Optional as a null-safety contract and can unwrap it idiomatically in a REST flow",
      "common_mistake": "Calling .get() directly (NoSuchElementException) or using isPresent() then get() instead of functional methods",
      "to_stand_out": "Show orElseThrow() mapping to an HTTP 404 via @ControllerAdvice, and note Optional shouldn't be an entity field"
    },
    "company_tags": [
      "amazon",
      "tcs",
      "infosys",
      "cognizant"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "Why findById Returns Optional",
          "content": "**`Optional<T>`** (Java 8) is a container that either holds a value or is empty — it makes **null-safety explicit**. An entity may not exist, and the old `null`-return approach caused countless **NullPointerExceptions** when callers forgot to check. By returning `Optional<T>`, Spring Data forces you to acknowledge absence: you must unwrap with `isPresent()`, `orElse()`, `orElseThrow()`, or `map()` before using the value.\n\nSpring Data uses it for every single-result query that might find nothing: `findById`, query-by-example, and custom single-object methods."
        },
        {
          "type": "concept_map",
          "title": "Handling Optional at a Glance",
          "content": "amber|orElseThrow()|~Must-exist case|throws on empty|maps to HTTP 404|best for REST controllers\nblue|orElse(default)|~Fallback value|returns default if empty|e.g. guest profile|safe substitution\nemerald|map() / flatMap()|~Transform inside|no unwrap needed|chains safely|stays Optional\nrose|.get() alone|~Anti-pattern|NoSuchElementException|defeats Optional|never do this"
        },
        {
          "type": "before_code",
          "title": "Wrong — .get() Without Checking",
          "content": "```java\n@GetMapping(\"/{id}\")\npublic Order getOrder(@PathVariable Long id) {\n    return repository.findById(id).get();\n    // NoSuchElementException if absent → 500 error, just like an NPE\n}\n```"
        },
        {
          "type": "after_code",
          "title": "Right — orElseThrow() Maps to 404",
          "content": "```java\n@GetMapping(\"/{id}\")\npublic ResponseEntity<Order> getOrder(@PathVariable Long id) {\n    Order order = repository.findById(id)\n        .orElseThrow(() -> new EntityNotFoundException(\"Order not found: \" + id));\n    return ResponseEntity.ok(order);\n}\n\n// Or transform without unwrapping:\nString status = repository.findById(id)\n        .map(Order::getStatus)\n        .orElse(\"UNKNOWN\");\n```\nA `@ControllerAdvice` handler turns `EntityNotFoundException` into an HTTP 404."
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **`.get()` without a check** — throws `NoSuchElementException`; defeats the whole point of Optional.\n- **Returning `Optional<Order>` from a controller** — unwrap in the controller; return `ResponseEntity` or the entity.\n- **`isPresent()` then `get()`** — verbose; prefer `ifPresent()`, `map()`, or `orElseThrow()`.\n- **Optional as an entity field or method parameter** — it's not `Serializable`; use it only as a return type."
        },
        {
          "type": "key_points",
          "title": "Key Points",
          "content": "- **`Optional<T>`** replaces `null` to make absence explicit and prevent NPEs.\n- **`orElseThrow()`** is the REST default — empty → exception → HTTP 404 via `@ControllerAdvice`.\n- **`orElse(default)`** supplies a fallback; **`map()`** transforms without unwrapping.\n- **Never `.get()` unguarded** — it throws `NoSuchElementException`.\n- **Don't use Optional** as an entity field or parameter — return type only."
        },
        {
          "type": "speakable_answer",
          "title": "How to Answer This Verbally",
          "content": "findById returns an Optional of the entity instead of the entity directly or null. The reason is null-safety. If a method returns null when the row doesn't exist, callers often forget to check, and you get a NullPointerException at runtime that crashes the app. Optional forces me to handle the missing case explicitly — I can't just call methods on the result, I have to unwrap it first. The most common pattern in a REST API is orElseThrow, where I throw an exception if the entity isn't found, and my exception handler turns that into a 404 response. So I'd write repository dot findById id dot orElseThrow with a lambda that builds an EntityNotFoundException. If I have a sensible default, I use orElse and provide a fallback value. And if I just want to read one field, I use map to transform the value while it's still inside the Optional, then orElse a default. The biggest mistake freshers make is calling get directly without checking. That throws a NoSuchElementException, which is just as bad as a null pointer. So I never call get on its own — I use orElseThrow, orElse, or the functional methods like map and ifPresent, which are cleaner than checking isPresent and then calling get. One more thing: Optional is meant for return types, not for entity fields or method parameters."
        }
      ]
    },
    "followup_questions": [
      "What is Java Optional?",
      "What is the difference between orElse() and orElseThrow()?",
      "How do you create a 404 response from a missing entity in Spring Boot?"
    ],
    "seo": {
      "metaTitle": "findById Optional Return Type Spring Data | Java Interview Questions for Freshers",
      "metaDescription": "Learn why Spring Data findById returns Optional<T>, how to handle it correctly with orElseThrow and orElse, and best practices for REST APIs."
    },
    "order": 4,
    "speakable_v2": {
      "archetype": "A",
      "pillar": "P02",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 app with Hibernate and PostgreSQL",
      "familiarity_anchors": [
        "JPA",
        "Hibernate",
        "database"
      ],
      "hook": "Calling get on an empty Optional is the same trap as a null pointer, just wearing a different exception name.",
      "beats": [
        {
          "kind": "what_it_is",
          "layout": "paragraph",
          "text": "findById returns an Optional instead of the entity or null, which forces the caller to handle the case where the entity doesn't exist and prevents null pointer exceptions."
        },
        {
          "kind": "parts_or_states",
          "layout": "bullets",
          "items": [
            "Use orElseThrow when the entity must exist, which maps cleanly to a 404",
            "Use orElse for a fallback or map to transform the value without unwrapping"
          ]
        },
        {
          "kind": "example",
          "layout": "paragraph",
          "text": "In a controller, findById then orElseThrow with an EntityNotFoundException lets a controller advice handler return a 404."
        },
        {
          "kind": "pitfalls",
          "layout": "bullets",
          "items": [
            "Calling get without checking, which throws a NoSuchElementException",
            "Returning the Optional itself from the controller instead of unwrapping it"
          ]
        }
      ],
      "cap": " Use isPresent, orElseThrow, or orElse to handle the result.",
      "followup_handoff": [
        "Can you explain orElse versus orElseThrow?",
        "What would you say if asked how to return a 404 for a missing entity?"
      ],
      "tts_overrides": {},
      "speakable_status": "pending_review"
    }
  },
  {
    "id": "crud-repository-006",
    "slug": "crudrepository-vs-jparepository-vs-pagingandsortingrepository",
    "question": "What is the difference between CrudRepository, JpaRepository, and PagingAndSortingRepository?",
    "title": "CrudRepository vs JpaRepository vs PagingAndSortingRepository",
    "direct_answer": "It's a three-level hierarchy: **CrudRepository** (basic CRUD) → **PagingAndSortingRepository** (adds `findAll(Pageable)` and `findAll(Sort)`) → **JpaRepository** (adds JPA extras: `flush()`, `saveAndFlush()`, `deleteAllInBatch()`, and `List` returns). For standard Spring Data JPA, **extend JpaRepository** — it includes everything.",
    "layout_type": "comparison-arena",
    "difficulty": "easy",
    "importance": "high",
    "reading_time_minutes": 3,
    "last_updated": "2026-06-04",
    "interviewer_intent": {
      "testing": "Whether you know the repository hierarchy and that JpaRepository transitively includes paging/sorting",
      "common_mistake": "Picking PagingAndSortingRepository for pagination without realizing JpaRepository already includes it plus batch methods",
      "to_stand_out": "Recommend JpaRepository as the default and explain deleteAllInBatch() vs deleteAll() for bulk performance"
    },
    "company_tags": [
      "amazon",
      "tcs",
      "infosys",
      "microsoft"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "The Spring Data Repository Hierarchy",
          "content": "Spring Data JPA layers three interfaces. **`CrudRepository`** is the base — `save()`, `findById()`, `deleteById()`. **`PagingAndSortingRepository`** extends it and adds `findAll(Pageable)` (pagination) and `findAll(Sort)` (sorting). **`JpaRepository`** sits on top, extending paging/sorting and adding JPA-specific methods: `flush()`, `saveAndFlush()`, `deleteAllInBatch()`, plus `List` return types.\n\nFor most Spring Boot apps you extend **`JpaRepository`** — it includes all CRUD, all paging/sorting, and the JPA extras in one place."
        },
        {
          "type": "concept_map",
          "title": "Hierarchy at a Glance",
          "content": "amber|CrudRepository|~Level 1|save, findById, delete|Iterable returns|basic CRUD only\nblue|PagingAndSortingRepository|~Level 2|findAll(Pageable)|findAll(Sort)|extends CrudRepository\nemerald|JpaRepository|~Level 3 (top)|flush, saveAndFlush|deleteAllInBatch, List returns|the production default\nviolet|Rule|~What to pick|extend JpaRepository|includes everything below|drop down only if needed"
        },
        {
          "type": "comparison_table",
          "title": "Repository Interfaces Compared",
          "content": "| Feature | CrudRepository | PagingAndSortingRepository | JpaRepository |\n|---|---|---|---|\n| Basic CRUD | Yes | Inherited | Inherited |\n| Pagination | No | Yes (`findAll(Pageable)`) | Inherited |\n| Sorting | No | Yes (`findAll(Sort)`) | Inherited |\n| Batch ops | No | No | Yes (`deleteAllInBatch`) |\n| Flush control | No | No | Yes (`flush`, `saveAndFlush`) |\n| `findAll` returns | `Iterable<T>` | `Iterable<T>` | `List<T>` |\n\nJpaRepository is a transitive superset of the other two."
        },
        {
          "type": "before_code",
          "title": "Limited — CrudRepository Can't Paginate",
          "content": "```java\npublic interface OrderRepository extends CrudRepository<Order, Long> { }\n\n// No findAll(Pageable) available — you can't paginate without changing the interface\n// Iterable<Order> all = repository.findAll(); // and it's not even a List\n```"
        },
        {
          "type": "after_code",
          "title": "Complete — JpaRepository Has It All",
          "content": "```java\npublic interface OrderRepository extends JpaRepository<Order, Long> { }\n\n// Paging + sorting (from PagingAndSortingRepository):\nPage<Order> page = repository.findAll(PageRequest.of(0, 20, Sort.by(\"createdAt\")));\n\n// JPA extras:\nrepository.saveAndFlush(order);          // write immediately\nrepository.deleteAllInBatch(oldOrders);  // single DELETE, not N loads\nList<Order> list = repository.findAll(); // List, not Iterable\n```"
        },
        {
          "type": "when_to_use",
          "title": "When to Use Each",
          "content": "Use **`CrudRepository`** only for a truly minimal service with no paging — rare in practice. Use **`PagingAndSortingRepository`** if you want paging without JPA extras (e.g. a non-JPA Spring Data module). For standard Spring Data JPA, always extend **`JpaRepository`**: it adds `deleteAllInBatch()` (one DELETE statement for bulk cleanup), `saveAndFlush()` (force a mid-transaction sync), and convenient `List` returns. Starting with JpaRepository avoids a later interface change when you inevitably need pagination."
        },
        {
          "type": "key_points",
          "title": "Key Points",
          "content": "- **CrudRepository** — basic CRUD, `Iterable` returns.\n- **PagingAndSortingRepository** — adds `findAll(Pageable)` and `findAll(Sort)`.\n- **JpaRepository** — adds `flush()`, `saveAndFlush()`, `deleteAllInBatch()`, `List` returns.\n- **JpaRepository includes everything** below it — extend it by default.\n- **`deleteAllInBatch()`** is one SQL DELETE; `deleteAll()` loads each entity first."
        },
        {
          "type": "speakable_answer",
          "title": "How to Answer This Verbally",
          "content": "These three interfaces form a hierarchy. CrudRepository is the base — it gives you the basic operations like save, findById, findAll, and deleteById. That's enough for simple cases, but most apps need more. PagingAndSortingRepository extends CrudRepository and adds two methods: findAll with a Pageable for pagination and findAll with a Sort for sorting. Those are essential for REST APIs returning large lists. At the top, JpaRepository extends PagingAndSortingRepository and adds JPA-specific methods — flush to force Hibernate to sync changes immediately, saveAndFlush to save and flush in one call, and batch operations like deleteAllInBatch that delete rows in a single SQL statement instead of loading each entity. JpaRepository also changes findAll's return type from Iterable to List, which is easier to work with. In practice, for any Spring Data JPA project I extend JpaRepository, because it includes everything from the other two plus the extras. The hierarchy exists for flexibility across different Spring Data modules, but JpaRepository is the standard choice. One thing freshers miss is that deleteAllInBatch is far faster for bulk deletes because it doesn't load entities into memory — it just runs a delete statement. And if I start with CrudRepository and later need pagination, I'd have to change the interface anyway, so starting with JpaRepository saves that rework."
        }
      ]
    },
    "followup_questions": [
      "What does JpaRepository add over CrudRepository?",
      "How does findAll(Pageable) work?",
      "What is getReferenceById() vs findById()?"
    ],
    "seo": {
      "metaTitle": "CrudRepository vs JpaRepository vs PagingAndSortingRepository | Java Interview Questions for Freshers",
      "metaDescription": "Compare Spring Data repository interfaces CrudRepository, PagingAndSortingRepository, and JpaRepository — hierarchy, methods, and when to use each."
    },
    "order": 6,
    "speakable_v2": {
      "archetype": "B",
      "pillar": "P02",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 app with Hibernate and PostgreSQL",
      "familiarity_anchors": [
        "JPA",
        "Hibernate",
        "database"
      ],
      "hook": "Pick PagingAndSortingRepository for pagination and you've under-shot — JpaRepository already includes it plus batch deletes and flush control.",
      "beats": [
        {
          "kind": "what_each_is",
          "layout": "paragraph",
          "text": "CrudRepository is basic CRUD. PagingAndSortingRepository extends it with pagination and sorting. JpaRepository extends both and adds JPA extras like flush, saveAndFlush, and batch deletes."
        },
        {
          "kind": "differences",
          "layout": "bullets",
          "items": [
            "PagingAndSortingRepository adds findAll with a Pageable and findAll with a Sort",
            "JpaRepository adds flush, saveAndFlush, deleteAllInBatch, and List return types"
          ]
        },
        {
          "kind": "when_to_pick",
          "layout": "paragraph",
          "text": "Extend JpaRepository by default since it transitively includes everything below it."
        },
        {
          "kind": "tiny_example",
          "layout": "paragraph",
          "text": "deleteAllInBatch runs a single delete statement, while deleteAll loads every entity into memory first."
        }
      ],
      "cap": " Use JpaRepository for most Spring Data JPA work.",
      "followup_handoff": [
        "Can you explain what JpaRepository adds over CrudRepository?",
        "What would you say if asked how findAll with a Pageable works?"
      ],
      "tts_overrides": {},
      "speakable_status": "pending_review"
    }
  },
  {
    "id": "crud-repository-002",
    "slug": "how-spring-data-generates-repository-implementations",
    "question": "How does Spring Data JPA generate repository implementations automatically?",
    "title": "How Spring Data Generates Repository Implementations",
    "direct_answer": "At startup, Spring Data scans interfaces extending `CrudRepository`/`JpaRepository`, builds a **runtime proxy** for each (backed by **`SimpleJpaRepository`**), and registers it as a **bean**. There are **no generated `.class` files** — it's in-memory proxying. Derived method names (e.g. `findByEmail`) are parsed into queries at startup; the proxy holds an `EntityManager` to run them.",
    "layout_type": "architecture-map",
    "difficulty": "medium",
    "importance": "high",
    "reading_time_minutes": 3,
    "last_updated": "2026-06-04",
    "interviewer_intent": {
      "testing": "Whether you understand Spring's runtime proxy mechanism rather than imagining compile-time code generation",
      "common_mistake": "Believing Spring generates .java/.class files at build time, or writing an Impl class for the whole interface",
      "to_stand_out": "Name SimpleJpaRepository as the backing class, @EnableJpaRepositories as the trigger, and the proxy self-invocation caveat"
    },
    "company_tags": [
      "amazon",
      "microsoft",
      "tcs",
      "infosys"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "Proxy-Based Repository Generation",
          "content": "Spring Data JPA creates repository implementations via **runtime proxies**. At startup, the scanning triggered by **`@EnableJpaRepositories`** (auto-configured in Spring Boot) discovers every interface extending `CrudRepository`/`JpaRepository`.\n\nFor each, Spring builds a **proxy** that delegates to **`SimpleJpaRepository`** — the concrete class holding the real Hibernate/JPA logic — and registers it as a **bean**. This happens **at runtime**, not compile time: no `.class` files are generated. Derived method names like `findByEmailAndStatus` are parsed into queries during startup, and the proxy uses an injected **`EntityManager`** to execute them."
        },
        {
          "type": "architecture_diagram",
          "title": "How a Repository Call Flows",
          "content": "```mermaid\nflowchart TB\n    A[\"@EnableJpaRepositories<br/>(auto-configured)\"] -->|\"scans interfaces\"| B[\"OrderRepository<br/>(your interface)\"]\n    B -->|\"creates\"| C[\"Runtime proxy bean\"]\n    C -->|\"delegates to\"| D[\"SimpleJpaRepository\"]\n    D -->|\"uses\"| E[\"EntityManager\"]\n    E -->|\"JDBC + dialect\"| F[\"Database\"]\n```"
        },
        {
          "type": "phase",
          "title": "What Happens at Startup",
          "content": "- **Scan** — `@EnableJpaRepositories` finds repository interfaces on the classpath\n- **Parse** — derived method names are validated and turned into JPQL\n- **Proxy** — a proxy is created per interface, backed by `SimpleJpaRepository`\n- **Register** — the proxy becomes a singleton bean, injectable by constructor\n- **Run** — a call like `save()` hits the proxy → `SimpleJpaRepository.save()` → `EntityManager`"
        },
        {
          "type": "code_example",
          "title": "You Declare; Spring Implements",
          "content": "```java\n// You write just this:\npublic interface OrderRepository extends JpaRepository<Order, Long> {\n    List<Order> findByStatus(String status); // parsed into JPQL at startup\n}\n\n@Service\npublic class OrderService {\n    private final OrderRepository repository; // injected proxy bean\n    OrderService(OrderRepository r) { this.repository = r; }\n\n    public Order place(Order o) {\n        return repository.save(o); // proxy → SimpleJpaRepository.save() → EntityManager\n    }\n}\n// No OrderRepositoryImpl exists — check target/classes, there's nothing there.\n```"
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **Writing a full `Impl` class** — `OrderRepositoryImpl` for the whole interface causes bean conflicts; implement only custom fragments.\n- **Expecting compile-time code** — no `.java`/`.class` files are generated; it's runtime proxies.\n- **Self-invocation surprises** — `@Transactional` works through the same proxy, so same-class internal calls bypass it.\n- **Mistyping a derived method** — `findByEmial` fails at startup; Spring validates names against entity fields."
        },
        {
          "type": "key_points",
          "title": "Key Points",
          "content": "- **Runtime proxies** are created at startup — not compile-time code generation.\n- **`SimpleJpaRepository`** is the backing implementation the proxy delegates to.\n- **`@EnableJpaRepositories`** triggers scanning (auto-configured in Spring Boot).\n- **Derived method names** are parsed into JPQL during startup using an `EntityManager`.\n- **No `.class` files** are produced; same-class self-invocation bypasses proxy behavior."
        },
        {
          "type": "speakable_answer",
          "title": "How to Answer This Verbally",
          "content": "Spring Data JPA generates repository implementations at runtime using proxies. When the app starts, it scans for any interface that extends CrudRepository or JpaRepository. For each one, Spring creates a proxy object in memory that implements my interface, and that proxy delegates all calls to a class called SimpleJpaRepository, which contains the real database logic using Hibernate and JPA. The proxy is then registered as a bean so I can inject it anywhere. It's important that this happens at runtime, not during compilation — Spring doesn't write any Java or class files into my target folder. The whole thing is triggered by the EnableJpaRepositories annotation, which Spring Boot auto-configures. When I call repository dot save, I'm really calling a proxy method that forwards to SimpleJpaRepository dot save, which talks to the EntityManager to run the SQL. For derived queries, Spring parses the method name at startup — so findByEmailAndStatus becomes a JPQL query — and it validates the field names against my entity, which is why a typo in the method name fails fast at startup. The common misconception is that I need to write the implementation class myself. I don't. The only time I write implementation code is for a custom method Spring can't derive, and even then I implement just that one method in a separate fragment, not the whole interface. One caveat: because transactions also go through the proxy, calling a transactional repository or service method from inside the same class bypasses the proxy."
        }
      ]
    },
    "followup_questions": [
      "What is SimpleJpaRepository?",
      "What is @EnableJpaRepositories?",
      "How does Spring Data parse derived query method names?"
    ],
    "seo": {
      "metaTitle": "How Spring Data Generates Repositories | Java Interview Questions for Freshers",
      "metaDescription": "Learn how Spring Data JPA automatically generates repository implementations using dynamic proxies, SimpleJpaRepository, and EntityManager."
    },
    "order": 2,
    "speakable_v2": {
      "archetype": "A",
      "pillar": "P02",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 app with Hibernate and PostgreSQL",
      "familiarity_anchors": [
        "JPA",
        "Hibernate",
        "database"
      ],
      "hook": "Look in your target folder for the repository implementation and you'll find nothing — Spring builds it in memory as a proxy.",
      "beats": [
        {
          "kind": "what_it_is",
          "layout": "paragraph",
          "text": "Spring Data uses proxy-based generation. At startup it scans repository interfaces, creates a proxy for each backed by SimpleJpaRepository, and registers it as a bean — all at runtime with no code to write."
        },
        {
          "kind": "parts_or_states",
          "layout": "bullets",
          "items": [
            "The proxy delegates to SimpleJpaRepository, which holds the real Hibernate logic",
            "Derived method names are parsed into JPQL at startup using an EntityManager"
          ]
        },
        {
          "kind": "example",
          "layout": "paragraph",
          "text": "Calling save really calls the proxy, which forwards to SimpleJpaRepository's save, which uses the EntityManager to run the SQL."
        },
        {
          "kind": "pitfalls",
          "layout": "bullets",
          "items": [
            "Thinking Spring generates Java or class files at build time",
            "Writing an implementation class for the whole interface and causing bean conflicts"
          ]
        }
      ],
      "cap": " The proxy is backed by SimpleJpaRepository.",
      "followup_handoff": [
        "Can you explain what SimpleJpaRepository is?",
        "What would you say if asked how derived query names are parsed?"
      ],
      "tts_overrides": {},
      "speakable_status": "pending_review"
    }
  },
  {
    "id": "crud-repository-003",
    "slug": "spring-data-save-insert-vs-update-decision",
    "question": "How does Spring Data's save() method decide to INSERT or UPDATE?",
    "title": "Spring Data save() INSERT vs UPDATE Decision",
    "direct_answer": "`save()` checks **`isNew()`**: if the entity is new (`@Id` is `null` for objects or `0` for primitives) it calls **`persist()`** → INSERT; otherwise it calls **`merge()`** → UPDATE. `merge()` does a **SELECT first**. For manually assigned IDs, implement **`Persistable`** to override `isNew()` and avoid that extra SELECT.",
    "layout_type": "concept-explainer",
    "difficulty": "medium",
    "importance": "high",
    "reading_time_minutes": 3,
    "last_updated": "2026-06-04",
    "interviewer_intent": {
      "testing": "Whether you understand the new-vs-existing decision and the persist/merge mechanics behind save()",
      "common_mistake": "Assuming save() always inserts, or not knowing merge() runs a SELECT before the UPDATE",
      "to_stand_out": "Explain Persistable/isNew() for manually assigned IDs and that dirty checking can update without calling save()"
    },
    "company_tags": [
      "amazon",
      "tcs",
      "infosys",
      "cognizant"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "How save() Chooses INSERT or UPDATE",
          "content": "`save()` does both: **INSERT** for new entities, **UPDATE** for existing ones, in one call. It decides via **`isNew()`** from the `Persistable` contract. By default an entity is new when its **`@Id` is `null`** (object types) or **`0`** (primitives).\n\nIf new → `EntityManager.persist()` → INSERT (and Hibernate fills the generated id). If not new → `EntityManager.merge()` → UPDATE — and `merge()` runs a **SELECT first** to load the row. That's why you set the id to `null` when creating, and why repeatedly merging detached entities costs an extra query."
        },
        {
          "type": "concept_map",
          "title": "save() Decision at a Glance",
          "content": "amber|isNew() = true|~id is null or 0|calls persist()|INSERT|id filled after save\nblue|isNew() = false|~id has a value|calls merge()|SELECT then UPDATE|two queries\nemerald|Dirty checking|~managed entity|no save() needed|UPDATE at commit|cheapest path\nviolet|Persistable|~custom isNew()|for manual IDs|avoids merge SELECT|override the default"
        },
        {
          "type": "before_code",
          "title": "Inefficient — Manual ID Forces merge()'s SELECT",
          "content": "```java\n// Manually assigned UUID id is non-null → save() calls merge()\n// merge() runs a SELECT to check existence BEFORE the INSERT\nvar order = new Order();\norder.setId(UUID.randomUUID());   // not null!\nrepository.save(order);           // SELECT + INSERT (wasteful)\n```"
        },
        {
          "type": "after_code",
          "title": "Efficient — Implement Persistable to Skip the SELECT",
          "content": "```java\n@Entity\npublic class Order implements Persistable<UUID> {\n    @Id private UUID id;\n    @Transient private boolean isNew = true; // not persisted\n\n    @Override public UUID getId() { return id; }\n    @Override public boolean isNew() { return isNew; }\n\n    @PostLoad @PostPersist\n    void markNotNew() { this.isNew = false; }\n}\n// Now save() trusts isNew() → persist() → straight INSERT, no SELECT.\n```\nFor auto-generated IDs you don't need this — a null id already signals \"new\"."
        },
        {
          "type": "code_example",
          "title": "Auto-Generated ID: INSERT then UPDATE",
          "content": "```java\n@Service\npublic class ProductService {\n    private final ProductRepository repository;\n    ProductService(ProductRepository r) { this.repository = r; }\n\n    public Product create(String name, BigDecimal price) {\n        var p = new Product();\n        p.setName(name); p.setPrice(price);\n        return repository.save(p); // id null → persist() → INSERT (id filled)\n    }\n\n    @Transactional\n    public Product updatePrice(Long id, BigDecimal price) {\n        var p = repository.findById(id).orElseThrow();\n        p.setPrice(price);\n        return p; // managed → dirty checking → UPDATE at commit (no save needed)\n    }\n}\n```"
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **Assuming save() only inserts** — a non-null id makes it an UPDATE; reusing a detached entity with an id won't INSERT.\n- **Calling save() when nothing changed** — managed entities update via dirty checking at commit; an extra `save()` is redundant.\n- **Manually assigned IDs** — a non-null id forces `merge()`'s SELECT-before-INSERT; implement `Persistable` to skip it.\n- **Cross-transaction edits** — modifying detached entities means `merge()` does a SELECT; prefer fetching fresh in the same transaction."
        },
        {
          "type": "key_points",
          "title": "Key Points",
          "content": "- **`save()` is dual-purpose** — `persist()` (INSERT) for new, `merge()` (UPDATE) for existing.\n- **`isNew()` default** — `@Id` null/0 means new.\n- **`merge()` runs a SELECT first** — extra round trip for existing/detached entities.\n- **Dirty checking** updates managed entities at commit without calling `save()`.\n- **Manual IDs** — implement `Persistable.isNew()` to avoid the merge SELECT."
        },
        {
          "type": "speakable_answer",
          "title": "How to Answer This Verbally",
          "content": "Spring Data's save method handles both inserts and updates in one call, and it decides which by checking whether the entity is new. The default logic looks at the id field: if it's null, or zero for a primitive, the entity is considered new, so save calls persist and does an insert. If the id has a value, the entity is treated as existing, so save calls merge and does an update. For example, creating a product with a null id leads to an insert, and afterwards Hibernate fills in the generated id. For an update, I fetch by id, change a field, and call save again; since the id is set, it's a merge and an update. An important detail is that merge always runs a select first to check whether the row exists, so it's two queries. That's also why it's often better to modify an entity inside a transaction and let Hibernate's dirty checking flush the update at commit, without calling save explicitly. A common mistake is reusing an entity across transactions and expecting an insert, but getting an update because the id is set. The trickier case is manually assigned ids, like a UUID I generate in code. Since the id is non-null, save calls merge and does that wasteful select before the insert. To avoid it, I implement the Persistable interface and override isNew to return a transient flag, so save trusts me and goes straight to an insert."
        }
      ]
    },
    "followup_questions": [
      "What is the Persistable interface?",
      "What happens if you call save() on a manually-assigned ID entity?",
      "What is the difference between save() and saveAndFlush()?"
    ],
    "seo": {
      "metaTitle": "Spring Data save() INSERT vs UPDATE | Java Interview Questions for Freshers",
      "metaDescription": "Learn how Spring Data JPA save() decides between INSERT (persist) and UPDATE (merge) based on entity ID, and the Persistable interface for custom logic."
    },
    "order": 3,
    "speakable_v2": {
      "archetype": "A",
      "pillar": "P02",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 app with Hibernate and PostgreSQL",
      "familiarity_anchors": [
        "JPA",
        "Hibernate",
        "database"
      ],
      "hook": "Assign your own UUID id and save quietly fires a SELECT before the INSERT — because a non-null id makes it think the row already exists.",
      "beats": [
        {
          "kind": "what_it_is",
          "layout": "paragraph",
          "text": "save checks whether the entity is new. If the id is null or zero it calls persist and inserts; if the id has a value it calls merge and updates."
        },
        {
          "kind": "parts_or_states",
          "layout": "bullets",
          "items": [
            "It uses isNew from the Persistable contract, defaulting to a null-id check",
            "merge runs a select first, so an update is actually two queries"
          ]
        },
        {
          "kind": "example",
          "layout": "paragraph",
          "text": "Inside a transaction, modifying a managed entity updates it via dirty checking at commit, so you don't even need to call save."
        },
        {
          "kind": "pitfalls",
          "layout": "bullets",
          "items": [
            "Assuming save always inserts, when a non-null id makes it an update",
            "Manually assigned ids forcing merge's select; implement Persistable to skip it"
          ]
        }
      ],
      "cap": " It uses isNew from Persistable, or a null id check by default.",
      "followup_handoff": [
        "Can you explain the Persistable interface?",
        "What would you say if asked about save versus saveAndFlush?"
      ],
      "tts_overrides": {},
      "speakable_status": "pending_review"
    }
  },
  {
    "id": "crud-repository-005",
    "slug": "deletebyid-vs-delete-in-spring-data-jpa",
    "question": "What is the difference between deleteById and delete in Spring Data?",
    "title": "deleteById vs delete in Spring Data JPA",
    "direct_answer": "**`deleteById(id)`** first runs a **SELECT** to load the entity (so cascades and `@PreRemove` fire), then DELETEs — two queries. **`delete(entity)`** deletes a managed entity directly — one query. Missing id: `deleteById()` throws **`EmptyResultDataAccessException`**; `delete(null)` throws `IllegalArgumentException`. For large bulk deletes without cascades, use `deleteAllInBatch()`.",
    "layout_type": "comparison-arena",
    "difficulty": "medium",
    "importance": "medium",
    "reading_time_minutes": 3,
    "last_updated": "2026-06-04",
    "interviewer_intent": {
      "testing": "Whether you know deleteById does a SELECT first (for cascades/callbacks) and the differing exceptions",
      "common_mistake": "Assuming deleteById is cheaper because it takes only an id, missing the hidden SELECT",
      "to_stand_out": "Explain that the load enables cascade deletes and @PreRemove, and use delete() when the entity is already loaded"
    },
    "company_tags": [
      "tcs",
      "infosys",
      "amazon",
      "wipro"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "Two Paths to Deletion",
          "content": "Spring Data offers **`deleteById(ID)`** and **`delete(T entity)`**. The difference is what runs before the DELETE. `deleteById()` does a **SELECT** to load the entity, confirms it exists, then DELETEs — the load is needed so Hibernate can fire **lifecycle callbacks** (`@PreRemove`) and apply **cascade deletes** to children. `delete(entity)` assumes you already hold a managed entity and skips the SELECT, going straight to DELETE.\n\nBoth remove the row; the path affects performance and referential integrity."
        },
        {
          "type": "concept_map",
          "title": "deleteById vs delete at a Glance",
          "content": "amber|deleteById(id)|~SELECT then DELETE|two queries|fires cascades + @PreRemove|safe default for REST\nblue|delete(entity)|~DELETE directly|one query|entity must be managed|skip when already loaded\nemerald|Missing target|~error behavior|deleteById: EmptyResultDataAccessException|delete(null): IllegalArgumentException|handle for 404s\nviolet|Bulk|~many rows|deleteAllInBatch()|single SQL, no cascades|fastest for cleanup"
        },
        {
          "type": "comparison_table",
          "title": "deleteById vs delete",
          "content": "| Aspect | `deleteById(id)` | `delete(entity)` |\n|---|---|---|\n| Queries | SELECT + DELETE (2) | DELETE only (1) |\n| Needs | Just the id | A managed entity |\n| If missing | `EmptyResultDataAccessException` | `IllegalArgumentException` if null |\n| Cascades / `@PreRemove` | Fires (entity loaded) | Only if already managed |\n| Performance | Slower (extra SELECT) | Faster if already loaded |\n\nUse `deleteById` when you only have the id; `delete` when you've already fetched the entity."
        },
        {
          "type": "before_code",
          "title": "Wasteful — Load Then deleteById (Double SELECT)",
          "content": "```java\nvar order = orderRepo.findById(id).orElseThrow(); // SELECT #1\n// ... validate order ...\norderRepo.deleteById(id); // deleteById does SELECT #2, then DELETE\n```"
        },
        {
          "type": "after_code",
          "title": "Better — Reuse the Loaded Entity with delete()",
          "content": "```java\nvar order = orderRepo.findById(id)\n        .orElseThrow(() -> new EntityNotFoundException(\"Order \" + id));\n// ... validate order ...\norderRepo.delete(order); // no extra SELECT — one DELETE\n\n// Bulk cleanup without cascades:\norderRepo.deleteAllInBatch(oldOrders); // single SQL DELETE\n```"
        },
        {
          "type": "when_to_use",
          "title": "When to Use Each",
          "content": "Use **`deleteById()`** when you only have the id and want Spring to verify existence, fire `@PreRemove`, and cascade to children — the typical REST delete endpoint. Use **`delete(entity)`** when you already fetched the entity (e.g. for validation) to avoid a redundant SELECT, and for `deleteAll(entities)` over a fetched set. For deleting hundreds of rows without cascades, **`deleteAllInBatch()`** issues one DELETE statement and is far faster."
        },
        {
          "type": "common_mistakes",
          "title": "Common Mistakes",
          "content": "- **Thinking `deleteById()` is cheaper** — it does a hidden SELECT first; if you already have the entity, prefer `delete()`.\n- **Not handling `EmptyResultDataAccessException`** — deleting a missing id throws; map it to a 404 instead of a 500.\n- **`delete()` on a detached/null entity** — null throws `IllegalArgumentException`; ensure the entity is managed.\n- **`deleteAll()` for huge sets** — it loads each entity; use `deleteAllInBatch()` for one SQL statement."
        },
        {
          "type": "key_points",
          "title": "Key Points",
          "content": "- **`deleteById(id)`** = SELECT + DELETE (two queries); enables cascades and `@PreRemove`.\n- **`delete(entity)`** = one DELETE if the entity is managed.\n- **Exceptions differ** — `EmptyResultDataAccessException` vs `IllegalArgumentException`.\n- **Reuse a loaded entity** with `delete()` to avoid a redundant SELECT.\n- **`deleteAllInBatch()`** is the fast path for bulk deletes (no cascades)."
        },
        {
          "type": "speakable_answer",
          "title": "How to Answer This Verbally",
          "content": "The main difference is what happens before the delete statement runs. When I call deleteById with an id, Spring Data first does a select to load the entity, checks that it exists, and only then issues the delete. That load is necessary because Hibernate needs the entity in its persistence context to fire lifecycle callbacks like PreRemove and to handle cascade deletes to child entities. So deleteById is actually two queries, a select and a delete. When I call delete with an entity instead, Spring assumes I already have a managed entity, so it skips the select and goes straight to the delete — one query. The exception behavior also differs. Deleting an id that doesn't exist throws EmptyResultDataAccessException, while passing null to delete throws IllegalArgumentException. In practice I use deleteById when I only have the id and want Spring to handle cascades safely, which fits a typical REST delete endpoint. I use delete when I've already loaded the entity in the same transaction, maybe to validate it first, so I don't waste a second select. A pitfall freshers hit is assuming deleteById is faster because it takes just an id, when it's actually slower because of that hidden select. And if I'm deleting hundreds of rows and don't need cascades, I reach for deleteAllInBatch, which runs a single delete statement without loading the entities at all."
        }
      ]
    },
    "followup_questions": [
      "Why does deleteById do a SELECT before DELETE?",
      "What is the difference between deleteAll() and deleteAllInBatch()?",
      "How do you efficiently delete many records in Spring Data JPA?"
    ],
    "seo": {
      "metaTitle": "deleteById vs delete Spring Data JPA | Java Interview Questions for Freshers",
      "metaDescription": "Compare deleteById and delete in Spring Data JPA — SELECT before DELETE behavior, cascade handling, and bulk delete options for performance."
    },
    "order": 5,
    "speakable_v2": {
      "archetype": "B",
      "pillar": "P02",
      "audience_assumption": "beginner",
      "voice": "friendly",
      "standard_example": "Spring Boot 3.2 app with Hibernate and PostgreSQL",
      "familiarity_anchors": [
        "JPA",
        "Hibernate",
        "database"
      ],
      "hook": "deleteById looks cheap because it takes just an id, but it quietly runs a SELECT first — so it's actually the slower of the two.",
      "beats": [
        {
          "kind": "what_each_is",
          "layout": "paragraph",
          "text": "deleteById loads the entity with a select first, to check existence and fire cascades, then deletes. delete takes a managed entity and deletes it directly."
        },
        {
          "kind": "differences",
          "layout": "bullets",
          "items": [
            "deleteById is two queries; delete is one if the entity is already loaded",
            "deleteById throws EmptyResultDataAccessException on a missing id; delete throws IllegalArgumentException on null"
          ]
        },
        {
          "kind": "when_to_pick",
          "layout": "paragraph",
          "text": "deleteById loads the entity to trigger lifecycle callbacks and cascade deletes, so it's the safe default when you only have the id."
        },
        {
          "kind": "tiny_example",
          "layout": "paragraph",
          "text": "If you already fetched the entity to validate it, call delete to avoid a redundant select."
        }
      ],
      "cap": " deleteById throws EmptyResultDataAccessException; delete throws IllegalArgumentException for null.",
      "followup_handoff": [
        "Can you explain why deleteById does a select first?",
        "What would you say if asked how to delete many rows efficiently?"
      ],
      "tts_overrides": {},
      "speakable_status": "pending_review"
    }
  }
]

{
  "topic": "Autowired",
  "topicSlug": "autowired",
  "questions": [
    {
      "id": "autowired-001",
      "slug": "autowired-annotation-in-spring-explained",
      "question": "What does @Autowired do in Spring?",
      "title": "@Autowired Annotation in Spring Explained",
      "direct_answer": "@Autowired marks an injection point that Spring should satisfy from matching beans. Candidate selection starts by type, then considers qualifier metadata and priorities such as @Primary; an injection-point name can be a fallback. A required dependency that cannot be resolved causes context creation to fail, often through an UnsatisfiedDependencyException whose cause explains whether the bean was missing or ambiguous.",
      "layout_type": "concept-explainer",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 3,
      "last_updated": "2026-09-08",
      "interviewer_intent": {
        "testing": "Whether you understand Spring's core dependency injection mechanism — that the container creates and wires objects for you, and resolves @Autowired by type first.",
        "common_mistake": "Believing @Autowired matches by field name rather than by type first, or not realizing it works on constructors, setters, and fields — and that field injection breaks plain unit tests.",
        "to_stand_out": "Explain that constructor injection is preferred because it enables final fields and immutability, makes dependencies explicit in the signature, and lets you unit test without the Spring context."
      },
      "company_tags": [
        "amazon",
        "tcs",
        "infosys",
        "microsoft"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "What @Autowired Requests",
            "content": "Autowiring is part of bean creation. The container does not search the heap or construct an arbitrary class just because its type matches. The dependency must be a registered candidate.\n\n~~~java\n@Service\nclass CheckoutService {\n    private final PaymentGateway gateway;\n\n    CheckoutService(@Qualifier(\"liveGateway\") PaymentGateway gateway) {\n        this.gateway = gateway;\n    }\n}\n~~~\n\nThe type narrows selection to PaymentGateway beans, and the qualifier picks the intended candidate. If liveGateway is absent, CheckoutService cannot be created. Optional is a domain decision: model it explicitly with Optional or ObjectProvider rather than hiding an unexpectedly missing required service in a null field."
          },
          {
            "type": "key_points",
            "title": "Quick revision",
            "content": [
              "Use constructor injection for required dependencies; a single constructor needs no @Autowired.",
              "Type compatibility finds candidates; qualifiers and @Primary disambiguate them.",
              "A missing or unresolved required dependency stops context creation.",
              "Read the full exception cause instead of assuming the top-level type is always NoSuchBeanDefinitionException."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "@Autowired is handled by Spring while it creates a bean. On a constructor parameter such as PaymentGateway, Spring finds assignable bean candidates. One candidate is injected directly. If there are several, @Qualifier can narrow them and @Primary can mark the ordinary default. If no valid result remains, the bean cannot be created and startup fails. The top-level error is commonly UnsatisfiedDependencyException, with NoSuchBeanDefinitionException or NoUniqueBeanDefinitionException deeper in the cause chain. A single constructor is selected without adding @Autowired.",
            "answerSize": "compact"
          },
          {
            "type": "concept_map",
            "title": "@Autowired at a Glance",
            "content": "amber|What It Does|~Auto dependency injection|Finds a matching bean|Injects it for you|No manual new keyword\nblue|Where It Goes|~Three injection points|Constructor (preferred)|Setter (optional deps)|Field (tests only)\nemerald|Resolution Order|~How Spring matches|By type first|Then by name if many|Qualifier breaks ties\nrose|If It Fails|~Fail-fast at startup|No bean and required|NoSuchBeanDefinitionException|App refuses to start"
          },
          {
            "type": "code_example",
            "title": "Constructor, Field, and Setter Injection",
            "content": "```java\n@Service\npublic class OrderService {\n    private final CustomerRepository customerRepo; // required, immutable\n    private PaymentGateway paymentGateway;         // optional\n\n    // Constructor injection — preferred for required dependencies.\n    // Since Spring 4.3, @Autowired is optional on a single constructor.\n    @Autowired\n    public OrderService(CustomerRepository customerRepo) {\n        this.customerRepo = customerRepo;\n    }\n\n    // Setter injection — used for optional dependencies.\n    @Autowired\n    public void setPaymentGateway(PaymentGateway gateway) {\n        this.paymentGateway = gateway;\n    }\n\n    public void processOrder(Long customerId, Order order) {\n        Customer customer = customerRepo.findById(customerId);\n        paymentGateway.charge(customer, order.getTotal());\n    }\n}\n\n@Repository\npublic class CustomerRepository {\n    public Customer findById(Long id) { /* JDBC logic */ return null; }\n}\n```"
          },
          {
            "type": "before_code",
            "title": "Without: Field Injection (Hard to Test)",
            "content": "```java\n@Service\npublic class OrderService {\n    // Field injection: convenient to type, but the dependency is hidden\n    // and the field can't be final. A plain unit test can't set it\n    // without reflection or starting the whole Spring context.\n    @Autowired\n    private CustomerRepository customerRepo;\n\n    public Customer load(Long id) {\n        return customerRepo.findById(id); // NPE in a plain `new OrderService()` test\n    }\n}\n```\nField injection compiles and runs under Spring, but it hides dependencies from callers and forces every unit test to spin up the container."
          },
          {
            "type": "after_code",
            "title": "With: Constructor Injection (Explicit, Testable)",
            "content": "```java\n@Service\npublic class OrderService {\n    private final CustomerRepository customerRepo; // final = immutable, always set\n\n    public OrderService(CustomerRepository customerRepo) {\n        this.customerRepo = customerRepo; // dependency is explicit in the signature\n    }\n\n    public Customer load(Long id) {\n        return customerRepo.findById(id);\n    }\n}\n\n// Plain unit test — no Spring context needed:\n// var service = new OrderService(new FakeCustomerRepository());\n```\nConstructor injection makes the dependency explicit, allows `final` fields, prevents partially constructed objects, and works in plain JUnit tests."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Mixing the new keyword with @Autowired**: Creating a service with `new OrderService()` bypasses Spring entirely — Spring never injects dependencies into objects you instantiate yourself. Let Spring create the bean by marking it `@Service` or `@Component`.\n- **Autowiring into static fields**: Spring's injection works on instance members only. Static fields belong to the class, not to a Spring-managed instance, so `@Autowired` on a static field silently stays null.\n- **Forgetting the stereotype on the injected class**: If `CustomerRepository` isn't annotated with `@Repository` or `@Component`, Spring never creates a bean for it, and `@Autowired` throws `NoSuchBeanDefinitionException` at startup."
          }
        ]
      },
      "followup_questions": [
        "How does Spring resolve @Autowired when there are multiple implementations?",
        "What is @Autowired(required=false)?",
        "What is the difference between @Autowired and @Inject?"
      ],
      "seo": {
        "metaTitle": "@Autowired Annotation in Spring Explained — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What does @Autowired do in Spring? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1,
      "speakable_v2": {
        "pillar": "P02",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "A",
        "hook": "Stop wiring objects by hand. @Autowired tells Spring to find the right bean and plug it into your class for you.",
        "standard_example": "Spring Boot 3 app: OrderService taking a UserRepository through its constructor",
        "familiarity_anchors": [
          "@Autowired",
          "bean",
          "dependency injection",
          "application context"
        ],
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "@Autowired tells Spring to inject a dependency for you. Spring looks in its application context for a bean whose type matches what you declared. If it finds one, it wires it in. If it finds none and the bean's required, it throws NoSuchBeanDefinitionException at startup."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "Without it, you'd call new on every collaborator yourself. That hard-wires your classes together and makes them painful to test. @Autowired hands that job to Spring. Your class just declares what it needs, and the container supplies it."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "Constructor injection puts dependencies in through the constructor. It's the recommended style because it allows final fields.",
              "Field injection drops @Autowired straight on the field. It's quick but hard to test outside Spring.",
              "Setter injection uses @Autowired on a setter. It fits optional dependencies that aren't always there.",
              "Collection injection on a List or Map grabs every matching bean at once."
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "Say OrderService needs a UserRepository. You declare it as a final field and take it in the constructor. Spring sees the single constructor and injects the bean. Here's the non-obvious part. If two beans implement UserRepository, Spring can't choose and throws NoUniqueBeanDefinitionException. Add @Qualifier to name the one you want."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Don't put @Autowired on a static field. Spring injects instances, not classes, so it stays null.",
              "Don't mix new with Spring. If you write new OrderService, Spring never wires that copy."
            ]
          }
        ],
        "cap": "So @Autowired is you declaring what you need, and Spring delivering it. Prefer the constructor and your code stays easy to test.",
        "followup_handoff": [
          "How does Spring pick between multiple beans of the same type?",
          "Why is constructor injection recommended over field injection?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending"
      }
    },
    {
      "id": "autowired-002",
      "slug": "qualifier-annotation-in-spring",
      "question": "What is @Qualifier in Spring and when do you need it?",
      "title": "@Qualifier Annotation in Spring",
      "direct_answer": "@Qualifier narrows the set of beans eligible for an injection point when type alone is not enough. Its value can match qualifier metadata and commonly matches a bean's qualifier or name, but it is better understood as a selection label rather than only as a bean-name lookup. @Primary supplies a default; @Qualifier requests a specific candidate.",
      "layout_type": "concept-explainer",
      "difficulty": "medium",
      "importance": "high",
      "reading_time_minutes": 3,
      "last_updated": "2026-09-08",
      "interviewer_intent": {
        "testing": "Ability to handle real cases where an interface has multiple implementations and one must be selected explicitly.",
        "common_mistake": "Not knowing the difference between @Qualifier and @Primary, or assuming bean names are always the class name with a lowercase first letter without configuring them.",
        "to_stand_out": "Give the decision rule: @Primary for the default used almost everywhere, @Qualifier when different places genuinely need different implementations — and keep qualifier names in constants to avoid typos."
      },
      "company_tags": [
        "amazon",
        "tcs",
        "infosys",
        "microsoft"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Using @Qualifier to Narrow Bean Candidates",
            "content": "A clear qualifier describes meaning, not an implementation accident.\n\n~~~java\n@Bean\n@Qualifier(\"sandbox\")\nPaymentGateway sandboxGateway() { return new FakeGateway(); }\n\n@Service\nclass DemoCheckout {\n    DemoCheckout(@Qualifier(\"sandbox\") PaymentGateway gateway) { }\n}\n~~~\n\nIf two PaymentGateway beans exist with no qualifying rule, Spring reports ambiguity. Marking one @Primary fixes the default case, while the sandbox qualifier still selects the test-like variant. For large codebases, a custom annotation such as @Sandbox can replace repeated strings and survive method renaming."
          },
          {
            "type": "key_points",
            "title": "Quick revision",
            "content": [
              "Multiple beans may implement the same interface.",
              "@Qualifier on the bean and injection point expresses which variant is required.",
              "A qualifier value can fall back to a matching bean name, but qualifiers can also be custom annotations.",
              "@Qualifier overrides an otherwise primary choice for that injection point."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "Use @Qualifier when a type has meaningful variants, such as a live and sandbox PaymentGateway. Label the beans and put the matching qualifier on the constructor parameter. Spring filters the type-compatible candidates with that qualifier before choosing one. The qualifier string often equals the bean name, which makes it look like a pure name lookup, but custom qualifier annotations can express roles more safely. @Primary is useful for the usual default; @Qualifier is useful when one consumer deliberately needs another variant.",
            "answerSize": "compact"
          },
          {
            "type": "concept_map",
            "title": "@Qualifier at a Glance",
            "content": "amber|The Problem|~Many beans, one type|Two PaymentGateway beans|Spring cannot choose|NoUniqueBeanDefinitionException\nblue|The Fix|~Name the bean you want|Qualifier(\"stripeGateway\")|Sits next to Autowired|Exact string match\nemerald|Default Names|~If you set none|Class name, lower first letter|StripeGateway to stripeGateway|Or Component(\"name\")\nviolet|vs @Primary|~Choosing a strategy|Primary for the default|Qualifier for exceptions|Combine both"
          },
          {
            "type": "step",
            "title": "Step 1: Define Beans with Qualifier Names",
            "content": "Create your beans and, optionally, assign explicit qualifier names. If you don't specify a name, Spring uses the class name with a lowercase first letter as the default bean name — `@Component` on `StripeGateway` creates a bean named `stripeGateway`. To use a custom name, write `@Component(\"stripePayment\")` or add `@Qualifier(\"stripe\")` at the class level. Custom names help when bean names should match business terms rather than class names. Define both implementations, each marked `@Component`, so Spring's context now holds two `PaymentGateway` beans."
          },
          {
            "type": "step",
            "title": "Step 2: Use @Qualifier at the Injection Point",
            "content": "At the injection point, combine `@Autowired` (or constructor injection) with `@Qualifier` to name the bean you want. Write `@Qualifier(\"stripeGateway\")` on the constructor parameter, field, or setter parameter, and Spring injects that exact bean, ignoring other candidates. For constructor injection — the preferred approach — the annotation goes directly on the parameter. The qualifier name is an **exact string match**, so a typo throws `NoSuchBeanDefinitionException`. That makes typos the most common failure when using `@Qualifier`."
          },
          {
            "type": "before_code",
            "title": "Without: Ambiguous Autowiring Fails",
            "content": "```java\n@Component\nclass StripeGateway implements PaymentGateway { /* ... */ }\n\n@Component\nclass PayPalGateway implements PaymentGateway { /* ... */ }\n\n@Service\nclass OrderService {\n    // Two PaymentGateway beans exist and neither is @Primary.\n    OrderService(PaymentGateway gateway) { /* ... */ }\n}\n// Startup fails with:\n// NoUniqueBeanDefinitionException: expected single matching bean but found 2\n```\nWith two candidates and no hint, Spring can't pick one — it stops at startup."
          },
          {
            "type": "after_code",
            "title": "With: @Qualifier Names the Bean",
            "content": "```java\n@Component(\"stripeGateway\")\nclass StripeGateway implements PaymentGateway { /* ... */ }\n\n@Component(\"payPalGateway\")\nclass PayPalGateway implements PaymentGateway { /* ... */ }\n\n@Service\nclass OrderService {\n    private final PaymentGateway gateway;\n\n    // Name the bean you want — Spring injects StripeGateway, not PayPalGateway.\n    OrderService(@Qualifier(\"stripeGateway\") PaymentGateway gateway) {\n        this.gateway = gateway;\n    }\n}\n```\nThe `@Qualifier` resolves the ambiguity by exact name, so the right implementation is injected."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Using @Qualifier without @Autowired**: It isn't a standalone injection trigger — it only modifies `@Autowired` or constructor injection. Pair it with one of those.\n- **Confusing @Qualifier with @Primary**: Freshers reach for `@Qualifier` everywhere when `@Primary` would be cleaner. If one implementation is the default most of the time, mark it `@Primary` and qualify only the exceptions.\n- **Hardcoding bean names everywhere**: Scattering string literals like `\"stripeGateway\"` makes code fragile — one rename and injection breaks. Keep names in constants or use a custom qualifier annotation."
          }
        ]
      },
      "followup_questions": [
        "What is the difference between @Qualifier and @Primary?",
        "What is NoUniqueBeanDefinitionException?",
        "Can you create a custom qualifier annotation?"
      ],
      "seo": {
        "metaTitle": "@Qualifier Annotation in Spring — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is @Qualifier in Spring and when do you need it? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 2,
      "speakable_v2": {
        "pillar": "P02",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "A",
        "hook": "Two beans, one type, and Spring can't choose. @Qualifier is how you point at the exact one you want.",
        "standard_example": "Spring Boot 3 app choosing StripeGateway with @Qualifier over PayPalGateway",
        "familiarity_anchors": [
          "@Qualifier",
          "@Primary",
          "NoUniqueBeanDefinitionException",
          "bean name"
        ],
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "@Qualifier tells Spring which bean to inject when several share the same type. You write it next to @Autowired and pass the bean name. Spring then picks that exact bean and ignores the rest. Without it, two candidates make Spring throw NoUniqueBeanDefinitionException at startup."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "Real apps often have two implementations of one interface. A Stripe gateway and a PayPal gateway, say. Spring can't read your mind about which to use. @Qualifier removes the guesswork by naming the bean you mean."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "Default name: with no value, the bean name is the class name with a lowercase first letter.",
              "Custom name: set @Component('stripe') or @Qualifier('stripe') on the bean to pick your own.",
              "At the injection point: @Qualifier sits on the constructor parameter, field, or setter."
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "Say StripeGateway and PayPalGateway both implement PaymentGateway. OrderService wants Stripe, so its constructor takes @Qualifier('stripeGateway') PaymentGateway. Spring injects StripeGateway and skips PayPal. Here's the non-obvious part. The name is an exact string match, so one typo throws NoSuchBeanDefinitionException."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Don't scatter the same bean name as a string everywhere. One rename and the typos pile up.",
              "Don't use @Qualifier alone. It only modifies @Autowired or constructor injection, it can't trigger it."
            ]
          }
        ],
        "cap": "So @Qualifier is your precision tool. Pair it with @Primary, lean on the default, and name the bean only for the exceptions.",
        "followup_handoff": [
          "What is the difference between @Qualifier and @Primary?",
          "How do you create a custom qualifier annotation in Spring?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending"
      }
    },
    {
      "id": "autowired-004",
      "slug": "nosuchbeandefinitionexception-in-spring",
      "question": "What happens if Spring cannot find a bean for @Autowired injection?",
      "title": "NoSuchBeanDefinitionException in Spring",
      "direct_answer": "If a required autowired dependency has no eligible bean, Spring cannot create the consuming bean and application-context startup fails. The top-level error is often UnsatisfiedDependencyException, with NoSuchBeanDefinitionException as a cause. Optional dependencies can be represented with Optional, ObjectProvider, or required=false, but making a core dependency optional usually hides a configuration problem.",
      "layout_type": "concept-explainer",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 3,
      "last_updated": "2026-09-08",
      "interviewer_intent": {
        "testing": "Practical debugging of one of the most common Spring startup failures, plus understanding of the fail-fast principle behind it.",
        "common_mistake": "Not checking the root cause systematically — it's usually a missing @Component, a package outside the component scan, a type mismatch, or a bean in an inactive profile.",
        "to_stand_out": "Walk through the checklist (stereotype annotation, scan coverage, exact type, active profile) and mention enabling debug mode to read the conditions report instead of guessing."
      },
      "company_tags": [
        "tcs",
        "infosys",
        "wipro",
        "amazon"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "When a Required Bean Is Missing",
            "content": "The diagnosis is a registration problem, not simply an exception-name question. Start with the injection point in the error. Ask whether an implementation is annotated or declared with @Bean, whether its package is scanned, whether @Profile or a conditional excluded it, and whether test slicing intentionally omitted it.\n\nOptional dependencies have explicit tools. Optional<T> represents zero or one value, while ObjectProvider<T> supports lazy lookup and availability checks. @Autowired(required=false) also exists, but a nullable field is easier to misuse. If the application cannot operate without the collaborator, keep it required and fix the context instead."
          },
          {
            "type": "key_points",
            "title": "Quick revision",
            "content": [
              "No eligible candidate means the consuming bean cannot be created.",
              "Read the nested cause and bean names; the outer exception may be UnsatisfiedDependencyException.",
              "Check component-scan boundaries, profiles, conditions, and configuration imports.",
              "Use Optional or ObjectProvider only when absence is valid application behavior."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "A missing dependency is detected while Spring builds the context, which is safer than leaving a required field null until traffic arrives. For example, PaymentService may require PaymentGateway, but the gateway implementation may sit outside component scanning or behind an inactive profile. The startup error names the failed bean and injection point. Follow the cause chain to the missing type, then verify registration and conditional rules. required=false can suppress failure, but then code must correctly handle absence; it should not be used to make a broken required configuration appear healthy.",
            "answerSize": "standard"
          },
          {
            "type": "concept_map",
            "title": "Missing Bean at a Glance",
            "content": "rose|The Error|~Thrown at startup|No qualifying bean of type|Fail-fast, app won't boot|Before serving requests\namber|Top Causes|~Usually configuration|Missing @Component/@Service|Package outside scan|Profile not active\nblue|Debug Steps|~Work the checklist|Check stereotype annotation|Check component scan|Confirm exact type\nemerald|Make It Optional|~Only if truly optional|required=false leaves null|Or use Optional<T>|Guard with a null check"
          },
          {
            "type": "before_code",
            "title": "Without: Missing Stereotype (App Won't Start)",
            "content": "```java\n// No @Repository / @Component here, so Spring never registers a bean.\npublic class EmailService {\n    public void send(String to, String body) { /* SMTP call */ }\n}\n\n@Service\npublic class OrderService {\n    private final EmailService emailService;\n\n    public OrderService(EmailService emailService) { // Spring has no EmailService bean\n        this.emailService = emailService;\n    }\n}\n// Startup fails with:\n// NoSuchBeanDefinitionException: No qualifying bean of type 'EmailService'\n```\nWithout a stereotype annotation, `EmailService` is just a plain class — Spring can't inject what it never created."
          },
          {
            "type": "after_code",
            "title": "With: Add the Stereotype (or Make It Optional)",
            "content": "```java\n@Service // now Spring creates and manages an EmailService bean\npublic class EmailService {\n    public void send(String to, String body) { /* SMTP call */ }\n}\n\n@Service\npublic class OrderService {\n    private final EmailService emailService;\n\n    // Truly optional dependency: null when no bean exists.\n    @Autowired(required = false)\n    private MetricsCollector metricsCollector;\n\n    public OrderService(EmailService emailService) {\n        this.emailService = emailService; // resolves now\n    }\n\n    public void processOrder(Order order) {\n        emailService.send(order.getCustomerEmail(), \"Confirmed\");\n        if (metricsCollector != null) {       // guard the optional bean\n            metricsCollector.recordOrderProcessed();\n        }\n    }\n}\n```\nMarking the class `@Service` fixes the required dependency; `required = false` is only for genuinely optional beans like metrics."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Forgetting @Component or @Service**: The most common cause. If the target class isn't annotated (or returned from a `@Bean` method), Spring never creates a bean, so injection fails.\n- **Wrong component scan package**: If a class is in `com.example.data` but `@SpringBootApplication` lives in `com.example.web`, Spring won't scan the data package. Make sure your main class sits above all bean packages.\n- **Mismatched types**: If you `@Autowired CustomerRepository` but only a different type is registered, Spring won't match them. Inject the type that actually exists.\n- **Profile mismatch**: A bean marked `@Profile(\"production\")` doesn't exist when you run with the `dev` profile active."
          }
        ]
      },
      "followup_questions": [
        "How do you debug NoSuchBeanDefinitionException?",
        "What is @Autowired(required=false)?",
        "What is NoUniqueBeanDefinitionException?"
      ],
      "seo": {
        "metaTitle": "NoSuchBeanDefinitionException in Spring — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What happens if Spring cannot find a bean for @Autowired injection? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 3,
      "speakable_v2": {
        "pillar": "P02",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "A",
        "hook": "Your app refuses to start and the log screams 'No qualifying bean'. That's Spring failing loud, on purpose.",
        "standard_example": "Spring Boot 3 app throwing NoSuchBeanDefinitionException when @Repository is missing",
        "familiarity_anchors": [
          "NoSuchBeanDefinitionException",
          "@Autowired",
          "component scan",
          "fail-fast"
        ],
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "NoSuchBeanDefinitionException fires at startup when Spring tries to inject a required @Autowired dependency but finds no matching bean. It's a fail-fast check. The application context won't finish loading until every required bean resolves. So you catch the bug in seconds, not in production."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "A broken wiring should blow up now, not three hours into a release. This exception is Spring guarding you against a null reference that would crash much later. Loud and early beats silent and late."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "Missing stereotype: the class has no @Service, @Component, or @Repository, so Spring never registers it.",
              "Wrong package: the class sits outside the component scan, so Spring never finds it.",
              "Type mismatch: you ask for an interface, but no registered bean implements it.",
              "Disabled bean: a @Profile or @Conditional kept it out of this environment."
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "Say OrderService @Autowires UserRepository, but you forgot @Repository on it. Spring throws 'No qualifying bean of type UserRepository' and stops. Here's the non-obvious part. The fastest fix is debug mode. Turn on debug in application properties and Spring prints why each bean did or didn't load."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Don't hide it with required equals false. A null field is just a crash you delayed.",
              "Check that @SpringBootApplication sits above all your beans. It sets the scan boundary."
            ]
          }
        ],
        "cap": "So it's a config bug, not a Spring bug. The fix is almost always a missing annotation or a package outside the scan.",
        "followup_handoff": [
          "How do you use the conditions report to debug missing beans?",
          "What's the difference between NoSuchBeanDefinitionException and NoUniqueBeanDefinitionException?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending"
      }
    },
    {
      "id": "autowired-005",
      "slug": "autowired-with-collections-in-spring",
      "question": "How does @Autowired work with collections?",
      "title": "@Autowired with Collections in Spring",
      "direct_answer": "When **@Autowired** targets a `List`, `Set`, or `Map`, Spring injects **all beans of the matching type** into that collection. For `List<PaymentHandler>`, Spring finds every bean implementing `PaymentHandler` and injects them all. For `Map<String, PaymentHandler>`, the **keys are the bean names** and the values are the bean instances.",
      "layout_type": "concept-explainer",
      "difficulty": "medium",
      "importance": "medium",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-08",
      "interviewer_intent": {
        "testing": "Whether you know Spring's injection goes beyond single beans and can collect all matching implementations into a List, Set, or Map.",
        "common_mistake": "Not knowing this exists and wiring a separate @Autowired field per implementation, or calling ApplicationContext.getBeansOfType() everywhere.",
        "to_stand_out": "Describe a plugin or Strategy-pattern example — payment processors or notification channels — where you iterate registered handlers, showing you understand open-closed design."
      },
      "company_tags": [
        "amazon",
        "microsoft",
        "tcs",
        "infosys"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Collection Injection: The Plugin Pattern Foundation",
            "content": "**Collection injection** is Spring autowiring every bean of a type into a single collection field. When you mark a `List<T>`, `Set<T>`, or `Map<String, T>` with **@Autowired**, Spring scans the application context for every bean matching `T` and injects them all at once.\n\nThis differs from injecting a single bean — you're asking for **every registered implementation**. For `List` and `Set` you get the beans themselves. For `Map<String, T>` the keys are the **bean names** and the values are the instances.\n\nThis is the foundation for **Strategy pattern** implementations, listener registries, and any case where you process many implementations without hardcoding which exist. It's also how frameworks gather components and wire them dynamically. Without it, you'd manually track every implementation or call `ApplicationContext.getBeansOfType()` everywhere, coupling your code to Spring's API."
          },
          {
            "type": "concept_map",
            "title": "Collection Injection at a Glance",
            "content": "amber|What Happens|~Inject all matching beans|List<Handler> gets every impl|Set or Map also work|One field, many beans\nblue|Map Keys|~For Map<String,T>|Key is the bean name|Value is the bean instance|Lookup by name\nemerald|Why It Helps|~Strategy / plugin pattern|Add impl equals add @Component|Orchestrator never changes|Open-closed principle\nviolet|Ordering|~Not guaranteed|Use @Order(n) to control|Lower runs first|Empty list if none match"
          },
          {
            "type": "code_example",
            "title": "Payment Processing with Multiple Handlers",
            "content": "```java\n@Service\npublic class PaymentService {\n    private final List<PaymentHandler> handlers;\n\n    // Spring injects ALL PaymentHandler beans into the list.\n    public PaymentService(List<PaymentHandler> handlers) {\n        this.handlers = handlers;\n    }\n\n    public void processPayment(Order order) {\n        for (PaymentHandler handler : handlers) {\n            if (handler.supports(order.getPaymentType())) {\n                handler.process(order);\n                return;\n            }\n        }\n        throw new IllegalArgumentException(\"No handler for \" + order.getPaymentType());\n    }\n}\n\n@Component\nclass CreditCardHandler implements PaymentHandler {\n    public boolean supports(String type) { return \"CREDIT_CARD\".equals(type); }\n    public void process(Order order) { /* Stripe API call */ }\n}\n\n@Component\nclass PayPalHandler implements PaymentHandler {\n    public boolean supports(String type) { return \"PAYPAL\".equals(type); }\n    public void process(Order order) { /* PayPal API call */ }\n}\n```"
          },
          {
            "type": "before_code",
            "title": "Without: One Field per Implementation",
            "content": "```java\n@Service\npublic class PaymentService {\n    // Every handler wired by name. Adding a new one means editing this class.\n    @Autowired private CreditCardHandler creditCard;\n    @Autowired private PayPalHandler payPal;\n\n    public void process(Order order) {\n        if (\"CREDIT_CARD\".equals(order.getPaymentType())) creditCard.process(order);\n        else if (\"PAYPAL\".equals(order.getPaymentType())) payPal.process(order);\n        // ...another branch for every new handler\n    }\n}\n```\nThis hardcodes each implementation, so adding a payment method forces you to edit the service — it isn't open to extension."
          },
          {
            "type": "after_code",
            "title": "With: Inject the Whole List",
            "content": "```java\n@Service\npublic class PaymentService {\n    private final List<PaymentHandler> handlers; // Spring injects them all\n\n    public PaymentService(List<PaymentHandler> handlers) {\n        this.handlers = handlers;\n    }\n\n    public void process(Order order) {\n        handlers.stream()\n            .filter(h -> h.supports(order.getPaymentType()))\n            .findFirst()\n            .orElseThrow(() -> new IllegalArgumentException(\"No handler\"))\n            .process(order);\n    }\n}\n// Add a new @Component handler later and PaymentService never changes.\n```\nNow the service depends only on the interface; dropping in a new `@Component` handler extends behaviour without touching this class."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **A separate field per implementation**: Wiring `@Autowired CreditCardHandler` and `@Autowired PayPalHandler` and branching on each breaks when a new handler appears. Collection injection removes that coupling.\n- **Not understanding Map keys**: With `Map<String, PaymentHandler>`, keys are Spring bean names (the uncapitalized class name by default). For `CreditCardHandler` the key is `\"creditCardHandler\"`, which surprises Learners expecting the class name.\n- **Expecting a specific order**: Spring injects into a `List<T>` in an undefined order unless you add `@Order`. Don't rely on position — use a `supports()` method or explicit priority."
          },
          {
            "type": "key_points",
            "title": "Quick revision",
            "content": [
              "**Collection injection**: @Autowired on List<T>, Set<T>, or Map<String,T> injects all beans of type T from the context",
              "**Map keys**: For Map<String,T>, keys are bean names (default: uncapitalized class name), values are bean instances",
              "**Plugin pattern**: Add new implementations without editing existing service code — just add a @Component",
              "**Strategy selection**: Iterate the list and call supports() or canHandle() to find the right implementation",
              "**Order control**: Use @Order(n) on beans to control List position — lower values come first"
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "When you put @Autowired on a List or Map field, Spring injects every bean of that type into the collection. So if you have List of PaymentHandler and three components implementing PaymentHandler, Spring gives you all three in that list. This is useful for strategy patterns where you don't know which implementation you'll need until runtime. Imagine you're building a payment service with a CreditCardHandler, a PayPalHandler, and maybe a CryptoHandler. Instead of autowiring each one separately, you autowire List of PaymentHandler and loop through to find the one that supports the current payment type. When someone adds an ApplePayHandler later, your service doesn't change at all, because Spring finds the new handler and adds it to the list automatically. For Map injection, the keys are the bean names, so if your class is CreditCardHandler, the key is creditCardHandler. A common mistake is creating a separate autowired field for each implementation. That couples your code to specific classes. With collection injection, your service only knows about the interface, and Spring wires up all the implementations. It's the foundation for plugin-style architectures, where you add features without touching existing code. Just remember the order isn't guaranteed, so use @Order if you care about it, and Spring gives you an empty list rather than null when nothing matches.",
            "answerSize": "compact"
          }
        ]
      },
      "followup_questions": [
        "How do you control the order of beans in an @Autowired list?",
        "What is the Strategy design pattern?",
        "How does Map<String, Bean> injection work?"
      ],
      "seo": {
        "metaTitle": "@Autowired with Collections in Spring — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How does @Autowired work with collections? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 4,
      "speakable_v2": {
        "pillar": "P02",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "A",
        "hook": "Declare one List field and Spring rounds up every matching bean for you. No manual registry to maintain.",
        "standard_example": "Spring Boot 3 app injecting a List of PaymentHandler beans",
        "familiarity_anchors": [
          "List injection",
          "Strategy pattern",
          "@Order",
          "@Component"
        ],
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "Put @Autowired on a List of an interface, and Spring injects every bean that implements it. It's the same matching as single injection, except Spring doesn't stop at the first hit. It collects them all. A Map of String to the type gives you bean name as key and bean as value."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "It's the backbone of the Strategy and plugin patterns. Instead of hard-coding each handler, you let Spring discover them. Adding a new one just means a new @Component class. Your orchestrator never changes."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "List injection: Spring injects all beans of the element type, in no guaranteed order.",
              "Ordered list: add @Order on each bean to fix their position in the list.",
              "Map injection: a Map of String to type gives bean name to bean instance, handy for lookup by key.",
              "Set injection: a Set works too when order and duplicates don't matter."
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "PaymentService declares a List of PaymentHandler. StripeHandler and PayPalHandler both carry @Component, so Spring injects both into the list. At runtime the service loops to find the handler that supports the payment type. Here's the non-obvious part. Add a third handler later and PaymentService never changes."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Don't rely on list order. It isn't deterministic without @Order or the Ordered interface.",
              "If the list comes back empty, no beans matched. Check the component scan and the @Component tags."
            ]
          }
        ],
        "cap": "So collection injection turns @Autowired into discovery. Let Spring assemble the list, and your code stays open to new handlers.",
        "followup_handoff": [
          "How does @Order control the position of beans in an injected List?",
          "How does Map<String, BeanType> injection differ from List injection?"
        ],
        "tts_overrides": {},
        "speakable_status": "approved"
      }
    },
    {
      "id": "autowired-006",
      "slug": "primary-annotation-in-spring",
      "question": "What is @Primary in Spring and when do you use it?",
      "title": "@Primary Annotation in Spring",
      "direct_answer": "@Primary marks one bean as the preferred candidate when type-based injection finds several eligible beans for a single-valued dependency. @Qualifier narrows candidates by qualifier metadata; it is not simply an instruction that universally 'overrides' every other rule. After narrowing, a primary candidate may still help resolve the remaining set.",
      "layout_type": "concept-explainer",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 3,
      "last_updated": "2026-09-08",
      "interviewer_intent": {
        "testing": "Knowledge of Spring's bean disambiguation strategy and the @Primary plus @Qualifier pairing for managing multiple beans of one type.",
        "common_mistake": "Putting @Qualifier at every injection point instead of using @Primary for the default, or thinking @Primary replaces @Qualifier or hides the other beans.",
        "to_stand_out": "Explain the trade-off: @Primary covers the 90 percent case with zero annotation, while @Qualifier is explicit at the 10 percent of points needing the alternative — a maintainability decision."
      },
      "company_tags": [
        "amazon",
        "tcs",
        "infosys",
        "cognizant"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "How @Primary Resolves a Default Candidate",
            "content": "Candidate resolution first considers the required type and qualifier constraints, then preference rules such as primary or priority where applicable. If two eligible beans are both primary, injection is still ambiguous. Prefer semantic custom qualifiers over fragile string bean names when the distinction represents a domain role."
          },
          {
            "type": "key_points",
            "title": "Quick revision",
            "content": [
              "Use @Primary for the normal default implementation.",
              "Use @Qualifier when an injection point needs a named or semantic subset.",
              "Bean name fallback and priority/order rules are separate mechanisms.",
              "Collections receive all matching beans; @Primary does not remove the others."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "For example, CardGateway can be @Primary while BankGateway is another PaymentGateway bean. Injecting one PaymentGateway chooses CardGateway. @Qualifier(\"bank\") chooses the bean carrying that qualifier. Injecting List<PaymentGateway> receives both, with ordering controlled separately by @Order or Ordered.",
            "answerSize": "compact"
          },
          {
            "type": "concept_map",
            "title": "@Primary at a Glance",
            "content": "amber|What It Does|~Marks the default bean|When many of one type|Injected when no Qualifier|The 90 percent case\nblue|How Spring Uses It|~A tie-breaker|Checked during resolution|Qualifier overrides it|Not a filter\nemerald|When to Use|~One clear default|Main DataSource @Primary|Alternates use Qualifier|Cuts boilerplate\nrose|Watch Out|~One per type|Two @Primary equals ambiguous|Throws at injection|Defeats the purpose"
          },
          {
            "type": "code_example",
            "title": "Primary DataSource with an Alternative",
            "content": "```java\n@Configuration\npublic class DataSourceConfig {\n\n    @Bean\n    @Primary  // default DataSource for most repositories\n    public DataSource mysqlDataSource() {\n        HikariDataSource ds = new HikariDataSource();\n        ds.setJdbcUrl(\"jdbc:mysql://localhost:3306/main\");\n        return ds;\n    }\n\n    @Bean(\"analyticsDataSource\")\n    public DataSource postgresDataSource() {\n        HikariDataSource ds = new HikariDataSource();\n        ds.setJdbcUrl(\"jdbc:postgresql://localhost:5432/analytics\");\n        return ds;\n    }\n}\n\n@Repository\npublic class OrderRepository {\n    private final DataSource dataSource;\n    // Gets mysqlDataSource because it is @Primary — no @Qualifier needed.\n    public OrderRepository(DataSource dataSource) {\n        this.dataSource = dataSource;\n    }\n}\n\n@Repository\npublic class AnalyticsRepository {\n    private final DataSource dataSource;\n    // Explicitly requests the alternative — @Qualifier overrides @Primary.\n    public AnalyticsRepository(@Qualifier(\"analyticsDataSource\") DataSource dataSource) {\n        this.dataSource = dataSource;\n    }\n}\n```"
          },
          {
            "type": "before_code",
            "title": "Without: @Qualifier Repeated Everywhere",
            "content": "```java\n// Two CacheService beans exist. Without @Primary, every injection point\n// must name the bean, or Spring throws NoUniqueBeanDefinitionException.\n@Service\npublic class ProductService {\n    private final CacheService cache;\n    public ProductService(@Qualifier(\"redisCacheService\") CacheService cache) {\n        this.cache = cache; // same qualifier copied into dozens of classes\n    }\n}\n```\nRepeating `@Qualifier(\"redisCacheService\")` in every class is noisy and breaks the moment someone renames the bean."
          },
          {
            "type": "after_code",
            "title": "With: @Primary Sets the Default Once",
            "content": "```java\n@Service\n@Primary // the default CacheService for the whole app, declared once\npublic class RedisCacheService implements CacheService { /* ... */ }\n\n@Service\npublic class InMemoryCacheService implements CacheService { /* ... */ }\n\n@Service\npublic class ProductService {\n    private final CacheService cache;\n    // No @Qualifier needed — Spring injects the @Primary RedisCacheService.\n    public ProductService(CacheService cache) {\n        this.cache = cache;\n    }\n}\n```\nOne `@Primary` removes the qualifier from every common injection point; only the rare class that needs `InMemoryCacheService` adds a `@Qualifier`."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Using @Qualifier everywhere instead of @Primary**: Freshers discover multiple beans cause errors, learn `@Qualifier`, then repeat it on every injection point. Mark the common bean `@Primary` and use `@Qualifier` only for the alternative.\n- **Thinking @Primary hides the other beans**: It doesn't. You can still inject a non-primary bean with `@Qualifier`. `@Primary` is just the tie-breaker when no qualifier is given.\n- **Marking two beans @Primary for one type**: Spring still can't decide and throws at injection time. Only one bean per type may be `@Primary`."
          }
        ]
      },
      "followup_questions": [
        "Can you have multiple @Primary beans of the same type?",
        "How does @Primary interact with @Qualifier?",
        "When should you use @Primary vs @Profile?"
      ],
      "seo": {
        "metaTitle": "@Primary Annotation in Spring — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is @Primary in Spring and when do you use it? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 5,
      "speakable_v2": {
        "pillar": "P02",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "A",
        "hook": "Two beans of one type, and Spring freezes. @Primary names the default so most injections need zero extra annotation.",
        "standard_example": "Spring Boot 3 app with RedisCacheService marked @Primary over InMemoryCacheService",
        "familiarity_anchors": [
          "@Primary",
          "@Qualifier",
          "default bean",
          "NoUniqueBeanDefinitionException"
        ],
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "@Primary marks one bean as the preferred pick when Spring finds several of the same type. Inject by type without a @Qualifier, and Spring hands you the @Primary one. You declare the default once at the bean, instead of repeating it at every injection point."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "Without it, two beans of one type force a @Qualifier on every single injection. That's noisy and easy to get wrong. @Primary lets you set the common choice once. Then @Qualifier only shows up for the rare exceptions."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "On a @Service or @Component: that class becomes the default bean for its type everywhere.",
              "On a @Bean method: the returned object becomes primary for its return type.",
              "Next to @Qualifier: @Qualifier always wins. Spring checks @Primary only when no qualifier is present."
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "Say RedisCacheService and InMemoryCacheService both implement CacheService. Most code wants Redis, so you put @Primary on RedisCacheService. Now every @Autowired CacheService field gets Redis for free. The one test that needs the in-memory version adds @Qualifier. Here's the non-obvious part. Mark both @Primary and Spring throws NoUniqueBeanDefinitionException again."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Don't mark two beans @Primary for one type. It doesn't break the tie, it just throws a different error.",
              "Don't reach for @Primary when many places need different beans. That's a job for @Qualifier."
            ]
          }
        ],
        "cap": "So @Primary sets a sensible default and @Qualifier overrides it precisely. Together they cut the annotation noise across your code.",
        "followup_handoff": [
          "What happens at startup if two beans are both @Primary for the same type?",
          "How does @Primary interact with @Profile for environment-specific beans?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending"
      }
    }
  ]
}

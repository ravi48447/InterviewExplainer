{
  "topic": "IoC Container",
  "topicSlug": "ioc-container",
  "questions": [
    {
      "id": "ioc-container-001",
      "slug": "inversion-of-control-ioc-in-spring-explained",
      "question": "What is Inversion of Control (IoC) in Spring?",
      "title": "Inversion of Control (IoC) in Spring Explained",
      "direct_answer": "Inversion of Control means application components declare their dependencies and lifecycle hooks while the Spring container creates and connects the managed object graph. Dependency injection is Spring's main IoC mechanism. Application code may still create ordinary values and short-lived domain objects; the important boundary is whether an object needs container services such as injection, scopes, lifecycle callbacks, transactions, or other proxy advice.",
      "layout_type": "concept-explainer",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 3,
      "last_updated": "2026-09-08",
      "interviewer_intent": {
        "testing": "Understanding of the core design principle behind Spring and dependency management",
        "common_mistake": "Confusing IoC with DI or thinking they're the same thing — IoC is the principle, DI is one implementation",
        "to_stand_out": "Explain concrete benefits: loose coupling, testability, and how it enables aspect-oriented programming"
      },
      "company_tags": [
        "amazon",
        "microsoft",
        "tcs",
        "infosys"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Inversion of Control in Spring",
            "content": "IoC is broader than DI: framework callbacks, events, and template methods also invert control of execution. In Spring's bean container, the visible result is a graph assembled from bean definitions and conditions. @PostConstruct is appropriate for lightweight validation or initialization that belongs to the bean; scarce clients and pools should normally use their dedicated managed lifecycle rather than opening ad-hoc connections in a callback."
          },
          {
            "type": "key_points",
            "title": "Quick revision",
            "content": [
              "IoC moves managed-object assembly to the container.",
              "DI supplies collaborators through constructors or other injection points.",
              "Only Spring-managed objects receive Spring lifecycle and proxy behavior.",
              "Using new is normal for plain values; it is a problem only when bypassing needed container behavior."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "For example, Spring constructs OrderService with an OrderRepository bean and can apply transaction advice when a call enters the managed service proxy. OrderService can still create an OrderId value with new because that value needs no container feature. Creating a second OrderService manually would bypass its injection and proxy configuration.",
            "answerSize": "standard"
          },
          {
            "type": "concept_map",
            "title": "Inversion of Control at a Glance",
            "content": "amber|What It Is|~Flip who is in control|The framework calls you|Container creates objects|Not your code with new\nblue|Without IoC|~You wire everything|new Dependency()|Tight coupling|Hard to swap\nemerald|With IoC|~The container wires|You declare needs|Spring injects them|Loose coupling\nviolet|DI Implements IoC|~The how|IoC is the principle|DI is the mechanism|Hollywood principle"
          },
          {
            "type": "code_example",
            "title": "IoC in Action: Spring Creates and Wires Everything",
            "content": "```java\n@Service\npublic class OrderService {\n    private final PaymentProcessor paymentProcessor;\n    private final EmailService emailService;\n\n    // Spring calls this constructor, injects dependencies\n    public OrderService(PaymentProcessor paymentProcessor, EmailService emailService) {\n        this.paymentProcessor = paymentProcessor;\n        this.emailService = emailService;\n    }\n\n    public void processOrder(Order order) {\n        paymentProcessor.charge(order.getAmount());\n        emailService.sendConfirmation(order.getCustomerEmail());\n    }\n}\n\n// Spring Boot main class — IoC container starts here\n@SpringBootApplication\npublic class ShopApplication {\n    public static void main(String[] args) {\n        var context = SpringApplication.run(ShopApplication.class, args);\n        // Spring already created OrderService with its dependencies\n        var orderService = context.getBean(OrderService.class);\n    }\n}\n```"
          },
          {
            "type": "when_to_use",
            "title": "When IoC Shines in Real Applications",
            "content": "Use IoC when building **multi-layered applications** where services depend on repositories, and controllers depend on services — Spring wires the entire graph automatically. It's essential for **enterprise applications** that need transaction management, security checks, or logging across layers without polluting business logic. IoC is critical for **testability**: when OrderService receives dependencies through constructor injection, you can pass mock implementations in unit tests without changing production code.\n\nUse it when you need **configuration flexibility**: swapping a MySQL repository for a PostgreSQL one becomes a configuration change, not a code rewrite. Finally, IoC enables **cross-cutting concerns** like caching or auditing via Spring AOP, which intercepts method calls transparently."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Mixing IoC and DI terminology**: Freshers often say IoC when they mean Dependency Injection. IoC is the overarching principle (framework controls flow), while DI is one technique to achieve IoC (injecting dependencies). Not all IoC uses DI — event-driven callbacks are also IoC.\n- **Using new keyword inside beans**: Creating dependencies with new PaymentProcessor() defeats IoC entirely. If Spring doesn't create the object, it can't manage lifecycle or apply AOP. Always let Spring instantiate collaborators.\n- **Forgetting IoC implications for testing**: Some freshers mock incorrectly by trying to replace Spring-managed beans with manual instances, breaking the container. Use @MockBean in Spring tests to replace beans within the IoC container itself."
          }
        ]
      },
      "followup_questions": [
        "What is the difference between IoC and Dependency Injection?",
        "What is the Spring ApplicationContext?",
        "How does IoC make code more testable?"
      ],
      "seo": {
        "metaTitle": "Inversion of Control (IoC) in Spring Explained — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is Inversion of Control (IoC) in Spring? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1,
      "speakable_v2": {
        "pillar": "P02",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "A",
        "hook": "In plain Java you call new for every object. With IoC, Spring does that for you, and your code stops caring how things get built.",
        "standard_example": "Spring app where @Service OrderService gets a PaymentProcessor injected",
        "familiarity_anchors": [
          "Inversion of Control",
          "IoC container",
          "dependency injection",
          "ApplicationContext"
        ],
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "Inversion of Control is a design principle. Control over creating objects and managing their lifecycle moves from your code to a framework. In Spring, you mark a class with @Service and the container builds and wires it. Your code doesn't call new anymore. The framework handles that for you."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "When your code creates its own dependencies, classes get tightly bound together. Change one constructor and the change ripples everywhere. It's brittle. IoC moves that wiring into one place, the container. Now each class just declares what it needs, and Spring hands it over."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "IoC, the principle: the framework runs the flow, not your code.",
              "Dependency injection, the mechanism: Spring pushes collaborators in through the constructor.",
              "ApplicationContext, the container: it reads annotations, builds beans, and wires them.",
              "Loose coupling, the payoff: classes lean on interfaces, not on objects they built themselves."
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "Picture an OrderService that needs a PaymentProcessor. It doesn't call new PaymentProcessor. It just lists it as a constructor parameter. At startup, Spring sees @Service, finds the PaymentProcessor bean, and passes it in. Here's the non-obvious part. IoC is wider than DI. Callbacks and event listeners count as IoC too."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Don't say IoC and DI are the same thing. IoC is the principle. DI is one way to do it.",
              "Don't build dependencies with new inside a bean. That object skips the container and misses injection."
            ]
          }
        ],
        "cap": "IoC flips who's in charge of construction. The framework takes over, so your code can focus on behavior.",
        "followup_handoff": [
          "What is the difference between IoC and Dependency Injection?",
          "What is the Spring ApplicationContext?",
          "How does IoC make code more testable?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending"
      }
    },
    {
      "id": "ioc-container-004",
      "slug": "how-ioc-promotes-loose-coupling",
      "question": "Why does IoC promote loose coupling?",
      "title": "How IoC Promotes Loose Coupling",
      "direct_answer": "IoC reduces coupling because a class receives collaborators instead of choosing how to construct them. Its constructor makes requirements visible, and configuration can select an implementation. Depending on an interface is useful when there are meaningful alternatives, but DI also works with concrete classes; creating an interface for every class is not required for loose, testable code.",
      "layout_type": "concept-explainer",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 3,
      "last_updated": "2026-09-08",
      "interviewer_intent": {
        "testing": "Ability to articulate the design benefits of IoC beyond just saying it's good",
        "common_mistake": "Just saying 'reduces dependencies' without explaining what tight coupling actually costs you",
        "to_stand_out": "Use interface plus DI to show how you can swap implementations at runtime or test time"
      },
      "company_tags": [
        "amazon",
        "tcs",
        "infosys",
        "cognizant"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "How IoC Reduces Construction Coupling",
            "content": "Construction changes then stay in configuration rather than spreading through business methods. This improves focused tests and makes replacements explicit. It does not remove coupling: OrderService still depends on the PaymentGateway contract, and changing that contract affects it. Good boundaries minimize the kind of coupling that changes for unrelated reasons."
          },
          {
            "type": "key_points",
            "title": "Quick revision",
            "content": [
              "The class does not own dependency construction or configuration.",
              "Constructor injection makes required collaborators visible.",
              "Use an abstraction when it represents a useful boundary or alternatives.",
              "Runtime per-customer choices belong in a strategy/router, not ordinary one-time bean injection."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "For example, OrderService can receive PaymentGateway and a test can pass FakePaymentGateway without starting Spring. If card and bank payment must be selected for each order, inject a PaymentRouter or a map of qualified strategies and choose from order data; Spring does not automatically reinject a different field for each request.",
            "answerSize": "standard"
          },
          {
            "type": "concept_map",
            "title": "IoC and Loose Coupling",
            "content": "amber|Tight Coupling|~The new keyword|Class builds its deps|Locked to a concrete type|Hard to change or test\nblue|IoC Inverts It|~Container supplies deps|Depend on an interface|Impl chosen by config|Swap freely\nemerald|Why It Is Loose|~No direct construction|Consumer knows the contract only|New impl needs no edits|Mock in tests\nviolet|Payoff|~Flexible and testable|OCP-friendly|Parallel development|Maintainable"
          },
          {
            "type": "code_example",
            "title": "Tight Coupling vs Loose Coupling with Interfaces",
            "content": "```java\n// Tight coupling — UserService hardcoded to EmailService\nclass UserServiceTight {\n    private EmailService emailService = new EmailService(); // Cannot swap or test\n\n    public void registerUser(String email) {\n        emailService.send(email, \"Welcome!\");\n    }\n}\n\n// Loose coupling — depends on interface, implementation injected\ninterface NotificationService {\n    void send(String recipient, String message);\n}\n\n@Service\nclass UserService {\n    private final NotificationService notificationService;\n\n    // Spring injects any NotificationService implementation\n    public UserService(NotificationService notificationService) {\n        this.notificationService = notificationService;\n    }\n\n    public void registerUser(String email) {\n        notificationService.send(email, \"Welcome!\"); // Works with any implementation\n    }\n}\n\n@Service\nclass EmailNotificationService implements NotificationService {\n    public void send(String recipient, String message) { /* send email */ }\n}\n```"
          },
          {
            "type": "when_to_use",
            "title": "When Loose Coupling Pays Off",
            "content": "Use interface-based IoC when you need **multiple implementations** of the same behavior — email vs SMS notifications, MySQL vs PostgreSQL repositories, REST vs SOAP clients. Spring injects the right one based on configuration or qualifiers. It's essential for **testing**: inject a MockNotificationService in unit tests to verify UserService logic without sending real emails. Loose coupling enables **feature toggles**: switch between implementations at runtime using Spring profiles (dev profile uses EmailService, prod uses SmsService).\n\nUse it for **third-party integrations** where the external service might change — depend on a PaymentGateway interface, swap Stripe for PayPal without touching business logic. Finally, loose coupling is critical in **microservices** where you might replace an external API call with a local fallback or circuit breaker."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Depending on concrete classes instead of interfaces**: Freshers inject EmailService directly instead of NotificationService interface. This limits flexibility — you can't swap implementations without changing the consumer. Always code to interfaces.\n- **Creating dependencies with new inside beans**: Using new EmailService() defeats IoC. Spring can't manage or replace that instance. Let Spring inject all collaborators.\n- **Thinking loose coupling means zero dependencies**: Loose coupling doesn't eliminate dependencies — it eliminates control over their creation. UserService still depends on NotificationService, but it doesn't create or configure it.\n- **Not leveraging constructor injection**: Some freshers use field injection, which hides dependencies and makes testing harder. Constructor injection makes dependencies explicit and enables immutability."
          }
        ]
      },
      "followup_questions": [
        "What is the difference between tight coupling and loose coupling?",
        "How does programming to interfaces relate to IoC?",
        "How do you inject different implementations for different environments?"
      ],
      "seo": {
        "metaTitle": "How IoC Promotes Loose Coupling — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for Why does IoC promote loose coupling? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 4,
      "speakable_v2": {
        "pillar": "P02",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "A",
        "hook": "The moment a class calls new on a dependency, it's stuck with that exact class. IoC lets you swap it without touching the class.",
        "standard_example": "Spring app where OrderService depends on a NotificationService interface",
        "familiarity_anchors": [
          "loose coupling",
          "tight coupling",
          "program to an interface",
          "dependency injection"
        ],
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "Loose coupling means a class doesn't know how its dependencies are built. With IoC, the class just asks for an interface, and Spring injects whatever implementation is registered. There's no new call and no knowledge of concrete types. Swapping the implementation becomes a config change, not a code change."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "Tight coupling makes maintenance painful. If OrderService calls new EmailService and that constructor changes, you fix OrderService too. With an interface and IoC, you update the implementation once. Every class that injects it picks up the change for free."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "Tight coupling: a class calls new ConcreteClass. Any change to that class hits the caller.",
              "Loose coupling: a class depends on an interface. It doesn't care which class implements it.",
              "IoC makes it work. Spring decides which implementation to inject, and the caller stays the same.",
              "Testing win: inject a fake in tests without touching the real class. The fake implements the same interface."
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "In a Spring app, OrderService takes a NotificationService in its constructor. Spring injects EmailNotificationService in production. In tests, you inject a mock instead. Here's the non-obvious part. This only works if you code to the interface. Declare EmailNotificationService directly and you're locked in again."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Don't inject a concrete class when an interface exists. That throws away the loose coupling IoC gives you.",
              "Don't add interfaces everywhere. If there's only ever one implementation, the extra layer is just ceremony."
            ]
          }
        ],
        "cap": "IoC creates loose coupling by removing new from your classes. Depend on interfaces, and your code stays open to change.",
        "followup_handoff": [
          "What is the difference between tight coupling and loose coupling?",
          "How does programming to interfaces relate to IoC?",
          "How do you inject different implementations for different environments?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending"
      }
    },
    {
      "id": "ioc-container-006",
      "slug": "what-is-a-spring-bean",
      "question": "What is a Spring bean and what makes a class a Spring bean?",
      "title": "What Is a Spring Bean?",
      "direct_answer": "A Spring bean is an object registered with and obtained from a Spring BeanFactory/ApplicationContext. The container applies configured creation, dependency injection, post-processing, scope, and lifecycle behavior. Not every bean is physically created by Spring, and destruction is not complete for every scope: factory methods can return existing objects, and prototype destruction is the caller's responsibility.",
      "layout_type": "concept-explainer",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 3,
      "last_updated": "2026-09-08",
      "interviewer_intent": {
        "testing": "Fundamental understanding of what distinguishes a Spring-managed bean from a regular Java object in the same application",
        "common_mistake": "Assuming every object you create with new keyword in a Spring app is automatically a Spring bean and gets dependency injection",
        "to_stand_out": "Clearly distinguish that beans have a managed lifecycle and can be injected, while regular objects you create with new are invisible to Spring and don't get any container features"
      },
      "company_tags": [
        "tcs",
        "infosys",
        "wipro",
        "cognizant"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "What Makes an Object a Spring Bean",
            "content": "For singleton beans, the container normally invokes destruction callbacks when the context closes. For prototype beans, it initializes and hands out instances but does not later track every instance for destruction. Request/session scopes have their own lifecycle.\n\nPost-processors may inject fields, invoke aware callbacks, apply @PostConstruct, and create AOP proxies. Consequently, the reference injected into consumers can be a managed proxy rather than the raw constructed object."
          },
          {
            "type": "key_points",
            "title": "Quick revision",
            "content": [
              "Component scanning, @Bean methods, imports, XML, and programmatic registration can define beans.",
              "BeanPostProcessors can wrap or replace the exposed object with a proxy.",
              "Scope controls instance ownership and lifetime.",
              "An object created with new outside the container is not a bean merely because its class has @Component."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "@Service on PricingService makes it a component candidate only when its package is scanned. A @Bean method can register a third-party client. A FactoryBean can create another object, and an existing singleton can be registered programmatically. The defining fact is container registration and management, not one annotation or the claim that Spring always called the constructor itself.",
            "answerSize": "standard"
          },
          {
            "type": "concept_map",
            "title": "Spring Bean at a Glance",
            "content": "amber|What It Is|~Container-managed object|Created by Spring|Not by your new|Lives in the context\nblue|What Makes One|~Registration|@Component plus scan|Or a @Bean method|Then the container builds it\nemerald|What You Get|~Container services|DI and lifecycle|Scope and AOP|Managed for you\nviolet|Plain Object vs Bean|~The difference|new equals unmanaged|Bean equals wired and scoped|Spring controls it"
          },
          {
            "type": "code_example",
            "title": "Creating Spring Beans: Annotations vs Configuration",
            "content": "```java\n// Method 1: Stereotype annotations (auto-detected via component scanning)\n@Service  // Makes this class a Spring bean\npublic class OrderService {\n    private final OrderRepository repository;\n\n    public OrderService(OrderRepository repository) {  // Spring injects repository\n        this.repository = repository;\n    }\n\n    public void placeOrder(Order order) {\n        repository.save(order);\n    }\n}\n\n@Repository  // Also a Spring bean, specialized for data access\npublic class OrderRepository {\n    // Spring manages this as a singleton bean\n}\n\n// Method 2: Explicit @Bean declaration in configuration class\n@Configuration\npublic class AppConfig {\n    \n    @Bean  // Spring calls this method and manages the returned object\n    public ObjectMapper jsonMapper() {\n        var mapper = new ObjectMapper();\n        mapper.configure(DeserializationFeature.FAIL_ON_UNKNOWN_PROPERTIES, false);\n        return mapper;  // This ObjectMapper instance becomes a Spring bean\n    }\n\n    @Bean\n    public RestTemplate restTemplate(ObjectMapper mapper) {  // Spring injects the bean above\n        return new RestTemplate();\n    }\n}\n\n// NOT a Spring bean (plain Java object)\npublic class OrderValidator {\n    public boolean validate(Order order) {\n        return order.total() > 0;  // Spring doesn't manage this, no DI happens\n    }\n}\n```"
          },
          {
            "type": "when_to_use",
            "title": "When to Make a Class a Spring Bean",
            "content": "Make a class a Spring bean when it needs **container-managed features**: dependency injection (it collaborates with other beans), lifecycle management (initialization or cleanup logic), or AOP capabilities (transactions, caching, security).\n\nUse **@Service** for business logic classes like PaymentService or UserService.\n\nUse **@Repository** for data access classes like CustomerRepository (gets automatic exception translation).\n\nUse **@Controller** or @RestController for web layer classes handling HTTP requests.\n\nUse **@Component** as a generic stereotype when the class doesn't fit the other categories.\n\nUse **@Bean methods** in @Configuration when you need to configure third-party classes (like ObjectMapper, RestTemplate) that you don't control the source code for, or when instantiation requires complex logic (conditional creation, builder patterns). Don't make it a bean if it's a **simple value object** (DTOs, records, entities), **utility class** with static methods, or **short-lived object** created frequently (better as a prototype or plain new instance)."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Mixing new with DI**: Freshers annotate a class with @Service but then create instances with new OrderService() in other beans, bypassing Spring's container. That new instance isn't a bean — it won't get dependencies injected, won't be transactional, and lives outside Spring's control. Always let Spring create beans by injecting them.\n- **Forgetting component scanning**: Annotating a class with @Component but placing it outside the base package Spring scans (usually the package with @SpringBootApplication), so Spring never finds it. Check your component scan configuration or move the class under the scanned package.\n- **Expecting every object to be a bean**: Treating JPA entities or DTOs like Order or CustomerDTO as Spring beans and trying to inject dependencies into them. Entities are managed by JPA, not Spring — they're data objects, not service layer components. If an entity needs business logic, put that logic in a separate @Service bean."
          }
        ]
      },
      "followup_questions": [
        "Is every Java object in a Spring app a Spring bean?",
        "What is the difference between a Spring bean and a POJO?",
        "How does Spring know which classes to create as beans?"
      ],
      "seo": {
        "metaTitle": "What Is a Spring Bean? — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a Spring bean and what makes a class a Spring bean? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 6,
      "speakable_v2": {
        "pillar": "P02",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "A",
        "hook": "Not every Java object is a Spring bean. Only the ones Spring knows about, creates, and manages from startup to shutdown.",
        "standard_example": "Spring app where @Service UserService and @Repository UserRepository are beans",
        "familiarity_anchors": [
          "Spring bean",
          "@Component",
          "managed lifecycle",
          "ApplicationContext"
        ],
        "familiarity_override": true,
        "familiarity_override_reason": "This question defines what a Spring bean is; the codex bean-lifecycle phase example is the wrong frame for it.",
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "A Spring bean is an object the Spring container creates and manages. Spring builds it, injects its dependencies, and its @PostConstruct runs after dependency injection. At shutdown, Spring destroys it. A plain object you make with new isn't a bean. Spring doesn't even know it exists."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "Spring needs to know which objects to manage. Not everything should be a bean. Domain objects like User and Order are created per request and then garbage collected. Beans are the long-lived service objects Spring wires together at startup."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "Register with @Component or its cousins. Spring finds the class during component scanning and makes it a bean.",
              "Register with a @Bean method in a @Configuration class. You write the creation code, Spring keeps the result.",
              "A plain object made with new isn't registered, isn't managed, and can't be injected.",
              "Beans are singletons by default. One instance per ApplicationContext, shared everywhere."
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "In a Spring app, UserService and UserRepository are beans. Spring creates them once and wires them together. A User object returned from findById isn't a bean. It's plain data, made per request, then garbage collected. Here's the non-obvious part. Entities and DTOs are never beans, even inside a Spring app."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Don't put @Component on domain objects like User or Order. They're data holders, not shared services.",
              "Don't create a bean with new in your code. That copy skips the container and gets no injection."
            ]
          }
        ],
        "cap": "A Spring bean is any object the container owns. Use @Component or @Bean, and Spring manages it for you.",
        "followup_handoff": [
          "Is every Java object in a Spring app a Spring bean?",
          "What is the difference between a Spring bean and a POJO?",
          "How does Spring know which classes to create as beans?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending"
      }
    },
    {
      "id": "ioc-container-002",
      "slug": "spring-ioc-container-types-beanfactory-vs-applicationcontext",
      "question": "What is the Spring IoC container and what are its types?",
      "title": "Spring IoC Container Types: BeanFactory vs ApplicationContext",
      "direct_answer": "BeanFactory is Spring's basic bean-container interface. ApplicationContext extends it and adds an application-level programming model for events, messages, resources, environment support, and automatic discovery of many infrastructure components. The common claim that BeanFactory is always lazy and ApplicationContext is always eager is too simple: creation depends on scope, lazy flags, and whether singleton pre-instantiation is requested.",
      "layout_type": "comparison-arena",
      "difficulty": "medium",
      "importance": "high",
      "reading_time_minutes": 4,
      "last_updated": "2026-09-08",
      "interviewer_intent": {
        "testing": "Knowledge of Spring's container hierarchy and which container is actually used in production",
        "common_mistake": "Not knowing BeanFactory exists or thinking it's used in modern Spring Boot applications",
        "to_stand_out": "Name specific ApplicationContext implementations like AnnotationConfigApplicationContext and their use cases"
      },
      "company_tags": [
        "amazon",
        "tcs",
        "infosys",
        "cognizant"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "BeanFactory and ApplicationContext",
            "content": "BeanFactory is the container contract underneath Spring's dependency injection. ApplicationContext builds an application environment around it. That includes resource loading, messages for internationalization, application events, environment and profile access, and lifecycle integration.\n\nThe eager-versus-lazy comparison is only a shortcut. During refresh, a normal ApplicationContext asks its bean factory to pre-instantiate non-lazy singleton definitions. A lazy singleton waits for first use, and a prototype is created on each resolution. Code using DefaultListableBeanFactory directly controls when pre-instantiation happens. Compare capabilities and lifecycle orchestration rather than memorizing 'BeanFactory lazy, ApplicationContext eager' as an absolute rule."
          },
          {
            "type": "key_points",
            "title": "Quick revision",
            "content": [
              "BeanFactory provides bean registration, lookup, dependency resolution, and lifecycle foundations.",
              "ApplicationContext extends BeanFactory with events, message resolution, resources, and application infrastructure.",
              "A normal context pre-instantiates non-lazy singleton beans during refresh.",
              "Lazy beans, prototypes, and direct BeanFactory use have different creation timing."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "Most applications, including Spring Boot apps, use an ApplicationContext. It is a BeanFactory plus higher-level facilities and it automatically recognizes post-processors and other infrastructure during refresh. A plain BeanFactory is useful for low-level or constrained container use but needs more manual setup. ApplicationContext commonly creates non-lazy singleton beans during refresh, which catches configuration errors early. That is a default behavior, not an essential definition: @Lazy can defer a singleton, prototypes are created on request, and a configurable BeanFactory can explicitly pre-instantiate singletons.",
            "answerSize": "standard"
          },
          {
            "type": "concept_map",
            "title": "BeanFactory vs ApplicationContext",
            "content": "amber|BeanFactory|~Basic container|Lazy bean creation|Core DI only|Rarely used directly\nblue|ApplicationContext|~Full-featured|Eager singletons|Events, i18n, AOP|The standard choice\nemerald|Key Difference|~Features|BeanFactory is minimal|ApplicationContext is a superset|Enterprise-ready\nviolet|Which to Use|~ApplicationContext|Boot uses it|BeanFactory for tiny/memory|Almost always ApplicationContext"
          },
          {
            "type": "comparison_table",
            "title": "BeanFactory vs ApplicationContext",
            "content": "| Feature | BeanFactory | ApplicationContext |\n| --- | --- | --- |\n| Bean initialization | Lazy — created on first getBean call | Eager — singletons created at startup |\n| AOP support | Not supported | Fully supported for transactions and aspects |\n| Event mechanism | Not available | Application events and listeners built-in |\n| Use case | Memory-constrained legacy systems only | All modern Spring Boot applications |\n| Common implementations | XmlBeanFactory (deprecated) | AnnotationConfigApplicationContext, WebApplicationContext |"
          },
          {
            "type": "before_code",
            "title": "BeanFactory - minimal, lazy",
            "content": "```java\n// Basic container: beans created lazily, few features\nBeanFactory factory =\n    new XmlBeanFactory(new ClassPathResource(\"beans.xml\"));\nOrderService svc = factory.getBean(OrderService.class); // built now\n```"
          },
          {
            "type": "after_code",
            "title": "ApplicationContext - full-featured",
            "content": "```java\n// Standard container: eager singletons, events, i18n, AOP\nApplicationContext ctx =\n    new AnnotationConfigApplicationContext(AppConfig.class);\nOrderService svc = ctx.getBean(OrderService.class);\n```\n`ApplicationContext` is a superset of `BeanFactory`; Spring Boot creates one for you, so you rarely touch either directly."
          },
          {
            "type": "when_to_use",
            "title": "Which Container for Which Scenario",
            "content": "Use **ApplicationContext** for all production Spring Boot applications — it's the standard and provides the features you need: transaction management, AOP, event-driven logic, and web request handling. Spring Boot auto-configures it for you.\n\nUse **AnnotationConfigApplicationContext** when building standalone Java applications (non-web) that need Spring's DI — you pass it your @Configuration classes. For web apps, Spring Boot uses **AnnotationConfigServletWebServerApplicationContext**, which integrates with embedded Tomcat. BeanFactory is only relevant in **memory-critical embedded systems** or when maintaining very old Spring 1.x codebases. In interviews, knowing BeanFactory exists shows depth, but emphasize that ApplicationContext is always used in real projects. Never initialize the container manually in Spring Boot — SpringApplication.run does it for you."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Thinking BeanFactory is still used**: Freshers sometimes believe BeanFactory is a viable option for new projects. In reality, it's deprecated for most use cases — ApplicationContext is the modern standard.\n- **Not understanding eager vs lazy**: Some think all beans are lazy by default. With ApplicationContext, singleton beans initialize at startup, which catches configuration errors early rather than at runtime.\n- **Manually creating ApplicationContext in Spring Boot**: Writing new AnnotationConfigApplicationContext in a Spring Boot app breaks auto-configuration. SpringApplication.run creates and configures the container — never bypass it.\n- **Confusing container with context**: Saying 'Spring context' and 'IoC container' interchangeably is fine, but know that ApplicationContext is the interface name and the container is the runtime engine it provides."
          }
        ]
      },
      "followup_questions": [
        "What is the difference between BeanFactory and ApplicationContext?",
        "What ApplicationContext implementation does Spring Boot create?",
        "How do you get the ApplicationContext in a Spring bean?"
      ],
      "seo": {
        "metaTitle": "Spring IoC Container Types: BeanFactory vs ApplicationContext — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is the Spring IoC container and what are its types? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 2,
      "speakable_v2": {
        "pillar": "P02",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "B",
        "hook": "BeanFactory gives you the bare-minimum container. ApplicationContext adds everything you actually want: events, AOP, profiles, and eager startup.",
        "standard_example": "Spring Boot app where SpringApplication.run returns an ApplicationContext",
        "familiarity_anchors": [
          "BeanFactory",
          "ApplicationContext",
          "IoC container",
          "getBean"
        ],
        "beats": [
          {
            "kind": "what_each_is",
            "layout": "paragraph",
            "text": "BeanFactory is the basic IoC container. It creates beans lazily, only when getBean's called, and it does little else. ApplicationContext extends BeanFactory and adds the enterprise features. It builds singletons eagerly at startup, and it brings events, i18n, and AOP."
          },
          {
            "kind": "differences",
            "layout": "mini_table",
            "columns": [
              "BeanFactory",
              "ApplicationContext"
            ],
            "rows": [
              {
                "axis": "Bean startup",
                "values": [
                  "Lazy. Built on first getBean call.",
                  "Eager. Singletons built at startup."
                ]
              },
              {
                "axis": "Extra features",
                "values": [
                  "None. Just core DI.",
                  "AOP, events, and i18n built in."
                ]
              },
              {
                "axis": "Where it's used",
                "values": [
                  "Rare. Legacy or tiny memory.",
                  "The default in Spring Boot."
                ]
              }
            ]
          },
          {
            "kind": "when_to_pick",
            "layout": "bullets",
            "items": [
              "Always use ApplicationContext in Spring Boot. SpringApplication.run creates one for you.",
              "Know BeanFactory for questions about the container hierarchy. Don't reach for it in practice.",
              "For web apps, Spring Boot picks the servlet-based context automatically. You don't configure it."
            ]
          },
          {
            "kind": "tiny_example",
            "layout": "paragraph",
            "text": "You almost never touch either directly. SpringApplication.run hands you an ApplicationContext. Important: it injects with @Autowired, but using getBean for routine lookups is the Service Locator anti-pattern. Inject the bean you need instead."
          }
        ],
        "cap": "For any Spring Boot app, ApplicationContext gives you what you need. BeanFactory is just the interface it extends.",
        "followup_handoff": [
          "What is the difference between BeanFactory and ApplicationContext?",
          "What ApplicationContext implementation does Spring Boot create?",
          "How do you get the ApplicationContext in a Spring bean?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending"
      }
    },
    {
      "id": "ioc-container-003",
      "slug": "spring-bean-lifecycle-explained",
      "question": "What is the lifecycle of a Spring bean?",
      "title": "Spring Bean Lifecycle Explained",
      "direct_answer": "A common singleton lifecycle is definition -> instantiation -> dependency population -> aware callbacks -> BeanPostProcessor before-initialization -> @PostConstruct/other init callbacks -> after-initialization (possibly a proxy) -> use -> destruction callbacks when the context closes. Constructor injection happens during instantiation rather than in a later generic property-injection step. Scope and custom post-processors change the details.",
      "layout_type": "lifecycle-timeline",
      "difficulty": "medium",
      "importance": "high",
      "reading_time_minutes": 4,
      "last_updated": "2026-09-08",
      "interviewer_intent": {
        "testing": "Understanding of bean lifecycle hooks for proper initialization and cleanup logic",
        "common_mistake": "Using the constructor for logic that depends on injected fields — injection happens after construction",
        "to_stand_out": "Explain why @PostConstruct is needed and give a concrete use case like opening database connections"
      },
      "company_tags": [
        "amazon",
        "microsoft",
        "infosys",
        "tcs"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Spring Bean Lifecycle Beyond the Short Version",
            "content": "The familiar constructor -> injection -> @PostConstruct -> @PreDestroy diagram is a useful first view but misses extension points and misplaces constructor injection. Destruction also depends on scope.\n\nDo not start threads or publish this from a constructor before the bean is fully processed. Use lifecycle mechanisms suited to the work, and make external resources safe under both graceful shutdown and abrupt termination."
          },
          {
            "type": "key_points",
            "title": "Quick revision",
            "content": [
              "Instantiation and constructor dependency resolution happen together.",
              "BeanPostProcessors can inspect, wrap, or replace beans before exposure.",
              "Initialization callback order can include @PostConstruct, InitializingBean, and custom init methods.",
              "Prototype beans do not receive automatic destruction callbacks from the container."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "A singleton service is constructed after its constructor dependencies are resolved. Spring then populates setter/field properties, invokes relevant aware interfaces, runs registered post-processors and initialization callbacks, and may expose an AOP proxy. On normal context shutdown, @PreDestroy or other destruction callbacks run for managed singleton scopes. A hard process kill may prevent cleanup.\n\nFor example, a connection-owning client can validate its configuration in @PostConstruct and close its executor in @PreDestroy. Destruction callbacks apply to managed lifecycle beans, not automatically to every prototype instance.",
            "answerSize": "standard"
          },
          {
            "type": "flow_diagram",
            "title": "Spring Bean Lifecycle",
            "content": "```mermaid\nflowchart TD\n  A[Instantiate bean] --> B[Populate properties / DI]\n  B --> C[Aware callbacks<br/>BeanNameAware, etc.]\n  C --> D[BeanPostProcessor<br/>before init]\n  D --> E[\"@PostConstruct /<br/>afterPropertiesSet / init-method\"]\n  E --> F[BeanPostProcessor<br/>after init]\n  F --> G[Bean ready to use]\n  G --> H[\"@PreDestroy /<br/>destroy-method\"]\n```"
          },
          {
            "type": "code_example",
            "title": "Bean Lifecycle with Initialization and Cleanup",
            "content": "```java\n@Service\npublic class ReportService {\n    private final DataSource dataSource;\n    private Connection connection;\n\n    // Step 1: Constructor called (dependency injection happens here)\n    public ReportService(DataSource dataSource) {\n        this.dataSource = dataSource;\n        System.out.println(\"Constructor: dataSource injected\");\n    }\n\n    // Step 2: After dependency injection, before bean is used\n    @PostConstruct\n    public void initialize() throws SQLException {\n        // Now dataSource is guaranteed to be non-null\n        this.connection = dataSource.getConnection();\n        System.out.println(\"PostConstruct: connection opened\");\n    }\n\n    public List<Report> generateReports() {\n        // Bean is ready — use connection for queries\n        return List.of();\n    }\n\n    // Step 3: Before container shutdown\n    @PreDestroy\n    public void cleanup() throws SQLException {\n        if (connection != null && !connection.isClosed()) {\n            connection.close();\n            System.out.println(\"PreDestroy: connection closed\");\n        }\n    }\n}\n```"
          },
          {
            "type": "when_to_use",
            "title": "When Lifecycle Hooks Are Essential",
            "content": "Use **@PostConstruct** when initialization logic depends on injected dependencies — database connection pools, caching warmup, or loading configuration from external services. It's the right place to validate that required dependencies are configured correctly. For example, checking that a DataSource connection can be established before the app starts serving traffic.\n\nUse **@PreDestroy** for cleanup tasks: closing file handles, shutting down thread pools, flushing in-memory caches to disk, or releasing external resources like database connections. Without @PreDestroy, your app might leave connections open when redeploying or shutting down in production. Lifecycle hooks are also useful for **scheduled tasks**: starting background jobs in @PostConstruct, stopping them in @PreDestroy.\n\nAvoid using constructor for complex logic — exceptions in constructors are harder to debug than in @PostConstruct, where Spring logs clear error messages."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Using constructor for dependency-dependent logic**: Freshers often initialize caches or open connections in the constructor. This fails because injected fields are null until after construction. Always use @PostConstruct for logic that needs injected dependencies.\n- **Forgetting @PreDestroy cleanup**: Not closing resources leads to connection leaks, file handle exhaustion, or thread pools that keep the JVM alive after shutdown. If you open it in @PostConstruct, close it in @PreDestroy.\n- **Assuming lifecycle for prototype beans**: Spring doesn't call @PreDestroy on prototype-scoped beans. You must manage cleanup manually. Freshers often expect Spring to destroy prototypes automatically, causing resource leaks.\n- **Blocking in @PostConstruct**: Long-running initialization (like warming a large cache) blocks application startup. Consider async initialization with @Async or lazy loading strategies instead."
          }
        ]
      },
      "followup_questions": [
        "Why should initialization code go in @PostConstruct rather than the constructor?",
        "What is InitializingBean and DisposableBean?",
        "When is @PreDestroy not called?"
      ],
      "seo": {
        "metaTitle": "Spring Bean Lifecycle Explained — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is the lifecycle of a Spring bean? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 3,
      "speakable_v2": {
        "pillar": "P02",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "A",
        "hook": "Your bean's constructor runs before injection finishes. @PostConstruct is the safe spot to set up anything that needs an injected dependency.",
        "standard_example": "A bean going instantiate then populate then @PostConstruct then ready then @PreDestroy",
        "familiarity_anchors": [
          "bean lifecycle",
          "@PostConstruct",
          "@PreDestroy",
          "dependency injection"
        ],
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "A Spring bean goes through clear phases. Spring calls the constructor, then injects dependencies. Its @PostConstruct runs after dependency injection, so setup code sees a fully wired bean. The bean serves requests until shutdown. Then @PreDestroy runs on context close."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "The lifecycle gives you clean hooks for setup and cleanup. @PostConstruct lets you run setup after injection is done, like loading a cache or warming a pool. @PreDestroy lets you release resources on the way out, instead of trusting the garbage collector."
          },
          {
            "kind": "parts_or_states",
            "layout": "ordered_list",
            "steps": [
              "Instantiation. Spring calls the constructor. The @Autowired fields aren't set yet.",
              "Injection. Spring sets the @Autowired fields and calls setters. Now dependencies are wired.",
              "@PostConstruct. Your init method runs. All dependencies are available here.",
              "In use. The bean handles real requests.",
              "@PreDestroy. Spring calls it just before shutdown. Release connections and flush caches."
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "Say a CacheService has an @Autowired DataSource. Its @PostConstruct method loads the cache at startup. Put that in the constructor and the DataSource is still null. @PreDestroy then clears the cache before shutdown. Here's the non-obvious part. With constructor injection the field is ready in the constructor, so @PostConstruct only matters for field or setter injection."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Don't read @Autowired fields in the constructor. They're null at that point. Use @PostConstruct instead.",
              "@PreDestroy doesn't run for prototype beans or a hard kill. Don't rely on it for critical saves."
            ]
          }
        ],
        "cap": "Know the order: construct, inject, @PostConstruct, use, @PreDestroy. That sequence prevents the usual init bugs.",
        "followup_handoff": [
          "Why should initialization code go in @PostConstruct rather than the constructor?",
          "What is InitializingBean and DisposableBean?",
          "When is @PreDestroy not called?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending"
      }
    },
    {
      "id": "ioc-container-005",
      "slug": "spring-applicationcontext-access-from-a-bean",
      "question": "What is ApplicationContext and how do you access it in a Spring bean?",
      "title": "Spring ApplicationContext Access from a Bean",
      "direct_answer": "ApplicationContext is Spring's application-level container API for beans, events, resources, messages, environment, and lifecycle. A managed bean can receive it through constructor injection, @Autowired, or ApplicationContextAware. Direct context lookup is useful for framework/infrastructure cases, but ordinary business code should usually inject the specific dependency or ObjectProvider instead of turning the context into a service locator.",
      "layout_type": "concept-explainer",
      "difficulty": "medium",
      "importance": "medium",
      "reading_time_minutes": 3,
      "last_updated": "2026-09-08",
      "interviewer_intent": {
        "testing": "Understanding of the container itself and practical knowledge of accessing it from within managed beans",
        "common_mistake": "Using ApplicationContext.getBean() to manually look up dependencies instead of leveraging proper dependency injection",
        "to_stand_out": "Explain that directly injecting ApplicationContext implements the service locator anti-pattern and should only be used for framework-level operations like dynamic bean lookup or event publishing"
      },
      "company_tags": [
        "amazon",
        "infosys",
        "tcs",
        "capgemini"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Accessing ApplicationContext Without Hiding Dependencies",
            "content": "ApplicationContext extends BeanFactory capabilities with broader application services and automatic recognition of many infrastructure beans. It is also a ResourceLoader and MessageSource.\n\nWhen dynamic lookup is truly needed, constrain it by type and document ownership. Avoid storing the context in a global static holder, which hides lifecycle boundaries and creates test-order problems."
          },
          {
            "type": "key_points",
            "title": "Quick revision",
            "content": [
              "Constructor injection can receive ApplicationContext like another resolvable dependency.",
              "ApplicationContextAware is a lifecycle callback that couples the class to Spring.",
              "ObjectProvider supports optional, lazy, or repeated typed resolution with less container exposure.",
              "Publish events through ApplicationEventPublisher when only event publishing is needed."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "A plugin registry that must resolve beans dynamically may justify a typed context lookup. A PaymentService that simply needs PaymentGateway should inject PaymentGateway directly; calling context.getBean(PaymentGateway.class) hides the requirement and makes plain tests harder. If it only publishes events, inject ApplicationEventPublisher rather than the whole context.",
            "answerSize": "compact"
          },
          {
            "type": "concept_map",
            "title": "ApplicationContext at a Glance",
            "content": "amber|What It Is|~The Spring container|Holds all beans|Does DI and lifecycle|A central registry\nblue|How to Access|~Usually you do not|Spring injects beans|Or ApplicationContextAware|getBean as a last resort\nemerald|Common Types|~Pick by app|AnnotationConfigApplicationContext|WebApplicationContext|Boot auto-creates one\nviolet|Extra Powers|~Beyond DI|Events and i18n|Resource loading|Environment / profiles"
          },
          {
            "type": "code_example",
            "title": "Two Ways to Access ApplicationContext in a Bean",
            "content": "```java\n// Method 1: Direct injection (simpler, preferred for one-time use)\n@Service\npublic class ReportService {\n    private final ApplicationContext context;\n\n    public ReportService(ApplicationContext context) {\n        this.context = context;  // Spring injects the container itself\n    }\n\n    public void generateDynamicReport(String reportType) {\n        // Dynamic bean lookup based on runtime input\n        var generator = context.getBean(reportType + \"Generator\", ReportGenerator.class);\n        generator.generate();\n    }\n}\n\n// Method 2: ApplicationContextAware interface (for framework code)\n@Component\npublic class BeanRegistryService implements ApplicationContextAware {\n    private ApplicationContext context;\n\n    @Override\n    public void setApplicationContext(ApplicationContext ctx) {\n        this.context = ctx;  // Spring calls this during bean initialization\n    }\n\n    public List<String> listAllBeans() {\n        return List.of(context.getBeanDefinitionNames());\n    }\n}\n```"
          },
          {
            "type": "when_to_use",
            "title": "When to Access ApplicationContext Directly",
            "content": "Use ApplicationContext access sparingly — only when dependency injection alone can't solve the problem. **Dynamic bean lookup**: when you need to select a bean at runtime based on user input or configuration (like a plugin system where bean names come from a database). **Event publishing**: to broadcast application events that multiple listeners can react to, like sending a UserRegisteredEvent after signup. **Prototype bean creation**: when you need multiple instances of a prototype-scoped bean within a singleton (calling getBean() each time).\n\nAvoid it for normal dependencies — if you're calling getBean() to wire up collaborators, you're bypassing Spring's DI and creating tight coupling to the container itself."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Service Locator Anti-Pattern**: Freshers inject ApplicationContext and call getBean() everywhere to fetch dependencies instead of declaring them as constructor parameters. This hides dependencies, makes testing harder (can't mock easily), and defeats the purpose of DI. Spring should inject what you need — don't go hunting for beans yourself.\n- **Overusing for Static Access**: Storing ApplicationContext in a static field to access beans from non-Spring classes (like utility methods). This creates a global singleton, breaks testability, and couples your code to Spring. If a class needs Spring beans, make it a Spring bean too.\n- **Ignoring Constructor Injection**: Using ApplicationContext to fetch optional dependencies instead of using Optional<T> or @Autowired(required=false) on a constructor parameter, which is cleaner and more explicit."
          }
        ]
      },
      "followup_questions": [
        "What is the Service Locator anti-pattern?",
        "What is ApplicationContextAware?",
        "How is ApplicationContext different from BeanFactory?"
      ],
      "seo": {
        "metaTitle": "Spring ApplicationContext Access from a Bean — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is ApplicationContext and how do you access it in a Spring bean? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 5,
      "speakable_v2": {
        "pillar": "P02",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "A",
        "hook": "ApplicationContext injects like any other bean. But calling getBean inside business logic is a code smell worth avoiding.",
        "standard_example": "Spring app where OrderService injects ApplicationEventPublisher to publish an event",
        "familiarity_anchors": [
          "ApplicationContext",
          "ApplicationContextAware",
          "getBean",
          "dependency injection"
        ],
        "familiarity_override": true,
        "familiarity_override_reason": "This question is about reaching the container from a bean; the codex bean-lifecycle example doesn't fit.",
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "ApplicationContext is Spring's full IoC container. A bean can grab it in two ways. One, inject it with @Autowired. Two, implement ApplicationContextAware. Spring sets it during bean creation, the same phase where @PostConstruct runs after dependency injection."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "Sometimes a bean needs container powers. It might publish an event. It might read a property. It might look up a bean whose type is only known at runtime. ApplicationContext opens that door."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "Inject ApplicationContext with @Autowired. It's simple and readable.",
              "Implement ApplicationContextAware. It shows the class leans on the container.",
              "Call getBean to look up a bean at runtime. Fair when the type is only known then.",
              "Call publishEvent to send an event to listeners. That's the main clean use."
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "In a Spring app, OrderService saves an order. Then it fires an event. It injects ApplicationEventPublisher, a narrower interface, and calls publishEvent. That's clean. Here's the non-obvious part. Calling getBean for EmailService inside a method is the Service Locator anti-pattern. Just inject EmailService."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Don't use getBean for a dependency you know at compile time. Inject it with @Autowired.",
              "Prefer ApplicationEventPublisher for events. It's a narrow interface that says what you need."
            ]
          }
        ],
        "cap": "Inject ApplicationContext only for container needs like events or lookup. For known dependencies, inject them directly.",
        "followup_handoff": [
          "What is the Service Locator anti-pattern?",
          "What is ApplicationContextAware?",
          "How is ApplicationContext different from BeanFactory?"
        ],
        "tts_overrides": {},
        "speakable_status": "pending"
      }
    }
  ]
}

{
  "topic": "Encapsulation",
  "topicSlug": "encapsulation",
  "questions": [
    {
      "id": "encapsulation-q001",
      "slug": "ruby-encapsulation-basics",
      "question": "What is encapsulation in Ruby?",
      "title": "Encapsulation in Ruby",
      "direct_answer": "Encapsulation = bundle state with the methods that act on it, and hide internals from outside. In Ruby: instance variables (@x) are ALWAYS private — outsiders can't read or set them directly. You expose state via methods (attr_reader :name, a custom balance method) and control mutation (def balance=(v); raise if v < 0; @balance = v; end). Private methods (private keyword; def private_m; end) can only be called without a receiver (self.private_m raises; just private_m works) — they're internal helpers. Protected (protected) can be called by other instances of the same class. Encapsulation keeps invariants: a BankAccount's @balance is only changed through deposit/withdraw, which can enforce rules. Use attr_reader over attr_accessor; make helpers private; expose a small public API. This decouples internals from users.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know @vars are always private, private methods need no receiver, and you expose state via methods.",
        "common_mistake": "Trying self.private_m (raises — private methods take no receiver), or using attr_accessor when attr_reader would do.",
        "to_stand_out": "Encapsulation = bundle state + methods, hide internals. @vars always private. private = no receiver; protected = same class. Expose via attr_reader."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Bundle and Hide",
            "content": "Encapsulation equals bundle state with the methods that act on it, and hide internals from outside. In Ruby: instance variables (x) are ALWAYS private — outsiders can't read or set them directly. You expose state via methods (attr_reader name, a custom balance method) and control mutation (def balance equals v; raise if v less than 0; balance var v; end). Private methods (private keyword; def private m; end) can only be called without a receiver (self dot private m raises; just private m works) — they're internal helpers. Protected (protected) can be called by other instances of the same class. Encapsulation keeps invariants: a BankAccount's balance is only changed through deposit withdraw, which can enforce rules. Use attr_reader over attr_accessor; make helpers private; expose a small public API. This decouples internals from users."
          },
          {
            "type": "flow_diagram",
            "title": "Encapsulation",
            "content": "bundle state + methods; hide internals. @vars always private. private = no receiver; protected = same class. Expose via attr_reader.\n\n```mermaid\nflowchart LR\n  V[@vars: always private] --> M[expose via methods]\n  M --> A[attr_reader :name]\n  Pr[private] --> N[no receiver: self.m raises]\n  Pt[protected] --> S[same class only]\n```"
          },
          {
            "type": "code_example",
            "title": "Encapsulation",
            "content": "```ruby\nclass BankAccount\n  def initialize(owner); @owner = owner; @balance = 0; end\n\n  # PUBLIC API: expose state via methods\n  attr_reader :owner, :balance\n\n  # Controlled mutation (enforces an INVARIANT)\n  def deposit(amount)\n    raise 'must be positive' if amount <= 0\n    @balance += amount\n  end\n  def withdraw(amount)\n    raise 'insufficient' if amount > @balance\n    @balance -= amount\n  end\n\n  private                 # everything below is private\n  def fee; 1; end         # internal helper (no receiver)\n  def apply_fee; @balance -= fee; end\nend\n\nacct = BankAccount.new('Ada')\nacct.owner            # 'Ada'   (public getter)\nacct.balance          # 0      (public getter)\nacct.deposit(100)     # controlled mutation\nacct.withdraw(30)\nacct.balance          # 70\n\n# @balance is PRIVATE — can't read/write directly:\n# acct.balance = -5    # NoMethodError (no setter) — invariant protected\n# acct.@balance        # syntax error\n\n# Private methods: called WITHOUT a receiver:\n# acct.fee             # NoMethodError (private, needs receiver)\n# self.fee             # NoMethodError (private can't take a receiver)\nfee                   # works only INSIDE the class (no receiver)\n\n# Protected: callable by other instances of the same class\nclass Pair\n  protected\n  def secret; 42; end\n  def other_secret(p); p.secret; end   # ok: same class\nend\n```"
          },
          {
            "type": "comparison_table",
            "title": "Encapsulation",
            "columns": [
              "Mechanism",
              "Visibility"
            ],
            "rows": [
              [
                "@instance vars",
                "always private"
              ],
              [
                "attr_reader",
                "public getter"
              ],
              [
                "private",
                "no receiver (self.m raises)"
              ],
              [
                "protected",
                "same-class instances"
              ],
              [
                "public (default)",
                "anyone can call"
              ]
            ]
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "items": [
              "Trying self.private_m (raises — private methods take no receiver).",
              "Using attr_accessor when attr_reader would do (over-exposing setters).",
              "Forgetting @vars are always private (can't be read/written directly)."
            ]
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "Encapsulation = bundle state with methods that act on it; hide internals.",
              "Instance variables (@x) are ALWAYS private in Ruby — can't read/write directly.",
              "Expose state via methods (attr_reader); control mutation (def x=(v) with rules).",
              "private = called without a receiver (self.private_m raises); internal helpers.",
              "protected = callable by other instances of the same class; public = anyone."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "Encapsulation equals bundle state with the methods that act on it, and hide internals from outside. In Ruby: instance variables (x) are ALWAYS private — outsiders can't read or set them directly. You expose state via methods (attr_reader name, a custom balance method) and control mutation (def balance equals v; raise if v less than 0; balance var v; end). Private methods (private keyword; def private m; end) can only be called without a receiver (self dot private m raises; just private m works) — they're internal helpers. Protected (protected) can be called by other instances of the same class. Encapsulation keeps invariants: a BankAccount's balance is only changed through deposit withdraw, which can enforce rules. Use attr_reader over attr_accessor; make helpers private; expose a small public API. This decouples internals from users. A common mistake is trying self dot private m, which raises since private methods take no receiver, and using attr_accessor when attr_reader would do."
          }
        ]
      },
      "followup_questions": [
        "What is encapsulation (bundle state + methods; hide internals)?",
        "Are @instance variables public (no — always private; expose via attr_reader)?",
        "How do private methods work (no receiver; self.private_m raises)?",
        "What's protected vs private (protected = same-class instances; private = no receiver)?"
      ],
      "seo": {
        "metaTitle": "Encapsulation in Ruby — Bundle and Hide",
        "metaDescription": "Encapsulation = bundle state + methods, hide internals. @vars always private. private = no receiver; protected = same class. Expose via attr_reader."
      },
      "order": 1
    }
  ]
}

{
  "topic": "Duck Typing",
  "topicSlug": "duck-typing",
  "questions": [
    {
      "id": "duck-typing-q001",
      "slug": "ruby-duck-typing-basics",
      "question": "What is duck typing in Ruby?",
      "title": "Duck Typing",
      "direct_answer": "Duck typing: 'If it walks like a duck and quacks like a duck, it's a duck.' In Ruby, you don't check an object's class — you call its methods. `def speak(x); x.sound; end` works for any object with a `sound` method (Dog, Cat, Car), regardless of class. You rely on behavior, not type. This makes code flexible: a method accepting `#each` works with Array, Hash, Range, Set, your custom Enumerable. Compare to static typing where you'd declare a parameter type. Duck typing favors calling the method and rescuing NoMethodError (or letting it surface) over `is_a?` checks. The risk: an object might have the method but do the wrong thing (a `sound` that returns a number). Tests and clear contracts mitigate this. Use `respond_to?` only when you must branch; otherwise just call the method.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know duck typing = rely on behavior (methods), not class — 'if it quacks like a duck'.",
        "common_mistake": "Over-checking types with is_a? (use duck typing — call the method), or assuming a method exists without a clear contract.",
        "to_stand_out": "Duck typing = rely on behavior, not class. def speak(x); x.sound works for anything with sound. Favor calling methods over is_a? checks."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define Duck Typing simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "Duck typing: rely on behavior (methods), not class — 'if it quacks like a duck'.",
              "def speak(x); x.sound; end works for any object with a sound method (Dog, Cat, Car).",
              "A method accepting #each works with Array, Hash, Range, Set, custom Enumerable.",
              "Favor calling the method over is_a? checks; use respond_to? only when you must branch.",
              "Risk: a method may exist but do the wrong thing — tests and clear contracts mitigate."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "Duck typing: If it walks like a duck and quacks like a duck, it's a duck. In Ruby, you don't check an object's class — you call its methods. def speak x; x dot sound; end works for any object with a sound method (Dog, Cat, Car), regardless of class. You rely on behavior, not type. This makes code flexible: a method accepting each works with Array, Hash, Range, Set, your custom Enumerable. Compare to static typing where you'd declare a parameter type. Duck typing favors calling the method and rescuing NoMethodError (or letting it surface) over is_a question checks. The risk: an object might have the method but do the wrong thing (a sound that returns a number). Tests and clear contracts mitigate this. Use respond_to question only when you must branch; otherwise just call the method. A common mistake is over-checking types with is_a question, when you should use duck typing, and assuming a method exists without a clear contract."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Duck typing: 'If it walks like a duck and quacks like a duck, it's a duck.' In Ruby, you don't check an object's class — you call its methods.\n\n`def speak(x); x.sound; end` works for any object with a `sound` method (Dog, Cat, Car), regardless of class.\n\nYou rely on behavior, not type. This makes code flexible: a method accepting `#each` works with Array, Hash, Range, Set, your custom Enumerable. Compare to static typing where you'd declare a parameter type. Duck typing favors calling the method and rescuing NoMethodError (or letting it surface) over `is_a?` checks. The risk: an object might have the method but do the wrong thing (a `sound` that returns a number). Tests and clear contracts mitigate this. Use `respond_to?` only when you must branch; otherwise just call the method.\n\nOver-checking types with is_a? (use duck typing — call the method), or assuming a method exists without a clear contract."
          },
          {
            "type": "flow_diagram",
            "title": "Duck Typing",
            "content": "rely on behavior, not class. speak(x) works for any x with #sound. Favor calling methods over is_a?.\n\n```mermaid\nflowchart LR\n  D[speak(x): x.sound] --> W[works for Dog]\n  D --> C[works for Cat]\n  D --> Car[works for Car]\n  M[method each] --> A[Array / Hash / Range / Set]\n```"
          },
          {
            "type": "code_example",
            "title": "Duck typing",
            "content": "```ruby\n# Duck typing: rely on BEHAVIOR (methods), not class\nclass Dog;  def sound; 'Woof';  end; end\nclass Cat;  def sound; 'Meow';  end; end\nclass Car;  def sound; 'Vroom'; end; end\n\ndef speak(x)\n  x.sound        # works for ANY object with a `sound` method\nend\nspeak(Dog.new)   # 'Woof'\nspeak(Cat.new)   # 'Meow'\nspeak(Car.new)   # 'Vroom'  (no class check needed)\n\n# Same with iteration: anything with `each` works\ndef total(coll)\n  sum = 0\n  coll.each { |x| sum += x }   # Array, Hash, Range, Set, custom\n  sum\nend\ntotal([1, 2, 3])           # 6\ntotal(1..3)                # 6  (Range responds to each)\n\n# DON'T over-check types:\n# def speak(x)\n#   return 'woof' if x.is_a?(Dog)\n#   return 'meow' if x.is_a?(Cat)   # brittle, not extensible\n# end\n\n# Use respond_to? only when you MUST branch:\ndef safe_sound(x)\n  if x.respond_to?(:sound)\n    x.sound\n  else\n    '...'\n  end\nend\n\n# Risk: a `sound` that returns a number still 'works' but may be wrong.\n# Mitigate with tests + clear contracts. Prefer calling the method.\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Duck Typing in a real project?",
        "How would you compare Duck Typing with an alternative?",
        "What is a common mistake when using Duck Typing?"
      ],
      "seo": {
        "metaTitle": "Duck Typing — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is duck typing in Ruby? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "ruby-oop-basics-duck-typing-q001",
      "slug": "ruby-oop-basics-duck-typing-when-to-use",
      "question": "When would you use Duck Typing in a real project?",
      "title": "When would you use Duck Typing in a real project",
      "direct_answer": "I would use duck typing in Ruby when the problem matches its main benefit. Duck typing: 'If it walks like a duck and quacks like a duck, it's a duck.' In Ruby, you don't check an object's class — you call its methods. In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain duck typing in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about duck typing in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining duck typing in Ruby in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. Duck typing: 'If it walks like a duck and quacks like a duck, it's a duck.' In Ruby, you don't check an object's class — you call its methods. `def speak(x); x.sound; end` works for any object with a `sound` method (Dog, Cat, Car), regardless of class. You rely on behavior, not type. This makes code flexible: a method accepting `#each` works with Array, Hash, Range, Set, your custom Enumerable. Compare to static typing where you'd declare a parameter type. Duck typing favors calling the method and rescuing NoMethodError (or letting it surface) over `is_a?` checks. The risk: an object might have the method but do the wrong thing (a `sound` that returns a number). Tests and clear contracts mitigate this. Use `respond_to?` only when you must branch; otherwise just call the method."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use duck typing in Ruby when its rule makes the code safer, clearer, or more efficient for the actual problem. Duck typing: 'If it walks like a duck and quacks like a duck, it's a duck.' In Ruby, you don't check an object's class — you call its methods. `def speak(x); x.sound; end` works for any object with a `sound` method (Dog, Cat, Car), regardless of class. You rely on behavior, not type. This makes code flexible: a method accepting `#each` works with Array, Hash, Range, Set, your custom Enumerable. Compare to static typing where you'd declare a parameter type. Duck typing favors calling the method and rescuing NoMethodError (or letting it surface) over `is_a?` checks. The risk: an object might have the method but do the wrong thing (a `sound` that returns a number). Tests and clear contracts mitigate this. Use `respond_to?` only when you must branch; otherwise just call the method. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Duck typing: 'If it walks like a duck and quacks like a duck, it's a duck.' In Ruby, you don't check an object's class — you call its methods.\n\n`def speak(x); x.sound; end` works for any object with a `sound` method (Dog, Cat, Car), regardless of class.\n\nYou rely on behavior, not type. This makes code flexible: a method accepting `#each` works with Array, Hash, Range, Set, your custom Enumerable. Compare to static typing where you'd declare a parameter type. Duck typing favors calling the method and rescuing NoMethodError (or letting it surface) over `is_a?` checks. The risk: an object might have the method but do the wrong thing (a `sound` that returns a number). Tests and clear contracts mitigate this. Use `respond_to?` only when you must branch; otherwise just call the method.\n\nOver-checking types with is_a? (use duck typing — call the method), or assuming a method exists without a clear contract."
          },
          {
            "type": "code_example",
            "title": "Duck typing",
            "content": "```ruby\n# Duck typing: rely on BEHAVIOR (methods), not class\nclass Dog;  def sound; 'Woof';  end; end\nclass Cat;  def sound; 'Meow';  end; end\nclass Car;  def sound; 'Vroom'; end; end\n\ndef speak(x)\n  x.sound        # works for ANY object with a `sound` method\nend\nspeak(Dog.new)   # 'Woof'\nspeak(Cat.new)   # 'Meow'\nspeak(Car.new)   # 'Vroom'  (no class check needed)\n\n# Same with iteration: anything with `each` works\ndef total(coll)\n  sum = 0\n  coll.each { |x| sum += x }   # Array, Hash, Range, Set, custom\n  sum\nend\ntotal([1, 2, 3])           # 6\ntotal(1..3)                # 6  (Range responds to each)\n\n# DON'T over-check types:\n# def speak(x)\n#   return 'woof' if x.is_a?(Dog)\n#   return 'meow' if x.is_a?(Cat)   # brittle, not extensible\n# end\n\n# Use respond_to? only when you MUST branch:\ndef safe_sound(x)\n  if x.respond_to?(:sound)\n    x.sound\n  else\n    '...'\n  end\nend\n\n# Risk: a `sound` that returns a number still 'works' but may be wrong.\n# Mitigate with tests + clear contracts. Prefer calling the method.\n```"
          }
        ]
      },
      "followup_questions": [
        "What is duck typing in Ruby?",
        "How would you compare Duck Typing with an alternative?",
        "What is a common mistake when using Duck Typing?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Duck Typing in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Duck Typing in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-oop-basics-duck-typing-q002",
      "slug": "ruby-oop-basics-duck-typing-common-mistake",
      "question": "What is a common mistake when using Duck Typing?",
      "title": "a common mistake when using Duck Typing",
      "direct_answer": "A common mistake with duck typing in Ruby is using the rule without checking its boundary conditions. Duck typing: 'If it walks like a duck and quacks like a duck, it's a duck.' In Ruby, you don't check an object's class — you call its methods. That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain duck typing in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about duck typing in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of duck typing in Ruby. Duck typing: 'If it walks like a duck and quacks like a duck, it's a duck.' In Ruby, you don't check an object's class — you call its methods. `def speak(x); x.sound; end` works for any object with a `sound` method (Dog, Cat, Car), regardless of class. You rely on behavior, not type. This makes code flexible: a method accepting `#each` works with Array, Hash, Range, Set, your custom Enumerable. Compare to static typing where you'd declare a parameter type. Duck typing favors calling the method and rescuing NoMethodError (or letting it surface) over `is_a?` checks. The risk: an object might have the method but do the wrong thing (a `sound` that returns a number). Tests and clear contracts mitigate this. Use `respond_to?` only when you must branch; otherwise just call the method."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with duck typing in Ruby is assuming the happy path is the whole rule. Duck typing: 'If it walks like a duck and quacks like a duck, it's a duck.' In Ruby, you don't check an object's class — you call its methods. `def speak(x); x.sound; end` works for any object with a `sound` method (Dog, Cat, Car), regardless of class. You rely on behavior, not type. This makes code flexible: a method accepting `#each` works with Array, Hash, Range, Set, your custom Enumerable. Compare to static typing where you'd declare a parameter type. Duck typing favors calling the method and rescuing NoMethodError (or letting it surface) over `is_a?` checks. The risk: an object might have the method but do the wrong thing (a `sound` that returns a number). Tests and clear contracts mitigate this. Use `respond_to?` only when you must branch; otherwise just call the method. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "Duck typing: 'If it walks like a duck and quacks like a duck, it's a duck.' In Ruby, you don't check an object's class — you call its methods.\n\n`def speak(x); x.sound; end` works for any object with a `sound` method (Dog, Cat, Car), regardless of class.\n\nYou rely on behavior, not type. This makes code flexible: a method accepting `#each` works with Array, Hash, Range, Set, your custom Enumerable. Compare to static typing where you'd declare a parameter type. Duck typing favors calling the method and rescuing NoMethodError (or letting it surface) over `is_a?` checks. The risk: an object might have the method but do the wrong thing (a `sound` that returns a number). Tests and clear contracts mitigate this. Use `respond_to?` only when you must branch; otherwise just call the method.\n\nOver-checking types with is_a? (use duck typing — call the method), or assuming a method exists without a clear contract."
          },
          {
            "type": "code_example",
            "title": "Duck typing",
            "content": "```ruby\n# Duck typing: rely on BEHAVIOR (methods), not class\nclass Dog;  def sound; 'Woof';  end; end\nclass Cat;  def sound; 'Meow';  end; end\nclass Car;  def sound; 'Vroom'; end; end\n\ndef speak(x)\n  x.sound        # works for ANY object with a `sound` method\nend\nspeak(Dog.new)   # 'Woof'\nspeak(Cat.new)   # 'Meow'\nspeak(Car.new)   # 'Vroom'  (no class check needed)\n\n# Same with iteration: anything with `each` works\ndef total(coll)\n  sum = 0\n  coll.each { |x| sum += x }   # Array, Hash, Range, Set, custom\n  sum\nend\ntotal([1, 2, 3])           # 6\ntotal(1..3)                # 6  (Range responds to each)\n\n# DON'T over-check types:\n# def speak(x)\n#   return 'woof' if x.is_a?(Dog)\n#   return 'meow' if x.is_a?(Cat)   # brittle, not extensible\n# end\n\n# Use respond_to? only when you MUST branch:\ndef safe_sound(x)\n  if x.respond_to?(:sound)\n    x.sound\n  else\n    '...'\n  end\nend\n\n# Risk: a `sound` that returns a number still 'works' but may be wrong.\n# Mitigate with tests + clear contracts. Prefer calling the method.\n```"
          }
        ]
      },
      "followup_questions": [
        "What is duck typing in Ruby?",
        "When would you use Duck Typing in a real project?",
        "How would you compare Duck Typing with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Duck Typing — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Duck Typing? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-oop-basics-duck-typing-q003",
      "slug": "ruby-oop-basics-duck-typing-compare",
      "question": "How would you compare Duck Typing with an alternative?",
      "title": "compare Duck Typing with an alternative",
      "direct_answer": "I compare duck typing in Ruby with the simpler alternative by looking at correctness first, then time, memory, and readability. Duck typing: 'If it walks like a duck and quacks like a duck, it's a duck.' In Ruby, you don't check an object's class — you call its methods. The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain duck typing in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about duck typing in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. Duck typing: 'If it walks like a duck and quacks like a duck, it's a duck.' In Ruby, you don't check an object's class — you call its methods. `def speak(x); x.sound; end` works for any object with a `sound` method (Dog, Cat, Car), regardless of class. You rely on behavior, not type. This makes code flexible: a method accepting `#each` works with Array, Hash, Range, Set, your custom Enumerable. Compare to static typing where you'd declare a parameter type. Duck typing favors calling the method and rescuing NoMethodError (or letting it surface) over `is_a?` checks. The risk: an object might have the method but do the wrong thing (a `sound` that returns a number). Tests and clear contracts mitigate this. Use `respond_to?` only when you must branch; otherwise just call the method."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare duck typing in Ruby with the straightforward alternative on the same example. Duck typing: 'If it walks like a duck and quacks like a duck, it's a duck.' In Ruby, you don't check an object's class — you call its methods. `def speak(x); x.sound; end` works for any object with a `sound` method (Dog, Cat, Car), regardless of class. You rely on behavior, not type. This makes code flexible: a method accepting `#each` works with Array, Hash, Range, Set, your custom Enumerable. Compare to static typing where you'd declare a parameter type. Duck typing favors calling the method and rescuing NoMethodError (or letting it surface) over `is_a?` checks. The risk: an object might have the method but do the wrong thing (a `sound` that returns a number). Tests and clear contracts mitigate this. Use `respond_to?` only when you must branch; otherwise just call the method. If duck typing in Ruby removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "Duck typing: 'If it walks like a duck and quacks like a duck, it's a duck.' In Ruby, you don't check an object's class — you call its methods.\n\n`def speak(x); x.sound; end` works for any object with a `sound` method (Dog, Cat, Car), regardless of class.\n\nYou rely on behavior, not type. This makes code flexible: a method accepting `#each` works with Array, Hash, Range, Set, your custom Enumerable. Compare to static typing where you'd declare a parameter type. Duck typing favors calling the method and rescuing NoMethodError (or letting it surface) over `is_a?` checks. The risk: an object might have the method but do the wrong thing (a `sound` that returns a number). Tests and clear contracts mitigate this. Use `respond_to?` only when you must branch; otherwise just call the method.\n\nOver-checking types with is_a? (use duck typing — call the method), or assuming a method exists without a clear contract."
          },
          {
            "type": "code_example",
            "title": "Duck typing",
            "content": "```ruby\n# Duck typing: rely on BEHAVIOR (methods), not class\nclass Dog;  def sound; 'Woof';  end; end\nclass Cat;  def sound; 'Meow';  end; end\nclass Car;  def sound; 'Vroom'; end; end\n\ndef speak(x)\n  x.sound        # works for ANY object with a `sound` method\nend\nspeak(Dog.new)   # 'Woof'\nspeak(Cat.new)   # 'Meow'\nspeak(Car.new)   # 'Vroom'  (no class check needed)\n\n# Same with iteration: anything with `each` works\ndef total(coll)\n  sum = 0\n  coll.each { |x| sum += x }   # Array, Hash, Range, Set, custom\n  sum\nend\ntotal([1, 2, 3])           # 6\ntotal(1..3)                # 6  (Range responds to each)\n\n# DON'T over-check types:\n# def speak(x)\n#   return 'woof' if x.is_a?(Dog)\n#   return 'meow' if x.is_a?(Cat)   # brittle, not extensible\n# end\n\n# Use respond_to? only when you MUST branch:\ndef safe_sound(x)\n  if x.respond_to?(:sound)\n    x.sound\n  else\n    '...'\n  end\nend\n\n# Risk: a `sound` that returns a number still 'works' but may be wrong.\n# Mitigate with tests + clear contracts. Prefer calling the method.\n```"
          }
        ]
      },
      "followup_questions": [
        "What is duck typing in Ruby?",
        "When would you use Duck Typing in a real project?",
        "What is a common mistake when using Duck Typing?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Duck Typing with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Duck Typing with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-oop-basics-duck-typing-q004",
      "slug": "ruby-oop-basics-duck-typing-scenario",
      "question": "How would you debug a problem involving Duck Typing?",
      "title": "debug a problem involving Duck Typing",
      "direct_answer": "I would debug a problem involving duck typing in Ruby by reproducing it with the smallest input first. Duck typing: 'If it walks like a duck and quacks like a duck, it's a duck.' In Ruby, you don't check an object's class — you call its methods. Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain duck typing in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about duck typing in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what duck typing in Ruby is supposed to guarantee. Duck typing: 'If it walks like a duck and quacks like a duck, it's a duck.' In Ruby, you don't check an object's class — you call its methods. `def speak(x); x.sound; end` works for any object with a `sound` method (Dog, Cat, Car), regardless of class. You rely on behavior, not type. This makes code flexible: a method accepting `#each` works with Array, Hash, Range, Set, your custom Enumerable. Compare to static typing where you'd declare a parameter type. Duck typing favors calling the method and rescuing NoMethodError (or letting it surface) over `is_a?` checks. The risk: an object might have the method but do the wrong thing (a `sound` that returns a number). Tests and clear contracts mitigate this. Use `respond_to?` only when you must branch; otherwise just call the method."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the duck typing in Ruby bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: Duck typing: 'If it walks like a duck and quacks like a duck, it's a duck.' In Ruby, you don't check an object's class — you call its methods. `def speak(x); x.sound; end` works for any object with a `sound` method (Dog, Cat, Car), regardless of class. You rely on behavior, not type. This makes code flexible: a method accepting `#each` works with Array, Hash, Range, Set, your custom Enumerable. Compare to static typing where you'd declare a parameter type. Duck typing favors calling the method and rescuing NoMethodError (or letting it surface) over `is_a?` checks. The risk: an object might have the method but do the wrong thing (a `sound` that returns a number). Tests and clear contracts mitigate this. Use `respond_to?` only when you must branch; otherwise just call the method. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "Duck typing: 'If it walks like a duck and quacks like a duck, it's a duck.' In Ruby, you don't check an object's class — you call its methods.\n\n`def speak(x); x.sound; end` works for any object with a `sound` method (Dog, Cat, Car), regardless of class.\n\nYou rely on behavior, not type. This makes code flexible: a method accepting `#each` works with Array, Hash, Range, Set, your custom Enumerable. Compare to static typing where you'd declare a parameter type. Duck typing favors calling the method and rescuing NoMethodError (or letting it surface) over `is_a?` checks. The risk: an object might have the method but do the wrong thing (a `sound` that returns a number). Tests and clear contracts mitigate this. Use `respond_to?` only when you must branch; otherwise just call the method.\n\nOver-checking types with is_a? (use duck typing — call the method), or assuming a method exists without a clear contract."
          },
          {
            "type": "code_example",
            "title": "Duck typing",
            "content": "```ruby\n# Duck typing: rely on BEHAVIOR (methods), not class\nclass Dog;  def sound; 'Woof';  end; end\nclass Cat;  def sound; 'Meow';  end; end\nclass Car;  def sound; 'Vroom'; end; end\n\ndef speak(x)\n  x.sound        # works for ANY object with a `sound` method\nend\nspeak(Dog.new)   # 'Woof'\nspeak(Cat.new)   # 'Meow'\nspeak(Car.new)   # 'Vroom'  (no class check needed)\n\n# Same with iteration: anything with `each` works\ndef total(coll)\n  sum = 0\n  coll.each { |x| sum += x }   # Array, Hash, Range, Set, custom\n  sum\nend\ntotal([1, 2, 3])           # 6\ntotal(1..3)                # 6  (Range responds to each)\n\n# DON'T over-check types:\n# def speak(x)\n#   return 'woof' if x.is_a?(Dog)\n#   return 'meow' if x.is_a?(Cat)   # brittle, not extensible\n# end\n\n# Use respond_to? only when you MUST branch:\ndef safe_sound(x)\n  if x.respond_to?(:sound)\n    x.sound\n  else\n    '...'\n  end\nend\n\n# Risk: a `sound` that returns a number still 'works' but may be wrong.\n# Mitigate with tests + clear contracts. Prefer calling the method.\n```"
          }
        ]
      },
      "followup_questions": [
        "What is duck typing in Ruby?",
        "When would you use Duck Typing in a real project?",
        "How would you compare Duck Typing with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Duck Typing — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Duck Typing? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

{
  "topic": "Class Methods",
  "topicSlug": "class-methods",
  "questions": [
    {
      "id": "class-methods-q001",
      "slug": "ruby-class-methods-basics",
      "question": "What are class methods in Ruby?",
      "title": "Class Methods in Ruby",
      "direct_answer": "A class method is called on the class itself, not an instance: `Greeter.hello` (not `Greeter.new.hello`). Define with `def self.name` or `def ClassName.name`: class Greeter; def self.hello; 'hi'; end; end; Greeter.hello # 'hi'. Class methods live on the class's singleton class — every class is itself an object, and class methods are its singleton methods. Use them for things that don't need instance state: factories (def self.from_json), utility helpers (Math.sqrt), counters (def self.count; @@count; end), config. They can't see instance variables (@x) — only class vars (@@x) and class-level state. `self` inside a class body refers to the class. To define many at once: `class << self; def a; end; def b; end; end` opens the singleton class. Instance methods = behavior of an instance; class methods = behavior of the class.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know class methods are called on the class (not an instance) and defined with `def self.name`.",
        "common_mistake": "Trying to call a class method on an instance (or vice versa), or accessing @x from a class method (only @@x works).",
        "to_stand_out": "Class methods = called on the class (Greeter.hello); defined with def self.name or class << self. For factories, utilities, counters. Can't see @x."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define Class Methods in Ruby simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "A class method is called on the class itself (Greeter.hello, not Greeter.new.hello).",
              "Define with `def self.name` or open `class << self` for many.",
              "Use for factories (from_json), utilities (Math.sqrt), counters (@@count), config.",
              "Class methods can't see instance vars (@x) — only class vars (@@x) and class state.",
              "`self` inside a class body refers to the class; class methods live on the singleton class."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "A class method is called on the class itself, not an instance: Greeter dot hello (not Greeter dot new dot hello). Define with def self dot name or def ClassName dot name: class Greeter; def self dot hello; hi; end; end; Greeter dot hello hi. Class methods live on the class's singleton class — every class is itself an object, and class methods are its singleton methods. Use them for things that don't need instance state: factories (def self from_json), utility helpers (Math sqrt), counters (def self count; count; end), config. They can't see instance variables (x) — only class vars (count) and class-level state. self inside a class body refers to the class. To define many at once: class double-less-than self; def a; def b; end opens the singleton class. Instance methods = behavior of an instance; class methods = behavior of the class. A common mistake is calling a class method on an instance, which raises, and accessing x from a class method, when only count works."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "A class method is called on the class itself, not an instance: `Greeter.hello` (not `Greeter.new.hello`).\n\nDefine with `def self.name` or `def ClassName.name`: class Greeter; def self.hello; 'hi'; end; end; Greeter.hello # 'hi'.\n\nClass methods live on the class's singleton class — every class is itself an object, and class methods are its singleton methods. Use them for things that don't need instance state: factories (def self.from_json), utility helpers (Math.sqrt), counters (def self.count; @@count; end), config. They can't see instance variables (@x) — only class vars (@@x) and class-level state. `self` inside a class body refers to the class. To define many at once: `class << self; def a; end; def b; end; end` opens the singleton class.\n\nTrying to call a class method on an instance (or vice versa), or accessing @x from a class method (only @@x works)."
          },
          {
            "type": "flow_diagram",
            "title": "Class Methods",
            "content": "called on the class; def self.name or class << self. Factories/utilities/counters. Can't see @x.\n\n```mermaid\nflowchart LR\n  C[class Greeter] --> M[def self.hello]\n  M --> C2[Greeter.hello]\n  F[factory] --> FJ[def self.from_json]\n  X[can't see @x] --> CV[only @@x]\n```"
          },
          {
            "type": "code_example",
            "title": "Class methods",
            "content": "```ruby\n# Define a class method with `def self.name`\nclass Greeter\n  def self.hello; 'hi from class'; end   # class method\n  def hello;     'hi from instance'; end   # instance method\nend\n\nGreeter.hello            # 'hi from class'   (on the CLASS)\nGreeter.new.hello       # 'hi from instance' (on an INSTANCE)\n# Greeter.new.hello(class) -> NoMethodError\n\n# Use cases (no instance state needed):\nclass User\n  attr_accessor :name\n  def initialize(name); @name = name; end\n\n  # FACTORY: build an instance from data\n  def self.from_json(json)\n    new(JSON.parse(json)['name'])\n  end\n\n  # COUNTER: track how many were made\n  @@count = 0\n  def initialize(name); @name = name; @@count += 1; end\n  def self.count; @@count; end\nend\nu = User.from_json('{\"name\":\"Ada\"}')   # factory\nUser.count                            # 1 (class-level counter)\n\n# `self` inside class body = the class\nclass C\n  puts self             # C\nend\n\n# Define many class methods: open the singleton class\nclass Math2\n  class << self\n    def square(x); x * x; end\n    def cube(x);   x * x * x; end\n  end\nend\nMath2.square(3)         # 9\nMath2.cube(2)           # 8\n\n# Class methods CANNOT see instance vars (@x) — only @@x / class state.\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Class Methods in a real project?",
        "How would you compare Class Methods with an alternative?",
        "What is a common mistake when using Class Methods?"
      ],
      "seo": {
        "metaTitle": "Class Methods in Ruby — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What are class methods in Ruby? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "ruby-oop-basics-class-methods-q001",
      "slug": "ruby-oop-basics-class-methods-when-to-use",
      "question": "When would you use Class Methods in a real project?",
      "title": "When would you use Class Methods in a real project",
      "direct_answer": "I would use class methods in Ruby when the problem matches its main benefit. A class method is called on the class itself, not an instance: `Greeter.hello` (not `Greeter.new.hello`). In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain class methods in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about class methods in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining class methods in Ruby in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. A class method is called on the class itself, not an instance: `Greeter.hello` (not `Greeter.new.hello`). Define with `def self.name` or `def ClassName.name`: class Greeter; def self.hello; 'hi'; end; end; Greeter.hello # 'hi'. Class methods live on the class's singleton class — every class is itself an object, and class methods are its singleton methods. Use them for things that don't need instance state: factories (def self.from_json), utility helpers (Math.sqrt), counters (def self.count; @@count; end), config. They can't see instance variables (@x) — only class vars (@@x) and class-level state. `self` inside a class body refers to the class. To define many at once: `class << self; def a; end; def b; end; end` opens the singleton class. Instance methods = behavior of an instance; class methods = behavior of the class."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use class methods in Ruby when its rule makes the code safer, clearer, or more efficient for the actual problem. A class method is called on the class itself, not an instance: `Greeter.hello` (not `Greeter.new.hello`). Define with `def self.name` or `def ClassName.name`: class Greeter; def self.hello; 'hi'; end; end; Greeter.hello # 'hi'. Class methods live on the class's singleton class — every class is itself an object, and class methods are its singleton methods. Use them for things that don't need instance state: factories (def self.from_json), utility helpers (Math.sqrt), counters (def self.count; @@count; end), config. They can't see instance variables (@x) — only class vars (@@x) and class-level state. `self` inside a class body refers to the class. To define many at once: `class << self; def a; end; def b; end; end` opens the singleton class. Instance methods = behavior of an instance; class methods = behavior of the class. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "A class method is called on the class itself, not an instance: `Greeter.hello` (not `Greeter.new.hello`).\n\nDefine with `def self.name` or `def ClassName.name`: class Greeter; def self.hello; 'hi'; end; end; Greeter.hello # 'hi'.\n\nClass methods live on the class's singleton class — every class is itself an object, and class methods are its singleton methods. Use them for things that don't need instance state: factories (def self.from_json), utility helpers (Math.sqrt), counters (def self.count; @@count; end), config. They can't see instance variables (@x) — only class vars (@@x) and class-level state. `self` inside a class body refers to the class. To define many at once: `class << self; def a; end; def b; end; end` opens the singleton class.\n\nTrying to call a class method on an instance (or vice versa), or accessing @x from a class method (only @@x works)."
          },
          {
            "type": "code_example",
            "title": "Class methods",
            "content": "```ruby\n# Define a class method with `def self.name`\nclass Greeter\n  def self.hello; 'hi from class'; end   # class method\n  def hello;     'hi from instance'; end   # instance method\nend\n\nGreeter.hello            # 'hi from class'   (on the CLASS)\nGreeter.new.hello       # 'hi from instance' (on an INSTANCE)\n# Greeter.new.hello(class) -> NoMethodError\n\n# Use cases (no instance state needed):\nclass User\n  attr_accessor :name\n  def initialize(name); @name = name; end\n\n  # FACTORY: build an instance from data\n  def self.from_json(json)\n    new(JSON.parse(json)['name'])\n  end\n\n  # COUNTER: track how many were made\n  @@count = 0\n  def initialize(name); @name = name; @@count += 1; end\n  def self.count; @@count; end\nend\nu = User.from_json('{\"name\":\"Ada\"}')   # factory\nUser.count                            # 1 (class-level counter)\n\n# `self` inside class body = the class\nclass C\n  puts self             # C\nend\n\n# Define many class methods: open the singleton class\nclass Math2\n  class << self\n    def square(x); x * x; end\n    def cube(x);   x * x * x; end\n  end\nend\nMath2.square(3)         # 9\nMath2.cube(2)           # 8\n\n# Class methods CANNOT see instance vars (@x) — only @@x / class state.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are class methods in Ruby?",
        "How would you compare Class Methods with an alternative?",
        "What is a common mistake when using Class Methods?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Class Methods in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Class Methods in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-oop-basics-class-methods-q002",
      "slug": "ruby-oop-basics-class-methods-common-mistake",
      "question": "What is a common mistake when using Class Methods?",
      "title": "a common mistake when using Class Methods",
      "direct_answer": "A common mistake with class methods in Ruby is using the rule without checking its boundary conditions. A class method is called on the class itself, not an instance: `Greeter.hello` (not `Greeter.new.hello`). That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain class methods in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about class methods in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of class methods in Ruby. A class method is called on the class itself, not an instance: `Greeter.hello` (not `Greeter.new.hello`). Define with `def self.name` or `def ClassName.name`: class Greeter; def self.hello; 'hi'; end; end; Greeter.hello # 'hi'. Class methods live on the class's singleton class — every class is itself an object, and class methods are its singleton methods. Use them for things that don't need instance state: factories (def self.from_json), utility helpers (Math.sqrt), counters (def self.count; @@count; end), config. They can't see instance variables (@x) — only class vars (@@x) and class-level state. `self` inside a class body refers to the class. To define many at once: `class << self; def a; end; def b; end; end` opens the singleton class. Instance methods = behavior of an instance; class methods = behavior of the class."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with class methods in Ruby is assuming the happy path is the whole rule. A class method is called on the class itself, not an instance: `Greeter.hello` (not `Greeter.new.hello`). Define with `def self.name` or `def ClassName.name`: class Greeter; def self.hello; 'hi'; end; end; Greeter.hello # 'hi'. Class methods live on the class's singleton class — every class is itself an object, and class methods are its singleton methods. Use them for things that don't need instance state: factories (def self.from_json), utility helpers (Math.sqrt), counters (def self.count; @@count; end), config. They can't see instance variables (@x) — only class vars (@@x) and class-level state. `self` inside a class body refers to the class. To define many at once: `class << self; def a; end; def b; end; end` opens the singleton class. Instance methods = behavior of an instance; class methods = behavior of the class. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "A class method is called on the class itself, not an instance: `Greeter.hello` (not `Greeter.new.hello`).\n\nDefine with `def self.name` or `def ClassName.name`: class Greeter; def self.hello; 'hi'; end; end; Greeter.hello # 'hi'.\n\nClass methods live on the class's singleton class — every class is itself an object, and class methods are its singleton methods. Use them for things that don't need instance state: factories (def self.from_json), utility helpers (Math.sqrt), counters (def self.count; @@count; end), config. They can't see instance variables (@x) — only class vars (@@x) and class-level state. `self` inside a class body refers to the class. To define many at once: `class << self; def a; end; def b; end; end` opens the singleton class.\n\nTrying to call a class method on an instance (or vice versa), or accessing @x from a class method (only @@x works)."
          },
          {
            "type": "code_example",
            "title": "Class methods",
            "content": "```ruby\n# Define a class method with `def self.name`\nclass Greeter\n  def self.hello; 'hi from class'; end   # class method\n  def hello;     'hi from instance'; end   # instance method\nend\n\nGreeter.hello            # 'hi from class'   (on the CLASS)\nGreeter.new.hello       # 'hi from instance' (on an INSTANCE)\n# Greeter.new.hello(class) -> NoMethodError\n\n# Use cases (no instance state needed):\nclass User\n  attr_accessor :name\n  def initialize(name); @name = name; end\n\n  # FACTORY: build an instance from data\n  def self.from_json(json)\n    new(JSON.parse(json)['name'])\n  end\n\n  # COUNTER: track how many were made\n  @@count = 0\n  def initialize(name); @name = name; @@count += 1; end\n  def self.count; @@count; end\nend\nu = User.from_json('{\"name\":\"Ada\"}')   # factory\nUser.count                            # 1 (class-level counter)\n\n# `self` inside class body = the class\nclass C\n  puts self             # C\nend\n\n# Define many class methods: open the singleton class\nclass Math2\n  class << self\n    def square(x); x * x; end\n    def cube(x);   x * x * x; end\n  end\nend\nMath2.square(3)         # 9\nMath2.cube(2)           # 8\n\n# Class methods CANNOT see instance vars (@x) — only @@x / class state.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are class methods in Ruby?",
        "When would you use Class Methods in a real project?",
        "How would you compare Class Methods with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Class Methods — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Class Methods? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-oop-basics-class-methods-q003",
      "slug": "ruby-oop-basics-class-methods-compare",
      "question": "How would you compare Class Methods with an alternative?",
      "title": "compare Class Methods with an alternative",
      "direct_answer": "I compare class methods in Ruby with the simpler alternative by looking at correctness first, then time, memory, and readability. A class method is called on the class itself, not an instance: `Greeter.hello` (not `Greeter.new.hello`). The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain class methods in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about class methods in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. A class method is called on the class itself, not an instance: `Greeter.hello` (not `Greeter.new.hello`). Define with `def self.name` or `def ClassName.name`: class Greeter; def self.hello; 'hi'; end; end; Greeter.hello # 'hi'. Class methods live on the class's singleton class — every class is itself an object, and class methods are its singleton methods. Use them for things that don't need instance state: factories (def self.from_json), utility helpers (Math.sqrt), counters (def self.count; @@count; end), config. They can't see instance variables (@x) — only class vars (@@x) and class-level state. `self` inside a class body refers to the class. To define many at once: `class << self; def a; end; def b; end; end` opens the singleton class. Instance methods = behavior of an instance; class methods = behavior of the class."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare class methods in Ruby with the straightforward alternative on the same example. A class method is called on the class itself, not an instance: `Greeter.hello` (not `Greeter.new.hello`). Define with `def self.name` or `def ClassName.name`: class Greeter; def self.hello; 'hi'; end; end; Greeter.hello # 'hi'. Class methods live on the class's singleton class — every class is itself an object, and class methods are its singleton methods. Use them for things that don't need instance state: factories (def self.from_json), utility helpers (Math.sqrt), counters (def self.count; @@count; end), config. They can't see instance variables (@x) — only class vars (@@x) and class-level state. `self` inside a class body refers to the class. To define many at once: `class << self; def a; end; def b; end; end` opens the singleton class. Instance methods = behavior of an instance; class methods = behavior of the class. If class methods in Ruby removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "A class method is called on the class itself, not an instance: `Greeter.hello` (not `Greeter.new.hello`).\n\nDefine with `def self.name` or `def ClassName.name`: class Greeter; def self.hello; 'hi'; end; end; Greeter.hello # 'hi'.\n\nClass methods live on the class's singleton class — every class is itself an object, and class methods are its singleton methods. Use them for things that don't need instance state: factories (def self.from_json), utility helpers (Math.sqrt), counters (def self.count; @@count; end), config. They can't see instance variables (@x) — only class vars (@@x) and class-level state. `self` inside a class body refers to the class. To define many at once: `class << self; def a; end; def b; end; end` opens the singleton class.\n\nTrying to call a class method on an instance (or vice versa), or accessing @x from a class method (only @@x works)."
          },
          {
            "type": "code_example",
            "title": "Class methods",
            "content": "```ruby\n# Define a class method with `def self.name`\nclass Greeter\n  def self.hello; 'hi from class'; end   # class method\n  def hello;     'hi from instance'; end   # instance method\nend\n\nGreeter.hello            # 'hi from class'   (on the CLASS)\nGreeter.new.hello       # 'hi from instance' (on an INSTANCE)\n# Greeter.new.hello(class) -> NoMethodError\n\n# Use cases (no instance state needed):\nclass User\n  attr_accessor :name\n  def initialize(name); @name = name; end\n\n  # FACTORY: build an instance from data\n  def self.from_json(json)\n    new(JSON.parse(json)['name'])\n  end\n\n  # COUNTER: track how many were made\n  @@count = 0\n  def initialize(name); @name = name; @@count += 1; end\n  def self.count; @@count; end\nend\nu = User.from_json('{\"name\":\"Ada\"}')   # factory\nUser.count                            # 1 (class-level counter)\n\n# `self` inside class body = the class\nclass C\n  puts self             # C\nend\n\n# Define many class methods: open the singleton class\nclass Math2\n  class << self\n    def square(x); x * x; end\n    def cube(x);   x * x * x; end\n  end\nend\nMath2.square(3)         # 9\nMath2.cube(2)           # 8\n\n# Class methods CANNOT see instance vars (@x) — only @@x / class state.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are class methods in Ruby?",
        "When would you use Class Methods in a real project?",
        "What is a common mistake when using Class Methods?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Class Methods with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Class Methods with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-oop-basics-class-methods-q004",
      "slug": "ruby-oop-basics-class-methods-scenario",
      "question": "How would you debug a problem involving Class Methods?",
      "title": "debug a problem involving Class Methods",
      "direct_answer": "I would debug a problem involving class methods in Ruby by reproducing it with the smallest input first. A class method is called on the class itself, not an instance: `Greeter.hello` (not `Greeter.new.hello`). Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain class methods in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about class methods in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what class methods in Ruby is supposed to guarantee. A class method is called on the class itself, not an instance: `Greeter.hello` (not `Greeter.new.hello`). Define with `def self.name` or `def ClassName.name`: class Greeter; def self.hello; 'hi'; end; end; Greeter.hello # 'hi'. Class methods live on the class's singleton class — every class is itself an object, and class methods are its singleton methods. Use them for things that don't need instance state: factories (def self.from_json), utility helpers (Math.sqrt), counters (def self.count; @@count; end), config. They can't see instance variables (@x) — only class vars (@@x) and class-level state. `self` inside a class body refers to the class. To define many at once: `class << self; def a; end; def b; end; end` opens the singleton class. Instance methods = behavior of an instance; class methods = behavior of the class."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the class methods in Ruby bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: A class method is called on the class itself, not an instance: `Greeter.hello` (not `Greeter.new.hello`). Define with `def self.name` or `def ClassName.name`: class Greeter; def self.hello; 'hi'; end; end; Greeter.hello # 'hi'. Class methods live on the class's singleton class — every class is itself an object, and class methods are its singleton methods. Use them for things that don't need instance state: factories (def self.from_json), utility helpers (Math.sqrt), counters (def self.count; @@count; end), config. They can't see instance variables (@x) — only class vars (@@x) and class-level state. `self` inside a class body refers to the class. To define many at once: `class << self; def a; end; def b; end; end` opens the singleton class. Instance methods = behavior of an instance; class methods = behavior of the class. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "A class method is called on the class itself, not an instance: `Greeter.hello` (not `Greeter.new.hello`).\n\nDefine with `def self.name` or `def ClassName.name`: class Greeter; def self.hello; 'hi'; end; end; Greeter.hello # 'hi'.\n\nClass methods live on the class's singleton class — every class is itself an object, and class methods are its singleton methods. Use them for things that don't need instance state: factories (def self.from_json), utility helpers (Math.sqrt), counters (def self.count; @@count; end), config. They can't see instance variables (@x) — only class vars (@@x) and class-level state. `self` inside a class body refers to the class. To define many at once: `class << self; def a; end; def b; end; end` opens the singleton class.\n\nTrying to call a class method on an instance (or vice versa), or accessing @x from a class method (only @@x works)."
          },
          {
            "type": "code_example",
            "title": "Class methods",
            "content": "```ruby\n# Define a class method with `def self.name`\nclass Greeter\n  def self.hello; 'hi from class'; end   # class method\n  def hello;     'hi from instance'; end   # instance method\nend\n\nGreeter.hello            # 'hi from class'   (on the CLASS)\nGreeter.new.hello       # 'hi from instance' (on an INSTANCE)\n# Greeter.new.hello(class) -> NoMethodError\n\n# Use cases (no instance state needed):\nclass User\n  attr_accessor :name\n  def initialize(name); @name = name; end\n\n  # FACTORY: build an instance from data\n  def self.from_json(json)\n    new(JSON.parse(json)['name'])\n  end\n\n  # COUNTER: track how many were made\n  @@count = 0\n  def initialize(name); @name = name; @@count += 1; end\n  def self.count; @@count; end\nend\nu = User.from_json('{\"name\":\"Ada\"}')   # factory\nUser.count                            # 1 (class-level counter)\n\n# `self` inside class body = the class\nclass C\n  puts self             # C\nend\n\n# Define many class methods: open the singleton class\nclass Math2\n  class << self\n    def square(x); x * x; end\n    def cube(x);   x * x * x; end\n  end\nend\nMath2.square(3)         # 9\nMath2.cube(2)           # 8\n\n# Class methods CANNOT see instance vars (@x) — only @@x / class state.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are class methods in Ruby?",
        "When would you use Class Methods in a real project?",
        "How would you compare Class Methods with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Class Methods — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Class Methods? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

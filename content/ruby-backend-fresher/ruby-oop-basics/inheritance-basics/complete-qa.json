{
  "topic": "Inheritance Basics",
  "topicSlug": "inheritance-basics",
  "questions": [
    {
      "id": "inheritance-basics-q001",
      "slug": "ruby-inheritance-basics",
      "question": "How does inheritance work at a basic level in Ruby?",
      "title": "Inheritance Basics",
      "direct_answer": "At its simplest: `class B < A; end` makes B inherit A's methods. When you call b.foo, Ruby looks up b's class (B), then its superclass (A), then Object, etc., until it finds foo — the method lookup chain. B can override A's method (define its own foo); it can call A's version with super. An instance of B is an instance of A too (b.is_a?(A) # true). Constructors are inherited in lookup but usually you redefine initialize and call super with the right args. Class variables (@@x) are shared up the chain (often a surprise). Instance variables (@x) belong to the object, not the class. Inheritance models 'B is a kind of A'. Keep trees shallow; one level is common, two is okay, more gets fragile. Use modules when you want to share behavior without an is-a link.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know method lookup goes up the superclass chain and that B instances are also A instances.",
        "common_mistake": "Forgetting method lookup walks the chain (or that @@vars are shared up it), or deep trees.",
        "to_stand_out": "Inheritance basics: class B < A; method lookup walks B -> A -> Object. B overrides A; super calls A. b.is_a?(A). @@vars shared up the chain."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define Inheritance Basics simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "class B < A makes B inherit A's methods; method lookup goes B -> A -> Object.",
              "B can override A's method; super calls A's version.",
              "An instance of B is also an instance of A (b.is_a?(A) # true).",
              "Redefine initialize and call super with the right args (constructors aren't auto-called).",
              "@@class vars are shared up the chain (a subclass's change affects the parent)."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "At its simplest: class B less-than A; end makes B inherit A's methods. When you call b dot foo, Ruby looks up b's class (B), then its superclass (A), then Object, etc., until it finds foo — the method lookup chain. B can override A's method (define its own foo); it can call A's version with super. An instance of B is an instance of A too (b is_a A true). Constructors are inherited in lookup but usually you redefine initialize and call super with the right args. Class variables (count) are shared up the chain (often a surprise). Instance variables (x) belong to the object, not the class. Inheritance models B is a kind of A. Keep trees shallow; one level is common, two is okay, more gets fragile. Use modules when you want to share behavior without an is-a link. A common mistake is forgetting method lookup walks the chain, and that class vars are shared up it, which is a surprise."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "At its simplest: `class B < A; end` makes B inherit A's methods.\n\nWhen you call b.foo, Ruby looks up b's class (B), then its superclass (A), then Object, etc., until it finds foo — the method lookup chain.\n\nB can override A's method (define its own foo); it can call A's version with super. An instance of B is an instance of A too (b.is_a?(A) # true). Constructors are inherited in lookup but usually you redefine initialize and call super with the right args. Class variables (@@x) are shared up the chain (often a surprise). Instance variables (@x) belong to the object, not the class. Inheritance models 'B is a kind of A'. Keep trees shallow; one level is common, two is okay, more gets fragile.\n\nA common mistake is forgetting method lookup walks the chain (or that @@vars are shared up it), or deep trees."
          },
          {
            "type": "flow_diagram",
            "title": "Inheritance Basics",
            "content": "class B < A; method lookup B -> A -> Object. override + super. b.is_a?(A). @@vars shared up.\n\n```mermaid\nflowchart LR\n  C[b.foo] --> L1[look in B]\n  L1 --> L2[then A]\n  L2 --> L3[then Object]\n  O[B overrides foo] --> S[super -> A's foo]\n```"
          },
          {
            "type": "code_example",
            "title": "Inheritance basics",
            "content": "```ruby\nclass A\n  def greet; \"hello from A\"; end\n  def name;  'A'; end\nend\n\n# B inherits A's methods\nclass B < A\n  def greet                  # override A#greet\n    super + \" (B too)\"       # super -> A#greet\n  end\nend\n\nb = B.new\nb.greet       # 'hello from A (B too)'  (B's greet, called super)\nb.name        # 'A'  (inherited from A; not overridden)\nb.is_a?(A)   # true  (a B IS an A)\nb.is_a?(B)   # true\n\n# METHOD LOOKUP walks the chain: B -> A -> Object -> Kernel -> BasicObject\nB.ancestors   # [B, A, Object, Kernel, BasicObject]\n# Ruby finds the FIRST method named `greet` going up -> B's greet.\n\n# initialize: usually redefine and call super\nclass Named\n  def initialize(name); @name = name; end\n  attr_reader :name\nend\nclass Dog < Named\n  def initialize(name, breed)\n    super(name)              # pass name to Named#initialize\n    @breed = breed\n  end\n  attr_reader :breed\nend\nd = Dog.new('Rex', 'Lab')\nd.name     # 'Rex'  (set by super -> Named#initialize)\nd.breed    # 'Lab'\n\n# @@class vars are SHARED up the chain (surprise!):\nclass Base; @@count = 0; end\nclass Sub < Base; @@count = 5; end   # changes Base's @@count too!\n\n# Keep trees SHALLOW; use modules to share behavior without is-a.\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Inheritance Basics in a real project?",
        "How would you compare Inheritance Basics with an alternative?",
        "What is a common mistake when using Inheritance Basics?"
      ],
      "seo": {
        "metaTitle": "Inheritance Basics — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How does inheritance work at a basic level in Ruby? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "ruby-oop-basics-inheritance-basics-q001",
      "slug": "ruby-oop-basics-inheritance-basics-when-to-use",
      "question": "When would you use Inheritance Basics in a real project?",
      "title": "When would you use Inheritance Basics in a real project",
      "direct_answer": "I would use How does inheritance work at a basic level in Ruby when the problem matches its main benefit. At its simplest: `class B < A; end` makes B inherit A's methods. In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How does inheritance work at a basic level in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How does inheritance work at a basic level in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining How does inheritance work at a basic level in Ruby in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. At its simplest: `class B < A; end` makes B inherit A's methods. When you call b.foo, Ruby looks up b's class (B), then its superclass (A), then Object, etc., until it finds foo — the method lookup chain. B can override A's method (define its own foo); it can call A's version with super. An instance of B is an instance of A too (b.is_a?(A) # true). Constructors are inherited in lookup but usually you redefine initialize and call super with the right args. Class variables (@@x) are shared up the chain (often a surprise). Instance variables (@x) belong to the object, not the class. Inheritance models 'B is a kind of A'. Keep trees shallow; one level is common, two is okay, more gets fragile. Use modules when you want to share behavior without an is-a link."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use How does inheritance work at a basic level in Ruby when its rule makes the code safer, clearer, or more efficient for the actual problem. At its simplest: `class B < A; end` makes B inherit A's methods. When you call b.foo, Ruby looks up b's class (B), then its superclass (A), then Object, etc., until it finds foo — the method lookup chain. B can override A's method (define its own foo); it can call A's version with super. An instance of B is an instance of A too (b.is_a?(A) # true). Constructors are inherited in lookup but usually you redefine initialize and call super with the right args. Class variables (@@x) are shared up the chain (often a surprise). Instance variables (@x) belong to the object, not the class. Inheritance models 'B is a kind of A'. Keep trees shallow; one level is common, two is okay, more gets fragile. Use modules when you want to share behavior without an is-a link. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "At its simplest: `class B < A; end` makes B inherit A's methods.\n\nWhen you call b.foo, Ruby looks up b's class (B), then its superclass (A), then Object, etc., until it finds foo — the method lookup chain.\n\nB can override A's method (define its own foo); it can call A's version with super. An instance of B is an instance of A too (b.is_a?(A) # true). Constructors are inherited in lookup but usually you redefine initialize and call super with the right args. Class variables (@@x) are shared up the chain (often a surprise). Instance variables (@x) belong to the object, not the class. Inheritance models 'B is a kind of A'. Keep trees shallow; one level is common, two is okay, more gets fragile.\n\nA common mistake is forgetting method lookup walks the chain (or that @@vars are shared up it), or deep trees."
          },
          {
            "type": "code_example",
            "title": "Inheritance basics",
            "content": "```ruby\nclass A\n  def greet; \"hello from A\"; end\n  def name;  'A'; end\nend\n\n# B inherits A's methods\nclass B < A\n  def greet                  # override A#greet\n    super + \" (B too)\"       # super -> A#greet\n  end\nend\n\nb = B.new\nb.greet       # 'hello from A (B too)'  (B's greet, called super)\nb.name        # 'A'  (inherited from A; not overridden)\nb.is_a?(A)   # true  (a B IS an A)\nb.is_a?(B)   # true\n\n# METHOD LOOKUP walks the chain: B -> A -> Object -> Kernel -> BasicObject\nB.ancestors   # [B, A, Object, Kernel, BasicObject]\n# Ruby finds the FIRST method named `greet` going up -> B's greet.\n\n# initialize: usually redefine and call super\nclass Named\n  def initialize(name); @name = name; end\n  attr_reader :name\nend\nclass Dog < Named\n  def initialize(name, breed)\n    super(name)              # pass name to Named#initialize\n    @breed = breed\n  end\n  attr_reader :breed\nend\nd = Dog.new('Rex', 'Lab')\nd.name     # 'Rex'  (set by super -> Named#initialize)\nd.breed    # 'Lab'\n\n# @@class vars are SHARED up the chain (surprise!):\nclass Base; @@count = 0; end\nclass Sub < Base; @@count = 5; end   # changes Base's @@count too!\n\n# Keep trees SHALLOW; use modules to share behavior without is-a.\n```"
          }
        ]
      },
      "followup_questions": [
        "How does inheritance work at a basic level in Ruby?",
        "How would you compare Inheritance Basics with an alternative?",
        "What is a common mistake when using Inheritance Basics?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Inheritance Basics in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Inheritance Basics in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-oop-basics-inheritance-basics-q002",
      "slug": "ruby-oop-basics-inheritance-basics-common-mistake",
      "question": "What is a common mistake when using Inheritance Basics?",
      "title": "a common mistake when using Inheritance Basics",
      "direct_answer": "A common mistake with How does inheritance work at a basic level in Ruby is using the rule without checking its boundary conditions. At its simplest: `class B < A; end` makes B inherit A's methods. That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How does inheritance work at a basic level in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How does inheritance work at a basic level in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of How does inheritance work at a basic level in Ruby. At its simplest: `class B < A; end` makes B inherit A's methods. When you call b.foo, Ruby looks up b's class (B), then its superclass (A), then Object, etc., until it finds foo — the method lookup chain. B can override A's method (define its own foo); it can call A's version with super. An instance of B is an instance of A too (b.is_a?(A) # true). Constructors are inherited in lookup but usually you redefine initialize and call super with the right args. Class variables (@@x) are shared up the chain (often a surprise). Instance variables (@x) belong to the object, not the class. Inheritance models 'B is a kind of A'. Keep trees shallow; one level is common, two is okay, more gets fragile. Use modules when you want to share behavior without an is-a link."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with How does inheritance work at a basic level in Ruby is assuming the happy path is the whole rule. At its simplest: `class B < A; end` makes B inherit A's methods. When you call b.foo, Ruby looks up b's class (B), then its superclass (A), then Object, etc., until it finds foo — the method lookup chain. B can override A's method (define its own foo); it can call A's version with super. An instance of B is an instance of A too (b.is_a?(A) # true). Constructors are inherited in lookup but usually you redefine initialize and call super with the right args. Class variables (@@x) are shared up the chain (often a surprise). Instance variables (@x) belong to the object, not the class. Inheritance models 'B is a kind of A'. Keep trees shallow; one level is common, two is okay, more gets fragile. Use modules when you want to share behavior without an is-a link. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "At its simplest: `class B < A; end` makes B inherit A's methods.\n\nWhen you call b.foo, Ruby looks up b's class (B), then its superclass (A), then Object, etc., until it finds foo — the method lookup chain.\n\nB can override A's method (define its own foo); it can call A's version with super. An instance of B is an instance of A too (b.is_a?(A) # true). Constructors are inherited in lookup but usually you redefine initialize and call super with the right args. Class variables (@@x) are shared up the chain (often a surprise). Instance variables (@x) belong to the object, not the class. Inheritance models 'B is a kind of A'. Keep trees shallow; one level is common, two is okay, more gets fragile.\n\nA common mistake is forgetting method lookup walks the chain (or that @@vars are shared up it), or deep trees."
          },
          {
            "type": "code_example",
            "title": "Inheritance basics",
            "content": "```ruby\nclass A\n  def greet; \"hello from A\"; end\n  def name;  'A'; end\nend\n\n# B inherits A's methods\nclass B < A\n  def greet                  # override A#greet\n    super + \" (B too)\"       # super -> A#greet\n  end\nend\n\nb = B.new\nb.greet       # 'hello from A (B too)'  (B's greet, called super)\nb.name        # 'A'  (inherited from A; not overridden)\nb.is_a?(A)   # true  (a B IS an A)\nb.is_a?(B)   # true\n\n# METHOD LOOKUP walks the chain: B -> A -> Object -> Kernel -> BasicObject\nB.ancestors   # [B, A, Object, Kernel, BasicObject]\n# Ruby finds the FIRST method named `greet` going up -> B's greet.\n\n# initialize: usually redefine and call super\nclass Named\n  def initialize(name); @name = name; end\n  attr_reader :name\nend\nclass Dog < Named\n  def initialize(name, breed)\n    super(name)              # pass name to Named#initialize\n    @breed = breed\n  end\n  attr_reader :breed\nend\nd = Dog.new('Rex', 'Lab')\nd.name     # 'Rex'  (set by super -> Named#initialize)\nd.breed    # 'Lab'\n\n# @@class vars are SHARED up the chain (surprise!):\nclass Base; @@count = 0; end\nclass Sub < Base; @@count = 5; end   # changes Base's @@count too!\n\n# Keep trees SHALLOW; use modules to share behavior without is-a.\n```"
          }
        ]
      },
      "followup_questions": [
        "How does inheritance work at a basic level in Ruby?",
        "When would you use Inheritance Basics in a real project?",
        "How would you compare Inheritance Basics with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Inheritance Basics — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Inheritance Basics? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-oop-basics-inheritance-basics-q003",
      "slug": "ruby-oop-basics-inheritance-basics-compare",
      "question": "How would you compare Inheritance Basics with an alternative?",
      "title": "compare Inheritance Basics with an alternative",
      "direct_answer": "I compare How does inheritance work at a basic level in Ruby with the simpler alternative by looking at correctness first, then time, memory, and readability. At its simplest: `class B < A; end` makes B inherit A's methods. The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How does inheritance work at a basic level in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How does inheritance work at a basic level in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. At its simplest: `class B < A; end` makes B inherit A's methods. When you call b.foo, Ruby looks up b's class (B), then its superclass (A), then Object, etc., until it finds foo — the method lookup chain. B can override A's method (define its own foo); it can call A's version with super. An instance of B is an instance of A too (b.is_a?(A) # true). Constructors are inherited in lookup but usually you redefine initialize and call super with the right args. Class variables (@@x) are shared up the chain (often a surprise). Instance variables (@x) belong to the object, not the class. Inheritance models 'B is a kind of A'. Keep trees shallow; one level is common, two is okay, more gets fragile. Use modules when you want to share behavior without an is-a link."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare How does inheritance work at a basic level in Ruby with the straightforward alternative on the same example. At its simplest: `class B < A; end` makes B inherit A's methods. When you call b.foo, Ruby looks up b's class (B), then its superclass (A), then Object, etc., until it finds foo — the method lookup chain. B can override A's method (define its own foo); it can call A's version with super. An instance of B is an instance of A too (b.is_a?(A) # true). Constructors are inherited in lookup but usually you redefine initialize and call super with the right args. Class variables (@@x) are shared up the chain (often a surprise). Instance variables (@x) belong to the object, not the class. Inheritance models 'B is a kind of A'. Keep trees shallow; one level is common, two is okay, more gets fragile. Use modules when you want to share behavior without an is-a link. If How does inheritance work at a basic level in Ruby removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "At its simplest: `class B < A; end` makes B inherit A's methods.\n\nWhen you call b.foo, Ruby looks up b's class (B), then its superclass (A), then Object, etc., until it finds foo — the method lookup chain.\n\nB can override A's method (define its own foo); it can call A's version with super. An instance of B is an instance of A too (b.is_a?(A) # true). Constructors are inherited in lookup but usually you redefine initialize and call super with the right args. Class variables (@@x) are shared up the chain (often a surprise). Instance variables (@x) belong to the object, not the class. Inheritance models 'B is a kind of A'. Keep trees shallow; one level is common, two is okay, more gets fragile.\n\nA common mistake is forgetting method lookup walks the chain (or that @@vars are shared up it), or deep trees."
          },
          {
            "type": "code_example",
            "title": "Inheritance basics",
            "content": "```ruby\nclass A\n  def greet; \"hello from A\"; end\n  def name;  'A'; end\nend\n\n# B inherits A's methods\nclass B < A\n  def greet                  # override A#greet\n    super + \" (B too)\"       # super -> A#greet\n  end\nend\n\nb = B.new\nb.greet       # 'hello from A (B too)'  (B's greet, called super)\nb.name        # 'A'  (inherited from A; not overridden)\nb.is_a?(A)   # true  (a B IS an A)\nb.is_a?(B)   # true\n\n# METHOD LOOKUP walks the chain: B -> A -> Object -> Kernel -> BasicObject\nB.ancestors   # [B, A, Object, Kernel, BasicObject]\n# Ruby finds the FIRST method named `greet` going up -> B's greet.\n\n# initialize: usually redefine and call super\nclass Named\n  def initialize(name); @name = name; end\n  attr_reader :name\nend\nclass Dog < Named\n  def initialize(name, breed)\n    super(name)              # pass name to Named#initialize\n    @breed = breed\n  end\n  attr_reader :breed\nend\nd = Dog.new('Rex', 'Lab')\nd.name     # 'Rex'  (set by super -> Named#initialize)\nd.breed    # 'Lab'\n\n# @@class vars are SHARED up the chain (surprise!):\nclass Base; @@count = 0; end\nclass Sub < Base; @@count = 5; end   # changes Base's @@count too!\n\n# Keep trees SHALLOW; use modules to share behavior without is-a.\n```"
          }
        ]
      },
      "followup_questions": [
        "How does inheritance work at a basic level in Ruby?",
        "When would you use Inheritance Basics in a real project?",
        "What is a common mistake when using Inheritance Basics?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Inheritance Basics with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Inheritance Basics with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-oop-basics-inheritance-basics-q004",
      "slug": "ruby-oop-basics-inheritance-basics-scenario",
      "question": "How would you debug a problem involving Inheritance Basics?",
      "title": "debug a problem involving Inheritance Basics",
      "direct_answer": "I would debug a problem involving How does inheritance work at a basic level in Ruby by reproducing it with the smallest input first. At its simplest: `class B < A; end` makes B inherit A's methods. Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How does inheritance work at a basic level in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How does inheritance work at a basic level in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what How does inheritance work at a basic level in Ruby is supposed to guarantee. At its simplest: `class B < A; end` makes B inherit A's methods. When you call b.foo, Ruby looks up b's class (B), then its superclass (A), then Object, etc., until it finds foo — the method lookup chain. B can override A's method (define its own foo); it can call A's version with super. An instance of B is an instance of A too (b.is_a?(A) # true). Constructors are inherited in lookup but usually you redefine initialize and call super with the right args. Class variables (@@x) are shared up the chain (often a surprise). Instance variables (@x) belong to the object, not the class. Inheritance models 'B is a kind of A'. Keep trees shallow; one level is common, two is okay, more gets fragile. Use modules when you want to share behavior without an is-a link."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the How does inheritance work at a basic level in Ruby bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: At its simplest: `class B < A; end` makes B inherit A's methods. When you call b.foo, Ruby looks up b's class (B), then its superclass (A), then Object, etc., until it finds foo — the method lookup chain. B can override A's method (define its own foo); it can call A's version with super. An instance of B is an instance of A too (b.is_a?(A) # true). Constructors are inherited in lookup but usually you redefine initialize and call super with the right args. Class variables (@@x) are shared up the chain (often a surprise). Instance variables (@x) belong to the object, not the class. Inheritance models 'B is a kind of A'. Keep trees shallow; one level is common, two is okay, more gets fragile. Use modules when you want to share behavior without an is-a link. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "At its simplest: `class B < A; end` makes B inherit A's methods.\n\nWhen you call b.foo, Ruby looks up b's class (B), then its superclass (A), then Object, etc., until it finds foo — the method lookup chain.\n\nB can override A's method (define its own foo); it can call A's version with super. An instance of B is an instance of A too (b.is_a?(A) # true). Constructors are inherited in lookup but usually you redefine initialize and call super with the right args. Class variables (@@x) are shared up the chain (often a surprise). Instance variables (@x) belong to the object, not the class. Inheritance models 'B is a kind of A'. Keep trees shallow; one level is common, two is okay, more gets fragile.\n\nA common mistake is forgetting method lookup walks the chain (or that @@vars are shared up it), or deep trees."
          },
          {
            "type": "code_example",
            "title": "Inheritance basics",
            "content": "```ruby\nclass A\n  def greet; \"hello from A\"; end\n  def name;  'A'; end\nend\n\n# B inherits A's methods\nclass B < A\n  def greet                  # override A#greet\n    super + \" (B too)\"       # super -> A#greet\n  end\nend\n\nb = B.new\nb.greet       # 'hello from A (B too)'  (B's greet, called super)\nb.name        # 'A'  (inherited from A; not overridden)\nb.is_a?(A)   # true  (a B IS an A)\nb.is_a?(B)   # true\n\n# METHOD LOOKUP walks the chain: B -> A -> Object -> Kernel -> BasicObject\nB.ancestors   # [B, A, Object, Kernel, BasicObject]\n# Ruby finds the FIRST method named `greet` going up -> B's greet.\n\n# initialize: usually redefine and call super\nclass Named\n  def initialize(name); @name = name; end\n  attr_reader :name\nend\nclass Dog < Named\n  def initialize(name, breed)\n    super(name)              # pass name to Named#initialize\n    @breed = breed\n  end\n  attr_reader :breed\nend\nd = Dog.new('Rex', 'Lab')\nd.name     # 'Rex'  (set by super -> Named#initialize)\nd.breed    # 'Lab'\n\n# @@class vars are SHARED up the chain (surprise!):\nclass Base; @@count = 0; end\nclass Sub < Base; @@count = 5; end   # changes Base's @@count too!\n\n# Keep trees SHALLOW; use modules to share behavior without is-a.\n```"
          }
        ]
      },
      "followup_questions": [
        "How does inheritance work at a basic level in Ruby?",
        "When would you use Inheritance Basics in a real project?",
        "How would you compare Inheritance Basics with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Inheritance Basics — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Inheritance Basics? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

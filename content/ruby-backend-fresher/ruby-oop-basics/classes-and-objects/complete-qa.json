{
  "topic": "Classes and Objects",
  "topicSlug": "classes-and-objects",
  "questions": [
    {
      "id": "classes-and-objects-q001",
      "slug": "ruby-classes-and-objects-basics",
      "question": "What are classes and objects in Ruby?",
      "title": "Classes and Objects",
      "direct_answer": "A class is a blueprint; an object is an instance. `class Dog; def bark; 'Woof'; end; end; d = Dog.new; d.bark` -> 'Woof'. `new` calls `initialize` (the constructor), which sets up instance variables (@name): `def initialize(name); @name = name; end`. Instance variables (@x) hold per-object state; instance methods define behavior. `Dog.new('Rex')` creates an object of class Dog; `d.class` is Dog; `d.is_a?(Dog)` is true. Classes are objects too (Class) — Dog is an instance of Class, so classes have methods (Dog.new, Dog.name). Inheritance: class Puppy < Dog; end (Puppy gets Dog's methods). Everything in Ruby is an object (numbers, strings, nil, classes) — they all have methods and a class. Define a class with state (initialize + @vars) and behavior (methods); create instances with new.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know a class is a blueprint, an object is an instance, new calls initialize, and @vars hold state.",
        "common_mistake": "Forgetting to set @vars in initialize (nil later), or confusing class (Dog) with instance (d).",
        "to_stand_out": "Class = blueprint; object = instance (Dog.new). new calls initialize; @vars = per-object state. Everything is an object. Inheritance: Puppy < Dog."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Blueprint and Instance",
            "content": "A class is a blueprint; an object is an instance. class Dog; def bark; Woof; end; end; d Dog dot new; d dot bark to Woof. new calls initialize (the constructor), which sets up instance variables (name): def initialize name; name var name; end. Instance variables (x) hold per-object state; instance methods define behavior. Dog new Rex creates an object of class Dog; d dot class is Dog; d dot is_a Dog is true. Classes are objects too (Class) — Dog is an instance of Class, so classes have methods (Dog dot new, Dog dot name). Inheritance: class Puppy less-than Dog; end (Puppy gets Dog's methods). Everything in Ruby is an object (numbers, strings, nil, classes) — they all have methods and a class. Define a class with state (initialize plus vars) and behavior (methods); create instances with new."
          },
          {
            "type": "flow_diagram",
            "title": "Classes + Objects",
            "content": "class = blueprint; object = instance (Dog.new). new -> initialize; @vars = state. Everything is an object.\n\n```mermaid\nflowchart LR\n  C[class Dog] --> N[Dog.new('Rex')]\n  N --> I[initialize sets @name]\n  N --> M[d.bark uses @name]\n  P[Puppy < Dog] --> Inh[inherits Dog's methods]\n```"
          },
          {
            "type": "code_example",
            "title": "Classes and objects",
            "content": "```ruby\n# A class is a blueprint; an object is an instance\nclass Dog\n  def initialize(name); @name = name; end   # constructor (new calls this)\n  def bark;        \"#{@name} says Woof\"; end   # instance method\nend\n\nd = Dog.new('Rex')      # new -> initialize -> @name = 'Rex'\nd.bark                  # 'Rex says Woof'\nd.class                 # Dog\nd.is_a?(Dog)           # true\n\n# Instance variables = per-object STATE; instance methods = BEHAVIOR\nclass Counter\n  def initialize; @n = 0; end\n  def inc; @n += 1; end\n  def count; @n; end\nend\nc = Counter.new\nc.inc; c.inc; c.count   # 2  (state lives on the instance)\n\n# Everything is an object (numbers, strings, nil, even classes):\n42.class                # Integer\n'hi'.class             # String\nnil.class              # NilClass\nDog.class              # Class   (classes are objects of Class)\n\n# Inheritance: subclass gets superclass's methods\nclass Puppy < Dog\n  def yap; 'yip'; end\nend\np = Puppy.new('Buddy')\np.bark                 # 'Buddy says Woof' (inherited from Dog)\np.yap                  # 'yip'\np.is_a?(Dog)          # true\np.is_a?(Puppy)        # true\n\n# Define STATE (initialize + @vars) + BEHAVIOR (methods); create with new.\n```"
          },
          {
            "type": "comparison_table",
            "title": "Class Basics",
            "columns": [
              "Concept",
              "Meaning"
            ],
            "rows": [
              [
                "class",
                "blueprint"
              ],
              [
                "object",
                "instance (Dog.new)"
              ],
              [
                "initialize",
                "constructor (new calls it)"
              ],
              [
                "@x",
                "instance variable (per-object state)"
              ],
              [
                "inheritance",
                "Puppy < Dog"
              ],
              [
                "everything",
                "is an object (even classes)"
              ]
            ]
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "items": [
              "Forgetting to set @vars in initialize (they're nil later).",
              "Confusing the class (Dog) with an instance (d) — Dog is the blueprint, d is one object.",
              "Forgetting everything is an object (even nil and classes)."
            ]
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "A class is a blueprint; an object is an instance (Dog.new creates one).",
              "`new` calls `initialize` (the constructor), which sets instance variables (@name).",
              "Instance variables (@x) hold per-object state; instance methods define behavior.",
              "Classes are objects too (Dog is an instance of Class); everything is an object.",
              "Inheritance: class Puppy < Dog; end — Puppy gets Dog's methods."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "A class is a blueprint; an object is an instance. class Dog; def bark; Woof; end; end; d Dog dot new; d dot bark to Woof. new calls initialize (the constructor), which sets up instance variables (name): def initialize name; name var name; end. Instance variables (x) hold per-object state; instance methods define behavior. Dog new Rex creates an object of class Dog; d dot class is Dog; d dot is_a Dog is true. Classes are objects too (Class) — Dog is an instance of Class, so classes have methods (Dog dot new, Dog dot name). Inheritance: class Puppy less-than Dog; end (Puppy gets Dog's methods). Everything in Ruby is an object (numbers, strings, nil, classes) — they all have methods and a class. Define a class with state (initialize plus vars) and behavior (methods); create instances with new. A common mistake is forgetting to set vars in initialize, leaving them nil, and confusing the class with an instance."
          }
        ]
      },
      "followup_questions": [
        "What's a class vs object (blueprint vs instance; Dog.new)?",
        "What does new do (calls initialize, the constructor, to set @vars)?",
        "What holds state (@x = per-object instance variables)?",
        "Is everything an object (yes — numbers, strings, nil, even classes are objects)?"
      ],
      "seo": {
        "metaTitle": "Classes and Objects — Blueprint and Instance",
        "metaDescription": "Class = blueprint; object = instance (Dog.new). new calls initialize; @vars = per-object state. Everything is an object. Inheritance: Puppy < Dog."
      },
      "order": 1
    }
  ]
}

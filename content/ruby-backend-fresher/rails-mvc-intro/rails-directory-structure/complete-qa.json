{
  "topic": "Rails Directory Structure",
  "topicSlug": "rails-directory-structure",
  "questions": [
    {
      "id": "rails-directory-structure-q001",
      "slug": "rails-directory-structure-basics",
      "question": "What's in a Rails app's directory structure?",
      "title": "The Rails Directory Structure",
      "direct_answer": "A Rails app has a predictable layout: app/ holds your code — models (app/models), controllers (app/controllers), views (app/views), and jobs (app/jobs), mailers (app/mailers); config/ holds routes.rb, environments, database.yml; db/ holds migrations (migrate/) and schema.rb; test/ or spec/ holds tests; Gemfile and Gemfile.lock list dependencies. The app/ folders map to MVC: models, controllers, views. config/routes.rb is the entry point (URLs -> actions). db/migrate/ holds schema changes over time; rake db:migrate runs them. Knowing the layout is how you find where a feature lives — 'where's the Post logic? app/models/post.rb.'",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you can name the key folders (app/models, app/controllers, app/views, config/routes.rb, db/migrate) and what lives in each.",
        "common_mistake": "Not knowing where a feature lives (model in app/models, not app/controllers), or putting logic outside the conventional folders.",
        "to_stand_out": "app/ (models, controllers, views, jobs); config/routes.rb (entry point); db/migrate (schema changes); Gemfile (deps). Folders map to MVC; convention finds everything."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Where Everything Lives",
            "content": "A Rails app has a predictable layout: app holds your code — models (app slash models), controllers (app slash controllers), views (app slash views), and jobs (app slash jobs), mailers (app slash mailers); config holds routes.rb, environments, database dot yml; db holds migrations (migrate) and schema dot rb; test or spec holds tests; Gemfile and Gemfile.lock list dependencies. The app folders map to MVC: models, controllers, views. config slash routes.rb is the entry point (URLs to actions). db slash migrate holds schema changes over time; rake db migrate runs them. Knowing the layout is how you find where a feature lives — where's the Post logic? app slash models slash post dot rb."
          },
          {
            "type": "flow_diagram",
            "title": "The Layout",
            "content": "app/ (models/controllers/views); config/routes.rb; db/migrate; Gemfile. Folders map to MVC.\n\n```mermaid\nflowchart LR\n  A[app/models + controllers + views] --> C[config/routes.rb]\n  C --> D[db/migrate + schema.rb]\n  D --> G[Gemfile + Gemfile.lock]\n  G --> T[test/ or spec/]\n```"
          },
          {
            "type": "code_example",
            "title": "The directory structure",
            "content": "```ruby\n# A Rails app's layout (convention over configuration):\n# app/                        # YOUR CODE\n#   models/   post.rb          # data + rules (Post)\n#   controllers/ posts_controller.rb  # orchestration\n#   views/    posts/show.html.erb     # presentation\n#   jobs/     cleanup_job.rb          # background jobs\n#   mailers/  user_mailer.rb          # email\n#\n# config/\n#   routes.rb              # URLs -> controller#action (entry point)\n#   environments/           # development.rb / production.rb\n#   database.yml           # DB config\n#\n# db/\n#   migrate/               # schema changes over time\n#     20260808_create_posts.rb\n#   schema.rb               # current schema snapshot\n#   seeds.rb                # seed data (rake db:seed)\n#\n# Gemfile / Gemfile.lock    # dependencies\n# test/ or spec/            # tests\n#\n# Find a feature: Post logic -> app/models/post.rb\n#   PostsController      -> app/controllers/posts_controller.rb\n#   the show page        -> app/views/posts/show.html.erb\n```"
          },
          {
            "type": "comparison_table",
            "title": "Key Folders",
            "columns": [
              "Folder",
              "Holds"
            ],
            "rows": [
              [
                "app/models",
                "data + rules (Post)"
              ],
              [
                "app/controllers",
                "orchestration"
              ],
              [
                "app/views",
                "presentation"
              ],
              [
                "config/routes.rb",
                "URLs -> actions"
              ],
              [
                "db/migrate",
                "schema changes"
              ],
              [
                "Gemfile",
                "dependencies"
              ]
            ]
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "items": [
              "Not knowing where a feature lives (a model is in app/models, not app/controllers).",
              "Putting logic outside the conventional folders (Rails won't find it).",
              "Editing schema.rb by hand (it's generated from migrations; don't)."
            ]
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "app/ holds your code: models, controllers, views, jobs, mailers.",
              "config/routes.rb is the entry point: URLs map to controller#action.",
              "db/migrate/ holds schema changes over time; db/schema.rb is the snapshot.",
              "Gemfile + Gemfile.lock list dependencies; test/ or spec/ hold tests.",
              "The folders map to MVC; knowing the layout is how you find where a feature lives."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "A Rails app has a predictable layout: app holds your code — models (app slash models), controllers (app slash controllers), views (app slash views), and jobs (app slash jobs), mailers (app slash mailers); config holds routes.rb, environments, database dot yml; db holds migrations (migrate) and schema dot rb; test or spec holds tests; Gemfile and Gemfile.lock list dependencies. The app folders map to MVC: models, controllers, views. config slash routes.rb is the entry point (URLs to actions). db slash migrate holds schema changes over time; rake db migrate runs them. Knowing the layout is how you find where a feature lives — where's the Post logic? app slash models slash post dot rb. A common mistake is not knowing where a feature lives, like a model in app slash models not app slash controllers, and editing schema dot rb by hand, when it's generated from migrations."
          }
        ]
      },
      "followup_questions": [
        "What's in app/ (models, controllers, views, jobs, mailers)?",
        "What's the entry point (config/routes.rb: URLs -> controller#action)?",
        "What's in db/ (migrate/ for schema changes; schema.rb snapshot)?",
        "How do you find a feature (Post -> app/models/post.rb; controller -> app/controllers)?"
      ],
      "seo": {
        "metaTitle": "The Rails Directory Structure — Where Everything Lives",
        "metaDescription": "app/ (models, controllers, views, jobs); config/routes.rb (entry point); db/migrate (schema changes); Gemfile (deps). Folders map to MVC; convention finds everything."
      },
      "order": 1
    }
  ]
}

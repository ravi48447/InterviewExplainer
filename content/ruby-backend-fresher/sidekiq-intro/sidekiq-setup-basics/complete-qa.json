{
  "topic": "Sidekiq Setup Basics",
  "topicSlug": "sidekiq-setup-basics",
  "questions": [
    {
      "id": "sidekiq-setup-basics-q001",
      "slug": "ruby-sidekiq-setup-basics",
      "question": "How do you set up Sidekiq in a Rails app?",
      "title": "Sidekiq Setup Basics",
      "direct_answer": "Setup: (1) gem 'sidekiq' in Gemfile, bundle install. (2) Set the adapter: config.active_job.queue_adapter = :sidekiq (or use Sidekiq directly: include Sidekiq::Job). (3) Configure Sidekiq: config/initializers/sidekiq.rb with Sidekiq.configure_server / configure_client (Redis URL, logging). (4) Run workers: `bundle exec sidekiq` (reads config/sidekiq.yml for queues, concurrency). (5) (Optional) Mount the Web UI: `require 'sidekiq/web'; mount Sidekiq::Web => '/sidekiq'` in routes (protect with auth in prod). Redis is required (the queue store). Concurrency (threads per process) is set in sidekiq.yml (default 5-10). Queues: sidekiq.yml lists queues with priorities. For prod, run multiple Sidekiq processes behind a process manager (systemd, Upstart). Keep jobs small and idempotent; pass IDs not objects.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know the Sidekiq setup steps: gem, adapter/worker, Redis config, bundle exec sidekiq, Web UI mount.",
        "common_mistake": "Exposing the Sidekiq Web UI without auth in production (anyone could retry/kill jobs).",
        "to_stand_out": "Sidekiq setup: gem 'sidekiq'; adapter = :sidekiq or include Sidekiq::Job; Redis config; bundle exec sidekiq; mount Sidekiq::Web (protect it)."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Wire It Up",
            "content": "Setup: (1) gem sidekiq in Gemfile, bundle install. (2) Set the adapter: config active job queue adapter sidekiq (or use Sidekiq directly: include Sidekiq Job). (3) Configure Sidekiq: config initializers sidekiq rb with Sidekiq configure server or configure client (Redis URL, logging). (4) Run workers: bundle exec sidekiq (reads config sidekiq yml for queues, concurrency). (5) Optional: mount the Web UI: require sidekiq web; mount Sidekiq Web slash sidekiq in routes (protect with auth in prod). Redis is required (the queue store). Concurrency (threads per process) is set in sidekiq yml (default 5 to 10). Queues: sidekiq yml lists queues with priorities. For prod, run multiple Sidekiq processes behind a process manager (systemd, Upstart). Keep jobs small and idempotent; pass IDs not objects."
          },
          {
            "type": "flow_diagram",
            "title": "Sidekiq Setup",
            "content": "gem 'sidekiq'; adapter or include Sidekiq::Job; Redis config; bundle exec sidekiq; mount Sidekiq::Web (protect).\n\n```mermaid\nflowchart LR\n  G[gem 'sidekiq'] --> C[configure Redis]\n  C --> R[sidekiq.yml: queues, concurrency]\n  R --> W[bundle exec sidekiq]\n  W --> U[Web UI: mount /sidekiq + auth]\n```"
          },
          {
            "type": "code_example",
            "title": "Sidekiq setup",
            "content": "```ruby\n# 1) Gemfile\ngem 'sidekiq'\n# then: bundle install\n\n# 2) Set the Active Job adapter (config/environments/production.rb)\nconfig.active_job.queue_adapter = :sidekiq\n\n#    OR use Sidekiq directly:\nclass MailJob\n  include Sidekiq::Job              # (older: include Sidekiq::Worker)\n  def perform(user_id); User.find(user_id); end\nend\nMailJob.perform_async(1)\n\n# 3) config/initializers/sidekiq.rb (Redis + logging)\nSidekiq.configure_server do |c|\n  c.redis = { url: ENV['REDIS_URL'] || 'redis://localhost:6379/0' }\nend\nSidekiq.configure_client do |c|\n  c.redis = { url: ENV['REDIS_URL'] || 'redis://localhost:6379/0' }\nend\n\n# 4) config/sidekiq.yml (queues + concurrency)\n#:concurrency: 10\n#:queues:\n#  - [default, 1]\n#  - [mailers, 2]\n#  - [urgent, 3]\n\n# Run workers:  bundle exec sidekiq\n\n# 5) Web UI (config/routes.rb) -- PROTECT in production!\n# require 'sidekiq/web'\n# authenticate :admin do\n#   mount Sidekiq::Web => '/sidekiq'\n# end\n\n# Redis is REQUIRED (the queue store). Concurrency = threads per process.\n# In prod: run multiple Sidekiq processes (systemd/Upstart), each with N threads.\n# Keep jobs SMALL and IDEMPOTENT; pass IDs, not objects (JSON-serialized).\n```"
          },
          {
            "type": "comparison_table",
            "title": "Sidekiq Setup",
            "columns": [
              "Step",
              "Action"
            ],
            "rows": [
              [
                "1",
                "gem 'sidekiq'; bundle"
              ],
              [
                "2",
                "adapter = :sidekiq or include Sidekiq::Job"
              ],
              [
                "3",
                "configure Redis (server/client)"
              ],
              [
                "4",
                "sidekiq.yml (queues, concurrency)"
              ],
              [
                "5",
                "bundle exec sidekiq; mount Web UI"
              ]
            ]
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "items": [
              "Exposing the Sidekiq Web UI without auth in production (anyone can retry/kill jobs).",
              "Forgetting Redis is required (the queue store).",
              "Running one process in prod (run several for throughput/redundancy)."
            ]
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "Setup: (1) gem 'sidekiq' in Gemfile, bundle install.",
              "(2) config.active_job.queue_adapter = :sidekiq, or include Sidekiq::Job directly.",
              "(3) config/initializers/sidekiq.rb: configure Redis (server + client).",
              "(4) config/sidekiq.yml: queues + concurrency; run `bundle exec sidekiq`.",
              "(5) Mount Sidekiq::Web at /sidekiq — protect with auth in production.",
              "Redis is required; concurrency = threads/process; run multiple processes in prod."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "Setup: (1) gem sidekiq in Gemfile, bundle install. (2) Set the adapter: config active job queue adapter sidekiq (or use Sidekiq directly: include Sidekiq Job). (3) Configure Sidekiq: config initializers sidekiq rb with Sidekiq configure server or configure client (Redis URL, logging). (4) Run workers: bundle exec sidekiq (reads config sidekiq yml for queues, concurrency). (5) Optional: mount the Web UI: require sidekiq web; mount Sidekiq Web slash sidekiq in routes (protect with auth in prod). Redis is required (the queue store). Concurrency (threads per process) is set in sidekiq yml (default 5 to 10). Queues: sidekiq yml lists queues with priorities. For prod, run multiple Sidekiq processes behind a process manager (systemd, Upstart). Keep jobs small and idempotent; pass IDs not objects. A common mistake is exposing the Sidekiq Web UI without auth in production, when anyone could retry or kill jobs."
          }
        ]
      },
      "followup_questions": [
        "What are the setup steps (gem, adapter/worker, Redis config, bundle exec sidekiq, Web UI)?",
        "What's required (Redis — the queue store)?",
        "How do you run workers (bundle exec sidekiq; reads sidekiq.yml)?",
        "How do you secure the Web UI (mount /sidekiq behind auth in prod)?"
      ],
      "seo": {
        "metaTitle": "Sidekiq Setup Basics — Wire It Up",
        "metaDescription": "Sidekiq setup: gem 'sidekiq'; adapter = :sidekiq or include Sidekiq::Job; Redis config; bundle exec sidekiq; mount Sidekiq::Web (protect it)."
      },
      "order": 1
    }
  ]
}

{
  "topic": "Data Types and Variables",
  "topicSlug": "data-types-variables",
  "questions": [
    {
      "id": "data-types-variables-q001",
      "slug": "ruby-data-types-variables-basics",
      "question": "What are the data types and variables in Ruby?",
      "title": "Data Types and Variables in Ruby",
      "direct_answer": "Ruby is dynamically typed: variables do not have declared types; they hold references to objects, and the object determines the available behaviour. Common objects include integers, floats, strings, symbols, arrays, hashes, ranges, booleans, and `nil`. A variable's prefix communicates scope: local `name`, instance `@name`, class `@@name`, global `$name`, or constant `NAME`.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 5,
      "last_updated": "2026-09-07",
      "interviewer_intent": {
        "testing": "Whether you know Ruby is dynamically typed, the core types (Integer/String/Symbol/Array/Hash/nil), and the variable scopes (local/instance/class/global/constant).",
        "common_mistake": "Checking types with == (use .class or duck typing), or confusing symbol (:a) and string ('a') keys.",
        "to_stand_out": "Ruby is dynamically typed. Types: Integer, Float, String, Symbol, Array, Hash, Range, Boolean, nil. Vars: local x, @x instance, @@x class, $x global, UPPER const. Everything is an object."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "key_points",
            "title": "Quick revision",
            "content": "- Variables reference objects; the variable itself has no declared type.\n- Common objects include numbers, Strings, Symbols, Arrays, Hashes, and Ranges.\n- Local, instance, class, global, and constant names signal different scopes.\n- Every value is an object and responds to methods defined for its class.\n- Ruby favours behaviour and duck typing over repeated class checks."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "answerSize": "compact",
            "content": "Ruby is dynamically typed. A variable is a name that refers to an object; the object has a class and provides the available methods. Ruby does not declare a variable as `int count`. After `count = 3`, the name refers to an Integer, and a later assignment can make it refer to a String.\n\nCommon Ruby objects include Integer, Float, String, Symbol, Array, Hash, Range, `true`, `false`, and `nil`. Every value is an object, so even `3.times` is a method call. Ruby often uses duck typing: code asks for the required behaviour instead of repeatedly checking the object's exact class.\n\nAssignment does not copy a mutable object. After `backup = items`, both names refer to the same Array, so `items << \"book\"` is visible through `backup`. Later, `items = []` only points `items` at a different Array; it does not change what `backup` refers to.\n\nRuby spelling communicates scope: `total` is local, `@total` belongs to one object, `@@total` is shared through a class hierarchy, `$total` is global, and `TOTAL` is a constant. Constants express an intention not to reassign; Ruby can still reassign one with a warning. Only `nil` and `false` are falsey—zero and empty collections are truthy.",
            "beats": [
              {
                "cue": "Explain the binding model",
                "stage": "Names point to objects",
                "spokenText": "Ruby is dynamically typed. A variable is a name that refers to an object; the object has a class and provides the available methods. Ruby does not declare a variable as `int count`. After `count = 3`, the name refers to an Integer, and a later assignment can make it refer to a String.",
                "support": {
                  "type": "comparison",
                  "title": "Keep name, object, and assignment separate",
                  "items": [
                    {
                      "label": "Variable name",
                      "value": "count",
                      "detail": "Has a scope and refers to an object.",
                      "tone": "blue"
                    },
                    {
                      "label": "Object",
                      "value": "3",
                      "detail": "Carries its class, state, and behaviour.",
                      "tone": "green"
                    },
                    {
                      "label": "Assignment",
                      "value": "count = 3",
                      "detail": "Changes the binding; it does not declare a fixed variable type.",
                      "tone": "neutral"
                    }
                  ]
                }
              },
              {
                "cue": "Name common objects and behaviour",
                "stage": "Objects carry behaviour",
                "spokenText": "Common Ruby objects include Integer, Float, String, Symbol, Array, Hash, Range, `true`, `false`, and `nil`. Every value is an object, so even `3.times` is a method call. Ruby often uses duck typing: code asks for the required behaviour instead of repeatedly checking the object's exact class."
              },
              {
                "cue": "Show aliasing, mutation, and rebinding",
                "stage": "Mutation is not reassignment",
                "spokenText": "Assignment does not copy a mutable object. After `backup = items`, both names refer to the same Array, so `items << \"book\"` is visible through `backup`. Later, `items = []` only points `items` at a different Array; it does not change what `backup` refers to.",
                "support": {
                  "type": "code",
                  "title": "Watch two names share one Array",
                  "language": "ruby",
                  "code": "value = 3\nputs value.class\n\nvalue = \"three\"\nputs value.class\n\nitems = []\nbackup = items\nitems << \"book\"\np backup\n\nitems = []\np items\np backup",
                  "caption": "Mutation changes the shared Array; reassignment changes only one name's binding."
                }
              },
              {
                "cue": "Explain the scope encoded in variable spelling",
                "stage": "Spelling shows scope",
                "spokenText": "Ruby spelling communicates scope: `total` is local, `@total` belongs to one object, `@@total` is shared through a class hierarchy, `$total` is global, and `TOTAL` is a constant. Constants express an intention not to reassign; Ruby can still reassign one with a warning. Only `nil` and `false` are falsey—zero and empty collections are truthy.",
                "recallRule": "Names have scope, objects have type and behaviour, and assignment changes a binding rather than fixing a variable's type."
              }
            ]
          },
          {
            "type": "deep_explanation",
            "title": "Names point to objects",
            "content": "An assignment changes a binding between a name and an object. It does not pour a new type into a variable. This explains why `value = 10` followed by `value = 'ten'` is valid, and why aliases to the same Array observe the same mutation. Method arguments follow the same model: Ruby passes the object reference as a value, so a method can mutate the referenced object but cannot rebind the caller's local variable.\n\nObject behaviour matters more than labels. Integers respond to arithmetic, Strings to text operations, and collections to iteration. Duck typing takes that one step further: a method can accept any object that responds to the required messages. `render(item)` may need only `item.to_s`, not a specific inheritance tree.\n\nVariable spelling creates scope. A local name lives in its lexical context. An instance variable is stored on a particular object and begins as `nil` if it has not been assigned. Class variables are shared with descendants and can surprise callers, so class-instance variables are often safer for independent class state. Globals are visible broadly and make dependencies difficult to follow.\n\nRuby truthiness is separate from type. Only `nil` and `false` are falsey; zero, empty Strings, and empty Arrays are truthy. Type checks such as `is_a?` are still appropriate at system boundaries, but ordinary collaboration is clearer when code asks for the operation it needs."
          },
          {
            "type": "concept_map",
            "title": "Separate scope from type",
            "content": "blue|Variable name|~Chooses a binding and scope|`name`, `@name`, `@@name`, `$name`, `NAME`\nemerald|Object|~Carries class, state, and behaviour|Integer, String, Array, custom object\namber|Assignment|~Points a name at an object|does not declare or copy the object's type\nviolet|Method call|~Uses behaviour|duck typing asks what the object can do"
          },
          {
            "type": "code_example",
            "title": "Reassignment is not mutation",
            "content": "```ruby\nvalue = 3\nvalue = 'three'              # same local name, different object\n\nitems = []\nbackup = items               # both names reference one Array\nitems << 'book'              # mutate that Array\nbackup                       # => ['book']\n\nitems = []                   # rebind only items\nbackup                       # => ['book']\n\nclass Cart\n  LIMIT = 20                 # constant naming expresses intent\n  def initialize\n    @items = []               # state on this Cart object\n  end\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Data Types Variables in a real project?",
        "How would you compare Data Types Variables with an alternative?",
        "What is a common mistake when using Data Types Variables?"
      ],
      "seo": {
        "metaTitle": "Data Types and Variables in Ruby — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What are the data types and variables in Ruby? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    }
  ]
}

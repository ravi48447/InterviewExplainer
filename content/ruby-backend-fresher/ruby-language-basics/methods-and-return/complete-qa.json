{
  "topic": "Methods and Return",
  "topicSlug": "methods-and-return",
  "questions": [
    {
      "id": "methods-and-return-q001",
      "slug": "ruby-methods-and-return-basics",
      "question": "How do methods and return work in Ruby?",
      "title": "Methods and Return in Ruby",
      "direct_answer": "A method is defined with def: def greet(name); \"Hello, #{name}\"; end. Ruby returns the last expression by default (implicit return) — no need for `return` unless you exit early. def add(a,b); a + b; end returns a+b. Arguments: required (a), default (a=1), keyword (name:), splat (*args collects extras into an array), double-splat (**opts collects keyword args into a hash). Methods are called with or without parens (puts 'hi' or puts('hi')). `return` exits immediately (return x if x.nil?); without a value, returns nil. Methods end with end. Method naming: snake_case; predicate methods end with ? (empty?); dangerous ones with ! (save!). Every call returns something (nil if nothing else).",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know def/end, implicit return (last expression), default/keyword/splat args, and ?/! naming.",
        "common_mistake": "Adding explicit `return` everywhere (Ruby returns the last expression), or using parens inconsistently.",
        "to_stand_out": "Methods: def name(args); ...; end. Implicit return (last expression). Args: required, default (a=1), keyword (name:), *args, **opts. snake_case; ? predicates, ! dangerous."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Define, Call, Return",
            "content": "A method is defined with def: def greet name; Hello name; end. Ruby returns the last expression by default (implicit return) — no need for return unless you exit early. def add a b; a plus b; end returns a plus b. Arguments: required (a), default (a 1), keyword (name), splat (star args collects extras into an array), double-splat (star star opts collects keyword args into a hash). Methods are called with or without parens (puts hi or puts hi). return exits immediately (return x if x dot nil); without a value, returns nil. Methods end with end. Method naming: snake_case; predicate methods end with ? (empty); dangerous ones with ! (save bang). Every call returns something (nil if nothing else)."
          },
          {
            "type": "flow_diagram",
            "title": "Method Anatomy",
            "content": "def name(args)...end. Implicit return. Args: required/default/keyword/*args/**opts. snake_case; ?/! naming.\n\n```mermaid\nflowchart LR\n  D[def greet(name)] --> B[body: last expr = return]\n  B --> R[implicit return]\n  A[args] --> Re[required a]\n  A --> De[default a=1]\n  A --> K[keyword name:]\n  A --> S[splat *args / **opts]\n```"
          },
          {
            "type": "code_example",
            "title": "Methods and return",
            "content": "```ruby\n# Define a method\ndef greet(name)\n  \"Hello, #{name}\"     # last expression -> IMPLICIT return\nend\ngreet('Ada')            # 'Hello, Ada'\n\n# Implicit return: no `return` needed (last expression is returned)\ndef add(a, b)\n  a + b                  # returns a + b\nend\n\n# `return` exits EARLY:\ndef find_first(xs)\n  xs.each { |x| return x if x > 0 }   # exit early\n  nil                    # default\nend\n\n# Arguments:\ndef f(a, b = 1, name:, *args, **opts)\n  # a: required; b: default (1); name: keyword (required);\n  # *args: extras -> Array; **opts: keyword extras -> Hash\nend\nf(10, name: 'Ada', 1, 2, extra: 'x')\n\n# Call with or without parens (both fine):\nputs 'hi'\nputs('hi')\n\n# Naming: snake_case; ? = predicate (returns bool); ! = dangerous\narr.empty?    # ? -> returns true/false\narr.sort!     # ! -> mutates in place (dangerous)\n\n# Every call returns something (nil if nothing else):\nresult = [1,2].each { |x| x }   # returns [1,2] (each returns the original)\n```"
          },
          {
            "type": "comparison_table",
            "title": "Method Facts",
            "columns": [
              "Feature",
              "How"
            ],
            "rows": [
              [
                "Define",
                "def name(args) ... end"
              ],
              [
                "Return",
                "implicit (last expression)"
              ],
              [
                "Early exit",
                "return x"
              ],
              [
                "Args",
                "required, default a=1, keyword name:, *args, **opts"
              ],
              [
                "Naming",
                "snake_case; ? predicate, ! dangerous"
              ]
            ]
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "items": [
              "Adding explicit `return` everywhere — Ruby returns the last expression.",
              "Using parens inconsistently (pick a style; Ruby allows both).",
              "Forgetting ? = predicate and ! = dangerous (naming convention)."
            ]
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "A method is def name(args) ... end; Ruby returns the last expression (implicit return).",
              "`return` exits early; without a value, returns nil.",
              "Args: required (a), default (a=1), keyword (name:), splat (*args), double-splat (**opts).",
              "Call with or without parens (puts 'hi' or puts('hi')).",
              "Naming: snake_case; ? = predicate (empty?), ! = dangerous (sort!)."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "A method is defined with def: def greet name; Hello name; end. Ruby returns the last expression by default (implicit return) — no need for return unless you exit early. def add a b; a plus b; end returns a plus b. Arguments: required (a), default (a 1), keyword (name), splat (star args collects extras into an array), double-splat (star star opts collects keyword args into a hash). Methods are called with or without parens (puts hi or puts hi). return exits immediately (return x if x dot nil); without a value, returns nil. Methods end with end. Method naming: snake_case; predicate methods end with ? (empty); dangerous ones with ! (save bang). Every call returns something (nil if nothing else). A common mistake is adding explicit return everywhere, when Ruby returns the last expression, and using parens inconsistently."
          }
        ]
      },
      "followup_questions": [
        "How do you define a method (def name(args) ... end)?",
        "What's implicit return (the last expression is returned; no `return` needed)?",
        "What argument kinds are there (required, default a=1, keyword name:, *args, **opts)?",
        "What's the ?/! naming (? predicate returns bool; ! dangerous/mutates)?"
      ],
      "seo": {
        "metaTitle": "Methods and Return in Ruby — Define, Call, Return",
        "metaDescription": "Methods: def name(args); ...; end. Implicit return (last expression). Args: required, default (a=1), keyword (name:), *args, **opts. snake_case; ? predicates, ! dangerous."
      },
      "order": 1
    }
  ]
}

{
  "topic": "Numbered Block Parameters",
  "topicSlug": "numbered-block-parameters",
  "questions": [
    {
      "id": "numbered-block-parameters-q001",
      "slug": "ruby-numbered-block-parameters-basics",
      "question": "What are numbered block parameters in Ruby?",
      "title": "Numbered Block Parameters",
      "direct_answer": "Numbered parameters (Ruby 2.7+) let you skip declaring block params: `[1,2,3].each { puts _1 }` — _1 is the first block argument, _2 the second, etc. No `|x|` needed. Useful for tiny blocks where a name adds no clarity: `words.map { _1.upcase }`, `sum = nums.reduce(0) { _1 + _2 }` (_1 is the accumulator, _2 the element). They're a shorthand — for readability or more than ~2 params, declare named params (|x, y|). _1 is the same as declaring |x|; mixing _1 and named params in the same block is an error. They work in any block (each/map/reduce/times). They're purely syntactic sugar — the block is still a closure. Use them for one-liners; named params for anything with logic or more args.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know numbered parameters (Ruby 2.7+) — _1, _2, ... in blocks without declaring params.",
        "common_mistake": "Mixing _1 and named params (error), or overusing them for complex blocks (hurts readability).",
        "to_stand_out": "Numbered block params (Ruby 2.7+): _1, _2 in blocks without |x|. Shorthand for tiny blocks. reduce: _1 + _2. Don't mix with named params."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Skip the Params",
            "content": "Numbered parameters (Ruby 2.7 plus) let you skip declaring block params: 1 2 3 dot each block puts _1 — _1 is the first block argument, _2 the second, etc. No pipe x pipe needed. Useful for tiny blocks where a name adds no clarity: words dot map block _1 dot upcase, sum nums reduce 0 block _1 plus _2 (_1 is the accumulator, _2 the element). They're a shorthand — for readability or more than ~2 params, declare named params (pipe x y pipe). _1 is the same as declaring pipe x pipe; mixing _1 and named params in the same block is an error. They work in any block (each/map/reduce/times). They're purely syntactic sugar — the block is still a closure. Use them for one-liners; named params for anything with logic or more args."
          },
          {
            "type": "flow_diagram",
            "title": "Numbered Params",
            "content": "_1, _2 in blocks (no |x|). Shorthand. reduce: _1 + _2. Don't mix with named params.\n\n```mermaid\nflowchart LR\n  B[block { puts _1 }] --> P[_1 = first arg]\n  R[reduce { _1 + _2 }] --> A[_1 acc, _2 elem]\n  M[mix _1 and |x|] --> E[error]\n```"
          },
          {
            "type": "code_example",
            "title": "Numbered block parameters",
            "content": "```ruby\n# Ruby 2.7+: numbered block parameters (_1, _2, ...)\n# No need to declare |x|:\n[1, 2, 3].each { puts _1 }       # 1 2 3\nwords = %w[hi bye]\nwords.map { _1.upcase }          # ['HI', 'BYE']\n\n# _1 = first arg, _2 = second, etc.\n%w[a b].each_with_index { puts \"#{_1} #{_2}\" }  # a 0 / b 1\n\n# In reduce: _1 is the accumulator, _2 the element\nsum = [1, 2, 3].reduce(0) { _1 + _2 }   # 6\n# (same as: reduce(0) { |acc, n| acc + n })\n\n# Equivalent to declaring params:\n[1,2,3].map { |x| x * 2 }   ==  [1,2,3].map { _1 * 2 }   # true\n\n# DON'T mix _1 and named params in the same block (error):\n# [1,2].map { |x| _1 }        # SyntaxError\n\n# Works in any block (each/map/reduce/times/select):\n3.times { puts _1 }            # 0 1 2\n[1,2,3].select { _1.even? }   # [2]\n\n# Use for ONE-LINERS; named params for logic or many args.\n# Purely syntactic sugar; the block is still a closure.\n```"
          },
          {
            "type": "comparison_table",
            "title": "Numbered Params",
            "columns": [
              "Feature",
              "How"
            ],
            "rows": [
              [
                "Syntax",
                "_1, _2, ... (no |x|)"
              ],
              [
                "_1",
                "first block arg"
              ],
              [
                "_2",
                "second block arg"
              ],
              [
                "reduce",
                "_1 = accumulator, _2 = element"
              ],
              [
                "Mix",
                "_1 + |x| = error"
              ]
            ]
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "items": [
              "Mixing _1 and named params in the same block (SyntaxError).",
              "Overusing them for complex blocks (hurts readability — use named params).",
              "Forgetting _1 in reduce is the accumulator (not the element)."
            ]
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "Numbered parameters (Ruby 2.7+): _1, _2 in blocks without declaring |x|.",
              "_1 is the first block arg, _2 the second; works in any block.",
              "In reduce, _1 is the accumulator, _2 the element.",
              "Don't mix _1 and named params in the same block (SyntaxError).",
              "Use for one-liners; named params for logic or more than ~2 args (readability)."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "Numbered parameters (Ruby 2.7 plus) let you skip declaring block params: 1 2 3 dot each block puts _1 — _1 is the first block argument, _2 the second, etc. No pipe x pipe needed. Useful for tiny blocks where a name adds no clarity: words dot map block _1 dot upcase, sum nums reduce 0 block _1 plus _2 (_1 is the accumulator, _2 the element). They're a shorthand — for readability or more than ~2 params, declare named params (pipe x y pipe). _1 is the same as declaring pipe x pipe; mixing _1 and named params in the same block is an error. They work in any block (each/map/reduce/times). They're purely syntactic sugar — the block is still a closure. Use them for one-liners; named params for anything with logic or more args. A common mistake is mixing _1 and named params, which is an error, and overusing them for complex blocks, which hurts readability."
          }
        ]
      },
      "followup_questions": [
        "What are numbered params (Ruby 2.7+; _1, _2 in blocks; no |x|)?",
        "What's _1 / _2 (first / second block arg)?",
        "How do they work in reduce (_1 = accumulator; _2 = element)?",
        "What's the gotcha (don't mix _1 and named params; use named params for logic)?"
      ],
      "seo": {
        "metaTitle": "Numbered Block Parameters — Skip the Params",
        "metaDescription": "Numbered block params (Ruby 2.7+): _1, _2 in blocks without |x|. Shorthand for tiny blocks. reduce: _1 + _2. Don't mix with named params."
      },
      "order": 1
    }
  ]
}

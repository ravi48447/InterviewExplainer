{
  "topic": "Endless Methods",
  "topicSlug": "endless-methods",
  "questions": [
    {
      "id": "endless-methods-q001",
      "slug": "ruby-endless-methods-basics",
      "question": "What are endless methods in Ruby?",
      "title": "Endless Methods in Ruby",
      "direct_answer": "Endless methods (Ruby 3.0+) let you write a one-line method without `end`: `def greet(name) = \"Hello, #{name}\"`. The `=` replaces the body delimiter; no end needed. They must be a single expression (no multi-line body). Useful for trivial getters/computed values: `def full_name = [first, last].join(' ')`; `def area = @w * @h`. You can't have a body with multiple statements — use a normal def for that. Endless methods still return the expression (implicit return). They reduce visual noise for tiny methods. Note: no arguments needs empty parens `def x() = 1` (or `def x = 1` is allowed in 3.0+). They're a readability feature — use them for one-liners, fall back to def/end for anything with logic.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know endless methods (Ruby 3.0+) use `=` instead of end and are for one-line expressions.",
        "common_mistake": "Using an endless method for multi-statement logic (they must be a single expression), or expecting `end`.",
        "to_stand_out": "Endless methods (Ruby 3.0+): `def f(x) = expr` — one-line, no end. For trivial getters/computed values. Must be a single expression."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "One-Line, No End",
            "content": "Endless methods (Ruby 3.0 plus) let you write a one-line method without end: def greet name equals Hello name. The equals replaces the body delimiter; no end needed. They must be a single expression (no multi-line body). Useful for trivial getters and computed values: def full name equals first last join space; def area equals w times h. You can't have a body with multiple statements — use a normal def for that. Endless methods still return the expression (implicit return). They reduce visual noise for tiny methods. Note: no arguments needs empty parens def x parens equals 1 (or def x equals 1 is allowed in 3.0 plus). They're a readability feature — use them for one-liners, fall back to def end for anything with logic."
          },
          {
            "type": "flow_diagram",
            "title": "Endless Methods",
            "content": "def f(x) = expr — one line, no end. Single expression. Implicit return.\n\n```mermaid\nflowchart LR\n  D[def greet name] --> E[= Hello, name]\n  E --> R[returns the expr]\n  L[logic? multi-statement?] --> F[use def/end instead]\n```"
          },
          {
            "type": "code_example",
            "title": "Endless methods",
            "content": "```ruby\n# Ruby 3.0+: endless methods (no `end`)\ndef greet(name) = \"Hello, #{name}\"\n\ngreet('Ada')          # 'Hello, Ada'\n\n# Single expression; IMPLICIT return (no return keyword needed)\ndef square(n) = n * n\nsquare(5)            # 25\n\n# Great for trivial getters / computed values:\nclass Rect\n  def initialize(w, h); @w = w; @h = h; end\n  def area = @w * @h            # endless getter\n  def perimeter = 2 * (@w + @h)\nend\nr = Rect.new(3, 4)\nr.area          # 12\nr.perimeter     # 14\n\n# No args: empty parens (or omit in 3.0+)\ndef x() = 1\ndef y = 2        # also valid\n\n# CAN'T have multiple statements — use a normal def:\n# def bad = a; b; c    # syntax error (use def/end)\n\n# Use for ONE-LINERS; fall back to def/end for logic.\n```"
          },
          {
            "type": "comparison_table",
            "title": "Endless Methods",
            "columns": [
              "Feature",
              "How"
            ],
            "rows": [
              [
                "Syntax",
                "def f(x) = expr"
              ],
              [
                "End",
                "none (no end)"
              ],
              [
                "Body",
                "single expression only"
              ],
              [
                "Return",
                "implicit (the expression)"
              ],
              [
                "Use for",
                "one-line getters/values"
              ]
            ]
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "items": [
              "Using an endless method for multi-statement logic (must be single expression).",
              "Expecting an `end` (there is none).",
              "Adding explicit `return` (not allowed; it's a single expression)."
            ]
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "Endless methods (Ruby 3.0+): `def f(x) = expr` — one line, no end.",
              "They must be a single expression; the expression is returned (implicit return).",
              "Great for trivial getters/computed values (def area = @w * @h).",
              "No args: `def x() = 1` (or `def x = 1` in 3.0+).",
              "Fall back to def/end for anything with multiple statements or logic."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "Endless methods (Ruby 3.0 plus) let you write a one-line method without end: def greet name equals Hello name. The equals replaces the body delimiter; no end needed. They must be a single expression (no multi-line body). Useful for trivial getters and computed values: def full name equals first last join space; def area equals w times h. You can't have a body with multiple statements — use a normal def for that. Endless methods still return the expression (implicit return). They reduce visual noise for tiny methods. Note: no arguments needs empty parens def x parens equals 1 (or def x equals 1 is allowed in 3.0 plus). They're a readability feature — use them for one-liners, fall back to def end for anything with logic. A common mistake is using an endless method for multi-statement logic, since they must be a single expression, and expecting an end, when there is none."
          }
        ]
      },
      "followup_questions": [
        "What's an endless method (Ruby 3.0+; def f(x) = expr; no end)?",
        "What's the constraint (single expression only; no multi-statement)?",
        "What's returned (the expression — implicit return)?",
        "When do you use them (one-liners; trivial getters; fall back to def/end for logic)?"
      ],
      "seo": {
        "metaTitle": "Endless Methods in Ruby — One-Line, No End",
        "metaDescription": "Endless methods (Ruby 3.0+): `def f(x) = expr` — one-line, no end. For trivial getters/computed values. Must be a single expression."
      },
      "order": 1
    }
  ]
}

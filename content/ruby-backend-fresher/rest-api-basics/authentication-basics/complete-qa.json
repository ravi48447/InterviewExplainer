{
  "topic": "Authentication Basics",
  "topicSlug": "authentication-basics",
  "questions": [
    {
      "id": "authentication-basics-q001",
      "slug": "rails-api-authentication-basics",
      "question": "How do you authenticate a Rails API?",
      "title": "Authenticating a Rails API",
      "direct_answer": "For an API (no sessions/cookies), the common patterns: token auth (the client sends a header Authorization: Bearer <token>; the server looks up the user by the token), and JWT (a signed token encoding the user id + expiry; the server verifies the signature). In Rails: a token is often a column on User (user.auth_token) or a separate API key table; a before_action checks it (authenticate_user! reads the header, sets @current_user). Devise + devise-jwt add JWT to the full-stack Devise flow. For stateless APIs, JWT/token avoids server sessions; for a first-party SPA, a secure cookie + same-site may be simpler. Always use HTTPS so the token isn't sniffed, and never log tokens.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know token/Bearer-header auth and JWT for stateless APIs, and a before_action to enforce it.",
        "common_mistake": "Sending tokens in the URL or body (use a header), or logging tokens (never).",
        "to_stand_out": "API auth: a Bearer token in the Authorization header (or JWT). A before_action reads it and sets @current_user. Use HTTPS; never log tokens."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Tokens for Stateless APIs",
            "content": "For an API (no sessions/cookies), the common patterns: token auth (the client sends a header Authorization Bearer token; the server looks up the user by the token), and JWT (a signed token encoding the user id and expiry; the server verifies the signature). In Rails: a token is often a column on User (user dot auth_token) or a separate API key table; a before_action checks it (authenticate_user! reads the header, sets current_user). Devise plus devise-jwt add JWT to the full-stack Devise flow. For stateless APIs, JWT or token avoids server sessions; for a first-party SPA, a secure cookie plus same-site may be simpler. Always use HTTPS so the token isn't sniffed, and never log tokens."
          },
          {
            "type": "flow_diagram",
            "title": "Request -> Token -> User",
            "content": "Client sends Authorization: Bearer <token>; before_action reads it and sets @current_user.\n\n```mermaid\nflowchart LR\n  C[client: Authorization: Bearer <token>] --> B[before_action: authenticate_user!]\n  B --> L[lookup user by token]\n  L --> U[set @current_user; allow or 401]\n```"
          },
          {
            "type": "code_example",
            "title": "API authentication",
            "content": "```ruby\n# Client sends a header:  Authorization: Bearer <token>\n\n# app/controllers/api_controller.rb\nmodule Api\n  class ApiController < ApplicationController\n    before_action :authenticate_user!   # protect all API controllers\n\n    private\n    def authenticate_user!\n      token = request.headers['Authorization'].to_s.sub('Bearer ', '')\n      @current_user = User.find_by(auth_token: token)\n      render json: { error: 'unauthorized' }, status: :unauthorized unless @current_user\n    end\n\n    def current_user; @current_user; end\n  end\nend\n\n# An API controller inheriting it is protected:\nmodule Api\n  class PostsController < ApiController\n    def index\n      render json: current_user.posts   # @current_user is set\n    end\n  end\nend\n\n# JWT alternative (signed token; devise-jwt or jwt gem):\n#   encode: JWT.encode({ user_id: u.id, exp: 1.hour.from_now.to_i }, SECRET)\n#   decode: JWT.decode(token, SECRET) -> verify signature + expiry\n# Always: HTTPS (token isn't sniffed); never log tokens.\n```"
          },
          {
            "type": "comparison_table",
            "title": "Auth Patterns",
            "columns": [
              "Pattern",
              "How"
            ],
            "rows": [
              [
                "Bearer token",
                "Authorization header + lookup"
              ],
              [
                "JWT",
                "signed token; verify signature/expiry"
              ],
              [
                "before_action",
                "authenticate_user! enforces it"
              ],
              [
                "Devise+JWT",
                "full-stack auth + JWT"
              ],
              [
                "HTTPS",
                "always (don't sniff tokens)"
              ]
            ]
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "items": [
              "Sending tokens in the URL or body — use the Authorization header.",
              "Logging tokens — never; they're credentials.",
              "Rolling your own crypto for JWT — use a vetted gem (jwt, devise-jwt)."
            ]
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "For an API, auth is a Bearer token in the Authorization header or a JWT.",
              "A before_action (authenticate_user!) reads the header and sets @current_user.",
              "A token is a column on User or a separate API key table; JWT encodes user id + expiry.",
              "Devise + devise-jwt add JWT to the full-stack Devise flow.",
              "Always use HTTPS (don't sniff tokens) and never log tokens."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "For an API (no sessions/cookies), the common patterns: token auth (the client sends a header Authorization Bearer token; the server looks up the user by the token), and JWT (a signed token encoding the user id and expiry; the server verifies the signature). In Rails: a token is often a column on User (user dot auth_token) or a separate API key table; a before_action checks it (authenticate_user! reads the header, sets current_user). Devise plus devise-jwt add JWT to the full-stack Devise flow. For stateless APIs, JWT or token avoids server sessions; for a first-party SPA, a secure cookie plus same-site may be simpler. Always use HTTPS so the token isn't sniffed, and never log tokens. A common mistake is sending tokens in the URL or body, when you should use a header, and logging tokens, which you should never do."
          }
        ]
      },
      "followup_questions": [
        "How does a client send a token (Authorization: Bearer <token> header)?",
        "How does the server enforce it (before_action authenticate_user! sets @current_user)?",
        "What's JWT (a signed token with user id + expiry; verify signature)?",
        "What security rules apply (always HTTPS; never log tokens)?"
      ],
      "seo": {
        "metaTitle": "Authenticating a Rails API — Tokens for Stateless APIs",
        "metaDescription": "API auth: a Bearer token in the Authorization header (or JWT). A before_action reads it and sets @current_user. Use HTTPS; never log tokens."
      },
      "order": 1
    }
  ]
}

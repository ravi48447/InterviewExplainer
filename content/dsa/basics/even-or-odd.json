{
  "id": "even-or-odd",
  "slug": "even-or-odd",
  "title": "Even or Odd",
  "difficulty": "easy",
  "category": "basics",
  "patterns": [
    "modulo"
  ],
  "companies": [],
  "frequency": "medium",
  "readingTimeMinutes": 6,
  "lastUpdated": "2026-06-01",
  "directAnswer": "Check `n % 2`. If it's 0 the number is even, otherwise odd. For negatives prefer `n % 2 == 0` (true) rather than `== 1`, since `-3 % 2` is `-1` in Java/Python.",
  "remember": {
    "pattern": "Parity via modulo / AND",
    "rules": [
      "Even ⟺ `n % 2 == 0`.",
      "Never test `== 1` for odd.",
      "`n & 1` is the safe, fast parity check."
    ],
    "takeaway": "Always phrase parity as 'remainder is zero', never 'remainder is one'.",
    "formula": "even  <=>  (n % 2 == 0)  <=>  ((n & 1) == 0)",
    "whenToUse": [
      "Classifying integers by parity",
      "Toggling/alternating behaviour every other step"
    ],
    "antiSignals": [
      "You wrote `% 2 == 1` to mean odd — fix it for negatives"
    ]
  },
  "problemStatement": "Given an integer `n`, return `\"Even\"` if it is even and `\"Odd\"` if it is odd.",
  "understanding": "A number is even when it leaves no remainder on division by 2. The remainder operator `%` answers this in one step.",
  "constraints": [
    "-10^9 ≤ n ≤ 10^9"
  ],
  "examples": [
    {
      "input": "n = 4",
      "output": "\"Even\"",
      "explanation": "4 % 2 == 0."
    },
    {
      "input": "n = 7",
      "output": "\"Odd\"",
      "explanation": "7 % 2 == 1."
    },
    {
      "input": "n = -3",
      "output": "\"Odd\"",
      "explanation": "Sign doesn't matter for parity."
    }
  ],
  "approaches": [
    {
      "name": "Modulo by 2",
      "whenToMention": "This is the standard, expected solution — lead with it.",
      "complexity": {
        "time": "O(1)",
        "space": "O(1)"
      },
      "explanation": "A number is even when it leaves no remainder on division by 2. The modulo operator `%` answers this directly, so `n % 2 == 0` is the test for evenness.\n\n**Why you must test `== 0`, never `== 1`.** In Java, C, and C++ the `%` operator follows the sign of the *dividend*, so `-3 % 2` is `-1`, not `1`. Code that checks `n % 2 == 1` to detect odd numbers therefore returns the wrong answer for every negative odd value. Always phrase parity as 'the remainder is zero' for even, and treat everything else as odd.\n\n**The bitwise alternative.** The lowest bit of an integer is `1` exactly when the number is odd, so `(n & 1) == 0` tests evenness too. It sidesteps the negative-remainder pitfall entirely (the low bit of `-3` is `1`) and is a touch faster. Mentioning it is a nice signal.\n\n**Python note.** Python's `%` follows the sign of the *divisor*, so `-3 % 2` is `1` there — but the safe habit of testing `== 0` still works everywhere, which is exactly why you should default to it.",
      "code": {
        "java": "public String evenOrOdd(int n) {\n    return (n & 1) == 0 ? \"Even\" : \"Odd\";\n}",
        "python": "def even_or_odd(n: int) -> str:\n    return \"Even\" if n % 2 == 0 else \"Odd\""
      },
      "hints": [
        "Even means 'divisible by 2'. What operator tells you the remainder?",
        "Test `n % 2 == 0` for even — and think about what `-3 % 2` returns in Java.",
        "The bitwise check `(n & 1) == 0` avoids the negative-remainder trap entirely."
      ],
      "insight": "The bitwise alternative `(n & 1) == 0` avoids the negative-remainder pitfall entirely and is a nice thing to mention.",
      "complexityReasoning": "one modulo (or one bitwise AND) — constant work.",
      "dryRun": {
        "input": "n = -3",
        "intro": "Why the test must be `== 0`, not `== 1`.",
        "steps": [
          {
            "step": "n % 2 in Java",
            "action": "-3 % 2 = -1 (sign follows dividend)",
            "state": "not 0 → Odd ✓"
          },
          {
            "step": "n % 2 == 1?",
            "action": "-1 == 1 is false",
            "state": "would wrongly say Even ✗"
          },
          {
            "step": "(n & 1)",
            "action": "low bit of -3 is 1",
            "state": "→ Odd ✓"
          }
        ],
        "result": "\"Odd\""
      },
      "edgeCases": [
        {
          "input": "n = 0",
          "behavior": "0 % 2 == 0 → Even, the correct parity of zero."
        },
        {
          "input": "n = -4",
          "behavior": "Even; `% 2` is 0 for negatives too when you test against 0."
        },
        {
          "input": "n = -7",
          "behavior": "Odd; the bitwise check or the `!= 0` test handles it where `== 1` fails."
        }
      ],
      "pitfalls": [
        "Testing `n % 2 == 1` for odd — wrong for negative odd numbers in Java/C++.",
        "Confusing `=` (assignment) with `==` (comparison) inside the condition."
      ]
    }
  ],
  "commonMistakes": [
    "Testing `n % 2 == 1` — fails for negative odd numbers.",
    "Confusing `=` (assignment) with `==` (comparison)."
  ],
  "followupVariations": [
    {
      "title": "Count Even and Odd",
      "slug": "count-even-odd-array",
      "hint": "Apply this parity test across an array and tally each bucket."
    },
    {
      "title": "Sign of a Number",
      "slug": "sign-of-a-number",
      "hint": "Another 'classify one integer' branch problem."
    }
  ],
  "patternNote": "A core fresher pattern: Parity via modulo / AND. Master it on small inputs and it transfers to harder problems.",
  "seo": {
    "metaTitle": "Even or Odd — Solution in Java & Python | InterviewExplainer",
    "metaDescription": "Check `n % 2`. If it's 0 the number is even, otherwise odd. For negatives prefer `n % 2 == 0` (true) rather than `== 1`, since `-3 % 2` is `-1` in Jav",
    "canonicalUrl": "/dsa/problem/even-or-odd"
  },
  "interviewerIntent": {
    "testing": "Whether you know that `%` can be negative and choose a parity test that survives negatives.",
    "commonMistake": "Detecting odd with `n % 2 == 1`, which fails on negative inputs.",
    "toStandOut": "Offer `(n & 1)` as the language-agnostic, faster check and explain the signed-modulo subtlety."
  },
  "clarifyingQuestions": [
    {
      "question": "Can the input be negative?",
      "answer": "Yes — which is exactly why I test `% 2 == 0` rather than `== 1`."
    },
    {
      "question": "Is zero even or odd?",
      "answer": "Even: 0 % 2 == 0."
    }
  ],
  "commonMistakesDetailed": [
    {
      "title": "Detecting odd with `== 1`",
      "why": "In Java/C++ `%` follows the dividend's sign, so `-3 % 2` is `-1`; `-1 == 1` is false and the number is misclassified.",
      "lang": "java",
      "bad": "boolean odd = (n % 2 == 1);     // false for -3, -5, ...",
      "good": "boolean odd = (n % 2 != 0);     // or (n & 1) == 1"
    }
  ]
}

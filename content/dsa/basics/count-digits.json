{
  "id": "count-digits",
  "slug": "count-digits",
  "title": "Count Digits",
  "difficulty": "easy",
  "category": "basics",
  "patterns": [
    "digit-dp"
  ],
  "companies": [],
  "frequency": "medium",
  "readingTimeMinutes": 6,
  "lastUpdated": "2026-06-01",
  "directAnswer": "Work with `abs(n)`. If it's 0 return 1. Otherwise divide by 10 until it hits 0, counting each division.",
  "remember": {
    "pattern": "Strip digits with /10",
    "rules": [
      "Use abs(n).",
      "Special-case 0 → 1.",
      "Each /10 removes one digit."
    ],
    "takeaway": "The divide-by-10 loop is the backbone of every digit problem.",
    "formula": "count = 0; while n>0: n//=10; count++   (handle 0 -> 1)",
    "whenToUse": [
      "Anything that processes a number digit by digit"
    ]
  },
  "problemStatement": "Given an integer `n`, return how many digits it has. Treat the count of `0` as 1, and ignore the sign of negatives.",
  "understanding": "Repeatedly divide by 10, counting steps until the number becomes 0. Handle 0 and negatives explicitly.",
  "constraints": [
    "-10^9 ≤ n ≤ 10^9"
  ],
  "examples": [
    {
      "input": "n = 12345",
      "output": "5",
      "explanation": "Five digits."
    },
    {
      "input": "n = 0",
      "output": "1",
      "explanation": "Zero has one digit."
    },
    {
      "input": "n = -42",
      "output": "2",
      "explanation": "Sign ignored."
    }
  ],
  "approaches": [
    {
      "name": "Divide by 10",
      "whenToMention": "This is the standard, expected solution — lead with it.",
      "complexity": {
        "time": "O(d) where d = number of digits",
        "space": "O(1)"
      },
      "explanation": "The digit count of a number is how many times you can integer-divide it by 10 before it reaches 0. Each `/= 10` chops the last digit; count the chops.\n\n**Why `% 10` and `/ 10` are the digit toolkit.** `n % 10` reads the last digit; `n / 10` removes it. Every digit problem — count, sum, reverse, palindrome — is built from this pair. Here we only need the removal half, counting iterations.\n\n**Why zero needs a special return.** The loop condition `n > 0` is false immediately for n = 0, so it would count 0 digits — but '0' has one digit. Return 1 up front for that input.\n\n**Why take the absolute value.** A negative like -42 has 2 digits; the sign isn't a digit. Working with `abs(n)` makes the loop behave for negatives (and avoids `n > 0` terminating instantly on a negative).\n\n**The `log10` alternative.** `(int) Math.log10(abs(n)) + 1` is O(1), but it's vulnerable to floating-point rounding on exact powers of ten — the divide loop is the robust answer.",
      "code": {
        "java": "public int countDigits(int n) {\n    n = Math.abs(n);\n    if (n == 0) return 1;\n    int count = 0;\n    while (n > 0) {\n        n /= 10;\n        count++;\n    }\n    return count;\n}",
        "python": "def count_digits(n: int) -> int:\n    n = abs(n)\n    if n == 0:\n        return 1\n    count = 0\n    while n > 0:\n        n //= 10\n        count += 1\n    return count"
      },
      "hints": [
        "How many times can you divide the number by 10 before it becomes 0?",
        "Use `% 10`/`/ 10` — here you only need the `/ 10` to strip digits and count.",
        "Handle 0 (one digit) and negatives (take abs) explicitly."
      ],
      "insight": "A slick alternative is `(int) Math.log10(abs(n)) + 1`, but the divide loop is safer and avoids floating-point edge cases.",
      "complexityReasoning": "one division per digit, so it's proportional to the number of digits d ≈ log10(n).",
      "lineByLine": {
        "java": [
          {
            "line": "if (n == 0) return 1;",
            "explanation": "The loop can't count zero, so handle it directly."
          },
          {
            "line": "n /= 10;",
            "explanation": "Strip the last digit each iteration."
          }
        ]
      },
      "dryRun": {
        "input": "n = 12345",
        "intro": "Strip a digit each step; count the strips.",
        "steps": [
          {
            "step": "12345 / 10",
            "action": "= 1234",
            "state": "count = 1"
          },
          {
            "step": "1234 / 10",
            "action": "= 123",
            "state": "count = 2"
          },
          {
            "step": "123 / 10",
            "action": "= 12",
            "state": "count = 3"
          },
          {
            "step": "12 / 10",
            "action": "= 1",
            "state": "count = 4"
          },
          {
            "step": "1 / 10",
            "action": "= 0, loop ends",
            "state": "count = 5"
          }
        ],
        "result": "5"
      },
      "edgeCases": [
        {
          "input": "n = 0",
          "behavior": "Loop never runs; the up-front `return 1` gives the right answer."
        },
        {
          "input": "n = -42",
          "behavior": "abs makes it 42 → 2 digits; sign ignored."
        },
        {
          "input": "n = 9",
          "behavior": "Single division to 0 → 1 digit."
        }
      ],
      "pitfalls": [
        "Returning 0 for input 0 instead of 1.",
        "Forgetting `abs`, so the `n > 0` loop ends immediately on a negative."
      ]
    }
  ],
  "commonMistakes": [
    "Returning 0 for input 0.",
    "Forgetting to take the absolute value, so the loop misbehaves on negatives."
  ],
  "followupVariations": [
    {
      "title": "Sum of Digits",
      "slug": "sum-of-digits",
      "hint": "Same loop, but accumulate `% 10` instead of counting."
    },
    {
      "title": "Reverse a Number",
      "slug": "reverse-a-number",
      "hint": "Same loop, but rebuild the number from the stripped digits."
    }
  ],
  "patternNote": "A core fresher pattern: Strip digits with /10. Master it on small inputs and it transfers to harder problems.",
  "seo": {
    "metaTitle": "Count Digits — Solution in Java & Python | InterviewExplainer",
    "metaDescription": "Work with `abs(n)`. If it's 0 return 1. Otherwise divide by 10 until it hits 0, counting each division.",
    "canonicalUrl": "/dsa/problem/count-digits"
  },
  "interviewerIntent": {
    "testing": "Whether you know the divide-by-10 digit idiom and handle the 0 / negative boundaries.",
    "commonMistake": "Off-by-one on zero, or a loop that never runs for negatives.",
    "toStandOut": "Mention the log10 O(1) trick and why the loop is safer (no float rounding)."
  },
  "clarifyingQuestions": [
    {
      "question": "Does the sign count?",
      "answer": "No — I take the absolute value; -42 has 2 digits."
    },
    {
      "question": "How is 0 handled?",
      "answer": "As one digit, special-cased before the loop."
    }
  ]
}

{
  "id": "number-triangle",
  "slug": "number-triangle",
  "title": "Number Triangle",
  "difficulty": "easy",
  "category": "basics",
  "patterns": [
    "nested-loops"
  ],
  "companies": [],
  "frequency": "medium",
  "readingTimeMinutes": 6,
  "lastUpdated": "2026-06-01",
  "directAnswer": "Outer row 1..n; inner loop appends j for j = 1..i; newline between rows.",
  "remember": {
    "pattern": "Nested numeric loop",
    "rules": [
      "Outer = rows.",
      "Inner prints 1..i.",
      "Newlines between rows."
    ],
    "takeaway": "Swap the star for the loop variable to print numbers.",
    "formula": "for i in 1..n: for j in 1..i: print(j)",
    "whenToUse": [
      "Index-valued patterns"
    ]
  },
  "problemStatement": "Given `n`, return a triangle where row `i` contains the numbers 1..i, e.g. row 3 is `123`. Rows separated by newlines.",
  "understanding": "Nested loop where the inner loop prints 1..i.",
  "constraints": [
    "1 ≤ n ≤ 9"
  ],
  "examples": [
    {
      "input": "n = 3",
      "output": "\"1\\n12\\n123\"",
      "explanation": "Increasing per row."
    },
    {
      "input": "n = 1",
      "output": "\"1\"",
      "explanation": "Single."
    },
    {
      "input": "n = 4",
      "output": "\"1\\n12\\n123\\n1234\"",
      "explanation": "Four rows."
    }
  ],
  "approaches": [
    {
      "name": "Nested numeric loop",
      "whenToMention": "This is the standard, expected solution — lead with it.",
      "complexity": {
        "time": "O(n²)",
        "space": "O(n²)"
      },
      "explanation": "Same nested structure as the star triangle, but the inner loop emits the running number `j` (1..i) instead of a fixed star — so row 3 is `123`.\n\n**The one change from the star version.** Replace the constant `'*'` with the inner loop variable `j`. Everything else — outer row loop, inner bound i, newlines between rows — is identical. Recognising that a pattern problem is 'the same skeleton with a different cell value' is the transferable insight.\n\n**Why n ≤ 9.** Capping n at 9 keeps each printed number a single digit, so the columns line up; beyond 9 the multi-digit numbers would misalign.\n\n**Print j, not i.** Emitting the outer index i (a fixed value per row) instead of the inner j is the common slip.",
      "code": {
        "java": "public String numberTriangle(int n) {\n    StringBuilder sb = new StringBuilder();\n    for (int i = 1; i <= n; i++) {\n        for (int j = 1; j <= i; j++) sb.append(j);\n        if (i < n) sb.append('\\n');\n    }\n    return sb.toString();\n}",
        "python": "def number_triangle(n: int) -> str:\n    return '\\n'.join(''.join(str(j) for j in range(1, i + 1)) for i in range(1, n + 1))"
      },
      "hints": [
        "Start from the star triangle — what single thing changes?",
        "Print the inner loop variable j, not a star.",
        "Why is n capped at 9?"
      ],
      "complexityReasoning": "row i emits i digits; total is O(n²).",
      "dryRun": {
        "input": "n = 3",
        "intro": "Inner loop prints 1..i.",
        "steps": [
          {
            "step": "row 1",
            "action": "1",
            "state": "\"1\""
          },
          {
            "step": "row 2",
            "action": "1,2",
            "state": "\"1\\n12\""
          },
          {
            "step": "row 3",
            "action": "1,2,3",
            "state": "\"1\\n12\\n123\""
          }
        ],
        "result": "\"1\\n12\\n123\""
      },
      "edgeCases": [
        {
          "input": "n = 1",
          "behavior": "\"1\"."
        },
        {
          "input": "n = 4",
          "behavior": "\"1\\n12\\n123\\n1234\"."
        }
      ],
      "pitfalls": [
        "Printing i instead of the running j.",
        "Off-by-one in the inner bound."
      ]
    }
  ],
  "commonMistakes": [
    "Printing i instead of the running j.",
    "Off-by-one in the inner bound."
  ],
  "followupVariations": [
    {
      "title": "Right Triangle Star",
      "slug": "right-triangle-star",
      "hint": "The star version of the same skeleton."
    }
  ],
  "patternNote": "A core fresher pattern: Nested numeric loop. Master it on small inputs and it transfers to harder problems.",
  "seo": {
    "metaTitle": "Number Triangle — Solution in Java & Python | InterviewExplainer",
    "metaDescription": "Outer row 1..n; inner loop appends j for j = 1..i; newline between rows.",
    "canonicalUrl": "/dsa/problem/number-triangle"
  },
  "interviewerIntent": {
    "testing": "Whether you reuse the nested-loop skeleton with a different cell value.",
    "commonMistake": "Emitting the row index instead of the column index.",
    "toStandOut": "Articulate that pattern problems share one skeleton."
  },
  "clarifyingQuestions": [
    {
      "question": "Single-digit assumption?",
      "answer": "Yes, n ≤ 9 keeps columns aligned."
    }
  ]
}

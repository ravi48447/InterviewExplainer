{
  "id": "swap-without-temp",
  "slug": "swap-without-temp",
  "title": "Swap Without a Temp",
  "difficulty": "easy",
  "category": "basics",
  "patterns": [
    "bit-manipulation"
  ],
  "companies": [],
  "frequency": "medium",
  "readingTimeMinutes": 6,
  "lastUpdated": "2026-06-01",
  "directAnswer": "XOR swap: `a ^= b; b ^= a; a ^= b;`. Or arithmetic `a = a+b; b = a-b; a = a-b;` (risks overflow). In real code prefer a temp or tuple swap.",
  "remember": {
    "pattern": "XOR / arithmetic swap",
    "rules": [
      "XOR is self-inverse.",
      "Three XORs swap.",
      "Prefer a temp in real code."
    ],
    "takeaway": "XOR swap is a neat trick, but a temp variable is clearer.",
    "formula": "a^=b; b^=a; a^=b   (prefer: a,b = b,a)",
    "whenToUse": [
      "In-place swap puzzles"
    ],
    "antiSignals": [
      "Same memory location — XOR-swap zeroes it"
    ]
  },
  "problemStatement": "Given two integers `a` and `b`, swap their values without using a third (temporary) variable. Return them swapped as `[a, b]`.",
  "understanding": "XOR swap or arithmetic swap exchanges values in place; in practice a tuple/parallel assignment is clearest.",
  "constraints": [
    "-10^9 ≤ a, b ≤ 10^9"
  ],
  "examples": [
    {
      "input": "a = 3, b = 7",
      "output": "[7, 3]",
      "explanation": "Swapped."
    },
    {
      "input": "a = 0, b = 5",
      "output": "[5, 0]",
      "explanation": "Swapped with zero."
    },
    {
      "input": "a = 4, b = 4",
      "output": "[4, 4]",
      "explanation": "Equal stays equal."
    }
  ],
  "approaches": [
    {
      "name": "XOR swap",
      "whenToMention": "This is the standard, expected solution — lead with it.",
      "complexity": {
        "time": "O(1)",
        "space": "O(1)"
      },
      "explanation": "Swap two values without a third variable. The puzzle answers are the **XOR swap** (`a^=b; b^=a; a^=b`) and the arithmetic swap (add/subtract) — but the honest engineering answer is to just use a temp or tuple swap.\n\n**Why XOR swap works.** XOR is its own inverse (x ^ x = 0) and commutative. The three chained XORs algebraically exchange the values: after `a ^= b`, a holds a⊕b; then `b ^= a` makes b = b⊕(a⊕b) = a; then `a ^= b` makes a = (a⊕b)⊕a = b.\n\n**The fatal aliasing bug.** If the two operands are the *same* memory location (e.g. `swap(arr[i], arr[i])` with i == j), XOR-swap zeroes the value (x ^ x = 0). A temp swap is immune. The arithmetic version can also overflow.\n\n**The senior signal.** State that in real code clarity wins: `a, b = b, a` (or a temp) is preferred unless you're in a genuinely memory-constrained context. Knowing the trick *and* when not to use it is the point.",
      "code": {
        "java": "public int[] swap(int a, int b) {\n    a ^= b;\n    b ^= a;\n    a ^= b;\n    return new int[]{a, b};\n}",
        "python": "def swap(a: int, b: int) -> list[int]:\n    a, b = b, a\n    return [a, b]"
      },
      "hints": [
        "XOR is its own inverse — can three XORs exchange two values?",
        "What breaks if both operands are the same memory cell?",
        "What would you actually ship in production?"
      ],
      "insight": "The honest senior answer: clarity beats cleverness; a temp variable or tuple swap is preferred unless memory is truly constrained.",
      "complexityReasoning": "a fixed number of operations regardless of input — constant time.",
      "lineByLine": {
        "java": [
          {
            "line": "a ^= b; b ^= a; a ^= b;",
            "explanation": "Three XORs exchange the values in place."
          }
        ]
      },
      "dryRun": {
        "input": "a = 3, b = 7",
        "intro": "Three chained XORs.",
        "steps": [
          {
            "step": "a ^= b",
            "action": "a = 3^7",
            "state": "a=4(=011^111)"
          },
          {
            "step": "b ^= a",
            "action": "b = 7^(3^7)=3",
            "state": "b=3"
          },
          {
            "step": "a ^= b",
            "action": "a = (3^7)^3=7",
            "state": "a=7"
          }
        ],
        "result": "[7, 3]"
      },
      "edgeCases": [
        {
          "input": "a = 0, b = 5",
          "behavior": "Swaps to [5, 0]."
        },
        {
          "input": "a = 4, b = 4",
          "behavior": "Equal values stay [4, 4]."
        },
        {
          "input": "same memory cell",
          "behavior": "XOR-swap would zero it — use a temp."
        }
      ],
      "pitfalls": [
        "XOR-swapping a variable with itself (same memory) zeroes it.",
        "Arithmetic swap overflowing for large values."
      ]
    }
  ],
  "commonMistakes": [
    "XOR-swapping a variable with itself (same memory) zeroes it.",
    "Arithmetic swap overflowing for large values."
  ],
  "followupVariations": [
    {
      "title": "Reverse an Array",
      "slug": "reverse-array",
      "hint": "Swapping pairs (use a temp) in place."
    }
  ],
  "patternNote": "A core fresher pattern: XOR / arithmetic swap. Master it on small inputs and it transfers to harder problems.",
  "seo": {
    "metaTitle": "Swap Without a Temp — Solution in Java & Python | InterviewExplainer",
    "metaDescription": "XOR swap: `a ^= b; b ^= a; a ^= b;`. Or arithmetic `a = a+b; b = a-b; a = a-b;` (risks overflow). In real code prefer a temp or tuple swap.",
    "canonicalUrl": "/dsa/problem/swap-without-temp"
  },
  "interviewerIntent": {
    "testing": "Whether you know the trick AND its pitfalls/when to avoid it.",
    "commonMistake": "Ignoring the same-location aliasing bug or overflow.",
    "toStandOut": "Recommend a temp/tuple swap for clarity; reserve XOR for constrained contexts."
  },
  "clarifyingQuestions": [
    {
      "question": "Is a temp truly disallowed?",
      "answer": "If so I'll XOR-swap and guard against same-location aliasing."
    }
  ]
}

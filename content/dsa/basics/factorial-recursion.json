{
  "id": "factorial-recursion",
  "slug": "factorial-recursion",
  "title": "Factorial (Recursion)",
  "difficulty": "easy",
  "category": "basics",
  "patterns": [
    "recursion"
  ],
  "companies": [],
  "frequency": "high",
  "readingTimeMinutes": 6,
  "lastUpdated": "2026-06-01",
  "directAnswer": "Base case: `n <= 1` returns 1. Recursive case: return `n * factorial(n-1)`. Use `long` since factorials grow fast.",
  "remember": {
    "pattern": "Base case + shrink",
    "rules": [
      "0!/1! = 1.",
      "n! = n·(n-1)!.",
      "Use long."
    ],
    "takeaway": "Every recursion = a base case plus a smaller subproblem.",
    "formula": "f(n) = n<=1 ? 1 : n * f(n-1)",
    "whenToUse": [
      "Teaching recursion",
      "Naturally self-similar products"
    ],
    "antiSignals": [
      "Large n — recursion risks stack overflow; iterate"
    ]
  },
  "problemStatement": "Given a non-negative integer `n`, compute `n!` (n factorial) recursively.",
  "understanding": "Define factorial in terms of a smaller factorial: n! = n · (n-1)!, with 0! = 1 as the base case.",
  "constraints": [
    "0 ≤ n ≤ 20 (fits in a 64-bit long)"
  ],
  "examples": [
    {
      "input": "n = 5",
      "output": "120",
      "explanation": "5·4·3·2·1."
    },
    {
      "input": "n = 0",
      "output": "1",
      "explanation": "Base case."
    },
    {
      "input": "n = 1",
      "output": "1",
      "explanation": "1! = 1."
    }
  ],
  "approaches": [
    {
      "name": "Linear recursion",
      "whenToMention": "This is the standard, expected solution — lead with it.",
      "complexity": {
        "time": "O(n)",
        "space": "O(n) call stack"
      },
      "explanation": "The textbook first recursion: n! = n · (n−1)!, with 0! = 1 (and 1! = 1) as the base case that stops the descent. Every recursion needs exactly these two pieces — a **base case** and a **recursive case that shrinks the problem**.\n\n**Why the base case is non-negotiable.** Without `if (n <= 1) return 1`, the calls descend forever (n, n−1, …, into negatives), overflowing the call stack. The base case is what guarantees termination.\n\n**How the call stack unwinds.** factorial(5) waits for factorial(4), which waits for factorial(3), … down to factorial(1)=1; then the stack unwinds multiplying back up: 1→2→6→24→120. Each frame uses stack space, so this is O(n) stack — iteration avoids that.\n\n**Overflow.** Use `long`; 20! is the largest factorial that fits in signed 64-bit, and 13! already overflows int.",
      "code": {
        "java": "public long factorial(int n) {\n    if (n <= 1) return 1;\n    return (long) n * factorial(n - 1);\n}",
        "python": "def factorial(n: int) -> int:\n    if n <= 1:\n        return 1\n    return n * factorial(n - 1)"
      },
      "hints": [
        "Express n! in terms of a smaller factorial.",
        "What input stops the recursion?",
        "Each call adds a stack frame — what's the space cost?"
      ],
      "insight": "20! is the largest factorial fitting in a signed 64-bit long; beyond that you need BigInteger.",
      "complexityReasoning": "n nested calls, one multiply each, with an O(n)-deep call stack.",
      "lineByLine": {
        "java": [
          {
            "line": "if (n <= 1) return 1;",
            "explanation": "Base case stops the recursion."
          },
          {
            "line": "return (long) n * factorial(n - 1);",
            "explanation": "Recursive case shrinks n by one."
          }
        ]
      },
      "dryRun": {
        "input": "n = 4",
        "intro": "Descend to the base, then unwind.",
        "steps": [
          {
            "step": "call",
            "action": "4·f(3)·…·f(1)",
            "state": "stack builds"
          },
          {
            "step": "base",
            "action": "f(1)=1",
            "state": "unwind starts"
          },
          {
            "step": "unwind",
            "action": "1→2→6→24",
            "state": "multiply back up"
          }
        ],
        "result": "24"
      },
      "edgeCases": [
        {
          "input": "n = 0",
          "behavior": "Base case returns 1 (0! = 1)."
        },
        {
          "input": "n = 1",
          "behavior": "Base case returns 1."
        },
        {
          "input": "n = 21",
          "behavior": "Overflows even long."
        }
      ],
      "pitfalls": [
        "Missing base case → infinite recursion / stack overflow.",
        "Using int and overflowing past 12!."
      ]
    }
  ],
  "commonMistakes": [
    "Missing base case → infinite recursion / stack overflow.",
    "Using int and overflowing past 12!."
  ],
  "followupVariations": [
    {
      "title": "Factorial of a Number",
      "slug": "factorial",
      "hint": "The iterative version, no stack cost."
    },
    {
      "title": "Sum 1 to N (Recursion)",
      "slug": "sum-n-recursion",
      "hint": "Same base-plus-shrink shape."
    }
  ],
  "patternNote": "A core fresher pattern: Base case + shrink. Master it on small inputs and it transfers to harder problems.",
  "seo": {
    "metaTitle": "Factorial (Recursion) — Solution in Java & Python | InterviewExplainer",
    "metaDescription": "Base case: `n <= 1` returns 1. Recursive case: return `n * factorial(n-1)`. Use `long` since factorials grow fast.",
    "canonicalUrl": "/dsa/problem/factorial-recursion"
  },
  "interviewerIntent": {
    "testing": "Whether you structure a recursion with a correct base case and shrinking step.",
    "commonMistake": "Omitting or mis-placing the base case; int overflow.",
    "toStandOut": "Note iteration avoids the O(n) stack, and quote the long overflow limit (20!)."
  },
  "clarifyingQuestions": [
    {
      "question": "Recursion required, or is iteration fine?",
      "answer": "I'll show recursion as asked; iteration avoids stack depth."
    }
  ],
  "commonMistakesDetailed": [
    {
      "title": "No base case",
      "why": "Without a stopping condition the function recurses past 0 into negatives until the stack overflows.",
      "lang": "java",
      "bad": "long f(int n){ return n * f(n-1); }       // never stops",
      "good": "long f(int n){ if (n <= 1) return 1; return n * f(n-1); }"
    }
  ]
}

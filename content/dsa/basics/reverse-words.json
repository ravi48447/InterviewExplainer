{
  "id": "reverse-words",
  "slug": "reverse-words",
  "title": "Reverse Words in a Sentence",
  "difficulty": "easy",
  "category": "basics",
  "patterns": [
    "string"
  ],
  "companies": [],
  "frequency": "medium",
  "readingTimeMinutes": 6,
  "lastUpdated": "2026-06-01",
  "directAnswer": "Split on whitespace (dropping empties), reverse the array of words, and join with a single space.",
  "remember": {
    "pattern": "Tokenise → reverse → join",
    "rules": [
      "Split on \\s+.",
      "Reverse the word list.",
      "Join with single spaces."
    ],
    "takeaway": "Word-level operations start by tokenising into words.",
    "formula": "' '.join(s.split()[::-1])",
    "whenToUse": [
      "Word-order manipulation"
    ]
  },
  "problemStatement": "Given a sentence `s`, return it with the order of the words reversed (single spaces between words, no leading/trailing spaces in the output).",
  "understanding": "Split into words, reverse the list, and join with single spaces.",
  "constraints": [
    "1 ≤ s.length ≤ 10^4"
  ],
  "examples": [
    {
      "input": "s = \"the quick fox\"",
      "output": "\"fox quick the\"",
      "explanation": "Word order reversed."
    },
    {
      "input": "s = \"hello\"",
      "output": "\"hello\"",
      "explanation": "One word."
    },
    {
      "input": "s = \"a b c\"",
      "output": "\"c b a\"",
      "explanation": "Reversed."
    }
  ],
  "approaches": [
    {
      "name": "Split, reverse, join",
      "whenToMention": "This is the standard, expected solution — lead with it.",
      "complexity": {
        "time": "O(n)",
        "space": "O(n)"
      },
      "explanation": "Reverse the *order of words* (not characters): tokenise on whitespace, reverse the list of words, and join with single spaces.\n\n**Why tokenise → reverse → join.** Splitting on runs of whitespace turns the sentence into clean word tokens (dropping extra spaces); reversing the token list flips word order; joining with one space normalises the output spacing. Each step is simple and composes cleanly.\n\n**Why `trim()` + split on `\\s+`.** Leading/trailing spaces would otherwise create empty tokens; trimming and splitting on whitespace runs avoids them so the output has no stray spaces.\n\n**Don't reverse characters.** Reversing the characters of the whole string would also reverse each word's spelling — a different (wrong) result. The unit of reversal here is the word.",
      "code": {
        "java": "public String reverseWords(String s) {\n    String[] words = s.trim().split(\"\\\\s+\");\n    StringBuilder sb = new StringBuilder();\n    for (int i = words.length - 1; i >= 0; i--) {\n        sb.append(words[i]);\n        if (i > 0) sb.append(' ');\n    }\n    return sb.toString();\n}",
        "python": "def reverse_words(s: str) -> str:\n    return ' '.join(s.split()[::-1])"
      },
      "hints": [
        "Reverse words, not characters — what's the unit you operate on?",
        "Split on whitespace, reverse the list, join with single spaces.",
        "Trim and split on \\s+ so extra spaces don't make empty tokens."
      ],
      "complexityReasoning": "tokenising, reversing, and joining are each linear in the input length.",
      "lineByLine": {
        "java": [
          {
            "line": "s.trim().split(\"\\\\s+\")",
            "explanation": "Tokenise on runs of whitespace, ignoring extra spaces."
          },
          {
            "line": "for (i = n-1; i >= 0; i--)",
            "explanation": "Emit words in reverse order."
          }
        ]
      },
      "dryRun": {
        "input": "s = \"the quick fox\"",
        "intro": "Split, reverse, join.",
        "steps": [
          {
            "step": "split",
            "action": "[the, quick, fox]",
            "state": "tokens"
          },
          {
            "step": "reverse",
            "action": "[fox, quick, the]",
            "state": "reversed"
          },
          {
            "step": "join",
            "action": "single spaces",
            "state": "fox quick the"
          }
        ],
        "result": "\"fox quick the\""
      },
      "edgeCases": [
        {
          "input": "s = \"hello\"",
          "behavior": "One word → \"hello\"."
        },
        {
          "input": "s = \"  a   b  \"",
          "behavior": "Extra spaces normalised → \"b a\"."
        },
        {
          "input": "s = \"a b c\"",
          "behavior": "→ \"c b a\"."
        }
      ],
      "pitfalls": [
        "Reversing characters instead of words.",
        "Leaving double spaces from a naive split."
      ]
    }
  ],
  "commonMistakes": [
    "Reversing characters instead of words.",
    "Leaving double spaces from a naive split."
  ],
  "followupVariations": [
    {
      "title": "Reverse Words in a String",
      "slug": "reverse-words-in-string",
      "hint": "In-place: reverse all, then each word.",
      "leetcodeNumber": 151
    },
    {
      "title": "Reverse a String",
      "slug": "reverse-string",
      "hint": "Character-level reversal."
    }
  ],
  "patternNote": "A core fresher pattern: Tokenise → reverse → join. Master it on small inputs and it transfers to harder problems.",
  "seo": {
    "metaTitle": "Reverse Words in a Sentence — Solution in Java & Python | InterviewExplainer",
    "metaDescription": "Split on whitespace (dropping empties), reverse the array of words, and join with a single space.",
    "canonicalUrl": "/dsa/problem/reverse-words"
  },
  "interviewerIntent": {
    "testing": "Whether you operate at word granularity and normalise messy spacing.",
    "commonMistake": "Character reversal, or empty tokens from a bad split.",
    "toStandOut": "Mention the in-place O(1)-space version: reverse whole, then reverse each word."
  },
  "clarifyingQuestions": [
    {
      "question": "Collapse multiple spaces in output?",
      "answer": "Yes — I join with single spaces and trim."
    }
  ]
}

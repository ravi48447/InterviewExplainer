{
  "id": "sum-of-min-and-max",
  "slug": "sum-of-min-and-max",
  "title": "Sum of Min and Max",
  "difficulty": "easy",
  "category": "basics",
  "patterns": [
    "linear-scan"
  ],
  "companies": [],
  "frequency": "medium",
  "readingTimeMinutes": 6,
  "lastUpdated": "2026-06-01",
  "directAnswer": "Seed both `min` and `max` to `nums[0]`, update both as you scan, and return `min + max`. One pass instead of two.",
  "remember": {
    "pattern": "Dual running extremes",
    "rules": [
      "Seed both with nums[0].",
      "Two independent ifs per element.",
      "Return their sum."
    ],
    "takeaway": "Min and max can share one pass — track both.",
    "formula": "lo=hi=nums[0]; for x: lo=min(lo,x); hi=max(hi,x); return lo+hi",
    "whenToUse": [
      "Multiple reductions over one array",
      "Fusing passes"
    ]
  },
  "problemStatement": "Given a non-empty integer array `nums`, return the sum of its smallest and largest elements.",
  "understanding": "Track both the running minimum and maximum in a single pass, then add them.",
  "constraints": [
    "1 ≤ nums.length ≤ 10^5"
  ],
  "examples": [
    {
      "input": "nums = [3, 7, 2, 9]",
      "output": "11",
      "explanation": "min 2 + max 9."
    },
    {
      "input": "nums = [5]",
      "output": "10",
      "explanation": "5 + 5."
    },
    {
      "input": "nums = [-4, -1]",
      "output": "-5",
      "explanation": "-4 + -1."
    }
  ],
  "approaches": [
    {
      "name": "Track min and max together",
      "whenToMention": "This is the standard, expected solution — lead with it.",
      "complexity": {
        "time": "O(n)",
        "space": "O(1)"
      },
      "explanation": "Track the running minimum and maximum **together** in one pass, then add them. Two independent `if`s per element — one can lower the min, the other can raise the max — avoid a second scan.\n\n**Why one pass suffices.** Min and max are independent reductions over the same data, so they can share a single traversal. Scanning twice is correct but wasteful; combining them halves the passes.\n\n**Why seed both with `nums[0]`.** Same reasoning as array-max/min: seeding with a real element keeps both extremes valid for all-negative or all-positive arrays. Seeding with 0 breaks one side or the other.\n\n**Overflow.** min + max of values up to 10⁹ stays within int, but returning a `long` is a safe habit when you're unsure of the range.",
      "code": {
        "java": "public long sumMinMax(int[] nums) {\n    int min = nums[0], max = nums[0];\n    for (int x : nums) {\n        if (x < min) min = x;\n        if (x > max) max = x;\n    }\n    return (long) min + max;\n}",
        "python": "def sum_min_max(nums: list[int]) -> int:\n    lo = hi = nums[0]\n    for x in nums:\n        if x < lo:\n            lo = x\n        if x > hi:\n            hi = x\n    return lo + hi"
      },
      "hints": [
        "Do you need two passes, or can min and max share one?",
        "Two separate ifs per element: maybe-lower-min, maybe-raise-max.",
        "Seed both extremes with nums[0], not 0."
      ],
      "complexityReasoning": "two comparisons per element in a single pass, O(1) state.",
      "dryRun": {
        "input": "nums = [3, 7, 2, 9]",
        "intro": "Track both extremes at once.",
        "steps": [
          {
            "step": "seed",
            "action": "min=max=3",
            "state": "(3,3)"
          },
          {
            "step": "7",
            "action": "max→7",
            "state": "(3,7)"
          },
          {
            "step": "2",
            "action": "min→2",
            "state": "(2,7)"
          },
          {
            "step": "9",
            "action": "max→9",
            "state": "(2,9)"
          },
          {
            "step": "sum",
            "action": "2 + 9",
            "state": "11"
          }
        ],
        "result": "11"
      },
      "edgeCases": [
        {
          "input": "nums = [5]",
          "behavior": "min = max = 5 → 10."
        },
        {
          "input": "nums = [-4, -1]",
          "behavior": "min -4, max -1 → -5."
        }
      ],
      "pitfalls": [
        "Seeding min/max with 0 instead of nums[0].",
        "Scanning twice when one pass works."
      ]
    }
  ],
  "commonMistakes": [
    "Seeding min/max with 0 instead of nums[0].",
    "Scanning twice when one pass suffices."
  ],
  "followupVariations": [
    {
      "title": "Maximum in an Array",
      "slug": "array-maximum",
      "hint": "One of the two reductions."
    },
    {
      "title": "Minimum in an Array",
      "slug": "array-minimum",
      "hint": "The other reduction."
    }
  ],
  "patternNote": "A core fresher pattern: Dual running extremes. Master it on small inputs and it transfers to harder problems.",
  "seo": {
    "metaTitle": "Sum of Min and Max — Solution in Java & Python | InterviewExplainer",
    "metaDescription": "Seed both `min` and `max` to `nums[0]`, update both as you scan, and return `min + max`. One pass instead of two.",
    "canonicalUrl": "/dsa/problem/sum-of-min-and-max"
  },
  "interviewerIntent": {
    "testing": "Whether you fuse two reductions into one pass and seed correctly.",
    "commonMistake": "0-seeding, or two separate scans.",
    "toStandOut": "Note both extremes are independent reductions that can share a traversal."
  },
  "clarifyingQuestions": [
    {
      "question": "Single-element array?",
      "answer": "min equals max, so the answer is twice that element."
    }
  ]
}

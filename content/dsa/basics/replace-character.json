{
  "id": "replace-character",
  "slug": "replace-character",
  "title": "Replace a Character",
  "difficulty": "easy",
  "category": "basics",
  "patterns": [
    "linear-scan"
  ],
  "companies": [],
  "frequency": "medium",
  "readingTimeMinutes": 6,
  "lastUpdated": "2026-06-01",
  "directAnswer": "Build the result, appending `newCh` where the character equals `oldCh` and the original character otherwise.",
  "remember": {
    "pattern": "Map-and-build",
    "rules": [
      "Match → replacement.",
      "Else → original.",
      "One pass."
    ],
    "takeaway": "Per-character transforms are a single map-and-build pass.",
    "formula": "for ch: out += (ch==old ? new : ch)",
    "whenToUse": [
      "Per-character mapping"
    ]
  },
  "problemStatement": "Given a string `s` and two characters `oldCh` and `newCh`, return `s` with every `oldCh` replaced by `newCh`.",
  "understanding": "Copy the string, swapping any matching character for the replacement.",
  "constraints": [
    "0 ≤ s.length ≤ 10^4"
  ],
  "examples": [
    {
      "input": "s = \"banana\", old='a', new='o'",
      "output": "\"bonono\"",
      "explanation": "All a's → o."
    },
    {
      "input": "s = \"abc\", old='x', new='y'",
      "output": "\"abc\"",
      "explanation": "No match."
    },
    {
      "input": "s = \"aaa\", old='a', new='b'",
      "output": "\"bbb\"",
      "explanation": "All replaced."
    }
  ],
  "approaches": [
    {
      "name": "Map-and-build",
      "whenToMention": "This is the standard, expected solution — lead with it.",
      "complexity": {
        "time": "O(n)",
        "space": "O(n)"
      },
      "explanation": "Transform each character: output the replacement where it matches `oldCh`, otherwise the original. One pass with a char array (Java) or `str.replace` (Python) — the original is untouched because strings are immutable.\n\n**Why a map-and-build.** Each output character is a pure function of the input character (replace-or-keep), so a single pass produces the result. This generalises to any per-character mapping.\n\n**Direction matters.** Replace `oldCh` *with* `newCh`; swapping the arguments is the common bug. Be clear which is the search target and which is the replacement.\n\n**Performance.** Use a char array or builder, not `+=` in a loop, to stay O(n).",
      "code": {
        "java": "public String replace(String s, char oldCh, char newCh) {\n    char[] c = s.toCharArray();\n    for (int i = 0; i < c.length; i++) {\n        if (c[i] == oldCh) c[i] = newCh;\n    }\n    return new String(c);\n}",
        "python": "def replace(s: str, old: str, new: str) -> str:\n    return s.replace(old, new)"
      },
      "hints": [
        "Each output char is either the replacement or the original.",
        "Be careful which argument is the target and which is the replacement.",
        "Use a char array / builder, not += in a loop."
      ],
      "complexityReasoning": "one comparison and write per character.",
      "dryRun": {
        "input": "s = \"banana\", old='a', new='o'",
        "intro": "Map matches to the replacement.",
        "steps": [
          {
            "step": "b",
            "action": "keep",
            "state": "b"
          },
          {
            "step": "a",
            "action": "→ o",
            "state": "bo"
          },
          {
            "step": "n",
            "action": "keep",
            "state": "bon"
          },
          {
            "step": "...",
            "action": "a→o each",
            "state": "bonono"
          }
        ],
        "result": "\"bonono\""
      },
      "edgeCases": [
        {
          "input": "s = \"abc\", old='x'",
          "behavior": "No match → unchanged."
        },
        {
          "input": "s = \"aaa\", old='a', new='b'",
          "behavior": "→ \"bbb\"."
        },
        {
          "input": "s = \"\"",
          "behavior": "Empty → \"\"."
        }
      ],
      "pitfalls": [
        "Replacing the wrong direction (new with old).",
        "Using += in a loop instead of a char array/builder."
      ]
    }
  ],
  "commonMistakes": [
    "Replacing the wrong direction (new with old).",
    "Using += in a loop instead of a char array/builder."
  ],
  "followupVariations": [
    {
      "title": "Remove Spaces",
      "slug": "remove-spaces",
      "hint": "A filter instead of a replace."
    }
  ],
  "patternNote": "A core fresher pattern: Map-and-build. Master it on small inputs and it transfers to harder problems.",
  "seo": {
    "metaTitle": "Replace a Character — Solution in Java & Python | InterviewExplainer",
    "metaDescription": "Build the result, appending `newCh` where the character equals `oldCh` and the original character otherwise.",
    "canonicalUrl": "/dsa/problem/replace-character"
  },
  "interviewerIntent": {
    "testing": "Whether you do a clean per-character transform without the concatenation trap.",
    "commonMistake": "Swapping old/new, or O(n²) building.",
    "toStandOut": "Frame it as a general map-over-characters transform."
  },
  "clarifyingQuestions": [
    {
      "question": "Replace all occurrences?",
      "answer": "Yes — every match; first-only would be a variant."
    }
  ]
}

{
  "id": "fibonacci-recursion",
  "slug": "fibonacci-recursion",
  "title": "Fibonacci (Recursion)",
  "difficulty": "easy",
  "category": "basics",
  "patterns": [
    "recursion"
  ],
  "companies": [],
  "frequency": "high",
  "readingTimeMinutes": 6,
  "lastUpdated": "2026-06-01",
  "directAnswer": "Base cases F(0)=0, F(1)=1; else `fib(n-1) + fib(n-2)`. Naive is O(2ⁿ); add a memo array/map for O(n).",
  "remember": {
    "pattern": "Overlapping subproblems → memoise",
    "rules": [
      "F(0)=0, F(1)=1.",
      "Naive is exponential.",
      "Memoise → O(n)."
    ],
    "takeaway": "Recursion with repeated subproblems begs for memoisation.",
    "formula": "F(n)=F(n-1)+F(n-2); MEMOISE",
    "whenToUse": [
      "Demonstrating memoisation",
      "Overlapping-subproblem recursions"
    ],
    "antiSignals": [
      "Naive recursion on large n"
    ]
  },
  "problemStatement": "Given `n` (0-indexed), compute the n-th Fibonacci number recursively. F(0)=0, F(1)=1.",
  "understanding": "F(n) = F(n-1) + F(n-2) with two base cases. The naive recursion is exponential — memoise to fix it.",
  "constraints": [
    "0 ≤ n ≤ 40 (naive); larger needs memoisation"
  ],
  "examples": [
    {
      "input": "n = 6",
      "output": "8",
      "explanation": "0,1,1,2,3,5,8."
    },
    {
      "input": "n = 0",
      "output": "0",
      "explanation": "Base case."
    },
    {
      "input": "n = 1",
      "output": "1",
      "explanation": "Base case."
    }
  ],
  "approaches": [
    {
      "name": "Two-branch recursion (+ memo)",
      "whenToMention": "This is the standard, expected solution — lead with it.",
      "complexity": {
        "time": "O(2ⁿ) naive, O(n) memoised",
        "space": "O(n)"
      },
      "explanation": "F(n) = F(n−1) + F(n−2), with two base cases F(0)=0, F(1)=1. The naive recursion is the canonical example of **overlapping subproblems** causing exponential blow-up — and memoisation is the expected fix.\n\n**Why it's O(2ⁿ).** The two recursive branches re-evaluate the same values repeatedly: computing F(5) recomputes F(3) twice, F(2) three times, and so on. The call tree roughly doubles each level, so the work is exponential. F(50) naively makes billions of calls.\n\n**The fix: memoisation.** Cache each F(k) the first time it's computed (an array or hash map, or `@lru_cache`). Subsequent calls hit the cache in O(1), collapsing the whole thing to O(n). Iteration (rolling two variables) is the O(1)-space alternative.\n\n**The real interview point** is recognising the exponential blow-up and removing it — not writing the recurrence.",
      "code": {
        "java": "public long fib(int n) {\n    if (n < 2) return n;\n    return fib(n - 1) + fib(n - 2);\n}\n// Memoised: cache results in a long[] indexed by n.",
        "python": "def fib(n: int) -> int:\n    if n < 2:\n        return n\n    return fib(n - 1) + fib(n - 2)\n# Or: from functools import lru_cache and decorate."
      },
      "hints": [
        "Write the two-base-case recurrence first.",
        "Sketch the call tree — which subproblems repeat?",
        "Cache results (memoise) to collapse O(2ⁿ) to O(n)."
      ],
      "insight": "The interview point isn't the recurrence — it's recognising the exponential blow-up and fixing it with memoisation or iteration.",
      "complexityReasoning": "naive recursion's call tree doubles each level → O(2ⁿ); memoising computes each F(k) once → O(n).",
      "lineByLine": {
        "java": [
          {
            "line": "if (n < 2) return n;",
            "explanation": "Handles both F(0)=0 and F(1)=1."
          },
          {
            "line": "return fib(n - 1) + fib(n - 2);",
            "explanation": "Two recursive branches — the source of the blow-up."
          }
        ]
      },
      "dryRun": {
        "input": "n = 5 (naive call tree)",
        "intro": "See the repeated work.",
        "steps": [
          {
            "step": "F(5)",
            "action": "F(4)+F(3)",
            "state": "branches"
          },
          {
            "step": "F(4),F(3)",
            "action": "both expand F(2)",
            "state": "F(2) recomputed"
          },
          {
            "step": "...",
            "action": "exponential repeats",
            "state": "O(2^n)"
          }
        ],
        "result": "5"
      },
      "edgeCases": [
        {
          "input": "n = 0",
          "behavior": "Base case → 0."
        },
        {
          "input": "n = 1",
          "behavior": "Base case → 1."
        },
        {
          "input": "n = 50",
          "behavior": "Naive times out; memoised is instant."
        }
      ],
      "pitfalls": [
        "Leaving the naive O(2ⁿ) version for large n.",
        "Only one base case, mishandling n = 0 or 1."
      ]
    }
  ],
  "commonMistakes": [
    "Leaving the naive O(2ⁿ) version for large n.",
    "Only one base case, mishandling n = 0 or 1."
  ],
  "followupVariations": [
    {
      "title": "Nth Fibonacci Number",
      "slug": "nth-fibonacci",
      "hint": "Iterative O(n)/O(1) version."
    },
    {
      "title": "Climbing Stairs",
      "slug": "climbing-stairs",
      "hint": "Same recurrence as a DP.",
      "leetcodeNumber": 70
    }
  ],
  "patternNote": "A core fresher pattern: Overlapping subproblems → memoise. Master it on small inputs and it transfers to harder problems.",
  "seo": {
    "metaTitle": "Fibonacci (Recursion) — Solution in Java & Python | InterviewExplainer",
    "metaDescription": "Base cases F(0)=0, F(1)=1; else `fib(n-1) + fib(n-2)`. Naive is O(2ⁿ); add a memo array/map for O(n).",
    "canonicalUrl": "/dsa/problem/fibonacci-recursion"
  },
  "interviewerIntent": {
    "testing": "Whether you spot overlapping subproblems and apply memoisation.",
    "commonMistake": "Shipping the exponential recursion.",
    "toStandOut": "Offer top-down memoisation and the O(1)-space iterative roll."
  },
  "clarifyingQuestions": [
    {
      "question": "Is performance graded?",
      "answer": "If so I'll memoise or iterate rather than recurse naively."
    }
  ],
  "commonMistakesDetailed": [
    {
      "title": "Naive recursion without memo",
      "why": "Recomputing F(n-1) and F(n-2) re-evaluates shared subproblems exponentially.",
      "lang": "python",
      "bad": "def fib(n):\n    if n<2: return n\n    return fib(n-1)+fib(n-2)   # O(2^n)",
      "good": "from functools import lru_cache\n@lru_cache(None)\ndef fib(n):\n    if n<2: return n\n    return fib(n-1)+fib(n-2)   # O(n)"
    }
  ]
}

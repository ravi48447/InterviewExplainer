{
  "id": "reverse-string",
  "slug": "reverse-string",
  "title": "Reverse a String",
  "difficulty": "easy",
  "category": "basics",
  "patterns": [
    "two-pointers"
  ],
  "companies": [],
  "frequency": "high",
  "readingTimeMinutes": 6,
  "lastUpdated": "2026-06-01",
  "directAnswer": "Convert to a char array and two-pointer swap the ends inward, or in Python use slicing `s[::-1]`. O(n) time.",
  "remember": {
    "pattern": "Two-pointer swap (chars)",
    "rules": [
      "Work on a char array in Java.",
      "Swap ends, converge.",
      "Python: s[::-1]."
    ],
    "takeaway": "String reverse is array reverse on characters.",
    "formula": "char[] c; l,r=0,n-1; while l<r: swap; l++; r--",
    "whenToUse": [
      "In-place reversal of a sequence"
    ]
  },
  "problemStatement": "Given a string `s`, return it with the characters in reverse order.",
  "understanding": "Either swap from both ends inward (in place on a char array) or build the result back-to-front.",
  "constraints": [
    "0 ≤ s.length ≤ 10^4"
  ],
  "examples": [
    {
      "input": "s = \"hello\"",
      "output": "\"olleh\"",
      "explanation": "Characters reversed."
    },
    {
      "input": "s = \"a\"",
      "output": "\"a\"",
      "explanation": "Single char unchanged."
    },
    {
      "input": "s = \"ab\"",
      "output": "\"ba\"",
      "explanation": "Two chars swapped."
    }
  ],
  "approaches": [
    {
      "name": "Two-pointer swap",
      "whenToMention": "This is the standard, expected solution — lead with it.",
      "complexity": {
        "time": "O(n)",
        "space": "O(1) extra (in place on char array)"
      },
      "explanation": "Reversing a string is the array two-pointer swap applied to characters: swap the first and last, second and second-last, converging in the middle. O(n) time, O(1) extra space.\n\n**Why a char array in Java.** Java `String`s are **immutable** — you can't assign `s[i] = …`. So you convert to a `char[]`, swap in place, and build a new String. Python strings are also immutable, but slicing `s[::-1]` builds the reversed copy in one expression.\n\n**Why two pointers, not a fresh buffer per char.** The in-place swap touches each character once with no per-character allocation. Building the result by repeatedly prepending characters would be O(n²) in many languages.\n\n**Loop bound.** Use `left < right` so the middle character of an odd-length string is left untouched and the pointers don't cross and undo the work.",
      "code": {
        "java": "public String reverse(String s) {\n    char[] c = s.toCharArray();\n    int left = 0, right = c.length - 1;\n    while (left < right) {\n        char t = c[left];\n        c[left] = c[right];\n        c[right] = t;\n        left++;\n        right--;\n    }\n    return new String(c);\n}",
        "python": "def reverse(s: str) -> str:\n    return s[::-1]"
      },
      "hints": [
        "Reuse the array-reverse idea — on characters this time.",
        "Java strings are immutable: work on a char array.",
        "Two pointers from both ends, swap, converge; Python can use s[::-1]."
      ],
      "insight": "Mentioning that Java strings are immutable (hence the char array) shows language awareness.",
      "complexityReasoning": "n/2 swaps with O(1) extra state (the output buffer aside).",
      "lineByLine": {
        "java": [
          {
            "line": "char[] c = s.toCharArray();",
            "explanation": "Java strings are immutable, so reverse a mutable char array."
          },
          {
            "line": "while (left < right) { … }",
            "explanation": "Swap the two ends and converge."
          }
        ]
      },
      "dryRun": {
        "input": "s = \"abcd\"",
        "intro": "Swap ends inward.",
        "steps": [
          {
            "step": "l=0,r=3",
            "action": "swap a,d",
            "state": "dbca"
          },
          {
            "step": "l=1,r=2",
            "action": "swap b,c",
            "state": "dcba"
          },
          {
            "step": "l=2,r=1",
            "action": "stop",
            "state": "done"
          }
        ],
        "result": "\"dcba\""
      },
      "edgeCases": [
        {
          "input": "s = \"a\"",
          "behavior": "Single char unchanged."
        },
        {
          "input": "s = \"\"",
          "behavior": "Empty string unchanged."
        },
        {
          "input": "s = \"ab\"",
          "behavior": "Two chars swap to \"ba\"."
        }
      ],
      "pitfalls": [
        "Trying to mutate a Java String directly (immutable).",
        "Off-by-one on the right index (length vs length − 1)."
      ]
    }
  ],
  "commonMistakes": [
    "Trying to mutate a Java String directly (it's immutable).",
    "Off-by-one on the right index."
  ],
  "followupVariations": [
    {
      "title": "Reverse an Array",
      "slug": "reverse-array",
      "hint": "Same swap on integers."
    },
    {
      "title": "Reverse Words in a Sentence",
      "slug": "reverse-words",
      "hint": "Reverse word order, not characters."
    }
  ],
  "patternNote": "A core fresher pattern: Two-pointer swap (chars). Master it on small inputs and it transfers to harder problems.",
  "seo": {
    "metaTitle": "Reverse a String — Solution in Java & Python | InterviewExplainer",
    "metaDescription": "Convert to a char array and two-pointer swap the ends inward, or in Python use slicing `s[::-1]`. O(n) time.",
    "canonicalUrl": "/dsa/problem/reverse-string"
  },
  "interviewerIntent": {
    "testing": "Whether you know strings are immutable and reverse in place on a char array.",
    "commonMistake": "Mutating the String, or O(n²) prepend-building.",
    "toStandOut": "Mention Unicode/surrogate pairs as a caveat for true character reversal."
  },
  "clarifyingQuestions": [
    {
      "question": "Reverse by code unit or grapheme?",
      "answer": "Standard problems reverse code units; emoji/combining marks need care."
    }
  ]
}

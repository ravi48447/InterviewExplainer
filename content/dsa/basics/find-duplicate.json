{
  "id": "find-duplicate",
  "slug": "find-duplicate",
  "title": "Find a Duplicate",
  "difficulty": "easy",
  "category": "basics",
  "patterns": [
    "hash-set"
  ],
  "companies": [],
  "frequency": "medium",
  "readingTimeMinutes": 6,
  "lastUpdated": "2026-06-01",
  "directAnswer": "Walk the array adding each value to a hash set; the first value that's already present is a duplicate. O(n) time, O(n) space.",
  "remember": {
    "pattern": "Hash set membership",
    "rules": [
      "Add as you go.",
      "Already-present ⟹ duplicate.",
      "O(n) time, O(n) space."
    ],
    "takeaway": "'Seen before?' questions are a hash set in disguise.",
    "formula": "seen=set(); for x: if x in seen: return x; seen.add(x)",
    "whenToUse": [
      "Detecting repeats",
      "'Have I seen this?' membership"
    ],
    "antiSignals": [
      "Values bounded to 1..n and O(1) space required — use sign marking"
    ]
  },
  "problemStatement": "Given an integer array `nums` that contains at least one repeated value, return any value that appears more than once.",
  "understanding": "Remember what you've seen in a hash set; the first value already in the set is a duplicate.",
  "constraints": [
    "2 ≤ nums.length ≤ 10^5",
    "At least one duplicate exists"
  ],
  "examples": [
    {
      "input": "nums = [1, 3, 2, 3, 4]",
      "output": "3",
      "explanation": "3 repeats."
    },
    {
      "input": "nums = [5, 5]",
      "output": "5",
      "explanation": "5 repeats."
    },
    {
      "input": "nums = [1, 2, 2, 1]",
      "output": "2 or 1",
      "explanation": "Either repeated value is valid."
    }
  ],
  "approaches": [
    {
      "name": "Hash set seen",
      "whenToMention": "This is the standard, expected solution — lead with it.",
      "complexity": {
        "time": "O(n)",
        "space": "O(n)"
      },
      "explanation": "Use a hash **set** as a memory of what you've already seen. Walk the array; the first element that is already in the set is a duplicate. O(n) time, O(n) space.\n\n**Why a set.** A set answers 'have I seen this value before?' in O(1) average time. That converts the O(n²) brute force (compare every pair) into a single linear pass — you trade O(n) memory for the speedup.\n\n**Check-then-add ordering.** You must test membership *before* inserting; insert first and the value is always 'present', so you'd never detect the repeat. In Java the idiom `if (!seen.add(x)) return x;` is neat because `add` returns false when the element already existed.\n\n**Constrained variants.** If values are guaranteed in 1..n, you can find a duplicate in O(1) space using index-sign marking or Floyd's cycle detection — good things to mention, since the set solution uses extra memory.",
      "code": {
        "java": "public int findDuplicate(int[] nums) {\n    java.util.Set<Integer> seen = new java.util.HashSet<>();\n    for (int x : nums) {\n        if (!seen.add(x)) return x;\n    }\n    return -1;\n}",
        "python": "def find_duplicate(nums: list[int]) -> int:\n    seen = set()\n    for x in nums:\n        if x in seen:\n            return x\n        seen.add(x)\n    return -1"
      },
      "hints": [
        "What data structure answers 'seen this before?' in O(1)?",
        "Add elements to a set; the first failed insert is your duplicate.",
        "Check membership before inserting, not after."
      ],
      "insight": "Mention the O(n²) nested-loop brute force first, then the O(n) set — the trade is time for O(n) space.",
      "complexityReasoning": "each element does an O(1) set lookup/insert, so O(n) time; the set can hold up to n values, so O(n) space.",
      "lineByLine": {
        "java": [
          {
            "line": "if (!seen.add(x)) return x;",
            "explanation": "Set.add returns false when the value was already present — that's the duplicate."
          }
        ]
      },
      "dryRun": {
        "input": "nums = [1, 3, 2, 3, 4]",
        "intro": "Track seen values; first repeat wins.",
        "steps": [
          {
            "step": "1",
            "action": "not in set",
            "state": "{1}"
          },
          {
            "step": "3",
            "action": "not in set",
            "state": "{1,3}"
          },
          {
            "step": "2",
            "action": "not in set",
            "state": "{1,2,3}"
          },
          {
            "step": "3",
            "action": "already in set",
            "state": "return 3"
          }
        ],
        "result": "3"
      },
      "edgeCases": [
        {
          "input": "nums = [5, 5]",
          "behavior": "Immediate repeat → 5."
        },
        {
          "input": "nums = [1, 2, 2, 1]",
          "behavior": "Returns the first repeat encountered (2)."
        }
      ],
      "pitfalls": [
        "Nested loops (O(n²)) when a set gives O(n).",
        "Inserting before checking, so the repeat is never detected."
      ]
    }
  ],
  "commonMistakes": [
    "Using nested loops (O(n²)) when a set gives O(n).",
    "Adding before checking and never detecting the repeat."
  ],
  "followupVariations": [
    {
      "title": "Contains Duplicate",
      "slug": "contains-duplicate",
      "hint": "Return just true/false using the same set.",
      "leetcodeNumber": 217
    },
    {
      "title": "Find the Duplicate Number (O(1) space)",
      "slug": "find-the-duplicate-number",
      "hint": "Floyd's cycle detection when values are in 1..n.",
      "leetcodeNumber": 287
    }
  ],
  "patternNote": "A core fresher pattern: Hash set membership. Master it on small inputs and it transfers to harder problems.",
  "seo": {
    "metaTitle": "Find a Duplicate — Solution in Java & Python | InterviewExplainer",
    "metaDescription": "Walk the array adding each value to a hash set; the first value that's already present is a duplicate. O(n) time, O(n) space.",
    "canonicalUrl": "/dsa/problem/find-duplicate"
  },
  "interviewerIntent": {
    "testing": "Whether you reach for a hash set to trade space for time, with correct ordering.",
    "commonMistake": "O(n²) pair comparison, or add-before-check.",
    "toStandOut": "Offer the O(1)-space variants when values are bounded to 1..n (sign marking / Floyd)."
  },
  "clarifyingQuestions": [
    {
      "question": "Are values bounded (e.g. 1..n)?",
      "answer": "If so I can do it in O(1) space; otherwise a set is the clean O(n) answer."
    },
    {
      "question": "Return any duplicate or the first?",
      "answer": "I return the first repeat encountered."
    }
  ],
  "commonMistakesDetailed": [
    {
      "title": "Add before checking",
      "why": "If you insert the value first, the membership test always succeeds and no duplicate is ever reported.",
      "lang": "python",
      "bad": "for x in nums:\n    seen.add(x)\n    if x in seen: return x   # always true",
      "good": "for x in nums:\n    if x in seen: return x\n    seen.add(x)"
    }
  ]
}

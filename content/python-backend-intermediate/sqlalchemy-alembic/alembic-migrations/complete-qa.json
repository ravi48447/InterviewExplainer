[
  {
    "id": "py-sqla-q030",
    "slug": "alembic-migration-workflow",
    "question": "How does Alembic work for database migrations and what is the recommended workflow?",
    "title": "Alembic Migration Workflow",
    "direct_answer": "Alembic tracks schema changes as versioned Python migration files. Workflow: alembic revision --autogenerate -m 'add users table' (inspect model changes, generate migration), review the generated upgrade()/downgrade() functions, then alembic upgrade head to apply. Each migration has a revision ID and depends on previous revisions. In production, run alembic upgrade head before starting the new app version. Never edit applied migrations — create a new one.",
    "interviewer_intent": "Tests production database migration knowledge — migration strategy is where many schema change deployments go wrong.",
    "company_tags": [
      "stripe",
      "amazon"
    ],
    "answer": {
      "sections": [
        {
          "type": "overview",
          "title": "Alembic migration workflow",
          "content": "Alembic is SQLAlchemy's migration tool. It compares your current SQLAlchemy models against the live database schema (via --autogenerate) and generates a migration file with upgrade() and downgrade() functions.\n\nThe revision chain: each migration file has a revision ID and a down_revision pointer. Alembic walks the chain to know what order to apply migrations. alembic current shows the current head. alembic history shows the chain.\n\nAutogenerate limitations: it detects column additions, removals, type changes. It does NOT detect: renamed columns (sees add + drop), server defaults, check constraints, custom types. Always review the generated migration before applying.\n\nProduction workflow:\n1. Develop: alembic revision --autogenerate -m 'description'\n2. Review: check the generated file for correctness\n3. Test: apply to staging, run tests\n4. Deploy: run alembic upgrade head before or during new app deployment (zero-downtime requires backward-compatible migrations)"
        },
        {
          "type": "code",
          "title": "Alembic setup and migration",
          "content": "# alembic.ini — key setting\n# sqlalchemy.url = postgresql://user:pw@localhost/db\n\n# env.py — connect to your models\nfrom myapp.models import Base\ntarget_metadata = Base.metadata  # alembic uses this for --autogenerate\n\n# Generate migration\n# alembic revision --autogenerate -m 'add email index to users'\n\n# Generated migration file\n\"\"\"add email index to users\nRevision ID: abc123\nDown Revision: def456\n\"\"\"\nfrom alembic import op\nimport sqlalchemy as sa\n\ndef upgrade() -> None:\n    op.create_index('ix_users_email', 'users', ['email'], unique=True)\n\ndef downgrade() -> None:\n    op.drop_index('ix_users_email', 'users')\n\n# Applying\n# alembic upgrade head        — apply all pending migrations\n# alembic upgrade abc123      — apply up to specific revision\n# alembic downgrade -1        — roll back one step\n# alembic current             — show current revision\n# alembic history --verbose   — show migration chain\n\n# Zero-downtime migration pattern for adding a required column\ndef upgrade() -> None:\n    # Step 1: add nullable column\n    op.add_column('users', sa.Column('phone', sa.String(20), nullable=True))\n    # Step 2: backfill existing rows (batch if large table)\n    op.execute(\"UPDATE users SET phone = '' WHERE phone IS NULL\")\n    # Step 3: make NOT NULL (separate migration after deploy)\n    # op.alter_column('users', 'phone', nullable=False)"
        }
      ]
    },
    "followup_questions": [
      "How do you handle migrations for large tables with millions of rows?",
      "What is a zero-downtime migration strategy for adding NOT NULL columns?",
      "How do you manage Alembic in a multi-environment (dev/staging/prod) setup?"
    ],
    "seo": {
      "metaTitle": "Alembic Migration Workflow — Autogenerate, Review, and Production Deploy",
      "metaDescription": "Alembic database migrations: autogenerate workflow, revision chain, production deployment order, and zero-downtime patterns for schema changes."
    },
    "speakable_v2": {
      "archetype": "C",
      "pillar": "P03",
      "hook": "Alembic's autogenerate is useful but never trust it blindly — always review the generated migration before applying it.",
      "beats": [
        {
          "kind": "workflow",
          "layout": "paragraph",
          "content": "alembic revision --autogenerate compares your models to the live schema and generates upgrade/downgrade functions. Review it — autogenerate misses renamed columns, server defaults, and custom constraints."
        },
        {
          "kind": "production",
          "layout": "paragraph",
          "content": "Run alembic upgrade head before the new app version starts. For zero-downtime, migrations must be backward-compatible with the old app version — add columns as nullable first, make them required in a later migration."
        },
        {
          "kind": "never_edit",
          "layout": "paragraph",
          "content": "Never edit an applied migration. The revision chain is immutable history. If a migration was wrong, create a new one that fixes it."
        }
      ],
      "cap": "Add the Alembic upgrade step to your deployment pipeline, not your app startup. Running migrations at app startup causes issues when multiple instances start simultaneously.",
      "followup_handoff": "Follow up with large table migration strategies (pg-osc, pt-online-schema-change) or multi-environment Alembic config.",
      "tts_overrides": {}
    }
  }
]
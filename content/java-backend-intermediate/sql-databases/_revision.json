{
  "title": "SQL & Relational Databases — Revision",
  "estimatedMinutes": 12,
  "sections": [
    {
      "id": "core",
      "title": "Why this module matters",
      "body": "SQL interview questions are unavoidable in any backend interview — joins (inner, outer, cross, self), subqueries vs joins, window functions, indexes (B-tree vs hash, when an index hurts), the EXPLAIN plan, transaction isolation levels, ACID, normalization, sharding, partitioning, replication, connection pooling, and migration tools (Flyway, Liquibase). This page covers cross-database SQL fundamentals plus the operational questions that decide a senior interview. For Postgres-specific deep dives (window functions, JSONB, MVCC, partitioning), use the dedicated PostgreSQL page; for MongoDB, see the MongoDB page; for Redis, see the Redis page."
    },
    {
      "id": "mental-model",
      "title": "Mental model",
      "body": "Three lenses senior interviews probe:\n\n- **Query model** — joins, subqueries, CTEs, window functions.\n- **Storage / index model** — B-tree default, hash for equality, partial / covering indexes.\n- **Concurrency model** — ACID + isolation levels (read-committed default in PG, repeatable-read default in MySQL InnoDB).\n\n**EXPLAIN** is your evidence. Always read the plan, not the SQL."
    },
    {
      "id": "must-know",
      "title": "Must-know concepts",
      "body": "The interview surface for this module breaks into these focus areas:\n\n- **Advanced sql features** — drill the named patterns, the failure modes, and the recommended Java / framework idiom.\n- **Backup recovery** — drill the named patterns, the failure modes, and the recommended Java / framework idiom.\n- **Comparisons** — drill the named patterns, the failure modes, and the recommended Java / framework idiom.\n- **Connection pooling** — drill the named patterns, the failure modes, and the recommended Java / framework idiom.\n- **Database migrations** — drill the named patterns, the failure modes, and the recommended Java / framework idiom.\n- **Indexes and performance** — drill the named patterns, the failure modes, and the recommended Java / framework idiom.\n- **Joins and subqueries** — drill the named patterns, the failure modes, and the recommended Java / framework idiom.\n- **Partitioning and sharding** — drill the named patterns, the failure modes, and the recommended Java / framework idiom.\n- **Query optimization** — drill the named patterns, the failure modes, and the recommended Java / framework idiom.\n- **Replication** — drill the named patterns, the failure modes, and the recommended Java / framework idiom.\n- **Sql fundamentals** — drill the named patterns, the failure modes, and the recommended Java / framework idiom.\n- **Transactions and acid** — drill the named patterns, the failure modes, and the recommended Java / framework idiom.\n- **Scenario-based** — applied questions tying multiple topics together. Drill these *last*, after the fundamentals are sharp."
    },
    {
      "id": "interview-hooks",
      "title": "Likely interview hooks",
      "body": "- **Index doesn't help when** — `WHERE func(col)`, leading wildcard `LIKE '%x'`, mismatched types, low selectivity.\n- **N+1** at the SQL layer is solved with **JOIN** or **`IN (subquery)`** — or aggregation in DB, not app.\n- **Phantom reads / lost updates** — name them with the isolation level that prevents each.\n- **Sharding** is a reorg, not an optimisation; mention it when read replicas + indexes have run out.\n- **Connection pooling** — HikariCP defaults are fine; size = `connections × instances` against DB max."
    },
    {
      "id": "speaking-template",
      "title": "Speaking template",
      "body": "> *\"I'd write the query, run `EXPLAIN ANALYZE`, look for <seq-scan | sort | hash join> on the hot path, fix it with <index | rewrite | denormalisation>, and only after the query is tight do I think about <partitioning | sharding | replication>.\"*"
    }
  ]
}

{
  "topic": "Gcp And Azure Overview",
  "topicSlug": "gcp-and-azure-overview",
  "questions": [
    {
      "id": "gcp-005",
      "slug": "gcp-secret-manager-spring-boot",
      "question": "How do you configure GCP Secret Manager with Spring Boot?",
      "title": "Integrating GCP Secret Manager with Spring Boot",
      "direct_answer": "Add the `spring-cloud-gcp-starter-secretmanager` dependency and reference secrets in `application.properties` using the `sm://` URI scheme. Spring Cloud GCP resolves these at startup as standard Spring property values, enabling Secret Manager to act as a secure, auditable replacement for environment variables or plaintext config files.",
      "layout_type": "recipe-builder",
      "interviewer_intent": {
        "testing": "Security-conscious configuration management — understanding how to keep credentials out of code and environment variables while integrating cleanly with the Spring property system.",
        "common_mistake": "Hard-coding the secret version (`:latest` vs a pinned version number), which prevents rollback, or granting the app's service account the `secretmanager.admin` role instead of the minimal `secretmanager.secretAccessor` role.",
        "to_stand_out": "Discuss secret rotation strategies, the difference between accessing secrets as property sources vs. the SecretManagerTemplate API, and how to handle bootstrap context refresh for runtime secret rotation."
      },
      "company_tags": [
        "Google",
        "Airbnb",
        "Lyft",
        "Robinhood"
      ],
      "reading_time_minutes": 4,
      "last_updated": "2025-01-15",
      "difficulty": "medium",
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "The Problem: Credentials That Leak Through Config Files and Env Vars",
            "content": "Every Spring Boot application needs database passwords, API keys, and encryption secrets. The naive approach — hardcoding them in `application.yml` or passing them as environment variables — creates a sprawling attack surface: credentials in git history, in CI logs (leaked via `env` dumps), in container inspect output, and in deployment manifests anyone with cluster access can read.\n\nSecret Manager solves this by storing credentials in a **versioned, auditable, IAM-controlled vault** that Spring Boot reads at startup through a custom property source. Your application code references `${sm://db-password}` exactly like any other Spring property — the resolution to the actual secret value happens transparently. No code changes, no special APIs in your domain logic.\n\nBut the integration has two distinct modes that serve different use cases: **property source resolution** for startup-time secrets (database passwords, API keys), and **`SecretManagerTemplate`** for runtime secret access (rotating encryption keys, per-tenant credentials). Understanding when to use each determines whether your secret management is production-grade or just a more complicated environment variable."
          },
          {
            "type": "step",
            "title": "The sm:// Property Source: Secrets as Spring Properties Without Code Changes",
            "content": "The property source approach is the most common and requires zero Java code. Add the starter and bootstrap dependencies, then reference secrets in your config files using the `sm://` URI scheme.\n\nThe bootstrap dependency is critical — Secret Manager properties must resolve before the application context initializes, which means before DataSource beans are created. Without `spring-cloud-starter-bootstrap`, the `sm://` placeholders resolve to literal strings and your database connection fails with an authentication error that doesn't mention Secret Manager at all.\n\n```xml\n<dependency>\n  <groupId>com.google.cloud</groupId>\n  <artifactId>spring-cloud-gcp-starter-secretmanager</artifactId>\n</dependency>\n<dependency>\n  <groupId>org.springframework.cloud</groupId>\n  <artifactId>spring-cloud-starter-bootstrap</artifactId>\n</dependency>\n```\n\n```properties\n# bootstrap.properties — resolves BEFORE application context\nspring.cloud.gcp.secretmanager.enabled=true\nspring.cloud.gcp.project-id=my-project\n\n# application.properties — uses resolved secrets\nspring.datasource.password=${sm://db-password}\napi.stripe.key=${sm://stripe-api-key/3}  # pinned to version 3\n```\n\nThe `sm://` scheme accepts three forms: `sm://SECRET_ID` (latest version), `sm://SECRET_ID/VERSION` (pinned), and the fully qualified `sm://projects/PROJECT/secrets/SECRET/versions/VERSION`. In production, **always pin to a specific version number**. Using `:latest` means a secret rotation changes what your running application uses without a redeploy — you lose reproducibility and can't rollback by reverting a deployment."
          },
          {
            "type": "step",
            "title": "IAM Least Privilege: Why secretAccessor Matters More Than You Think",
            "content": "The service account running your application needs IAM permissions to read secrets. The common mistake is granting `roles/secretmanager.admin` at the project level — this gives the application permission to create, delete, and modify any secret in the project. If the application is compromised, the attacker controls your entire secret store.\n\nThe correct role is `roles/secretmanager.secretAccessor`, granted on **specific secrets**, not the project.\n\n```bash\ngcloud secrets add-iam-policy-binding db-password \\\n  --member=\"serviceAccount:order-sa@my-project.iam.gserviceaccount.com\" \\\n  --role=\"roles/secretmanager.secretAccessor\"\n```\n\nThis follows the principle of least privilege: the order service can read `db-password` but cannot read `stripe-api-key` (which belongs to the payment service) and cannot modify or delete anything. On Cloud Run and GKE with Workload Identity, the service account is attached to the workload automatically — no JSON key files.\n\nA subtle but important distinction: the **execution environment** determines whether credentials flow automatically. On Cloud Run, the attached service account provides ADC. On GKE with Workload Identity, the Kubernetes service account maps to a GCP service account. Locally, `gcloud auth application-default login` sets up ADC for development. The application code is identical in all three cases — only the IAM binding changes."
          },
          {
            "type": "step",
            "title": "Runtime Secret Access: SecretManagerTemplate for Rotation and Per-Tenant Keys",
            "content": "Property source resolution handles startup-time secrets, but some scenarios require fetching secrets at runtime: **rotating encryption keys** without restarting the application, **per-tenant API keys** where the secret name is dynamic, or **secret creation** during automated onboarding flows.\n\n`SecretManagerTemplate` provides programmatic access for these cases. Unlike the property source, it can fetch specific versions, create new secrets, and add versions — all at runtime.\n\n```java\n@Service\npublic class EncryptionKeyService {\n    private final SecretManagerTemplate secretManager;\n\n    public EncryptionKeyService(SecretManagerTemplate secretManager) {\n        this.secretManager = secretManager;\n    }\n\n    public String getCurrentKey(String tenantId) {\n        // Dynamic secret name based on tenant\n        return secretManager.getSecretString(\n            \"projects/my-project/secrets/encrypt-key-\" + tenantId + \"/versions/latest\");\n    }\n\n    public void rotateKey(String tenantId, byte[] newKey) {\n        String secretId = \"encrypt-key-\" + tenantId;\n        secretManager.createSecretIfMissing(secretId);\n        secretManager.addSecretVersion(secretId, newKey);\n    }\n}\n```\n\nFor automated rotation, the pattern is: rotate the secret in Secret Manager, then trigger a redeployment (on Cloud Run, deploy a new revision pointing to the new version) or a Spring Cloud Config refresh (which re-resolves the property source). A Pub/Sub notification on secret version creation can automate this trigger, giving you fully automated credential rotation without manual intervention or plaintext exposure at any point in the chain."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **`sm://` prefix in Spring properties resolves from Secret Manager** — `${sm://my-db-password}` fetches the latest secret version at ApplicationContext startup; business code sees a plain string value\n- **`Secret Manager Secret Accessor` IAM role required** — grant to the Cloud Run service account or GKE Workload Identity service account; least-privilege access to specific secret names, not all secrets\n- **Secret versioning enables zero-downtime rotation** — create new version, update the resource (database password), disable old version; running applications use the cached startup value; rolling restart picks up the new version\n- **Workload Identity for GKE** — map a Kubernetes service account to a GCP service account with the Secret Accessor role; pods authenticate as the GCP service account without storing key files\n- **Never use env vars for secrets in Cloud Run revision config** — environment variable values appear in Cloud Run revision metadata and `gcloud run revisions describe` output; Secret Manager values do not"
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "GCP Secret Manager stores secrets (database passwords, API keys, OAuth tokens) with versioning, audit logging, and IAM-controlled access. Spring Cloud GCP integrates it as a Spring `PropertySource` so secrets appear as regular application properties.\n\n**Spring Cloud GCP integration**\n- Add `spring-cloud-gcp-starter-secretmanager`; configure `spring.cloud.gcp.secretmanager.project-id`; reference secrets with the `sm://` prefix: `spring.datasource.password=${sm://my-db-password}` resolves the secret at startup\n- Spring Cloud GCP fetches the secret value from Secret Manager when the ApplicationContext initializes — the resolved value is available as a standard Spring property, so `@Value(\"${spring.datasource.password}\")` works without any Secret Manager SDK calls in business code\n\n**Secret versioning**\n- Secret Manager maintains versions — `sm://my-db-password` resolves the latest enabled version; `sm://my-db-password/1` resolves version 1 explicitly\n- During rotation: create a new version of the secret, update the database password to match, then disable the old version; running applications continue using the old version (cached at startup) until restarted\n\n**Authentication**\n- Secret Manager access requires the `Secret Manager Secret Accessor` IAM role — grant it to the service account used by Cloud Run or GKE; on GKE with Workload Identity, the Kubernetes service account maps to a GCP service account with the role assigned\n- Application Default Credentials (`GOOGLE_APPLICATION_CREDENTIALS` locally, or the metadata server on GKE/Cloud Run) handles authentication automatically\n\n**Secret Manager vs environment variables**\n- Never store secrets as environment variables in Cloud Run or GKE pod specs — they appear in deployment logs, kubectl describe output, and Cloud Run revision configuration; Secret Manager keeps them out of the platform control plane"
          }
        ]
      },
      "followup_questions": [
        "How do you implement automatic secret rotation in a Spring Boot application on Cloud Run?",
        "What is the difference between Secret Manager and Cloud KMS in GCP?",
        "How do you test Spring Boot code that depends on Secret Manager properties locally?"
      ],
      "seo": {
        "metaTitle": "GCP Secret Manager with Spring Boot — Setup and Best Practices",
        "metaDescription": "Configure Google Cloud Secret Manager in Spring Boot using the sm:// property source, SecretManagerTemplate, and IAM least-privilege service accounts."
      },
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P10",
        "hook": "GCP Secret Manager stores secrets (database passwords, API keys, OAuth tokens) with versioning, audit logging, and IAM-controlled access.",
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "Every Spring Boot application needs database passwords, API keys, and encryption secrets. The naive approach — hardcoding them in application.yml or passing them as environment variables — creates a sprawling attack surface: credentials in git history, in CI logs (leaked via env dumps), in container inspect output, and in deployment manifests anyone with cluster access can read. Secret Manager solves this by storing credentials in a versioned, auditable, IAM-controlled vault that Spring Boot reads at startup through a custom property."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "Security-conscious configuration management — understanding how to keep credentials out of code and environment variables while integrating cleanly with the Spring property system."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "sm:// prefix in Spring properties resolves from Secret Manager — ${sm://my-db-password} fetches the latest secret version at ApplicationContext startup",
              "business code sees a plain string value Secret Manager Secret Accessor IAM role required — grant to the Cloud Run service account or GKE Workload Identity service account",
              "least-privilege access to specific secret names, not all secrets Secret versioning enables zero-downtime rotation — create new version, update the resource (database password), disable old version",
              "running applications use the cached startup value",
              "rolling restart picks up the new version Workload Identity for GKE — map a Kubernetes service account to a GCP service account with the Secret Accessor role",
              "pods authenticate as the GCP service account without storing key files Never use env vars for secrets in Cloud Run revision config — environment variable values appear in Cloud Run revision metadata and gcloud run revisions describe output",
              "Secret Manager values do not"
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "Every Spring Boot application needs database passwords, API keys, and encryption secrets. The naive approach — hardcoding them in application.yml or passing them as environment variables — creates a sprawling attack surface: credentials in git history, in CI logs (leaked via env dumps), in container inspect output, and in deployment manifests anyone with cluster access can read. Secret Manager solves this by storing credentials in a versioned, auditable, IAM-controlled vault that Spring Boot reads at startup through a custom property source. Your application code references ${sm://db-password} exactly like any other Spring property — the resolution to the actual secret value happens transparently. No code changes, no special APIs in your domain logic. But the integration has two distinct modes that."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Hard-coding the secret version (:latest vs a pinned version number), which prevents rollback, or granting the app's service account the secretmanager",
              "admin role instead of the minimal secretmanager",
              "secretAccessor role"
            ]
          }
        ],
        "cap": "Spring Cloud GCP integration Add spring-cloud-gcp-starter-secretmanager; configure spring.cloud.gcp.secretmanager.project-id; reference secrets with the sm:// prefix: spring.datasource.password=${sm:/",
        "familiarity_anchors": [
          "Spring",
          "Cloud",
          "Secret"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": [
          "How do you implement automatic secret rotation in a Spring Boot application on Cloud Run?",
          "What is the difference between Secret Manager and Cloud KMS in GCP?",
          "How do you test Spring Boot code that depends on Secret Manager properties locally?"
        ],
        "audience_assumption": "familiar",
        "voice": "friendly",
        "standard_example": "Spring Boot app deployed on GCP Cloud Run"
      }
    },
    {
      "id": "gcp-008",
      "slug": "gcp-iam-authentication-spring-boot",
      "question": "How do you implement GCP IAM authentication in Spring Boot?",
      "title": "Implementing GCP IAM Authentication in Spring Boot",
      "direct_answer": "GCP IAM authentication in Spring Boot is primarily handled through Application Default Credentials (ADC) and service accounts. Locally, ADC uses the credentials from `gcloud auth application-default login`; on GCP infrastructure it automatically uses the attached service account. For service-to-service authentication, use identity tokens or the `GoogleCredentials` class to generate bearer tokens for HTTP calls.",
      "layout_type": "recipe-builder",
      "interviewer_intent": {
        "testing": "Understanding of GCP's credential chain, service account design, and how to implement secure service-to-service calls without managing long-lived keys.",
        "common_mistake": "Downloading JSON service account key files and committing them to repositories or baking them into Docker images — this is a significant security anti-pattern GCP's Workload Identity was designed to eliminate.",
        "to_stand_out": "Explain Workload Identity Federation for GKE, the difference between access tokens and identity tokens, and how to use `google-auth-library-java` to obtain tokens programmatically for custom HTTP clients."
      },
      "company_tags": [
        "Google",
        "Confluent",
        "HashiCorp",
        "Grafana Labs"
      ],
      "reading_time_minutes": 5,
      "last_updated": "2025-01-15",
      "difficulty": "hard",
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "The Security Anti-Pattern GCP's Entire Auth Model Was Built to Eliminate",
            "content": "The most common GCP authentication mistake — and the one Workload Identity was specifically designed to fix — is downloading a JSON service account key file, committing it to a repository or baking it into a Docker image, and referencing it via `GOOGLE_APPLICATION_CREDENTIALS`. That key file is a **long-lived private key** that never expires, cannot be rotated without application changes, and gives anyone who obtains it full access to whatever the service account can do. It's the cloud equivalent of hardcoding a database password.\n\nGCP's credential model is built on a single principle: **the platform should provide credentials to the application automatically, without files or environment variables**. On Cloud Run, the attached service account injects credentials via the instance metadata server. On GKE, Workload Identity binds a Kubernetes service account to a GCP service account at the IAM level. Locally, `gcloud auth application-default login` populates ADC for development.\n\nThe Java client libraries read credentials through the **Application Default Credentials (ADC) chain** — a prioritized sequence of credential sources. Understanding this chain is the foundation of GCP authentication, and the application code is identical regardless of where it runs."
          },
          {
            "type": "phase",
            "title": "The ADC Resolution Chain: How the SDK Finds Credentials",
            "content": "When any GCP client library initializes — `BigQueryOptions.getDefaultInstance()`, `StorageOptions.getDefaultInstance()`, `PubSubTemplate` — it walks the ADC chain in order until it finds credentials.\n\nThe chain resolves in this priority:\n\n1. **`GOOGLE_APPLICATION_CREDENTIALS` environment variable** — points to a JSON key file. This is the escape hatch for development and rare break-glass scenarios, not for production.\n\n2. **User credentials from `gcloud`** — `gcloud auth application-default login` writes credentials to `~/.config/gcloud/application_default_credentials.json`. Used for local development.\n\n3. **Attached service account** — on Compute Engine, Cloud Run, App Engine, and Cloud Functions, the instance metadata server provides credentials for the attached service account. On GKE with Workload Identity, the metadata server is intercepted by the Workload Identity agent.\n\n4. **Cloud Shell credentials** — automatically available in Cloud Shell.\n\nThe critical insight is that **steps 3 and 4 require zero configuration in your application**. No environment variables, no files, no Spring properties. The code is simply:\n\n```java\nBigQuery bigQuery = BigQueryOptions.getDefaultInstance().getService();\nStorage storage = StorageOptions.getDefaultInstance().getService();\n```\n\nSpring Cloud GCP starters follow the same chain — `PubSubTemplate`, `SecretManagerTemplate`, and Spring Data modules all authenticate via ADC transparently. The only Spring property you might set is `spring.cloud.gcp.project-id`, and even that is auto-detected on GCP infrastructure."
          },
          {
            "type": "step",
            "title": "Workload Identity on GKE: Keyless Authentication for Kubernetes Pods",
            "content": "On GKE, the default credential mechanism attaches a node-level service account to all pods on that node — meaning every pod shares the same permissions. **Workload Identity** solves this by creating a binding between a Kubernetes service account and a GCP service account, giving each workload its own identity.\n\nThe setup involves three IAM operations: enabling Workload Identity on the cluster, creating a GCP service account with the minimum required roles, and binding the Kubernetes service account to it.\n\n```bash\n# Enable Workload Identity pool on the cluster\ngcloud container clusters update my-cluster \\\n  --workload-pool=my-project.svc.id.goog\n\n# Create a GCP SA with only the roles this service needs\ngcloud iam service-accounts create order-sa\ngcloud projects add-iam-policy-binding my-project \\\n  --member=\"serviceAccount:order-sa@my-project.iam.gserviceaccount.com\" \\\n  --role=\"roles/pubsub.publisher\"\n\n# Bind the K8s SA to the GCP SA\ngcloud iam service-accounts add-iam-policy-binding \\\n  order-sa@my-project.iam.gserviceaccount.com \\\n  --role roles/iam.workloadIdentityUser \\\n  --member \"serviceAccount:my-project.svc.id.goog[orders/order-sa]\"\n```\n\nThe Kubernetes service account needs a single annotation to complete the binding. After this, any pod using this service account authenticates as `order-sa@my-project.iam.gserviceaccount.com` through the metadata server — no key files, no mounted secrets.\n\n```yaml\napiVersion: v1\nkind: ServiceAccount\nmetadata:\n  name: order-sa\n  namespace: orders\n  annotations:\n    iam.gke.io/gcp-service-account: order-sa@my-project.iam.gserviceaccount.com\n```\n\nThe Java application code doesn't change at all — `BigQueryOptions.getDefaultInstance()` picks up the Workload Identity credentials through the same ADC chain. The only difference is the IAM binding, which happens at the infrastructure level."
          },
          {
            "type": "step",
            "title": "Access Tokens vs Identity Tokens: The Distinction That Matters for Service-to-Service Calls",
            "content": "GCP uses two types of tokens, and confusing them is a common source of \"403 Forbidden\" errors in service-to-service calls.\n\n**Access tokens** authorize calls to GCP APIs — Pub/Sub, Cloud Storage, BigQuery. They're scoped to API permissions and are what ADC provides by default. You never manually handle these; the client libraries manage them.\n\n**Identity tokens (OIDC)** prove your service's identity to another HTTP service. When Service A calls Service B on Cloud Run, Service A presents an identity token with Service B's URL as the audience. Service B (if configured with `--no-allow-unauthenticated`) validates the token and confirms the caller is who they claim to be.\n\nThe Java implementation uses `IdTokenCredentials` from the `google-auth-library`.\n\n```java\n@Service\npublic class PaymentServiceClient {\n    private static final String PAYMENT_URL = \"https://payment-svc-abc.run.app\";\n\n    public PaymentResult charge(PaymentRequest req) throws IOException {\n        IdTokenCredentials creds = IdTokenCredentials.newBuilder()\n            .setIdTokenProvider(\n                (IdTokenProvider) ServiceAccountCredentials.getApplicationDefault())\n            .setTargetAudience(PAYMENT_URL) // audience = the service being called\n            .build();\n        creds.refresh();\n\n        HttpHeaders headers = new HttpHeaders();\n        headers.setBearerAuth(creds.getIdToken().getTokenValue());\n\n        return restTemplate.exchange(\n            PAYMENT_URL + \"/api/charge\", HttpMethod.POST,\n            new HttpEntity<>(req, headers), PaymentResult.class).getBody();\n    }\n}\n```\n\nThe `targetAudience` must match the URL of the service being called — Cloud Run validates this automatically. On the receiving side, Cloud Run handles token verification when deployed with `--no-allow-unauthenticated`, so Service B doesn't need any auth code. For custom verification (e.g., extracting the caller's service account email for authorization logic), use `GoogleIdTokenVerifier` to parse and validate the token in a Spring filter."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Application Default Credentials resolves from environment automatically** — metadata server on GKE/Cloud Run; `gcloud auth application-default login` locally; same code works in both environments\n- **One service account per application with least-privilege IAM** — `roles/cloudsql.client` for Cloud SQL; `roles/storage.objectCreator` for a specific bucket; not `roles/owner` or `roles/editor`\n- **Workload Identity for GKE** — Kubernetes service account maps to GCP service account; no key files in pod specs or Kubernetes secrets; credentials rotated by the platform\n- **Never commit service account key files** — key files are permanent credentials; if committed, they must be immediately revoked and rotated; use Workload Identity or ADC instead\n- **Resource-level IAM conditions** — grant roles on specific resource ARNs, not the entire project; `roles/storage.objectViewer` on one bucket, not all Cloud Storage in the project"
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "GCP authentication for Spring Boot follows one principle: let the platform provide credentials, never manage keys manually. Application Default Credentials (ADC) implements this by resolving credentials from the environment automatically.\n\n**Application Default Credentials — the resolution chain**\n- ADC resolves credentials in order: GOOGLE_APPLICATION_CREDENTIALS environment variable (points to a service account key file), then gcloud user credentials (for local development), then the metadata server (for GKE, Cloud Run, Compute Engine)\n- Which means on Cloud Run and GKE, your Spring Boot code calls `GoogleCredentials.getApplicationDefault()` or uses any GCP client library, and credentials are resolved from the platform metadata server — no key files, no environment variables\n\n**Service accounts**\n- A service account is a non-human GCP identity — create one per application with only the IAM roles it needs; grant `roles/cloudsql.client` for Cloud SQL access, `roles/storage.objectCreator` for writes to a specific bucket\n- On GKE, use Workload Identity to bind a Kubernetes service account to a GCP service account — pods that use that Kubernetes service account automatically authenticate as the GCP service account\n\n**Local development with gcloud**\n- `gcloud auth application-default login` stores user credentials locally; ADC picks them up when running locally; the same code that uses ADC in production works locally without any credential configuration changes\n- For testing with a specific service account locally: download a service account key (JSON file), set `GOOGLE_APPLICATION_CREDENTIALS=/path/to/key.json` — but delete the key file after testing; never commit it\n\n**Least-privilege IAM**\n- GCP IAM supports granular conditions — grant access only to specific resources: `roles/storage.objectCreator` on `projects/my-project/buckets/uploads-bucket` restricts the role to one bucket, not all Cloud Storage\n- Use `gcloud projects get-iam-policy` and `gcloud asset search-all-iam-policies` to audit which service accounts have which roles"
          }
        ]
      },
      "followup_questions": [
        "What is the difference between an IAM access token and an OIDC identity token in GCP?",
        "How do you use Workload Identity Federation to authenticate from GitHub Actions to GCP?",
        "How do you implement service account impersonation in Java for cross-project access?"
      ],
      "seo": {
        "metaTitle": "GCP IAM Authentication in Spring Boot — Service Accounts and Workload Identity",
        "metaDescription": "Implement GCP IAM authentication in Spring Boot using Application Default Credentials, Workload Identity on GKE, and OIDC identity tokens for service-to-service calls."
      },
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P10",
        "hook": "GCP authentication for Spring Boot follows one principle: let the platform provide credentials, never manage keys manually.",
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "The most common GCP authentication mistake — and the one Workload Identity was specifically designed to fix — is downloading a JSON service account key file, committing it to a repository or baking it into a Docker image, and referencing it via GOOGLE_APPLICATION_CREDENTIALS. That key file is a long-lived private key that never expires, cannot be rotated without application changes, and gives anyone who obtains it full access to whatever the service account can do. It's the cloud equivalent of."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "Understanding of GCP's credential chain, service account design, and how to implement secure service-to-service calls without managing long-lived keys."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "Application Default Credentials resolves from environment automatically — metadata server on GKE/Cloud Run; gcloud auth application-default login locally; same code works in both environments One service account per application with least-privilege IAM — roles/cloudsql",
              "client for Cloud SQL; roles/storage",
              "objectCreator for a specific bucket; not roles/owner or roles/editor Workload Identity for GKE — Kubernetes service account maps to GCP service account; no key files in pod specs or Kubernetes secrets; credentials rotated by the platform Never commit service account key files — key files are permanent credentials; if committed, they must be immediately revoked and rotated; use Workload Identity or ADC instead Resource-level IAM conditions — grant roles on specific resource ARNs, not the entire project; roles/storage",
              "objectViewer on one bucket, not all Cloud Storage in the project"
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "The most common GCP authentication mistake — and the one Workload Identity was specifically designed to fix — is downloading a JSON service account key file, committing it to a repository or baking it into a Docker image, and referencing it via GOOGLE_APPLICATION_CREDENTIALS. That key file is a long-lived private key that never expires, cannot be rotated without application changes, and gives anyone who obtains it full access to whatever the service account can do. It's the cloud equivalent of hardcoding a database password. GCP's credential model is built on a single principle: the platform should provide credentials to the application automatically, without files or environment variables. On Cloud Run, the attached service account injects credentials via the instance metadata."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Downloading JSON service account key files and committing them to repositories or baking them into Docker images",
              "this is a significant security anti-pattern GCP's Workload Identity was designed to eliminate"
            ]
          }
        ],
        "cap": "Application Default Credentials — the resolution chain ADC resolves credentials in order: GOOGLE_APPLICATION_CREDENTIALS environment variable (points to a service account key file), then gcloud user c",
        "familiarity_anchors": [
          "Spring",
          "Application",
          "Default"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": [
          "What is the difference between an IAM access token and an OIDC identity token in GCP?",
          "How do you use Workload Identity Federation to authenticate from GitHub Actions to GCP?",
          "How do you implement service account impersonation in Java for cross-project access?"
        ],
        "audience_assumption": "familiar",
        "voice": "friendly",
        "standard_example": "Spring Boot app deployed on GCP Cloud Run"
      }
    }
  ]
}

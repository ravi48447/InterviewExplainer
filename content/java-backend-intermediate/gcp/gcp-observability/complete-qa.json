{
  "topic": "Gcp And Azure Overview",
  "topicSlug": "gcp-and-azure-overview",
  "questions": [
    {
      "id": "gcp-010",
      "slug": "monitor-java-applications-gcp-cloud-monitoring",
      "question": "How do you monitor Java applications on GCP with Cloud Monitoring?",
      "title": "Monitoring Java Applications on GCP with Cloud Monitoring",
      "direct_answer": "Integrate Cloud Monitoring into Spring Boot by adding the Micrometer Google Cloud Monitoring registry, which automatically exports Actuator metrics as custom metrics. Supplement with structured logging to Cloud Logging via Logback, distributed tracing via Cloud Trace, and uptime checks or alerting policies in Cloud Monitoring to create a complete observability stack.",
      "layout_type": "recipe-builder",
      "interviewer_intent": {
        "testing": "Observability maturity — whether the candidate understands the three pillars of observability (metrics, logs, traces) and can implement them in a Spring Boot application on GCP without standing up additional infrastructure.",
        "common_mistake": "Relying solely on application logs for debugging production issues, or not correlating trace IDs across log entries and distributed traces, making root cause analysis in microservices difficult.",
        "to_stand_out": "Discuss Service Level Indicators (SLIs), error budget alerting, and how to use Cloud Monitoring's Metrics Explorer to build dashboards correlating JVM metrics with infrastructure metrics."
      },
      "company_tags": [
        "Google",
        "Wix",
        "Typeform",
        "Rappi"
      ],
      "reading_time_minutes": 5,
      "last_updated": "2025-01-15",
      "difficulty": "medium",
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Why Logs Alone Aren't Observability: The Three Pillars and How They Connect",
            "content": "The most common monitoring anti-pattern is treating logs as the entire observability strategy. Developers add `log.info` everywhere, pipe everything to Cloud Logging, and call it done. Then a user reports that the checkout page is slow. You grep the logs, find thousands of entries, but can't answer the basic question: **which service in the request chain is the bottleneck?**\n\nObservability requires three pillars working together: **metrics** tell you what's wrong (error rate spiked, latency increased, heap is full), **traces** tell you where it's wrong (the payment service call took 8 seconds), and **logs** tell you why it's wrong (the connection pool was exhausted because a query didn't have a timeout).\n\nOn GCP, these three pillars map to: **Cloud Monitoring** via Micrometer for metrics, **Cloud Trace** via Spring Cloud Sleuth/Micrometer Tracing for distributed traces, and **Cloud Logging** via structured JSON logging for logs. The critical integration point is the **trace ID** — every log entry includes the trace ID, and Cloud Logging links directly to the Cloud Trace waterfall diagram. This correlation is what turns three separate data streams into a single debugging workflow."
          },
          {
            "type": "step",
            "title": "Metrics: Micrometer Registry Turns Actuator Into Cloud Monitoring Dashboards",
            "content": "Spring Boot Actuator already collects dozens of JVM and application metrics — heap usage, GC pause time, HTTP request latency histograms, thread counts, connection pool sizes. The Micrometer Cloud Monitoring registry exports all of these as custom metrics under the `custom.googleapis.com/` namespace, where they appear in Metrics Explorer for dashboards and alerting.\n\nThe setup takes two dependencies and a few lines of configuration. No code changes to your application — Micrometer instruments Spring MVC, WebClient, JDBC, and HikariCP automatically.\n\n```xml\n<dependency>\n  <groupId>org.springframework.boot</groupId>\n  <artifactId>spring-boot-starter-actuator</artifactId>\n</dependency>\n<dependency>\n  <groupId>com.google.cloud</groupId>\n  <artifactId>micrometer-registry-stackdriver</artifactId>\n</dependency>\n```\n\n```yaml\nmanagement:\n  metrics:\n    export:\n      stackdriver:\n        enabled: true\n        project-id: my-project\n        step: 60s  # push interval\n    tags:\n      application: order-service\n      environment: production\n```\n\nThe `step: 60s` push interval balances cost and resolution — custom metrics are priced per time series, so pushing every 10 seconds with many tags can get expensive. The `tags` are global labels attached to every metric, enabling filtering by application and environment in dashboards. After adding this, you can build Metrics Explorer dashboards showing JVM heap alongside HTTP latency percentiles and database connection pool utilization — correlations that are impossible with logs alone."
          },
          {
            "type": "step",
            "title": "Structured Logging: From grep to Query-Based Debugging",
            "content": "Plain-text logs are effectively unsearchable at scale. Cloud Logging with structured JSON logging transforms logs into queryable data — you can filter by severity, service, trace ID, or any custom field you add via MDC.\n\nThe Spring Cloud GCP logging starter formats log entries as JSON and includes the trace ID automatically when tracing is enabled. This means every log entry links to its distributed trace in Cloud Trace.\n\n```xml\n<dependency>\n  <groupId>com.google.cloud</groupId>\n  <artifactId>spring-cloud-gcp-starter-logging</artifactId>\n</dependency>\n```\n\nCustom context via MDC (Mapped Diagnostic Context) adds business-relevant fields to every log entry within a request scope. This is the difference between grepping for \"error\" across millions of lines and querying `jsonPayload.orderId=\"ORD-12345\"` to see every log entry for one specific order across all services.\n\n```java\n@Component\npublic class RequestContextFilter extends OncePerRequestFilter {\n    @Override\n    protected void doFilterInternal(HttpServletRequest req,\n            HttpServletResponse res, FilterChain chain)\n            throws ServletException, IOException {\n        MDC.put(\"orderId\", req.getHeader(\"X-Order-Id\"));\n        MDC.put(\"userId\", req.getHeader(\"X-User-Id\"));\n        try {\n            chain.doFilter(req, res);\n        } finally {\n            MDC.clear(); // prevent context leaking to next request\n        }\n    }\n}\n```\n\nThe `MDC.clear()` in the finally block is critical — without it, thread pool reuse in Tomcat would leak one request's context into the next request's logs, making debugging even more confusing than having no context at all."
          },
          {
            "type": "step",
            "title": "Distributed Tracing and Alerting: Connecting the Dots Across Services",
            "content": "Distributed tracing instruments the request path across service boundaries. When the order service calls inventory, which calls warehouse, a single trace ID follows the request through all three services. Cloud Trace renders this as a waterfall diagram showing exactly how long each service took.\n\nSpring Cloud Sleuth (or Micrometer Tracing in newer Spring Boot versions) instruments `RestTemplate`, `WebClient`, and Spring MVC automatically. The trace ID propagates in HTTP headers and appears in log entries.\n\n```yaml\nspring:\n  cloud:\n    gcp:\n      trace:\n        enabled: true\n  sleuth:\n    sampler:\n      probability: 0.1  # sample 10% of requests\n```\n\nThe sampling rate is a cost and performance trade-off. At 100% sampling, every request generates trace data — expensive for high-throughput services and measurably increases latency. At 10%, you capture enough to diagnose patterns without the overhead. For latency-sensitive services processing thousands of requests per second, 1% is often sufficient.\n\n**Alerting on the four golden signals** closes the loop. In Cloud Monitoring, create alerting policies for:\n\n- **Error rate**: HTTP 5xx responses exceeding 1% of total traffic\n- **Latency**: p99 response time exceeding your SLO (e.g., 500ms)\n- **Throughput**: sudden drops in requests per second (indicates upstream failures)\n- **Saturation**: HikariCP active connections at pool maximum, JVM heap above 85%\n\nFor mature teams, **SLO monitoring** expresses these as error budgets: \"the order service must have 99.9% availability this month, which means we can tolerate 43 minutes of downtime.\" Cloud Monitoring's SLO feature calculates burn rate and alerts when you're consuming the budget faster than expected — giving the team an objective, non-reactive signal to address reliability before customers are impacted."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **`micrometer-registry-stackdriver` auto-publishes JVM + HTTP metrics** — add dependency and project-id config; no Cloud Monitoring SDK calls; metrics appear in Cloud Monitoring dashboards within 60 seconds\n- **Four golden signals: rate, errors, latency, saturation** — `http.server.requests.count`, 5xx error rate, P99 latency, and JVM heap saturation are the minimum dashboard for any Spring Boot service\n- **Alert on SLO violations, not individual metric thresholds** — error budget burn rate alerts trigger before the SLO is violated; raw metric threshold alerts produce noise on transient spikes\n- **Cloud Run and GKE auto-collect stdout logs** — no Logstash or Filebeat configuration; JSON logs sent to stdout appear in Cloud Logging automatically; structured JSON fields are queryable in the Logging Query Language\n- **Trace correlation with Cloud Trace** — Spring Cloud GCP's Sleuth integration propagates trace IDs; Cloud Trace collects distributed traces; latency breakdowns across microservices without manual instrumentation"
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "GCP Cloud Monitoring integrates with Spring Boot through Micrometer — the `micrometer-registry-stackdriver` registry pushes all application metrics to Cloud Monitoring automatically, giving you JVM, HTTP, and HikariCP metrics without manual Cloud Monitoring SDK calls.\n\n**Publishing metrics with Micrometer**\n- Add `micrometer-registry-stackdriver` and configure `management.metrics.export.stackdriver.project-id`; Micrometer pushes all registered meters to Cloud Monitoring every 60 seconds\n- JVM metrics (heap usage, GC pauses, thread count), HTTP request metrics (count, latency, error rate), and HikariCP pool metrics appear automatically — no custom instrumentation needed for the common signals\n\n**Custom metrics**\n- `Counter.builder(\"orders.placed\").register(meterRegistry).increment()` adds a custom counter; `Timer.builder(\"checkout.duration\").register(meterRegistry).record(duration)` adds a latency distribution\n- Tag metrics with labels — `tags(\"service\", \"order-service\", \"env\", \"prod\")` — which means Cloud Monitoring dashboards can filter and group by service and environment\n\n**Cloud Monitoring dashboards and alerting**\n- Create dashboards for the four golden signals: request rate (`http.server.requests.count`), error rate (`http.server.requests{status=5xx}`), latency (`http.server.requests.p99`), and saturation (`jvm.memory.used` / `jvm.memory.max`)\n- Alerting policies on metric threshold violations send notifications to PagerDuty, Slack, or email via notification channels; configure alert conditions with `alignment_period` and `cross_series_reducer` to avoid alert noise from transient spikes\n\n**Cloud Logging integration**\n- Spring Boot JSON logs shipped to stdout are automatically collected by GKE's logging agent and Cloud Run's built-in log collection; they appear in Cloud Logging queryable with the Logging Query Language\n- `resource.type=\"cloud_run_revision\" AND severity=\"ERROR\"` filters errors from a specific Cloud Run service; combine with metrics alerts for correlated observability"
          }
        ]
      },
      "followup_questions": [
        "How do you use Cloud Profiler to diagnose memory leaks in a Java application?",
        "How do you create log-based metrics in Cloud Logging and use them in Cloud Monitoring alerts?",
        "What is the difference between Cloud Trace sampling rate and how should you set it for a high-traffic service?"
      ],
      "seo": {
        "metaTitle": "Monitoring Java Apps on GCP with Cloud Monitoring — Micrometer, Traces, and Alerts",
        "metaDescription": "Set up complete observability for Spring Boot on GCP using Micrometer metrics, Cloud Logging structured logs, Cloud Trace distributed tracing, and Cloud Monitoring alerting policies."
      },
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P10",
        "hook": "GCP Cloud Monitoring integrates with Spring Boot through Micrometer — the micrometer-registry-stackdriver registry pushes all application metrics to Cloud Monitoring automatically, giving you JVM, HTTP, and HikariCP metrics without manual Cloud Monitoring SDK calls.",
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "The most common monitoring anti-pattern is treating logs as the entire observability strategy. Developers add log.info everywhere, pipe everything to Cloud Logging, and call it done. Then a user reports that the checkout page is slow. You grep the logs, find thousands of entries, but can't answer the basic question: which service in the request chain is the bottleneck? Observability requires three pillars working together: metrics tell you what's wrong (error rate spiked, latency increased, heap is full), traces tell."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "Observability maturity — whether the candidate understands the three pillars of observability (metrics, logs, traces) and can implement them in a Spring Boot application on GCP without standing up additional infrastructure."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "micrometer-registry-stackdriver auto-publishes JVM + HTTP metrics — add dependency and project-id config; no Cloud Monitoring SDK calls; metrics appear in Cloud Monitoring dashboards within 60 seconds Four golden signals: rate, errors, latency, saturation — http",
              "count, 5xx error rate, P99 latency, and JVM heap saturation are the minimum dashboard for any Spring Boot service Alert on SLO violations, not individual metric thresholds — error budget burn rate alerts trigger before the SLO is violated; raw metric threshold alerts produce noise on transient spikes Cloud Run and GKE auto-collect stdout logs — no Logstash or Filebeat configuration; JSON logs sent to stdout appear in Cloud Logging automatically; structured JSON fields are queryable in the Logging Query Language Trace correlation with Cloud Trace — Spring Cloud GCP's Sleuth integration propagates trace IDs; Cloud Trace collects distributed traces; latency breakdowns across microservices without manual instrumentation"
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "The most common monitoring anti-pattern is treating logs as the entire observability strategy. Developers add log.info everywhere, pipe everything to Cloud Logging, and call it done. Then a user reports that the checkout page is slow. You grep the logs, find thousands of entries, but can't answer the basic question: which service in the request chain is the bottleneck? Observability requires three pillars working together: metrics tell you what's wrong (error rate spiked, latency increased, heap is full), traces tell you where it's wrong (the payment service call took 8 seconds), and logs tell you why it's wrong (the connection pool was exhausted because a query didn't have a timeout). On GCP, these three pillars map to: Cloud Monitoring via."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Relying solely on application logs for debugging production issues, or not correlating trace IDs across log entries and distributed traces, making root cause analysis in microservices difficult"
            ]
          }
        ],
        "cap": "Publishing metrics with Micrometer Add micrometer-registry-stackdriver and configure management.metrics.export.stackdriver.project-id; Micrometer pushes all registered meters to Cloud Monitoring every",
        "familiarity_anchors": [
          "Integrate",
          "Cloud",
          "Monitoring"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": [
          "How do you use Cloud Profiler to diagnose memory leaks in a Java application?",
          "How do you create log-based metrics in Cloud Logging and use them in Cloud Monitoring alerts?",
          "What is the difference between Cloud Trace sampling rate and how should you set it for a high-traffic service?"
        ],
        "audience_assumption": "familiar",
        "voice": "friendly",
        "standard_example": "Spring Boot app deployed on GCP Cloud Run"
      }
    }
  ]
}

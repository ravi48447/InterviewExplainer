{
  "topic": "Go Binary Deployment",
  "topicSlug": "go-binary-deployment",
  "questions": [
    {
      "id": "cloud-deployment-go-go-binary-deployment-q001",
      "slug": "cloud-deployment-go-go-binary-deployment-interview-basics",
      "question": "What should a Go developer know about deploying a Go binary?",
      "title": "What should a Go developer know about deploying a Go binary",
      "direct_answer": "A Go deployment usually promotes a compiled executable built from a known source revision and module graph. The release also defines its target operating system and architecture, runtime configuration, service identity, network address, files, and shutdown behaviour. `go build` translates packages and linked dependencies into an executable for the selected `GOOS` and `GOARCH`. CGO, build tags, embedded files, version metadata, and compiler version can all affect what the artifact needs at runtime.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 4,
      "interviewer_intent": {
        "testing": "Whether the learner understands the real rules and trade-offs behind deploying a Go binary.",
        "common_mistake": "Failures often come from compiling for the wrong platform, relying on unavailable dynamic libraries, forgetting certificates or migrations, binding only to localhost, embedding secrets, or replacing the binary without a safe restart and rollback plan.",
        "to_stand_out": "Connect the rule to the concrete example and state its important boundary."
      },
      "answer": {
        "sections": [
          {
            "type": "key_points",
            "title": "Quick revision",
            "items": [
              "A Go deployment usually promotes a compiled executable built from a known source revision and module graph. The release also defines its target operating system and architecture, runtime configuration, service identity, network address, files, and shutdown behaviour.",
              "`go build` translates packages and linked dependencies into an executable for the selected `GOOS` and `GOARCH`. CGO, build tags, embedded files, version metadata, and compiler version can all affect what the artifact needs at runtime.",
              "The executable is the main release artifact.",
              "Target OS and architecture are build inputs.",
              "CGO can add native runtime dependencies.",
              "Configuration belongs outside the artifact."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "answerSize": "compact",
            "content": "A Go deployment usually promotes a compiled executable built from a known source revision and module graph. The release also defines its target operating system and architecture, runtime configuration, service identity, network address, files, and shutdown behaviour.\n\n`go build` translates packages and linked dependencies into an executable for the selected `GOOS` and `GOARCH`. CGO, build tags, embedded files, version metadata, and compiler version can all affect what the artifact needs at runtime.\n\nFor example, CI can build `CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -trimpath -o api ./cmd/api`, calculate a checksum, and publish that immutable artifact. Production verifies the checksum and provides environment-specific settings.\n\nA single executable does not contain the host kernel, external configuration, database schema, certificates, or every dynamically linked library. Static linking also does not remove the need for patching, provenance, access control, and observability.\n\nBuild once in CI and promote the same tested artifact through environments when reproducibility matters. Run it under a service manager, container runtime, or orchestrator that supplies configuration, restarts, logs, health checks, and resource controls."
          },
          {
            "type": "deep_explanation",
            "title": "The core model: deploying a Go binary",
            "content": "deploying a Go binary is easiest to understand by following the data or control from input to result. The details below form one connected model rather than a list of unrelated rules.\n\nCompilation makes Go deployment compact, but the binary is only one part of the process contract. CI should produce the artifact from locked module requirements, run tests, record provenance, and publish a checksum or signed package. Cross-compilation is straightforward for pure-Go code but native dependencies can require a matching C toolchain and libraries. At runtime, the service needs a dedicated identity, a working directory only if the program assumes one, network and filesystem permissions, trusted certificates, configuration, and limits. The process manager should start the binary directly, collect standard output and error, and send termination signals. The application should stop accepting new requests, finish in-flight work within a deadline, and exit with a meaningful status. A rollout observes readiness before sending traffic and keeps the previous artifact available. These details turn “copy one binary” into a repeatable, reversible release instead of a manual server change."
          },
          {
            "type": "flow_diagram",
            "title": "deploying a Go binary flow",
            "content": "```mermaid\nflowchart LR\n  A[Source + go.mod] --> B[Test and build in CI]\n  B --> C[Versioned binary + checksum]\n  C --> D[Deploy unchanged artifact]\n  D --> E[Health check]\n  E --> F[Traffic or rollback]\n```"
          },
          {
            "type": "code_example",
            "title": "deploying a Go binary example",
            "content": "~~~sh\nCGO_ENABLED=0 GOOS=linux GOARCH=amd64 \\\n  go build -trimpath -ldflags \"-X main.version=$VERSION\" \\\n  -o dist/catalog-api ./cmd/catalog-api\nsha256sum dist/catalog-api\n~~~"
          },
          {
            "type": "comparison_table",
            "title": "deploying a Go binary at a glance",
            "content": "| Layer | Examples | Deployment check |\n|---|---|---|\n| Artifact | Binary, embedded assets | Checksum and target platform |\n| Runtime | User, files, certificates, port | Least privilege and startup test |\n| Lifecycle | Health, signals, rollback | Rehearsed under real process manager |"
          },
          {
            "type": "practice_prompt",
            "title": "Check the idea",
            "content": "Explain deploying a Go binary using the example above. Name the input, the rule that changes it, and the final result."
          }
        ]
      },
      "followup_questions": [
        "Which guarantee of deploying a Go binary matters most in this example?",
        "What boundary or failure case should be tested?",
        "When would shipping source code and compiling on the server be a clearer choice?"
      ],
      "order": 1,
      "seo": {
        "metaTitle": "What should a Go developer know about deploying a Go binary | InterviewExplainer",
        "metaDescription": "A Go deployment usually promotes a compiled executable built from a known source revision and module graph. The release also defines its target operating s..."
      }
    },
    {
      "id": "cloud-deployment-go-go-binary-deployment-q002",
      "slug": "cloud-deployment-go-go-binary-deployment-when-to-use",
      "question": "When is deploying a Go binary the right choice?",
      "title": "When is deploying a Go binary the right choice",
      "direct_answer": "Build once in CI and promote the same tested artifact through environments when reproducibility matters. Run it under a service manager, container runtime, or orchestrator that supplies configuration, restarts, logs, health checks, and resource controls. A single executable does not contain the host kernel, external configuration, database schema, certificates, or every dynamically linked library. Static linking also does not remove the need for patching, provenance, access control, and observability.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 4,
      "interviewer_intent": {
        "testing": "Whether the learner understands the real rules and trade-offs behind deploying a Go binary.",
        "common_mistake": "Failures often come from compiling for the wrong platform, relying on unavailable dynamic libraries, forgetting certificates or migrations, binding only to localhost, embedding secrets, or replacing the binary without a safe restart and rollback plan.",
        "to_stand_out": "Connect the rule to the concrete example and state its important boundary."
      },
      "answer": {
        "sections": [
          {
            "type": "key_points",
            "title": "Quick revision",
            "items": [
              "Build once in CI and promote the same tested artifact through environments when reproducibility matters. Run it under a service manager, container runtime, or orchestrator that supplies configuration, restarts, logs, health checks, and resource controls.",
              "A single executable does not contain the host kernel, external configuration, database schema, certificates, or every dynamically linked library. Static linking also does not remove the need for patching, provenance, access control, and observability.",
              "The executable is the main release artifact.",
              "Target OS and architecture are build inputs.",
              "CGO can add native runtime dependencies.",
              "Configuration belongs outside the artifact."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "answerSize": "compact",
            "content": "A Go deployment usually promotes a compiled executable built from a known source revision and module graph. The release also defines its target operating system and architecture, runtime configuration, service identity, network address, files, and shutdown behaviour.\n\nBuild once in CI and promote the same tested artifact through environments when reproducibility matters. Run it under a service manager, container runtime, or orchestrator that supplies configuration, restarts, logs, health checks, and resource controls.\n\nFor example, CI can build `CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -trimpath -o api ./cmd/api`, calculate a checksum, and publish that immutable artifact. Production verifies the checksum and provides environment-specific settings.\n\nA prebuilt artifact is fast to start and proves that every environment runs the same bits. Building on each server can adapt to that host, but it requires compilers and source there and makes releases harder to reproduce and audit.\n\nA single executable does not contain the host kernel, external configuration, database schema, certificates, or every dynamically linked library. Static linking also does not remove the need for patching, provenance, access control, and observability."
          },
          {
            "type": "deep_explanation",
            "title": "The practical decision: deploying a Go binary",
            "content": "The useful question is not whether deploying a Go binary can be used, but which requirement makes it the clearest and safest fit. That decision includes its cost and its failure boundary.\n\nCompilation makes Go deployment compact, but the binary is only one part of the process contract. CI should produce the artifact from locked module requirements, run tests, record provenance, and publish a checksum or signed package. Cross-compilation is straightforward for pure-Go code but native dependencies can require a matching C toolchain and libraries. At runtime, the service needs a dedicated identity, a working directory only if the program assumes one, network and filesystem permissions, trusted certificates, configuration, and limits. The process manager should start the binary directly, collect standard output and error, and send termination signals. The application should stop accepting new requests, finish in-flight work within a deadline, and exit with a meaningful status. A rollout observes readiness before sending traffic and keeps the previous artifact available. These details turn “copy one binary” into a repeatable, reversible release instead of a manual server change."
          },
          {
            "type": "flow_diagram",
            "title": "deploying a Go binary flow",
            "content": "```mermaid\nflowchart LR\n  A[Source + go.mod] --> B[Test and build in CI]\n  B --> C[Versioned binary + checksum]\n  C --> D[Deploy unchanged artifact]\n  D --> E[Health check]\n  E --> F[Traffic or rollback]\n```"
          },
          {
            "type": "code_example",
            "title": "deploying a Go binary example",
            "content": "~~~sh\nCGO_ENABLED=0 GOOS=linux GOARCH=amd64 \\\n  go build -trimpath -ldflags \"-X main.version=$VERSION\" \\\n  -o dist/catalog-api ./cmd/catalog-api\nsha256sum dist/catalog-api\n~~~"
          },
          {
            "type": "comparison_table",
            "title": "deploying a Go binary at a glance",
            "content": "| Layer | Examples | Deployment check |\n|---|---|---|\n| Artifact | Binary, embedded assets | Checksum and target platform |\n| Runtime | User, files, certificates, port | Least privilege and startup test |\n| Lifecycle | Health, signals, rollback | Rehearsed under real process manager |"
          },
          {
            "type": "practice_prompt",
            "title": "Check the idea",
            "content": "Write one requirement that favours deploying a Go binary and one requirement that would make shipping source code and compiling on the server a better fit."
          }
        ]
      },
      "followup_questions": [
        "Which guarantee of deploying a Go binary matters most in this example?",
        "What boundary or failure case should be tested?",
        "When would shipping source code and compiling on the server be a clearer choice?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When is deploying a Go binary the right choice | InterviewExplainer",
        "metaDescription": "Build once in CI and promote the same tested artifact through environments when reproducibility matters. Run it under a service manager, container runtime,..."
      }
    },
    {
      "id": "cloud-deployment-go-go-binary-deployment-q003",
      "slug": "cloud-deployment-go-go-binary-deployment-common-mistake",
      "question": "What can go wrong with deploying a Go binary?",
      "title": "What can go wrong with deploying a Go binary",
      "direct_answer": "Failures often come from compiling for the wrong platform, relying on unavailable dynamic libraries, forgetting certificates or migrations, binding only to localhost, embedding secrets, or replacing the binary without a safe restart and rollback plan. Record the source revision and toolchain, test on the target platform, inspect linkage, include required runtime files, run as a restricted user, expose build information, and rehearse health-checked rollout, graceful shutdown, and rollback.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 4,
      "interviewer_intent": {
        "testing": "Whether the learner understands the real rules and trade-offs behind deploying a Go binary.",
        "common_mistake": "Failures often come from compiling for the wrong platform, relying on unavailable dynamic libraries, forgetting certificates or migrations, binding only to localhost, embedding secrets, or replacing the binary without a safe restart and rollback plan.",
        "to_stand_out": "Connect the rule to the concrete example and state its important boundary."
      },
      "answer": {
        "sections": [
          {
            "type": "key_points",
            "title": "Quick revision",
            "items": [
              "Failures often come from compiling for the wrong platform, relying on unavailable dynamic libraries, forgetting certificates or migrations, binding only to localhost, embedding secrets, or replacing the binary without a safe restart and rollback plan.",
              "Record the source revision and toolchain, test on the target platform, inspect linkage, include required runtime files, run as a restricted user, expose build information, and rehearse health-checked rollout, graceful shutdown, and rollback.",
              "The executable is the main release artifact.",
              "Target OS and architecture are build inputs.",
              "CGO can add native runtime dependencies.",
              "Configuration belongs outside the artifact."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "answerSize": "compact",
            "content": "A Go deployment usually promotes a compiled executable built from a known source revision and module graph. The release also defines its target operating system and architecture, runtime configuration, service identity, network address, files, and shutdown behaviour.\n\nFailures often come from compiling for the wrong platform, relying on unavailable dynamic libraries, forgetting certificates or migrations, binding only to localhost, embedding secrets, or replacing the binary without a safe restart and rollback plan.\n\nFor example, CI can build `CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -trimpath -o api ./cmd/api`, calculate a checksum, and publish that immutable artifact. Production verifies the checksum and provides environment-specific settings.\n\nRecord the source revision and toolchain, test on the target platform, inspect linkage, include required runtime files, run as a restricted user, expose build information, and rehearse health-checked rollout, graceful shutdown, and rollback.\n\nA single executable does not contain the host kernel, external configuration, database schema, certificates, or every dynamically linked library. Static linking also does not remove the need for patching, provenance, access control, and observability."
          },
          {
            "type": "deep_explanation",
            "title": "Why the failure happens: deploying a Go binary",
            "content": "Failures around deploying a Go binary usually come from one incorrect assumption about ownership, state, ordering, or lifetime. Finding that assumption is more useful than adding a defensive line at random.\n\nCompilation makes Go deployment compact, but the binary is only one part of the process contract. CI should produce the artifact from locked module requirements, run tests, record provenance, and publish a checksum or signed package. Cross-compilation is straightforward for pure-Go code but native dependencies can require a matching C toolchain and libraries. At runtime, the service needs a dedicated identity, a working directory only if the program assumes one, network and filesystem permissions, trusted certificates, configuration, and limits. The process manager should start the binary directly, collect standard output and error, and send termination signals. The application should stop accepting new requests, finish in-flight work within a deadline, and exit with a meaningful status. A rollout observes readiness before sending traffic and keeps the previous artifact available. These details turn “copy one binary” into a repeatable, reversible release instead of a manual server change."
          },
          {
            "type": "flow_diagram",
            "title": "deploying a Go binary flow",
            "content": "```mermaid\nflowchart LR\n  A[Source + go.mod] --> B[Test and build in CI]\n  B --> C[Versioned binary + checksum]\n  C --> D[Deploy unchanged artifact]\n  D --> E[Health check]\n  E --> F[Traffic or rollback]\n```"
          },
          {
            "type": "code_example",
            "title": "deploying a Go binary example",
            "content": "~~~sh\nCGO_ENABLED=0 GOOS=linux GOARCH=amd64 \\\n  go build -trimpath -ldflags \"-X main.version=$VERSION\" \\\n  -o dist/catalog-api ./cmd/catalog-api\nsha256sum dist/catalog-api\n~~~"
          },
          {
            "type": "comparison_table",
            "title": "deploying a Go binary at a glance",
            "content": "| Layer | Examples | Deployment check |\n|---|---|---|\n| Artifact | Binary, embedded assets | Checksum and target platform |\n| Runtime | User, files, certificates, port | Least privilege and startup test |\n| Lifecycle | Health, signals, rollback | Rehearsed under real process manager |"
          },
          {
            "type": "practice_prompt",
            "title": "Check the idea",
            "content": "Change the example so it shows the common failure. Then state the smallest correction that restores the intended rule."
          }
        ]
      },
      "followup_questions": [
        "Which guarantee of deploying a Go binary matters most in this example?",
        "What boundary or failure case should be tested?",
        "When would shipping source code and compiling on the server be a clearer choice?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "What can go wrong with deploying a Go binary | InterviewExplainer",
        "metaDescription": "Failures often come from compiling for the wrong platform, relying on unavailable dynamic libraries, forgetting certificates or migrations, binding only to..."
      }
    },
    {
      "id": "cloud-deployment-go-go-binary-deployment-q004",
      "slug": "cloud-deployment-go-go-binary-deployment-compare",
      "question": "How does deploying a Go binary differ from shipping source code and compiling on the server?",
      "title": "How does deploying a Go binary differ from shipping source code and compiling on the server",
      "direct_answer": "A prebuilt artifact is fast to start and proves that every environment runs the same bits. Building on each server can adapt to that host, but it requires compilers and source there and makes releases harder to reproduce and audit. A single executable does not contain the host kernel, external configuration, database schema, certificates, or every dynamically linked library. Static linking also does not remove the need for patching, provenance, access control, and observability.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 4,
      "interviewer_intent": {
        "testing": "Whether the learner understands the real rules and trade-offs behind deploying a Go binary.",
        "common_mistake": "Failures often come from compiling for the wrong platform, relying on unavailable dynamic libraries, forgetting certificates or migrations, binding only to localhost, embedding secrets, or replacing the binary without a safe restart and rollback plan.",
        "to_stand_out": "Connect the rule to the concrete example and state its important boundary."
      },
      "answer": {
        "sections": [
          {
            "type": "key_points",
            "title": "Quick revision",
            "items": [
              "A prebuilt artifact is fast to start and proves that every environment runs the same bits. Building on each server can adapt to that host, but it requires compilers and source there and makes releases harder to reproduce and audit.",
              "A single executable does not contain the host kernel, external configuration, database schema, certificates, or every dynamically linked library. Static linking also does not remove the need for patching, provenance, access control, and observability.",
              "The executable is the main release artifact.",
              "Target OS and architecture are build inputs.",
              "CGO can add native runtime dependencies.",
              "Configuration belongs outside the artifact."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "answerSize": "compact",
            "content": "A Go deployment usually promotes a compiled executable built from a known source revision and module graph. The release also defines its target operating system and architecture, runtime configuration, service identity, network address, files, and shutdown behaviour.\n\nA prebuilt artifact is fast to start and proves that every environment runs the same bits. Building on each server can adapt to that host, but it requires compilers and source there and makes releases harder to reproduce and audit.\n\n`go build` translates packages and linked dependencies into an executable for the selected `GOOS` and `GOARCH`. CGO, build tags, embedded files, version metadata, and compiler version can all affect what the artifact needs at runtime.\n\nFor example, CI can build `CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -trimpath -o api ./cmd/api`, calculate a checksum, and publish that immutable artifact. Production verifies the checksum and provides environment-specific settings.\n\nA single executable does not contain the host kernel, external configuration, database schema, certificates, or every dynamically linked library. Static linking also does not remove the need for patching, provenance, access control, and observability."
          },
          {
            "type": "deep_explanation",
            "title": "The important trade-off: deploying a Go binary",
            "content": "deploying a Go binary and shipping source code and compiling on the server solve related problems, but they expose different guarantees. The best choice follows from the guarantee the program actually needs.\n\nCompilation makes Go deployment compact, but the binary is only one part of the process contract. CI should produce the artifact from locked module requirements, run tests, record provenance, and publish a checksum or signed package. Cross-compilation is straightforward for pure-Go code but native dependencies can require a matching C toolchain and libraries. At runtime, the service needs a dedicated identity, a working directory only if the program assumes one, network and filesystem permissions, trusted certificates, configuration, and limits. The process manager should start the binary directly, collect standard output and error, and send termination signals. The application should stop accepting new requests, finish in-flight work within a deadline, and exit with a meaningful status. A rollout observes readiness before sending traffic and keeps the previous artifact available. These details turn “copy one binary” into a repeatable, reversible release instead of a manual server change."
          },
          {
            "type": "flow_diagram",
            "title": "deploying a Go binary flow",
            "content": "```mermaid\nflowchart LR\n  A[Source + go.mod] --> B[Test and build in CI]\n  B --> C[Versioned binary + checksum]\n  C --> D[Deploy unchanged artifact]\n  D --> E[Health check]\n  E --> F[Traffic or rollback]\n```"
          },
          {
            "type": "code_example",
            "title": "deploying a Go binary example",
            "content": "~~~sh\nCGO_ENABLED=0 GOOS=linux GOARCH=amd64 \\\n  go build -trimpath -ldflags \"-X main.version=$VERSION\" \\\n  -o dist/catalog-api ./cmd/catalog-api\nsha256sum dist/catalog-api\n~~~"
          },
          {
            "type": "comparison_table",
            "title": "deploying a Go binary at a glance",
            "content": "| Layer | Examples | Deployment check |\n|---|---|---|\n| Artifact | Binary, embedded assets | Checksum and target platform |\n| Runtime | User, files, certificates, port | Least privilege and startup test |\n| Lifecycle | Health, signals, rollback | Rehearsed under real process manager |"
          },
          {
            "type": "practice_prompt",
            "title": "Check the idea",
            "content": "Apply both options to the same small input. Compare correctness, resource use, and clarity before selecting one."
          }
        ]
      },
      "followup_questions": [
        "Which guarantee of deploying a Go binary matters most in this example?",
        "What boundary or failure case should be tested?",
        "When would shipping source code and compiling on the server be a clearer choice?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "How does deploying a Go binary differ from shipping source code and compiling on the server | InterviewExplainer",
        "metaDescription": "A prebuilt artifact is fast to start and proves that every environment runs the same bits. Building on each server can adapt to that host, but it requires..."
      }
    },
    {
      "id": "cloud-deployment-go-go-binary-deployment-q005",
      "slug": "cloud-deployment-go-go-binary-deployment-scenario",
      "question": "How do you troubleshoot deploying a Go binary?",
      "title": "How do you troubleshoot deploying a Go binary",
      "direct_answer": "When a binary works in CI but not on the server, compare architecture, operating system, CGO linkage, user permissions, environment, current directory, ports, certificates, and files. The exact startup error usually identifies the first broken runtime assumption. Record the source revision and toolchain, test on the target platform, inspect linkage, include required runtime files, run as a restricted user, expose build information, and rehearse health-checked rollout, graceful shutdown, and rollback.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 4,
      "interviewer_intent": {
        "testing": "Whether the learner understands the real rules and trade-offs behind deploying a Go binary.",
        "common_mistake": "Failures often come from compiling for the wrong platform, relying on unavailable dynamic libraries, forgetting certificates or migrations, binding only to localhost, embedding secrets, or replacing the binary without a safe restart and rollback plan.",
        "to_stand_out": "Connect the rule to the concrete example and state its important boundary."
      },
      "answer": {
        "sections": [
          {
            "type": "key_points",
            "title": "Quick revision",
            "items": [
              "When a binary works in CI but not on the server, compare architecture, operating system, CGO linkage, user permissions, environment, current directory, ports, certificates, and files. The exact startup error usually identifies the first broken runtime assumption.",
              "Record the source revision and toolchain, test on the target platform, inspect linkage, include required runtime files, run as a restricted user, expose build information, and rehearse health-checked rollout, graceful shutdown, and rollback.",
              "The executable is the main release artifact.",
              "Target OS and architecture are build inputs.",
              "CGO can add native runtime dependencies.",
              "Configuration belongs outside the artifact."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "answerSize": "compact",
            "content": "A Go deployment usually promotes a compiled executable built from a known source revision and module graph. The release also defines its target operating system and architecture, runtime configuration, service identity, network address, files, and shutdown behaviour.\n\nWhen a binary works in CI but not on the server, compare architecture, operating system, CGO linkage, user permissions, environment, current directory, ports, certificates, and files. The exact startup error usually identifies the first broken runtime assumption.\n\nFor example, CI can build `CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build -trimpath -o api ./cmd/api`, calculate a checksum, and publish that immutable artifact. Production verifies the checksum and provides environment-specific settings.\n\nRecord the source revision and toolchain, test on the target platform, inspect linkage, include required runtime files, run as a restricted user, expose build information, and rehearse health-checked rollout, graceful shutdown, and rollback.\n\nA single executable does not contain the host kernel, external configuration, database schema, certificates, or every dynamically linked library. Static linking also does not remove the need for patching, provenance, access control, and observability."
          },
          {
            "type": "deep_explanation",
            "title": "A reliable diagnosis: deploying a Go binary",
            "content": "A good investigation traces the first point where the real state differs from the expected state. With deploying a Go binary, that means observing the relevant input, transition, and result rather than guessing from the final symptom.\n\nCompilation makes Go deployment compact, but the binary is only one part of the process contract. CI should produce the artifact from locked module requirements, run tests, record provenance, and publish a checksum or signed package. Cross-compilation is straightforward for pure-Go code but native dependencies can require a matching C toolchain and libraries. At runtime, the service needs a dedicated identity, a working directory only if the program assumes one, network and filesystem permissions, trusted certificates, configuration, and limits. The process manager should start the binary directly, collect standard output and error, and send termination signals. The application should stop accepting new requests, finish in-flight work within a deadline, and exit with a meaningful status. A rollout observes readiness before sending traffic and keeps the previous artifact available. These details turn “copy one binary” into a repeatable, reversible release instead of a manual server change."
          },
          {
            "type": "flow_diagram",
            "title": "deploying a Go binary flow",
            "content": "```mermaid\nflowchart LR\n  A[Source + go.mod] --> B[Test and build in CI]\n  B --> C[Versioned binary + checksum]\n  C --> D[Deploy unchanged artifact]\n  D --> E[Health check]\n  E --> F[Traffic or rollback]\n```"
          },
          {
            "type": "code_example",
            "title": "deploying a Go binary example",
            "content": "~~~sh\nCGO_ENABLED=0 GOOS=linux GOARCH=amd64 \\\n  go build -trimpath -ldflags \"-X main.version=$VERSION\" \\\n  -o dist/catalog-api ./cmd/catalog-api\nsha256sum dist/catalog-api\n~~~"
          },
          {
            "type": "comparison_table",
            "title": "deploying a Go binary at a glance",
            "content": "| Layer | Examples | Deployment check |\n|---|---|---|\n| Artifact | Binary, embedded assets | Checksum and target platform |\n| Runtime | User, files, certificates, port | Least privilege and startup test |\n| Lifecycle | Health, signals, rollback | Rehearsed under real process manager |"
          },
          {
            "type": "practice_prompt",
            "title": "Check the idea",
            "content": "List the first three observations you would collect, the smallest reproducer, and the regression test that proves the fix."
          }
        ]
      },
      "followup_questions": [
        "Which guarantee of deploying a Go binary matters most in this example?",
        "What boundary or failure case should be tested?",
        "When would shipping source code and compiling on the server be a clearer choice?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "How do you troubleshoot deploying a Go binary | InterviewExplainer",
        "metaDescription": "When a binary works in CI but not on the server, compare architecture, operating system, CGO linkage, user permissions, environment, current directory, por..."
      }
    }
  ]
}

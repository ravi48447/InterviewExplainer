# Speakable health dashboard

> Generated by `scripts/audit_speakable.py --all --report`. Mirrors lint-rules.md §7.8.4. The legacy questions are listed for visibility but excluded from green-percentage calculations because they have no v2 to lint.

Total questions audited: **7003**

## By pillar

| Pillar | legacy | pending/warn | fail | pass | total | green % (excl. legacy) |
|---|---:|---:|---:|---:|---:|---:|
| P01 | 0 | 1 | 3418 | 4 | 3423 | 0% |
| P02 | 0 | 0 | 1250 | 1 | 1251 | 0% |
| P03 | 0 | 1 | 92 | 0 | 93 | 0% |
| P04 | 0 | 1 | 108 | 0 | 109 | 0% |
| P05 | 0 | 0 | 65 | 1 | 66 | 2% |
| P06 | 0 | 0 | 32 | 2 | 34 | 6% |
| P07 | 0 | 0 | 32 | 1 | 33 | 3% |
| P08 | 0 | 0 | 182 | 1 | 183 | 1% |
| P09 | 0 | 0 | 302 | 1 | 303 | 0% |
| P10 | 0 | 0 | 50 | 1 | 51 | 2% |
| P11 | 0 | 1 | 43 | 1 | 45 | 2% |
| P12 | 0 | 0 | 82 | 2 | 84 | 2% |
| — | 1328 | 0 | 0 | 0 | 1328 | — |

## By archetype

| Archetype | legacy | pending/warn | fail | pass | total | green % (excl. legacy) |
|---|---:|---:|---:|---:|---:|---:|
| A | 0 | 1 | 1908 | 6 | 1915 | 0% |
| B | 0 | 3 | 864 | 2 | 869 | 0% |
| C | 0 | 0 | 2494 | 1 | 2495 | 0% |
| D | 0 | 0 | 78 | 1 | 79 | 1% |
| E | 0 | 0 | 97 | 2 | 99 | 2% |
| F | 0 | 0 | 174 | 1 | 175 | 1% |
| G | 0 | 0 | 41 | 2 | 43 | 5% |
| — | 1328 | 0 | 0 | 0 | 1328 | — |

## Top 10 failing questions

| # | Score | Path · slug | Pillar | Archetype | Top failed rules |
|---:|---:|---|---|---|---|
| 1 | 0 | `content/.archive/jbi-20260417-041052/architecture-patterns/architectural-styles/complete-qa.json` · monolith-vs-microservices-vs-modular-monolith | P01 | F | 7.1.8: schema: <root>: {'archetype': 'F', 'pillar': 'P01', 'hook': 'The three architectures sit on a spectrum of deployment independence versus operational simplicity.', 'beats': [{'kind': 'requirements_fr_nfr', 'layout': 'paragraph', 'text': 'For most of the 2010s, "microservices" was the default answer to every architecture question. Teams split monoliths into dozens of services, invested in Kubernetes clusters, service meshes, and distributed tracing — then discovered that the operational tax exceeded the benefit for organizations that didn\'t actually need independent deployment at scale. Shopify runs billions of dollars in commerce on a modular monolith. Amazon Prime Video publicly consolidated microservices back into a monolith for a video monitoring tool, cutting costs by 90%. The core trade-off is deployment independence versus operational simplicity. Microservices buy you independent deployability, independent scaling, and team autonomy — but you pay with distributed tracing, network latency between services, eventual consistency instead of ACID, and a platform engineering team.'}, {'kind': 'capacity', 'layout': 'paragraph', 'text': 'For most of the 2010s, "microservices" was the default answer to every architecture question. Teams split monoliths into dozens of services, invested in Kubernetes clusters, service meshes, and distributed tracing — then discovered that the operational tax exceeded the benefit for organizations that didn\'t actually need independent deployment at scale. Shopify runs billions of dollars in commerce on a modular monolith. Amazon Prime Video publicly consolidated microservices back into a monolith for a video monitoring tool, cutting costs by 90%. The core trade-off is deployment independence versus operational simplicity. Microservices buy you independent deployability, independent scaling, and team autonomy — but you pay with distributed tracing, network latency between services.'}, {'kind': 'api', 'layout': 'bullets', 'items': ['For most of the 2010s, "microservices" was the default answer to every architecture question', "Teams split monoliths into dozens of services, invested in Kubernetes clusters, service meshes, and distributed tracing — then discovered that the operational tax exceeded the benefit for organizations that didn't actually need independent deployment at scale", 'Shopify runs billions of dollars in commerce on a modular monolith', 'Amazon Prime Video publicly consolidated microservices back into a monolith for a video monitoring tool, cutting costs by 90%', 'The core trade-off is deployment independence versus operational simplicity']}, {'kind': 'data_model', 'layout': 'paragraph', 'text': 'For most of the 2010s, "microservices" was the default answer to every architecture question. Teams split monoliths into dozens of services, invested in Kubernetes clusters, service meshes, and distributed tracing — then discovered that the operational tax exceeded the benefit for organizations that didn\'t actually need independent deployment at scale. Shopify runs billions of dollars in commerce on a modular monolith. Amazon Prime Video publicly consolidated microservices back into a monolith for a video monitoring tool, cutting costs by 90%. The core trade-off is deployment independence versus operational simplicity. Microservices buy you independent deployability, independent scaling, and team autonomy — but you pay with distributed tracing, network latency between services, eventual consistency instead of ACID, and a platform engineering team.'}, {'kind': 'high_level', 'layout': 'paragraph', 'text': 'For most of the 2010s, "microservices" was the default answer to every architecture question. Teams split monoliths into dozens of services, invested in Kubernetes clusters, service meshes, and distributed tracing — then discovered that the operational tax exceeded the benefit for organizations that didn\'t actually need independent deployment at scale. Shopify runs billions of dollars in commerce on a modular monolith. Amazon Prime Video publicly consolidated microservices back into a monolith for a video monitoring tool, cutting costs by 90%. The core trade-off is deployment independence versus operational simplicity. Microservices buy you independent deployability, independent scaling, and team autonomy — but you pay with distributed tracing, network latency between services, eventual consistency instead of ACID, and a platform engineering team.'}, {'kind': 'bottleneck_deep_dive', 'layout': 'callout', 'text': "Recommending microservices for every project, or treating 'monolith' as a pejorative rather than a valid and often better choice"}, {'kind': 'tradeoffs', 'layout': 'paragraph', 'text': 'Monolith ≠ Big Ball of Mud — a well-structured monolith with enforced module boundaries (ArchUnit, Gradle subprojects) is a deliberate architecture, not a legacy problem. Treating "monolith" as a pejorative is a red flag in interviews Microservices tax: N services × (CI pipeline + dashboard + alerts + runbook) — each new service adds platform overhead that must be justified by concrete independence needs, not theoretical future flexibility Modular Monolith preserves ACID and stack traces — local transactions and single-process debugging disappear the moment you.'}], 'cap': 'Defaulting to microservices without that evidence is premature complexity.', 'familiarity_anchors': ['Monolith', 'Microservices', 'Modular'], 'tts_overrides': {}, 'speakable_status': 'pending_review', 'followup_handoff': ['How do you enforce module boundaries in a Modular Monolith in Java (e.g., using JPMS or ArchUnit)?', 'When is the right time to split a Modular Monolith into microservices?', 'How do you handle shared state (e.g., a users table) in a Modular Monolith?']} is not valid under any of the given schemas; 7.3.1: avg sentence length 23.9 > 16; 7.3.2: Flesch-Kincaid grade 17.9 > 10.5 (textbook voice) |
| 2 | 0 | `content/.archive/jbi-20260417-041052/architecture-patterns/architectural-styles/complete-qa.json` · backend-for-frontend-bff-pattern | P01 | A | 7.1.8: schema: <root>: {'archetype': 'A', 'pillar': 'P01', 'hook': 'BFF solves the impedance mismatch between generic microservice APIs and specific client needs.', 'beats': [{'kind': 'definition', 'layout': 'paragraph', 'text': 'Picture a product page on an e-commerce platform. The web dashboard needs the full product description, specifications table, 20 review summaries, seller info, and related product recommendations — all rendered above the fold. The mobile app needs a compact card: thumbnail, title, price, star rating, and a "Buy Now" button. A partner\'s inventory feed needs raw SKU data in a contractual format with zero presentation logic. A single generic API serving all three faces a losing proposition. It either returns.'}, {'kind': 'why_exists', 'layout': 'paragraph', 'text': 'Understanding of client-driven API design and how BFF solves the impedance mismatch between generic microservice APIs and specific client needs'}, {'kind': 'parts_or_states', 'layout': 'bullets', 'items': ['One BFF per client platform (web, mobile, partner), not per feature — proliferating BFFs per screen or per service duplicates caching, retry logic, and error handling across services that call the same downstream APIs BFF ≠ API Gateway — the Gateway handles authentication, routing, and rate limiting (infrastructure); the BFF handles client-specific aggregation and response shaping (application logic)', 'They are complementary layers Strict no-business-logic rule — BFFs contain only data fetching, transformation, and composition', 'If a change enforces an invariant or makes a business decision, it belongs in the domain service Parallel fan-out + response caching — use CompletableFuture to call downstream services concurrently, and cache the composed response in Redis with a short TTL', 'This turns three sequential calls into one cached lookup for mobile clients GraphQL solves the same over-fetching problem differently — instead of N BFF services, a single GraphQL schema lets each client query exactly the fields it needs', 'The trade-off is schema governance complexity at scale']}, {'kind': 'example', 'layout': 'paragraph', 'text': 'Picture a product page on an e-commerce platform. The web dashboard needs the full product description, specifications table, 20 review summaries, seller info, and related product recommendations — all rendered above the fold. The mobile app needs a compact card: thumbnail, title, price, star rating, and a "Buy Now" button. A partner\'s inventory feed needs raw SKU data in a contractual format with zero presentation logic. A single generic API serving all three faces a losing proposition. It either returns everything (mobile downloads data it will never render, wasting bandwidth and battery) or returns the minimum (web makes five follow-up calls to assemble one page, increasing latency). Every API change becomes a negotiation across three client teams with conflicting priorities.'}, {'kind': 'pitfalls', 'layout': 'bullets', 'items': ['Confusing BFF with an API Gateway — the Gateway handles routing and cross-cutting concerns, the BFF handles client-specific aggregation and transformation']}], 'cap': 'GraphQL is worth evaluating as an alternative — it lets each client specify exactly the fields it wants without maintaining separate BFF services, though it introduces schema governance challenges at', 'familiarity_anchors': ['Backend', 'Frontend'], 'tts_overrides': {}, 'speakable_status': 'pending_review', 'followup_handoff': ['How do you handle authentication in a BFF — does the BFF validate tokens or trust the gateway?', 'What is the difference between a BFF and an API Gateway?', 'How does GraphQL compare to BFF as a solution to over-fetching and under-fetching?']} is not valid under any of the given schemas; 7.3.1: avg sentence length 28.9 > 16; 7.3.2: Flesch-Kincaid grade 17.1 > 10.5 (textbook voice) |
| 3 | 0 | `content/.archive/jbi-20260417-041052/architecture-patterns/architectural-styles/complete-qa.json` · service-mesh-architecture | P01 | A | 7.1.8: schema: <root>: {'archetype': 'A', 'pillar': 'P01', 'hook': 'A Service Mesh solves the problem of every microservice team reimplementing the same infrastructure: mTLS, tracing, metrics, retries, circuit breaking.', 'beats': [{'kind': 'definition', 'layout': 'paragraph', 'text': 'When you have 30 microservices, each team faces the same infrastructure checklist: configure mTLS certificates for service-to-service encryption, add distributed tracing headers, emit request metrics to Prometheus, implement circuit breakers for failing downstream calls, configure retry policies with exponential backoff. Without coordination, you get 30 different implementations — some use Resilience4j, some use custom retry loops, some skip tracing entirely. Security posture is inconsistent. Observability has gaps. When the platform team mandates a change — say, rotating TLS certificates —.'}, {'kind': 'why_exists', 'layout': 'paragraph', 'text': 'Understanding of how Service Mesh offloads operational cross-cutting concerns from application code and the Istio/Envoy architecture'}, {'kind': 'parts_or_states', 'layout': 'bullets', 'items': ['Envoy sidecars intercept all pod traffic via iptables — the application speaks plain HTTP to localhost while Envoy transparently handles mTLS, tracing, metrics, and retries', 'Zero application code for any of this Istiod issues and rotates mTLS certificates automatically — every service-to-service call is authenticated and encrypted', "Certificate rotation happens without application restarts or deployment VirtualService + DestinationRule = declarative traffic management — canary weights, routing rules, and circuit breaker thresholds are YAML resources applied with kubectl, not code deployed through CI Traffic mirroring validates new versions with real production traffic — the mirrored request is fire-and-forget; users see only the stable version's response while the mirror reveals behavioral differences Mesh circuit breaking ≠ application circuit breaking — Istio ejects unhealthy pods at the network level; Resilience4j implements domain-aware fallbacks at the application level", "They're complementary, and many production systems run both"]}, {'kind': 'example', 'layout': 'paragraph', 'text': 'When you have 30 microservices, each team faces the same infrastructure checklist: configure mTLS certificates for service-to-service encryption, add distributed tracing headers, emit request metrics to Prometheus, implement circuit breakers for failing downstream calls, configure retry policies with exponential backoff. Without coordination, you get 30 different implementations — some use Resilience4j, some use custom retry loops, some skip tracing entirely. Security posture is inconsistent. Observability has gaps. When the platform team mandates a change — say, rotating TLS certificates — they must coordinate across 30 codebases. A Service Mesh centralizes all of this into a dedicated infrastructure layer that handles service-to-service communication transparently. It consists of two components: a data plane of Envoy proxy sidecars injected alongside each service container.'}, {'kind': 'pitfalls', 'layout': 'bullets', 'items': ['Confusing Service Mesh features with application-layer patterns — circuit breaking in Resilience4j vs', 'circuit breaking in Istio serve different purposes and can coexist']}], 'cap': 'Many teams run both deliberately.', 'familiarity_anchors': ['Service', 'Implemented', 'Sidecar'], 'tts_overrides': {}, 'speakable_status': 'pending_review', 'followup_handoff': ["How does Istio's mTLS work without any changes to the Spring Boot application code?", 'What is the performance overhead of routing all traffic through Envoy sidecars?', 'How do you implement canary deployments using Istio traffic management?']} is not valid under any of the given schemas; 7.3.1: avg sentence length 26.1 > 16; 7.3.2: Flesch-Kincaid grade 19.0 > 10.5 (textbook voice) |
| 4 | 0 | `content/.archive/jbi-20260417-041052/architecture-patterns/architectural-styles/complete-qa.json` · layered-architecture-spring-boot | P01 | F | 7.1.8: schema: <root>: {'archetype': 'F', 'pillar': 'P01', 'hook': 'Layered architecture in Spring Boot is built on a one-way dependency rule: Controller → Service → Repository.', 'beats': [{'kind': 'requirements_fr_nfr', 'layout': 'paragraph', 'text': "Every Spring Boot application starts clean. The first controller is 30 lines: accept request, call repository, return response. By month three, that controller validates input format, checks authorization, queries the database, applies a discount calculation, sends a notification email, logs an audit trail, and formats the response — all in one method. By month six, it's 400 lines, and a bug in the discount logic requires editing a class that also handles HTTP status codes, email templates, and SQL. Layered architecture exists because of a dependency direction rule: Controller → Service → Repository. Each layer has one responsibility and depends only on the layer below it. Controllers translate HTTP into method calls. Services apply business rules without knowing whether the."}, {'kind': 'capacity', 'layout': 'paragraph', 'text': "Every Spring Boot application starts clean. The first controller is 30 lines: accept request, call repository, return response. By month three, that controller validates input format, checks authorization, queries the database, applies a discount calculation, sends a notification email, logs an audit trail, and formats the response — all in one method. By month six, it's 400 lines, and a bug in the discount logic requires editing a class that also handles HTTP status codes, email templates, and SQL. Layered architecture exists because of a dependency direction rule: Controller → Service → Repository. Each layer has one responsibility and depends only on the layer below it. Controllers translate HTTP into."}, {'kind': 'api', 'layout': 'bullets', 'items': ['Every Spring Boot application starts clean', 'The first controller is 30 lines: accept request, call repository, return response', 'By month three, that controller validates input format, checks authorization, queries the database, applies a discount calculation, sends a notification email, logs an audit trail, and formats the response — all in one method', "By month six, it's 400 lines, and a bug in the discount logic requires editing a class that also handles HTTP status codes, email templates, and SQL", 'Layered architecture exists because of a dependency direction rule: Controller → Service → Repository']}, {'kind': 'data_model', 'layout': 'paragraph', 'text': 'The Controller\'s job is narrow: receive an HTTP request, validate the request format (not business rules), call the appropriate Service method, and map the result to an HTTP response with the correct status code. A well-written controller method is under 10 lines. The most pervasive violation is putting business logic in the controller. A controller that checks if (user.subscriptionExpired()) return 403 has embedded a business rule in an HTTP handler. When that same expiry check is needed from a scheduled batch job, the logic is trapped inside a REST endpoint. ``java @RestController @RequestMapping("/api/orders") public class OrderController { private final OrderService orderService; @PostMapping public ResponseEntity<OrderResponse> create( @Valid @RequestBody CreateOrderRequest request) { // Controller: translate HTTP in, delegate, translate HTTP out Order.'}, {'kind': 'high_level', 'layout': 'paragraph', 'text': "Every Spring Boot application starts clean. The first controller is 30 lines: accept request, call repository, return response. By month three, that controller validates input format, checks authorization, queries the database, applies a discount calculation, sends a notification email, logs an audit trail, and formats the response — all in one method. By month six, it's 400 lines, and a bug in the discount logic requires editing a class that also handles HTTP status codes, email templates, and SQL. Layered architecture exists because of a dependency direction rule: Controller → Service → Repository. Each layer has one responsibility and depends only on the layer below it. Controllers translate HTTP into method calls. Services apply business rules without knowing whether the."}, {'kind': 'bottleneck_deep_dive', 'layout': 'callout', 'text': 'Putting business logic in the Controller (validation, authorization, orchestration) instead of keeping controllers thin. Letting JPA entities leak into the Service layer, coupling business logic to database schema.'}, {'kind': 'tradeoffs', 'layout': 'paragraph', 'text': 'Dependency direction is the actual rule — Controller → Service → Repository is one-way. The moment a Service imports a Controller class, the architecture\'s isolation collapses and "layers" become a naming convention with no enforcement Controllers: ~10 lines per endpoint — validate request format with @Valid, delegate to Service, map response. Business logic in a controller is trapped logic unreachable from GraphQL, CLI, or scheduled jobs @Transactional on the Service, never the Repository — the Service defines which operations form one unit of work. Individual.'}], 'cap': 'If not, concerns have leaked across the boundaries.', 'familiarity_anchors': ['Layered', 'Spring', 'Controller'], 'tts_overrides': {}, 'speakable_status': 'pending_review', 'followup_handoff': ['How do you prevent business logic from leaking into the Controller layer?', 'Where should @Transactional go — Controller, Service, or Repository?', 'When does strict layered architecture become over-engineering for a simple CRUD app?']} is not valid under any of the given schemas; 7.3.1: avg sentence length 19.0 > 16; 7.3.2: Flesch-Kincaid grade 13.4 > 10.5 (textbook voice) |
| 5 | 0 | `content/.archive/jbi-20260417-041052/architecture-patterns/architectural-styles/complete-qa.json` · hexagonal-architecture-ports-adapters | P01 | F | 7.1.8: schema: <root>: {'archetype': 'F', 'pillar': 'P01', 'hook': 'Hexagonal Architecture solves a dependency problem that builds up gradually.', 'beats': [{'kind': 'requirements_fr_nfr', 'layout': 'paragraph', 'text': 'In a typical Spring Boot application after a year of development, open any Service class and count the imports. You\'ll find @Entity annotations from JPA, @JsonProperty from Jackson, @Cacheable from Spring, maybe an @KafkaListener. The business logic is tangled with infrastructure assumptions. Testing the Service requires a running database because domain objects ARE JPA entities. Swapping PostgreSQL for MongoDB means rewriting business logic classes because persistence annotations are embedded in them. This happens because the dependency direction is wrong. The domain depends on infrastructure — JPA, Spring, Kafka — instead of the other way around. Hexagonal Architecture inverts this. The domain core defines ports — interfaces that describe what it needs. "I need to save an order" becomes OrderPersistencePort. "I.'}, {'kind': 'capacity', 'layout': 'paragraph', 'text': "In a typical Spring Boot application after a year of development, open any Service class and count the imports. You'll find @Entity annotations from JPA, @JsonProperty from Jackson, @Cacheable from Spring, maybe an @KafkaListener. The business logic is tangled with infrastructure assumptions. Testing the Service requires a running database because domain objects ARE JPA entities. Swapping PostgreSQL for MongoDB means rewriting business logic classes because persistence annotations are embedded in them. This happens because the dependency direction is wrong. The domain depends on infrastructure — JPA, Spring, Kafka — instead of the other way around. Hexagonal Architecture inverts this. The domain core defines ports — interfaces that describe what it."}, {'kind': 'api', 'layout': 'bullets', 'items': ['In a typical Spring Boot application after a year of development, open any Service class and count the imports', "You'll find @Entity annotations from JPA, @JsonProperty from Jackson, @Cacheable from Spring, maybe an @KafkaListener", 'The business logic is tangled with infrastructure assumptions', 'Testing the Service requires a running database because domain objects ARE JPA entities', 'Swapping PostgreSQL for MongoDB means rewriting business logic classes because persistence annotations are embedded in them']}, {'kind': 'data_model', 'layout': 'paragraph', 'text': 'Driving ports (inbound or primary ports) define what the application can do — its use cases. In Java, a driving port is an interface in the domain package: PlaceOrderUseCase, CancelSubscriptionPort. No framework annotations, no HTTP concepts, no messaging concepts. Driving adapters are the concrete entry points that call those ports. A REST controller is a driving adapter. A GraphQL resolver is a driving adapter. A Kafka consumer processing incoming commands is a driving adapter. Each translates from its specific protocol into a call on the domain\'s port. ``java // PORT — in the domain package, zero framework imports public interface PlaceOrderUseCase { OrderResult execute(PlaceOrderCommand command); } // ADAPTER — in the infrastructure.web package, depends on the port @RestController @RequestMapping("/api/orders") public.'}, {'kind': 'high_level', 'layout': 'paragraph', 'text': 'In a typical Spring Boot application after a year of development, open any Service class and count the imports. You\'ll find @Entity annotations from JPA, @JsonProperty from Jackson, @Cacheable from Spring, maybe an @KafkaListener. The business logic is tangled with infrastructure assumptions. Testing the Service requires a running database because domain objects ARE JPA entities. Swapping PostgreSQL for MongoDB means rewriting business logic classes because persistence annotations are embedded in them. This happens because the dependency direction is wrong. The domain depends on infrastructure — JPA, Spring, Kafka — instead of the other way around. Hexagonal Architecture inverts this. The domain core defines ports — interfaces that describe what it needs. "I need to save an order" becomes OrderPersistencePort. "I.'}, {'kind': 'bottleneck_deep_dive', 'layout': 'callout', 'text': "Treating Hexagonal as just 'use interfaces everywhere' without understanding that the direction of dependency is the point — the domain never imports infrastructure packages"}, {'kind': 'tradeoffs', 'layout': 'paragraph', 'text': 'The domain defines ports, infrastructure provides adapters — dependency direction is always inward. The domain package has zero imports from Spring, JPA, Jackson, or any framework. If you see @Entity in the domain, the architecture is violated Driving (inbound) ports vs driven (outbound) ports — driving ports define use cases triggered by controllers, CLI, events; driven ports define external resources the domain needs (persistence, messaging). Both are interfaces in the domain package Adapter mapping prevents entity leakage — JPA @Entity classes exist only inside the.'}], 'cap': 'Without automated enforcement, the boundary erodes within months in any team under delivery pressure.', 'familiarity_anchors': ['Hexagonal', 'Architecture', 'Ports'], 'tts_overrides': {}, 'speakable_status': 'pending_review', 'followup_handoff': ['How does Hexagonal Architecture differ from Clean Architecture?', 'How do you handle cross-cutting concerns like logging and transactions in Hexagonal Architecture?', "When is Hexagonal Architecture over-engineering — what's the minimum project size where it pays off?"]} is not valid under any of the given schemas; 7.3.2: Flesch-Kincaid grade 12.4 > 10.5 (textbook voice); 7.3.4: passive voice ratio 18% > 10% (active < 90%) |
| 6 | 0 | `content/.archive/jbi-20260417-041052/architecture-patterns/architectural-styles/complete-qa.json` · event-sourcing-pattern | P01 | A | 7.1.8: schema: <root>: {'archetype': 'A', 'pillar': 'P01', 'hook': 'The common misconception is that Event Sourcing means adding an audit log alongside your normal database.', 'beats': [{'kind': 'definition', 'layout': 'paragraph', 'text': 'Adding a changes_log table alongside your accounts table is not Event Sourcing. It\'s an audit log — a supplementary record that shadows the "real" data. The balance column in the accounts table is still the truth. If the audit log drifts or has gaps, you trust the column. Event Sourcing eliminates the column entirely. There is no balance = $950 stored anywhere. What exists is a sequence of immutable facts: AccountOpened(owner: "Alice"), MoneyDeposited($1000), MoneyWithdrawn($50). The balance of $950 is a.'}, {'kind': 'why_exists', 'layout': 'paragraph', 'text': 'Whether the candidate understands the fundamental inversion — events are the source of truth, current state is a derived projection — and can articulate when this trade-off is worth the complexity'}, {'kind': 'parts_or_states', 'layout': 'bullets', 'items': ['Events ARE the truth, not a supplement — there is no balance column', 'Current state is computed by replaying events through a deterministic apply() method', '"CRUD plus audit log" is a different pattern entirely Optimistic concurrency via version number — each append includes the expected aggregate version', 'Concurrent writes fail with a version conflict, not a database lock', 'The application retries by reloading events and re-validating Snapshots every 100–500 events — serialize aggregate state periodically so reconstitution replays only recent events']}, {'kind': 'example', 'layout': 'paragraph', 'text': 'Adding a changes_log table alongside your accounts table is not Event Sourcing. It\'s an audit log — a supplementary record that shadows the "real" data. The balance column in the accounts table is still the truth. If the audit log drifts or has gaps, you trust the column. Event Sourcing eliminates the column entirely. There is no balance = $950 stored anywhere. What exists is a sequence of immutable facts: AccountOpened(owner: "Alice"), MoneyDeposited($1000), MoneyWithdrawn($50). The balance of $950 is a computation — replay those events through a state-transition function, and the balance emerges. The events are the truth. The balance is a derived view. This distinction matters because it changes what\'s possible. Need the balance on March 15th? Replay events.'}, {'kind': 'pitfalls', 'layout': 'bullets', 'items': ['Confusing Event Sourcing with just adding an audit log to a traditional database', "The key insight is that events ARE the primary record — you don't have a separate 'real' state table that events supplement"]}], 'cap': "For a standard CRUD application, the engineering overhead doesn't pay for itself.", 'familiarity_anchors': ['Event', 'Sourcing', 'Current'], 'tts_overrides': {}, 'speakable_status': 'pending_review', 'followup_handoff': ['How do you handle event schema evolution when event formats need to change?', 'What is the relationship between Event Sourcing and CQRS?', 'How do snapshots work and when should you take them?']} is not valid under any of the given schemas; 7.3.2: Flesch-Kincaid grade 11.0 > 10.5 (textbook voice); 7.3.7: disallowed pronoun form: matches \bthe candidate\b |
| 7 | 0 | `content/.archive/jbi-20260417-041052/architecture-patterns/architectural-styles/complete-qa.json` · strangler-fig-pattern-migration | P01 | A | 7.1.8: schema: <root>: {'archetype': 'A', 'pillar': 'P01', 'hook': 'The Strangler Fig pattern replaces the risky big-bang rewrite with incremental migration.', 'beats': [{'kind': 'definition', 'layout': 'paragraph', 'text': 'The instinct when facing a painful monolith is appealing: rebuild it from scratch. A clean slate, modern patterns, no legacy baggage. In practice, big-bang rewrites fail at devastating rates. The new system must replicate every feature — including undocumented edge cases, hidden business rules, and integrations nobody remembers — before it can replace anything. Meanwhile, the business keeps shipping features on the old system. By the time the rewrite is "ready," it\'s missing months of changes and the team has.'}, {'kind': 'why_exists', 'layout': 'paragraph', 'text': 'Whether the candidate understands incremental migration as a risk-reduction strategy and can describe the routing mechanism that makes it work — not just the metaphor'}, {'kind': 'parts_or_states', 'layout': 'bullets', 'items': ['Routing layer makes migration invisible — API Gateway path-based routing or feature flags direct each request to either the monolith or the new service', "Clients see one API; only the backend changes Anti-Corruption Layer prevents inheriting legacy problems — the new service defines its own clean domain model and translates from the monolith's model at the boundary", 'Without an ACL, you build a distributed version of the same mess Full decommission is often not the goal — most successful migrations extract the 3–5 most painful features and leave a smaller, healthier monolith', 'Migrating everything has diminishing returns versus engineering cost CDC with Debezium for data synchronization — stream database changes from the monolith to the new service without modifying monolith code', 'Plan a deliberate cutover date for data ownership; indefinite co-ownership creates consistency drift Feature flags enable canary migration — route 5% of users to the new service, monitor metrics, increase gradually']}, {'kind': 'example', 'layout': 'paragraph', 'text': 'The instinct when facing a painful monolith is appealing: rebuild it from scratch. A clean slate, modern patterns, no legacy baggage. In practice, big-bang rewrites fail at devastating rates. The new system must replicate every feature — including undocumented edge cases, hidden business rules, and integrations nobody remembers — before it can replace anything. Meanwhile, the business keeps shipping features on the old system. By the time the rewrite is "ready," it\'s missing months of changes and the team has lost executive confidence. The Strangler Fig pattern (named after a tropical vine that grows around a host tree, eventually replacing it) takes the opposite approach. You don\'t replace the monolith all at once. You pick one feature — the one.'}, {'kind': 'pitfalls', 'layout': 'bullets', 'items': ['Thinking the goal is always to fully decommission the monolith', 'Many successful migrations stop when the most problematic features have been extracted, leaving a smaller, healthier monolith for the rest']}], 'cap': 'Migrating everything has diminishing returns.', 'familiarity_anchors': ['Strangler', 'Gateway'], 'tts_overrides': {}, 'speakable_status': 'pending_review', 'followup_handoff': ['How do you decide which features to extract from the monolith first?', "How do you handle data that's shared between the monolith and the new service during migration?", 'What is an Anti-Corruption Layer and why is it critical during Strangler Fig migration?']} is not valid under any of the given schemas; 7.3.1: avg sentence length 23.0 > 16; 7.3.2: Flesch-Kincaid grade 14.3 > 10.5 (textbook voice) |
| 8 | 0 | `content/.archive/jbi-20260417-041052/architecture-patterns/clean-architecture/complete-qa.json` · clean-architecture-layers-dependency-rule | P01 | F | 7.1.8: schema: <root>: {'archetype': 'F', 'pillar': 'P01', 'hook': 'The problem Clean Architecture solves is structural: business rules change slowly, but frameworks change rapidly.', 'beats': [{'kind': 'requirements_fr_nfr', 'layout': 'paragraph', 'text': 'In most Java enterprise applications, business logic is scattered across service classes that import Spring, JPA, and HTTP dependencies. When you upgrade Spring Boot from version 2 to 3, swap PostgreSQL for MongoDB, or replace REST with gRPC, every change ripples through your business logic. The core rules — how an order is validated, how a discount is calculated, how a fraud check triggers — get entangled with the technology that delivers them. Clean Architecture addresses this by structuring code into four concentric layers with a single, strict rule: source code dependencies must only point inward. The innermost layer contains enterprise business rules (Entities), surrounded by application business rules (Use Cases), then Interface Adapters (controllers, repository implementations), and finally Frameworks.'}, {'kind': 'capacity', 'layout': 'paragraph', 'text': 'In most Java enterprise applications, business logic is scattered across service classes that import Spring, JPA, and HTTP dependencies. When you upgrade Spring Boot from version 2 to 3, swap PostgreSQL for MongoDB, or replace REST with gRPC, every change ripples through your business logic. The core rules — how an order is validated, how a discount is calculated, how a fraud check triggers — get entangled with the technology that delivers them. Clean Architecture addresses this by structuring code into four concentric layers with a single, strict rule: source code dependencies must only point inward. The innermost layer contains enterprise business rules (Entities), surrounded by application business rules (Use.'}, {'kind': 'api', 'layout': 'bullets', 'items': ['In most Java enterprise applications, business logic is scattered across service classes that import Spring, JPA, and HTTP dependencies', 'When you upgrade Spring Boot from version 2 to 3, swap PostgreSQL for MongoDB, or replace REST with gRPC, every change ripples through your business logic', 'The core rules — how an order is validated, how a discount is calculated, how a fraud check triggers — get entangled with the technology that delivers them', 'Clean Architecture addresses this by structuring code into four concentric layers with a single, strict rule: source code dependencies must only point inward', 'The innermost layer contains enterprise business rules (Entities), surrounded by application business rules (Use Cases), then Interface Adapters (controllers, repository implementations), and finally Frameworks & Drivers (Spring Boot, Hibernate, PostgreSQL)']}, {'kind': 'data_model', 'layout': 'paragraph', 'text': 'Entities are the most stable layer because they encode rules that exist independent of any software system. An Order entity knows its invariants: the total cannot be negative, a cancelled order cannot be shipped, a discount requires a minimum quantity. These rules would exist even if the company ran on spreadsheets. In Java, an Entity is a plain POJO with zero framework imports — no @Entity, no @Component, no Lombok @Data. The class owns its validation and state transitions through expressive methods. ``java public class Order { private Money total; private OrderStatus status; public void cancel() { if (status == OrderStatus.SHIPPED) { throw new OrderAlreadyShippedException(id); } this.status = OrderStatus.CANCELLED; } } ` This class compiles with nothing but the JDK.'}, {'kind': 'high_level', 'layout': 'paragraph', 'text': 'In most Java enterprise applications, business logic is scattered across service classes that import Spring, JPA, and HTTP dependencies. When you upgrade Spring Boot from version 2 to 3, swap PostgreSQL for MongoDB, or replace REST with gRPC, every change ripples through your business logic. The core rules — how an order is validated, how a discount is calculated, how a fraud check triggers — get entangled with the technology that delivers them. Clean Architecture addresses this by structuring code into four concentric layers with a single, strict rule: source code dependencies must only point inward. The innermost layer contains enterprise business rules (Entities), surrounded by application business rules (Use Cases), then Interface Adapters (controllers, repository implementations), and finally Frameworks.'}, {'kind': 'bottleneck_deep_dive', 'layout': 'callout', 'text': 'Confusing the direction of dependencies — many developers assume the domain should call infrastructure, which violates the rule'}, {'kind': 'tradeoffs', 'layout': 'paragraph', 'text': 'Dependencies point only inward — a @RestController can call PlaceOrderUseCase, but the interactor must never import @Controller, @Entity, or any Spring annotation; violating this couples business logic to framework upgrade cycles Entities are pure POJOs, not JPA entities — domain Order has business methods and invariants with zero framework imports; OrderJpaEntity is a separate class in the infrastructure layer Use Case layer owns its port interfaces — OrderRepository is defined next to the interactor, not in the persistence package; the infrastructure provides the implementation, inverting.'}], 'cap': 'I enforce this with ArchUnit rules in CI — if anyone adds a Spring import to the domain package, the build fails before it reaches production.', 'familiarity_anchors': ['Clean', 'Architecture', 'Entities'], 'tts_overrides': {}, 'speakable_status': 'pending_review', 'followup_handoff': ['How would you enforce the Dependency Rule at build time using ArchUnit?', 'What is the difference between an Entity in Clean Architecture and a JPA @Entity?', 'How does the Dependency Rule relate to the Dependency Inversion Principle (DIP)?']} is not valid under any of the given schemas; 7.3.1: avg sentence length 31.3 > 16; 7.3.2: Flesch-Kincaid grade 17.6 > 10.5 (textbook voice) |
| 9 | 0 | `content/.archive/jbi-20260417-041052/architecture-patterns/clean-architecture/complete-qa.json` · use-case-interactor-pattern-java | P01 | C | 7.1.8: schema: <root>: {'archetype': 'C', 'pillar': 'P01', 'hook': 'The problem with @Service classes is they grow unbounded.', 'beats': [{'kind': 'mental_model', 'layout': 'paragraph', 'text': 'In most Spring Boot projects, business logic lives in @Service classes that grow into thousand-line monsters. OrderService starts with createOrder(), then gains cancelOrder(), updateOrderStatus(), calculateRefund(), applyPromotion(), until it becomes a god class that every developer dreads touching. Each method pulls in different repository dependencies, making the class impossible to test without mocking half the application. The Use Case pattern solves this by giving every business operation its own class. PlaceOrderInteractor, CancelOrderInteractor, CalculateRefundInteractor — each has a single execute() method, depends only on the interfaces it needs, and can be tested.'}, {'kind': 'mechanism', 'layout': 'ordered_list', 'steps': ['The first step is establishing the contract between the outside world and the business operation.', "The input port is an interface that declares the operation's signature — this is what controllers will call.", '``java public interface PlaceOrderUseCase { PlaceOrderResponse execute(PlaceOrderRequest request); } public record PlaceOrderRequest(String customerId, List<LineItem> items) {} public record PlaceOrderResponse(String orderId, BigDecimal total, OrderStatus status) {} ` These records are not HTTP DTOs — they have no Jackson annotations, no Bean Validation constraints.', 'The web layer has its own DTOs and maps between them.', 'This separation matters because the use case defines what information it needs, not what a REST API happens to accept.', 'If you expose a new GraphQL endpoint tomorrow, it maps to the same use case request with zero business logic changes.']}, {'kind': 'edge_cases', 'layout': 'bullets', 'items': ["One class per business operation — PlaceOrderInteractor, CancelOrderInteractor replace a 2000-line OrderService; class names become the application's feature list Input ports are interfaces, not classes — controllers depend on PlaceOrderUseCase, not PlaceOrderInteractor; swapping implementations requires zero controller changes Request/Response records ≠ HTTP DTOs — use case DTOs have no Jackson or validation annotations; the web layer maps its own DTOs to keep HTTP concerns out of business logic Constructor injection without @Autowired — the interactor is a plain Java class; Spring wires it in a @Configuration bean method, keeping the use case package annotation-free Flat orchestration, not interactor chains — one interactor should not call another; each orchestrates entities and ports directly to keep the dependency graph shallow and traceable"]}, {'kind': 'failure_mode', 'layout': 'callout', 'text': 'Injecting repositories or services directly instead of using interfaces, making the use case depend on the outer layer'}], 'cap': 'Each interactor orchestrates entities and ports directly.', 'familiarity_anchors': ['Interactor', 'Spring'], 'tts_overrides': {}, 'speakable_status': 'pending_review', 'followup_handoff': ['How would you handle transactions in a Use Case that spans multiple repositories?', 'Should Use Cases contain validation logic or should that be in the domain entities?', 'How do you handle asynchronous use cases (e.g., returning a CompletableFuture)?']} is not valid under any of the given schemas; 7.3.1: avg sentence length 27.8 > 16; 7.3.2: Flesch-Kincaid grade 18.4 > 10.5 (textbook voice) |
| 10 | 0 | `content/.archive/jbi-20260417-041052/architecture-patterns/clean-architecture/complete-qa.json` · ports-and-adapters-hexagonal-architecture | P01 | C | 7.1.8: schema: <root>: {'archetype': 'C', 'pillar': 'P01', 'hook': 'Hexagonal Architecture and Clean Architecture express the same fundamental idea: your business logic defines the interfaces, and everything external adapts to them.', 'beats': [{'kind': 'mental_model', 'layout': 'paragraph', 'text': "Think of your application as a house with many doors. Each door is a standardized opening — the house doesn't care whether a person, a robot, or a delivery drone walks through it. The door is the port (a standard interface), and whoever walks through it is the adapter (a specific implementation). Replace the person with a robot, the house doesn't change. Add a new door on the back wall, the house's internal rooms don't change either. Alistair Cockburn designed Hexagonal Architecture around this symmetry. The application core defines ports."}, {'kind': 'mechanism', 'layout': 'ordered_list', 'steps': ['Think of your application as a house with many doors.', "Each door is a standardized opening — the house doesn't care whether a person, a robot, or a delivery drone walks through it.", 'The door is the port (a standard interface), and whoever walks through it is the adapter (a specific implementation).', "Replace the person with a robot, the house doesn't change.", "Add a new door on the back wall, the house's internal rooms don't change either.", 'Alistair Cockburn designed Hexagonal Architecture around this symmetry.']}, {'kind': 'edge_cases', 'layout': 'bullets', 'items': ['Hexagon = symmetry, not six sides — the shape communicates that REST, CLI, Kafka, and tests are all equal entry/exit points, breaking the "REST on top, DB on bottom" mental model Primary adapters call in, secondary adapters are called — a @RestController is a driving adapter translating HTTP to use case calls; JpaOrderRepository is a driven adapter implementing a domain port Ports are owned by the application core — OrderRepository and PaymentGateway interfaces live inside the hexagon; external systems adapt to the core\'s vocabulary, not the reverse Pluggable testing without framework overhead — swap StripePaymentAdapter for FakePaymentGateway implementing the same port; interactor tests run without network, database, or Spring context Clean Architecture refines Hexagonal — same inversion principle with explicit inner subdivisions (Entities → Use Cases); teams often blend both vocabularies in practice']}, {'kind': 'failure_mode', 'layout': 'callout', 'text': 'Treating ports and adapters as just a naming convention rather than understanding the inversion of control they enforce'}], 'cap': 'If you need a running Stripe sandbox and PostgreSQL just to test order validation, the ports are leaking infrastructure.', 'familiarity_anchors': ['Hexagonal', 'Architecture', 'Alistair'], 'tts_overrides': {}, 'speakable_status': 'pending_review', 'followup_handoff': ['How would you structure Spring Boot packages to reflect the hexagonal architecture?', 'What is the difference between an input port and an output port in concrete Spring code?', 'How do you handle cross-adapter concerns like authentication in a hexagonal system?']} is not valid under any of the given schemas; 7.3.1: avg sentence length 22.4 > 16; 7.3.2: Flesch-Kincaid grade 14.2 > 10.5 (textbook voice) |

## Method notes

- **green** = `status: pass` (score ≥ 90, zero zero-tolerance fails).
- **legacy** rows are not lint-failures; they signal questions waiting for Phase 2/3 v2 production.
- **warn** counts include both pure warnings and lint-warn outcomes (score 80–89 or any warned rule).
- Per-rule definitions: `docs/speakable/lint-rules.md` §7.1–7.8.

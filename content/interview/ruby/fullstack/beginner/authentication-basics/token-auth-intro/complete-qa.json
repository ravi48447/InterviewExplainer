{
  "topic": "Token Auth Intro",
  "topicSlug": "token-auth-intro",
  "questions": [
    {
      "id": "token-auth-intro-q001",
      "slug": "rails-token-auth-intro",
      "question": "What is token authentication and when do you use it?",
      "title": "Token Authentication",
      "direct_answer": "Token auth is authenticating with a token the client sends on each request (usually in the Authorization header: Authorization: Bearer <token>), instead of a session cookie. It's used for APIs and SPAs where the client isn't a browser with cookies — mobile apps, other services, a React front end. On login the server issues a token (random, stored hashed in the DB, or a signed JWT), the client stores it and sends it on each request, and the server verifies it to find the user. It's stateless (no session to look up) and works cross-origin, unlike cookie sessions.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know token auth sends a token in the Authorization header (stateless, for APIs/SPAs) vs cookie sessions.",
        "common_mistake": "Sending the token in a query param (logged) instead of the Authorization header, or storing tokens in localStorage (XSS).",
        "to_stand_out": "Token auth: client sends Authorization: Bearer <token> each request; server verifies to find the user. Stateless, for APIs/SPAs/mobile, cross-origin."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define Token Authentication simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "Token auth: the client sends Authorization: Bearer <token> on each request.",
              "Used for APIs/SPAs/mobile where the client isn't a browser with cookies.",
              "Stateless — no session record to look up; works cross-origin.",
              "On login the server issues a token (random+hashed in DB, or a signed JWT).",
              "Store tokens hashed (like passwords); send in the Authorization header, not a query param."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "Token authentication is authenticating with a token the client sends on each request — usually in the Authorization header as Authorization Bearer token — instead of a session cookie. It's used for APIs and SPAs where the client isn't a browser with cookies: mobile apps, other services, a React front end. On login the server issues a token, a random secret stored hashed in the database or a signed JWT, the client stores it and sends it on each request, and the server verifies it to find the user. It's stateless, with no session record to look up, and works cross-origin, unlike cookie sessions which are tied to a single domain. A common mistake is sending the token in a query param, because that gets logged, instead of the Authorization header, or storing tokens in localStorage, where XSS can steal them, or storing the token in the database in plaintext, when you should store a hash like a password."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Token auth is authenticating with a token the client sends on each request (usually in the Authorization header: Authorization: Bearer <token>), instead of a session cookie.\n\nIt's used for APIs and SPAs where the client isn't a browser with cookies — mobile apps, other services, a React front end.\n\nOn login the server issues a token (random, stored hashed in the DB, or a signed JWT), the client stores it and sends it on each request, and the server verifies it to find the user. It's stateless (no session to look up) and works cross-origin, unlike cookie sessions.\n\nSending the token in a query param (logged) instead of the Authorization header, or storing tokens in localStorage (XSS)."
          },
          {
            "type": "flow_diagram",
            "title": "Login -> Token -> Each Request",
            "content": "The client logs in once, gets a token, and sends it on every request; the server verifies it.\n\n```mermaid\nflowchart LR\n  L[login] --> I[server issues token]\n  I --> S[client stores token]\n  R[each request] -->|Authorization: Bearer token| V[server verifies -> user]\n```"
          },
          {
            "type": "code_example",
            "title": "Token auth flow",
            "content": "```ruby\n# Issue a token on login\nclass Api::SessionsController < ApplicationController\n  def create\n    user = User.authenticate(params[:email], params[:password])\n    if user\n      token = SecureRandom.hex(32)                # or a signed JWT\n      user.update(auth_token: Digest::SHA256.hexdigest(token))  # store hashed!\n      render json: { token: token }                # send to client\n    else\n      render json: { error: 'invalid' }, status: :unauthorized\n    end\n  end\nend\n\n# Verify on each request (a before_action)\nclass ApiController < ApplicationController\n  before_action :authenticate_token!\n\n  def authenticate_token!\n    token = request.headers['Authorization']&.sub('Bearer ', '')\n    user = User.find_by(auth_token: Digest::SHA256.hexdigest(token))\n    render json: { error: 'unauthorized' }, status: :unauthorized unless user\n    @current_user = user\n  end\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Token Auth Intro in a real project?",
        "How would you compare Token Auth Intro with an alternative?",
        "What is a common mistake when using Token Auth Intro?"
      ],
      "seo": {
        "metaTitle": "Token Authentication — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is token authentication and when do you use it? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "authentication-basics-token-auth-intro-q001",
      "slug": "authentication-basics-token-auth-intro-when-to-use",
      "question": "When would you use Token Auth Intro in a real project?",
      "title": "When would you use Token Auth Intro in a real project",
      "direct_answer": "I would use token authentication and when do you use it when the problem matches its main benefit. Token auth is authenticating with a token the client sends on each request (usually in the Authorization header: Authorization: Bearer <token>), instead of a session cookie. In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain token authentication and when do you use it in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about token authentication and when do you use it without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining token authentication and when do you use it in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. Token auth is authenticating with a token the client sends on each request (usually in the Authorization header: Authorization: Bearer <token>), instead of a session cookie. It's used for APIs and SPAs where the client isn't a browser with cookies — mobile apps, other services, a React front end. On login the server issues a token (random, stored hashed in the DB, or a signed JWT), the client stores it and sends it on each request, and the server verifies it to find the user. It's stateless (no session to look up) and works cross-origin, unlike cookie sessions."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use token authentication and when do you use it when its rule makes the code safer, clearer, or more efficient for the actual problem. Token auth is authenticating with a token the client sends on each request (usually in the Authorization header: Authorization: Bearer <token>), instead of a session cookie. It's used for APIs and SPAs where the client isn't a browser with cookies — mobile apps, other services, a React front end. On login the server issues a token (random, stored hashed in the DB, or a signed JWT), the client stores it and sends it on each request, and the server verifies it to find the user. It's stateless (no session to look up) and works cross-origin, unlike cookie sessions. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Token auth is authenticating with a token the client sends on each request (usually in the Authorization header: Authorization: Bearer <token>), instead of a session cookie.\n\nIt's used for APIs and SPAs where the client isn't a browser with cookies — mobile apps, other services, a React front end.\n\nOn login the server issues a token (random, stored hashed in the DB, or a signed JWT), the client stores it and sends it on each request, and the server verifies it to find the user. It's stateless (no session to look up) and works cross-origin, unlike cookie sessions.\n\nSending the token in a query param (logged) instead of the Authorization header, or storing tokens in localStorage (XSS)."
          },
          {
            "type": "code_example",
            "title": "Token auth flow",
            "content": "```ruby\n# Issue a token on login\nclass Api::SessionsController < ApplicationController\n  def create\n    user = User.authenticate(params[:email], params[:password])\n    if user\n      token = SecureRandom.hex(32)                # or a signed JWT\n      user.update(auth_token: Digest::SHA256.hexdigest(token))  # store hashed!\n      render json: { token: token }                # send to client\n    else\n      render json: { error: 'invalid' }, status: :unauthorized\n    end\n  end\nend\n\n# Verify on each request (a before_action)\nclass ApiController < ApplicationController\n  before_action :authenticate_token!\n\n  def authenticate_token!\n    token = request.headers['Authorization']&.sub('Bearer ', '')\n    user = User.find_by(auth_token: Digest::SHA256.hexdigest(token))\n    render json: { error: 'unauthorized' }, status: :unauthorized unless user\n    @current_user = user\n  end\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "What is token authentication and when do you use it?",
        "How would you compare Token Auth Intro with an alternative?",
        "What is a common mistake when using Token Auth Intro?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Token Auth Intro in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Token Auth Intro in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "authentication-basics-token-auth-intro-q002",
      "slug": "authentication-basics-token-auth-intro-common-mistake",
      "question": "What is a common mistake when using Token Auth Intro?",
      "title": "a common mistake when using Token Auth Intro",
      "direct_answer": "A common mistake with token authentication and when do you use it is using the rule without checking its boundary conditions. Token auth is authenticating with a token the client sends on each request (usually in the Authorization header: Authorization: Bearer <token>), instead of a session cookie. That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain token authentication and when do you use it in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about token authentication and when do you use it without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of token authentication and when do you use it. Token auth is authenticating with a token the client sends on each request (usually in the Authorization header: Authorization: Bearer <token>), instead of a session cookie. It's used for APIs and SPAs where the client isn't a browser with cookies — mobile apps, other services, a React front end. On login the server issues a token (random, stored hashed in the DB, or a signed JWT), the client stores it and sends it on each request, and the server verifies it to find the user. It's stateless (no session to look up) and works cross-origin, unlike cookie sessions."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with token authentication and when do you use it is assuming the happy path is the whole rule. Token auth is authenticating with a token the client sends on each request (usually in the Authorization header: Authorization: Bearer <token>), instead of a session cookie. It's used for APIs and SPAs where the client isn't a browser with cookies — mobile apps, other services, a React front end. On login the server issues a token (random, stored hashed in the DB, or a signed JWT), the client stores it and sends it on each request, and the server verifies it to find the user. It's stateless (no session to look up) and works cross-origin, unlike cookie sessions. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "Token auth is authenticating with a token the client sends on each request (usually in the Authorization header: Authorization: Bearer <token>), instead of a session cookie.\n\nIt's used for APIs and SPAs where the client isn't a browser with cookies — mobile apps, other services, a React front end.\n\nOn login the server issues a token (random, stored hashed in the DB, or a signed JWT), the client stores it and sends it on each request, and the server verifies it to find the user. It's stateless (no session to look up) and works cross-origin, unlike cookie sessions.\n\nSending the token in a query param (logged) instead of the Authorization header, or storing tokens in localStorage (XSS)."
          },
          {
            "type": "code_example",
            "title": "Token auth flow",
            "content": "```ruby\n# Issue a token on login\nclass Api::SessionsController < ApplicationController\n  def create\n    user = User.authenticate(params[:email], params[:password])\n    if user\n      token = SecureRandom.hex(32)                # or a signed JWT\n      user.update(auth_token: Digest::SHA256.hexdigest(token))  # store hashed!\n      render json: { token: token }                # send to client\n    else\n      render json: { error: 'invalid' }, status: :unauthorized\n    end\n  end\nend\n\n# Verify on each request (a before_action)\nclass ApiController < ApplicationController\n  before_action :authenticate_token!\n\n  def authenticate_token!\n    token = request.headers['Authorization']&.sub('Bearer ', '')\n    user = User.find_by(auth_token: Digest::SHA256.hexdigest(token))\n    render json: { error: 'unauthorized' }, status: :unauthorized unless user\n    @current_user = user\n  end\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "What is token authentication and when do you use it?",
        "When would you use Token Auth Intro in a real project?",
        "How would you compare Token Auth Intro with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Token Auth Intro — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Token Auth Intro? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "authentication-basics-token-auth-intro-q003",
      "slug": "authentication-basics-token-auth-intro-compare",
      "question": "How would you compare Token Auth Intro with an alternative?",
      "title": "compare Token Auth Intro with an alternative",
      "direct_answer": "I compare token authentication and when do you use it with the simpler alternative by looking at correctness first, then time, memory, and readability. Token auth is authenticating with a token the client sends on each request (usually in the Authorization header: Authorization: Bearer <token>), instead of a session cookie. The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain token authentication and when do you use it in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about token authentication and when do you use it without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. Token auth is authenticating with a token the client sends on each request (usually in the Authorization header: Authorization: Bearer <token>), instead of a session cookie. It's used for APIs and SPAs where the client isn't a browser with cookies — mobile apps, other services, a React front end. On login the server issues a token (random, stored hashed in the DB, or a signed JWT), the client stores it and sends it on each request, and the server verifies it to find the user. It's stateless (no session to look up) and works cross-origin, unlike cookie sessions."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare token authentication and when do you use it with the straightforward alternative on the same example. Token auth is authenticating with a token the client sends on each request (usually in the Authorization header: Authorization: Bearer <token>), instead of a session cookie. It's used for APIs and SPAs where the client isn't a browser with cookies — mobile apps, other services, a React front end. On login the server issues a token (random, stored hashed in the DB, or a signed JWT), the client stores it and sends it on each request, and the server verifies it to find the user. It's stateless (no session to look up) and works cross-origin, unlike cookie sessions. If token authentication and when do you use it removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "Token auth is authenticating with a token the client sends on each request (usually in the Authorization header: Authorization: Bearer <token>), instead of a session cookie.\n\nIt's used for APIs and SPAs where the client isn't a browser with cookies — mobile apps, other services, a React front end.\n\nOn login the server issues a token (random, stored hashed in the DB, or a signed JWT), the client stores it and sends it on each request, and the server verifies it to find the user. It's stateless (no session to look up) and works cross-origin, unlike cookie sessions.\n\nSending the token in a query param (logged) instead of the Authorization header, or storing tokens in localStorage (XSS)."
          },
          {
            "type": "code_example",
            "title": "Token auth flow",
            "content": "```ruby\n# Issue a token on login\nclass Api::SessionsController < ApplicationController\n  def create\n    user = User.authenticate(params[:email], params[:password])\n    if user\n      token = SecureRandom.hex(32)                # or a signed JWT\n      user.update(auth_token: Digest::SHA256.hexdigest(token))  # store hashed!\n      render json: { token: token }                # send to client\n    else\n      render json: { error: 'invalid' }, status: :unauthorized\n    end\n  end\nend\n\n# Verify on each request (a before_action)\nclass ApiController < ApplicationController\n  before_action :authenticate_token!\n\n  def authenticate_token!\n    token = request.headers['Authorization']&.sub('Bearer ', '')\n    user = User.find_by(auth_token: Digest::SHA256.hexdigest(token))\n    render json: { error: 'unauthorized' }, status: :unauthorized unless user\n    @current_user = user\n  end\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "What is token authentication and when do you use it?",
        "When would you use Token Auth Intro in a real project?",
        "What is a common mistake when using Token Auth Intro?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Token Auth Intro with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Token Auth Intro with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "authentication-basics-token-auth-intro-q004",
      "slug": "authentication-basics-token-auth-intro-scenario",
      "question": "How would you debug a problem involving Token Auth Intro?",
      "title": "debug a problem involving Token Auth Intro",
      "direct_answer": "I would debug a problem involving token authentication and when do you use it by reproducing it with the smallest input first. Token auth is authenticating with a token the client sends on each request (usually in the Authorization header: Authorization: Bearer <token>), instead of a session cookie. Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain token authentication and when do you use it in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about token authentication and when do you use it without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what token authentication and when do you use it is supposed to guarantee. Token auth is authenticating with a token the client sends on each request (usually in the Authorization header: Authorization: Bearer <token>), instead of a session cookie. It's used for APIs and SPAs where the client isn't a browser with cookies — mobile apps, other services, a React front end. On login the server issues a token (random, stored hashed in the DB, or a signed JWT), the client stores it and sends it on each request, and the server verifies it to find the user. It's stateless (no session to look up) and works cross-origin, unlike cookie sessions."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the token authentication and when do you use it bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: Token auth is authenticating with a token the client sends on each request (usually in the Authorization header: Authorization: Bearer <token>), instead of a session cookie. It's used for APIs and SPAs where the client isn't a browser with cookies — mobile apps, other services, a React front end. On login the server issues a token (random, stored hashed in the DB, or a signed JWT), the client stores it and sends it on each request, and the server verifies it to find the user. It's stateless (no session to look up) and works cross-origin, unlike cookie sessions. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "Token auth is authenticating with a token the client sends on each request (usually in the Authorization header: Authorization: Bearer <token>), instead of a session cookie.\n\nIt's used for APIs and SPAs where the client isn't a browser with cookies — mobile apps, other services, a React front end.\n\nOn login the server issues a token (random, stored hashed in the DB, or a signed JWT), the client stores it and sends it on each request, and the server verifies it to find the user. It's stateless (no session to look up) and works cross-origin, unlike cookie sessions.\n\nSending the token in a query param (logged) instead of the Authorization header, or storing tokens in localStorage (XSS)."
          },
          {
            "type": "code_example",
            "title": "Token auth flow",
            "content": "```ruby\n# Issue a token on login\nclass Api::SessionsController < ApplicationController\n  def create\n    user = User.authenticate(params[:email], params[:password])\n    if user\n      token = SecureRandom.hex(32)                # or a signed JWT\n      user.update(auth_token: Digest::SHA256.hexdigest(token))  # store hashed!\n      render json: { token: token }                # send to client\n    else\n      render json: { error: 'invalid' }, status: :unauthorized\n    end\n  end\nend\n\n# Verify on each request (a before_action)\nclass ApiController < ApplicationController\n  before_action :authenticate_token!\n\n  def authenticate_token!\n    token = request.headers['Authorization']&.sub('Bearer ', '')\n    user = User.find_by(auth_token: Digest::SHA256.hexdigest(token))\n    render json: { error: 'unauthorized' }, status: :unauthorized unless user\n    @current_user = user\n  end\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "What is token authentication and when do you use it?",
        "When would you use Token Auth Intro in a real project?",
        "How would you compare Token Auth Intro with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Token Auth Intro — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Token Auth Intro? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

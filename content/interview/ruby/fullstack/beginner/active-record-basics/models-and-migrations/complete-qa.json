{
  "topic": "Models and Migrations",
  "topicSlug": "models-and-migrations",
  "questions": [
    {
      "id": "models-and-migrations-q001",
      "slug": "rails-models-and-migrations",
      "question": "How do Rails models and migrations work together?",
      "title": "Rails Models and Migrations",
      "direct_answer": "A model (app/models/user.rb, class User < ApplicationRecord) is the Ruby class that maps to a database table and gives you the query methods and validations. A migration is the Ruby file that creates or changes that table (the schema). They work together: the migration creates the users table and its columns; the model is the Ruby code you interact with that reads and writes those rows. The model is the behavior; the migration is the structure. You generate both with rails g model User name:string, which creates the migration AND an empty model file.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you distinguish the model (behavior, Ruby class) from the migration (structure, schema change) and know rails g model creates both.",
        "common_mistake": "Thinking the model 'is' the table, or editing the model file to change columns (you change the schema via a migration).",
        "to_stand_out": "The model is the Ruby class (behavior); the migration is the schema change (structure). rails g model creates both; you change columns via new migrations, not the model."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define Rails Models and Migrations simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "The model (app/models/user.rb) is the Ruby class — behavior: validations, associations, methods.",
              "The migration (db/migrate) is the schema change — structure: create_table, add_column, add_index.",
              "rails g model User name:string creates BOTH a migration and an empty model file.",
              "Change columns via a new migration, not by editing the model file.",
              "The model reads/writes the table the migration created."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "A Rails model — app/models/user.rb, class User inherits ApplicationRecord — is the Ruby class that maps to a database table and gives you the query methods, validations, and associations. A migration is the Ruby file that creates or changes that table, the schema. They work together: the migration creates the users table and its columns; the model is the Ruby code you interact with that reads and writes those rows. The model is the behavior; the migration is the structure. You generate both at once with rails g model User name string, which creates the migration file and an empty model file you then fill with validations and associations. A common mistake is thinking the model is the table, when the model is the Ruby class and the table is the schema, or editing the model file to add a column, when you should change the schema via a new migration."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "A model (app/models/user.rb, class User < ApplicationRecord) is the Ruby class that maps to a database table and gives you the query methods and validations.\n\nA migration is the Ruby file that creates or changes that table (the schema).\n\nThey work together: the migration creates the users table and its columns; the model is the Ruby code you interact with that reads and writes those rows. The model is the behavior; the migration is the structure. You generate both with rails g model User name:string, which creates the migration AND an empty model file.\n\nThinking the model 'is' the table, or editing the model file to change columns (you change the schema via a migration)."
          },
          {
            "type": "flow_diagram",
            "title": "Model and Migration Together",
            "content": "The migration builds the table; the model is the class you use to read/write it.\n\n```mermaid\nflowchart LR\n  G[rails g model User] --> M[migration: create users table]\n  G --> C[model: class User < ApplicationRecord]\n  C -->|reads/writes| T[users table built by M]\n```"
          },
          {
            "type": "code_example",
            "title": "Generating and using a model + migration",
            "content": "```ruby\n# Generate BOTH a model and its migration:\n#   rails g model User name:string email:string\n# Creates:\n#   db/migrate/<ts>_create_users.rb  (the migration - structure)\n#   app/models/user.rb              (the model - behavior)\n\nclass CreateUsers < ActiveRecord::Migration[7.0]\n  def change\n    create_table :users do |t|     # STRUCTURE: the table and columns\n      t.string :name\n      t.string :email\n      t.timestamps\n    end\n  end\nend\n\nclass User < ApplicationRecord     # BEHAVIOR: validations, associations\n  validates :email, presence: true, uniqueness: true\n  has_many :posts\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Models And Migrations in a real project?",
        "How would you compare Models And Migrations with an alternative?",
        "What is a common mistake when using Models And Migrations?"
      ],
      "seo": {
        "metaTitle": "Rails Models and Migrations — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How do Rails models and migrations work together? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "active-record-basics-models-and-migrations-q001",
      "slug": "active-record-basics-models-and-migrations-when-to-use",
      "question": "When would you use Models And Migrations in a real project?",
      "title": "When would you use Models And Migrations in a real project",
      "direct_answer": "I would use How do Rails models and migrations work together when the problem matches its main benefit. A model (app/models/user.rb, class User < ApplicationRecord) is the Ruby class that maps to a database table and gives you the query methods and validations. In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How do Rails models and migrations work together in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do Rails models and migrations work together without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining How do Rails models and migrations work together in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. A model (app/models/user.rb, class User < ApplicationRecord) is the Ruby class that maps to a database table and gives you the query methods and validations. A migration is the Ruby file that creates or changes that table (the schema). They work together: the migration creates the users table and its columns; the model is the Ruby code you interact with that reads and writes those rows. The model is the behavior; the migration is the structure. You generate both with rails g model User name:string, which creates the migration AND an empty model file."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use How do Rails models and migrations work together when its rule makes the code safer, clearer, or more efficient for the actual problem. A model (app/models/user.rb, class User < ApplicationRecord) is the Ruby class that maps to a database table and gives you the query methods and validations. A migration is the Ruby file that creates or changes that table (the schema). They work together: the migration creates the users table and its columns; the model is the Ruby code you interact with that reads and writes those rows. The model is the behavior; the migration is the structure. You generate both with rails g model User name:string, which creates the migration AND an empty model file. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "A model (app/models/user.rb, class User < ApplicationRecord) is the Ruby class that maps to a database table and gives you the query methods and validations.\n\nA migration is the Ruby file that creates or changes that table (the schema).\n\nThey work together: the migration creates the users table and its columns; the model is the Ruby code you interact with that reads and writes those rows. The model is the behavior; the migration is the structure. You generate both with rails g model User name:string, which creates the migration AND an empty model file.\n\nThinking the model 'is' the table, or editing the model file to change columns (you change the schema via a migration)."
          },
          {
            "type": "code_example",
            "title": "Generating and using a model + migration",
            "content": "```ruby\n# Generate BOTH a model and its migration:\n#   rails g model User name:string email:string\n# Creates:\n#   db/migrate/<ts>_create_users.rb  (the migration - structure)\n#   app/models/user.rb              (the model - behavior)\n\nclass CreateUsers < ActiveRecord::Migration[7.0]\n  def change\n    create_table :users do |t|     # STRUCTURE: the table and columns\n      t.string :name\n      t.string :email\n      t.timestamps\n    end\n  end\nend\n\nclass User < ApplicationRecord     # BEHAVIOR: validations, associations\n  validates :email, presence: true, uniqueness: true\n  has_many :posts\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "How do Rails models and migrations work together?",
        "How would you compare Models And Migrations with an alternative?",
        "What is a common mistake when using Models And Migrations?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Models And Migrations in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Models And Migrations in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "active-record-basics-models-and-migrations-q002",
      "slug": "active-record-basics-models-and-migrations-common-mistake",
      "question": "What is a common mistake when using Models And Migrations?",
      "title": "a common mistake when using Models And Migrations",
      "direct_answer": "A common mistake with How do Rails models and migrations work together is using the rule without checking its boundary conditions. A model (app/models/user.rb, class User < ApplicationRecord) is the Ruby class that maps to a database table and gives you the query methods and validations. That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How do Rails models and migrations work together in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do Rails models and migrations work together without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of How do Rails models and migrations work together. A model (app/models/user.rb, class User < ApplicationRecord) is the Ruby class that maps to a database table and gives you the query methods and validations. A migration is the Ruby file that creates or changes that table (the schema). They work together: the migration creates the users table and its columns; the model is the Ruby code you interact with that reads and writes those rows. The model is the behavior; the migration is the structure. You generate both with rails g model User name:string, which creates the migration AND an empty model file."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with How do Rails models and migrations work together is assuming the happy path is the whole rule. A model (app/models/user.rb, class User < ApplicationRecord) is the Ruby class that maps to a database table and gives you the query methods and validations. A migration is the Ruby file that creates or changes that table (the schema). They work together: the migration creates the users table and its columns; the model is the Ruby code you interact with that reads and writes those rows. The model is the behavior; the migration is the structure. You generate both with rails g model User name:string, which creates the migration AND an empty model file. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "A model (app/models/user.rb, class User < ApplicationRecord) is the Ruby class that maps to a database table and gives you the query methods and validations.\n\nA migration is the Ruby file that creates or changes that table (the schema).\n\nThey work together: the migration creates the users table and its columns; the model is the Ruby code you interact with that reads and writes those rows. The model is the behavior; the migration is the structure. You generate both with rails g model User name:string, which creates the migration AND an empty model file.\n\nThinking the model 'is' the table, or editing the model file to change columns (you change the schema via a migration)."
          },
          {
            "type": "code_example",
            "title": "Generating and using a model + migration",
            "content": "```ruby\n# Generate BOTH a model and its migration:\n#   rails g model User name:string email:string\n# Creates:\n#   db/migrate/<ts>_create_users.rb  (the migration - structure)\n#   app/models/user.rb              (the model - behavior)\n\nclass CreateUsers < ActiveRecord::Migration[7.0]\n  def change\n    create_table :users do |t|     # STRUCTURE: the table and columns\n      t.string :name\n      t.string :email\n      t.timestamps\n    end\n  end\nend\n\nclass User < ApplicationRecord     # BEHAVIOR: validations, associations\n  validates :email, presence: true, uniqueness: true\n  has_many :posts\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "How do Rails models and migrations work together?",
        "When would you use Models And Migrations in a real project?",
        "How would you compare Models And Migrations with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Models And Migrations — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Models And Migrations? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "active-record-basics-models-and-migrations-q003",
      "slug": "active-record-basics-models-and-migrations-compare",
      "question": "How would you compare Models And Migrations with an alternative?",
      "title": "compare Models And Migrations with an alternative",
      "direct_answer": "I compare How do Rails models and migrations work together with the simpler alternative by looking at correctness first, then time, memory, and readability. A model (app/models/user.rb, class User < ApplicationRecord) is the Ruby class that maps to a database table and gives you the query methods and validations. The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How do Rails models and migrations work together in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do Rails models and migrations work together without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. A model (app/models/user.rb, class User < ApplicationRecord) is the Ruby class that maps to a database table and gives you the query methods and validations. A migration is the Ruby file that creates or changes that table (the schema). They work together: the migration creates the users table and its columns; the model is the Ruby code you interact with that reads and writes those rows. The model is the behavior; the migration is the structure. You generate both with rails g model User name:string, which creates the migration AND an empty model file."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare How do Rails models and migrations work together with the straightforward alternative on the same example. A model (app/models/user.rb, class User < ApplicationRecord) is the Ruby class that maps to a database table and gives you the query methods and validations. A migration is the Ruby file that creates or changes that table (the schema). They work together: the migration creates the users table and its columns; the model is the Ruby code you interact with that reads and writes those rows. The model is the behavior; the migration is the structure. You generate both with rails g model User name:string, which creates the migration AND an empty model file. If How do Rails models and migrations work together removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "A model (app/models/user.rb, class User < ApplicationRecord) is the Ruby class that maps to a database table and gives you the query methods and validations.\n\nA migration is the Ruby file that creates or changes that table (the schema).\n\nThey work together: the migration creates the users table and its columns; the model is the Ruby code you interact with that reads and writes those rows. The model is the behavior; the migration is the structure. You generate both with rails g model User name:string, which creates the migration AND an empty model file.\n\nThinking the model 'is' the table, or editing the model file to change columns (you change the schema via a migration)."
          },
          {
            "type": "code_example",
            "title": "Generating and using a model + migration",
            "content": "```ruby\n# Generate BOTH a model and its migration:\n#   rails g model User name:string email:string\n# Creates:\n#   db/migrate/<ts>_create_users.rb  (the migration - structure)\n#   app/models/user.rb              (the model - behavior)\n\nclass CreateUsers < ActiveRecord::Migration[7.0]\n  def change\n    create_table :users do |t|     # STRUCTURE: the table and columns\n      t.string :name\n      t.string :email\n      t.timestamps\n    end\n  end\nend\n\nclass User < ApplicationRecord     # BEHAVIOR: validations, associations\n  validates :email, presence: true, uniqueness: true\n  has_many :posts\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "How do Rails models and migrations work together?",
        "When would you use Models And Migrations in a real project?",
        "What is a common mistake when using Models And Migrations?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Models And Migrations with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Models And Migrations with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "active-record-basics-models-and-migrations-q004",
      "slug": "active-record-basics-models-and-migrations-scenario",
      "question": "How would you debug a problem involving Models And Migrations?",
      "title": "debug a problem involving Models And Migrations",
      "direct_answer": "I would debug a problem involving How do Rails models and migrations work together by reproducing it with the smallest input first. A model (app/models/user.rb, class User < ApplicationRecord) is the Ruby class that maps to a database table and gives you the query methods and validations. Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How do Rails models and migrations work together in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do Rails models and migrations work together without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what How do Rails models and migrations work together is supposed to guarantee. A model (app/models/user.rb, class User < ApplicationRecord) is the Ruby class that maps to a database table and gives you the query methods and validations. A migration is the Ruby file that creates or changes that table (the schema). They work together: the migration creates the users table and its columns; the model is the Ruby code you interact with that reads and writes those rows. The model is the behavior; the migration is the structure. You generate both with rails g model User name:string, which creates the migration AND an empty model file."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the How do Rails models and migrations work together bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: A model (app/models/user.rb, class User < ApplicationRecord) is the Ruby class that maps to a database table and gives you the query methods and validations. A migration is the Ruby file that creates or changes that table (the schema). They work together: the migration creates the users table and its columns; the model is the Ruby code you interact with that reads and writes those rows. The model is the behavior; the migration is the structure. You generate both with rails g model User name:string, which creates the migration AND an empty model file. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "A model (app/models/user.rb, class User < ApplicationRecord) is the Ruby class that maps to a database table and gives you the query methods and validations.\n\nA migration is the Ruby file that creates or changes that table (the schema).\n\nThey work together: the migration creates the users table and its columns; the model is the Ruby code you interact with that reads and writes those rows. The model is the behavior; the migration is the structure. You generate both with rails g model User name:string, which creates the migration AND an empty model file.\n\nThinking the model 'is' the table, or editing the model file to change columns (you change the schema via a migration)."
          },
          {
            "type": "code_example",
            "title": "Generating and using a model + migration",
            "content": "```ruby\n# Generate BOTH a model and its migration:\n#   rails g model User name:string email:string\n# Creates:\n#   db/migrate/<ts>_create_users.rb  (the migration - structure)\n#   app/models/user.rb              (the model - behavior)\n\nclass CreateUsers < ActiveRecord::Migration[7.0]\n  def change\n    create_table :users do |t|     # STRUCTURE: the table and columns\n      t.string :name\n      t.string :email\n      t.timestamps\n    end\n  end\nend\n\nclass User < ApplicationRecord     # BEHAVIOR: validations, associations\n  validates :email, presence: true, uniqueness: true\n  has_many :posts\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "How do Rails models and migrations work together?",
        "When would you use Models And Migrations in a real project?",
        "How would you compare Models And Migrations with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Models And Migrations — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Models And Migrations? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

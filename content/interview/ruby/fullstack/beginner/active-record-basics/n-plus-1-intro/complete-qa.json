{
  "topic": "N+1 Intro",
  "topicSlug": "n-plus-1-intro",
  "questions": [
    {
      "id": "n-plus-1-intro-q001",
      "slug": "rails-activerecord-n-plus-1-intro",
      "question": "What is the N+1 query problem in ActiveRecord and how do you fix it?",
      "title": "The N+1 Query Problem",
      "direct_answer": "The N+1 problem is when you fetch N records and then trigger one extra query per record to load an association — 1 query for the posts plus N queries for each post's comments (N+1 total), which is slow. It happens when you call an association method on each record in a loop (post.comments inside posts.each). You fix it with eager loading: Post.includes(:comments) loads all the comments in one extra query upfront (or one JOIN), so iterating no longer fires per-row queries. Spot it in the log as repeated SELECTs on the associated table.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you can spot N+1 (one query + N per-record queries) and fix it with includes (eager loading).",
        "common_mistake": "Not noticing the N+1 in the log, or 'fixing' it by caching instead of eager loading.",
        "to_stand_out": "N+1 = 1 query for the records + N per-record association queries. Fix with Post.includes(:comments) to eager-load in one extra query."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define The N+1 Query Problem simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "N+1 = 1 query for the records plus N per-record association queries (one per row).",
              "It happens when you call an association in a loop (post.comments inside posts.each).",
              "Fix with Post.includes(:comments) to eager-load all comments in one extra query.",
              "Spot it in the log as repeated SELECTs on the associated table.",
              "Only eager-load associations you actually use (don't includes everything blindly)."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "The N+1 problem is when you fetch N records and then trigger one extra query per record to load an association — one query for the posts plus N queries for each post's comments, N+1 total, which is slow. It happens when you call an association method on each record in a loop: post.comments inside posts.each fires a query for each post. You fix it with eager loading: Post.includes of comments loads all the comments in one extra query upfront, or one JOIN, so iterating no longer fires per-row queries. You spot it in the log as repeated SELECTs on the associated table, which is the signature of N+1. A common mistake is not noticing the N+1 in the log, or fixing it by caching instead of eager loading, when the real fix is includes."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "The N+1 problem is when you fetch N records and then trigger one extra query per record to load an association — 1 query for the posts plus N queries for each post's comments (N+1 total), which is slow.\n\nIt happens when you call an association method on each record in a loop (post.comments inside posts.each).\n\nYou fix it with eager loading: Post.includes(:comments) loads all the comments in one extra query upfront (or one JOIN), so iterating no longer fires per-row queries. Spot it in the log as repeated SELECTs on the associated table.\n\nNot noticing the N+1 in the log, or 'fixing' it by caching instead of eager loading."
          },
          {
            "type": "flow_diagram",
            "title": "N+1 vs Eager Loaded",
            "content": "Without includes, each post.comments fires a query; with includes, one extra query loads them all.\n\n```mermaid\nflowchart LR\n  subgraph N+1\n    Q1[SELECT * FROM posts] --> P1[post1.comments -> query]\n    P1 --> P2[post2.comments -> query]\n  end\n  subgraph Fixed\n    I[Post.includes\\(:comments\\)] --> Q2[1 query for posts + 1 for all comments]\n  end\n```"
          },
          {
            "type": "code_example",
            "title": "N+1 and the includes fix",
            "content": "```ruby\n# N+1: 1 query for posts, then N queries (one per post) for comments.\nposts = Post.all\nposts.each { |p| puts p.comments.count }   # N queries on comments!\n\n# FIX: eager-load comments with includes (1 extra query for ALL comments).\nposts = Post.includes(:comments)\nposts.each { |p| puts p.comments.length }   # no per-post query\n\n# In the log you'll see:\n#   SELECT 'posts'.* FROM 'posts'\n#   SELECT 'comments'.* FROM 'comments' WHERE 'comments'.'post_id' IN (1,2,3,...)  # one query\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use N Plus 1 Intro in a real project?",
        "How would you compare N Plus 1 Intro with an alternative?",
        "What is a common mistake when using N Plus 1 Intro?"
      ],
      "seo": {
        "metaTitle": "The N+1 Query Problem — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is the N+1 query problem in ActiveRecord and how do you fix it? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "active-record-basics-n-plus-1-intro-q001",
      "slug": "active-record-basics-n-plus-1-intro-when-to-use",
      "question": "When would you use N Plus 1 Intro in a real project?",
      "title": "When would you use N Plus 1 Intro in a real project",
      "direct_answer": "I would use the N+1 query problem in ActiveRecord and how do you fix it when the problem matches its main benefit. The N+1 problem is when you fetch N records and then trigger one extra query per record to load an association — 1 query for the posts plus N queries for each post's comments (N+1 total), which is slow. In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain the N+1 query problem in ActiveRecord and how do you fix it in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about the N+1 query problem in ActiveRecord and how do you fix it without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining the N+1 query problem in ActiveRecord and how do you fix it in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. The N+1 problem is when you fetch N records and then trigger one extra query per record to load an association — 1 query for the posts plus N queries for each post's comments (N+1 total), which is slow. It happens when you call an association method on each record in a loop (post.comments inside posts.each). You fix it with eager loading: Post.includes(:comments) loads all the comments in one extra query upfront (or one JOIN), so iterating no longer fires per-row queries. Spot it in the log as repeated SELECTs on the associated table."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use the N+1 query problem in ActiveRecord and how do you fix it when its rule makes the code safer, clearer, or more efficient for the actual problem. The N+1 problem is when you fetch N records and then trigger one extra query per record to load an association — 1 query for the posts plus N queries for each post's comments (N+1 total), which is slow. It happens when you call an association method on each record in a loop (post.comments inside posts.each). You fix it with eager loading: Post.includes(:comments) loads all the comments in one extra query upfront (or one JOIN), so iterating no longer fires per-row queries. Spot it in the log as repeated SELECTs on the associated table. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "The N+1 problem is when you fetch N records and then trigger one extra query per record to load an association — 1 query for the posts plus N queries for each post's comments (N+1 total), which is slow.\n\nIt happens when you call an association method on each record in a loop (post.comments inside posts.each).\n\nYou fix it with eager loading: Post.includes(:comments) loads all the comments in one extra query upfront (or one JOIN), so iterating no longer fires per-row queries. Spot it in the log as repeated SELECTs on the associated table.\n\nNot noticing the N+1 in the log, or 'fixing' it by caching instead of eager loading."
          },
          {
            "type": "code_example",
            "title": "N+1 and the includes fix",
            "content": "```ruby\n# N+1: 1 query for posts, then N queries (one per post) for comments.\nposts = Post.all\nposts.each { |p| puts p.comments.count }   # N queries on comments!\n\n# FIX: eager-load comments with includes (1 extra query for ALL comments).\nposts = Post.includes(:comments)\nposts.each { |p| puts p.comments.length }   # no per-post query\n\n# In the log you'll see:\n#   SELECT 'posts'.* FROM 'posts'\n#   SELECT 'comments'.* FROM 'comments' WHERE 'comments'.'post_id' IN (1,2,3,...)  # one query\n```"
          }
        ]
      },
      "followup_questions": [
        "How would you compare N Plus 1 Intro with an alternative?",
        "What is a common mistake when using N Plus 1 Intro?",
        "What is the N+1 query problem in ActiveRecord and how do you fix it?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use N Plus 1 Intro in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use N Plus 1 Intro in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "active-record-basics-n-plus-1-intro-q002",
      "slug": "active-record-basics-n-plus-1-intro-common-mistake",
      "question": "What is a common mistake when using N Plus 1 Intro?",
      "title": "a common mistake when using N Plus 1 Intro",
      "direct_answer": "A common mistake with the N+1 query problem in ActiveRecord and how do you fix it is using the rule without checking its boundary conditions. The N+1 problem is when you fetch N records and then trigger one extra query per record to load an association — 1 query for the posts plus N queries for each post's comments (N+1 total), which is slow. That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain the N+1 query problem in ActiveRecord and how do you fix it in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about the N+1 query problem in ActiveRecord and how do you fix it without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of the N+1 query problem in ActiveRecord and how do you fix it. The N+1 problem is when you fetch N records and then trigger one extra query per record to load an association — 1 query for the posts plus N queries for each post's comments (N+1 total), which is slow. It happens when you call an association method on each record in a loop (post.comments inside posts.each). You fix it with eager loading: Post.includes(:comments) loads all the comments in one extra query upfront (or one JOIN), so iterating no longer fires per-row queries. Spot it in the log as repeated SELECTs on the associated table."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with the N+1 query problem in ActiveRecord and how do you fix it is assuming the happy path is the whole rule. The N+1 problem is when you fetch N records and then trigger one extra query per record to load an association — 1 query for the posts plus N queries for each post's comments (N+1 total), which is slow. It happens when you call an association method on each record in a loop (post.comments inside posts.each). You fix it with eager loading: Post.includes(:comments) loads all the comments in one extra query upfront (or one JOIN), so iterating no longer fires per-row queries. Spot it in the log as repeated SELECTs on the associated table. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "The N+1 problem is when you fetch N records and then trigger one extra query per record to load an association — 1 query for the posts plus N queries for each post's comments (N+1 total), which is slow.\n\nIt happens when you call an association method on each record in a loop (post.comments inside posts.each).\n\nYou fix it with eager loading: Post.includes(:comments) loads all the comments in one extra query upfront (or one JOIN), so iterating no longer fires per-row queries. Spot it in the log as repeated SELECTs on the associated table.\n\nNot noticing the N+1 in the log, or 'fixing' it by caching instead of eager loading."
          },
          {
            "type": "code_example",
            "title": "N+1 and the includes fix",
            "content": "```ruby\n# N+1: 1 query for posts, then N queries (one per post) for comments.\nposts = Post.all\nposts.each { |p| puts p.comments.count }   # N queries on comments!\n\n# FIX: eager-load comments with includes (1 extra query for ALL comments).\nposts = Post.includes(:comments)\nposts.each { |p| puts p.comments.length }   # no per-post query\n\n# In the log you'll see:\n#   SELECT 'posts'.* FROM 'posts'\n#   SELECT 'comments'.* FROM 'comments' WHERE 'comments'.'post_id' IN (1,2,3,...)  # one query\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use N Plus 1 Intro in a real project?",
        "How would you compare N Plus 1 Intro with an alternative?",
        "What is the N+1 query problem in ActiveRecord and how do you fix it?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using N Plus 1 Intro — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using N Plus 1 Intro? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "active-record-basics-n-plus-1-intro-q003",
      "slug": "active-record-basics-n-plus-1-intro-compare",
      "question": "How would you compare N Plus 1 Intro with an alternative?",
      "title": "compare N Plus 1 Intro with an alternative",
      "direct_answer": "I compare the N+1 query problem in ActiveRecord and how do you fix it with the simpler alternative by looking at correctness first, then time, memory, and readability. The N+1 problem is when you fetch N records and then trigger one extra query per record to load an association — 1 query for the posts plus N queries for each post's comments (N+1 total), which is slow. The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain the N+1 query problem in ActiveRecord and how do you fix it in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about the N+1 query problem in ActiveRecord and how do you fix it without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. The N+1 problem is when you fetch N records and then trigger one extra query per record to load an association — 1 query for the posts plus N queries for each post's comments (N+1 total), which is slow. It happens when you call an association method on each record in a loop (post.comments inside posts.each). You fix it with eager loading: Post.includes(:comments) loads all the comments in one extra query upfront (or one JOIN), so iterating no longer fires per-row queries. Spot it in the log as repeated SELECTs on the associated table."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare the N+1 query problem in ActiveRecord and how do you fix it with the straightforward alternative on the same example. The N+1 problem is when you fetch N records and then trigger one extra query per record to load an association — 1 query for the posts plus N queries for each post's comments (N+1 total), which is slow. It happens when you call an association method on each record in a loop (post.comments inside posts.each). You fix it with eager loading: Post.includes(:comments) loads all the comments in one extra query upfront (or one JOIN), so iterating no longer fires per-row queries. Spot it in the log as repeated SELECTs on the associated table. If the N+1 query problem in ActiveRecord and how do you fix it removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "The N+1 problem is when you fetch N records and then trigger one extra query per record to load an association — 1 query for the posts plus N queries for each post's comments (N+1 total), which is slow.\n\nIt happens when you call an association method on each record in a loop (post.comments inside posts.each).\n\nYou fix it with eager loading: Post.includes(:comments) loads all the comments in one extra query upfront (or one JOIN), so iterating no longer fires per-row queries. Spot it in the log as repeated SELECTs on the associated table.\n\nNot noticing the N+1 in the log, or 'fixing' it by caching instead of eager loading."
          },
          {
            "type": "code_example",
            "title": "N+1 and the includes fix",
            "content": "```ruby\n# N+1: 1 query for posts, then N queries (one per post) for comments.\nposts = Post.all\nposts.each { |p| puts p.comments.count }   # N queries on comments!\n\n# FIX: eager-load comments with includes (1 extra query for ALL comments).\nposts = Post.includes(:comments)\nposts.each { |p| puts p.comments.length }   # no per-post query\n\n# In the log you'll see:\n#   SELECT 'posts'.* FROM 'posts'\n#   SELECT 'comments'.* FROM 'comments' WHERE 'comments'.'post_id' IN (1,2,3,...)  # one query\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use N Plus 1 Intro in a real project?",
        "What is a common mistake when using N Plus 1 Intro?",
        "What is the N+1 query problem in ActiveRecord and how do you fix it?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare N Plus 1 Intro with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare N Plus 1 Intro with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "active-record-basics-n-plus-1-intro-q004",
      "slug": "active-record-basics-n-plus-1-intro-scenario",
      "question": "How would you debug a problem involving N Plus 1 Intro?",
      "title": "debug a problem involving N Plus 1 Intro",
      "direct_answer": "I would debug a problem involving the N+1 query problem in ActiveRecord and how do you fix it by reproducing it with the smallest input first. The N+1 problem is when you fetch N records and then trigger one extra query per record to load an association — 1 query for the posts plus N queries for each post's comments (N+1 total), which is slow. Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain the N+1 query problem in ActiveRecord and how do you fix it in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about the N+1 query problem in ActiveRecord and how do you fix it without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what the N+1 query problem in ActiveRecord and how do you fix it is supposed to guarantee. The N+1 problem is when you fetch N records and then trigger one extra query per record to load an association — 1 query for the posts plus N queries for each post's comments (N+1 total), which is slow. It happens when you call an association method on each record in a loop (post.comments inside posts.each). You fix it with eager loading: Post.includes(:comments) loads all the comments in one extra query upfront (or one JOIN), so iterating no longer fires per-row queries. Spot it in the log as repeated SELECTs on the associated table."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the the N+1 query problem in ActiveRecord and how do you fix it bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: The N+1 problem is when you fetch N records and then trigger one extra query per record to load an association — 1 query for the posts plus N queries for each post's comments (N+1 total), which is slow. It happens when you call an association method on each record in a loop (post.comments inside posts.each). You fix it with eager loading: Post.includes(:comments) loads all the comments in one extra query upfront (or one JOIN), so iterating no longer fires per-row queries. Spot it in the log as repeated SELECTs on the associated table. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "The N+1 problem is when you fetch N records and then trigger one extra query per record to load an association — 1 query for the posts plus N queries for each post's comments (N+1 total), which is slow.\n\nIt happens when you call an association method on each record in a loop (post.comments inside posts.each).\n\nYou fix it with eager loading: Post.includes(:comments) loads all the comments in one extra query upfront (or one JOIN), so iterating no longer fires per-row queries. Spot it in the log as repeated SELECTs on the associated table.\n\nNot noticing the N+1 in the log, or 'fixing' it by caching instead of eager loading."
          },
          {
            "type": "code_example",
            "title": "N+1 and the includes fix",
            "content": "```ruby\n# N+1: 1 query for posts, then N queries (one per post) for comments.\nposts = Post.all\nposts.each { |p| puts p.comments.count }   # N queries on comments!\n\n# FIX: eager-load comments with includes (1 extra query for ALL comments).\nposts = Post.includes(:comments)\nposts.each { |p| puts p.comments.length }   # no per-post query\n\n# In the log you'll see:\n#   SELECT 'posts'.* FROM 'posts'\n#   SELECT 'comments'.* FROM 'comments' WHERE 'comments'.'post_id' IN (1,2,3,...)  # one query\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use N Plus 1 Intro in a real project?",
        "How would you compare N Plus 1 Intro with an alternative?",
        "What is a common mistake when using N Plus 1 Intro?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving N Plus 1 Intro — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving N Plus 1 Intro? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

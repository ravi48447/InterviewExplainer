{
  "topic": "Custom Exceptions",
  "topicSlug": "custom-exceptions",
  "questions": [
    {
      "id": "custom-exceptions-q001",
      "slug": "ruby-custom-exceptions-basics",
      "question": "How do you define and use custom exceptions in Ruby?",
      "title": "Custom Exceptions in Ruby",
      "direct_answer": "To make a custom exception, define a class that inherits from StandardError (or a more specific built-in): class PaymentError < StandardError; end. You raise it with raise PaymentError, 'card declined' (the message is in e.message) and rescue it by class: rescue PaymentError => e. Custom exceptions let callers rescue YOUR error specifically, not a generic StandardError, so the handler can act on the real cause. Keep them under StandardError (not Exception) so they're caught by default rescue and don't capture signals. You can add fields (attr_reader :order_id) by overriding initialize.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know a custom exception inherits from StandardError, is raised with raise, and rescued by class so callers act on the real cause.",
        "common_mistake": "Inheriting from Exception (not StandardError) — won't be caught by default rescue; or rescuing a generic StandardError instead of the specific class.",
        "to_stand_out": "Define a custom exception as class X < StandardError. raise X, 'msg'; rescue X => e. Keep under StandardError; callers rescue YOUR error specifically."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define Custom Exceptions in Ruby simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "A custom exception is a class inheriting from StandardError (or a specific built-in).",
              "raise X, 'msg' raises it; the message is in e.message (call super(msg) if you add fields).",
              "rescue X => e rescues it specifically so the handler acts on the real cause.",
              "Keep under StandardError (not Exception) so default rescue catches it.",
              "Add fields (attr_reader :order_id) by overriding initialize and calling super(msg)."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "To make a custom exception, define a class that inherits from StandardError, or a more specific built-in: class PaymentError less-than StandardError, end. You raise it with raise PaymentError, card declined, and the message is in e dot message, and rescue it by class: rescue PaymentError arrow e. Custom exceptions let callers rescue YOUR error specifically, not a generic StandardError, so the handler can act on the real cause instead of guessing. Keep them under StandardError, not Exception, so they're caught by the default rescue and don't capture signals. You can add fields like attr_reader order_id by overriding initialize to take extra data. A common mistake is inheriting from Exception, not StandardError, because it won't be caught by default rescue, and rescuing a generic StandardError instead of the specific class."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "To make a custom exception, define a class that inherits from StandardError (or a more specific built-in): class PaymentError < StandardError; end.\n\nYou raise it with raise PaymentError, 'card declined' (the message is in e.message) and rescue it by class: rescue PaymentError => e.\n\nCustom exceptions let callers rescue YOUR error specifically, not a generic StandardError, so the handler can act on the real cause. Keep them under StandardError (not Exception) so they're caught by default rescue and don't capture signals. You can add fields (attr_reader :order_id) by overriding initialize.\n\nInheriting from Exception (not StandardError) — won't be caught by default rescue; or rescuing a generic StandardError instead of the specific class."
          },
          {
            "type": "flow_diagram",
            "title": "Define -> Raise -> Rescue",
            "content": "class X < StandardError; raise X, msg; rescue X => e.\n\n```mermaid\nflowchart LR\n  D[class PaymentError < StandardError] --> R[raise PaymentError, 'declined']\n  R --> C[rescue PaymentError => e]\n  C --> H[handle the real cause]\n```"
          },
          {
            "type": "code_example",
            "title": "Custom exceptions",
            "content": "```ruby\n# Define a custom exception (inherit from StandardError!)\nclass PaymentError < StandardError\n  attr_reader :order_id\n  def initialize(msg, order_id: nil)\n    super(msg)                 # set the message\n    @order_id = order_id        # extra context\n  end\nend\n\n# Raise it with a message and context:\ndef charge(order)\n  raise PaymentError.new('card declined', order_id: order.id) if order.failed?\nend\n\n# Rescue it specifically by class:\nbegin\n  charge(order)\nrescue PaymentError => e\n  logger.error(\"payment failed for #{e.order_id}: #{e.message}\")\n  # handler acts on the REAL cause, not a generic StandardError\nend\n\n# Keep it under StandardError (NOT Exception) so default rescue catches it.\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Custom Exceptions in a real project?",
        "How would you compare Custom Exceptions with an alternative?",
        "What is a common mistake when using Custom Exceptions?"
      ],
      "seo": {
        "metaTitle": "Custom Exceptions in Ruby — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How do you define and use custom exceptions in Ruby? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "error-handling-basics-custom-exceptions-q001",
      "slug": "error-handling-basics-custom-exceptions-when-to-use",
      "question": "When would you use Custom Exceptions in a real project?",
      "title": "When would you use Custom Exceptions in a real project",
      "direct_answer": "I would use How do you define and use custom exceptions in Ruby when the problem matches its main benefit. To make a custom exception, define a class that inherits from StandardError (or a more specific built-in): class PaymentError < StandardError; end. In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How do you define and use custom exceptions in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do you define and use custom exceptions in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining How do you define and use custom exceptions in Ruby in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. To make a custom exception, define a class that inherits from StandardError (or a more specific built-in): class PaymentError < StandardError; end. You raise it with raise PaymentError, 'card declined' (the message is in e.message) and rescue it by class: rescue PaymentError => e. Custom exceptions let callers rescue YOUR error specifically, not a generic StandardError, so the handler can act on the real cause. Keep them under StandardError (not Exception) so they're caught by default rescue and don't capture signals. You can add fields (attr_reader :order_id) by overriding initialize."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use How do you define and use custom exceptions in Ruby when its rule makes the code safer, clearer, or more efficient for the actual problem. To make a custom exception, define a class that inherits from StandardError (or a more specific built-in): class PaymentError < StandardError; end. You raise it with raise PaymentError, 'card declined' (the message is in e.message) and rescue it by class: rescue PaymentError => e. Custom exceptions let callers rescue YOUR error specifically, not a generic StandardError, so the handler can act on the real cause. Keep them under StandardError (not Exception) so they're caught by default rescue and don't capture signals. You can add fields (attr_reader :order_id) by overriding initialize. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "To make a custom exception, define a class that inherits from StandardError (or a more specific built-in): class PaymentError < StandardError; end.\n\nYou raise it with raise PaymentError, 'card declined' (the message is in e.message) and rescue it by class: rescue PaymentError => e.\n\nCustom exceptions let callers rescue YOUR error specifically, not a generic StandardError, so the handler can act on the real cause. Keep them under StandardError (not Exception) so they're caught by default rescue and don't capture signals. You can add fields (attr_reader :order_id) by overriding initialize.\n\nInheriting from Exception (not StandardError) — won't be caught by default rescue; or rescuing a generic StandardError instead of the specific class."
          },
          {
            "type": "code_example",
            "title": "Custom exceptions",
            "content": "```ruby\n# Define a custom exception (inherit from StandardError!)\nclass PaymentError < StandardError\n  attr_reader :order_id\n  def initialize(msg, order_id: nil)\n    super(msg)                 # set the message\n    @order_id = order_id        # extra context\n  end\nend\n\n# Raise it with a message and context:\ndef charge(order)\n  raise PaymentError.new('card declined', order_id: order.id) if order.failed?\nend\n\n# Rescue it specifically by class:\nbegin\n  charge(order)\nrescue PaymentError => e\n  logger.error(\"payment failed for #{e.order_id}: #{e.message}\")\n  # handler acts on the REAL cause, not a generic StandardError\nend\n\n# Keep it under StandardError (NOT Exception) so default rescue catches it.\n```"
          }
        ]
      },
      "followup_questions": [
        "How do you define and use custom exceptions in Ruby?",
        "How would you compare Custom Exceptions with an alternative?",
        "What is a common mistake when using Custom Exceptions?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Custom Exceptions in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Custom Exceptions in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "error-handling-basics-custom-exceptions-q002",
      "slug": "error-handling-basics-custom-exceptions-common-mistake",
      "question": "What is a common mistake when using Custom Exceptions?",
      "title": "a common mistake when using Custom Exceptions",
      "direct_answer": "A common mistake with How do you define and use custom exceptions in Ruby is using the rule without checking its boundary conditions. To make a custom exception, define a class that inherits from StandardError (or a more specific built-in): class PaymentError < StandardError; end. That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How do you define and use custom exceptions in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do you define and use custom exceptions in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of How do you define and use custom exceptions in Ruby. To make a custom exception, define a class that inherits from StandardError (or a more specific built-in): class PaymentError < StandardError; end. You raise it with raise PaymentError, 'card declined' (the message is in e.message) and rescue it by class: rescue PaymentError => e. Custom exceptions let callers rescue YOUR error specifically, not a generic StandardError, so the handler can act on the real cause. Keep them under StandardError (not Exception) so they're caught by default rescue and don't capture signals. You can add fields (attr_reader :order_id) by overriding initialize."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with How do you define and use custom exceptions in Ruby is assuming the happy path is the whole rule. To make a custom exception, define a class that inherits from StandardError (or a more specific built-in): class PaymentError < StandardError; end. You raise it with raise PaymentError, 'card declined' (the message is in e.message) and rescue it by class: rescue PaymentError => e. Custom exceptions let callers rescue YOUR error specifically, not a generic StandardError, so the handler can act on the real cause. Keep them under StandardError (not Exception) so they're caught by default rescue and don't capture signals. You can add fields (attr_reader :order_id) by overriding initialize. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "To make a custom exception, define a class that inherits from StandardError (or a more specific built-in): class PaymentError < StandardError; end.\n\nYou raise it with raise PaymentError, 'card declined' (the message is in e.message) and rescue it by class: rescue PaymentError => e.\n\nCustom exceptions let callers rescue YOUR error specifically, not a generic StandardError, so the handler can act on the real cause. Keep them under StandardError (not Exception) so they're caught by default rescue and don't capture signals. You can add fields (attr_reader :order_id) by overriding initialize.\n\nInheriting from Exception (not StandardError) — won't be caught by default rescue; or rescuing a generic StandardError instead of the specific class."
          },
          {
            "type": "code_example",
            "title": "Custom exceptions",
            "content": "```ruby\n# Define a custom exception (inherit from StandardError!)\nclass PaymentError < StandardError\n  attr_reader :order_id\n  def initialize(msg, order_id: nil)\n    super(msg)                 # set the message\n    @order_id = order_id        # extra context\n  end\nend\n\n# Raise it with a message and context:\ndef charge(order)\n  raise PaymentError.new('card declined', order_id: order.id) if order.failed?\nend\n\n# Rescue it specifically by class:\nbegin\n  charge(order)\nrescue PaymentError => e\n  logger.error(\"payment failed for #{e.order_id}: #{e.message}\")\n  # handler acts on the REAL cause, not a generic StandardError\nend\n\n# Keep it under StandardError (NOT Exception) so default rescue catches it.\n```"
          }
        ]
      },
      "followup_questions": [
        "How do you define and use custom exceptions in Ruby?",
        "When would you use Custom Exceptions in a real project?",
        "How would you compare Custom Exceptions with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Custom Exceptions — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Custom Exceptions? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "error-handling-basics-custom-exceptions-q003",
      "slug": "error-handling-basics-custom-exceptions-compare",
      "question": "How would you compare Custom Exceptions with an alternative?",
      "title": "compare Custom Exceptions with an alternative",
      "direct_answer": "I compare How do you define and use custom exceptions in Ruby with the simpler alternative by looking at correctness first, then time, memory, and readability. To make a custom exception, define a class that inherits from StandardError (or a more specific built-in): class PaymentError < StandardError; end. The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How do you define and use custom exceptions in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do you define and use custom exceptions in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. To make a custom exception, define a class that inherits from StandardError (or a more specific built-in): class PaymentError < StandardError; end. You raise it with raise PaymentError, 'card declined' (the message is in e.message) and rescue it by class: rescue PaymentError => e. Custom exceptions let callers rescue YOUR error specifically, not a generic StandardError, so the handler can act on the real cause. Keep them under StandardError (not Exception) so they're caught by default rescue and don't capture signals. You can add fields (attr_reader :order_id) by overriding initialize."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare How do you define and use custom exceptions in Ruby with the straightforward alternative on the same example. To make a custom exception, define a class that inherits from StandardError (or a more specific built-in): class PaymentError < StandardError; end. You raise it with raise PaymentError, 'card declined' (the message is in e.message) and rescue it by class: rescue PaymentError => e. Custom exceptions let callers rescue YOUR error specifically, not a generic StandardError, so the handler can act on the real cause. Keep them under StandardError (not Exception) so they're caught by default rescue and don't capture signals. You can add fields (attr_reader :order_id) by overriding initialize. If How do you define and use custom exceptions in Ruby removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "To make a custom exception, define a class that inherits from StandardError (or a more specific built-in): class PaymentError < StandardError; end.\n\nYou raise it with raise PaymentError, 'card declined' (the message is in e.message) and rescue it by class: rescue PaymentError => e.\n\nCustom exceptions let callers rescue YOUR error specifically, not a generic StandardError, so the handler can act on the real cause. Keep them under StandardError (not Exception) so they're caught by default rescue and don't capture signals. You can add fields (attr_reader :order_id) by overriding initialize.\n\nInheriting from Exception (not StandardError) — won't be caught by default rescue; or rescuing a generic StandardError instead of the specific class."
          },
          {
            "type": "code_example",
            "title": "Custom exceptions",
            "content": "```ruby\n# Define a custom exception (inherit from StandardError!)\nclass PaymentError < StandardError\n  attr_reader :order_id\n  def initialize(msg, order_id: nil)\n    super(msg)                 # set the message\n    @order_id = order_id        # extra context\n  end\nend\n\n# Raise it with a message and context:\ndef charge(order)\n  raise PaymentError.new('card declined', order_id: order.id) if order.failed?\nend\n\n# Rescue it specifically by class:\nbegin\n  charge(order)\nrescue PaymentError => e\n  logger.error(\"payment failed for #{e.order_id}: #{e.message}\")\n  # handler acts on the REAL cause, not a generic StandardError\nend\n\n# Keep it under StandardError (NOT Exception) so default rescue catches it.\n```"
          }
        ]
      },
      "followup_questions": [
        "How do you define and use custom exceptions in Ruby?",
        "When would you use Custom Exceptions in a real project?",
        "What is a common mistake when using Custom Exceptions?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Custom Exceptions with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Custom Exceptions with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "error-handling-basics-custom-exceptions-q004",
      "slug": "error-handling-basics-custom-exceptions-scenario",
      "question": "How would you debug a problem involving Custom Exceptions?",
      "title": "debug a problem involving Custom Exceptions",
      "direct_answer": "I would debug a problem involving How do you define and use custom exceptions in Ruby by reproducing it with the smallest input first. To make a custom exception, define a class that inherits from StandardError (or a more specific built-in): class PaymentError < StandardError; end. Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How do you define and use custom exceptions in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do you define and use custom exceptions in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what How do you define and use custom exceptions in Ruby is supposed to guarantee. To make a custom exception, define a class that inherits from StandardError (or a more specific built-in): class PaymentError < StandardError; end. You raise it with raise PaymentError, 'card declined' (the message is in e.message) and rescue it by class: rescue PaymentError => e. Custom exceptions let callers rescue YOUR error specifically, not a generic StandardError, so the handler can act on the real cause. Keep them under StandardError (not Exception) so they're caught by default rescue and don't capture signals. You can add fields (attr_reader :order_id) by overriding initialize."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the How do you define and use custom exceptions in Ruby bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: To make a custom exception, define a class that inherits from StandardError (or a more specific built-in): class PaymentError < StandardError; end. You raise it with raise PaymentError, 'card declined' (the message is in e.message) and rescue it by class: rescue PaymentError => e. Custom exceptions let callers rescue YOUR error specifically, not a generic StandardError, so the handler can act on the real cause. Keep them under StandardError (not Exception) so they're caught by default rescue and don't capture signals. You can add fields (attr_reader :order_id) by overriding initialize. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "To make a custom exception, define a class that inherits from StandardError (or a more specific built-in): class PaymentError < StandardError; end.\n\nYou raise it with raise PaymentError, 'card declined' (the message is in e.message) and rescue it by class: rescue PaymentError => e.\n\nCustom exceptions let callers rescue YOUR error specifically, not a generic StandardError, so the handler can act on the real cause. Keep them under StandardError (not Exception) so they're caught by default rescue and don't capture signals. You can add fields (attr_reader :order_id) by overriding initialize.\n\nInheriting from Exception (not StandardError) — won't be caught by default rescue; or rescuing a generic StandardError instead of the specific class."
          },
          {
            "type": "code_example",
            "title": "Custom exceptions",
            "content": "```ruby\n# Define a custom exception (inherit from StandardError!)\nclass PaymentError < StandardError\n  attr_reader :order_id\n  def initialize(msg, order_id: nil)\n    super(msg)                 # set the message\n    @order_id = order_id        # extra context\n  end\nend\n\n# Raise it with a message and context:\ndef charge(order)\n  raise PaymentError.new('card declined', order_id: order.id) if order.failed?\nend\n\n# Rescue it specifically by class:\nbegin\n  charge(order)\nrescue PaymentError => e\n  logger.error(\"payment failed for #{e.order_id}: #{e.message}\")\n  # handler acts on the REAL cause, not a generic StandardError\nend\n\n# Keep it under StandardError (NOT Exception) so default rescue catches it.\n```"
          }
        ]
      },
      "followup_questions": [
        "How do you define and use custom exceptions in Ruby?",
        "When would you use Custom Exceptions in a real project?",
        "How would you compare Custom Exceptions with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Custom Exceptions — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Custom Exceptions? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

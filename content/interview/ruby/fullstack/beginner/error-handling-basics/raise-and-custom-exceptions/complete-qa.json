{
  "topic": "Raise and Custom Exceptions",
  "topicSlug": "raise-and-custom-exceptions",
  "questions": [
    {
      "id": "raise-and-custom-exceptions-q001",
      "slug": "ruby-raise-and-custom-exceptions-basics",
      "question": "How do you raise exceptions in Ruby?",
      "title": "Raising Exceptions in Ruby",
      "direct_answer": "raise throws an exception: raise 'something broke' (raises a RuntimeError with that message), raise ArgumentError, 'expected a string' (raises a specific class), raise SomeError.new('msg', extra: x) (with a custom object). You raise when a precondition is violated (a nil arg where a value is required, an impossible state, a failed business rule) — not for normal control flow. The caller then rescues it. raise with no class raises RuntimeError; with a class, that class; with an instance, that instance. Pair raise with a specific class (or custom exception) so the caller can rescue the right thing.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know the forms of raise (message -> RuntimeError, class+message, instance) and when to raise (violated precondition, not flow).",
        "common_mistake": "Raising for normal control flow (use a return value), or raise with no class (RuntimeError) where a specific class fits.",
        "to_stand_out": "raise 'msg' (RuntimeError), raise Class, 'msg', raise Instance. Raise on a violated precondition, not for normal flow; pick a specific class."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define Raising Exceptions in Ruby simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "raise 'msg' raises a RuntimeError; raise Class, 'msg' raises a specific class.",
              "raise Instance raises a custom object (with extra fields).",
              "Raise on a violated precondition (nil arg, impossible state, failed business rule).",
              "Don't raise for normal control flow — use a return value (nil or a Result).",
              "In a rescue, bare raise re-raises the same exception (preserves the backtrace)."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "raise throws an exception. raise something broke raises a RuntimeError with that message; raise ArgumentError, expected a string raises a specific class with a message; raise SomeError dot new with msg and extra raises a custom object with extra data. You raise when a precondition is violated — a nil argument where a value is required, an impossible state, a failed business rule — not for normal control flow, because exceptions are for exceptional cases and the caller then rescues it. raise with no class raises RuntimeError; with a class, that class; with an instance, that instance. Pair raise with a specific class or a custom exception so the caller can rescue the right thing instead of a generic RuntimeError. A common mistake is raising for normal control flow, when you should use a return value, and raise with no class where a specific class fits."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "raise throws an exception: raise 'something broke' (raises a RuntimeError with that message), raise ArgumentError, 'expected a string' (raises a specific class), raise SomeError.new('msg', extra: x) (with a custom object).\n\nYou raise when a precondition is violated (a nil arg where a value is required, an impossible state, a failed business rule) — not for normal control flow.\n\nThe caller then rescues it. raise with no class raises RuntimeError; with a class, that class; with an instance, that instance. Pair raise with a specific class (or custom exception) so the caller can rescue the right thing.\n\nRaising for normal control flow (use a return value), or raise with no class (RuntimeError) where a specific class fits."
          },
          {
            "type": "flow_diagram",
            "title": "Forms of raise",
            "content": "message -> RuntimeError; class + message; instance.\n\n```mermaid\nflowchart LR\n  M[raise 'msg'] --> R[RuntimeError]\n  C[raise Class, 'msg'] --> S[that class]\n  I[raise Instance] --> O[that instance]\n```"
          },
          {
            "type": "code_example",
            "title": "Raising exceptions",
            "content": "```ruby\n# 1) raise with a message -> RuntimeError\nraise 'expected a positive number'\n\n# 2) raise a specific class + message\nraise ArgumentError, 'expected a string' unless name.is_a?(String)\nraise ArgumentError, 'count must be > 0' if count <= 0\n\n# 3) raise a custom instance (with extra fields)\nraise PaymentError.new('card declined', order_id: order.id)\n\n# 4) re-raise the current exception (in a rescue)\nbegin\n  work\nrescue => e\n  log(e)\n  raise          # re-raise the SAME exception (preserves backtrace)\nend\n\n# Use raise for violated preconditions, NOT normal flow:\ndef divide(a, b)\n  raise ArgumentError, 'divisor cannot be zero' if b == 0\n  a / b\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Raise And Custom Exceptions in a real project?",
        "How would you compare Raise And Custom Exceptions with an alternative?",
        "What is a common mistake when using Raise And Custom Exceptions?"
      ],
      "seo": {
        "metaTitle": "Raising Exceptions in Ruby — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How do you raise exceptions in Ruby? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "error-handling-basics-raise-and-custom-exceptions-q001",
      "slug": "error-handling-basics-raise-and-custom-exceptions-when-to-use",
      "question": "When would you use Raise And Custom Exceptions in a real project?",
      "title": "When would you use Raise And Custom Exceptions in a real project",
      "direct_answer": "I would use How do you raise exceptions in Ruby when the problem matches its main benefit. raise throws an exception: raise 'something broke' (raises a RuntimeError with that message), raise ArgumentError, 'expected a string' (raises a specific class), raise SomeError.new('msg', extra: x) (with a custom object). In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How do you raise exceptions in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do you raise exceptions in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining How do you raise exceptions in Ruby in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. raise throws an exception: raise 'something broke' (raises a RuntimeError with that message), raise ArgumentError, 'expected a string' (raises a specific class), raise SomeError.new('msg', extra: x) (with a custom object). You raise when a precondition is violated (a nil arg where a value is required, an impossible state, a failed business rule) — not for normal control flow. The caller then rescues it. raise with no class raises RuntimeError; with a class, that class; with an instance, that instance. Pair raise with a specific class (or custom exception) so the caller can rescue the right thing."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use How do you raise exceptions in Ruby when its rule makes the code safer, clearer, or more efficient for the actual problem. raise throws an exception: raise 'something broke' (raises a RuntimeError with that message), raise ArgumentError, 'expected a string' (raises a specific class), raise SomeError.new('msg', extra: x) (with a custom object). You raise when a precondition is violated (a nil arg where a value is required, an impossible state, a failed business rule) — not for normal control flow. The caller then rescues it. raise with no class raises RuntimeError; with a class, that class; with an instance, that instance. Pair raise with a specific class (or custom exception) so the caller can rescue the right thing. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "raise throws an exception: raise 'something broke' (raises a RuntimeError with that message), raise ArgumentError, 'expected a string' (raises a specific class), raise SomeError.new('msg', extra: x) (with a custom object).\n\nYou raise when a precondition is violated (a nil arg where a value is required, an impossible state, a failed business rule) — not for normal control flow.\n\nThe caller then rescues it. raise with no class raises RuntimeError; with a class, that class; with an instance, that instance. Pair raise with a specific class (or custom exception) so the caller can rescue the right thing.\n\nRaising for normal control flow (use a return value), or raise with no class (RuntimeError) where a specific class fits."
          },
          {
            "type": "code_example",
            "title": "Raising exceptions",
            "content": "```ruby\n# 1) raise with a message -> RuntimeError\nraise 'expected a positive number'\n\n# 2) raise a specific class + message\nraise ArgumentError, 'expected a string' unless name.is_a?(String)\nraise ArgumentError, 'count must be > 0' if count <= 0\n\n# 3) raise a custom instance (with extra fields)\nraise PaymentError.new('card declined', order_id: order.id)\n\n# 4) re-raise the current exception (in a rescue)\nbegin\n  work\nrescue => e\n  log(e)\n  raise          # re-raise the SAME exception (preserves backtrace)\nend\n\n# Use raise for violated preconditions, NOT normal flow:\ndef divide(a, b)\n  raise ArgumentError, 'divisor cannot be zero' if b == 0\n  a / b\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "How do you raise exceptions in Ruby?",
        "How would you compare Raise And Custom Exceptions with an alternative?",
        "What is a common mistake when using Raise And Custom Exceptions?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Raise And Custom Exceptions in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Raise And Custom Exceptions in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "error-handling-basics-raise-and-custom-exceptions-q002",
      "slug": "error-handling-basics-raise-and-custom-exceptions-common-mistake",
      "question": "What is a common mistake when using Raise And Custom Exceptions?",
      "title": "a common mistake when using Raise And Custom Exceptions",
      "direct_answer": "A common mistake with How do you raise exceptions in Ruby is using the rule without checking its boundary conditions. raise throws an exception: raise 'something broke' (raises a RuntimeError with that message), raise ArgumentError, 'expected a string' (raises a specific class), raise SomeError.new('msg', extra: x) (with a custom object). That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How do you raise exceptions in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do you raise exceptions in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of How do you raise exceptions in Ruby. raise throws an exception: raise 'something broke' (raises a RuntimeError with that message), raise ArgumentError, 'expected a string' (raises a specific class), raise SomeError.new('msg', extra: x) (with a custom object). You raise when a precondition is violated (a nil arg where a value is required, an impossible state, a failed business rule) — not for normal control flow. The caller then rescues it. raise with no class raises RuntimeError; with a class, that class; with an instance, that instance. Pair raise with a specific class (or custom exception) so the caller can rescue the right thing."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with How do you raise exceptions in Ruby is assuming the happy path is the whole rule. raise throws an exception: raise 'something broke' (raises a RuntimeError with that message), raise ArgumentError, 'expected a string' (raises a specific class), raise SomeError.new('msg', extra: x) (with a custom object). You raise when a precondition is violated (a nil arg where a value is required, an impossible state, a failed business rule) — not for normal control flow. The caller then rescues it. raise with no class raises RuntimeError; with a class, that class; with an instance, that instance. Pair raise with a specific class (or custom exception) so the caller can rescue the right thing. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "raise throws an exception: raise 'something broke' (raises a RuntimeError with that message), raise ArgumentError, 'expected a string' (raises a specific class), raise SomeError.new('msg', extra: x) (with a custom object).\n\nYou raise when a precondition is violated (a nil arg where a value is required, an impossible state, a failed business rule) — not for normal control flow.\n\nThe caller then rescues it. raise with no class raises RuntimeError; with a class, that class; with an instance, that instance. Pair raise with a specific class (or custom exception) so the caller can rescue the right thing.\n\nRaising for normal control flow (use a return value), or raise with no class (RuntimeError) where a specific class fits."
          },
          {
            "type": "code_example",
            "title": "Raising exceptions",
            "content": "```ruby\n# 1) raise with a message -> RuntimeError\nraise 'expected a positive number'\n\n# 2) raise a specific class + message\nraise ArgumentError, 'expected a string' unless name.is_a?(String)\nraise ArgumentError, 'count must be > 0' if count <= 0\n\n# 3) raise a custom instance (with extra fields)\nraise PaymentError.new('card declined', order_id: order.id)\n\n# 4) re-raise the current exception (in a rescue)\nbegin\n  work\nrescue => e\n  log(e)\n  raise          # re-raise the SAME exception (preserves backtrace)\nend\n\n# Use raise for violated preconditions, NOT normal flow:\ndef divide(a, b)\n  raise ArgumentError, 'divisor cannot be zero' if b == 0\n  a / b\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "How do you raise exceptions in Ruby?",
        "When would you use Raise And Custom Exceptions in a real project?",
        "How would you compare Raise And Custom Exceptions with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Raise And Custom Exceptions — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Raise And Custom Exceptions? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "error-handling-basics-raise-and-custom-exceptions-q003",
      "slug": "error-handling-basics-raise-and-custom-exceptions-compare",
      "question": "How would you compare Raise And Custom Exceptions with an alternative?",
      "title": "compare Raise And Custom Exceptions with an alternative",
      "direct_answer": "I compare How do you raise exceptions in Ruby with the simpler alternative by looking at correctness first, then time, memory, and readability. raise throws an exception: raise 'something broke' (raises a RuntimeError with that message), raise ArgumentError, 'expected a string' (raises a specific class), raise SomeError.new('msg', extra: x) (with a custom object). The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How do you raise exceptions in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do you raise exceptions in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. raise throws an exception: raise 'something broke' (raises a RuntimeError with that message), raise ArgumentError, 'expected a string' (raises a specific class), raise SomeError.new('msg', extra: x) (with a custom object). You raise when a precondition is violated (a nil arg where a value is required, an impossible state, a failed business rule) — not for normal control flow. The caller then rescues it. raise with no class raises RuntimeError; with a class, that class; with an instance, that instance. Pair raise with a specific class (or custom exception) so the caller can rescue the right thing."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare How do you raise exceptions in Ruby with the straightforward alternative on the same example. raise throws an exception: raise 'something broke' (raises a RuntimeError with that message), raise ArgumentError, 'expected a string' (raises a specific class), raise SomeError.new('msg', extra: x) (with a custom object). You raise when a precondition is violated (a nil arg where a value is required, an impossible state, a failed business rule) — not for normal control flow. The caller then rescues it. raise with no class raises RuntimeError; with a class, that class; with an instance, that instance. Pair raise with a specific class (or custom exception) so the caller can rescue the right thing. If How do you raise exceptions in Ruby removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "raise throws an exception: raise 'something broke' (raises a RuntimeError with that message), raise ArgumentError, 'expected a string' (raises a specific class), raise SomeError.new('msg', extra: x) (with a custom object).\n\nYou raise when a precondition is violated (a nil arg where a value is required, an impossible state, a failed business rule) — not for normal control flow.\n\nThe caller then rescues it. raise with no class raises RuntimeError; with a class, that class; with an instance, that instance. Pair raise with a specific class (or custom exception) so the caller can rescue the right thing.\n\nRaising for normal control flow (use a return value), or raise with no class (RuntimeError) where a specific class fits."
          },
          {
            "type": "code_example",
            "title": "Raising exceptions",
            "content": "```ruby\n# 1) raise with a message -> RuntimeError\nraise 'expected a positive number'\n\n# 2) raise a specific class + message\nraise ArgumentError, 'expected a string' unless name.is_a?(String)\nraise ArgumentError, 'count must be > 0' if count <= 0\n\n# 3) raise a custom instance (with extra fields)\nraise PaymentError.new('card declined', order_id: order.id)\n\n# 4) re-raise the current exception (in a rescue)\nbegin\n  work\nrescue => e\n  log(e)\n  raise          # re-raise the SAME exception (preserves backtrace)\nend\n\n# Use raise for violated preconditions, NOT normal flow:\ndef divide(a, b)\n  raise ArgumentError, 'divisor cannot be zero' if b == 0\n  a / b\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "How do you raise exceptions in Ruby?",
        "When would you use Raise And Custom Exceptions in a real project?",
        "What is a common mistake when using Raise And Custom Exceptions?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Raise And Custom Exceptions with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Raise And Custom Exceptions with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "error-handling-basics-raise-and-custom-exceptions-q004",
      "slug": "error-handling-basics-raise-and-custom-exceptions-scenario",
      "question": "How would you debug a problem involving Raise And Custom Exceptions?",
      "title": "debug a problem involving Raise And Custom Exceptions",
      "direct_answer": "I would debug a problem involving How do you raise exceptions in Ruby by reproducing it with the smallest input first. raise throws an exception: raise 'something broke' (raises a RuntimeError with that message), raise ArgumentError, 'expected a string' (raises a specific class), raise SomeError.new('msg', extra: x) (with a custom object). Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How do you raise exceptions in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do you raise exceptions in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what How do you raise exceptions in Ruby is supposed to guarantee. raise throws an exception: raise 'something broke' (raises a RuntimeError with that message), raise ArgumentError, 'expected a string' (raises a specific class), raise SomeError.new('msg', extra: x) (with a custom object). You raise when a precondition is violated (a nil arg where a value is required, an impossible state, a failed business rule) — not for normal control flow. The caller then rescues it. raise with no class raises RuntimeError; with a class, that class; with an instance, that instance. Pair raise with a specific class (or custom exception) so the caller can rescue the right thing."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the How do you raise exceptions in Ruby bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: raise throws an exception: raise 'something broke' (raises a RuntimeError with that message), raise ArgumentError, 'expected a string' (raises a specific class), raise SomeError.new('msg', extra: x) (with a custom object). You raise when a precondition is violated (a nil arg where a value is required, an impossible state, a failed business rule) — not for normal control flow. The caller then rescues it. raise with no class raises RuntimeError; with a class, that class; with an instance, that instance. Pair raise with a specific class (or custom exception) so the caller can rescue the right thing. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "raise throws an exception: raise 'something broke' (raises a RuntimeError with that message), raise ArgumentError, 'expected a string' (raises a specific class), raise SomeError.new('msg', extra: x) (with a custom object).\n\nYou raise when a precondition is violated (a nil arg where a value is required, an impossible state, a failed business rule) — not for normal control flow.\n\nThe caller then rescues it. raise with no class raises RuntimeError; with a class, that class; with an instance, that instance. Pair raise with a specific class (or custom exception) so the caller can rescue the right thing.\n\nRaising for normal control flow (use a return value), or raise with no class (RuntimeError) where a specific class fits."
          },
          {
            "type": "code_example",
            "title": "Raising exceptions",
            "content": "```ruby\n# 1) raise with a message -> RuntimeError\nraise 'expected a positive number'\n\n# 2) raise a specific class + message\nraise ArgumentError, 'expected a string' unless name.is_a?(String)\nraise ArgumentError, 'count must be > 0' if count <= 0\n\n# 3) raise a custom instance (with extra fields)\nraise PaymentError.new('card declined', order_id: order.id)\n\n# 4) re-raise the current exception (in a rescue)\nbegin\n  work\nrescue => e\n  log(e)\n  raise          # re-raise the SAME exception (preserves backtrace)\nend\n\n# Use raise for violated preconditions, NOT normal flow:\ndef divide(a, b)\n  raise ArgumentError, 'divisor cannot be zero' if b == 0\n  a / b\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "How do you raise exceptions in Ruby?",
        "When would you use Raise And Custom Exceptions in a real project?",
        "How would you compare Raise And Custom Exceptions with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Raise And Custom Exceptions — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Raise And Custom Exceptions? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

{
  "topic": "Strong Params",
  "topicSlug": "strong-params",
  "questions": [
    {
      "id": "strong-params-q001",
      "slug": "ruby-strong-params-basics",
      "question": "What are strong parameters in Rails?",
      "title": "Strong Parameters",
      "direct_answer": "Strong params (Rails 4+) prevent mass-assignment vulnerabilities by requiring you to explicitly permit which fields can be set from user input. `params.require(:user).permit(:name, :email)` allows only name and email through — any other key (like :admin) is filtered out. Without it, `User.create(params[:user])` would let a user set any column, including admin (mass-assignment attack). `require` raises if the key is missing; `permit` returns an ActionController::Parameters that's been 'permitted' (marked safe for mass-assignment). You put it in the controller: `def user_params; params.require(:user).permit(:name, :email, :age); end` then `User.create(user_params)`. Nested: `permit(:name, address: [:city, :zip])`. It enforces a whitelist at the boundary — only the fields you list can reach your models.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know strong params require+permit to whitelist which fields mass-assignment allows, preventing setting :admin.",
        "common_mistake": "Passing raw params to create/update (mass-assignment vulnerability — use permit).",
        "to_stand_out": "Strong params: params.require(:user).permit(:name, :email). Whitelist mass-assignment fields; blocks :admin etc. require raises if missing."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define Strong Parameters simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "Strong params require you to explicitly permit which fields mass-assignment allows.",
              "params.require(:user).permit(:name, :email) — only name and email pass; :admin is dropped.",
              "require raises ActionController::ParameterMissing if the key is missing.",
              "permit returns a 'permitted' Parameters object (safe for mass-assignment).",
              "Without it, User.create(params[:user]) lets a user set any column (mass-assignment attack)."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "Strong params (Rails 4 plus) prevent mass-assignment vulnerabilities by requiring you to explicitly permit which fields can be set from user input. params require user permit name email allows only name and email through — any other key (like admin) is filtered out. Without it, User create params user would let a user set any column, including admin (mass-assignment attack). require raises if the key is missing; permit returns an ActionController Parameters that's been permitted (marked safe for mass-assignment). You put it in the controller: def user params; params require user permit name email age; end then User create user params. Nested: permit name address city zip. It enforces a whitelist at the boundary — only the fields you list can reach your models. A common mistake is passing raw params to create or update, a mass-assignment vulnerability, and forgetting require, which raises if the key is missing."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Strong params (Rails 4+) prevent mass-assignment vulnerabilities by requiring you to explicitly permit which fields can be set from user input.\n\n`params.require(:user).permit(:name, :email)` allows only name and email through — any other key (like :admin) is filtered out.\n\nWithout it, `User.create(params[:user])` would let a user set any column, including admin (mass-assignment attack). `require` raises if the key is missing; `permit` returns an ActionController::Parameters that's been 'permitted' (marked safe for mass-assignment). You put it in the controller: `def user_params; params.require(:user).permit(:name, :email, :age); end` then `User.create(user_params)`. Nested: `permit(:name, address: [:city, :zip])`. It enforces a whitelist at the boundary — only the fields you list can reach your models.\n\nPassing raw params to create/update (mass-assignment vulnerability — use permit)."
          },
          {
            "type": "flow_diagram",
            "title": "Strong Params",
            "content": "params.require(:user).permit(:name, :email). Whitelist; blocks :admin. require raises if missing.\n\n```mermaid\nflowchart LR\n  R[params.require :user] --> M[raises if :user missing]\n  P[.permit name email] --> W[whitelist]\n  W --> F[filters out :admin]\n  O[other keys] --> D[dropped]\n```"
          },
          {
            "type": "code_example",
            "title": "Strong params",
            "content": "```ruby\nclass UsersController < ApplicationController\n  def create\n    # BAD: User.create(params[:user])  -- mass-assignment vulnerability\n    #   a user could send user[admin]=1 and become admin!\n    @user = User.create(user_params)   # GOOD: only permitted fields\n    redirect_to @user\n  end\n\n  # The whitelist (the boundary):\n  def user_params\n    # require(:user) raises if :user is missing\n    # permit(...) returns only those keys; others are DROPPED\n    params.require(:user).permit(:name, :email, :age)\n  end\nend\n\n# What gets through:\n#   { user: { name:, email:, age:, admin: 1 } }\n#   -> user_params returns { name:, email:, age: }  (admin DROPPED)\n\n# require raises if the top key is missing:\n#   params.require(:user)  ->  ActionController::ParameterMissing if no :user\n\n# Nested attributes:\nparams.require(:user).permit(:name, address: [:city, :zip])\n#   -> { name:, address: { city:, zip: } }\n\n# Permit all of a sub-hash (careful!):\nparams.require(:user).permit(:name, tags: [])\n\n# permit returns a 'permitted' ActionController::Parameters (safe for mass-assignment).\n# params (unpermitted) raises ActiveModel::ForbiddenAttributesError if passed to create.\n\n# It enforces a WHITELIST at the boundary — only listed fields reach models.\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Strong Params in a real project?",
        "How would you compare Strong Params with an alternative?",
        "What is a common mistake when using Strong Params?"
      ],
      "seo": {
        "metaTitle": "Strong Parameters — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What are strong parameters in Rails? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "ruby-security-basics-strong-params-q001",
      "slug": "ruby-security-basics-strong-params-when-to-use",
      "question": "When would you use Strong Params in a real project?",
      "title": "When would you use Strong Params in a real project",
      "direct_answer": "I would use strong parameters in Rails when the problem matches its main benefit. Strong params (Rails 4+) prevent mass-assignment vulnerabilities by requiring you to explicitly permit which fields can be set from user input. In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain strong parameters in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about strong parameters in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining strong parameters in Rails in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. Strong params (Rails 4+) prevent mass-assignment vulnerabilities by requiring you to explicitly permit which fields can be set from user input. `params.require(:user).permit(:name, :email)` allows only name and email through — any other key (like :admin) is filtered out. Without it, `User.create(params[:user])` would let a user set any column, including admin (mass-assignment attack). `require` raises if the key is missing; `permit` returns an ActionController::Parameters that's been 'permitted' (marked safe for mass-assignment). You put it in the controller: `def user_params; params.require(:user).permit(:name, :email, :age); end` then `User.create(user_params)`. Nested: `permit(:name, address: [:city, :zip])`. It enforces a whitelist at the boundary — only the fields you list can reach your models."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use strong parameters in Rails when its rule makes the code safer, clearer, or more efficient for the actual problem. Strong params (Rails 4+) prevent mass-assignment vulnerabilities by requiring you to explicitly permit which fields can be set from user input. `params.require(:user).permit(:name, :email)` allows only name and email through — any other key (like :admin) is filtered out. Without it, `User.create(params[:user])` would let a user set any column, including admin (mass-assignment attack). `require` raises if the key is missing; `permit` returns an ActionController::Parameters that's been 'permitted' (marked safe for mass-assignment). You put it in the controller: `def user_params; params.require(:user).permit(:name, :email, :age); end` then `User.create(user_params)`. Nested: `permit(:name, address: [:city, :zip])`. It enforces a whitelist at the boundary — only the fields you list can reach your models. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Strong params (Rails 4+) prevent mass-assignment vulnerabilities by requiring you to explicitly permit which fields can be set from user input.\n\n`params.require(:user).permit(:name, :email)` allows only name and email through — any other key (like :admin) is filtered out.\n\nWithout it, `User.create(params[:user])` would let a user set any column, including admin (mass-assignment attack). `require` raises if the key is missing; `permit` returns an ActionController::Parameters that's been 'permitted' (marked safe for mass-assignment). You put it in the controller: `def user_params; params.require(:user).permit(:name, :email, :age); end` then `User.create(user_params)`. Nested: `permit(:name, address: [:city, :zip])`. It enforces a whitelist at the boundary — only the fields you list can reach your models.\n\nPassing raw params to create/update (mass-assignment vulnerability — use permit)."
          },
          {
            "type": "code_example",
            "title": "Strong params",
            "content": "```ruby\nclass UsersController < ApplicationController\n  def create\n    # BAD: User.create(params[:user])  -- mass-assignment vulnerability\n    #   a user could send user[admin]=1 and become admin!\n    @user = User.create(user_params)   # GOOD: only permitted fields\n    redirect_to @user\n  end\n\n  # The whitelist (the boundary):\n  def user_params\n    # require(:user) raises if :user is missing\n    # permit(...) returns only those keys; others are DROPPED\n    params.require(:user).permit(:name, :email, :age)\n  end\nend\n\n# What gets through:\n#   { user: { name:, email:, age:, admin: 1 } }\n#   -> user_params returns { name:, email:, age: }  (admin DROPPED)\n\n# require raises if the top key is missing:\n#   params.require(:user)  ->  ActionController::ParameterMissing if no :user\n\n# Nested attributes:\nparams.require(:user).permit(:name, address: [:city, :zip])\n#   -> { name:, address: { city:, zip: } }\n\n# Permit all of a sub-hash (careful!):\nparams.require(:user).permit(:name, tags: [])\n\n# permit returns a 'permitted' ActionController::Parameters (safe for mass-assignment).\n# params (unpermitted) raises ActiveModel::ForbiddenAttributesError if passed to create.\n\n# It enforces a WHITELIST at the boundary — only listed fields reach models.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are strong parameters in Rails?",
        "How would you compare Strong Params with an alternative?",
        "What is a common mistake when using Strong Params?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Strong Params in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Strong Params in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-security-basics-strong-params-q002",
      "slug": "ruby-security-basics-strong-params-common-mistake",
      "question": "What is a common mistake when using Strong Params?",
      "title": "a common mistake when using Strong Params",
      "direct_answer": "A common mistake with strong parameters in Rails is using the rule without checking its boundary conditions. Strong params (Rails 4+) prevent mass-assignment vulnerabilities by requiring you to explicitly permit which fields can be set from user input. That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain strong parameters in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about strong parameters in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of strong parameters in Rails. Strong params (Rails 4+) prevent mass-assignment vulnerabilities by requiring you to explicitly permit which fields can be set from user input. `params.require(:user).permit(:name, :email)` allows only name and email through — any other key (like :admin) is filtered out. Without it, `User.create(params[:user])` would let a user set any column, including admin (mass-assignment attack). `require` raises if the key is missing; `permit` returns an ActionController::Parameters that's been 'permitted' (marked safe for mass-assignment). You put it in the controller: `def user_params; params.require(:user).permit(:name, :email, :age); end` then `User.create(user_params)`. Nested: `permit(:name, address: [:city, :zip])`. It enforces a whitelist at the boundary — only the fields you list can reach your models."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with strong parameters in Rails is assuming the happy path is the whole rule. Strong params (Rails 4+) prevent mass-assignment vulnerabilities by requiring you to explicitly permit which fields can be set from user input. `params.require(:user).permit(:name, :email)` allows only name and email through — any other key (like :admin) is filtered out. Without it, `User.create(params[:user])` would let a user set any column, including admin (mass-assignment attack). `require` raises if the key is missing; `permit` returns an ActionController::Parameters that's been 'permitted' (marked safe for mass-assignment). You put it in the controller: `def user_params; params.require(:user).permit(:name, :email, :age); end` then `User.create(user_params)`. Nested: `permit(:name, address: [:city, :zip])`. It enforces a whitelist at the boundary — only the fields you list can reach your models. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "Strong params (Rails 4+) prevent mass-assignment vulnerabilities by requiring you to explicitly permit which fields can be set from user input.\n\n`params.require(:user).permit(:name, :email)` allows only name and email through — any other key (like :admin) is filtered out.\n\nWithout it, `User.create(params[:user])` would let a user set any column, including admin (mass-assignment attack). `require` raises if the key is missing; `permit` returns an ActionController::Parameters that's been 'permitted' (marked safe for mass-assignment). You put it in the controller: `def user_params; params.require(:user).permit(:name, :email, :age); end` then `User.create(user_params)`. Nested: `permit(:name, address: [:city, :zip])`. It enforces a whitelist at the boundary — only the fields you list can reach your models.\n\nPassing raw params to create/update (mass-assignment vulnerability — use permit)."
          },
          {
            "type": "code_example",
            "title": "Strong params",
            "content": "```ruby\nclass UsersController < ApplicationController\n  def create\n    # BAD: User.create(params[:user])  -- mass-assignment vulnerability\n    #   a user could send user[admin]=1 and become admin!\n    @user = User.create(user_params)   # GOOD: only permitted fields\n    redirect_to @user\n  end\n\n  # The whitelist (the boundary):\n  def user_params\n    # require(:user) raises if :user is missing\n    # permit(...) returns only those keys; others are DROPPED\n    params.require(:user).permit(:name, :email, :age)\n  end\nend\n\n# What gets through:\n#   { user: { name:, email:, age:, admin: 1 } }\n#   -> user_params returns { name:, email:, age: }  (admin DROPPED)\n\n# require raises if the top key is missing:\n#   params.require(:user)  ->  ActionController::ParameterMissing if no :user\n\n# Nested attributes:\nparams.require(:user).permit(:name, address: [:city, :zip])\n#   -> { name:, address: { city:, zip: } }\n\n# Permit all of a sub-hash (careful!):\nparams.require(:user).permit(:name, tags: [])\n\n# permit returns a 'permitted' ActionController::Parameters (safe for mass-assignment).\n# params (unpermitted) raises ActiveModel::ForbiddenAttributesError if passed to create.\n\n# It enforces a WHITELIST at the boundary — only listed fields reach models.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are strong parameters in Rails?",
        "When would you use Strong Params in a real project?",
        "How would you compare Strong Params with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Strong Params — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Strong Params? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-security-basics-strong-params-q003",
      "slug": "ruby-security-basics-strong-params-compare",
      "question": "How would you compare Strong Params with an alternative?",
      "title": "compare Strong Params with an alternative",
      "direct_answer": "I compare strong parameters in Rails with the simpler alternative by looking at correctness first, then time, memory, and readability. Strong params (Rails 4+) prevent mass-assignment vulnerabilities by requiring you to explicitly permit which fields can be set from user input. The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain strong parameters in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about strong parameters in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. Strong params (Rails 4+) prevent mass-assignment vulnerabilities by requiring you to explicitly permit which fields can be set from user input. `params.require(:user).permit(:name, :email)` allows only name and email through — any other key (like :admin) is filtered out. Without it, `User.create(params[:user])` would let a user set any column, including admin (mass-assignment attack). `require` raises if the key is missing; `permit` returns an ActionController::Parameters that's been 'permitted' (marked safe for mass-assignment). You put it in the controller: `def user_params; params.require(:user).permit(:name, :email, :age); end` then `User.create(user_params)`. Nested: `permit(:name, address: [:city, :zip])`. It enforces a whitelist at the boundary — only the fields you list can reach your models."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare strong parameters in Rails with the straightforward alternative on the same example. Strong params (Rails 4+) prevent mass-assignment vulnerabilities by requiring you to explicitly permit which fields can be set from user input. `params.require(:user).permit(:name, :email)` allows only name and email through — any other key (like :admin) is filtered out. Without it, `User.create(params[:user])` would let a user set any column, including admin (mass-assignment attack). `require` raises if the key is missing; `permit` returns an ActionController::Parameters that's been 'permitted' (marked safe for mass-assignment). You put it in the controller: `def user_params; params.require(:user).permit(:name, :email, :age); end` then `User.create(user_params)`. Nested: `permit(:name, address: [:city, :zip])`. It enforces a whitelist at the boundary — only the fields you list can reach your models. If strong parameters in Rails removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "Strong params (Rails 4+) prevent mass-assignment vulnerabilities by requiring you to explicitly permit which fields can be set from user input.\n\n`params.require(:user).permit(:name, :email)` allows only name and email through — any other key (like :admin) is filtered out.\n\nWithout it, `User.create(params[:user])` would let a user set any column, including admin (mass-assignment attack). `require` raises if the key is missing; `permit` returns an ActionController::Parameters that's been 'permitted' (marked safe for mass-assignment). You put it in the controller: `def user_params; params.require(:user).permit(:name, :email, :age); end` then `User.create(user_params)`. Nested: `permit(:name, address: [:city, :zip])`. It enforces a whitelist at the boundary — only the fields you list can reach your models.\n\nPassing raw params to create/update (mass-assignment vulnerability — use permit)."
          },
          {
            "type": "code_example",
            "title": "Strong params",
            "content": "```ruby\nclass UsersController < ApplicationController\n  def create\n    # BAD: User.create(params[:user])  -- mass-assignment vulnerability\n    #   a user could send user[admin]=1 and become admin!\n    @user = User.create(user_params)   # GOOD: only permitted fields\n    redirect_to @user\n  end\n\n  # The whitelist (the boundary):\n  def user_params\n    # require(:user) raises if :user is missing\n    # permit(...) returns only those keys; others are DROPPED\n    params.require(:user).permit(:name, :email, :age)\n  end\nend\n\n# What gets through:\n#   { user: { name:, email:, age:, admin: 1 } }\n#   -> user_params returns { name:, email:, age: }  (admin DROPPED)\n\n# require raises if the top key is missing:\n#   params.require(:user)  ->  ActionController::ParameterMissing if no :user\n\n# Nested attributes:\nparams.require(:user).permit(:name, address: [:city, :zip])\n#   -> { name:, address: { city:, zip: } }\n\n# Permit all of a sub-hash (careful!):\nparams.require(:user).permit(:name, tags: [])\n\n# permit returns a 'permitted' ActionController::Parameters (safe for mass-assignment).\n# params (unpermitted) raises ActiveModel::ForbiddenAttributesError if passed to create.\n\n# It enforces a WHITELIST at the boundary — only listed fields reach models.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are strong parameters in Rails?",
        "When would you use Strong Params in a real project?",
        "What is a common mistake when using Strong Params?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Strong Params with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Strong Params with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-security-basics-strong-params-q004",
      "slug": "ruby-security-basics-strong-params-scenario",
      "question": "How would you debug a problem involving Strong Params?",
      "title": "debug a problem involving Strong Params",
      "direct_answer": "I would debug a problem involving strong parameters in Rails by reproducing it with the smallest input first. Strong params (Rails 4+) prevent mass-assignment vulnerabilities by requiring you to explicitly permit which fields can be set from user input. Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain strong parameters in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about strong parameters in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what strong parameters in Rails is supposed to guarantee. Strong params (Rails 4+) prevent mass-assignment vulnerabilities by requiring you to explicitly permit which fields can be set from user input. `params.require(:user).permit(:name, :email)` allows only name and email through — any other key (like :admin) is filtered out. Without it, `User.create(params[:user])` would let a user set any column, including admin (mass-assignment attack). `require` raises if the key is missing; `permit` returns an ActionController::Parameters that's been 'permitted' (marked safe for mass-assignment). You put it in the controller: `def user_params; params.require(:user).permit(:name, :email, :age); end` then `User.create(user_params)`. Nested: `permit(:name, address: [:city, :zip])`. It enforces a whitelist at the boundary — only the fields you list can reach your models."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the strong parameters in Rails bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: Strong params (Rails 4+) prevent mass-assignment vulnerabilities by requiring you to explicitly permit which fields can be set from user input. `params.require(:user).permit(:name, :email)` allows only name and email through — any other key (like :admin) is filtered out. Without it, `User.create(params[:user])` would let a user set any column, including admin (mass-assignment attack). `require` raises if the key is missing; `permit` returns an ActionController::Parameters that's been 'permitted' (marked safe for mass-assignment). You put it in the controller: `def user_params; params.require(:user).permit(:name, :email, :age); end` then `User.create(user_params)`. Nested: `permit(:name, address: [:city, :zip])`. It enforces a whitelist at the boundary — only the fields you list can reach your models. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "Strong params (Rails 4+) prevent mass-assignment vulnerabilities by requiring you to explicitly permit which fields can be set from user input.\n\n`params.require(:user).permit(:name, :email)` allows only name and email through — any other key (like :admin) is filtered out.\n\nWithout it, `User.create(params[:user])` would let a user set any column, including admin (mass-assignment attack). `require` raises if the key is missing; `permit` returns an ActionController::Parameters that's been 'permitted' (marked safe for mass-assignment). You put it in the controller: `def user_params; params.require(:user).permit(:name, :email, :age); end` then `User.create(user_params)`. Nested: `permit(:name, address: [:city, :zip])`. It enforces a whitelist at the boundary — only the fields you list can reach your models.\n\nPassing raw params to create/update (mass-assignment vulnerability — use permit)."
          },
          {
            "type": "code_example",
            "title": "Strong params",
            "content": "```ruby\nclass UsersController < ApplicationController\n  def create\n    # BAD: User.create(params[:user])  -- mass-assignment vulnerability\n    #   a user could send user[admin]=1 and become admin!\n    @user = User.create(user_params)   # GOOD: only permitted fields\n    redirect_to @user\n  end\n\n  # The whitelist (the boundary):\n  def user_params\n    # require(:user) raises if :user is missing\n    # permit(...) returns only those keys; others are DROPPED\n    params.require(:user).permit(:name, :email, :age)\n  end\nend\n\n# What gets through:\n#   { user: { name:, email:, age:, admin: 1 } }\n#   -> user_params returns { name:, email:, age: }  (admin DROPPED)\n\n# require raises if the top key is missing:\n#   params.require(:user)  ->  ActionController::ParameterMissing if no :user\n\n# Nested attributes:\nparams.require(:user).permit(:name, address: [:city, :zip])\n#   -> { name:, address: { city:, zip: } }\n\n# Permit all of a sub-hash (careful!):\nparams.require(:user).permit(:name, tags: [])\n\n# permit returns a 'permitted' ActionController::Parameters (safe for mass-assignment).\n# params (unpermitted) raises ActiveModel::ForbiddenAttributesError if passed to create.\n\n# It enforces a WHITELIST at the boundary — only listed fields reach models.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are strong parameters in Rails?",
        "When would you use Strong Params in a real project?",
        "How would you compare Strong Params with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Strong Params — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Strong Params? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

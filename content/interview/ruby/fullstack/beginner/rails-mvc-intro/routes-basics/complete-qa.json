{
  "topic": "Routes Basics",
  "topicSlug": "routes-basics",
  "questions": [
    {
      "id": "routes-basics-q001",
      "slug": "rails-routes-basics",
      "question": "What are routes in Rails and how do you write them?",
      "title": "Rails Routes Basics",
      "direct_answer": "Routes map URLs to controller actions in config/routes.rb — they're the app's entry point. The forms: get '/about', to: 'pages#about' (a single route); resources :posts (the 7 REST routes); root to: 'posts#index' (the homepage). resources :posts generates GET /posts (index), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy), plus new/edit, and path helpers (posts_path, post_path(id)). Nested: resources :posts do resources :comments end gives /posts/:post_id/comments. Check your routes with rails routes (lists every route + helper). Routes are how a URL becomes a controller action.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know routes live in config/routes.rb, the forms (root, get, resources), and that resources :posts gives the 7 routes + helpers.",
        "common_mistake": "Hand-writing the 7 routes when resources :posts generates them, or forgetting root (no homepage).",
        "to_stand_out": "Routes map URLs to actions in config/routes.rb: root, get 'x', to: 'y#z', resources :posts (7 routes + helpers). Check with rails routes."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define Rails Routes Basics simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "Routes map URLs to actions in config/routes.rb — the app's entry point.",
              "Forms: root to:, get '/x', to: 'y#z', resources :posts, nested resources, namespace.",
              "resources :posts generates the 7 REST routes + path helpers (posts_path, post_path).",
              "Nested resources give child routes (/posts/:post_id/comments).",
              "Check every route + helper with `rails routes`."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "Routes map URLs to controller actions in config slash routes.rb — they're the app's entry point. The forms: get slash about, to pages hash about (a single route); resources posts (the 7 REST routes); root to posts index (the homepage). resources posts generates GET slash posts (index), POST slash posts (create), GET slash posts slash id (show), PATCH slash posts slash id (update), DELETE slash posts slash id (destroy), plus new and edit, and path helpers (posts_path, post_path of id). Nested: resources posts do resources comments end gives slash posts slash post_id slash comments. Check your routes with rails routes (lists every route and helper). Routes are how a URL becomes a controller action. A common mistake is hand-writing the 7 routes when resources posts generates them, and forgetting root, so there's no homepage."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Routes map URLs to controller actions in config/routes.rb — they're the app's entry point.\n\nThe forms: get '/about', to: 'pages#about' (a single route); resources :posts (the 7 REST routes); root to: 'posts#index' (the homepage).\n\nresources :posts generates GET /posts (index), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy), plus new/edit, and path helpers (posts_path, post_path(id)). Nested: resources :posts do resources :comments end gives /posts/:post_id/comments. Check your routes with rails routes (lists every route + helper). Routes are how a URL becomes a controller action.\n\nHand-writing the 7 routes when resources :posts generates them, or forgetting root (no homepage)."
          },
          {
            "type": "flow_diagram",
            "title": "routes.rb",
            "content": "root, get, resources :posts (7 routes + helpers). Check with rails routes.\n\n```mermaid\nflowchart LR\n  R[root to: 'posts#index'] --> G[get '/about', to: 'pages#about']\n  G --> Rs[resources :posts -> 7 routes]\n  Rs --> N[nested: resources :comments]\n```"
          },
          {
            "type": "code_example",
            "title": "Routes",
            "content": "```ruby\n# config/routes.rb\nRails.application.routes.draw do\n  root to: 'posts#index'                # GET / -> PostsController#index\n\n  get '/about', to: 'pages#about'        # a single route\n\n  resources :posts                       # the 7 REST routes + helpers\n  #   GET    /posts        -> index      posts_path\n  #   POST   /posts        -> create     posts_path\n  #   GET    /posts/:id    -> show       post_path(@post)\n  #   PATCH  /posts/:id    -> update     post_path(@post)\n  #   DELETE /posts/:id    -> destroy    post_path(@post)\n  #   GET    /posts/new    -> new        new_post_path\n  #   GET    /posts/:id/edit -> edit     edit_post_path(@post)\n\n  # Nested resource:\n  resources :posts do\n    resources :comments                  # /posts/:post_id/comments\n  end\n\n  # API-only namespacing:\n  namespace :api do\n    resources :posts                     # /api/posts -> Api::PostsController\n  end\nend\n\n# List every route + its helper:  rails routes\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Routes Basics in a real project?",
        "How would you compare Routes Basics with an alternative?",
        "What is a common mistake when using Routes Basics?"
      ],
      "seo": {
        "metaTitle": "Rails Routes Basics — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What are routes in Rails and how do you write them? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "rails-mvc-intro-routes-basics-q001",
      "slug": "rails-mvc-intro-routes-basics-when-to-use",
      "question": "When would you use Routes Basics in a real project?",
      "title": "When would you use Routes Basics in a real project",
      "direct_answer": "I would use routes in Rails and how do you write them when the problem matches its main benefit. Routes map URLs to controller actions in config/routes.rb — they're the app's entry point. In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain routes in Rails and how do you write them in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about routes in Rails and how do you write them without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining routes in Rails and how do you write them in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. Routes map URLs to controller actions in config/routes.rb — they're the app's entry point. The forms: get '/about', to: 'pages#about' (a single route); resources :posts (the 7 REST routes); root to: 'posts#index' (the homepage). resources :posts generates GET /posts (index), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy), plus new/edit, and path helpers (posts_path, post_path(id)). Nested: resources :posts do resources :comments end gives /posts/:post_id/comments. Check your routes with rails routes (lists every route + helper). Routes are how a URL becomes a controller action."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use routes in Rails and how do you write them when its rule makes the code safer, clearer, or more efficient for the actual problem. Routes map URLs to controller actions in config/routes.rb — they're the app's entry point. The forms: get '/about', to: 'pages#about' (a single route); resources :posts (the 7 REST routes); root to: 'posts#index' (the homepage). resources :posts generates GET /posts (index), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy), plus new/edit, and path helpers (posts_path, post_path(id)). Nested: resources :posts do resources :comments end gives /posts/:post_id/comments. Check your routes with rails routes (lists every route + helper). Routes are how a URL becomes a controller action. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Routes map URLs to controller actions in config/routes.rb — they're the app's entry point.\n\nThe forms: get '/about', to: 'pages#about' (a single route); resources :posts (the 7 REST routes); root to: 'posts#index' (the homepage).\n\nresources :posts generates GET /posts (index), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy), plus new/edit, and path helpers (posts_path, post_path(id)). Nested: resources :posts do resources :comments end gives /posts/:post_id/comments. Check your routes with rails routes (lists every route + helper). Routes are how a URL becomes a controller action.\n\nHand-writing the 7 routes when resources :posts generates them, or forgetting root (no homepage)."
          },
          {
            "type": "code_example",
            "title": "Routes",
            "content": "```ruby\n# config/routes.rb\nRails.application.routes.draw do\n  root to: 'posts#index'                # GET / -> PostsController#index\n\n  get '/about', to: 'pages#about'        # a single route\n\n  resources :posts                       # the 7 REST routes + helpers\n  #   GET    /posts        -> index      posts_path\n  #   POST   /posts        -> create     posts_path\n  #   GET    /posts/:id    -> show       post_path(@post)\n  #   PATCH  /posts/:id    -> update     post_path(@post)\n  #   DELETE /posts/:id    -> destroy    post_path(@post)\n  #   GET    /posts/new    -> new        new_post_path\n  #   GET    /posts/:id/edit -> edit     edit_post_path(@post)\n\n  # Nested resource:\n  resources :posts do\n    resources :comments                  # /posts/:post_id/comments\n  end\n\n  # API-only namespacing:\n  namespace :api do\n    resources :posts                     # /api/posts -> Api::PostsController\n  end\nend\n\n# List every route + its helper:  rails routes\n```"
          }
        ]
      },
      "followup_questions": [
        "What are routes in Rails and how do you write them?",
        "How would you compare Routes Basics with an alternative?",
        "What is a common mistake when using Routes Basics?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Routes Basics in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Routes Basics in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "rails-mvc-intro-routes-basics-q002",
      "slug": "rails-mvc-intro-routes-basics-common-mistake",
      "question": "What is a common mistake when using Routes Basics?",
      "title": "a common mistake when using Routes Basics",
      "direct_answer": "A common mistake with routes in Rails and how do you write them is using the rule without checking its boundary conditions. Routes map URLs to controller actions in config/routes.rb — they're the app's entry point. That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain routes in Rails and how do you write them in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about routes in Rails and how do you write them without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of routes in Rails and how do you write them. Routes map URLs to controller actions in config/routes.rb — they're the app's entry point. The forms: get '/about', to: 'pages#about' (a single route); resources :posts (the 7 REST routes); root to: 'posts#index' (the homepage). resources :posts generates GET /posts (index), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy), plus new/edit, and path helpers (posts_path, post_path(id)). Nested: resources :posts do resources :comments end gives /posts/:post_id/comments. Check your routes with rails routes (lists every route + helper). Routes are how a URL becomes a controller action."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with routes in Rails and how do you write them is assuming the happy path is the whole rule. Routes map URLs to controller actions in config/routes.rb — they're the app's entry point. The forms: get '/about', to: 'pages#about' (a single route); resources :posts (the 7 REST routes); root to: 'posts#index' (the homepage). resources :posts generates GET /posts (index), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy), plus new/edit, and path helpers (posts_path, post_path(id)). Nested: resources :posts do resources :comments end gives /posts/:post_id/comments. Check your routes with rails routes (lists every route + helper). Routes are how a URL becomes a controller action. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "Routes map URLs to controller actions in config/routes.rb — they're the app's entry point.\n\nThe forms: get '/about', to: 'pages#about' (a single route); resources :posts (the 7 REST routes); root to: 'posts#index' (the homepage).\n\nresources :posts generates GET /posts (index), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy), plus new/edit, and path helpers (posts_path, post_path(id)). Nested: resources :posts do resources :comments end gives /posts/:post_id/comments. Check your routes with rails routes (lists every route + helper). Routes are how a URL becomes a controller action.\n\nHand-writing the 7 routes when resources :posts generates them, or forgetting root (no homepage)."
          },
          {
            "type": "code_example",
            "title": "Routes",
            "content": "```ruby\n# config/routes.rb\nRails.application.routes.draw do\n  root to: 'posts#index'                # GET / -> PostsController#index\n\n  get '/about', to: 'pages#about'        # a single route\n\n  resources :posts                       # the 7 REST routes + helpers\n  #   GET    /posts        -> index      posts_path\n  #   POST   /posts        -> create     posts_path\n  #   GET    /posts/:id    -> show       post_path(@post)\n  #   PATCH  /posts/:id    -> update     post_path(@post)\n  #   DELETE /posts/:id    -> destroy    post_path(@post)\n  #   GET    /posts/new    -> new        new_post_path\n  #   GET    /posts/:id/edit -> edit     edit_post_path(@post)\n\n  # Nested resource:\n  resources :posts do\n    resources :comments                  # /posts/:post_id/comments\n  end\n\n  # API-only namespacing:\n  namespace :api do\n    resources :posts                     # /api/posts -> Api::PostsController\n  end\nend\n\n# List every route + its helper:  rails routes\n```"
          }
        ]
      },
      "followup_questions": [
        "What are routes in Rails and how do you write them?",
        "When would you use Routes Basics in a real project?",
        "How would you compare Routes Basics with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Routes Basics — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Routes Basics? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "rails-mvc-intro-routes-basics-q003",
      "slug": "rails-mvc-intro-routes-basics-compare",
      "question": "How would you compare Routes Basics with an alternative?",
      "title": "compare Routes Basics with an alternative",
      "direct_answer": "I compare routes in Rails and how do you write them with the simpler alternative by looking at correctness first, then time, memory, and readability. Routes map URLs to controller actions in config/routes.rb — they're the app's entry point. The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain routes in Rails and how do you write them in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about routes in Rails and how do you write them without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. Routes map URLs to controller actions in config/routes.rb — they're the app's entry point. The forms: get '/about', to: 'pages#about' (a single route); resources :posts (the 7 REST routes); root to: 'posts#index' (the homepage). resources :posts generates GET /posts (index), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy), plus new/edit, and path helpers (posts_path, post_path(id)). Nested: resources :posts do resources :comments end gives /posts/:post_id/comments. Check your routes with rails routes (lists every route + helper). Routes are how a URL becomes a controller action."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare routes in Rails and how do you write them with the straightforward alternative on the same example. Routes map URLs to controller actions in config/routes.rb — they're the app's entry point. The forms: get '/about', to: 'pages#about' (a single route); resources :posts (the 7 REST routes); root to: 'posts#index' (the homepage). resources :posts generates GET /posts (index), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy), plus new/edit, and path helpers (posts_path, post_path(id)). Nested: resources :posts do resources :comments end gives /posts/:post_id/comments. Check your routes with rails routes (lists every route + helper). Routes are how a URL becomes a controller action. If routes in Rails and how do you write them removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "Routes map URLs to controller actions in config/routes.rb — they're the app's entry point.\n\nThe forms: get '/about', to: 'pages#about' (a single route); resources :posts (the 7 REST routes); root to: 'posts#index' (the homepage).\n\nresources :posts generates GET /posts (index), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy), plus new/edit, and path helpers (posts_path, post_path(id)). Nested: resources :posts do resources :comments end gives /posts/:post_id/comments. Check your routes with rails routes (lists every route + helper). Routes are how a URL becomes a controller action.\n\nHand-writing the 7 routes when resources :posts generates them, or forgetting root (no homepage)."
          },
          {
            "type": "code_example",
            "title": "Routes",
            "content": "```ruby\n# config/routes.rb\nRails.application.routes.draw do\n  root to: 'posts#index'                # GET / -> PostsController#index\n\n  get '/about', to: 'pages#about'        # a single route\n\n  resources :posts                       # the 7 REST routes + helpers\n  #   GET    /posts        -> index      posts_path\n  #   POST   /posts        -> create     posts_path\n  #   GET    /posts/:id    -> show       post_path(@post)\n  #   PATCH  /posts/:id    -> update     post_path(@post)\n  #   DELETE /posts/:id    -> destroy    post_path(@post)\n  #   GET    /posts/new    -> new        new_post_path\n  #   GET    /posts/:id/edit -> edit     edit_post_path(@post)\n\n  # Nested resource:\n  resources :posts do\n    resources :comments                  # /posts/:post_id/comments\n  end\n\n  # API-only namespacing:\n  namespace :api do\n    resources :posts                     # /api/posts -> Api::PostsController\n  end\nend\n\n# List every route + its helper:  rails routes\n```"
          }
        ]
      },
      "followup_questions": [
        "What are routes in Rails and how do you write them?",
        "When would you use Routes Basics in a real project?",
        "What is a common mistake when using Routes Basics?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Routes Basics with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Routes Basics with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "rails-mvc-intro-routes-basics-q004",
      "slug": "rails-mvc-intro-routes-basics-scenario",
      "question": "How would you debug a problem involving Routes Basics?",
      "title": "debug a problem involving Routes Basics",
      "direct_answer": "I would debug a problem involving routes in Rails and how do you write them by reproducing it with the smallest input first. Routes map URLs to controller actions in config/routes.rb — they're the app's entry point. Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain routes in Rails and how do you write them in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about routes in Rails and how do you write them without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what routes in Rails and how do you write them is supposed to guarantee. Routes map URLs to controller actions in config/routes.rb — they're the app's entry point. The forms: get '/about', to: 'pages#about' (a single route); resources :posts (the 7 REST routes); root to: 'posts#index' (the homepage). resources :posts generates GET /posts (index), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy), plus new/edit, and path helpers (posts_path, post_path(id)). Nested: resources :posts do resources :comments end gives /posts/:post_id/comments. Check your routes with rails routes (lists every route + helper). Routes are how a URL becomes a controller action."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the routes in Rails and how do you write them bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: Routes map URLs to controller actions in config/routes.rb — they're the app's entry point. The forms: get '/about', to: 'pages#about' (a single route); resources :posts (the 7 REST routes); root to: 'posts#index' (the homepage). resources :posts generates GET /posts (index), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy), plus new/edit, and path helpers (posts_path, post_path(id)). Nested: resources :posts do resources :comments end gives /posts/:post_id/comments. Check your routes with rails routes (lists every route + helper). Routes are how a URL becomes a controller action. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "Routes map URLs to controller actions in config/routes.rb — they're the app's entry point.\n\nThe forms: get '/about', to: 'pages#about' (a single route); resources :posts (the 7 REST routes); root to: 'posts#index' (the homepage).\n\nresources :posts generates GET /posts (index), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy), plus new/edit, and path helpers (posts_path, post_path(id)). Nested: resources :posts do resources :comments end gives /posts/:post_id/comments. Check your routes with rails routes (lists every route + helper). Routes are how a URL becomes a controller action.\n\nHand-writing the 7 routes when resources :posts generates them, or forgetting root (no homepage)."
          },
          {
            "type": "code_example",
            "title": "Routes",
            "content": "```ruby\n# config/routes.rb\nRails.application.routes.draw do\n  root to: 'posts#index'                # GET / -> PostsController#index\n\n  get '/about', to: 'pages#about'        # a single route\n\n  resources :posts                       # the 7 REST routes + helpers\n  #   GET    /posts        -> index      posts_path\n  #   POST   /posts        -> create     posts_path\n  #   GET    /posts/:id    -> show       post_path(@post)\n  #   PATCH  /posts/:id    -> update     post_path(@post)\n  #   DELETE /posts/:id    -> destroy    post_path(@post)\n  #   GET    /posts/new    -> new        new_post_path\n  #   GET    /posts/:id/edit -> edit     edit_post_path(@post)\n\n  # Nested resource:\n  resources :posts do\n    resources :comments                  # /posts/:post_id/comments\n  end\n\n  # API-only namespacing:\n  namespace :api do\n    resources :posts                     # /api/posts -> Api::PostsController\n  end\nend\n\n# List every route + its helper:  rails routes\n```"
          }
        ]
      },
      "followup_questions": [
        "What are routes in Rails and how do you write them?",
        "When would you use Routes Basics in a real project?",
        "How would you compare Routes Basics with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Routes Basics — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Routes Basics? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

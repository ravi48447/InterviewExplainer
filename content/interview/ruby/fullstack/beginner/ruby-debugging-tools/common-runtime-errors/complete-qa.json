{
  "topic": "Common Runtime Errors",
  "topicSlug": "common-runtime-errors",
  "questions": [
    {
      "id": "common-runtime-errors-q001",
      "slug": "ruby-common-runtime-errors-basics",
      "question": "What are the most common Ruby runtime errors?",
      "title": "Common Ruby Runtime Errors",
      "direct_answer": "The errors you'll see most: NoMethodError (calling a method that doesn't exist, often on nil — `nil.empty?` raises: undefined method empty? for nil); NameError (using an undefined variable/method: `foo` when foo isn't defined); ArgumentError (wrong number of arguments: `add(1,2,3)` for a 2-arg method); TypeError (wrong type: `\"a\" + 1` raises no implicit conversion of Integer into String); IndexError/KeyError (out-of-range array / missing hash key); ZeroDivisionError (n/0); RuntimeError (generic raise). The #1 cause: nil — a method returned nil when you expected an object, and you called a method on it. Fix: read the message (it names the method and receiver), check what returned nil (use &. or a default), and add a guard or a validation.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you can name the common errors (NoMethodError on nil, ArgumentError count, TypeError) and that the #1 cause is nil.",
        "common_mistake": "Ignoring the error message (it names the method and receiver — read it), or not checking for nil before calling a method.",
        "to_stand_out": "Common errors: NoMethodError (often nil), NameError, ArgumentError (count), TypeError, KeyError, ZeroDivisionError. #1 cause is nil — read the message; check what returned nil."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define Common Ruby Runtime Errors simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "NoMethodError: method missing, most often on nil (nil.empty?).",
              "NameError: undefined variable/method; ArgumentError: wrong number of arguments.",
              "TypeError: wrong type ('a' + 1); KeyError: missing hash key (.fetch).",
              "ZeroDivisionError: n / 0; RuntimeError: generic raise.",
              "The #1 cause is nil — read the message (it names method+receiver); guard with &. or a default."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "The errors you'll see most: NoMethodError (calling a method that doesn't exist, often on nil — nil dot empty raises undefined method empty for nil); NameError (using an undefined variable/method: foo when foo isn't defined); ArgumentError (wrong number of arguments: add 1 2 3 for a 2-arg method); TypeError (wrong type: a plus 1 raises no implicit conversion of Integer into String); IndexError or KeyError (out-of-range array / missing hash key); ZeroDivisionError (n divided by 0); RuntimeError (generic raise). The number one cause: nil — a method returned nil when you expected an object, and you called a method on it. Fix: read the message (it names the method and receiver), check what returned nil (use safe-navigation or a default), and add a guard or a validation. A common mistake is ignoring the error message, which names the method and receiver, and not checking for nil before calling a method."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "The errors you'll see most: NoMethodError (calling a method that doesn't exist, often on nil — `nil.empty?` raises: undefined method empty?\n\nfor nil); NameError (using an undefined variable/method: `foo` when foo isn't defined); ArgumentError (wrong number of arguments: `add(1,2,3)` for a 2-arg method); TypeError (wrong type: `\"a\" + 1` raises no implicit conversion of Integer into String); IndexError/KeyError (out-of-range array / missing hash key); ZeroDivisionError (n/0); RuntimeError (generic raise).\n\nThe #1 cause: nil — a method returned nil when you expected an object, and you called a method on it. Fix: read the message (it names the method and receiver), check what returned nil (use &. or a default), and add a guard or a validation.\n\nIgnoring the error message (it names the method and receiver — read it), or not checking for nil before calling a method."
          },
          {
            "type": "flow_diagram",
            "title": "Common Errors",
            "content": "NoMethodError (often nil), NameError, ArgumentError (count), TypeError, KeyError, ZeroDivisionError. #1 cause: nil.\n\n```mermaid\nflowchart LR\n  N[NoMethodError] --> Nil[often nil.empty?]\n  A[ArgumentError] --> C[wrong arg count]\n  T[TypeError] --> W[wrong type]\n  K[KeyError] --> M[missing hash key]\n```"
          },
          {
            "type": "code_example",
            "title": "Common runtime errors",
            "content": "```ruby\n# NoMethodError: calling a method that doesn't exist (OFTEN on nil)\nnil.empty?       # NoMethodError: undefined method `empty?' for nil\nnil&.empty?      # nil (safe navigation: returns nil instead of raising)\n\n# NameError: undefined variable/method\nputs foo          # NameError: undefined local variable or method `foo'\n\n# ArgumentError: wrong number of arguments\ndef add(a, b); a + b; end\nadd(1, 2, 3)     # ArgumentError: wrong number of arguments (given 3, expected 2)\n\n# TypeError: wrong type\n'a' + 1          # TypeError: no implicit conversion of Integer into String\n'a' + 1.to_s    # 'a1' (convert first)\n\n# IndexError / KeyError\n[1,2,3][10]      # nil (Array returns nil out of range; .fetch raises)\n{a:1}.fetch(:b)  # KeyError: key not found (:b)\n\n# ZeroDivisionError\n1 / 0            # ZeroDivisionError: divided by 0\n\n# The #1 cause is NIL: a method returned nil; you called a method on it.\n# Fix: read the message (it names the method + receiver); guard with &. or a default.\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Common Runtime Errors in a real project?",
        "How would you compare Common Runtime Errors with an alternative?",
        "What is a common mistake when using Common Runtime Errors?"
      ],
      "seo": {
        "metaTitle": "Common Ruby Runtime Errors — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What are the most common Ruby runtime errors? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "ruby-debugging-tools-common-runtime-errors-q001",
      "slug": "ruby-debugging-tools-common-runtime-errors-when-to-use",
      "question": "When would you use Common Runtime Errors in a real project?",
      "title": "When would you use Common Runtime Errors in a real project",
      "direct_answer": "I would use the most common Ruby runtime errors when the problem matches its main benefit. The errors you'll see most: NoMethodError (calling a method that doesn't exist, often on nil — `nil.empty?` raises: undefined method empty? In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain the most common Ruby runtime errors in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about the most common Ruby runtime errors without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining the most common Ruby runtime errors in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. The errors you'll see most: NoMethodError (calling a method that doesn't exist, often on nil — `nil.empty?` raises: undefined method empty? for nil); NameError (using an undefined variable/method: `foo` when foo isn't defined); ArgumentError (wrong number of arguments: `add(1,2,3)` for a 2-arg method); TypeError (wrong type: `\"a\" + 1` raises no implicit conversion of Integer into String); IndexError/KeyError (out-of-range array / missing hash key); ZeroDivisionError (n/0); RuntimeError (generic raise). The #1 cause: nil — a method returned nil when you expected an object, and you called a method on it. Fix: read the message (it names the method and receiver), check what returned nil (use &. or a default), and add a guard or a validation."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use the most common Ruby runtime errors when its rule makes the code safer, clearer, or more efficient for the actual problem. The errors you'll see most: NoMethodError (calling a method that doesn't exist, often on nil — `nil.empty?` raises: undefined method empty? for nil); NameError (using an undefined variable/method: `foo` when foo isn't defined); ArgumentError (wrong number of arguments: `add(1,2,3)` for a 2-arg method); TypeError (wrong type: `\"a\" + 1` raises no implicit conversion of Integer into String); IndexError/KeyError (out-of-range array / missing hash key); ZeroDivisionError (n/0); RuntimeError (generic raise). The #1 cause: nil — a method returned nil when you expected an object, and you called a method on it. Fix: read the message (it names the method and receiver), check what returned nil (use &. or a default), and add a guard or a validation. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "The errors you'll see most: NoMethodError (calling a method that doesn't exist, often on nil — `nil.empty?` raises: undefined method empty?\n\nfor nil); NameError (using an undefined variable/method: `foo` when foo isn't defined); ArgumentError (wrong number of arguments: `add(1,2,3)` for a 2-arg method); TypeError (wrong type: `\"a\" + 1` raises no implicit conversion of Integer into String); IndexError/KeyError (out-of-range array / missing hash key); ZeroDivisionError (n/0); RuntimeError (generic raise).\n\nThe #1 cause: nil — a method returned nil when you expected an object, and you called a method on it. Fix: read the message (it names the method and receiver), check what returned nil (use &. or a default), and add a guard or a validation.\n\nIgnoring the error message (it names the method and receiver — read it), or not checking for nil before calling a method."
          },
          {
            "type": "code_example",
            "title": "Common runtime errors",
            "content": "```ruby\n# NoMethodError: calling a method that doesn't exist (OFTEN on nil)\nnil.empty?       # NoMethodError: undefined method `empty?' for nil\nnil&.empty?      # nil (safe navigation: returns nil instead of raising)\n\n# NameError: undefined variable/method\nputs foo          # NameError: undefined local variable or method `foo'\n\n# ArgumentError: wrong number of arguments\ndef add(a, b); a + b; end\nadd(1, 2, 3)     # ArgumentError: wrong number of arguments (given 3, expected 2)\n\n# TypeError: wrong type\n'a' + 1          # TypeError: no implicit conversion of Integer into String\n'a' + 1.to_s    # 'a1' (convert first)\n\n# IndexError / KeyError\n[1,2,3][10]      # nil (Array returns nil out of range; .fetch raises)\n{a:1}.fetch(:b)  # KeyError: key not found (:b)\n\n# ZeroDivisionError\n1 / 0            # ZeroDivisionError: divided by 0\n\n# The #1 cause is NIL: a method returned nil; you called a method on it.\n# Fix: read the message (it names the method + receiver); guard with &. or a default.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are the most common Ruby runtime errors?",
        "How would you compare Common Runtime Errors with an alternative?",
        "What is a common mistake when using Common Runtime Errors?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Common Runtime Errors in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Common Runtime Errors in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-debugging-tools-common-runtime-errors-q002",
      "slug": "ruby-debugging-tools-common-runtime-errors-common-mistake",
      "question": "What is a common mistake when using Common Runtime Errors?",
      "title": "a common mistake when using Common Runtime Errors",
      "direct_answer": "A common mistake with the most common Ruby runtime errors is using the rule without checking its boundary conditions. The errors you'll see most: NoMethodError (calling a method that doesn't exist, often on nil — `nil.empty?` raises: undefined method empty? That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain the most common Ruby runtime errors in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about the most common Ruby runtime errors without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of the most common Ruby runtime errors. The errors you'll see most: NoMethodError (calling a method that doesn't exist, often on nil — `nil.empty?` raises: undefined method empty? for nil); NameError (using an undefined variable/method: `foo` when foo isn't defined); ArgumentError (wrong number of arguments: `add(1,2,3)` for a 2-arg method); TypeError (wrong type: `\"a\" + 1` raises no implicit conversion of Integer into String); IndexError/KeyError (out-of-range array / missing hash key); ZeroDivisionError (n/0); RuntimeError (generic raise). The #1 cause: nil — a method returned nil when you expected an object, and you called a method on it. Fix: read the message (it names the method and receiver), check what returned nil (use &. or a default), and add a guard or a validation."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with the most common Ruby runtime errors is assuming the happy path is the whole rule. The errors you'll see most: NoMethodError (calling a method that doesn't exist, often on nil — `nil.empty?` raises: undefined method empty? for nil); NameError (using an undefined variable/method: `foo` when foo isn't defined); ArgumentError (wrong number of arguments: `add(1,2,3)` for a 2-arg method); TypeError (wrong type: `\"a\" + 1` raises no implicit conversion of Integer into String); IndexError/KeyError (out-of-range array / missing hash key); ZeroDivisionError (n/0); RuntimeError (generic raise). The #1 cause: nil — a method returned nil when you expected an object, and you called a method on it. Fix: read the message (it names the method and receiver), check what returned nil (use &. or a default), and add a guard or a validation. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "The errors you'll see most: NoMethodError (calling a method that doesn't exist, often on nil — `nil.empty?` raises: undefined method empty?\n\nfor nil); NameError (using an undefined variable/method: `foo` when foo isn't defined); ArgumentError (wrong number of arguments: `add(1,2,3)` for a 2-arg method); TypeError (wrong type: `\"a\" + 1` raises no implicit conversion of Integer into String); IndexError/KeyError (out-of-range array / missing hash key); ZeroDivisionError (n/0); RuntimeError (generic raise).\n\nThe #1 cause: nil — a method returned nil when you expected an object, and you called a method on it. Fix: read the message (it names the method and receiver), check what returned nil (use &. or a default), and add a guard or a validation.\n\nIgnoring the error message (it names the method and receiver — read it), or not checking for nil before calling a method."
          },
          {
            "type": "code_example",
            "title": "Common runtime errors",
            "content": "```ruby\n# NoMethodError: calling a method that doesn't exist (OFTEN on nil)\nnil.empty?       # NoMethodError: undefined method `empty?' for nil\nnil&.empty?      # nil (safe navigation: returns nil instead of raising)\n\n# NameError: undefined variable/method\nputs foo          # NameError: undefined local variable or method `foo'\n\n# ArgumentError: wrong number of arguments\ndef add(a, b); a + b; end\nadd(1, 2, 3)     # ArgumentError: wrong number of arguments (given 3, expected 2)\n\n# TypeError: wrong type\n'a' + 1          # TypeError: no implicit conversion of Integer into String\n'a' + 1.to_s    # 'a1' (convert first)\n\n# IndexError / KeyError\n[1,2,3][10]      # nil (Array returns nil out of range; .fetch raises)\n{a:1}.fetch(:b)  # KeyError: key not found (:b)\n\n# ZeroDivisionError\n1 / 0            # ZeroDivisionError: divided by 0\n\n# The #1 cause is NIL: a method returned nil; you called a method on it.\n# Fix: read the message (it names the method + receiver); guard with &. or a default.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are the most common Ruby runtime errors?",
        "When would you use Common Runtime Errors in a real project?",
        "How would you compare Common Runtime Errors with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Common Runtime Errors — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Common Runtime Errors? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-debugging-tools-common-runtime-errors-q003",
      "slug": "ruby-debugging-tools-common-runtime-errors-compare",
      "question": "How would you compare Common Runtime Errors with an alternative?",
      "title": "compare Common Runtime Errors with an alternative",
      "direct_answer": "I compare the most common Ruby runtime errors with the simpler alternative by looking at correctness first, then time, memory, and readability. The errors you'll see most: NoMethodError (calling a method that doesn't exist, often on nil — `nil.empty?` raises: undefined method empty? The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain the most common Ruby runtime errors in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about the most common Ruby runtime errors without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. The errors you'll see most: NoMethodError (calling a method that doesn't exist, often on nil — `nil.empty?` raises: undefined method empty? for nil); NameError (using an undefined variable/method: `foo` when foo isn't defined); ArgumentError (wrong number of arguments: `add(1,2,3)` for a 2-arg method); TypeError (wrong type: `\"a\" + 1` raises no implicit conversion of Integer into String); IndexError/KeyError (out-of-range array / missing hash key); ZeroDivisionError (n/0); RuntimeError (generic raise). The #1 cause: nil — a method returned nil when you expected an object, and you called a method on it. Fix: read the message (it names the method and receiver), check what returned nil (use &. or a default), and add a guard or a validation."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare the most common Ruby runtime errors with the straightforward alternative on the same example. The errors you'll see most: NoMethodError (calling a method that doesn't exist, often on nil — `nil.empty?` raises: undefined method empty? for nil); NameError (using an undefined variable/method: `foo` when foo isn't defined); ArgumentError (wrong number of arguments: `add(1,2,3)` for a 2-arg method); TypeError (wrong type: `\"a\" + 1` raises no implicit conversion of Integer into String); IndexError/KeyError (out-of-range array / missing hash key); ZeroDivisionError (n/0); RuntimeError (generic raise). The #1 cause: nil — a method returned nil when you expected an object, and you called a method on it. Fix: read the message (it names the method and receiver), check what returned nil (use &. or a default), and add a guard or a validation. If the most common Ruby runtime errors removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "The errors you'll see most: NoMethodError (calling a method that doesn't exist, often on nil — `nil.empty?` raises: undefined method empty?\n\nfor nil); NameError (using an undefined variable/method: `foo` when foo isn't defined); ArgumentError (wrong number of arguments: `add(1,2,3)` for a 2-arg method); TypeError (wrong type: `\"a\" + 1` raises no implicit conversion of Integer into String); IndexError/KeyError (out-of-range array / missing hash key); ZeroDivisionError (n/0); RuntimeError (generic raise).\n\nThe #1 cause: nil — a method returned nil when you expected an object, and you called a method on it. Fix: read the message (it names the method and receiver), check what returned nil (use &. or a default), and add a guard or a validation.\n\nIgnoring the error message (it names the method and receiver — read it), or not checking for nil before calling a method."
          },
          {
            "type": "code_example",
            "title": "Common runtime errors",
            "content": "```ruby\n# NoMethodError: calling a method that doesn't exist (OFTEN on nil)\nnil.empty?       # NoMethodError: undefined method `empty?' for nil\nnil&.empty?      # nil (safe navigation: returns nil instead of raising)\n\n# NameError: undefined variable/method\nputs foo          # NameError: undefined local variable or method `foo'\n\n# ArgumentError: wrong number of arguments\ndef add(a, b); a + b; end\nadd(1, 2, 3)     # ArgumentError: wrong number of arguments (given 3, expected 2)\n\n# TypeError: wrong type\n'a' + 1          # TypeError: no implicit conversion of Integer into String\n'a' + 1.to_s    # 'a1' (convert first)\n\n# IndexError / KeyError\n[1,2,3][10]      # nil (Array returns nil out of range; .fetch raises)\n{a:1}.fetch(:b)  # KeyError: key not found (:b)\n\n# ZeroDivisionError\n1 / 0            # ZeroDivisionError: divided by 0\n\n# The #1 cause is NIL: a method returned nil; you called a method on it.\n# Fix: read the message (it names the method + receiver); guard with &. or a default.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are the most common Ruby runtime errors?",
        "When would you use Common Runtime Errors in a real project?",
        "What is a common mistake when using Common Runtime Errors?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Common Runtime Errors with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Common Runtime Errors with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-debugging-tools-common-runtime-errors-q004",
      "slug": "ruby-debugging-tools-common-runtime-errors-scenario",
      "question": "How would you debug a problem involving Common Runtime Errors?",
      "title": "debug a problem involving Common Runtime Errors",
      "direct_answer": "I would debug a problem involving the most common Ruby runtime errors by reproducing it with the smallest input first. The errors you'll see most: NoMethodError (calling a method that doesn't exist, often on nil — `nil.empty?` raises: undefined method empty? Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain the most common Ruby runtime errors in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about the most common Ruby runtime errors without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what the most common Ruby runtime errors is supposed to guarantee. The errors you'll see most: NoMethodError (calling a method that doesn't exist, often on nil — `nil.empty?` raises: undefined method empty? for nil); NameError (using an undefined variable/method: `foo` when foo isn't defined); ArgumentError (wrong number of arguments: `add(1,2,3)` for a 2-arg method); TypeError (wrong type: `\"a\" + 1` raises no implicit conversion of Integer into String); IndexError/KeyError (out-of-range array / missing hash key); ZeroDivisionError (n/0); RuntimeError (generic raise). The #1 cause: nil — a method returned nil when you expected an object, and you called a method on it. Fix: read the message (it names the method and receiver), check what returned nil (use &. or a default), and add a guard or a validation."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the the most common Ruby runtime errors bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: The errors you'll see most: NoMethodError (calling a method that doesn't exist, often on nil — `nil.empty?` raises: undefined method empty? for nil); NameError (using an undefined variable/method: `foo` when foo isn't defined); ArgumentError (wrong number of arguments: `add(1,2,3)` for a 2-arg method); TypeError (wrong type: `\"a\" + 1` raises no implicit conversion of Integer into String); IndexError/KeyError (out-of-range array / missing hash key); ZeroDivisionError (n/0); RuntimeError (generic raise). The #1 cause: nil — a method returned nil when you expected an object, and you called a method on it. Fix: read the message (it names the method and receiver), check what returned nil (use &. or a default), and add a guard or a validation. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "The errors you'll see most: NoMethodError (calling a method that doesn't exist, often on nil — `nil.empty?` raises: undefined method empty?\n\nfor nil); NameError (using an undefined variable/method: `foo` when foo isn't defined); ArgumentError (wrong number of arguments: `add(1,2,3)` for a 2-arg method); TypeError (wrong type: `\"a\" + 1` raises no implicit conversion of Integer into String); IndexError/KeyError (out-of-range array / missing hash key); ZeroDivisionError (n/0); RuntimeError (generic raise).\n\nThe #1 cause: nil — a method returned nil when you expected an object, and you called a method on it. Fix: read the message (it names the method and receiver), check what returned nil (use &. or a default), and add a guard or a validation.\n\nIgnoring the error message (it names the method and receiver — read it), or not checking for nil before calling a method."
          },
          {
            "type": "code_example",
            "title": "Common runtime errors",
            "content": "```ruby\n# NoMethodError: calling a method that doesn't exist (OFTEN on nil)\nnil.empty?       # NoMethodError: undefined method `empty?' for nil\nnil&.empty?      # nil (safe navigation: returns nil instead of raising)\n\n# NameError: undefined variable/method\nputs foo          # NameError: undefined local variable or method `foo'\n\n# ArgumentError: wrong number of arguments\ndef add(a, b); a + b; end\nadd(1, 2, 3)     # ArgumentError: wrong number of arguments (given 3, expected 2)\n\n# TypeError: wrong type\n'a' + 1          # TypeError: no implicit conversion of Integer into String\n'a' + 1.to_s    # 'a1' (convert first)\n\n# IndexError / KeyError\n[1,2,3][10]      # nil (Array returns nil out of range; .fetch raises)\n{a:1}.fetch(:b)  # KeyError: key not found (:b)\n\n# ZeroDivisionError\n1 / 0            # ZeroDivisionError: divided by 0\n\n# The #1 cause is NIL: a method returned nil; you called a method on it.\n# Fix: read the message (it names the method + receiver); guard with &. or a default.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are the most common Ruby runtime errors?",
        "When would you use Common Runtime Errors in a real project?",
        "How would you compare Common Runtime Errors with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Common Runtime Errors — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Common Runtime Errors? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

{
  "topic": "Git Basics",
  "topicSlug": "git-basics",
  "questions": [
    {
      "id": "git-basics-q001",
      "slug": "ruby-git-basics",
      "question": "What are the basic Git commands a developer needs?",
      "title": "Git Basics",
      "direct_answer": "Git is version control: it tracks changes to your files over time so you can review history, branch, and collaborate. The core commands: git init (start a repo), git clone URL (copy one), git status (what's changed), git add (stage changes), git commit -m 'msg' (save a snapshot), git push (send commits to the remote), git pull (fetch + merge from remote), git branch (list/create branches), git checkout/switch -b feature (new branch), git merge feature (merge it in), git log (history). The model: working dir -> staging area (add) -> committed history (commit) -> remote (push).",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know the core loop (status, add, commit, push, pull) and the model (working dir -> staging -> commit -> remote).",
        "common_mistake": "Committing without checking git status (staging the wrong files), or not branching for features.",
        "to_stand_out": "Core loop: status, add, commit, push, pull. Model: working dir -> staging (add) -> commit -> remote (push). Branch per feature; merge to combine."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define Git Basics simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "Git tracks changes over time; the core loop is status, add, commit, push, pull.",
              "Model: working dir -> staging (add) -> commit (history) -> push (remote).",
              "Branch per feature (git switch -c) so main stays stable and reviewable.",
              "git merge combines a branch; git log shows history.",
              "Always git status before add/commit so you stage the right files."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "Git is version control: it tracks changes to your files over time so you can review history, branch, and collaborate. The core commands: git init to start a repo, git clone URL to copy one, git status to see what's changed, git add to stage changes, git commit dash m msg to save a snapshot, git push to send commits to the remote, git pull to fetch and merge from the remote, git branch to list or create branches, git checkout or switch dash b feature to make a new branch, git merge feature to merge it in, and git log to see history. The model is working dir to staging area via add to committed history via commit to the remote via push, and a branch per feature keeps main stable. A common mistake is committing without checking git status, because you may stage the wrong files, and working directly on main instead of branching per feature."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Git is version control: it tracks changes to your files over time so you can review history, branch, and collaborate.\n\nThe core commands: git init (start a repo), git clone URL (copy one), git status (what's changed), git add (stage changes), git commit -m 'msg' (save a snapshot), git push (send commits to the remote), git pull (fetch + merge from remote), git branch (list/create branches), git checkout/switch -b feature (new branch), git merge feature (merge it in), git log (history).\n\nThe model: working dir -> staging area (add) -> committed history (commit) -> remote (push).\n\nCommitting without checking git status (staging the wrong files), or not branching for features."
          },
          {
            "type": "flow_diagram",
            "title": "The Git Flow",
            "content": "working dir -> add (staging) -> commit (history) -> push (remote).\n\n```mermaid\nflowchart LR\n  W[working dir] -->|git add| S[staging]\n  S -->|git commit| H[local history]\n  H -->|git push| R[remote]\n  R -->|git pull| H\n```"
          },
          {
            "type": "code_example",
            "title": "The core Git commands",
            "content": "```ruby\n# Core loop (do this many times a day):\n#   git status                 # what's changed\n#   git add .                  # stage all changes (or `git add file.rb`)\n#   git commit -m 'fix login bug'  # save a snapshot\n#   git push                   # send commits to the remote\n#   git pull                   # fetch + merge from remote\n\n# Branching (one branch per feature):\n#   git switch -c add-login     # new branch\n#   ... work, commit ...\n#   git switch main            # go back\n#   git merge add-login        # merge the feature in\n\n# History:\n#   git log --oneline          # one-line history\n#   git diff                   # unstaged changes\n#   git diff --staged          # staged changes\n\n# Clone a repo:\n#   git clone https://github.com/org/repo.git\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Git Basics in a real project?",
        "How would you compare Git Basics with an alternative?",
        "What is a common mistake when using Git Basics?"
      ],
      "seo": {
        "metaTitle": "Git Basics — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What are the basic Git commands a developer needs? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "git-and-build-basics-git-basics-q001",
      "slug": "git-and-build-basics-git-basics-when-to-use",
      "question": "When would you use Git Basics in a real project?",
      "title": "When would you use Git Basics in a real project",
      "direct_answer": "I would use the basic Git commands a developer needs when the problem matches its main benefit. Git is version control: it tracks changes to your files over time so you can review history, branch, and collaborate. In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain the basic Git commands a developer needs in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about the basic Git commands a developer needs without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining the basic Git commands a developer needs in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. Git is version control: it tracks changes to your files over time so you can review history, branch, and collaborate. The core commands: git init (start a repo), git clone URL (copy one), git status (what's changed), git add (stage changes), git commit -m 'msg' (save a snapshot), git push (send commits to the remote), git pull (fetch + merge from remote), git branch (list/create branches), git checkout/switch -b feature (new branch), git merge feature (merge it in), git log (history). The model: working dir -> staging area (add) -> committed history (commit) -> remote (push)."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use the basic Git commands a developer needs when its rule makes the code safer, clearer, or more efficient for the actual problem. Git is version control: it tracks changes to your files over time so you can review history, branch, and collaborate. The core commands: git init (start a repo), git clone URL (copy one), git status (what's changed), git add (stage changes), git commit -m 'msg' (save a snapshot), git push (send commits to the remote), git pull (fetch + merge from remote), git branch (list/create branches), git checkout/switch -b feature (new branch), git merge feature (merge it in), git log (history). The model: working dir -> staging area (add) -> committed history (commit) -> remote (push). I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Git is version control: it tracks changes to your files over time so you can review history, branch, and collaborate.\n\nThe core commands: git init (start a repo), git clone URL (copy one), git status (what's changed), git add (stage changes), git commit -m 'msg' (save a snapshot), git push (send commits to the remote), git pull (fetch + merge from remote), git branch (list/create branches), git checkout/switch -b feature (new branch), git merge feature (merge it in), git log (history).\n\nThe model: working dir -> staging area (add) -> committed history (commit) -> remote (push).\n\nCommitting without checking git status (staging the wrong files), or not branching for features."
          },
          {
            "type": "code_example",
            "title": "The core Git commands",
            "content": "```ruby\n# Core loop (do this many times a day):\n#   git status                 # what's changed\n#   git add .                  # stage all changes (or `git add file.rb`)\n#   git commit -m 'fix login bug'  # save a snapshot\n#   git push                   # send commits to the remote\n#   git pull                   # fetch + merge from remote\n\n# Branching (one branch per feature):\n#   git switch -c add-login     # new branch\n#   ... work, commit ...\n#   git switch main            # go back\n#   git merge add-login        # merge the feature in\n\n# History:\n#   git log --oneline          # one-line history\n#   git diff                   # unstaged changes\n#   git diff --staged          # staged changes\n\n# Clone a repo:\n#   git clone https://github.com/org/repo.git\n```"
          }
        ]
      },
      "followup_questions": [
        "What are the basic Git commands a developer needs?",
        "How would you compare Git Basics with an alternative?",
        "What is a common mistake when using Git Basics?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Git Basics in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Git Basics in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "git-and-build-basics-git-basics-q002",
      "slug": "git-and-build-basics-git-basics-common-mistake",
      "question": "What is a common mistake when using Git Basics?",
      "title": "a common mistake when using Git Basics",
      "direct_answer": "A common mistake with the basic Git commands a developer needs is using the rule without checking its boundary conditions. Git is version control: it tracks changes to your files over time so you can review history, branch, and collaborate. That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain the basic Git commands a developer needs in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about the basic Git commands a developer needs without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of the basic Git commands a developer needs. Git is version control: it tracks changes to your files over time so you can review history, branch, and collaborate. The core commands: git init (start a repo), git clone URL (copy one), git status (what's changed), git add (stage changes), git commit -m 'msg' (save a snapshot), git push (send commits to the remote), git pull (fetch + merge from remote), git branch (list/create branches), git checkout/switch -b feature (new branch), git merge feature (merge it in), git log (history). The model: working dir -> staging area (add) -> committed history (commit) -> remote (push)."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with the basic Git commands a developer needs is assuming the happy path is the whole rule. Git is version control: it tracks changes to your files over time so you can review history, branch, and collaborate. The core commands: git init (start a repo), git clone URL (copy one), git status (what's changed), git add (stage changes), git commit -m 'msg' (save a snapshot), git push (send commits to the remote), git pull (fetch + merge from remote), git branch (list/create branches), git checkout/switch -b feature (new branch), git merge feature (merge it in), git log (history). The model: working dir -> staging area (add) -> committed history (commit) -> remote (push). For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "Git is version control: it tracks changes to your files over time so you can review history, branch, and collaborate.\n\nThe core commands: git init (start a repo), git clone URL (copy one), git status (what's changed), git add (stage changes), git commit -m 'msg' (save a snapshot), git push (send commits to the remote), git pull (fetch + merge from remote), git branch (list/create branches), git checkout/switch -b feature (new branch), git merge feature (merge it in), git log (history).\n\nThe model: working dir -> staging area (add) -> committed history (commit) -> remote (push).\n\nCommitting without checking git status (staging the wrong files), or not branching for features."
          },
          {
            "type": "code_example",
            "title": "The core Git commands",
            "content": "```ruby\n# Core loop (do this many times a day):\n#   git status                 # what's changed\n#   git add .                  # stage all changes (or `git add file.rb`)\n#   git commit -m 'fix login bug'  # save a snapshot\n#   git push                   # send commits to the remote\n#   git pull                   # fetch + merge from remote\n\n# Branching (one branch per feature):\n#   git switch -c add-login     # new branch\n#   ... work, commit ...\n#   git switch main            # go back\n#   git merge add-login        # merge the feature in\n\n# History:\n#   git log --oneline          # one-line history\n#   git diff                   # unstaged changes\n#   git diff --staged          # staged changes\n\n# Clone a repo:\n#   git clone https://github.com/org/repo.git\n```"
          }
        ]
      },
      "followup_questions": [
        "What are the basic Git commands a developer needs?",
        "When would you use Git Basics in a real project?",
        "How would you compare Git Basics with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Git Basics — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Git Basics? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "git-and-build-basics-git-basics-q003",
      "slug": "git-and-build-basics-git-basics-compare",
      "question": "How would you compare Git Basics with an alternative?",
      "title": "compare Git Basics with an alternative",
      "direct_answer": "I compare the basic Git commands a developer needs with the simpler alternative by looking at correctness first, then time, memory, and readability. Git is version control: it tracks changes to your files over time so you can review history, branch, and collaborate. The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain the basic Git commands a developer needs in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about the basic Git commands a developer needs without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. Git is version control: it tracks changes to your files over time so you can review history, branch, and collaborate. The core commands: git init (start a repo), git clone URL (copy one), git status (what's changed), git add (stage changes), git commit -m 'msg' (save a snapshot), git push (send commits to the remote), git pull (fetch + merge from remote), git branch (list/create branches), git checkout/switch -b feature (new branch), git merge feature (merge it in), git log (history). The model: working dir -> staging area (add) -> committed history (commit) -> remote (push)."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare the basic Git commands a developer needs with the straightforward alternative on the same example. Git is version control: it tracks changes to your files over time so you can review history, branch, and collaborate. The core commands: git init (start a repo), git clone URL (copy one), git status (what's changed), git add (stage changes), git commit -m 'msg' (save a snapshot), git push (send commits to the remote), git pull (fetch + merge from remote), git branch (list/create branches), git checkout/switch -b feature (new branch), git merge feature (merge it in), git log (history). The model: working dir -> staging area (add) -> committed history (commit) -> remote (push). If the basic Git commands a developer needs removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "Git is version control: it tracks changes to your files over time so you can review history, branch, and collaborate.\n\nThe core commands: git init (start a repo), git clone URL (copy one), git status (what's changed), git add (stage changes), git commit -m 'msg' (save a snapshot), git push (send commits to the remote), git pull (fetch + merge from remote), git branch (list/create branches), git checkout/switch -b feature (new branch), git merge feature (merge it in), git log (history).\n\nThe model: working dir -> staging area (add) -> committed history (commit) -> remote (push).\n\nCommitting without checking git status (staging the wrong files), or not branching for features."
          },
          {
            "type": "code_example",
            "title": "The core Git commands",
            "content": "```ruby\n# Core loop (do this many times a day):\n#   git status                 # what's changed\n#   git add .                  # stage all changes (or `git add file.rb`)\n#   git commit -m 'fix login bug'  # save a snapshot\n#   git push                   # send commits to the remote\n#   git pull                   # fetch + merge from remote\n\n# Branching (one branch per feature):\n#   git switch -c add-login     # new branch\n#   ... work, commit ...\n#   git switch main            # go back\n#   git merge add-login        # merge the feature in\n\n# History:\n#   git log --oneline          # one-line history\n#   git diff                   # unstaged changes\n#   git diff --staged          # staged changes\n\n# Clone a repo:\n#   git clone https://github.com/org/repo.git\n```"
          }
        ]
      },
      "followup_questions": [
        "What are the basic Git commands a developer needs?",
        "When would you use Git Basics in a real project?",
        "What is a common mistake when using Git Basics?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Git Basics with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Git Basics with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "git-and-build-basics-git-basics-q004",
      "slug": "git-and-build-basics-git-basics-scenario",
      "question": "How would you debug a problem involving Git Basics?",
      "title": "debug a problem involving Git Basics",
      "direct_answer": "I would debug a problem involving the basic Git commands a developer needs by reproducing it with the smallest input first. Git is version control: it tracks changes to your files over time so you can review history, branch, and collaborate. Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain the basic Git commands a developer needs in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about the basic Git commands a developer needs without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what the basic Git commands a developer needs is supposed to guarantee. Git is version control: it tracks changes to your files over time so you can review history, branch, and collaborate. The core commands: git init (start a repo), git clone URL (copy one), git status (what's changed), git add (stage changes), git commit -m 'msg' (save a snapshot), git push (send commits to the remote), git pull (fetch + merge from remote), git branch (list/create branches), git checkout/switch -b feature (new branch), git merge feature (merge it in), git log (history). The model: working dir -> staging area (add) -> committed history (commit) -> remote (push)."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the the basic Git commands a developer needs bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: Git is version control: it tracks changes to your files over time so you can review history, branch, and collaborate. The core commands: git init (start a repo), git clone URL (copy one), git status (what's changed), git add (stage changes), git commit -m 'msg' (save a snapshot), git push (send commits to the remote), git pull (fetch + merge from remote), git branch (list/create branches), git checkout/switch -b feature (new branch), git merge feature (merge it in), git log (history). The model: working dir -> staging area (add) -> committed history (commit) -> remote (push). Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "Git is version control: it tracks changes to your files over time so you can review history, branch, and collaborate.\n\nThe core commands: git init (start a repo), git clone URL (copy one), git status (what's changed), git add (stage changes), git commit -m 'msg' (save a snapshot), git push (send commits to the remote), git pull (fetch + merge from remote), git branch (list/create branches), git checkout/switch -b feature (new branch), git merge feature (merge it in), git log (history).\n\nThe model: working dir -> staging area (add) -> committed history (commit) -> remote (push).\n\nCommitting without checking git status (staging the wrong files), or not branching for features."
          },
          {
            "type": "code_example",
            "title": "The core Git commands",
            "content": "```ruby\n# Core loop (do this many times a day):\n#   git status                 # what's changed\n#   git add .                  # stage all changes (or `git add file.rb`)\n#   git commit -m 'fix login bug'  # save a snapshot\n#   git push                   # send commits to the remote\n#   git pull                   # fetch + merge from remote\n\n# Branching (one branch per feature):\n#   git switch -c add-login     # new branch\n#   ... work, commit ...\n#   git switch main            # go back\n#   git merge add-login        # merge the feature in\n\n# History:\n#   git log --oneline          # one-line history\n#   git diff                   # unstaged changes\n#   git diff --staged          # staged changes\n\n# Clone a repo:\n#   git clone https://github.com/org/repo.git\n```"
          }
        ]
      },
      "followup_questions": [
        "What are the basic Git commands a developer needs?",
        "When would you use Git Basics in a real project?",
        "How would you compare Git Basics with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Git Basics — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Git Basics? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

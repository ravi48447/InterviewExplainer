{
  "topic": "Arrays and Hashes",
  "topicSlug": "arrays-hashes",
  "questions": [
    {
      "id": "arrays-hashes-q001",
      "slug": "ruby-arrays-hashes-basics",
      "question": "What are arrays and hashes in Ruby?",
      "title": "Arrays and Hashes in Ruby",
      "direct_answer": "Arrays and Hashes are Ruby's core collections. An Array is an ordered, integer-indexed list: [1, 2, 3]; a[0] == 1; a << 4 appends; a.each, a.map, a[0..1] (slice). A Hash is a key-value map: {name: 'Ada', age: 30}; h[:name] == 'Ada'; h[:city] = 'NYC' adds. Use an Array when you have a list (order matters, duplicates ok, index by position); use a Hash when you map keys to values (lookup by key, unique keys). Both are mutable and include Enumerable (so each, map, select, reduce work on both). Arrays keep insertion order; Hashes also keep insertion order (since 1.9). Choose Array for sequences, Hash for lookups/records.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know an Array is an ordered indexed list and a Hash is a key-value map, and when to use each.",
        "common_mistake": "Using an Array for key-based lookup (O(n) — use a Hash), or a Hash when order/duplicates matter (use an Array).",
        "to_stand_out": "Array = ordered, indexed list ([1,2,3]; a[0]; <<). Hash = key-value map ({k:v}; h[:k]). Array for sequences; Hash for lookups. Both mutable + Enumerable."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define Arrays and Hashes in Ruby simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "An Array is an ordered, integer-indexed list ([1,2,3]; a[0]; <<).",
              "A Hash is a key-value map ({name: 'Ada'}; h[:name]; h[:k]=v).",
              "Use an Array for sequences (order/duplicates ok); a Hash for lookups (unique keys).",
              "Both are mutable and include Enumerable (each/map/select/reduce work on both).",
              "Arrays keep insertion order; Hashes also keep insertion order since Ruby 1.9."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "Arrays and Hashes are Ruby's core collections. An Array is an ordered, integer-indexed list: 1 2 3; a index 0 equals 1; a double-less-than 4 appends; a dot each, a dot map, a 0 dot dot 1 (slice). A Hash is a key-value map: name Ada, age 30; h square name equals Ada; h square city equals NYC adds. Use an Array when you have a list (order matters, duplicates ok, index by position); use a Hash when you map keys to values (lookup by key, unique keys). Both are mutable and include Enumerable (so each, map, select, reduce work on both). Arrays keep insertion order; Hashes also keep insertion order since 1.9. Choose Array for sequences, Hash for lookups and records. A common mistake is using an Array for key-based lookup, which is O of n, when a Hash is O of 1, and a Hash when order or duplicates matter, when you should use an Array."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Arrays and Hashes are Ruby's core collections.\n\nAn Array is an ordered, integer-indexed list: [1, 2, 3]; a[0] == 1; a << 4 appends; a.each, a.map, a[0..1] (slice).\n\nA Hash is a key-value map: {name: 'Ada', age: 30}; h[:name] == 'Ada'; h[:city] = 'NYC' adds. Use an Array when you have a list (order matters, duplicates ok, index by position); use a Hash when you map keys to values (lookup by key, unique keys). Both are mutable and include Enumerable (so each, map, select, reduce work on both). Arrays keep insertion order; Hashes also keep insertion order (since 1.9). Choose Array for sequences, Hash for lookups/records.\n\nA common mistake is using an Array for key-based lookup (O(n) — use a Hash), or a Hash when order/duplicates matter (use an Array)."
          },
          {
            "type": "flow_diagram",
            "title": "Array vs Hash",
            "content": "Array = ordered indexed list; Hash = key-value map. Array for sequences; Hash for lookups. Both Enumerable.\n\n```mermaid\nflowchart LR\n  A[Array: [1,2,3]] --> I[indexed: a[0]]\n  H[Hash: {k:v}] --> K[keyed: h[:k]]\n  A --> E[Enumerable: each/map]\n  H --> E\n```"
          },
          {
            "type": "code_example",
            "title": "Arrays and hashes",
            "content": "```ruby\n# Array: ordered, integer-indexed list\na = [10, 20, 30]\na[0]            # 10 (index by position)\na << 40         # append -> [10, 20, 30, 40]\na.length        # 4\na.each { |x| puts x }\na.map  { |x| x * 2 }   # [20, 40, 60, 80]\na[1..2]         # [20, 30] (slice, inclusive)\na.include?(20) # true\n\n# Hash: key-value map\nh = { name: 'Ada', age: 30 }\nh[:name]        # 'Ada' (lookup by key)\nh[:age] = 31    # set\nh[:city] = 'NYC'# add a key\nh.keys          # [:name, :age, :city]\nh.each { |k, v| puts \"#{k}=#{v}\" }\nh.delete(:age)  # remove a key\n\n# Both are MUTABLE and Enumerable (each/map/select/reduce work on both).\n# Array: order + duplicates ok, index by position. Use for SEQUENCES.\n# Hash: unique keys, lookup by key. Use for LOOKUPS / records.\n# Both keep insertion order (Hash since 1.9).\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Arrays Hashes in a real project?",
        "How would you compare Arrays Hashes with an alternative?",
        "What is a common mistake when using Arrays Hashes?"
      ],
      "seo": {
        "metaTitle": "Arrays and Hashes in Ruby — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What are arrays and hashes in Ruby? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "ruby-language-basics-arrays-hashes-q001",
      "slug": "ruby-language-basics-arrays-hashes-when-to-use",
      "question": "When would you use Arrays Hashes in a real project?",
      "title": "When would you use Arrays Hashes in a real project",
      "direct_answer": "I would use arrays and hashes in Ruby when the problem matches its main benefit. Arrays and Hashes are Ruby's core collections. In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain arrays and hashes in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about arrays and hashes in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining arrays and hashes in Ruby in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. Arrays and Hashes are Ruby's core collections. An Array is an ordered, integer-indexed list: [1, 2, 3]; a[0] == 1; a << 4 appends; a.each, a.map, a[0..1] (slice). A Hash is a key-value map: {name: 'Ada', age: 30}; h[:name] == 'Ada'; h[:city] = 'NYC' adds. Use an Array when you have a list (order matters, duplicates ok, index by position); use a Hash when you map keys to values (lookup by key, unique keys). Both are mutable and include Enumerable (so each, map, select, reduce work on both). Arrays keep insertion order; Hashes also keep insertion order (since 1.9). Choose Array for sequences, Hash for lookups/records."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use arrays and hashes in Ruby when its rule makes the code safer, clearer, or more efficient for the actual problem. Arrays and Hashes are Ruby's core collections. An Array is an ordered, integer-indexed list: [1, 2, 3]; a[0] == 1; a << 4 appends; a.each, a.map, a[0..1] (slice). A Hash is a key-value map: {name: 'Ada', age: 30}; h[:name] == 'Ada'; h[:city] = 'NYC' adds. Use an Array when you have a list (order matters, duplicates ok, index by position); use a Hash when you map keys to values (lookup by key, unique keys). Both are mutable and include Enumerable (so each, map, select, reduce work on both). Arrays keep insertion order; Hashes also keep insertion order (since 1.9). Choose Array for sequences, Hash for lookups/records. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Arrays and Hashes are Ruby's core collections.\n\nAn Array is an ordered, integer-indexed list: [1, 2, 3]; a[0] == 1; a << 4 appends; a.each, a.map, a[0..1] (slice).\n\nA Hash is a key-value map: {name: 'Ada', age: 30}; h[:name] == 'Ada'; h[:city] = 'NYC' adds. Use an Array when you have a list (order matters, duplicates ok, index by position); use a Hash when you map keys to values (lookup by key, unique keys). Both are mutable and include Enumerable (so each, map, select, reduce work on both). Arrays keep insertion order; Hashes also keep insertion order (since 1.9). Choose Array for sequences, Hash for lookups/records.\n\nA common mistake is using an Array for key-based lookup (O(n) — use a Hash), or a Hash when order/duplicates matter (use an Array)."
          },
          {
            "type": "code_example",
            "title": "Arrays and hashes",
            "content": "```ruby\n# Array: ordered, integer-indexed list\na = [10, 20, 30]\na[0]            # 10 (index by position)\na << 40         # append -> [10, 20, 30, 40]\na.length        # 4\na.each { |x| puts x }\na.map  { |x| x * 2 }   # [20, 40, 60, 80]\na[1..2]         # [20, 30] (slice, inclusive)\na.include?(20) # true\n\n# Hash: key-value map\nh = { name: 'Ada', age: 30 }\nh[:name]        # 'Ada' (lookup by key)\nh[:age] = 31    # set\nh[:city] = 'NYC'# add a key\nh.keys          # [:name, :age, :city]\nh.each { |k, v| puts \"#{k}=#{v}\" }\nh.delete(:age)  # remove a key\n\n# Both are MUTABLE and Enumerable (each/map/select/reduce work on both).\n# Array: order + duplicates ok, index by position. Use for SEQUENCES.\n# Hash: unique keys, lookup by key. Use for LOOKUPS / records.\n# Both keep insertion order (Hash since 1.9).\n```"
          }
        ]
      },
      "followup_questions": [
        "What are arrays and hashes in Ruby?",
        "How would you compare Arrays Hashes with an alternative?",
        "What is a common mistake when using Arrays Hashes?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Arrays Hashes in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Arrays Hashes in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-language-basics-arrays-hashes-q002",
      "slug": "ruby-language-basics-arrays-hashes-common-mistake",
      "question": "What is a common mistake when using Arrays Hashes?",
      "title": "a common mistake when using Arrays Hashes",
      "direct_answer": "A common mistake with arrays and hashes in Ruby is using the rule without checking its boundary conditions. Arrays and Hashes are Ruby's core collections. That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain arrays and hashes in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about arrays and hashes in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of arrays and hashes in Ruby. Arrays and Hashes are Ruby's core collections. An Array is an ordered, integer-indexed list: [1, 2, 3]; a[0] == 1; a << 4 appends; a.each, a.map, a[0..1] (slice). A Hash is a key-value map: {name: 'Ada', age: 30}; h[:name] == 'Ada'; h[:city] = 'NYC' adds. Use an Array when you have a list (order matters, duplicates ok, index by position); use a Hash when you map keys to values (lookup by key, unique keys). Both are mutable and include Enumerable (so each, map, select, reduce work on both). Arrays keep insertion order; Hashes also keep insertion order (since 1.9). Choose Array for sequences, Hash for lookups/records."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with arrays and hashes in Ruby is assuming the happy path is the whole rule. Arrays and Hashes are Ruby's core collections. An Array is an ordered, integer-indexed list: [1, 2, 3]; a[0] == 1; a << 4 appends; a.each, a.map, a[0..1] (slice). A Hash is a key-value map: {name: 'Ada', age: 30}; h[:name] == 'Ada'; h[:city] = 'NYC' adds. Use an Array when you have a list (order matters, duplicates ok, index by position); use a Hash when you map keys to values (lookup by key, unique keys). Both are mutable and include Enumerable (so each, map, select, reduce work on both). Arrays keep insertion order; Hashes also keep insertion order (since 1.9). Choose Array for sequences, Hash for lookups/records. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "Arrays and Hashes are Ruby's core collections.\n\nAn Array is an ordered, integer-indexed list: [1, 2, 3]; a[0] == 1; a << 4 appends; a.each, a.map, a[0..1] (slice).\n\nA Hash is a key-value map: {name: 'Ada', age: 30}; h[:name] == 'Ada'; h[:city] = 'NYC' adds. Use an Array when you have a list (order matters, duplicates ok, index by position); use a Hash when you map keys to values (lookup by key, unique keys). Both are mutable and include Enumerable (so each, map, select, reduce work on both). Arrays keep insertion order; Hashes also keep insertion order (since 1.9). Choose Array for sequences, Hash for lookups/records.\n\nA common mistake is using an Array for key-based lookup (O(n) — use a Hash), or a Hash when order/duplicates matter (use an Array)."
          },
          {
            "type": "code_example",
            "title": "Arrays and hashes",
            "content": "```ruby\n# Array: ordered, integer-indexed list\na = [10, 20, 30]\na[0]            # 10 (index by position)\na << 40         # append -> [10, 20, 30, 40]\na.length        # 4\na.each { |x| puts x }\na.map  { |x| x * 2 }   # [20, 40, 60, 80]\na[1..2]         # [20, 30] (slice, inclusive)\na.include?(20) # true\n\n# Hash: key-value map\nh = { name: 'Ada', age: 30 }\nh[:name]        # 'Ada' (lookup by key)\nh[:age] = 31    # set\nh[:city] = 'NYC'# add a key\nh.keys          # [:name, :age, :city]\nh.each { |k, v| puts \"#{k}=#{v}\" }\nh.delete(:age)  # remove a key\n\n# Both are MUTABLE and Enumerable (each/map/select/reduce work on both).\n# Array: order + duplicates ok, index by position. Use for SEQUENCES.\n# Hash: unique keys, lookup by key. Use for LOOKUPS / records.\n# Both keep insertion order (Hash since 1.9).\n```"
          }
        ]
      },
      "followup_questions": [
        "What are arrays and hashes in Ruby?",
        "When would you use Arrays Hashes in a real project?",
        "How would you compare Arrays Hashes with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Arrays Hashes — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Arrays Hashes? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-language-basics-arrays-hashes-q003",
      "slug": "ruby-language-basics-arrays-hashes-compare",
      "question": "How would you compare Arrays Hashes with an alternative?",
      "title": "compare Arrays Hashes with an alternative",
      "direct_answer": "I compare arrays and hashes in Ruby with the simpler alternative by looking at correctness first, then time, memory, and readability. Arrays and Hashes are Ruby's core collections. The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain arrays and hashes in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about arrays and hashes in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. Arrays and Hashes are Ruby's core collections. An Array is an ordered, integer-indexed list: [1, 2, 3]; a[0] == 1; a << 4 appends; a.each, a.map, a[0..1] (slice). A Hash is a key-value map: {name: 'Ada', age: 30}; h[:name] == 'Ada'; h[:city] = 'NYC' adds. Use an Array when you have a list (order matters, duplicates ok, index by position); use a Hash when you map keys to values (lookup by key, unique keys). Both are mutable and include Enumerable (so each, map, select, reduce work on both). Arrays keep insertion order; Hashes also keep insertion order (since 1.9). Choose Array for sequences, Hash for lookups/records."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare arrays and hashes in Ruby with the straightforward alternative on the same example. Arrays and Hashes are Ruby's core collections. An Array is an ordered, integer-indexed list: [1, 2, 3]; a[0] == 1; a << 4 appends; a.each, a.map, a[0..1] (slice). A Hash is a key-value map: {name: 'Ada', age: 30}; h[:name] == 'Ada'; h[:city] = 'NYC' adds. Use an Array when you have a list (order matters, duplicates ok, index by position); use a Hash when you map keys to values (lookup by key, unique keys). Both are mutable and include Enumerable (so each, map, select, reduce work on both). Arrays keep insertion order; Hashes also keep insertion order (since 1.9). Choose Array for sequences, Hash for lookups/records. If arrays and hashes in Ruby removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "Arrays and Hashes are Ruby's core collections.\n\nAn Array is an ordered, integer-indexed list: [1, 2, 3]; a[0] == 1; a << 4 appends; a.each, a.map, a[0..1] (slice).\n\nA Hash is a key-value map: {name: 'Ada', age: 30}; h[:name] == 'Ada'; h[:city] = 'NYC' adds. Use an Array when you have a list (order matters, duplicates ok, index by position); use a Hash when you map keys to values (lookup by key, unique keys). Both are mutable and include Enumerable (so each, map, select, reduce work on both). Arrays keep insertion order; Hashes also keep insertion order (since 1.9). Choose Array for sequences, Hash for lookups/records.\n\nA common mistake is using an Array for key-based lookup (O(n) — use a Hash), or a Hash when order/duplicates matter (use an Array)."
          },
          {
            "type": "code_example",
            "title": "Arrays and hashes",
            "content": "```ruby\n# Array: ordered, integer-indexed list\na = [10, 20, 30]\na[0]            # 10 (index by position)\na << 40         # append -> [10, 20, 30, 40]\na.length        # 4\na.each { |x| puts x }\na.map  { |x| x * 2 }   # [20, 40, 60, 80]\na[1..2]         # [20, 30] (slice, inclusive)\na.include?(20) # true\n\n# Hash: key-value map\nh = { name: 'Ada', age: 30 }\nh[:name]        # 'Ada' (lookup by key)\nh[:age] = 31    # set\nh[:city] = 'NYC'# add a key\nh.keys          # [:name, :age, :city]\nh.each { |k, v| puts \"#{k}=#{v}\" }\nh.delete(:age)  # remove a key\n\n# Both are MUTABLE and Enumerable (each/map/select/reduce work on both).\n# Array: order + duplicates ok, index by position. Use for SEQUENCES.\n# Hash: unique keys, lookup by key. Use for LOOKUPS / records.\n# Both keep insertion order (Hash since 1.9).\n```"
          }
        ]
      },
      "followup_questions": [
        "What are arrays and hashes in Ruby?",
        "When would you use Arrays Hashes in a real project?",
        "What is a common mistake when using Arrays Hashes?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Arrays Hashes with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Arrays Hashes with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-language-basics-arrays-hashes-q004",
      "slug": "ruby-language-basics-arrays-hashes-scenario",
      "question": "How would you debug a problem involving Arrays Hashes?",
      "title": "debug a problem involving Arrays Hashes",
      "direct_answer": "I would debug a problem involving arrays and hashes in Ruby by reproducing it with the smallest input first. Arrays and Hashes are Ruby's core collections. Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain arrays and hashes in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about arrays and hashes in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what arrays and hashes in Ruby is supposed to guarantee. Arrays and Hashes are Ruby's core collections. An Array is an ordered, integer-indexed list: [1, 2, 3]; a[0] == 1; a << 4 appends; a.each, a.map, a[0..1] (slice). A Hash is a key-value map: {name: 'Ada', age: 30}; h[:name] == 'Ada'; h[:city] = 'NYC' adds. Use an Array when you have a list (order matters, duplicates ok, index by position); use a Hash when you map keys to values (lookup by key, unique keys). Both are mutable and include Enumerable (so each, map, select, reduce work on both). Arrays keep insertion order; Hashes also keep insertion order (since 1.9). Choose Array for sequences, Hash for lookups/records."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the arrays and hashes in Ruby bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: Arrays and Hashes are Ruby's core collections. An Array is an ordered, integer-indexed list: [1, 2, 3]; a[0] == 1; a << 4 appends; a.each, a.map, a[0..1] (slice). A Hash is a key-value map: {name: 'Ada', age: 30}; h[:name] == 'Ada'; h[:city] = 'NYC' adds. Use an Array when you have a list (order matters, duplicates ok, index by position); use a Hash when you map keys to values (lookup by key, unique keys). Both are mutable and include Enumerable (so each, map, select, reduce work on both). Arrays keep insertion order; Hashes also keep insertion order (since 1.9). Choose Array for sequences, Hash for lookups/records. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "Arrays and Hashes are Ruby's core collections.\n\nAn Array is an ordered, integer-indexed list: [1, 2, 3]; a[0] == 1; a << 4 appends; a.each, a.map, a[0..1] (slice).\n\nA Hash is a key-value map: {name: 'Ada', age: 30}; h[:name] == 'Ada'; h[:city] = 'NYC' adds. Use an Array when you have a list (order matters, duplicates ok, index by position); use a Hash when you map keys to values (lookup by key, unique keys). Both are mutable and include Enumerable (so each, map, select, reduce work on both). Arrays keep insertion order; Hashes also keep insertion order (since 1.9). Choose Array for sequences, Hash for lookups/records.\n\nA common mistake is using an Array for key-based lookup (O(n) — use a Hash), or a Hash when order/duplicates matter (use an Array)."
          },
          {
            "type": "code_example",
            "title": "Arrays and hashes",
            "content": "```ruby\n# Array: ordered, integer-indexed list\na = [10, 20, 30]\na[0]            # 10 (index by position)\na << 40         # append -> [10, 20, 30, 40]\na.length        # 4\na.each { |x| puts x }\na.map  { |x| x * 2 }   # [20, 40, 60, 80]\na[1..2]         # [20, 30] (slice, inclusive)\na.include?(20) # true\n\n# Hash: key-value map\nh = { name: 'Ada', age: 30 }\nh[:name]        # 'Ada' (lookup by key)\nh[:age] = 31    # set\nh[:city] = 'NYC'# add a key\nh.keys          # [:name, :age, :city]\nh.each { |k, v| puts \"#{k}=#{v}\" }\nh.delete(:age)  # remove a key\n\n# Both are MUTABLE and Enumerable (each/map/select/reduce work on both).\n# Array: order + duplicates ok, index by position. Use for SEQUENCES.\n# Hash: unique keys, lookup by key. Use for LOOKUPS / records.\n# Both keep insertion order (Hash since 1.9).\n```"
          }
        ]
      },
      "followup_questions": [
        "What are arrays and hashes in Ruby?",
        "When would you use Arrays Hashes in a real project?",
        "How would you compare Arrays Hashes with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Arrays Hashes — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Arrays Hashes? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

{
  "topic": "JSON and Rails API",
  "topicSlug": "json-and-rails-api",
  "questions": [
    {
      "id": "json-and-rails-api-q001",
      "slug": "rails-json-and-api-basics",
      "question": "How do you build a JSON API in Rails?",
      "title": "Building a JSON API in Rails",
      "direct_answer": "A Rails JSON API returns JSON instead of HTML: use rails new --api for a lighter app (no views/assets), render json: in controllers, and set routes with namespace :api. The shape: render json: Post.all (uses to_json), or a serializer for a controlled shape (ActiveModel::Serializers, jsonapi-serializer). Use only: to limit fields and include: for associations to avoid over-exposure. Status codes matter: 201 on create, 204 on destroy, 422 on validation fail. Versioning (namespace :v1) lets you change the API without breaking clients. Keep controllers thin (find, authorize, render json) and put the JSON shape in a serializer once it's complex.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know --api, render json:, status codes, serializers, and versioning (namespace :api/v1) for a JSON API.",
        "common_mistake": "Over-exposing the whole model (use only:), or no versioning (breaking clients on change).",
        "to_stand_out": "JSON API: --api app, render json:, serializers for the shape, status codes (201/204/422), namespace :v1 for versioning. Keep controllers thin."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define Building a JSON API in Rails simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "A Rails JSON API uses --api (lighter app) and render json: in controllers.",
              "Use only: to limit fields and a serializer for a controlled JSON shape.",
              "Status codes: 201 on create, 204 on destroy, 422 on validation fail.",
              "Version with namespace :api do; namespace :v1 do so changes don't break clients.",
              "Keep controllers thin (find, authorize, render json); put the shape in a serializer."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "A Rails JSON API returns JSON instead of HTML: use rails new dash dash api for a lighter app (no views/assets), render json in controllers, and set routes with namespace api. The shape: render json Post dot all (uses to_json), or a serializer for a controlled shape (ActiveModel::Serializers, jsonapi-serializer). Use only to limit fields and include for associations to avoid over-exposure. Status codes matter: 201 on create, 204 on destroy, 422 on validation fail. Versioning (namespace v1) lets you change the API without breaking clients. Keep controllers thin (find, authorize, render json) and put the JSON shape in a serializer once it's complex. A common mistake is over-exposing the whole model, when you should use only, and no versioning, which breaks clients on change."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "A Rails JSON API returns JSON instead of HTML: use rails new --api for a lighter app (no views/assets), render json: in controllers, and set routes with namespace :api.\n\nThe shape: render json: Post.all (uses to_json), or a serializer for a controlled shape (ActiveModel::Serializers, jsonapi-serializer).\n\nUse only: to limit fields and include: for associations to avoid over-exposure. Status codes matter: 201 on create, 204 on destroy, 422 on validation fail. Versioning (namespace :v1) lets you change the API without breaking clients. Keep controllers thin (find, authorize, render json) and put the JSON shape in a serializer once it's complex.\n\nOver-exposing the whole model (use only:), or no versioning (breaking clients on change)."
          },
          {
            "type": "flow_diagram",
            "title": "JSON API Stack",
            "content": "--api + render json: + serializer + status codes + versioning.\n\n```mermaid\nflowchart LR\n  A[--api: no views] --> R[render json:]\n  R --> S[serializer: controlled shape]\n  S --> V[namespace :v1: versioning]\n  V --> C[201/204/422: status codes]\n```"
          },
          {
            "type": "code_example",
            "title": "A JSON API in Rails",
            "content": "```ruby\n# rails new backend --api   (lighter app: no views/assets)\n\n# config/routes.rb\nnamespace :api do\n  namespace :v1 do               # versioning\n    resources :posts               # /api/v1/posts\n  end\nend\n\n# app/controllers/api/v1/posts_controller.rb\nmodule Api\n  module V1\n    class PostsController < ApplicationController\n      def index; render json: Post.all; end\n\n      def create\n        post = Post.create!(post_params)\n        render json: post, status: :created   # 201\n      end\n\n      def destroy\n        Post.find(params[:id]).destroy\n        head :no_content                    # 204\n      end\n\n      private\n      def post_params; params.require(:post).permit(:title, :body); end\n    end\n  end\nend\n\n# Complex shape -> a serializer (ActiveModel::Serializers):\nclass PostSerializer < ActiveModel::Serializer\n  attributes :id, :title, :author_name\n  def author_name; object.user.name; end\nend\n# render json: post, serializer: PostSerializer\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Json And Rails Api in a real project?",
        "How would you compare Json And Rails Api with an alternative?",
        "What is a common mistake when using Json And Rails Api?"
      ],
      "seo": {
        "metaTitle": "Building a JSON API in Rails — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How do you build a JSON API in Rails? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "rest-api-basics-json-and-rails-api-q001",
      "slug": "rest-api-basics-json-and-rails-api-when-to-use",
      "question": "When would you use Json And Rails Api in a real project?",
      "title": "When would you use Json And Rails Api in a real project",
      "direct_answer": "I would use How do you build a JSON API in Rails when the problem matches its main benefit. A Rails JSON API returns JSON instead of HTML: use rails new --api for a lighter app (no views/assets), render json: in controllers, and set routes with namespace :api. In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How do you build a JSON API in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do you build a JSON API in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining How do you build a JSON API in Rails in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. A Rails JSON API returns JSON instead of HTML: use rails new --api for a lighter app (no views/assets), render json: in controllers, and set routes with namespace :api. The shape: render json: Post.all (uses to_json), or a serializer for a controlled shape (ActiveModel::Serializers, jsonapi-serializer). Use only: to limit fields and include: for associations to avoid over-exposure. Status codes matter: 201 on create, 204 on destroy, 422 on validation fail. Versioning (namespace :v1) lets you change the API without breaking clients. Keep controllers thin (find, authorize, render json) and put the JSON shape in a serializer once it's complex."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use How do you build a JSON API in Rails when its rule makes the code safer, clearer, or more efficient for the actual problem. A Rails JSON API returns JSON instead of HTML: use rails new --api for a lighter app (no views/assets), render json: in controllers, and set routes with namespace :api. The shape: render json: Post.all (uses to_json), or a serializer for a controlled shape (ActiveModel::Serializers, jsonapi-serializer). Use only: to limit fields and include: for associations to avoid over-exposure. Status codes matter: 201 on create, 204 on destroy, 422 on validation fail. Versioning (namespace :v1) lets you change the API without breaking clients. Keep controllers thin (find, authorize, render json) and put the JSON shape in a serializer once it's complex. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "A Rails JSON API returns JSON instead of HTML: use rails new --api for a lighter app (no views/assets), render json: in controllers, and set routes with namespace :api.\n\nThe shape: render json: Post.all (uses to_json), or a serializer for a controlled shape (ActiveModel::Serializers, jsonapi-serializer).\n\nUse only: to limit fields and include: for associations to avoid over-exposure. Status codes matter: 201 on create, 204 on destroy, 422 on validation fail. Versioning (namespace :v1) lets you change the API without breaking clients. Keep controllers thin (find, authorize, render json) and put the JSON shape in a serializer once it's complex.\n\nOver-exposing the whole model (use only:), or no versioning (breaking clients on change)."
          },
          {
            "type": "code_example",
            "title": "A JSON API in Rails",
            "content": "```ruby\n# rails new backend --api   (lighter app: no views/assets)\n\n# config/routes.rb\nnamespace :api do\n  namespace :v1 do               # versioning\n    resources :posts               # /api/v1/posts\n  end\nend\n\n# app/controllers/api/v1/posts_controller.rb\nmodule Api\n  module V1\n    class PostsController < ApplicationController\n      def index; render json: Post.all; end\n\n      def create\n        post = Post.create!(post_params)\n        render json: post, status: :created   # 201\n      end\n\n      def destroy\n        Post.find(params[:id]).destroy\n        head :no_content                    # 204\n      end\n\n      private\n      def post_params; params.require(:post).permit(:title, :body); end\n    end\n  end\nend\n\n# Complex shape -> a serializer (ActiveModel::Serializers):\nclass PostSerializer < ActiveModel::Serializer\n  attributes :id, :title, :author_name\n  def author_name; object.user.name; end\nend\n# render json: post, serializer: PostSerializer\n```"
          }
        ]
      },
      "followup_questions": [
        "How do you build a JSON API in Rails?",
        "How would you compare Json And Rails Api with an alternative?",
        "What is a common mistake when using Json And Rails Api?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Json And Rails Api in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Json And Rails Api in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "rest-api-basics-json-and-rails-api-q002",
      "slug": "rest-api-basics-json-and-rails-api-common-mistake",
      "question": "What is a common mistake when using Json And Rails Api?",
      "title": "a common mistake when using Json And Rails Api",
      "direct_answer": "A common mistake with How do you build a JSON API in Rails is using the rule without checking its boundary conditions. A Rails JSON API returns JSON instead of HTML: use rails new --api for a lighter app (no views/assets), render json: in controllers, and set routes with namespace :api. That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How do you build a JSON API in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do you build a JSON API in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of How do you build a JSON API in Rails. A Rails JSON API returns JSON instead of HTML: use rails new --api for a lighter app (no views/assets), render json: in controllers, and set routes with namespace :api. The shape: render json: Post.all (uses to_json), or a serializer for a controlled shape (ActiveModel::Serializers, jsonapi-serializer). Use only: to limit fields and include: for associations to avoid over-exposure. Status codes matter: 201 on create, 204 on destroy, 422 on validation fail. Versioning (namespace :v1) lets you change the API without breaking clients. Keep controllers thin (find, authorize, render json) and put the JSON shape in a serializer once it's complex."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with How do you build a JSON API in Rails is assuming the happy path is the whole rule. A Rails JSON API returns JSON instead of HTML: use rails new --api for a lighter app (no views/assets), render json: in controllers, and set routes with namespace :api. The shape: render json: Post.all (uses to_json), or a serializer for a controlled shape (ActiveModel::Serializers, jsonapi-serializer). Use only: to limit fields and include: for associations to avoid over-exposure. Status codes matter: 201 on create, 204 on destroy, 422 on validation fail. Versioning (namespace :v1) lets you change the API without breaking clients. Keep controllers thin (find, authorize, render json) and put the JSON shape in a serializer once it's complex. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "A Rails JSON API returns JSON instead of HTML: use rails new --api for a lighter app (no views/assets), render json: in controllers, and set routes with namespace :api.\n\nThe shape: render json: Post.all (uses to_json), or a serializer for a controlled shape (ActiveModel::Serializers, jsonapi-serializer).\n\nUse only: to limit fields and include: for associations to avoid over-exposure. Status codes matter: 201 on create, 204 on destroy, 422 on validation fail. Versioning (namespace :v1) lets you change the API without breaking clients. Keep controllers thin (find, authorize, render json) and put the JSON shape in a serializer once it's complex.\n\nOver-exposing the whole model (use only:), or no versioning (breaking clients on change)."
          },
          {
            "type": "code_example",
            "title": "A JSON API in Rails",
            "content": "```ruby\n# rails new backend --api   (lighter app: no views/assets)\n\n# config/routes.rb\nnamespace :api do\n  namespace :v1 do               # versioning\n    resources :posts               # /api/v1/posts\n  end\nend\n\n# app/controllers/api/v1/posts_controller.rb\nmodule Api\n  module V1\n    class PostsController < ApplicationController\n      def index; render json: Post.all; end\n\n      def create\n        post = Post.create!(post_params)\n        render json: post, status: :created   # 201\n      end\n\n      def destroy\n        Post.find(params[:id]).destroy\n        head :no_content                    # 204\n      end\n\n      private\n      def post_params; params.require(:post).permit(:title, :body); end\n    end\n  end\nend\n\n# Complex shape -> a serializer (ActiveModel::Serializers):\nclass PostSerializer < ActiveModel::Serializer\n  attributes :id, :title, :author_name\n  def author_name; object.user.name; end\nend\n# render json: post, serializer: PostSerializer\n```"
          }
        ]
      },
      "followup_questions": [
        "How do you build a JSON API in Rails?",
        "When would you use Json And Rails Api in a real project?",
        "How would you compare Json And Rails Api with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Json And Rails Api — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Json And Rails Api? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "rest-api-basics-json-and-rails-api-q003",
      "slug": "rest-api-basics-json-and-rails-api-compare",
      "question": "How would you compare Json And Rails Api with an alternative?",
      "title": "compare Json And Rails Api with an alternative",
      "direct_answer": "I compare How do you build a JSON API in Rails with the simpler alternative by looking at correctness first, then time, memory, and readability. A Rails JSON API returns JSON instead of HTML: use rails new --api for a lighter app (no views/assets), render json: in controllers, and set routes with namespace :api. The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How do you build a JSON API in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do you build a JSON API in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. A Rails JSON API returns JSON instead of HTML: use rails new --api for a lighter app (no views/assets), render json: in controllers, and set routes with namespace :api. The shape: render json: Post.all (uses to_json), or a serializer for a controlled shape (ActiveModel::Serializers, jsonapi-serializer). Use only: to limit fields and include: for associations to avoid over-exposure. Status codes matter: 201 on create, 204 on destroy, 422 on validation fail. Versioning (namespace :v1) lets you change the API without breaking clients. Keep controllers thin (find, authorize, render json) and put the JSON shape in a serializer once it's complex."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare How do you build a JSON API in Rails with the straightforward alternative on the same example. A Rails JSON API returns JSON instead of HTML: use rails new --api for a lighter app (no views/assets), render json: in controllers, and set routes with namespace :api. The shape: render json: Post.all (uses to_json), or a serializer for a controlled shape (ActiveModel::Serializers, jsonapi-serializer). Use only: to limit fields and include: for associations to avoid over-exposure. Status codes matter: 201 on create, 204 on destroy, 422 on validation fail. Versioning (namespace :v1) lets you change the API without breaking clients. Keep controllers thin (find, authorize, render json) and put the JSON shape in a serializer once it's complex. If How do you build a JSON API in Rails removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "A Rails JSON API returns JSON instead of HTML: use rails new --api for a lighter app (no views/assets), render json: in controllers, and set routes with namespace :api.\n\nThe shape: render json: Post.all (uses to_json), or a serializer for a controlled shape (ActiveModel::Serializers, jsonapi-serializer).\n\nUse only: to limit fields and include: for associations to avoid over-exposure. Status codes matter: 201 on create, 204 on destroy, 422 on validation fail. Versioning (namespace :v1) lets you change the API without breaking clients. Keep controllers thin (find, authorize, render json) and put the JSON shape in a serializer once it's complex.\n\nOver-exposing the whole model (use only:), or no versioning (breaking clients on change)."
          },
          {
            "type": "code_example",
            "title": "A JSON API in Rails",
            "content": "```ruby\n# rails new backend --api   (lighter app: no views/assets)\n\n# config/routes.rb\nnamespace :api do\n  namespace :v1 do               # versioning\n    resources :posts               # /api/v1/posts\n  end\nend\n\n# app/controllers/api/v1/posts_controller.rb\nmodule Api\n  module V1\n    class PostsController < ApplicationController\n      def index; render json: Post.all; end\n\n      def create\n        post = Post.create!(post_params)\n        render json: post, status: :created   # 201\n      end\n\n      def destroy\n        Post.find(params[:id]).destroy\n        head :no_content                    # 204\n      end\n\n      private\n      def post_params; params.require(:post).permit(:title, :body); end\n    end\n  end\nend\n\n# Complex shape -> a serializer (ActiveModel::Serializers):\nclass PostSerializer < ActiveModel::Serializer\n  attributes :id, :title, :author_name\n  def author_name; object.user.name; end\nend\n# render json: post, serializer: PostSerializer\n```"
          }
        ]
      },
      "followup_questions": [
        "How do you build a JSON API in Rails?",
        "When would you use Json And Rails Api in a real project?",
        "What is a common mistake when using Json And Rails Api?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Json And Rails Api with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Json And Rails Api with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "rest-api-basics-json-and-rails-api-q004",
      "slug": "rest-api-basics-json-and-rails-api-scenario",
      "question": "How would you debug a problem involving Json And Rails Api?",
      "title": "debug a problem involving Json And Rails Api",
      "direct_answer": "I would debug a problem involving How do you build a JSON API in Rails by reproducing it with the smallest input first. A Rails JSON API returns JSON instead of HTML: use rails new --api for a lighter app (no views/assets), render json: in controllers, and set routes with namespace :api. Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How do you build a JSON API in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do you build a JSON API in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what How do you build a JSON API in Rails is supposed to guarantee. A Rails JSON API returns JSON instead of HTML: use rails new --api for a lighter app (no views/assets), render json: in controllers, and set routes with namespace :api. The shape: render json: Post.all (uses to_json), or a serializer for a controlled shape (ActiveModel::Serializers, jsonapi-serializer). Use only: to limit fields and include: for associations to avoid over-exposure. Status codes matter: 201 on create, 204 on destroy, 422 on validation fail. Versioning (namespace :v1) lets you change the API without breaking clients. Keep controllers thin (find, authorize, render json) and put the JSON shape in a serializer once it's complex."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the How do you build a JSON API in Rails bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: A Rails JSON API returns JSON instead of HTML: use rails new --api for a lighter app (no views/assets), render json: in controllers, and set routes with namespace :api. The shape: render json: Post.all (uses to_json), or a serializer for a controlled shape (ActiveModel::Serializers, jsonapi-serializer). Use only: to limit fields and include: for associations to avoid over-exposure. Status codes matter: 201 on create, 204 on destroy, 422 on validation fail. Versioning (namespace :v1) lets you change the API without breaking clients. Keep controllers thin (find, authorize, render json) and put the JSON shape in a serializer once it's complex. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "A Rails JSON API returns JSON instead of HTML: use rails new --api for a lighter app (no views/assets), render json: in controllers, and set routes with namespace :api.\n\nThe shape: render json: Post.all (uses to_json), or a serializer for a controlled shape (ActiveModel::Serializers, jsonapi-serializer).\n\nUse only: to limit fields and include: for associations to avoid over-exposure. Status codes matter: 201 on create, 204 on destroy, 422 on validation fail. Versioning (namespace :v1) lets you change the API without breaking clients. Keep controllers thin (find, authorize, render json) and put the JSON shape in a serializer once it's complex.\n\nOver-exposing the whole model (use only:), or no versioning (breaking clients on change)."
          },
          {
            "type": "code_example",
            "title": "A JSON API in Rails",
            "content": "```ruby\n# rails new backend --api   (lighter app: no views/assets)\n\n# config/routes.rb\nnamespace :api do\n  namespace :v1 do               # versioning\n    resources :posts               # /api/v1/posts\n  end\nend\n\n# app/controllers/api/v1/posts_controller.rb\nmodule Api\n  module V1\n    class PostsController < ApplicationController\n      def index; render json: Post.all; end\n\n      def create\n        post = Post.create!(post_params)\n        render json: post, status: :created   # 201\n      end\n\n      def destroy\n        Post.find(params[:id]).destroy\n        head :no_content                    # 204\n      end\n\n      private\n      def post_params; params.require(:post).permit(:title, :body); end\n    end\n  end\nend\n\n# Complex shape -> a serializer (ActiveModel::Serializers):\nclass PostSerializer < ActiveModel::Serializer\n  attributes :id, :title, :author_name\n  def author_name; object.user.name; end\nend\n# render json: post, serializer: PostSerializer\n```"
          }
        ]
      },
      "followup_questions": [
        "How do you build a JSON API in Rails?",
        "When would you use Json And Rails Api in a real project?",
        "How would you compare Json And Rails Api with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Json And Rails Api — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Json And Rails Api? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

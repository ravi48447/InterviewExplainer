{
  "topic": "HTTP Basics",
  "topicSlug": "http-basics",
  "questions": [
    {
      "id": "http-basics-q001",
      "slug": "rails-api-http-basics",
      "question": "What HTTP basics does a Rails API developer need?",
      "title": "HTTP Basics for Rails APIs",
      "direct_answer": "HTTP is request/response: a client sends a request (method + URL + headers + optional body), the server returns a response (status code + headers + body). Methods: GET (read), POST (create), PUT/PATCH (update; PATCH partial), DELETE (remove). Headers carry metadata (Content-Type: application/json, Authorization: Bearer). Status codes: 2xx success (200 OK, 201 Created, 204 No Content), 4xx client error (400, 401, 403, 404, 422), 5xx server error (500). The body for an API is usually JSON. Stateless: each request carries what it needs (the token in the header); the server keeps no session between requests. Idempotence: GET and DELETE are safe to retry; POST is not.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know the request/response model, the verbs, status code families (2xx/4xx/5xx), and that an API body is usually JSON.",
        "common_mistake": "Returning 200 for a create (use 201), or forgetting the Content-Type header (the server can't parse the JSON body).",
        "to_stand_out": "HTTP = request (method/URL/headers/body) + response (status/headers/body). Verbs: GET/POST/PATCH/DELETE. Codes: 2xx ok, 4xx client, 5xx server. Body is JSON."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define HTTP Basics for Rails APIs simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "HTTP is request/response: request (method/URL/headers/body) + response (status/headers/body).",
              "Methods: GET (read), POST (create), PATCH (update, partial), DELETE (remove).",
              "Status codes: 2xx success (200/201/204), 4xx client error (401/403/404/422), 5xx server (500).",
              "Headers carry metadata: Content-Type: application/json, Authorization: Bearer.",
              "The body for an API is usually JSON; HTTP is stateless (token per request)."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "HTTP is request/response: a client sends a request (method + URL + headers + optional body), the server returns a response (status code + headers + body). Methods: GET (read), POST (create), PUT or PATCH (update; PATCH partial), DELETE (remove). Headers carry metadata (Content-Type application json, Authorization Bearer). Status codes: 2xx success (200 OK, 201 Created, 204 No Content), 4xx client error (400, 401, 403, 404, 422), 5xx server error (500). The body for an API is usually JSON. Stateless: each request carries what it needs (the token in the header); the server keeps no session between requests. Idempotence: GET and DELETE are safe to retry; POST is not. A common mistake is returning 200 for a create, when you should use 201, and forgetting the Content-Type header, so the server can't parse the JSON body."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "HTTP is request/response: a client sends a request (method + URL + headers + optional body), the server returns a response (status code + headers + body).\n\nMethods: GET (read), POST (create), PUT/PATCH (update; PATCH partial), DELETE (remove).\n\nHeaders carry metadata (Content-Type: application/json, Authorization: Bearer). Status codes: 2xx success (200 OK, 201 Created, 204 No Content), 4xx client error (400, 401, 403, 404, 422), 5xx server error (500). The body for an API is usually JSON. Stateless: each request carries what it needs (the token in the header); the server keeps no session between requests. Idempotence: GET and DELETE are safe to retry; POST is not.\n\nReturning 200 for a create (use 201), or forgetting the Content-Type header (the server can't parse the JSON body)."
          },
          {
            "type": "flow_diagram",
            "title": "Request/Response",
            "content": "request (method/URL/headers/body) + response (status/headers/body). Codes: 2xx ok, 4xx client, 5xx server.\n\n```mermaid\nflowchart LR\n  R[request: method+URL+headers+body] --> S[server]\n  S --> Re[response: status+headers+body]\n  Re --> C[2xx ok / 4xx client / 5xx server]\n```"
          },
          {
            "type": "code_example",
            "title": "HTTP basics",
            "content": "```ruby\n# A request:\n#   POST /api/posts HTTP/1.1\n#   Host: api.example.com\n#   Content-Type: application/json\n#   Authorization: Bearer <token>\n#\n#   { \"title\": \"Hello\", \"body\": \"world\" }   <- JSON body\n\n# The response:\n#   HTTP/1.1 201 Created\n#   Content-Type: application/json\n#\n#   { \"id\": 42, \"title\": \"Hello\" }\n\n# Methods:      GET (read) | POST (create) | PATCH (update) | DELETE (remove)\n# Status codes:\n#   2xx success  -> 200 OK, 201 Created, 204 No Content\n#   4xx client   -> 400 Bad, 401 Unauth, 403 Forbidden, 404 Not Found, 422 Validation\n#   5xx server   -> 500 Server Error\n# Headers carry metadata: Content-Type, Authorization, Accept.\n# Body for an API is usually JSON.\n# Stateless: each request carries what it needs (token in the header).\n# Idempotent (safe to retry): GET, DELETE, PUT. NOT idempotent: POST.\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Http Basics in a real project?",
        "How would you compare Http Basics with an alternative?",
        "What is a common mistake when using Http Basics?"
      ],
      "seo": {
        "metaTitle": "HTTP Basics for Rails APIs — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What HTTP basics does a Rails API developer need? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "rest-api-basics-http-basics-q001",
      "slug": "rest-api-basics-http-basics-when-to-use",
      "question": "When would you use Http Basics in a real project?",
      "title": "When would you use Http Basics in a real project",
      "direct_answer": "I would use What HTTP basics does a Rails API developer need when the problem matches its main benefit. HTTP is request/response: a client sends a request (method + URL + headers + optional body), the server returns a response (status code + headers + body). In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain What HTTP basics does a Rails API developer need in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about What HTTP basics does a Rails API developer need without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining What HTTP basics does a Rails API developer need in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. HTTP is request/response: a client sends a request (method + URL + headers + optional body), the server returns a response (status code + headers + body). Methods: GET (read), POST (create), PUT/PATCH (update; PATCH partial), DELETE (remove). Headers carry metadata (Content-Type: application/json, Authorization: Bearer). Status codes: 2xx success (200 OK, 201 Created, 204 No Content), 4xx client error (400, 401, 403, 404, 422), 5xx server error (500). The body for an API is usually JSON. Stateless: each request carries what it needs (the token in the header); the server keeps no session between requests. Idempotence: GET and DELETE are safe to retry; POST is not."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use What HTTP basics does a Rails API developer need when its rule makes the code safer, clearer, or more efficient for the actual problem. HTTP is request/response: a client sends a request (method + URL + headers + optional body), the server returns a response (status code + headers + body). Methods: GET (read), POST (create), PUT/PATCH (update; PATCH partial), DELETE (remove). Headers carry metadata (Content-Type: application/json, Authorization: Bearer). Status codes: 2xx success (200 OK, 201 Created, 204 No Content), 4xx client error (400, 401, 403, 404, 422), 5xx server error (500). The body for an API is usually JSON. Stateless: each request carries what it needs (the token in the header); the server keeps no session between requests. Idempotence: GET and DELETE are safe to retry; POST is not. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "HTTP is request/response: a client sends a request (method + URL + headers + optional body), the server returns a response (status code + headers + body).\n\nMethods: GET (read), POST (create), PUT/PATCH (update; PATCH partial), DELETE (remove).\n\nHeaders carry metadata (Content-Type: application/json, Authorization: Bearer). Status codes: 2xx success (200 OK, 201 Created, 204 No Content), 4xx client error (400, 401, 403, 404, 422), 5xx server error (500). The body for an API is usually JSON. Stateless: each request carries what it needs (the token in the header); the server keeps no session between requests. Idempotence: GET and DELETE are safe to retry; POST is not.\n\nReturning 200 for a create (use 201), or forgetting the Content-Type header (the server can't parse the JSON body)."
          },
          {
            "type": "code_example",
            "title": "HTTP basics",
            "content": "```ruby\n# A request:\n#   POST /api/posts HTTP/1.1\n#   Host: api.example.com\n#   Content-Type: application/json\n#   Authorization: Bearer <token>\n#\n#   { \"title\": \"Hello\", \"body\": \"world\" }   <- JSON body\n\n# The response:\n#   HTTP/1.1 201 Created\n#   Content-Type: application/json\n#\n#   { \"id\": 42, \"title\": \"Hello\" }\n\n# Methods:      GET (read) | POST (create) | PATCH (update) | DELETE (remove)\n# Status codes:\n#   2xx success  -> 200 OK, 201 Created, 204 No Content\n#   4xx client   -> 400 Bad, 401 Unauth, 403 Forbidden, 404 Not Found, 422 Validation\n#   5xx server   -> 500 Server Error\n# Headers carry metadata: Content-Type, Authorization, Accept.\n# Body for an API is usually JSON.\n# Stateless: each request carries what it needs (token in the header).\n# Idempotent (safe to retry): GET, DELETE, PUT. NOT idempotent: POST.\n```"
          }
        ]
      },
      "followup_questions": [
        "What HTTP basics does a Rails API developer need?",
        "How would you compare Http Basics with an alternative?",
        "What is a common mistake when using Http Basics?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Http Basics in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Http Basics in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "rest-api-basics-http-basics-q002",
      "slug": "rest-api-basics-http-basics-common-mistake",
      "question": "What is a common mistake when using Http Basics?",
      "title": "a common mistake when using Http Basics",
      "direct_answer": "A common mistake with What HTTP basics does a Rails API developer need is using the rule without checking its boundary conditions. HTTP is request/response: a client sends a request (method + URL + headers + optional body), the server returns a response (status code + headers + body). That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain What HTTP basics does a Rails API developer need in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about What HTTP basics does a Rails API developer need without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of What HTTP basics does a Rails API developer need. HTTP is request/response: a client sends a request (method + URL + headers + optional body), the server returns a response (status code + headers + body). Methods: GET (read), POST (create), PUT/PATCH (update; PATCH partial), DELETE (remove). Headers carry metadata (Content-Type: application/json, Authorization: Bearer). Status codes: 2xx success (200 OK, 201 Created, 204 No Content), 4xx client error (400, 401, 403, 404, 422), 5xx server error (500). The body for an API is usually JSON. Stateless: each request carries what it needs (the token in the header); the server keeps no session between requests. Idempotence: GET and DELETE are safe to retry; POST is not."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with What HTTP basics does a Rails API developer need is assuming the happy path is the whole rule. HTTP is request/response: a client sends a request (method + URL + headers + optional body), the server returns a response (status code + headers + body). Methods: GET (read), POST (create), PUT/PATCH (update; PATCH partial), DELETE (remove). Headers carry metadata (Content-Type: application/json, Authorization: Bearer). Status codes: 2xx success (200 OK, 201 Created, 204 No Content), 4xx client error (400, 401, 403, 404, 422), 5xx server error (500). The body for an API is usually JSON. Stateless: each request carries what it needs (the token in the header); the server keeps no session between requests. Idempotence: GET and DELETE are safe to retry; POST is not. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "HTTP is request/response: a client sends a request (method + URL + headers + optional body), the server returns a response (status code + headers + body).\n\nMethods: GET (read), POST (create), PUT/PATCH (update; PATCH partial), DELETE (remove).\n\nHeaders carry metadata (Content-Type: application/json, Authorization: Bearer). Status codes: 2xx success (200 OK, 201 Created, 204 No Content), 4xx client error (400, 401, 403, 404, 422), 5xx server error (500). The body for an API is usually JSON. Stateless: each request carries what it needs (the token in the header); the server keeps no session between requests. Idempotence: GET and DELETE are safe to retry; POST is not.\n\nReturning 200 for a create (use 201), or forgetting the Content-Type header (the server can't parse the JSON body)."
          },
          {
            "type": "code_example",
            "title": "HTTP basics",
            "content": "```ruby\n# A request:\n#   POST /api/posts HTTP/1.1\n#   Host: api.example.com\n#   Content-Type: application/json\n#   Authorization: Bearer <token>\n#\n#   { \"title\": \"Hello\", \"body\": \"world\" }   <- JSON body\n\n# The response:\n#   HTTP/1.1 201 Created\n#   Content-Type: application/json\n#\n#   { \"id\": 42, \"title\": \"Hello\" }\n\n# Methods:      GET (read) | POST (create) | PATCH (update) | DELETE (remove)\n# Status codes:\n#   2xx success  -> 200 OK, 201 Created, 204 No Content\n#   4xx client   -> 400 Bad, 401 Unauth, 403 Forbidden, 404 Not Found, 422 Validation\n#   5xx server   -> 500 Server Error\n# Headers carry metadata: Content-Type, Authorization, Accept.\n# Body for an API is usually JSON.\n# Stateless: each request carries what it needs (token in the header).\n# Idempotent (safe to retry): GET, DELETE, PUT. NOT idempotent: POST.\n```"
          }
        ]
      },
      "followup_questions": [
        "What HTTP basics does a Rails API developer need?",
        "When would you use Http Basics in a real project?",
        "How would you compare Http Basics with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Http Basics — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Http Basics? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "rest-api-basics-http-basics-q003",
      "slug": "rest-api-basics-http-basics-compare",
      "question": "How would you compare Http Basics with an alternative?",
      "title": "compare Http Basics with an alternative",
      "direct_answer": "I compare What HTTP basics does a Rails API developer need with the simpler alternative by looking at correctness first, then time, memory, and readability. HTTP is request/response: a client sends a request (method + URL + headers + optional body), the server returns a response (status code + headers + body). The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain What HTTP basics does a Rails API developer need in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about What HTTP basics does a Rails API developer need without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. HTTP is request/response: a client sends a request (method + URL + headers + optional body), the server returns a response (status code + headers + body). Methods: GET (read), POST (create), PUT/PATCH (update; PATCH partial), DELETE (remove). Headers carry metadata (Content-Type: application/json, Authorization: Bearer). Status codes: 2xx success (200 OK, 201 Created, 204 No Content), 4xx client error (400, 401, 403, 404, 422), 5xx server error (500). The body for an API is usually JSON. Stateless: each request carries what it needs (the token in the header); the server keeps no session between requests. Idempotence: GET and DELETE are safe to retry; POST is not."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare What HTTP basics does a Rails API developer need with the straightforward alternative on the same example. HTTP is request/response: a client sends a request (method + URL + headers + optional body), the server returns a response (status code + headers + body). Methods: GET (read), POST (create), PUT/PATCH (update; PATCH partial), DELETE (remove). Headers carry metadata (Content-Type: application/json, Authorization: Bearer). Status codes: 2xx success (200 OK, 201 Created, 204 No Content), 4xx client error (400, 401, 403, 404, 422), 5xx server error (500). The body for an API is usually JSON. Stateless: each request carries what it needs (the token in the header); the server keeps no session between requests. Idempotence: GET and DELETE are safe to retry; POST is not. If What HTTP basics does a Rails API developer need removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "HTTP is request/response: a client sends a request (method + URL + headers + optional body), the server returns a response (status code + headers + body).\n\nMethods: GET (read), POST (create), PUT/PATCH (update; PATCH partial), DELETE (remove).\n\nHeaders carry metadata (Content-Type: application/json, Authorization: Bearer). Status codes: 2xx success (200 OK, 201 Created, 204 No Content), 4xx client error (400, 401, 403, 404, 422), 5xx server error (500). The body for an API is usually JSON. Stateless: each request carries what it needs (the token in the header); the server keeps no session between requests. Idempotence: GET and DELETE are safe to retry; POST is not.\n\nReturning 200 for a create (use 201), or forgetting the Content-Type header (the server can't parse the JSON body)."
          },
          {
            "type": "code_example",
            "title": "HTTP basics",
            "content": "```ruby\n# A request:\n#   POST /api/posts HTTP/1.1\n#   Host: api.example.com\n#   Content-Type: application/json\n#   Authorization: Bearer <token>\n#\n#   { \"title\": \"Hello\", \"body\": \"world\" }   <- JSON body\n\n# The response:\n#   HTTP/1.1 201 Created\n#   Content-Type: application/json\n#\n#   { \"id\": 42, \"title\": \"Hello\" }\n\n# Methods:      GET (read) | POST (create) | PATCH (update) | DELETE (remove)\n# Status codes:\n#   2xx success  -> 200 OK, 201 Created, 204 No Content\n#   4xx client   -> 400 Bad, 401 Unauth, 403 Forbidden, 404 Not Found, 422 Validation\n#   5xx server   -> 500 Server Error\n# Headers carry metadata: Content-Type, Authorization, Accept.\n# Body for an API is usually JSON.\n# Stateless: each request carries what it needs (token in the header).\n# Idempotent (safe to retry): GET, DELETE, PUT. NOT idempotent: POST.\n```"
          }
        ]
      },
      "followup_questions": [
        "What HTTP basics does a Rails API developer need?",
        "When would you use Http Basics in a real project?",
        "What is a common mistake when using Http Basics?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Http Basics with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Http Basics with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "rest-api-basics-http-basics-q004",
      "slug": "rest-api-basics-http-basics-scenario",
      "question": "How would you debug a problem involving Http Basics?",
      "title": "debug a problem involving Http Basics",
      "direct_answer": "I would debug a problem involving What HTTP basics does a Rails API developer need by reproducing it with the smallest input first. HTTP is request/response: a client sends a request (method + URL + headers + optional body), the server returns a response (status code + headers + body). Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain What HTTP basics does a Rails API developer need in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about What HTTP basics does a Rails API developer need without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what What HTTP basics does a Rails API developer need is supposed to guarantee. HTTP is request/response: a client sends a request (method + URL + headers + optional body), the server returns a response (status code + headers + body). Methods: GET (read), POST (create), PUT/PATCH (update; PATCH partial), DELETE (remove). Headers carry metadata (Content-Type: application/json, Authorization: Bearer). Status codes: 2xx success (200 OK, 201 Created, 204 No Content), 4xx client error (400, 401, 403, 404, 422), 5xx server error (500). The body for an API is usually JSON. Stateless: each request carries what it needs (the token in the header); the server keeps no session between requests. Idempotence: GET and DELETE are safe to retry; POST is not."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the What HTTP basics does a Rails API developer need bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: HTTP is request/response: a client sends a request (method + URL + headers + optional body), the server returns a response (status code + headers + body). Methods: GET (read), POST (create), PUT/PATCH (update; PATCH partial), DELETE (remove). Headers carry metadata (Content-Type: application/json, Authorization: Bearer). Status codes: 2xx success (200 OK, 201 Created, 204 No Content), 4xx client error (400, 401, 403, 404, 422), 5xx server error (500). The body for an API is usually JSON. Stateless: each request carries what it needs (the token in the header); the server keeps no session between requests. Idempotence: GET and DELETE are safe to retry; POST is not. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "HTTP is request/response: a client sends a request (method + URL + headers + optional body), the server returns a response (status code + headers + body).\n\nMethods: GET (read), POST (create), PUT/PATCH (update; PATCH partial), DELETE (remove).\n\nHeaders carry metadata (Content-Type: application/json, Authorization: Bearer). Status codes: 2xx success (200 OK, 201 Created, 204 No Content), 4xx client error (400, 401, 403, 404, 422), 5xx server error (500). The body for an API is usually JSON. Stateless: each request carries what it needs (the token in the header); the server keeps no session between requests. Idempotence: GET and DELETE are safe to retry; POST is not.\n\nReturning 200 for a create (use 201), or forgetting the Content-Type header (the server can't parse the JSON body)."
          },
          {
            "type": "code_example",
            "title": "HTTP basics",
            "content": "```ruby\n# A request:\n#   POST /api/posts HTTP/1.1\n#   Host: api.example.com\n#   Content-Type: application/json\n#   Authorization: Bearer <token>\n#\n#   { \"title\": \"Hello\", \"body\": \"world\" }   <- JSON body\n\n# The response:\n#   HTTP/1.1 201 Created\n#   Content-Type: application/json\n#\n#   { \"id\": 42, \"title\": \"Hello\" }\n\n# Methods:      GET (read) | POST (create) | PATCH (update) | DELETE (remove)\n# Status codes:\n#   2xx success  -> 200 OK, 201 Created, 204 No Content\n#   4xx client   -> 400 Bad, 401 Unauth, 403 Forbidden, 404 Not Found, 422 Validation\n#   5xx server   -> 500 Server Error\n# Headers carry metadata: Content-Type, Authorization, Accept.\n# Body for an API is usually JSON.\n# Stateless: each request carries what it needs (token in the header).\n# Idempotent (safe to retry): GET, DELETE, PUT. NOT idempotent: POST.\n```"
          }
        ]
      },
      "followup_questions": [
        "What HTTP basics does a Rails API developer need?",
        "When would you use Http Basics in a real project?",
        "How would you compare Http Basics with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Http Basics — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Http Basics? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

{
  "topic": "RESTful Routes",
  "topicSlug": "restful-routes",
  "questions": [
    {
      "id": "restful-routes-q001",
      "slug": "rails-restful-routes-basics",
      "question": "What are RESTful routes in Rails?",
      "title": "RESTful Routes in Rails",
      "direct_answer": "RESTful routes map the HTTP verbs (GET/POST/PATCH/DELETE) to CRUD actions on a resource, so the same URL does different things by verb: GET /posts (index, list), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy). Rails gives you all of them with resources :posts in routes.rb, which generates the 7 standard routes and path helpers (posts_path, post_path(id)). REST keeps your API predictable: a resource, its URL, and the verb tell you the action. Nested resources (resources :posts do resources :comments end) give /posts/:post_id/comments for a child collection.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know resources :posts gives the 7 CRUD routes, the verb+URL+action mapping, and the path helpers (posts_path).",
        "common_mistake": "Hand-writing each route when resources :posts generates them, or using non-REST URLs (/list_posts, /create_post).",
        "to_stand_out": "resources :posts gives the 7 CRUD routes: GET /posts (index), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy). Predictable URLs + verbs."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define RESTful Routes in Rails simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "REST maps verbs to CRUD: GET (read), POST (create), PATCH (update), DELETE (remove).",
              "resources :posts generates the 7 standard routes and path helpers.",
              "Same URL, different verb = different action (/posts POST vs GET).",
              "Path helpers: posts_path, post_path(@post), new_post_path, edit_post_path.",
              "Nested resources (resources :comments inside :posts) give /posts/:post_id/comments."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "RESTful routes map the HTTP verbs (GET, POST, PATCH, DELETE) to CRUD actions on a resource, so the same URL does different things by verb: GET slash posts (index, list), POST slash posts (create), GET slash posts slash id (show), PATCH slash posts slash id (update), DELETE slash posts slash id (destroy). Rails gives you all of them with resources posts in routes.rb, which generates the 7 standard routes and path helpers (posts_path, post_path of id). REST keeps your API predictable: a resource, its URL, and the verb tell you the action. Nested resources — resources posts do resources comments end — give slash posts slash post_id slash comments for a child collection. A common mistake is hand-writing each route when resources posts generates them, and using non-REST URLs like list_posts or create_post, which break the convention."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "RESTful routes map the HTTP verbs (GET/POST/PATCH/DELETE) to CRUD actions on a resource, so the same URL does different things by verb: GET /posts (index, list), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy).\n\nRails gives you all of them with resources :posts in routes.rb, which generates the 7 standard routes and path helpers (posts_path, post_path(id)).\n\nREST keeps your API predictable: a resource, its URL, and the verb tell you the action. Nested resources (resources :posts do resources :comments end) give /posts/:post_id/comments for a child collection.\n\nHand-writing each route when resources :posts generates them, or using non-REST URLs (/list_posts, /create_post)."
          },
          {
            "type": "flow_diagram",
            "title": "resources :posts -> 7 Routes",
            "content": "One line generates the 7 CRUD routes with path helpers.\n\n```mermaid\nflowchart LR\n  R[resources :posts] --> G[GET /posts: index]\n  R --> P[POST /posts: create]\n  R --> S[GET /posts/:id: show]\n  R --> U[PATCH /posts/:id: update]\n  R --> D[DELETE /posts/:id: destroy]\n```"
          },
          {
            "type": "code_example",
            "title": "RESTful routes",
            "content": "```ruby\n# config/routes.rb\nRails.application.routes.draw do\n  resources :posts     # generates all 7 routes below\nend\n\n# The 7 routes `resources :posts` creates:\n#   Verb     URL              Action    Helper\n#   GET      /posts           index     posts_path\n#   GET      /posts/new       new       new_post_path\n#   POST     /posts           create    posts_path\n#   GET      /posts/:id       show      post_path(@post)\n#   GET      /posts/:id/edit  edit      edit_post_path(@post)\n#   PATCH    /posts/:id       update    post_path(@post)\n#   DELETE   /posts/:id       destroy   post_path(@post)\n\n# Nested resource (a post's comments):\nRails.application.routes.draw do\n  resources :posts do\n    resources :comments   # /posts/:post_id/comments\n  end\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Restful Routes in a real project?",
        "How would you compare Restful Routes with an alternative?",
        "What is a common mistake when using Restful Routes?"
      ],
      "seo": {
        "metaTitle": "RESTful Routes in Rails — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What are RESTful routes in Rails? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "rails-api-basics-restful-routes-q001",
      "slug": "rails-api-basics-restful-routes-when-to-use",
      "question": "When would you use Restful Routes in a real project?",
      "title": "When would you use Restful Routes in a real project",
      "direct_answer": "I would use RESTful routes in Rails when the problem matches its main benefit. RESTful routes map the HTTP verbs (GET/POST/PATCH/DELETE) to CRUD actions on a resource, so the same URL does different things by verb: GET /posts (index, list), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy). In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain RESTful routes in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about RESTful routes in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining RESTful routes in Rails in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. RESTful routes map the HTTP verbs (GET/POST/PATCH/DELETE) to CRUD actions on a resource, so the same URL does different things by verb: GET /posts (index, list), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy). Rails gives you all of them with resources :posts in routes.rb, which generates the 7 standard routes and path helpers (posts_path, post_path(id)). REST keeps your API predictable: a resource, its URL, and the verb tell you the action. Nested resources (resources :posts do resources :comments end) give /posts/:post_id/comments for a child collection."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use RESTful routes in Rails when its rule makes the code safer, clearer, or more efficient for the actual problem. RESTful routes map the HTTP verbs (GET/POST/PATCH/DELETE) to CRUD actions on a resource, so the same URL does different things by verb: GET /posts (index, list), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy). Rails gives you all of them with resources :posts in routes.rb, which generates the 7 standard routes and path helpers (posts_path, post_path(id)). REST keeps your API predictable: a resource, its URL, and the verb tell you the action. Nested resources (resources :posts do resources :comments end) give /posts/:post_id/comments for a child collection. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "RESTful routes map the HTTP verbs (GET/POST/PATCH/DELETE) to CRUD actions on a resource, so the same URL does different things by verb: GET /posts (index, list), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy).\n\nRails gives you all of them with resources :posts in routes.rb, which generates the 7 standard routes and path helpers (posts_path, post_path(id)).\n\nREST keeps your API predictable: a resource, its URL, and the verb tell you the action. Nested resources (resources :posts do resources :comments end) give /posts/:post_id/comments for a child collection.\n\nHand-writing each route when resources :posts generates them, or using non-REST URLs (/list_posts, /create_post)."
          },
          {
            "type": "code_example",
            "title": "RESTful routes",
            "content": "```ruby\n# config/routes.rb\nRails.application.routes.draw do\n  resources :posts     # generates all 7 routes below\nend\n\n# The 7 routes `resources :posts` creates:\n#   Verb     URL              Action    Helper\n#   GET      /posts           index     posts_path\n#   GET      /posts/new       new       new_post_path\n#   POST     /posts           create    posts_path\n#   GET      /posts/:id       show      post_path(@post)\n#   GET      /posts/:id/edit  edit      edit_post_path(@post)\n#   PATCH    /posts/:id       update    post_path(@post)\n#   DELETE   /posts/:id       destroy   post_path(@post)\n\n# Nested resource (a post's comments):\nRails.application.routes.draw do\n  resources :posts do\n    resources :comments   # /posts/:post_id/comments\n  end\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "What are RESTful routes in Rails?",
        "How would you compare Restful Routes with an alternative?",
        "What is a common mistake when using Restful Routes?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Restful Routes in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Restful Routes in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "rails-api-basics-restful-routes-q002",
      "slug": "rails-api-basics-restful-routes-common-mistake",
      "question": "What is a common mistake when using Restful Routes?",
      "title": "a common mistake when using Restful Routes",
      "direct_answer": "A common mistake with RESTful routes in Rails is using the rule without checking its boundary conditions. RESTful routes map the HTTP verbs (GET/POST/PATCH/DELETE) to CRUD actions on a resource, so the same URL does different things by verb: GET /posts (index, list), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy). That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain RESTful routes in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about RESTful routes in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of RESTful routes in Rails. RESTful routes map the HTTP verbs (GET/POST/PATCH/DELETE) to CRUD actions on a resource, so the same URL does different things by verb: GET /posts (index, list), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy). Rails gives you all of them with resources :posts in routes.rb, which generates the 7 standard routes and path helpers (posts_path, post_path(id)). REST keeps your API predictable: a resource, its URL, and the verb tell you the action. Nested resources (resources :posts do resources :comments end) give /posts/:post_id/comments for a child collection."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with RESTful routes in Rails is assuming the happy path is the whole rule. RESTful routes map the HTTP verbs (GET/POST/PATCH/DELETE) to CRUD actions on a resource, so the same URL does different things by verb: GET /posts (index, list), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy). Rails gives you all of them with resources :posts in routes.rb, which generates the 7 standard routes and path helpers (posts_path, post_path(id)). REST keeps your API predictable: a resource, its URL, and the verb tell you the action. Nested resources (resources :posts do resources :comments end) give /posts/:post_id/comments for a child collection. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "RESTful routes map the HTTP verbs (GET/POST/PATCH/DELETE) to CRUD actions on a resource, so the same URL does different things by verb: GET /posts (index, list), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy).\n\nRails gives you all of them with resources :posts in routes.rb, which generates the 7 standard routes and path helpers (posts_path, post_path(id)).\n\nREST keeps your API predictable: a resource, its URL, and the verb tell you the action. Nested resources (resources :posts do resources :comments end) give /posts/:post_id/comments for a child collection.\n\nHand-writing each route when resources :posts generates them, or using non-REST URLs (/list_posts, /create_post)."
          },
          {
            "type": "code_example",
            "title": "RESTful routes",
            "content": "```ruby\n# config/routes.rb\nRails.application.routes.draw do\n  resources :posts     # generates all 7 routes below\nend\n\n# The 7 routes `resources :posts` creates:\n#   Verb     URL              Action    Helper\n#   GET      /posts           index     posts_path\n#   GET      /posts/new       new       new_post_path\n#   POST     /posts           create    posts_path\n#   GET      /posts/:id       show      post_path(@post)\n#   GET      /posts/:id/edit  edit      edit_post_path(@post)\n#   PATCH    /posts/:id       update    post_path(@post)\n#   DELETE   /posts/:id       destroy   post_path(@post)\n\n# Nested resource (a post's comments):\nRails.application.routes.draw do\n  resources :posts do\n    resources :comments   # /posts/:post_id/comments\n  end\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "What are RESTful routes in Rails?",
        "When would you use Restful Routes in a real project?",
        "How would you compare Restful Routes with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Restful Routes — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Restful Routes? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "rails-api-basics-restful-routes-q003",
      "slug": "rails-api-basics-restful-routes-compare",
      "question": "How would you compare Restful Routes with an alternative?",
      "title": "compare Restful Routes with an alternative",
      "direct_answer": "I compare RESTful routes in Rails with the simpler alternative by looking at correctness first, then time, memory, and readability. RESTful routes map the HTTP verbs (GET/POST/PATCH/DELETE) to CRUD actions on a resource, so the same URL does different things by verb: GET /posts (index, list), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy). The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain RESTful routes in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about RESTful routes in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. RESTful routes map the HTTP verbs (GET/POST/PATCH/DELETE) to CRUD actions on a resource, so the same URL does different things by verb: GET /posts (index, list), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy). Rails gives you all of them with resources :posts in routes.rb, which generates the 7 standard routes and path helpers (posts_path, post_path(id)). REST keeps your API predictable: a resource, its URL, and the verb tell you the action. Nested resources (resources :posts do resources :comments end) give /posts/:post_id/comments for a child collection."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare RESTful routes in Rails with the straightforward alternative on the same example. RESTful routes map the HTTP verbs (GET/POST/PATCH/DELETE) to CRUD actions on a resource, so the same URL does different things by verb: GET /posts (index, list), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy). Rails gives you all of them with resources :posts in routes.rb, which generates the 7 standard routes and path helpers (posts_path, post_path(id)). REST keeps your API predictable: a resource, its URL, and the verb tell you the action. Nested resources (resources :posts do resources :comments end) give /posts/:post_id/comments for a child collection. If RESTful routes in Rails removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "RESTful routes map the HTTP verbs (GET/POST/PATCH/DELETE) to CRUD actions on a resource, so the same URL does different things by verb: GET /posts (index, list), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy).\n\nRails gives you all of them with resources :posts in routes.rb, which generates the 7 standard routes and path helpers (posts_path, post_path(id)).\n\nREST keeps your API predictable: a resource, its URL, and the verb tell you the action. Nested resources (resources :posts do resources :comments end) give /posts/:post_id/comments for a child collection.\n\nHand-writing each route when resources :posts generates them, or using non-REST URLs (/list_posts, /create_post)."
          },
          {
            "type": "code_example",
            "title": "RESTful routes",
            "content": "```ruby\n# config/routes.rb\nRails.application.routes.draw do\n  resources :posts     # generates all 7 routes below\nend\n\n# The 7 routes `resources :posts` creates:\n#   Verb     URL              Action    Helper\n#   GET      /posts           index     posts_path\n#   GET      /posts/new       new       new_post_path\n#   POST     /posts           create    posts_path\n#   GET      /posts/:id       show      post_path(@post)\n#   GET      /posts/:id/edit  edit      edit_post_path(@post)\n#   PATCH    /posts/:id       update    post_path(@post)\n#   DELETE   /posts/:id       destroy   post_path(@post)\n\n# Nested resource (a post's comments):\nRails.application.routes.draw do\n  resources :posts do\n    resources :comments   # /posts/:post_id/comments\n  end\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "What are RESTful routes in Rails?",
        "When would you use Restful Routes in a real project?",
        "What is a common mistake when using Restful Routes?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Restful Routes with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Restful Routes with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "rails-api-basics-restful-routes-q004",
      "slug": "rails-api-basics-restful-routes-scenario",
      "question": "How would you debug a problem involving Restful Routes?",
      "title": "debug a problem involving Restful Routes",
      "direct_answer": "I would debug a problem involving RESTful routes in Rails by reproducing it with the smallest input first. RESTful routes map the HTTP verbs (GET/POST/PATCH/DELETE) to CRUD actions on a resource, so the same URL does different things by verb: GET /posts (index, list), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy). Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain RESTful routes in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about RESTful routes in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what RESTful routes in Rails is supposed to guarantee. RESTful routes map the HTTP verbs (GET/POST/PATCH/DELETE) to CRUD actions on a resource, so the same URL does different things by verb: GET /posts (index, list), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy). Rails gives you all of them with resources :posts in routes.rb, which generates the 7 standard routes and path helpers (posts_path, post_path(id)). REST keeps your API predictable: a resource, its URL, and the verb tell you the action. Nested resources (resources :posts do resources :comments end) give /posts/:post_id/comments for a child collection."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the RESTful routes in Rails bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: RESTful routes map the HTTP verbs (GET/POST/PATCH/DELETE) to CRUD actions on a resource, so the same URL does different things by verb: GET /posts (index, list), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy). Rails gives you all of them with resources :posts in routes.rb, which generates the 7 standard routes and path helpers (posts_path, post_path(id)). REST keeps your API predictable: a resource, its URL, and the verb tell you the action. Nested resources (resources :posts do resources :comments end) give /posts/:post_id/comments for a child collection. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "RESTful routes map the HTTP verbs (GET/POST/PATCH/DELETE) to CRUD actions on a resource, so the same URL does different things by verb: GET /posts (index, list), POST /posts (create), GET /posts/:id (show), PATCH /posts/:id (update), DELETE /posts/:id (destroy).\n\nRails gives you all of them with resources :posts in routes.rb, which generates the 7 standard routes and path helpers (posts_path, post_path(id)).\n\nREST keeps your API predictable: a resource, its URL, and the verb tell you the action. Nested resources (resources :posts do resources :comments end) give /posts/:post_id/comments for a child collection.\n\nHand-writing each route when resources :posts generates them, or using non-REST URLs (/list_posts, /create_post)."
          },
          {
            "type": "code_example",
            "title": "RESTful routes",
            "content": "```ruby\n# config/routes.rb\nRails.application.routes.draw do\n  resources :posts     # generates all 7 routes below\nend\n\n# The 7 routes `resources :posts` creates:\n#   Verb     URL              Action    Helper\n#   GET      /posts           index     posts_path\n#   GET      /posts/new       new       new_post_path\n#   POST     /posts           create    posts_path\n#   GET      /posts/:id       show      post_path(@post)\n#   GET      /posts/:id/edit  edit      edit_post_path(@post)\n#   PATCH    /posts/:id       update    post_path(@post)\n#   DELETE   /posts/:id       destroy   post_path(@post)\n\n# Nested resource (a post's comments):\nRails.application.routes.draw do\n  resources :posts do\n    resources :comments   # /posts/:post_id/comments\n  end\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "What are RESTful routes in Rails?",
        "When would you use Restful Routes in a real project?",
        "How would you compare Restful Routes with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Restful Routes — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Restful Routes? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

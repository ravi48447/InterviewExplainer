{
  "topic": "HTTP Verbs and Status",
  "topicSlug": "http-verbs-and-status",
  "questions": [
    {
      "id": "http-verbs-and-status-q001",
      "slug": "rails-http-verbs-and-status-codes-basics",
      "question": "What HTTP verbs and status codes does a Rails API use?",
      "title": "HTTP Verbs and Status Codes in Rails",
      "direct_answer": "The verbs: GET (read, no side effects — index/show), POST (create — create), PUT/PATCH (update — update; PATCH for partial), DELETE (remove — destroy). Rails maps them to controller actions by REST convention. The status codes: 200 OK (success), 201 Created (after POST creates a resource), 204 No Content (after DELETE, no body), 400 Bad Request (client error), 401 Unauthorized (not logged in), 403 Forbidden (logged in but not allowed), 404 Not Found, 422 Unprocessable Entity (validation failure in Rails), 500 Server Error. You set a status with render json: x, status: :created. Using the right verb and status is the contract a well-behaved API keeps.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know the verbs (GET/POST/PUT-PATCH/DELETE) map to REST actions, and the common status codes (200/201/204/400/401/403/404/422/500).",
        "common_mistake": "Returning 200 for a create (use 201), or 404 for a validation failure (use 422 in Rails).",
        "to_stand_out": "Verbs: GET/POST/PUT-PATCH/DELETE map to index/create/update/destroy. Status: 200 OK, 201 Created, 204 No Content, 400/401/403/404, 422 (validation), 500."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define HTTP Verbs and Status Codes in Rails simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "Verbs: GET (read), POST (create), PUT/PATCH (update; PATCH for partial), DELETE (remove).",
              "Rails maps verbs to REST actions: index/show/create/update/destroy.",
              "Status codes: 200 OK, 201 Created, 204 No Content, 401/403/404, 422 (validation), 500.",
              "Set a status with render json: x, status: :created; head :no_content for 204.",
              "422 (not 404) is Rails' convention for a validation failure."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "The verbs: GET to read with no side effects (index, show), POST to create (create), PUT or PATCH to update (update; PATCH for partial), DELETE to remove (destroy). Rails maps them to controller actions by REST convention. The status codes: 200 OK for success, 201 Created after a POST creates a resource, 204 No Content after DELETE with no body, 400 Bad Request for a client error, 401 Unauthorized when not logged in, 403 Forbidden when logged in but not allowed, 404 Not Found, 422 Unprocessable Entity for a validation failure in Rails, and 500 for a server error. You set a status with render json x, status created. Using the right verb and status is the contract a well-behaved API keeps. A common mistake is returning 200 for a create, when you should use 201, and 404 for a validation failure, when Rails uses 422."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "The verbs: GET (read, no side effects — index/show), POST (create — create), PUT/PATCH (update — update; PATCH for partial), DELETE (remove — destroy).\n\nRails maps them to controller actions by REST convention.\n\nThe status codes: 200 OK (success), 201 Created (after POST creates a resource), 204 No Content (after DELETE, no body), 400 Bad Request (client error), 401 Unauthorized (not logged in), 403 Forbidden (logged in but not allowed), 404 Not Found, 422 Unprocessable Entity (validation failure in Rails), 500 Server Error. You set a status with render json: x, status: :created. Using the right verb and status is the contract a well-behaved API keeps.\n\nReturning 200 for a create (use 201), or 404 for a validation failure (use 422 in Rails)."
          },
          {
            "type": "flow_diagram",
            "title": "Verbs and Codes",
            "content": "GET/POST/PUT-PATCH/DELETE; 200/201/204/400/401/403/404/422/500.\n\n```mermaid\nflowchart LR\n  G[GET: index/show] --> P[POST: create -> 201]\n  P --> U[PATCH: update -> 200]\n  U --> D[DELETE: destroy -> 204]\n```"
          },
          {
            "type": "code_example",
            "title": "Verbs and status in Rails",
            "content": "```ruby\nmodule Api\n  class PostsController < ApplicationController\n    # GET /api/posts      -> index  (200)\n    def index; render json: Post.all; end\n\n    # GET /api/posts/:id  -> show   (200, or 404)\n    def show; render json: Post.find(params[:id]); end\n\n    # POST /api/posts     -> create (201)\n    def create\n      post = Post.create!(post_params)\n      render json: post, status: :created   # 201\n    end\n\n    # PATCH /api/posts/:id -> update (200, or 422 on validation fail)\n    def update\n      post = Post.find(params[:id])\n      post.update!(post_params)\n      render json: post                      # 200\n    end\n\n    # DELETE /api/posts/:id -> destroy (204, no body)\n    def destroy\n      Post.find(params[:id]).destroy\n      head :no_content                      # 204\n    end\n  end\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Http Verbs And Status in a real project?",
        "How would you compare Http Verbs And Status with an alternative?",
        "What is a common mistake when using Http Verbs And Status?"
      ],
      "seo": {
        "metaTitle": "HTTP Verbs and Status Codes in Rails — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What HTTP verbs and status codes does a Rails API use? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "rails-api-basics-http-verbs-and-status-q001",
      "slug": "rails-api-basics-http-verbs-and-status-when-to-use",
      "question": "When would you use Http Verbs And Status in a real project?",
      "title": "When would you use Http Verbs And Status in a real project",
      "direct_answer": "I would use What HTTP verbs and status codes does a Rails API use when the problem matches its main benefit. The verbs: GET (read, no side effects — index/show), POST (create — create), PUT/PATCH (update — update; PATCH for partial), DELETE (remove — destroy). In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain What HTTP verbs and status codes does a Rails API use in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about What HTTP verbs and status codes does a Rails API use without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining What HTTP verbs and status codes does a Rails API use in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. The verbs: GET (read, no side effects — index/show), POST (create — create), PUT/PATCH (update — update; PATCH for partial), DELETE (remove — destroy). Rails maps them to controller actions by REST convention. The status codes: 200 OK (success), 201 Created (after POST creates a resource), 204 No Content (after DELETE, no body), 400 Bad Request (client error), 401 Unauthorized (not logged in), 403 Forbidden (logged in but not allowed), 404 Not Found, 422 Unprocessable Entity (validation failure in Rails), 500 Server Error. You set a status with render json: x, status: :created. Using the right verb and status is the contract a well-behaved API keeps."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use What HTTP verbs and status codes does a Rails API use when its rule makes the code safer, clearer, or more efficient for the actual problem. The verbs: GET (read, no side effects — index/show), POST (create — create), PUT/PATCH (update — update; PATCH for partial), DELETE (remove — destroy). Rails maps them to controller actions by REST convention. The status codes: 200 OK (success), 201 Created (after POST creates a resource), 204 No Content (after DELETE, no body), 400 Bad Request (client error), 401 Unauthorized (not logged in), 403 Forbidden (logged in but not allowed), 404 Not Found, 422 Unprocessable Entity (validation failure in Rails), 500 Server Error. You set a status with render json: x, status: :created. Using the right verb and status is the contract a well-behaved API keeps. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "The verbs: GET (read, no side effects — index/show), POST (create — create), PUT/PATCH (update — update; PATCH for partial), DELETE (remove — destroy).\n\nRails maps them to controller actions by REST convention.\n\nThe status codes: 200 OK (success), 201 Created (after POST creates a resource), 204 No Content (after DELETE, no body), 400 Bad Request (client error), 401 Unauthorized (not logged in), 403 Forbidden (logged in but not allowed), 404 Not Found, 422 Unprocessable Entity (validation failure in Rails), 500 Server Error. You set a status with render json: x, status: :created. Using the right verb and status is the contract a well-behaved API keeps.\n\nReturning 200 for a create (use 201), or 404 for a validation failure (use 422 in Rails)."
          },
          {
            "type": "code_example",
            "title": "Verbs and status in Rails",
            "content": "```ruby\nmodule Api\n  class PostsController < ApplicationController\n    # GET /api/posts      -> index  (200)\n    def index; render json: Post.all; end\n\n    # GET /api/posts/:id  -> show   (200, or 404)\n    def show; render json: Post.find(params[:id]); end\n\n    # POST /api/posts     -> create (201)\n    def create\n      post = Post.create!(post_params)\n      render json: post, status: :created   # 201\n    end\n\n    # PATCH /api/posts/:id -> update (200, or 422 on validation fail)\n    def update\n      post = Post.find(params[:id])\n      post.update!(post_params)\n      render json: post                      # 200\n    end\n\n    # DELETE /api/posts/:id -> destroy (204, no body)\n    def destroy\n      Post.find(params[:id]).destroy\n      head :no_content                      # 204\n    end\n  end\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "What HTTP verbs and status codes does a Rails API use?",
        "How would you compare Http Verbs And Status with an alternative?",
        "What is a common mistake when using Http Verbs And Status?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Http Verbs And Status in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Http Verbs And Status in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "rails-api-basics-http-verbs-and-status-q002",
      "slug": "rails-api-basics-http-verbs-and-status-common-mistake",
      "question": "What is a common mistake when using Http Verbs And Status?",
      "title": "a common mistake when using Http Verbs And Status",
      "direct_answer": "A common mistake with What HTTP verbs and status codes does a Rails API use is using the rule without checking its boundary conditions. The verbs: GET (read, no side effects — index/show), POST (create — create), PUT/PATCH (update — update; PATCH for partial), DELETE (remove — destroy). That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain What HTTP verbs and status codes does a Rails API use in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about What HTTP verbs and status codes does a Rails API use without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of What HTTP verbs and status codes does a Rails API use. The verbs: GET (read, no side effects — index/show), POST (create — create), PUT/PATCH (update — update; PATCH for partial), DELETE (remove — destroy). Rails maps them to controller actions by REST convention. The status codes: 200 OK (success), 201 Created (after POST creates a resource), 204 No Content (after DELETE, no body), 400 Bad Request (client error), 401 Unauthorized (not logged in), 403 Forbidden (logged in but not allowed), 404 Not Found, 422 Unprocessable Entity (validation failure in Rails), 500 Server Error. You set a status with render json: x, status: :created. Using the right verb and status is the contract a well-behaved API keeps."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with What HTTP verbs and status codes does a Rails API use is assuming the happy path is the whole rule. The verbs: GET (read, no side effects — index/show), POST (create — create), PUT/PATCH (update — update; PATCH for partial), DELETE (remove — destroy). Rails maps them to controller actions by REST convention. The status codes: 200 OK (success), 201 Created (after POST creates a resource), 204 No Content (after DELETE, no body), 400 Bad Request (client error), 401 Unauthorized (not logged in), 403 Forbidden (logged in but not allowed), 404 Not Found, 422 Unprocessable Entity (validation failure in Rails), 500 Server Error. You set a status with render json: x, status: :created. Using the right verb and status is the contract a well-behaved API keeps. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "The verbs: GET (read, no side effects — index/show), POST (create — create), PUT/PATCH (update — update; PATCH for partial), DELETE (remove — destroy).\n\nRails maps them to controller actions by REST convention.\n\nThe status codes: 200 OK (success), 201 Created (after POST creates a resource), 204 No Content (after DELETE, no body), 400 Bad Request (client error), 401 Unauthorized (not logged in), 403 Forbidden (logged in but not allowed), 404 Not Found, 422 Unprocessable Entity (validation failure in Rails), 500 Server Error. You set a status with render json: x, status: :created. Using the right verb and status is the contract a well-behaved API keeps.\n\nReturning 200 for a create (use 201), or 404 for a validation failure (use 422 in Rails)."
          },
          {
            "type": "code_example",
            "title": "Verbs and status in Rails",
            "content": "```ruby\nmodule Api\n  class PostsController < ApplicationController\n    # GET /api/posts      -> index  (200)\n    def index; render json: Post.all; end\n\n    # GET /api/posts/:id  -> show   (200, or 404)\n    def show; render json: Post.find(params[:id]); end\n\n    # POST /api/posts     -> create (201)\n    def create\n      post = Post.create!(post_params)\n      render json: post, status: :created   # 201\n    end\n\n    # PATCH /api/posts/:id -> update (200, or 422 on validation fail)\n    def update\n      post = Post.find(params[:id])\n      post.update!(post_params)\n      render json: post                      # 200\n    end\n\n    # DELETE /api/posts/:id -> destroy (204, no body)\n    def destroy\n      Post.find(params[:id]).destroy\n      head :no_content                      # 204\n    end\n  end\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "What HTTP verbs and status codes does a Rails API use?",
        "When would you use Http Verbs And Status in a real project?",
        "How would you compare Http Verbs And Status with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Http Verbs And Status — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Http Verbs And Status? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "rails-api-basics-http-verbs-and-status-q003",
      "slug": "rails-api-basics-http-verbs-and-status-compare",
      "question": "How would you compare Http Verbs And Status with an alternative?",
      "title": "compare Http Verbs And Status with an alternative",
      "direct_answer": "I compare What HTTP verbs and status codes does a Rails API use with the simpler alternative by looking at correctness first, then time, memory, and readability. The verbs: GET (read, no side effects — index/show), POST (create — create), PUT/PATCH (update — update; PATCH for partial), DELETE (remove — destroy). The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain What HTTP verbs and status codes does a Rails API use in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about What HTTP verbs and status codes does a Rails API use without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. The verbs: GET (read, no side effects — index/show), POST (create — create), PUT/PATCH (update — update; PATCH for partial), DELETE (remove — destroy). Rails maps them to controller actions by REST convention. The status codes: 200 OK (success), 201 Created (after POST creates a resource), 204 No Content (after DELETE, no body), 400 Bad Request (client error), 401 Unauthorized (not logged in), 403 Forbidden (logged in but not allowed), 404 Not Found, 422 Unprocessable Entity (validation failure in Rails), 500 Server Error. You set a status with render json: x, status: :created. Using the right verb and status is the contract a well-behaved API keeps."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare What HTTP verbs and status codes does a Rails API use with the straightforward alternative on the same example. The verbs: GET (read, no side effects — index/show), POST (create — create), PUT/PATCH (update — update; PATCH for partial), DELETE (remove — destroy). Rails maps them to controller actions by REST convention. The status codes: 200 OK (success), 201 Created (after POST creates a resource), 204 No Content (after DELETE, no body), 400 Bad Request (client error), 401 Unauthorized (not logged in), 403 Forbidden (logged in but not allowed), 404 Not Found, 422 Unprocessable Entity (validation failure in Rails), 500 Server Error. You set a status with render json: x, status: :created. Using the right verb and status is the contract a well-behaved API keeps. If What HTTP verbs and status codes does a Rails API use removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "The verbs: GET (read, no side effects — index/show), POST (create — create), PUT/PATCH (update — update; PATCH for partial), DELETE (remove — destroy).\n\nRails maps them to controller actions by REST convention.\n\nThe status codes: 200 OK (success), 201 Created (after POST creates a resource), 204 No Content (after DELETE, no body), 400 Bad Request (client error), 401 Unauthorized (not logged in), 403 Forbidden (logged in but not allowed), 404 Not Found, 422 Unprocessable Entity (validation failure in Rails), 500 Server Error. You set a status with render json: x, status: :created. Using the right verb and status is the contract a well-behaved API keeps.\n\nReturning 200 for a create (use 201), or 404 for a validation failure (use 422 in Rails)."
          },
          {
            "type": "code_example",
            "title": "Verbs and status in Rails",
            "content": "```ruby\nmodule Api\n  class PostsController < ApplicationController\n    # GET /api/posts      -> index  (200)\n    def index; render json: Post.all; end\n\n    # GET /api/posts/:id  -> show   (200, or 404)\n    def show; render json: Post.find(params[:id]); end\n\n    # POST /api/posts     -> create (201)\n    def create\n      post = Post.create!(post_params)\n      render json: post, status: :created   # 201\n    end\n\n    # PATCH /api/posts/:id -> update (200, or 422 on validation fail)\n    def update\n      post = Post.find(params[:id])\n      post.update!(post_params)\n      render json: post                      # 200\n    end\n\n    # DELETE /api/posts/:id -> destroy (204, no body)\n    def destroy\n      Post.find(params[:id]).destroy\n      head :no_content                      # 204\n    end\n  end\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "What HTTP verbs and status codes does a Rails API use?",
        "When would you use Http Verbs And Status in a real project?",
        "What is a common mistake when using Http Verbs And Status?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Http Verbs And Status with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Http Verbs And Status with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "rails-api-basics-http-verbs-and-status-q004",
      "slug": "rails-api-basics-http-verbs-and-status-scenario",
      "question": "How would you debug a problem involving Http Verbs And Status?",
      "title": "debug a problem involving Http Verbs And Status",
      "direct_answer": "I would debug a problem involving What HTTP verbs and status codes does a Rails API use by reproducing it with the smallest input first. The verbs: GET (read, no side effects — index/show), POST (create — create), PUT/PATCH (update — update; PATCH for partial), DELETE (remove — destroy). Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain What HTTP verbs and status codes does a Rails API use in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about What HTTP verbs and status codes does a Rails API use without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what What HTTP verbs and status codes does a Rails API use is supposed to guarantee. The verbs: GET (read, no side effects — index/show), POST (create — create), PUT/PATCH (update — update; PATCH for partial), DELETE (remove — destroy). Rails maps them to controller actions by REST convention. The status codes: 200 OK (success), 201 Created (after POST creates a resource), 204 No Content (after DELETE, no body), 400 Bad Request (client error), 401 Unauthorized (not logged in), 403 Forbidden (logged in but not allowed), 404 Not Found, 422 Unprocessable Entity (validation failure in Rails), 500 Server Error. You set a status with render json: x, status: :created. Using the right verb and status is the contract a well-behaved API keeps."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the What HTTP verbs and status codes does a Rails API use bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: The verbs: GET (read, no side effects — index/show), POST (create — create), PUT/PATCH (update — update; PATCH for partial), DELETE (remove — destroy). Rails maps them to controller actions by REST convention. The status codes: 200 OK (success), 201 Created (after POST creates a resource), 204 No Content (after DELETE, no body), 400 Bad Request (client error), 401 Unauthorized (not logged in), 403 Forbidden (logged in but not allowed), 404 Not Found, 422 Unprocessable Entity (validation failure in Rails), 500 Server Error. You set a status with render json: x, status: :created. Using the right verb and status is the contract a well-behaved API keeps. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "The verbs: GET (read, no side effects — index/show), POST (create — create), PUT/PATCH (update — update; PATCH for partial), DELETE (remove — destroy).\n\nRails maps them to controller actions by REST convention.\n\nThe status codes: 200 OK (success), 201 Created (after POST creates a resource), 204 No Content (after DELETE, no body), 400 Bad Request (client error), 401 Unauthorized (not logged in), 403 Forbidden (logged in but not allowed), 404 Not Found, 422 Unprocessable Entity (validation failure in Rails), 500 Server Error. You set a status with render json: x, status: :created. Using the right verb and status is the contract a well-behaved API keeps.\n\nReturning 200 for a create (use 201), or 404 for a validation failure (use 422 in Rails)."
          },
          {
            "type": "code_example",
            "title": "Verbs and status in Rails",
            "content": "```ruby\nmodule Api\n  class PostsController < ApplicationController\n    # GET /api/posts      -> index  (200)\n    def index; render json: Post.all; end\n\n    # GET /api/posts/:id  -> show   (200, or 404)\n    def show; render json: Post.find(params[:id]); end\n\n    # POST /api/posts     -> create (201)\n    def create\n      post = Post.create!(post_params)\n      render json: post, status: :created   # 201\n    end\n\n    # PATCH /api/posts/:id -> update (200, or 422 on validation fail)\n    def update\n      post = Post.find(params[:id])\n      post.update!(post_params)\n      render json: post                      # 200\n    end\n\n    # DELETE /api/posts/:id -> destroy (204, no body)\n    def destroy\n      Post.find(params[:id]).destroy\n      head :no_content                      # 204\n    end\n  end\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "What HTTP verbs and status codes does a Rails API use?",
        "When would you use Http Verbs And Status in a real project?",
        "How would you compare Http Verbs And Status with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Http Verbs And Status — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Http Verbs And Status? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

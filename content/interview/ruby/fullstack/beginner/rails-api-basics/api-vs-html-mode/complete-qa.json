{
  "topic": "API vs HTML Mode",
  "topicSlug": "api-vs-html-mode",
  "questions": [
    {
      "id": "api-vs-html-mode-q001",
      "slug": "rails-api-vs-html-mode-basics",
      "question": "What's the difference between API mode and HTML mode in Rails?",
      "title": "Rails API vs HTML Mode",
      "direct_answer": "A full Rails app renders HTML views (it has ActionView, layouts, partials, asset compilation) for a server-rendered site. An API-only app (rails new app --api) strips out the view layer — no ActionView, no layouts, no assets — so it's lighter and returns JSON by default. You use API mode when the frontend is separate (a React/Vue SPA or a mobile app) and Rails is a JSON backend; you use full mode when Rails renders the HTML (server-rendered, with ERB/Haml). API mode still gives you the full model and controller stack; it just drops the view/asset machinery.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know --api strips the view/asset layer (JSON-only, lighter) and when to use each (separate frontend vs server-rendered).",
        "common_mistake": "Building a full app when the frontend is separate (unnecessary view/asset machinery), or expecting render :new in API mode (no ActionView).",
        "to_stand_out": "--api strips the view/asset layer (JSON-only, lighter). Use it for a separate frontend (SPA/mobile); use full mode when Rails renders HTML."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define Rails API vs HTML Mode simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "--api strips the view/asset layer; it returns JSON by default (lighter).",
              "Full mode has ActionView, layouts, assets, and renders HTML (server-rendered).",
              "Use API mode for a separate frontend (React/Vue SPA, mobile); full for server-rendered.",
              "API mode keeps the full model + controller stack — only views/assets are dropped.",
              "rails new app --api creates an API-only app; rails new app creates a full one."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "A full Rails app renders HTML views — it has ActionView, layouts, partials, and asset compilation — for a server-rendered site. An API-only app, created with rails new app dash dash api, strips out the view layer: no ActionView, no layouts, no assets, so it's lighter and returns JSON by default. You use API mode when the frontend is separate — a React or Vue SPA or a mobile app — and Rails is a JSON backend; you use full mode when Rails renders the HTML, server-rendered, with ERB or Haml. API mode still gives you the full model and controller stack; it just drops the view and asset machinery you don't need for a pure JSON API. A common mistake is building a full app when the frontend is separate, with unnecessary view and asset machinery, and expecting render of new or templates in API mode, when there's no ActionView."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "A full Rails app renders HTML views (it has ActionView, layouts, partials, asset compilation) for a server-rendered site.\n\nAn API-only app (rails new app --api) strips out the view layer — no ActionView, no layouts, no assets — so it's lighter and returns JSON by default.\n\nYou use API mode when the frontend is separate (a React/Vue SPA or a mobile app) and Rails is a JSON backend; you use full mode when Rails renders the HTML (server-rendered, with ERB/Haml). API mode still gives you the full model and controller stack; it just drops the view/asset machinery.\n\nBuilding a full app when the frontend is separate (unnecessary view/asset machinery), or expecting render :new in API mode (no ActionView)."
          },
          {
            "type": "flow_diagram",
            "title": "Full vs API-only",
            "content": "Full: views + assets + HTML; --api: JSON only, no views/assets.\n\n```mermaid\nflowchart LR\n  F[Full Rails: ERB + assets] --> H[server-rendered HTML]\n  A[--api: no views/assets] --> J[JSON only]\n```"
          },
          {
            "type": "code_example",
            "title": "API vs full mode",
            "content": "```ruby\n# Create an API-only app (no views, no assets, JSON by default):\n#   rails new backend --api\n\n# API controller returns JSON; no render :new / templates\nmodule Api\n  class PostsController < ApplicationController\n    def index\n      render json: Post.all\n    end\n  end\nend\n\n# A full app (rails new blog) renders HTML:\nclass PostsController < ApplicationController\n  def index\n    @posts = Post.all\n    render :index        # app/views/posts/index.html.erb\n  end\nend\n\n# Choose:\n#   --api      -> separate frontend (React/Vue SPA, mobile); Rails is JSON only\n#   full       -> Rails renders HTML (server-rendered, ERB/Haml)\n# API mode still has the full model + controller stack.\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Api Vs Html Mode in a real project?",
        "What is a common mistake when using Api Vs Html Mode?",
        "How would you compare Api Vs Html Mode with an alternative?"
      ],
      "seo": {
        "metaTitle": "Rails API vs HTML Mode — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What's the difference between API mode and HTML mode in Rails? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "rails-api-basics-api-vs-html-mode-q001",
      "slug": "rails-api-basics-api-vs-html-mode-when-to-use",
      "question": "When would you use Api Vs Html Mode in a real project?",
      "title": "When would you use Api Vs Html Mode in a real project",
      "direct_answer": "I would use What's the difference between API mode and HTML mode in Rails when the problem matches its main benefit. A full Rails app renders HTML views (it has ActionView, layouts, partials, asset compilation) for a server-rendered site. In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain What's the difference between API mode and HTML mode in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about What's the difference between API mode and HTML mode in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining What's the difference between API mode and HTML mode in Rails in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. A full Rails app renders HTML views (it has ActionView, layouts, partials, asset compilation) for a server-rendered site. An API-only app (rails new app --api) strips out the view layer — no ActionView, no layouts, no assets — so it's lighter and returns JSON by default. You use API mode when the frontend is separate (a React/Vue SPA or a mobile app) and Rails is a JSON backend; you use full mode when Rails renders the HTML (server-rendered, with ERB/Haml). API mode still gives you the full model and controller stack; it just drops the view/asset machinery."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use What's the difference between API mode and HTML mode in Rails when its rule makes the code safer, clearer, or more efficient for the actual problem. A full Rails app renders HTML views (it has ActionView, layouts, partials, asset compilation) for a server-rendered site. An API-only app (rails new app --api) strips out the view layer — no ActionView, no layouts, no assets — so it's lighter and returns JSON by default. You use API mode when the frontend is separate (a React/Vue SPA or a mobile app) and Rails is a JSON backend; you use full mode when Rails renders the HTML (server-rendered, with ERB/Haml). API mode still gives you the full model and controller stack; it just drops the view/asset machinery. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "A full Rails app renders HTML views (it has ActionView, layouts, partials, asset compilation) for a server-rendered site.\n\nAn API-only app (rails new app --api) strips out the view layer — no ActionView, no layouts, no assets — so it's lighter and returns JSON by default.\n\nYou use API mode when the frontend is separate (a React/Vue SPA or a mobile app) and Rails is a JSON backend; you use full mode when Rails renders the HTML (server-rendered, with ERB/Haml). API mode still gives you the full model and controller stack; it just drops the view/asset machinery.\n\nBuilding a full app when the frontend is separate (unnecessary view/asset machinery), or expecting render :new in API mode (no ActionView)."
          },
          {
            "type": "code_example",
            "title": "API vs full mode",
            "content": "```ruby\n# Create an API-only app (no views, no assets, JSON by default):\n#   rails new backend --api\n\n# API controller returns JSON; no render :new / templates\nmodule Api\n  class PostsController < ApplicationController\n    def index\n      render json: Post.all\n    end\n  end\nend\n\n# A full app (rails new blog) renders HTML:\nclass PostsController < ApplicationController\n  def index\n    @posts = Post.all\n    render :index        # app/views/posts/index.html.erb\n  end\nend\n\n# Choose:\n#   --api      -> separate frontend (React/Vue SPA, mobile); Rails is JSON only\n#   full       -> Rails renders HTML (server-rendered, ERB/Haml)\n# API mode still has the full model + controller stack.\n```"
          }
        ]
      },
      "followup_questions": [
        "What's the difference between API mode and HTML mode in Rails?",
        "What is a common mistake when using Api Vs Html Mode?",
        "How would you compare Api Vs Html Mode with an alternative?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Api Vs Html Mode in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Api Vs Html Mode in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "rails-api-basics-api-vs-html-mode-q002",
      "slug": "rails-api-basics-api-vs-html-mode-common-mistake",
      "question": "What is a common mistake when using Api Vs Html Mode?",
      "title": "a common mistake when using Api Vs Html Mode",
      "direct_answer": "A common mistake with What's the difference between API mode and HTML mode in Rails is using the rule without checking its boundary conditions. A full Rails app renders HTML views (it has ActionView, layouts, partials, asset compilation) for a server-rendered site. That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain What's the difference between API mode and HTML mode in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about What's the difference between API mode and HTML mode in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of What's the difference between API mode and HTML mode in Rails. A full Rails app renders HTML views (it has ActionView, layouts, partials, asset compilation) for a server-rendered site. An API-only app (rails new app --api) strips out the view layer — no ActionView, no layouts, no assets — so it's lighter and returns JSON by default. You use API mode when the frontend is separate (a React/Vue SPA or a mobile app) and Rails is a JSON backend; you use full mode when Rails renders the HTML (server-rendered, with ERB/Haml). API mode still gives you the full model and controller stack; it just drops the view/asset machinery."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with What's the difference between API mode and HTML mode in Rails is assuming the happy path is the whole rule. A full Rails app renders HTML views (it has ActionView, layouts, partials, asset compilation) for a server-rendered site. An API-only app (rails new app --api) strips out the view layer — no ActionView, no layouts, no assets — so it's lighter and returns JSON by default. You use API mode when the frontend is separate (a React/Vue SPA or a mobile app) and Rails is a JSON backend; you use full mode when Rails renders the HTML (server-rendered, with ERB/Haml). API mode still gives you the full model and controller stack; it just drops the view/asset machinery. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "A full Rails app renders HTML views (it has ActionView, layouts, partials, asset compilation) for a server-rendered site.\n\nAn API-only app (rails new app --api) strips out the view layer — no ActionView, no layouts, no assets — so it's lighter and returns JSON by default.\n\nYou use API mode when the frontend is separate (a React/Vue SPA or a mobile app) and Rails is a JSON backend; you use full mode when Rails renders the HTML (server-rendered, with ERB/Haml). API mode still gives you the full model and controller stack; it just drops the view/asset machinery.\n\nBuilding a full app when the frontend is separate (unnecessary view/asset machinery), or expecting render :new in API mode (no ActionView)."
          },
          {
            "type": "code_example",
            "title": "API vs full mode",
            "content": "```ruby\n# Create an API-only app (no views, no assets, JSON by default):\n#   rails new backend --api\n\n# API controller returns JSON; no render :new / templates\nmodule Api\n  class PostsController < ApplicationController\n    def index\n      render json: Post.all\n    end\n  end\nend\n\n# A full app (rails new blog) renders HTML:\nclass PostsController < ApplicationController\n  def index\n    @posts = Post.all\n    render :index        # app/views/posts/index.html.erb\n  end\nend\n\n# Choose:\n#   --api      -> separate frontend (React/Vue SPA, mobile); Rails is JSON only\n#   full       -> Rails renders HTML (server-rendered, ERB/Haml)\n# API mode still has the full model + controller stack.\n```"
          }
        ]
      },
      "followup_questions": [
        "What's the difference between API mode and HTML mode in Rails?",
        "When would you use Api Vs Html Mode in a real project?",
        "How would you compare Api Vs Html Mode with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Api Vs Html Mode — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Api Vs Html Mode? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "rails-api-basics-api-vs-html-mode-q003",
      "slug": "rails-api-basics-api-vs-html-mode-compare",
      "question": "How would you compare Api Vs Html Mode with an alternative?",
      "title": "compare Api Vs Html Mode with an alternative",
      "direct_answer": "I compare What's the difference between API mode and HTML mode in Rails with the simpler alternative by looking at correctness first, then time, memory, and readability. A full Rails app renders HTML views (it has ActionView, layouts, partials, asset compilation) for a server-rendered site. The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain What's the difference between API mode and HTML mode in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about What's the difference between API mode and HTML mode in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. A full Rails app renders HTML views (it has ActionView, layouts, partials, asset compilation) for a server-rendered site. An API-only app (rails new app --api) strips out the view layer — no ActionView, no layouts, no assets — so it's lighter and returns JSON by default. You use API mode when the frontend is separate (a React/Vue SPA or a mobile app) and Rails is a JSON backend; you use full mode when Rails renders the HTML (server-rendered, with ERB/Haml). API mode still gives you the full model and controller stack; it just drops the view/asset machinery."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare What's the difference between API mode and HTML mode in Rails with the straightforward alternative on the same example. A full Rails app renders HTML views (it has ActionView, layouts, partials, asset compilation) for a server-rendered site. An API-only app (rails new app --api) strips out the view layer — no ActionView, no layouts, no assets — so it's lighter and returns JSON by default. You use API mode when the frontend is separate (a React/Vue SPA or a mobile app) and Rails is a JSON backend; you use full mode when Rails renders the HTML (server-rendered, with ERB/Haml). API mode still gives you the full model and controller stack; it just drops the view/asset machinery. If What's the difference between API mode and HTML mode in Rails removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "A full Rails app renders HTML views (it has ActionView, layouts, partials, asset compilation) for a server-rendered site.\n\nAn API-only app (rails new app --api) strips out the view layer — no ActionView, no layouts, no assets — so it's lighter and returns JSON by default.\n\nYou use API mode when the frontend is separate (a React/Vue SPA or a mobile app) and Rails is a JSON backend; you use full mode when Rails renders the HTML (server-rendered, with ERB/Haml). API mode still gives you the full model and controller stack; it just drops the view/asset machinery.\n\nBuilding a full app when the frontend is separate (unnecessary view/asset machinery), or expecting render :new in API mode (no ActionView)."
          },
          {
            "type": "code_example",
            "title": "API vs full mode",
            "content": "```ruby\n# Create an API-only app (no views, no assets, JSON by default):\n#   rails new backend --api\n\n# API controller returns JSON; no render :new / templates\nmodule Api\n  class PostsController < ApplicationController\n    def index\n      render json: Post.all\n    end\n  end\nend\n\n# A full app (rails new blog) renders HTML:\nclass PostsController < ApplicationController\n  def index\n    @posts = Post.all\n    render :index        # app/views/posts/index.html.erb\n  end\nend\n\n# Choose:\n#   --api      -> separate frontend (React/Vue SPA, mobile); Rails is JSON only\n#   full       -> Rails renders HTML (server-rendered, ERB/Haml)\n# API mode still has the full model + controller stack.\n```"
          }
        ]
      },
      "followup_questions": [
        "What's the difference between API mode and HTML mode in Rails?",
        "When would you use Api Vs Html Mode in a real project?",
        "What is a common mistake when using Api Vs Html Mode?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Api Vs Html Mode with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Api Vs Html Mode with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "rails-api-basics-api-vs-html-mode-q004",
      "slug": "rails-api-basics-api-vs-html-mode-scenario",
      "question": "How would you debug a problem involving Api Vs Html Mode?",
      "title": "debug a problem involving Api Vs Html Mode",
      "direct_answer": "I would debug a problem involving What's the difference between API mode and HTML mode in Rails by reproducing it with the smallest input first. A full Rails app renders HTML views (it has ActionView, layouts, partials, asset compilation) for a server-rendered site. Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain What's the difference between API mode and HTML mode in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about What's the difference between API mode and HTML mode in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what What's the difference between API mode and HTML mode in Rails is supposed to guarantee. A full Rails app renders HTML views (it has ActionView, layouts, partials, asset compilation) for a server-rendered site. An API-only app (rails new app --api) strips out the view layer — no ActionView, no layouts, no assets — so it's lighter and returns JSON by default. You use API mode when the frontend is separate (a React/Vue SPA or a mobile app) and Rails is a JSON backend; you use full mode when Rails renders the HTML (server-rendered, with ERB/Haml). API mode still gives you the full model and controller stack; it just drops the view/asset machinery."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the What's the difference between API mode and HTML mode in Rails bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: A full Rails app renders HTML views (it has ActionView, layouts, partials, asset compilation) for a server-rendered site. An API-only app (rails new app --api) strips out the view layer — no ActionView, no layouts, no assets — so it's lighter and returns JSON by default. You use API mode when the frontend is separate (a React/Vue SPA or a mobile app) and Rails is a JSON backend; you use full mode when Rails renders the HTML (server-rendered, with ERB/Haml). API mode still gives you the full model and controller stack; it just drops the view/asset machinery. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "A full Rails app renders HTML views (it has ActionView, layouts, partials, asset compilation) for a server-rendered site.\n\nAn API-only app (rails new app --api) strips out the view layer — no ActionView, no layouts, no assets — so it's lighter and returns JSON by default.\n\nYou use API mode when the frontend is separate (a React/Vue SPA or a mobile app) and Rails is a JSON backend; you use full mode when Rails renders the HTML (server-rendered, with ERB/Haml). API mode still gives you the full model and controller stack; it just drops the view/asset machinery.\n\nBuilding a full app when the frontend is separate (unnecessary view/asset machinery), or expecting render :new in API mode (no ActionView)."
          },
          {
            "type": "code_example",
            "title": "API vs full mode",
            "content": "```ruby\n# Create an API-only app (no views, no assets, JSON by default):\n#   rails new backend --api\n\n# API controller returns JSON; no render :new / templates\nmodule Api\n  class PostsController < ApplicationController\n    def index\n      render json: Post.all\n    end\n  end\nend\n\n# A full app (rails new blog) renders HTML:\nclass PostsController < ApplicationController\n  def index\n    @posts = Post.all\n    render :index        # app/views/posts/index.html.erb\n  end\nend\n\n# Choose:\n#   --api      -> separate frontend (React/Vue SPA, mobile); Rails is JSON only\n#   full       -> Rails renders HTML (server-rendered, ERB/Haml)\n# API mode still has the full model + controller stack.\n```"
          }
        ]
      },
      "followup_questions": [
        "What's the difference between API mode and HTML mode in Rails?",
        "When would you use Api Vs Html Mode in a real project?",
        "What is a common mistake when using Api Vs Html Mode?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Api Vs Html Mode — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Api Vs Html Mode? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

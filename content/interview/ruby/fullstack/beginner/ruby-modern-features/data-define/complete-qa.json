{
  "topic": "Data Define",
  "topicSlug": "data-define",
  "questions": [
    {
      "id": "data-define-q001",
      "slug": "ruby-data-define-basics",
      "question": "What is the Data class (define) in Ruby?",
      "title": "The Data Class (define)",
      "direct_answer": "Data (Ruby 3.2+) defines immutable value objects: `Point = Data.define(:x, :y)` creates a class with a generated initialize and #== based on values (like Struct, but immutable and value-equal). p = Point.new(x: 1, y: 2) (or positional Point.new(1, 2)); p.x == 1; p == Point.new(1, 2) # true (value equality). Unlike Struct, Data objects are immutable — you can't reassign p.x = 5 (raises). They're perfect for value objects (points, money, config snapshots) where equality should be by value, not identity. Data.define(:x, :y) also accepts a block to add methods. Use Data over Struct when you want immutability and value equality; use Struct when you need mutation or a plain mutable record. Great for thread-safe, hashable, comparable value objects.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know Data.define (Ruby 3.2+) creates immutable value objects with value equality, and how it differs from Struct.",
        "common_mistake": "Trying to mutate a Data object (raises — they're immutable), or using Struct when you want value equality + immutability.",
        "to_stand_out": "Data.define(:x,:y) (Ruby 3.2+) -> immutable value objects; #== by value. Point.new(x:1,y:2). Use over Struct for immutability + value equality."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define The Data Class (define) simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "Data.define(:x, :y) (Ruby 3.2+) creates immutable value objects with value equality.",
              "Point.new(x: 1, y: 2) or positional Point.new(1, 2); p.x; p == Point.new(1,2) # true.",
              "Unlike Struct, Data objects are immutable — p.x = 5 raises.",
              "Data.define accepts a block to add methods; objects are hashable (use as keys).",
              "Use Data over Struct for immutability + value equality; Struct for mutable records."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "Data (Ruby 3.2 plus) defines immutable value objects: Point equals Data dot define x y creates a class with a generated initialize and equals equals based on values (like Struct, but immutable and value-equal). p Point dot new x 1 y 2 (or positional Point dot new 1 2); p dot x equals 1; p equals Point dot new 1 2 true (value equality). Unlike Struct, Data objects are immutable — you can't reassign p dot x 5 (raises). They're perfect for value objects (points, money, config snapshots) where equality should be by value, not identity. Data dot define x y also accepts a block to add methods. Use Data over Struct when you want immutability and value equality; use Struct when you need mutation or a plain mutable record. Great for thread-safe, hashable, comparable value objects. A common mistake is trying to mutate a Data object, which raises since they're immutable, and using Struct when you want value equality plus immutability."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Data (Ruby 3.2+) defines immutable value objects: `Point = Data.define(:x, :y)` creates a class with a generated initialize and #== based on values (like Struct, but immutable and value-equal).\n\np = Point.new(x: 1, y: 2) (or positional Point.new(1, 2)); p.x == 1; p == Point.new(1, 2) # true (value equality).\n\nUnlike Struct, Data objects are immutable — you can't reassign p.x = 5 (raises). They're perfect for value objects (points, money, config snapshots) where equality should be by value, not identity. Data.define(:x, :y) also accepts a block to add methods. Use Data over Struct when you want immutability and value equality; use Struct when you need mutation or a plain mutable record. Great for thread-safe, hashable, comparable value objects."
          },
          {
            "type": "flow_diagram",
            "title": "Data.define",
            "content": "immutable value objects; #== by value. Point.new(x:1,y:2). vs Struct (mutable).\n\n```mermaid\nflowchart LR\n  D[Data.define :x :y] --> C[class Point]\n  C --> N[Point.new x:1 y:2]\n  N --> I[immutable: p.x= raises]\n  N --> E[p == Point.new(1,2): value eq]\n```"
          },
          {
            "type": "code_example",
            "title": "Data.define",
            "content": "```ruby\n# Ruby 3.2+: define an immutable value object\nPoint = Data.define(:x, :y)\n\np = Point.new(x: 1, y: 2)   # keyword args\np = Point.new(1, 2)        # positional also works\n\np.x            # 1\np.y            # 2\np == Point.new(1, 2)  # true  (VALUE equality, like Struct)\n\n# IMMUTABLE (unlike Struct):\n# p.x = 5            # raises NoMethodError (no setter)\n\n# With a block (add methods):\nMoney = Data.define(:amount, :currency) do\n  def to_s; \"#{amount} #{currency}\"; end\nend\nm = Money.new(100, 'USD')\nm.to_s         # '100 USD'\n\n# Use as hash keys (value objects are hashable):\n{ Point.new(1,2) => 'a' }   # works (hash/eql based on values)\n\n# Data vs Struct:\n#   Data:   immutable, value-equal   -> value objects\n#   Struct: mutable, identity-ish    -> plain mutable records\n# Use Data for thread-safe, comparable value objects (points, money, config).\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Data Define in a real project?",
        "How would you compare Data Define with an alternative?",
        "What is a common mistake when using Data Define?"
      ],
      "seo": {
        "metaTitle": "The Data Class (define) — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is the Data class (define) in Ruby? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "ruby-modern-features-data-define-q001",
      "slug": "ruby-modern-features-data-define-when-to-use",
      "question": "When would you use Data Define in a real project?",
      "title": "When would you use Data Define in a real project",
      "direct_answer": "I would use the Data class (define) in Ruby when the problem matches its main benefit. Data (Ruby 3.2+) defines immutable value objects: `Point = Data.define(:x, :y)` creates a class with a generated initialize and #== based on values (like Struct, but immutable and value-equal). In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain the Data class (define) in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about the Data class (define) in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining the Data class (define) in Ruby in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. Data (Ruby 3.2+) defines immutable value objects: `Point = Data.define(:x, :y)` creates a class with a generated initialize and #== based on values (like Struct, but immutable and value-equal). p = Point.new(x: 1, y: 2) (or positional Point.new(1, 2)); p.x == 1; p == Point.new(1, 2) # true (value equality). Unlike Struct, Data objects are immutable — you can't reassign p.x = 5 (raises). They're perfect for value objects (points, money, config snapshots) where equality should be by value, not identity. Data.define(:x, :y) also accepts a block to add methods. Use Data over Struct when you want immutability and value equality; use Struct when you need mutation or a plain mutable record. Great for thread-safe, hashable, comparable value objects."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use the Data class (define) in Ruby when its rule makes the code safer, clearer, or more efficient for the actual problem. Data (Ruby 3.2+) defines immutable value objects: `Point = Data.define(:x, :y)` creates a class with a generated initialize and #== based on values (like Struct, but immutable and value-equal). p = Point.new(x: 1, y: 2) (or positional Point.new(1, 2)); p.x == 1; p == Point.new(1, 2) # true (value equality). Unlike Struct, Data objects are immutable — you can't reassign p.x = 5 (raises). They're perfect for value objects (points, money, config snapshots) where equality should be by value, not identity. Data.define(:x, :y) also accepts a block to add methods. Use Data over Struct when you want immutability and value equality; use Struct when you need mutation or a plain mutable record. Great for thread-safe, hashable, comparable value objects. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Data (Ruby 3.2+) defines immutable value objects: `Point = Data.define(:x, :y)` creates a class with a generated initialize and #== based on values (like Struct, but immutable and value-equal).\n\np = Point.new(x: 1, y: 2) (or positional Point.new(1, 2)); p.x == 1; p == Point.new(1, 2) # true (value equality).\n\nUnlike Struct, Data objects are immutable — you can't reassign p.x = 5 (raises). They're perfect for value objects (points, money, config snapshots) where equality should be by value, not identity. Data.define(:x, :y) also accepts a block to add methods. Use Data over Struct when you want immutability and value equality; use Struct when you need mutation or a plain mutable record. Great for thread-safe, hashable, comparable value objects."
          },
          {
            "type": "code_example",
            "title": "Data.define",
            "content": "```ruby\n# Ruby 3.2+: define an immutable value object\nPoint = Data.define(:x, :y)\n\np = Point.new(x: 1, y: 2)   # keyword args\np = Point.new(1, 2)        # positional also works\n\np.x            # 1\np.y            # 2\np == Point.new(1, 2)  # true  (VALUE equality, like Struct)\n\n# IMMUTABLE (unlike Struct):\n# p.x = 5            # raises NoMethodError (no setter)\n\n# With a block (add methods):\nMoney = Data.define(:amount, :currency) do\n  def to_s; \"#{amount} #{currency}\"; end\nend\nm = Money.new(100, 'USD')\nm.to_s         # '100 USD'\n\n# Use as hash keys (value objects are hashable):\n{ Point.new(1,2) => 'a' }   # works (hash/eql based on values)\n\n# Data vs Struct:\n#   Data:   immutable, value-equal   -> value objects\n#   Struct: mutable, identity-ish    -> plain mutable records\n# Use Data for thread-safe, comparable value objects (points, money, config).\n```"
          }
        ]
      },
      "followup_questions": [
        "What is the Data class (define) in Ruby?",
        "How would you compare Data Define with an alternative?",
        "What is a common mistake when using Data Define?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Data Define in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Data Define in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-modern-features-data-define-q002",
      "slug": "ruby-modern-features-data-define-common-mistake",
      "question": "What is a common mistake when using Data Define?",
      "title": "a common mistake when using Data Define",
      "direct_answer": "A common mistake with the Data class (define) in Ruby is using the rule without checking its boundary conditions. Data (Ruby 3.2+) defines immutable value objects: `Point = Data.define(:x, :y)` creates a class with a generated initialize and #== based on values (like Struct, but immutable and value-equal). That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain the Data class (define) in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about the Data class (define) in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of the Data class (define) in Ruby. Data (Ruby 3.2+) defines immutable value objects: `Point = Data.define(:x, :y)` creates a class with a generated initialize and #== based on values (like Struct, but immutable and value-equal). p = Point.new(x: 1, y: 2) (or positional Point.new(1, 2)); p.x == 1; p == Point.new(1, 2) # true (value equality). Unlike Struct, Data objects are immutable — you can't reassign p.x = 5 (raises). They're perfect for value objects (points, money, config snapshots) where equality should be by value, not identity. Data.define(:x, :y) also accepts a block to add methods. Use Data over Struct when you want immutability and value equality; use Struct when you need mutation or a plain mutable record. Great for thread-safe, hashable, comparable value objects."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with the Data class (define) in Ruby is assuming the happy path is the whole rule. Data (Ruby 3.2+) defines immutable value objects: `Point = Data.define(:x, :y)` creates a class with a generated initialize and #== based on values (like Struct, but immutable and value-equal). p = Point.new(x: 1, y: 2) (or positional Point.new(1, 2)); p.x == 1; p == Point.new(1, 2) # true (value equality). Unlike Struct, Data objects are immutable — you can't reassign p.x = 5 (raises). They're perfect for value objects (points, money, config snapshots) where equality should be by value, not identity. Data.define(:x, :y) also accepts a block to add methods. Use Data over Struct when you want immutability and value equality; use Struct when you need mutation or a plain mutable record. Great for thread-safe, hashable, comparable value objects. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "Data (Ruby 3.2+) defines immutable value objects: `Point = Data.define(:x, :y)` creates a class with a generated initialize and #== based on values (like Struct, but immutable and value-equal).\n\np = Point.new(x: 1, y: 2) (or positional Point.new(1, 2)); p.x == 1; p == Point.new(1, 2) # true (value equality).\n\nUnlike Struct, Data objects are immutable — you can't reassign p.x = 5 (raises). They're perfect for value objects (points, money, config snapshots) where equality should be by value, not identity. Data.define(:x, :y) also accepts a block to add methods. Use Data over Struct when you want immutability and value equality; use Struct when you need mutation or a plain mutable record. Great for thread-safe, hashable, comparable value objects."
          },
          {
            "type": "code_example",
            "title": "Data.define",
            "content": "```ruby\n# Ruby 3.2+: define an immutable value object\nPoint = Data.define(:x, :y)\n\np = Point.new(x: 1, y: 2)   # keyword args\np = Point.new(1, 2)        # positional also works\n\np.x            # 1\np.y            # 2\np == Point.new(1, 2)  # true  (VALUE equality, like Struct)\n\n# IMMUTABLE (unlike Struct):\n# p.x = 5            # raises NoMethodError (no setter)\n\n# With a block (add methods):\nMoney = Data.define(:amount, :currency) do\n  def to_s; \"#{amount} #{currency}\"; end\nend\nm = Money.new(100, 'USD')\nm.to_s         # '100 USD'\n\n# Use as hash keys (value objects are hashable):\n{ Point.new(1,2) => 'a' }   # works (hash/eql based on values)\n\n# Data vs Struct:\n#   Data:   immutable, value-equal   -> value objects\n#   Struct: mutable, identity-ish    -> plain mutable records\n# Use Data for thread-safe, comparable value objects (points, money, config).\n```"
          }
        ]
      },
      "followup_questions": [
        "What is the Data class (define) in Ruby?",
        "When would you use Data Define in a real project?",
        "How would you compare Data Define with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Data Define — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Data Define? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-modern-features-data-define-q003",
      "slug": "ruby-modern-features-data-define-compare",
      "question": "How would you compare Data Define with an alternative?",
      "title": "compare Data Define with an alternative",
      "direct_answer": "I compare the Data class (define) in Ruby with the simpler alternative by looking at correctness first, then time, memory, and readability. Data (Ruby 3.2+) defines immutable value objects: `Point = Data.define(:x, :y)` creates a class with a generated initialize and #== based on values (like Struct, but immutable and value-equal). The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain the Data class (define) in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about the Data class (define) in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. Data (Ruby 3.2+) defines immutable value objects: `Point = Data.define(:x, :y)` creates a class with a generated initialize and #== based on values (like Struct, but immutable and value-equal). p = Point.new(x: 1, y: 2) (or positional Point.new(1, 2)); p.x == 1; p == Point.new(1, 2) # true (value equality). Unlike Struct, Data objects are immutable — you can't reassign p.x = 5 (raises). They're perfect for value objects (points, money, config snapshots) where equality should be by value, not identity. Data.define(:x, :y) also accepts a block to add methods. Use Data over Struct when you want immutability and value equality; use Struct when you need mutation or a plain mutable record. Great for thread-safe, hashable, comparable value objects."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare the Data class (define) in Ruby with the straightforward alternative on the same example. Data (Ruby 3.2+) defines immutable value objects: `Point = Data.define(:x, :y)` creates a class with a generated initialize and #== based on values (like Struct, but immutable and value-equal). p = Point.new(x: 1, y: 2) (or positional Point.new(1, 2)); p.x == 1; p == Point.new(1, 2) # true (value equality). Unlike Struct, Data objects are immutable — you can't reassign p.x = 5 (raises). They're perfect for value objects (points, money, config snapshots) where equality should be by value, not identity. Data.define(:x, :y) also accepts a block to add methods. Use Data over Struct when you want immutability and value equality; use Struct when you need mutation or a plain mutable record. Great for thread-safe, hashable, comparable value objects. If the Data class (define) in Ruby removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "Data (Ruby 3.2+) defines immutable value objects: `Point = Data.define(:x, :y)` creates a class with a generated initialize and #== based on values (like Struct, but immutable and value-equal).\n\np = Point.new(x: 1, y: 2) (or positional Point.new(1, 2)); p.x == 1; p == Point.new(1, 2) # true (value equality).\n\nUnlike Struct, Data objects are immutable — you can't reassign p.x = 5 (raises). They're perfect for value objects (points, money, config snapshots) where equality should be by value, not identity. Data.define(:x, :y) also accepts a block to add methods. Use Data over Struct when you want immutability and value equality; use Struct when you need mutation or a plain mutable record. Great for thread-safe, hashable, comparable value objects."
          },
          {
            "type": "code_example",
            "title": "Data.define",
            "content": "```ruby\n# Ruby 3.2+: define an immutable value object\nPoint = Data.define(:x, :y)\n\np = Point.new(x: 1, y: 2)   # keyword args\np = Point.new(1, 2)        # positional also works\n\np.x            # 1\np.y            # 2\np == Point.new(1, 2)  # true  (VALUE equality, like Struct)\n\n# IMMUTABLE (unlike Struct):\n# p.x = 5            # raises NoMethodError (no setter)\n\n# With a block (add methods):\nMoney = Data.define(:amount, :currency) do\n  def to_s; \"#{amount} #{currency}\"; end\nend\nm = Money.new(100, 'USD')\nm.to_s         # '100 USD'\n\n# Use as hash keys (value objects are hashable):\n{ Point.new(1,2) => 'a' }   # works (hash/eql based on values)\n\n# Data vs Struct:\n#   Data:   immutable, value-equal   -> value objects\n#   Struct: mutable, identity-ish    -> plain mutable records\n# Use Data for thread-safe, comparable value objects (points, money, config).\n```"
          }
        ]
      },
      "followup_questions": [
        "What is the Data class (define) in Ruby?",
        "When would you use Data Define in a real project?",
        "What is a common mistake when using Data Define?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Data Define with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Data Define with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-modern-features-data-define-q004",
      "slug": "ruby-modern-features-data-define-scenario",
      "question": "How would you debug a problem involving Data Define?",
      "title": "debug a problem involving Data Define",
      "direct_answer": "I would debug a problem involving the Data class (define) in Ruby by reproducing it with the smallest input first. Data (Ruby 3.2+) defines immutable value objects: `Point = Data.define(:x, :y)` creates a class with a generated initialize and #== based on values (like Struct, but immutable and value-equal). Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain the Data class (define) in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about the Data class (define) in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what the Data class (define) in Ruby is supposed to guarantee. Data (Ruby 3.2+) defines immutable value objects: `Point = Data.define(:x, :y)` creates a class with a generated initialize and #== based on values (like Struct, but immutable and value-equal). p = Point.new(x: 1, y: 2) (or positional Point.new(1, 2)); p.x == 1; p == Point.new(1, 2) # true (value equality). Unlike Struct, Data objects are immutable — you can't reassign p.x = 5 (raises). They're perfect for value objects (points, money, config snapshots) where equality should be by value, not identity. Data.define(:x, :y) also accepts a block to add methods. Use Data over Struct when you want immutability and value equality; use Struct when you need mutation or a plain mutable record. Great for thread-safe, hashable, comparable value objects."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the the Data class (define) in Ruby bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: Data (Ruby 3.2+) defines immutable value objects: `Point = Data.define(:x, :y)` creates a class with a generated initialize and #== based on values (like Struct, but immutable and value-equal). p = Point.new(x: 1, y: 2) (or positional Point.new(1, 2)); p.x == 1; p == Point.new(1, 2) # true (value equality). Unlike Struct, Data objects are immutable — you can't reassign p.x = 5 (raises). They're perfect for value objects (points, money, config snapshots) where equality should be by value, not identity. Data.define(:x, :y) also accepts a block to add methods. Use Data over Struct when you want immutability and value equality; use Struct when you need mutation or a plain mutable record. Great for thread-safe, hashable, comparable value objects. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "Data (Ruby 3.2+) defines immutable value objects: `Point = Data.define(:x, :y)` creates a class with a generated initialize and #== based on values (like Struct, but immutable and value-equal).\n\np = Point.new(x: 1, y: 2) (or positional Point.new(1, 2)); p.x == 1; p == Point.new(1, 2) # true (value equality).\n\nUnlike Struct, Data objects are immutable — you can't reassign p.x = 5 (raises). They're perfect for value objects (points, money, config snapshots) where equality should be by value, not identity. Data.define(:x, :y) also accepts a block to add methods. Use Data over Struct when you want immutability and value equality; use Struct when you need mutation or a plain mutable record. Great for thread-safe, hashable, comparable value objects."
          },
          {
            "type": "code_example",
            "title": "Data.define",
            "content": "```ruby\n# Ruby 3.2+: define an immutable value object\nPoint = Data.define(:x, :y)\n\np = Point.new(x: 1, y: 2)   # keyword args\np = Point.new(1, 2)        # positional also works\n\np.x            # 1\np.y            # 2\np == Point.new(1, 2)  # true  (VALUE equality, like Struct)\n\n# IMMUTABLE (unlike Struct):\n# p.x = 5            # raises NoMethodError (no setter)\n\n# With a block (add methods):\nMoney = Data.define(:amount, :currency) do\n  def to_s; \"#{amount} #{currency}\"; end\nend\nm = Money.new(100, 'USD')\nm.to_s         # '100 USD'\n\n# Use as hash keys (value objects are hashable):\n{ Point.new(1,2) => 'a' }   # works (hash/eql based on values)\n\n# Data vs Struct:\n#   Data:   immutable, value-equal   -> value objects\n#   Struct: mutable, identity-ish    -> plain mutable records\n# Use Data for thread-safe, comparable value objects (points, money, config).\n```"
          }
        ]
      },
      "followup_questions": [
        "What is the Data class (define) in Ruby?",
        "When would you use Data Define in a real project?",
        "How would you compare Data Define with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Data Define — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Data Define? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

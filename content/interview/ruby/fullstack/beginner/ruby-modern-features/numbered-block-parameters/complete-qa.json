{
  "topic": "Numbered Block Parameters",
  "topicSlug": "numbered-block-parameters",
  "questions": [
    {
      "id": "numbered-block-parameters-q001",
      "slug": "ruby-numbered-block-parameters-basics",
      "question": "What are numbered block parameters in Ruby?",
      "title": "Numbered Block Parameters",
      "direct_answer": "Numbered parameters (Ruby 2.7+) let you skip declaring block params: `[1,2,3].each { puts _1 }` — _1 is the first block argument, _2 the second, etc. No `|x|` needed. Useful for tiny blocks where a name adds no clarity: `words.map { _1.upcase }`, `sum = nums.reduce(0) { _1 + _2 }` (_1 is the accumulator, _2 the element). They're a shorthand — for readability or more than ~2 params, declare named params (|x, y|). _1 is the same as declaring |x|; mixing _1 and named params in the same block is an error. They work in any block (each/map/reduce/times). They're purely syntactic sugar — the block is still a closure. Use them for one-liners; named params for anything with logic or more args.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know numbered parameters (Ruby 2.7+) — _1, _2, ... in blocks without declaring params.",
        "common_mistake": "Mixing _1 and named params (error), or overusing them for complex blocks (hurts readability).",
        "to_stand_out": "Numbered block params (Ruby 2.7+): _1, _2 in blocks without |x|. Shorthand for tiny blocks. reduce: _1 + _2. Don't mix with named params."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define Numbered Block Parameters simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "Numbered parameters (Ruby 2.7+): _1, _2 in blocks without declaring |x|.",
              "_1 is the first block arg, _2 the second; works in any block.",
              "In reduce, _1 is the accumulator, _2 the element.",
              "Don't mix _1 and named params in the same block (SyntaxError).",
              "Use for one-liners; named params for logic or more than ~2 args (readability)."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "Numbered parameters (Ruby 2.7 plus) let you skip declaring block params: 1 2 3 dot each block puts _1 — _1 is the first block argument, _2 the second, etc. No pipe x pipe needed. Useful for tiny blocks where a name adds no clarity: words dot map block _1 dot upcase, sum nums reduce 0 block _1 plus _2 (_1 is the accumulator, _2 the element). They're a shorthand — for readability or more than ~2 params, declare named params (pipe x y pipe). _1 is the same as declaring pipe x pipe; mixing _1 and named params in the same block is an error. They work in any block (each/map/reduce/times). They're purely syntactic sugar — the block is still a closure. Use them for one-liners; named params for anything with logic or more args. A common mistake is mixing _1 and named params, which is an error, and overusing them for complex blocks, which hurts readability."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Numbered parameters (Ruby 2.7+) let you skip declaring block params: `[1,2,3].each { puts _1 }` — _1 is the first block argument, _2 the second, etc.\n\nNo `|x|` needed.\n\nUseful for tiny blocks where a name adds no clarity: `words.map { _1.upcase }`, `sum = nums.reduce(0) { _1 + _2 }` (_1 is the accumulator, _2 the element). They're a shorthand — for readability or more than ~2 params, declare named params (|x, y|). _1 is the same as declaring |x|; mixing _1 and named params in the same block is an error. They work in any block (each/map/reduce/times). They're purely syntactic sugar — the block is still a closure. Use them for one-liners; named params for anything with logic or more args."
          },
          {
            "type": "flow_diagram",
            "title": "Numbered Params",
            "content": "_1, _2 in blocks (no |x|). Shorthand. reduce: _1 + _2. Don't mix with named params.\n\n```mermaid\nflowchart LR\n  B[block { puts _1 }] --> P[_1 = first arg]\n  R[reduce { _1 + _2 }] --> A[_1 acc, _2 elem]\n  M[mix _1 and |x|] --> E[error]\n```"
          },
          {
            "type": "code_example",
            "title": "Numbered block parameters",
            "content": "```ruby\n# Ruby 2.7+: numbered block parameters (_1, _2, ...)\n# No need to declare |x|:\n[1, 2, 3].each { puts _1 }       # 1 2 3\nwords = %w[hi bye]\nwords.map { _1.upcase }          # ['HI', 'BYE']\n\n# _1 = first arg, _2 = second, etc.\n%w[a b].each_with_index { puts \"#{_1} #{_2}\" }  # a 0 / b 1\n\n# In reduce: _1 is the accumulator, _2 the element\nsum = [1, 2, 3].reduce(0) { _1 + _2 }   # 6\n# (same as: reduce(0) { |acc, n| acc + n })\n\n# Equivalent to declaring params:\n[1,2,3].map { |x| x * 2 }   ==  [1,2,3].map { _1 * 2 }   # true\n\n# DON'T mix _1 and named params in the same block (error):\n# [1,2].map { |x| _1 }        # SyntaxError\n\n# Works in any block (each/map/reduce/times/select):\n3.times { puts _1 }            # 0 1 2\n[1,2,3].select { _1.even? }   # [2]\n\n# Use for ONE-LINERS; named params for logic or many args.\n# Purely syntactic sugar; the block is still a closure.\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Numbered Block Parameters in a real project?",
        "How would you compare Numbered Block Parameters with an alternative?",
        "What is a common mistake when using Numbered Block Parameters?"
      ],
      "seo": {
        "metaTitle": "Numbered Block Parameters — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What are numbered block parameters in Ruby? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "ruby-modern-features-numbered-block-parameters-q001",
      "slug": "ruby-modern-features-numbered-block-parameters-when-to-use",
      "question": "When would you use Numbered Block Parameters in a real project?",
      "title": "When would you use Numbered Block Parameters in a real project",
      "direct_answer": "I would use numbered block parameters in Ruby when the problem matches its main benefit. Numbered parameters (Ruby 2.7+) let you skip declaring block params: `[1,2,3].each { puts _1 }` — _1 is the first block argument, _2 the second, etc. In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain numbered block parameters in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about numbered block parameters in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining numbered block parameters in Ruby in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. Numbered parameters (Ruby 2.7+) let you skip declaring block params: `[1,2,3].each { puts _1 }` — _1 is the first block argument, _2 the second, etc. No `|x|` needed. Useful for tiny blocks where a name adds no clarity: `words.map { _1.upcase }`, `sum = nums.reduce(0) { _1 + _2 }` (_1 is the accumulator, _2 the element). They're a shorthand — for readability or more than ~2 params, declare named params (|x, y|). _1 is the same as declaring |x|; mixing _1 and named params in the same block is an error. They work in any block (each/map/reduce/times). They're purely syntactic sugar — the block is still a closure. Use them for one-liners; named params for anything with logic or more args."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use numbered block parameters in Ruby when its rule makes the code safer, clearer, or more efficient for the actual problem. Numbered parameters (Ruby 2.7+) let you skip declaring block params: `[1,2,3].each { puts _1 }` — _1 is the first block argument, _2 the second, etc. No `|x|` needed. Useful for tiny blocks where a name adds no clarity: `words.map { _1.upcase }`, `sum = nums.reduce(0) { _1 + _2 }` (_1 is the accumulator, _2 the element). They're a shorthand — for readability or more than ~2 params, declare named params (|x, y|). _1 is the same as declaring |x|; mixing _1 and named params in the same block is an error. They work in any block (each/map/reduce/times). They're purely syntactic sugar — the block is still a closure. Use them for one-liners; named params for anything with logic or more args. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Numbered parameters (Ruby 2.7+) let you skip declaring block params: `[1,2,3].each { puts _1 }` — _1 is the first block argument, _2 the second, etc.\n\nNo `|x|` needed.\n\nUseful for tiny blocks where a name adds no clarity: `words.map { _1.upcase }`, `sum = nums.reduce(0) { _1 + _2 }` (_1 is the accumulator, _2 the element). They're a shorthand — for readability or more than ~2 params, declare named params (|x, y|). _1 is the same as declaring |x|; mixing _1 and named params in the same block is an error. They work in any block (each/map/reduce/times). They're purely syntactic sugar — the block is still a closure. Use them for one-liners; named params for anything with logic or more args."
          },
          {
            "type": "code_example",
            "title": "Numbered block parameters",
            "content": "```ruby\n# Ruby 2.7+: numbered block parameters (_1, _2, ...)\n# No need to declare |x|:\n[1, 2, 3].each { puts _1 }       # 1 2 3\nwords = %w[hi bye]\nwords.map { _1.upcase }          # ['HI', 'BYE']\n\n# _1 = first arg, _2 = second, etc.\n%w[a b].each_with_index { puts \"#{_1} #{_2}\" }  # a 0 / b 1\n\n# In reduce: _1 is the accumulator, _2 the element\nsum = [1, 2, 3].reduce(0) { _1 + _2 }   # 6\n# (same as: reduce(0) { |acc, n| acc + n })\n\n# Equivalent to declaring params:\n[1,2,3].map { |x| x * 2 }   ==  [1,2,3].map { _1 * 2 }   # true\n\n# DON'T mix _1 and named params in the same block (error):\n# [1,2].map { |x| _1 }        # SyntaxError\n\n# Works in any block (each/map/reduce/times/select):\n3.times { puts _1 }            # 0 1 2\n[1,2,3].select { _1.even? }   # [2]\n\n# Use for ONE-LINERS; named params for logic or many args.\n# Purely syntactic sugar; the block is still a closure.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are numbered block parameters in Ruby?",
        "How would you compare Numbered Block Parameters with an alternative?",
        "What is a common mistake when using Numbered Block Parameters?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Numbered Block Parameters in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Numbered Block Parameters in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-modern-features-numbered-block-parameters-q002",
      "slug": "ruby-modern-features-numbered-block-parameters-common-mistake",
      "question": "What is a common mistake when using Numbered Block Parameters?",
      "title": "a common mistake when using Numbered Block Parameters",
      "direct_answer": "A common mistake with numbered block parameters in Ruby is using the rule without checking its boundary conditions. Numbered parameters (Ruby 2.7+) let you skip declaring block params: `[1,2,3].each { puts _1 }` — _1 is the first block argument, _2 the second, etc. That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain numbered block parameters in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about numbered block parameters in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of numbered block parameters in Ruby. Numbered parameters (Ruby 2.7+) let you skip declaring block params: `[1,2,3].each { puts _1 }` — _1 is the first block argument, _2 the second, etc. No `|x|` needed. Useful for tiny blocks where a name adds no clarity: `words.map { _1.upcase }`, `sum = nums.reduce(0) { _1 + _2 }` (_1 is the accumulator, _2 the element). They're a shorthand — for readability or more than ~2 params, declare named params (|x, y|). _1 is the same as declaring |x|; mixing _1 and named params in the same block is an error. They work in any block (each/map/reduce/times). They're purely syntactic sugar — the block is still a closure. Use them for one-liners; named params for anything with logic or more args."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with numbered block parameters in Ruby is assuming the happy path is the whole rule. Numbered parameters (Ruby 2.7+) let you skip declaring block params: `[1,2,3].each { puts _1 }` — _1 is the first block argument, _2 the second, etc. No `|x|` needed. Useful for tiny blocks where a name adds no clarity: `words.map { _1.upcase }`, `sum = nums.reduce(0) { _1 + _2 }` (_1 is the accumulator, _2 the element). They're a shorthand — for readability or more than ~2 params, declare named params (|x, y|). _1 is the same as declaring |x|; mixing _1 and named params in the same block is an error. They work in any block (each/map/reduce/times). They're purely syntactic sugar — the block is still a closure. Use them for one-liners; named params for anything with logic or more args. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "Numbered parameters (Ruby 2.7+) let you skip declaring block params: `[1,2,3].each { puts _1 }` — _1 is the first block argument, _2 the second, etc.\n\nNo `|x|` needed.\n\nUseful for tiny blocks where a name adds no clarity: `words.map { _1.upcase }`, `sum = nums.reduce(0) { _1 + _2 }` (_1 is the accumulator, _2 the element). They're a shorthand — for readability or more than ~2 params, declare named params (|x, y|). _1 is the same as declaring |x|; mixing _1 and named params in the same block is an error. They work in any block (each/map/reduce/times). They're purely syntactic sugar — the block is still a closure. Use them for one-liners; named params for anything with logic or more args."
          },
          {
            "type": "code_example",
            "title": "Numbered block parameters",
            "content": "```ruby\n# Ruby 2.7+: numbered block parameters (_1, _2, ...)\n# No need to declare |x|:\n[1, 2, 3].each { puts _1 }       # 1 2 3\nwords = %w[hi bye]\nwords.map { _1.upcase }          # ['HI', 'BYE']\n\n# _1 = first arg, _2 = second, etc.\n%w[a b].each_with_index { puts \"#{_1} #{_2}\" }  # a 0 / b 1\n\n# In reduce: _1 is the accumulator, _2 the element\nsum = [1, 2, 3].reduce(0) { _1 + _2 }   # 6\n# (same as: reduce(0) { |acc, n| acc + n })\n\n# Equivalent to declaring params:\n[1,2,3].map { |x| x * 2 }   ==  [1,2,3].map { _1 * 2 }   # true\n\n# DON'T mix _1 and named params in the same block (error):\n# [1,2].map { |x| _1 }        # SyntaxError\n\n# Works in any block (each/map/reduce/times/select):\n3.times { puts _1 }            # 0 1 2\n[1,2,3].select { _1.even? }   # [2]\n\n# Use for ONE-LINERS; named params for logic or many args.\n# Purely syntactic sugar; the block is still a closure.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are numbered block parameters in Ruby?",
        "When would you use Numbered Block Parameters in a real project?",
        "How would you compare Numbered Block Parameters with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Numbered Block Parameters — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Numbered Block Parameters? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-modern-features-numbered-block-parameters-q003",
      "slug": "ruby-modern-features-numbered-block-parameters-compare",
      "question": "How would you compare Numbered Block Parameters with an alternative?",
      "title": "compare Numbered Block Parameters with an alternative",
      "direct_answer": "I compare numbered block parameters in Ruby with the simpler alternative by looking at correctness first, then time, memory, and readability. Numbered parameters (Ruby 2.7+) let you skip declaring block params: `[1,2,3].each { puts _1 }` — _1 is the first block argument, _2 the second, etc. The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain numbered block parameters in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about numbered block parameters in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. Numbered parameters (Ruby 2.7+) let you skip declaring block params: `[1,2,3].each { puts _1 }` — _1 is the first block argument, _2 the second, etc. No `|x|` needed. Useful for tiny blocks where a name adds no clarity: `words.map { _1.upcase }`, `sum = nums.reduce(0) { _1 + _2 }` (_1 is the accumulator, _2 the element). They're a shorthand — for readability or more than ~2 params, declare named params (|x, y|). _1 is the same as declaring |x|; mixing _1 and named params in the same block is an error. They work in any block (each/map/reduce/times). They're purely syntactic sugar — the block is still a closure. Use them for one-liners; named params for anything with logic or more args."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare numbered block parameters in Ruby with the straightforward alternative on the same example. Numbered parameters (Ruby 2.7+) let you skip declaring block params: `[1,2,3].each { puts _1 }` — _1 is the first block argument, _2 the second, etc. No `|x|` needed. Useful for tiny blocks where a name adds no clarity: `words.map { _1.upcase }`, `sum = nums.reduce(0) { _1 + _2 }` (_1 is the accumulator, _2 the element). They're a shorthand — for readability or more than ~2 params, declare named params (|x, y|). _1 is the same as declaring |x|; mixing _1 and named params in the same block is an error. They work in any block (each/map/reduce/times). They're purely syntactic sugar — the block is still a closure. Use them for one-liners; named params for anything with logic or more args. If numbered block parameters in Ruby removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "Numbered parameters (Ruby 2.7+) let you skip declaring block params: `[1,2,3].each { puts _1 }` — _1 is the first block argument, _2 the second, etc.\n\nNo `|x|` needed.\n\nUseful for tiny blocks where a name adds no clarity: `words.map { _1.upcase }`, `sum = nums.reduce(0) { _1 + _2 }` (_1 is the accumulator, _2 the element). They're a shorthand — for readability or more than ~2 params, declare named params (|x, y|). _1 is the same as declaring |x|; mixing _1 and named params in the same block is an error. They work in any block (each/map/reduce/times). They're purely syntactic sugar — the block is still a closure. Use them for one-liners; named params for anything with logic or more args."
          },
          {
            "type": "code_example",
            "title": "Numbered block parameters",
            "content": "```ruby\n# Ruby 2.7+: numbered block parameters (_1, _2, ...)\n# No need to declare |x|:\n[1, 2, 3].each { puts _1 }       # 1 2 3\nwords = %w[hi bye]\nwords.map { _1.upcase }          # ['HI', 'BYE']\n\n# _1 = first arg, _2 = second, etc.\n%w[a b].each_with_index { puts \"#{_1} #{_2}\" }  # a 0 / b 1\n\n# In reduce: _1 is the accumulator, _2 the element\nsum = [1, 2, 3].reduce(0) { _1 + _2 }   # 6\n# (same as: reduce(0) { |acc, n| acc + n })\n\n# Equivalent to declaring params:\n[1,2,3].map { |x| x * 2 }   ==  [1,2,3].map { _1 * 2 }   # true\n\n# DON'T mix _1 and named params in the same block (error):\n# [1,2].map { |x| _1 }        # SyntaxError\n\n# Works in any block (each/map/reduce/times/select):\n3.times { puts _1 }            # 0 1 2\n[1,2,3].select { _1.even? }   # [2]\n\n# Use for ONE-LINERS; named params for logic or many args.\n# Purely syntactic sugar; the block is still a closure.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are numbered block parameters in Ruby?",
        "When would you use Numbered Block Parameters in a real project?",
        "What is a common mistake when using Numbered Block Parameters?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Numbered Block Parameters with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Numbered Block Parameters with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-modern-features-numbered-block-parameters-q004",
      "slug": "ruby-modern-features-numbered-block-parameters-scenario",
      "question": "How would you debug a problem involving Numbered Block Parameters?",
      "title": "debug a problem involving Numbered Block Parameters",
      "direct_answer": "I would debug a problem involving numbered block parameters in Ruby by reproducing it with the smallest input first. Numbered parameters (Ruby 2.7+) let you skip declaring block params: `[1,2,3].each { puts _1 }` — _1 is the first block argument, _2 the second, etc. Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain numbered block parameters in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about numbered block parameters in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what numbered block parameters in Ruby is supposed to guarantee. Numbered parameters (Ruby 2.7+) let you skip declaring block params: `[1,2,3].each { puts _1 }` — _1 is the first block argument, _2 the second, etc. No `|x|` needed. Useful for tiny blocks where a name adds no clarity: `words.map { _1.upcase }`, `sum = nums.reduce(0) { _1 + _2 }` (_1 is the accumulator, _2 the element). They're a shorthand — for readability or more than ~2 params, declare named params (|x, y|). _1 is the same as declaring |x|; mixing _1 and named params in the same block is an error. They work in any block (each/map/reduce/times). They're purely syntactic sugar — the block is still a closure. Use them for one-liners; named params for anything with logic or more args."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the numbered block parameters in Ruby bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: Numbered parameters (Ruby 2.7+) let you skip declaring block params: `[1,2,3].each { puts _1 }` — _1 is the first block argument, _2 the second, etc. No `|x|` needed. Useful for tiny blocks where a name adds no clarity: `words.map { _1.upcase }`, `sum = nums.reduce(0) { _1 + _2 }` (_1 is the accumulator, _2 the element). They're a shorthand — for readability or more than ~2 params, declare named params (|x, y|). _1 is the same as declaring |x|; mixing _1 and named params in the same block is an error. They work in any block (each/map/reduce/times). They're purely syntactic sugar — the block is still a closure. Use them for one-liners; named params for anything with logic or more args. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "Numbered parameters (Ruby 2.7+) let you skip declaring block params: `[1,2,3].each { puts _1 }` — _1 is the first block argument, _2 the second, etc.\n\nNo `|x|` needed.\n\nUseful for tiny blocks where a name adds no clarity: `words.map { _1.upcase }`, `sum = nums.reduce(0) { _1 + _2 }` (_1 is the accumulator, _2 the element). They're a shorthand — for readability or more than ~2 params, declare named params (|x, y|). _1 is the same as declaring |x|; mixing _1 and named params in the same block is an error. They work in any block (each/map/reduce/times). They're purely syntactic sugar — the block is still a closure. Use them for one-liners; named params for anything with logic or more args."
          },
          {
            "type": "code_example",
            "title": "Numbered block parameters",
            "content": "```ruby\n# Ruby 2.7+: numbered block parameters (_1, _2, ...)\n# No need to declare |x|:\n[1, 2, 3].each { puts _1 }       # 1 2 3\nwords = %w[hi bye]\nwords.map { _1.upcase }          # ['HI', 'BYE']\n\n# _1 = first arg, _2 = second, etc.\n%w[a b].each_with_index { puts \"#{_1} #{_2}\" }  # a 0 / b 1\n\n# In reduce: _1 is the accumulator, _2 the element\nsum = [1, 2, 3].reduce(0) { _1 + _2 }   # 6\n# (same as: reduce(0) { |acc, n| acc + n })\n\n# Equivalent to declaring params:\n[1,2,3].map { |x| x * 2 }   ==  [1,2,3].map { _1 * 2 }   # true\n\n# DON'T mix _1 and named params in the same block (error):\n# [1,2].map { |x| _1 }        # SyntaxError\n\n# Works in any block (each/map/reduce/times/select):\n3.times { puts _1 }            # 0 1 2\n[1,2,3].select { _1.even? }   # [2]\n\n# Use for ONE-LINERS; named params for logic or many args.\n# Purely syntactic sugar; the block is still a closure.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are numbered block parameters in Ruby?",
        "When would you use Numbered Block Parameters in a real project?",
        "How would you compare Numbered Block Parameters with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Numbered Block Parameters — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Numbered Block Parameters? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

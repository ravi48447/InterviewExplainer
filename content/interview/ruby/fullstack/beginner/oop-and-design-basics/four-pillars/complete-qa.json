{
  "topic": "Four Pillars",
  "topicSlug": "four-pillars",
  "questions": [
    {
      "id": "four-pillars-q001",
      "slug": "ruby-oop-four-pillars-basics",
      "question": "What are the four pillars of OOP and how do they work in Ruby?",
      "title": "The Four Pillars of OOP",
      "direct_answer": "The four pillars: Encapsulation (hide internal state, expose a clean interface — attr_accessor/attr_reader; private methods), Abstraction (model real things as classes with meaningful methods — a User with login, not a hash of data), Inheritance (a subclass reuses a parent's behavior — class Admin < User), Polymorphism (different objects respond to the same method their own way — duck typing, or overriding). Ruby does all four: encapsulation via private/protected and attr_*; abstraction via classes; inheritance via <; polymorphism via duck typing (no formal interfaces needed) and method overriding. They make code modular, reusable, and changeable.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you can name the four pillars and map them to Ruby (encapsulation: private/attr_*; inheritance: <; polymorphism: duck typing/override).",
        "common_mistake": "Thinking Ruby needs formal interfaces for polymorphism (it uses duck typing), or over-using inheritance where a module fits.",
        "to_stand_out": "Four pillars: Encapsulation (private/attr_*), Abstraction (classes), Inheritance (<), Polymorphism (duck typing/override). Ruby does all four; polymorphism uses duck typing, not formal interfaces."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define The Four Pillars of OOP simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "The four pillars: Encapsulation, Abstraction, Inheritance, Polymorphism.",
              "Encapsulation: hide state with private/protected and expose readers (attr_reader).",
              "Abstraction: model real things as classes with meaningful methods.",
              "Inheritance: a subclass reuses a parent's behavior with <.",
              "Polymorphism: different objects respond to the same method (duck typing / override)."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "The four pillars: Encapsulation — hide internal state, expose a clean interface, with attr_accessor and attr_reader and private methods. Abstraction — model real things as classes with meaningful methods, a User with login rather than a hash of data. Inheritance — a subclass reuses a parent's behavior, class Admin less-than User. Polymorphism — different objects respond to the same method their own way, via duck typing or method overriding. Ruby does all four: encapsulation via private and protected and attr_star; abstraction via classes; inheritance via the angle bracket; and polymorphism via duck typing — no formal interfaces needed — and method overriding. They make code modular, reusable, and changeable. A common mistake is thinking Ruby needs formal interfaces for polymorphism, when it uses duck typing, and over-using inheritance where a module would fit."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "The four pillars: Encapsulation (hide internal state, expose a clean interface — attr_accessor/attr_reader; private methods), Abstraction (model real things as classes with meaningful methods — a User with login, not a hash of data), Inheritance (a subclass reuses a parent's behavior — class Admin < User), Polymorphism (different objects respond to the same method their own way — duck typing, or overriding).\n\nRuby does all four: encapsulation via private/protected and attr_*; abstraction via classes; inheritance via <; polymorphism via duck typing (no formal interfaces needed) and method overriding.\n\nThey make code modular, reusable, and changeable.\n\nThinking Ruby needs formal interfaces for polymorphism (it uses duck typing), or over-using inheritance where a module fits."
          },
          {
            "type": "flow_diagram",
            "title": "The Four Pillars",
            "content": "Each maps to a Ruby feature.\n\n```mermaid\nflowchart TB\n  E[Encapsulation: private/attr_*] --> A[Abstraction: classes]\n  A --> I[Inheritance: <]\n  I --> P[Polymorphism: duck typing/override]\n```"
          },
          {
            "type": "code_example",
            "title": "The four pillars in Ruby",
            "content": "```ruby\nclass Animal\n  attr_reader :name                 # encapsulation: expose a reader\n  def initialize(name); @name = name; end\n  def speak; '...' end               # to be overridden (polymorphism)\n  private\n  def digest; end                    # encapsulation: hidden\nend\n\nclass Dog < Animal                  # inheritance: reuse Animal\n  def speak; 'Woof' end              # polymorphism: override speak\nend\n\nclass Cat < Animal\n  def speak; 'Meow' end\nend\n\n# Polymorphism via duck typing (no interface needed):\n[Dog.new('Rex'), Cat.new('Tom')].each { |a| puts a.speak }  # Woof, Meow\n# Any object with #speak works — that's duck typing.\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Four Pillars in a real project?",
        "How would you compare Four Pillars with an alternative?",
        "What is a common mistake when using Four Pillars?"
      ],
      "seo": {
        "metaTitle": "The Four Pillars of OOP — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What are the four pillars of OOP and how do they work in Ruby? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "oop-and-design-basics-four-pillars-q001",
      "slug": "oop-and-design-basics-four-pillars-when-to-use",
      "question": "When would you use Four Pillars in a real project?",
      "title": "When would you use Four Pillars in a real project",
      "direct_answer": "I would use the four pillars of OOP and how do they work in Ruby when the problem matches its main benefit. The four pillars: Encapsulation (hide internal state, expose a clean interface — attr_accessor/attr_reader; private methods), Abstraction (model real things as classes with meaningful methods — a User with login, not a hash of data), Inheritance (a subclass reuses a parent's behavior — class Admin < User), Polymorphism (different objects respond to the same method their own way — duck typing, or overriding). In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain the four pillars of OOP and how do they work in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about the four pillars of OOP and how do they work in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining the four pillars of OOP and how do they work in Ruby in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. The four pillars: Encapsulation (hide internal state, expose a clean interface — attr_accessor/attr_reader; private methods), Abstraction (model real things as classes with meaningful methods — a User with login, not a hash of data), Inheritance (a subclass reuses a parent's behavior — class Admin < User), Polymorphism (different objects respond to the same method their own way — duck typing, or overriding). Ruby does all four: encapsulation via private/protected and attr_*; abstraction via classes; inheritance via <; polymorphism via duck typing (no formal interfaces needed) and method overriding. They make code modular, reusable, and changeable."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use the four pillars of OOP and how do they work in Ruby when its rule makes the code safer, clearer, or more efficient for the actual problem. The four pillars: Encapsulation (hide internal state, expose a clean interface — attr_accessor/attr_reader; private methods), Abstraction (model real things as classes with meaningful methods — a User with login, not a hash of data), Inheritance (a subclass reuses a parent's behavior — class Admin < User), Polymorphism (different objects respond to the same method their own way — duck typing, or overriding). Ruby does all four: encapsulation via private/protected and attr_*; abstraction via classes; inheritance via <; polymorphism via duck typing (no formal interfaces needed) and method overriding. They make code modular, reusable, and changeable. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "The four pillars: Encapsulation (hide internal state, expose a clean interface — attr_accessor/attr_reader; private methods), Abstraction (model real things as classes with meaningful methods — a User with login, not a hash of data), Inheritance (a subclass reuses a parent's behavior — class Admin < User), Polymorphism (different objects respond to the same method their own way — duck typing, or overriding).\n\nRuby does all four: encapsulation via private/protected and attr_*; abstraction via classes; inheritance via <; polymorphism via duck typing (no formal interfaces needed) and method overriding.\n\nThey make code modular, reusable, and changeable.\n\nThinking Ruby needs formal interfaces for polymorphism (it uses duck typing), or over-using inheritance where a module fits."
          },
          {
            "type": "code_example",
            "title": "The four pillars in Ruby",
            "content": "```ruby\nclass Animal\n  attr_reader :name                 # encapsulation: expose a reader\n  def initialize(name); @name = name; end\n  def speak; '...' end               # to be overridden (polymorphism)\n  private\n  def digest; end                    # encapsulation: hidden\nend\n\nclass Dog < Animal                  # inheritance: reuse Animal\n  def speak; 'Woof' end              # polymorphism: override speak\nend\n\nclass Cat < Animal\n  def speak; 'Meow' end\nend\n\n# Polymorphism via duck typing (no interface needed):\n[Dog.new('Rex'), Cat.new('Tom')].each { |a| puts a.speak }  # Woof, Meow\n# Any object with #speak works — that's duck typing.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are the four pillars of OOP and how do they work in Ruby?",
        "How would you compare Four Pillars with an alternative?",
        "What is a common mistake when using Four Pillars?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Four Pillars in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Four Pillars in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "oop-and-design-basics-four-pillars-q002",
      "slug": "oop-and-design-basics-four-pillars-common-mistake",
      "question": "What is a common mistake when using Four Pillars?",
      "title": "a common mistake when using Four Pillars",
      "direct_answer": "A common mistake with the four pillars of OOP and how do they work in Ruby is using the rule without checking its boundary conditions. The four pillars: Encapsulation (hide internal state, expose a clean interface — attr_accessor/attr_reader; private methods), Abstraction (model real things as classes with meaningful methods — a User with login, not a hash of data), Inheritance (a subclass reuses a parent's behavior — class Admin < User), Polymorphism (different objects respond to the same method their own way — duck typing, or overriding). That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain the four pillars of OOP and how do they work in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about the four pillars of OOP and how do they work in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of the four pillars of OOP and how do they work in Ruby. The four pillars: Encapsulation (hide internal state, expose a clean interface — attr_accessor/attr_reader; private methods), Abstraction (model real things as classes with meaningful methods — a User with login, not a hash of data), Inheritance (a subclass reuses a parent's behavior — class Admin < User), Polymorphism (different objects respond to the same method their own way — duck typing, or overriding). Ruby does all four: encapsulation via private/protected and attr_*; abstraction via classes; inheritance via <; polymorphism via duck typing (no formal interfaces needed) and method overriding. They make code modular, reusable, and changeable."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with the four pillars of OOP and how do they work in Ruby is assuming the happy path is the whole rule. The four pillars: Encapsulation (hide internal state, expose a clean interface — attr_accessor/attr_reader; private methods), Abstraction (model real things as classes with meaningful methods — a User with login, not a hash of data), Inheritance (a subclass reuses a parent's behavior — class Admin < User), Polymorphism (different objects respond to the same method their own way — duck typing, or overriding). Ruby does all four: encapsulation via private/protected and attr_*; abstraction via classes; inheritance via <; polymorphism via duck typing (no formal interfaces needed) and method overriding. They make code modular, reusable, and changeable. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "The four pillars: Encapsulation (hide internal state, expose a clean interface — attr_accessor/attr_reader; private methods), Abstraction (model real things as classes with meaningful methods — a User with login, not a hash of data), Inheritance (a subclass reuses a parent's behavior — class Admin < User), Polymorphism (different objects respond to the same method their own way — duck typing, or overriding).\n\nRuby does all four: encapsulation via private/protected and attr_*; abstraction via classes; inheritance via <; polymorphism via duck typing (no formal interfaces needed) and method overriding.\n\nThey make code modular, reusable, and changeable.\n\nThinking Ruby needs formal interfaces for polymorphism (it uses duck typing), or over-using inheritance where a module fits."
          },
          {
            "type": "code_example",
            "title": "The four pillars in Ruby",
            "content": "```ruby\nclass Animal\n  attr_reader :name                 # encapsulation: expose a reader\n  def initialize(name); @name = name; end\n  def speak; '...' end               # to be overridden (polymorphism)\n  private\n  def digest; end                    # encapsulation: hidden\nend\n\nclass Dog < Animal                  # inheritance: reuse Animal\n  def speak; 'Woof' end              # polymorphism: override speak\nend\n\nclass Cat < Animal\n  def speak; 'Meow' end\nend\n\n# Polymorphism via duck typing (no interface needed):\n[Dog.new('Rex'), Cat.new('Tom')].each { |a| puts a.speak }  # Woof, Meow\n# Any object with #speak works — that's duck typing.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are the four pillars of OOP and how do they work in Ruby?",
        "When would you use Four Pillars in a real project?",
        "How would you compare Four Pillars with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Four Pillars — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Four Pillars? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "oop-and-design-basics-four-pillars-q003",
      "slug": "oop-and-design-basics-four-pillars-compare",
      "question": "How would you compare Four Pillars with an alternative?",
      "title": "compare Four Pillars with an alternative",
      "direct_answer": "I compare the four pillars of OOP and how do they work in Ruby with the simpler alternative by looking at correctness first, then time, memory, and readability. The four pillars: Encapsulation (hide internal state, expose a clean interface — attr_accessor/attr_reader; private methods), Abstraction (model real things as classes with meaningful methods — a User with login, not a hash of data), Inheritance (a subclass reuses a parent's behavior — class Admin < User), Polymorphism (different objects respond to the same method their own way — duck typing, or overriding). The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain the four pillars of OOP and how do they work in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about the four pillars of OOP and how do they work in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. The four pillars: Encapsulation (hide internal state, expose a clean interface — attr_accessor/attr_reader; private methods), Abstraction (model real things as classes with meaningful methods — a User with login, not a hash of data), Inheritance (a subclass reuses a parent's behavior — class Admin < User), Polymorphism (different objects respond to the same method their own way — duck typing, or overriding). Ruby does all four: encapsulation via private/protected and attr_*; abstraction via classes; inheritance via <; polymorphism via duck typing (no formal interfaces needed) and method overriding. They make code modular, reusable, and changeable."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare the four pillars of OOP and how do they work in Ruby with the straightforward alternative on the same example. The four pillars: Encapsulation (hide internal state, expose a clean interface — attr_accessor/attr_reader; private methods), Abstraction (model real things as classes with meaningful methods — a User with login, not a hash of data), Inheritance (a subclass reuses a parent's behavior — class Admin < User), Polymorphism (different objects respond to the same method their own way — duck typing, or overriding). Ruby does all four: encapsulation via private/protected and attr_*; abstraction via classes; inheritance via <; polymorphism via duck typing (no formal interfaces needed) and method overriding. They make code modular, reusable, and changeable. If the four pillars of OOP and how do they work in Ruby removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "The four pillars: Encapsulation (hide internal state, expose a clean interface — attr_accessor/attr_reader; private methods), Abstraction (model real things as classes with meaningful methods — a User with login, not a hash of data), Inheritance (a subclass reuses a parent's behavior — class Admin < User), Polymorphism (different objects respond to the same method their own way — duck typing, or overriding).\n\nRuby does all four: encapsulation via private/protected and attr_*; abstraction via classes; inheritance via <; polymorphism via duck typing (no formal interfaces needed) and method overriding.\n\nThey make code modular, reusable, and changeable.\n\nThinking Ruby needs formal interfaces for polymorphism (it uses duck typing), or over-using inheritance where a module fits."
          },
          {
            "type": "code_example",
            "title": "The four pillars in Ruby",
            "content": "```ruby\nclass Animal\n  attr_reader :name                 # encapsulation: expose a reader\n  def initialize(name); @name = name; end\n  def speak; '...' end               # to be overridden (polymorphism)\n  private\n  def digest; end                    # encapsulation: hidden\nend\n\nclass Dog < Animal                  # inheritance: reuse Animal\n  def speak; 'Woof' end              # polymorphism: override speak\nend\n\nclass Cat < Animal\n  def speak; 'Meow' end\nend\n\n# Polymorphism via duck typing (no interface needed):\n[Dog.new('Rex'), Cat.new('Tom')].each { |a| puts a.speak }  # Woof, Meow\n# Any object with #speak works — that's duck typing.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are the four pillars of OOP and how do they work in Ruby?",
        "When would you use Four Pillars in a real project?",
        "What is a common mistake when using Four Pillars?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Four Pillars with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Four Pillars with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "oop-and-design-basics-four-pillars-q004",
      "slug": "oop-and-design-basics-four-pillars-scenario",
      "question": "How would you debug a problem involving Four Pillars?",
      "title": "debug a problem involving Four Pillars",
      "direct_answer": "I would debug a problem involving the four pillars of OOP and how do they work in Ruby by reproducing it with the smallest input first. The four pillars: Encapsulation (hide internal state, expose a clean interface — attr_accessor/attr_reader; private methods), Abstraction (model real things as classes with meaningful methods — a User with login, not a hash of data), Inheritance (a subclass reuses a parent's behavior — class Admin < User), Polymorphism (different objects respond to the same method their own way — duck typing, or overriding). Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain the four pillars of OOP and how do they work in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about the four pillars of OOP and how do they work in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what the four pillars of OOP and how do they work in Ruby is supposed to guarantee. The four pillars: Encapsulation (hide internal state, expose a clean interface — attr_accessor/attr_reader; private methods), Abstraction (model real things as classes with meaningful methods — a User with login, not a hash of data), Inheritance (a subclass reuses a parent's behavior — class Admin < User), Polymorphism (different objects respond to the same method their own way — duck typing, or overriding). Ruby does all four: encapsulation via private/protected and attr_*; abstraction via classes; inheritance via <; polymorphism via duck typing (no formal interfaces needed) and method overriding. They make code modular, reusable, and changeable."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the the four pillars of OOP and how do they work in Ruby bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: The four pillars: Encapsulation (hide internal state, expose a clean interface — attr_accessor/attr_reader; private methods), Abstraction (model real things as classes with meaningful methods — a User with login, not a hash of data), Inheritance (a subclass reuses a parent's behavior — class Admin < User), Polymorphism (different objects respond to the same method their own way — duck typing, or overriding). Ruby does all four: encapsulation via private/protected and attr_*; abstraction via classes; inheritance via <; polymorphism via duck typing (no formal interfaces needed) and method overriding. They make code modular, reusable, and changeable. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "The four pillars: Encapsulation (hide internal state, expose a clean interface — attr_accessor/attr_reader; private methods), Abstraction (model real things as classes with meaningful methods — a User with login, not a hash of data), Inheritance (a subclass reuses a parent's behavior — class Admin < User), Polymorphism (different objects respond to the same method their own way — duck typing, or overriding).\n\nRuby does all four: encapsulation via private/protected and attr_*; abstraction via classes; inheritance via <; polymorphism via duck typing (no formal interfaces needed) and method overriding.\n\nThey make code modular, reusable, and changeable.\n\nThinking Ruby needs formal interfaces for polymorphism (it uses duck typing), or over-using inheritance where a module fits."
          },
          {
            "type": "code_example",
            "title": "The four pillars in Ruby",
            "content": "```ruby\nclass Animal\n  attr_reader :name                 # encapsulation: expose a reader\n  def initialize(name); @name = name; end\n  def speak; '...' end               # to be overridden (polymorphism)\n  private\n  def digest; end                    # encapsulation: hidden\nend\n\nclass Dog < Animal                  # inheritance: reuse Animal\n  def speak; 'Woof' end              # polymorphism: override speak\nend\n\nclass Cat < Animal\n  def speak; 'Meow' end\nend\n\n# Polymorphism via duck typing (no interface needed):\n[Dog.new('Rex'), Cat.new('Tom')].each { |a| puts a.speak }  # Woof, Meow\n# Any object with #speak works — that's duck typing.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are the four pillars of OOP and how do they work in Ruby?",
        "When would you use Four Pillars in a real project?",
        "How would you compare Four Pillars with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Four Pillars — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Four Pillars? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

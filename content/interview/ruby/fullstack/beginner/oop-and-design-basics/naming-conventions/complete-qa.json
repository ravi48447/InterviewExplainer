{
  "topic": "Naming Conventions",
  "topicSlug": "naming-conventions",
  "questions": [
    {
      "id": "naming-conventions-q001",
      "slug": "ruby-naming-conventions-basics",
      "question": "What are Ruby's naming conventions?",
      "title": "Ruby Naming Conventions",
      "direct_answer": "Ruby conventions: variables and methods are snake_case (user_name, full_name); classes and modules are CamelCase (class User; class AdminUser); constants are SCREAMING_SNAKE_CASE (MAX_RETRIES, VERSION); files are snake_case matching the class (user.rb holds class User; admin_user.rb holds AdminUser). Methods that return a boolean end with ? (empty?, valid?) and methods that mutate end with ! (sort!, save!). The convention is strong in Ruby: everyone follows it, so code reads predictably. RuboCop enforces it. Naming matters: a name like `each_address` reads as English, and the ?/! suffixes tell the reader what the method does at a glance.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know snake_case for vars/methods, CamelCase for classes, SCREAMING for constants, and ?/! suffixes for booleans/mutators.",
        "common_mistake": "Using camelCase for methods (Java style) or sprinkling ! on every method (it means mutate/danger, not emphasis).",
        "to_stand_out": "snake_case (vars/methods), CamelCase (classes), SCREAMING_SNAKE (constants), ? for booleans, ! for mutators. Files match the class. RuboCop enforces it."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define Ruby Naming Conventions simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "Variables and methods are snake_case (user_name, full_name).",
              "Classes and modules are CamelCase (class AdminUser).",
              "Constants are SCREAMING_SNAKE_CASE (MAX_RETRIES, VERSION).",
              "Files are snake_case matching the class (admin_user.rb holds AdminUser).",
              "Boolean methods end with ? (valid?); mutating methods end with ! (sort!)."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "Ruby conventions: variables and methods are snake_case (user_name, full_name); classes and modules are CamelCase (class User; class AdminUser); constants are SCREAMING_SNAKE_CASE (MAX_RETRIES, VERSION); files are snake_case matching the class (user dot rb holds class User, admin_user dot rb holds AdminUser). Methods that return a boolean end with a question mark (empty?, valid?) and methods that mutate end with a bang (sort!, save!). The convention is strong in Ruby: everyone follows it, so code reads predictably, and RuboCop enforces it. Naming matters: a name like each_address reads as English, and the question and bang suffixes tell the reader what the method does at a glance. A common mistake is using camelCase for methods, which is Java style when Ruby uses snake_case, and sprinkling bang on every method, when it means mutate or danger, not emphasis."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Ruby conventions: variables and methods are snake_case (user_name, full_name); classes and modules are CamelCase (class User; class AdminUser); constants are SCREAMING_SNAKE_CASE (MAX_RETRIES, VERSION); files are snake_case matching the class (user.rb holds class User; admin_user.rb holds AdminUser).\n\nMethods that return a boolean end with ?\n\n(empty?, valid?) and methods that mutate end with ! (sort!, save!). The convention is strong in Ruby: everyone follows it, so code reads predictably. RuboCop enforces it. Naming matters: a name like `each_address` reads as English, and the ?/! suffixes tell the reader what the method does at a glance.\n\nA common mistake is using camelCase for methods (Java style) or sprinkling ! on every method (it means mutate/danger, not emphasis)."
          },
          {
            "type": "flow_diagram",
            "title": "Conventions at a Glance",
            "content": "snake_case, CamelCase, SCREAMING, ?, ! — each with a meaning.\n\n```mermaid\nflowchart LR\n  V[vars/methods: snake_case] --> C[classes: CamelCase]\n  C --> K[constants: SCREAMING_SNAKE]\n  K --> Q[booleans: ?]\n  Q --> M[mutators: !]\n```"
          },
          {
            "type": "code_example",
            "title": "Naming conventions",
            "content": "```ruby\n# Variables and methods: snake_case\nuser_name = 'ada'\ndef full_name(first, last); \"#{first} #{last}\"; end\n\n# Classes and modules: CamelCase\nclass AdminUser; end\nmodule AuthHelper; end\n\n# Constants: SCREAMING_SNAKE_CASE\nMAX_RETRIES = 3\nVERSION = '1.0.0'\n\n# Files: snake_case matching the class\n#   app/models/admin_user.rb  -> class AdminUser\n#   app/models/user.rb        -> class User\n\n# Boolean methods end with ?\nuser.valid?       # returns true/false\narr.empty?\n\n# Mutating methods end with ! (danger/mutate)\narr.sort         # new sorted array (original unchanged)\narr.sort!        # sorts IN PLACE (original changed)\nuser.save        # returns true/false\nuser.save!       # raises on failure\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Naming Conventions in a real project?",
        "How would you compare Naming Conventions with an alternative?",
        "What is a common mistake when using Naming Conventions?"
      ],
      "seo": {
        "metaTitle": "Ruby Naming Conventions — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What are Ruby's naming conventions? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "oop-and-design-basics-naming-conventions-q001",
      "slug": "oop-and-design-basics-naming-conventions-when-to-use",
      "question": "When would you use Naming Conventions in a real project?",
      "title": "When would you use Naming Conventions in a real project",
      "direct_answer": "I would use Ruby's naming conventions when the problem matches its main benefit. Ruby conventions: variables and methods are snake_case (user_name, full_name); classes and modules are CamelCase (class User; class AdminUser); constants are SCREAMING_SNAKE_CASE (MAX_RETRIES, VERSION); files are snake_case matching the class (user.rb holds class User; admin_user.rb holds AdminUser). In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain Ruby's naming conventions in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about Ruby's naming conventions without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining Ruby's naming conventions in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. Ruby conventions: variables and methods are snake_case (user_name, full_name); classes and modules are CamelCase (class User; class AdminUser); constants are SCREAMING_SNAKE_CASE (MAX_RETRIES, VERSION); files are snake_case matching the class (user.rb holds class User; admin_user.rb holds AdminUser). Methods that return a boolean end with ? (empty?, valid?) and methods that mutate end with ! (sort!, save!). The convention is strong in Ruby: everyone follows it, so code reads predictably. RuboCop enforces it. Naming matters: a name like `each_address` reads as English, and the ?/! suffixes tell the reader what the method does at a glance."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use Ruby's naming conventions when its rule makes the code safer, clearer, or more efficient for the actual problem. Ruby conventions: variables and methods are snake_case (user_name, full_name); classes and modules are CamelCase (class User; class AdminUser); constants are SCREAMING_SNAKE_CASE (MAX_RETRIES, VERSION); files are snake_case matching the class (user.rb holds class User; admin_user.rb holds AdminUser). Methods that return a boolean end with ? (empty?, valid?) and methods that mutate end with ! (sort!, save!). The convention is strong in Ruby: everyone follows it, so code reads predictably. RuboCop enforces it. Naming matters: a name like `each_address` reads as English, and the ?/! suffixes tell the reader what the method does at a glance. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Ruby conventions: variables and methods are snake_case (user_name, full_name); classes and modules are CamelCase (class User; class AdminUser); constants are SCREAMING_SNAKE_CASE (MAX_RETRIES, VERSION); files are snake_case matching the class (user.rb holds class User; admin_user.rb holds AdminUser).\n\nMethods that return a boolean end with ?\n\n(empty?, valid?) and methods that mutate end with ! (sort!, save!). The convention is strong in Ruby: everyone follows it, so code reads predictably. RuboCop enforces it. Naming matters: a name like `each_address` reads as English, and the ?/! suffixes tell the reader what the method does at a glance.\n\nA common mistake is using camelCase for methods (Java style) or sprinkling ! on every method (it means mutate/danger, not emphasis)."
          },
          {
            "type": "code_example",
            "title": "Naming conventions",
            "content": "```ruby\n# Variables and methods: snake_case\nuser_name = 'ada'\ndef full_name(first, last); \"#{first} #{last}\"; end\n\n# Classes and modules: CamelCase\nclass AdminUser; end\nmodule AuthHelper; end\n\n# Constants: SCREAMING_SNAKE_CASE\nMAX_RETRIES = 3\nVERSION = '1.0.0'\n\n# Files: snake_case matching the class\n#   app/models/admin_user.rb  -> class AdminUser\n#   app/models/user.rb        -> class User\n\n# Boolean methods end with ?\nuser.valid?       # returns true/false\narr.empty?\n\n# Mutating methods end with ! (danger/mutate)\narr.sort         # new sorted array (original unchanged)\narr.sort!        # sorts IN PLACE (original changed)\nuser.save        # returns true/false\nuser.save!       # raises on failure\n```"
          }
        ]
      },
      "followup_questions": [
        "What are Ruby's naming conventions?",
        "How would you compare Naming Conventions with an alternative?",
        "What is a common mistake when using Naming Conventions?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Naming Conventions in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Naming Conventions in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "oop-and-design-basics-naming-conventions-q002",
      "slug": "oop-and-design-basics-naming-conventions-common-mistake",
      "question": "What is a common mistake when using Naming Conventions?",
      "title": "a common mistake when using Naming Conventions",
      "direct_answer": "A common mistake with Ruby's naming conventions is using the rule without checking its boundary conditions. Ruby conventions: variables and methods are snake_case (user_name, full_name); classes and modules are CamelCase (class User; class AdminUser); constants are SCREAMING_SNAKE_CASE (MAX_RETRIES, VERSION); files are snake_case matching the class (user.rb holds class User; admin_user.rb holds AdminUser). That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain Ruby's naming conventions in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about Ruby's naming conventions without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of Ruby's naming conventions. Ruby conventions: variables and methods are snake_case (user_name, full_name); classes and modules are CamelCase (class User; class AdminUser); constants are SCREAMING_SNAKE_CASE (MAX_RETRIES, VERSION); files are snake_case matching the class (user.rb holds class User; admin_user.rb holds AdminUser). Methods that return a boolean end with ? (empty?, valid?) and methods that mutate end with ! (sort!, save!). The convention is strong in Ruby: everyone follows it, so code reads predictably. RuboCop enforces it. Naming matters: a name like `each_address` reads as English, and the ?/! suffixes tell the reader what the method does at a glance."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with Ruby's naming conventions is assuming the happy path is the whole rule. Ruby conventions: variables and methods are snake_case (user_name, full_name); classes and modules are CamelCase (class User; class AdminUser); constants are SCREAMING_SNAKE_CASE (MAX_RETRIES, VERSION); files are snake_case matching the class (user.rb holds class User; admin_user.rb holds AdminUser). Methods that return a boolean end with ? (empty?, valid?) and methods that mutate end with ! (sort!, save!). The convention is strong in Ruby: everyone follows it, so code reads predictably. RuboCop enforces it. Naming matters: a name like `each_address` reads as English, and the ?/! suffixes tell the reader what the method does at a glance. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "Ruby conventions: variables and methods are snake_case (user_name, full_name); classes and modules are CamelCase (class User; class AdminUser); constants are SCREAMING_SNAKE_CASE (MAX_RETRIES, VERSION); files are snake_case matching the class (user.rb holds class User; admin_user.rb holds AdminUser).\n\nMethods that return a boolean end with ?\n\n(empty?, valid?) and methods that mutate end with ! (sort!, save!). The convention is strong in Ruby: everyone follows it, so code reads predictably. RuboCop enforces it. Naming matters: a name like `each_address` reads as English, and the ?/! suffixes tell the reader what the method does at a glance.\n\nA common mistake is using camelCase for methods (Java style) or sprinkling ! on every method (it means mutate/danger, not emphasis)."
          },
          {
            "type": "code_example",
            "title": "Naming conventions",
            "content": "```ruby\n# Variables and methods: snake_case\nuser_name = 'ada'\ndef full_name(first, last); \"#{first} #{last}\"; end\n\n# Classes and modules: CamelCase\nclass AdminUser; end\nmodule AuthHelper; end\n\n# Constants: SCREAMING_SNAKE_CASE\nMAX_RETRIES = 3\nVERSION = '1.0.0'\n\n# Files: snake_case matching the class\n#   app/models/admin_user.rb  -> class AdminUser\n#   app/models/user.rb        -> class User\n\n# Boolean methods end with ?\nuser.valid?       # returns true/false\narr.empty?\n\n# Mutating methods end with ! (danger/mutate)\narr.sort         # new sorted array (original unchanged)\narr.sort!        # sorts IN PLACE (original changed)\nuser.save        # returns true/false\nuser.save!       # raises on failure\n```"
          }
        ]
      },
      "followup_questions": [
        "What are Ruby's naming conventions?",
        "When would you use Naming Conventions in a real project?",
        "How would you compare Naming Conventions with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Naming Conventions — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Naming Conventions? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "oop-and-design-basics-naming-conventions-q003",
      "slug": "oop-and-design-basics-naming-conventions-compare",
      "question": "How would you compare Naming Conventions with an alternative?",
      "title": "compare Naming Conventions with an alternative",
      "direct_answer": "I compare Ruby's naming conventions with the simpler alternative by looking at correctness first, then time, memory, and readability. Ruby conventions: variables and methods are snake_case (user_name, full_name); classes and modules are CamelCase (class User; class AdminUser); constants are SCREAMING_SNAKE_CASE (MAX_RETRIES, VERSION); files are snake_case matching the class (user.rb holds class User; admin_user.rb holds AdminUser). The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain Ruby's naming conventions in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about Ruby's naming conventions without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. Ruby conventions: variables and methods are snake_case (user_name, full_name); classes and modules are CamelCase (class User; class AdminUser); constants are SCREAMING_SNAKE_CASE (MAX_RETRIES, VERSION); files are snake_case matching the class (user.rb holds class User; admin_user.rb holds AdminUser). Methods that return a boolean end with ? (empty?, valid?) and methods that mutate end with ! (sort!, save!). The convention is strong in Ruby: everyone follows it, so code reads predictably. RuboCop enforces it. Naming matters: a name like `each_address` reads as English, and the ?/! suffixes tell the reader what the method does at a glance."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare Ruby's naming conventions with the straightforward alternative on the same example. Ruby conventions: variables and methods are snake_case (user_name, full_name); classes and modules are CamelCase (class User; class AdminUser); constants are SCREAMING_SNAKE_CASE (MAX_RETRIES, VERSION); files are snake_case matching the class (user.rb holds class User; admin_user.rb holds AdminUser). Methods that return a boolean end with ? (empty?, valid?) and methods that mutate end with ! (sort!, save!). The convention is strong in Ruby: everyone follows it, so code reads predictably. RuboCop enforces it. Naming matters: a name like `each_address` reads as English, and the ?/! suffixes tell the reader what the method does at a glance. If Ruby's naming conventions removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "Ruby conventions: variables and methods are snake_case (user_name, full_name); classes and modules are CamelCase (class User; class AdminUser); constants are SCREAMING_SNAKE_CASE (MAX_RETRIES, VERSION); files are snake_case matching the class (user.rb holds class User; admin_user.rb holds AdminUser).\n\nMethods that return a boolean end with ?\n\n(empty?, valid?) and methods that mutate end with ! (sort!, save!). The convention is strong in Ruby: everyone follows it, so code reads predictably. RuboCop enforces it. Naming matters: a name like `each_address` reads as English, and the ?/! suffixes tell the reader what the method does at a glance.\n\nA common mistake is using camelCase for methods (Java style) or sprinkling ! on every method (it means mutate/danger, not emphasis)."
          },
          {
            "type": "code_example",
            "title": "Naming conventions",
            "content": "```ruby\n# Variables and methods: snake_case\nuser_name = 'ada'\ndef full_name(first, last); \"#{first} #{last}\"; end\n\n# Classes and modules: CamelCase\nclass AdminUser; end\nmodule AuthHelper; end\n\n# Constants: SCREAMING_SNAKE_CASE\nMAX_RETRIES = 3\nVERSION = '1.0.0'\n\n# Files: snake_case matching the class\n#   app/models/admin_user.rb  -> class AdminUser\n#   app/models/user.rb        -> class User\n\n# Boolean methods end with ?\nuser.valid?       # returns true/false\narr.empty?\n\n# Mutating methods end with ! (danger/mutate)\narr.sort         # new sorted array (original unchanged)\narr.sort!        # sorts IN PLACE (original changed)\nuser.save        # returns true/false\nuser.save!       # raises on failure\n```"
          }
        ]
      },
      "followup_questions": [
        "What are Ruby's naming conventions?",
        "When would you use Naming Conventions in a real project?",
        "What is a common mistake when using Naming Conventions?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Naming Conventions with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Naming Conventions with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "oop-and-design-basics-naming-conventions-q004",
      "slug": "oop-and-design-basics-naming-conventions-scenario",
      "question": "How would you debug a problem involving Naming Conventions?",
      "title": "debug a problem involving Naming Conventions",
      "direct_answer": "I would debug a problem involving Ruby's naming conventions by reproducing it with the smallest input first. Ruby conventions: variables and methods are snake_case (user_name, full_name); classes and modules are CamelCase (class User; class AdminUser); constants are SCREAMING_SNAKE_CASE (MAX_RETRIES, VERSION); files are snake_case matching the class (user.rb holds class User; admin_user.rb holds AdminUser). Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain Ruby's naming conventions in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about Ruby's naming conventions without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what Ruby's naming conventions is supposed to guarantee. Ruby conventions: variables and methods are snake_case (user_name, full_name); classes and modules are CamelCase (class User; class AdminUser); constants are SCREAMING_SNAKE_CASE (MAX_RETRIES, VERSION); files are snake_case matching the class (user.rb holds class User; admin_user.rb holds AdminUser). Methods that return a boolean end with ? (empty?, valid?) and methods that mutate end with ! (sort!, save!). The convention is strong in Ruby: everyone follows it, so code reads predictably. RuboCop enforces it. Naming matters: a name like `each_address` reads as English, and the ?/! suffixes tell the reader what the method does at a glance."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the Ruby's naming conventions bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: Ruby conventions: variables and methods are snake_case (user_name, full_name); classes and modules are CamelCase (class User; class AdminUser); constants are SCREAMING_SNAKE_CASE (MAX_RETRIES, VERSION); files are snake_case matching the class (user.rb holds class User; admin_user.rb holds AdminUser). Methods that return a boolean end with ? (empty?, valid?) and methods that mutate end with ! (sort!, save!). The convention is strong in Ruby: everyone follows it, so code reads predictably. RuboCop enforces it. Naming matters: a name like `each_address` reads as English, and the ?/! suffixes tell the reader what the method does at a glance. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "Ruby conventions: variables and methods are snake_case (user_name, full_name); classes and modules are CamelCase (class User; class AdminUser); constants are SCREAMING_SNAKE_CASE (MAX_RETRIES, VERSION); files are snake_case matching the class (user.rb holds class User; admin_user.rb holds AdminUser).\n\nMethods that return a boolean end with ?\n\n(empty?, valid?) and methods that mutate end with ! (sort!, save!). The convention is strong in Ruby: everyone follows it, so code reads predictably. RuboCop enforces it. Naming matters: a name like `each_address` reads as English, and the ?/! suffixes tell the reader what the method does at a glance.\n\nA common mistake is using camelCase for methods (Java style) or sprinkling ! on every method (it means mutate/danger, not emphasis)."
          },
          {
            "type": "code_example",
            "title": "Naming conventions",
            "content": "```ruby\n# Variables and methods: snake_case\nuser_name = 'ada'\ndef full_name(first, last); \"#{first} #{last}\"; end\n\n# Classes and modules: CamelCase\nclass AdminUser; end\nmodule AuthHelper; end\n\n# Constants: SCREAMING_SNAKE_CASE\nMAX_RETRIES = 3\nVERSION = '1.0.0'\n\n# Files: snake_case matching the class\n#   app/models/admin_user.rb  -> class AdminUser\n#   app/models/user.rb        -> class User\n\n# Boolean methods end with ?\nuser.valid?       # returns true/false\narr.empty?\n\n# Mutating methods end with ! (danger/mutate)\narr.sort         # new sorted array (original unchanged)\narr.sort!        # sorts IN PLACE (original changed)\nuser.save        # returns true/false\nuser.save!       # raises on failure\n```"
          }
        ]
      },
      "followup_questions": [
        "What are Ruby's naming conventions?",
        "When would you use Naming Conventions in a real project?",
        "How would you compare Naming Conventions with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Naming Conventions — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Naming Conventions? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

{
  "topic": "Controllers Basics",
  "topicSlug": "controllers-basics",
  "questions": [
    {
      "id": "controllers-basics-q001",
      "slug": "rails-controllers-basics",
      "question": "What does a Rails controller do?",
      "title": "Rails Controllers",
      "direct_answer": "A controller is the traffic cop of MVC: it receives a request (with params), asks the model for data, and renders a response. A controller is a class inheriting from ApplicationController, with one method per action (index, show, create); each action sets up instance variables (@post = Post.find(params[:id])) and renders (explicitly or by convention — render :show by default). Controllers hold NO business logic — that lives in models or service objects; the controller orchestrates (find, authorize, call a service, render). Strong params (params.require(:post).permit(:title)) guard mass-assignment. Keep controllers thin: find, authorize, delegate, render.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know a controller receives params, talks to models, and renders; that actions are methods; and that controllers should be thin (no business logic).",
        "common_mistake": "Putting business logic in controllers (they should delegate to models/services), or forgetting strong params (mass-assignment risk).",
        "to_stand_out": "A controller receives params, asks models for data, and renders. Actions are methods. Controllers are thin: find, authorize, delegate, render. Strong params guard mass-assignment."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define Rails Controllers simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "A controller receives a request (params), asks models for data, and renders a response.",
              "Actions are methods (index, show, create); @vars are passed to the view.",
              "Controllers are thin: find, authorize, delegate to a model/service, render.",
              "Strong params (params.require(:post).permit(:title)) guard mass-assignment.",
              "render is explicit (render json:) or by convention (render :show by default)."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "A controller is the traffic cop of MVC: it receives a request (with params), asks the model for data, and renders a response. A controller is a class inheriting from ApplicationController, with one method per action (index, show, create); each action sets up instance variables (@post = Post.find params id) and renders, explicitly or by convention — render show by default. Controllers hold NO business logic — that lives in models or service objects; the controller orchestrates: find, authorize, call a service, render. Strong params (params dot require post dot permit title) guard mass-assignment. Keep controllers thin: find, authorize, delegate, render. A common mistake is putting business logic in controllers, when they should delegate to models and services, and forgetting strong params, which is a mass-assignment risk."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "A controller is the traffic cop of MVC: it receives a request (with params), asks the model for data, and renders a response.\n\nA controller is a class inheriting from ApplicationController, with one method per action (index, show, create); each action sets up instance variables (@post = Post.find(params[:id])) and renders (explicitly or by convention — render :show by default).\n\nControllers hold NO business logic — that lives in models or service objects; the controller orchestrates (find, authorize, call a service, render). Strong params (params.require(:post).permit(:title)) guard mass-assignment. Keep controllers thin: find, authorize, delegate, render.\n\nPutting business logic in controllers (they should delegate to models/services), or forgetting strong params (mass-assignment risk)."
          },
          {
            "type": "flow_diagram",
            "title": "Request -> Controller -> Response",
            "content": "A controller receives params, finds models, and renders.\n\n```mermaid\nflowchart LR\n  R[request + params] --> A[action: index/show/create]\n  A --> M[find/authorize: @post = Post.find]\n  M --> De[delegate to model/service]\n  De --> Re[render json / render :show]\n```"
          },
          {
            "type": "code_example",
            "title": "A Rails controller",
            "content": "```ruby\nclass PostsController < ApplicationController\n  # GET /posts (params: ?page=)\n  def index\n    @posts = Post.page(params[:page])   # find (model)\n    # render :index by convention (HTML) OR:\n    render json: @posts                  # API\n  end\n\n  # GET /posts/:id\n  def show\n    @post = Post.find(params[:id])       # find\n    # render :show by convention\n  end\n\n  # POST /posts (params: { post: { title, body } })\n  def create\n    @post = Post.create!(post_params)    # delegate to model\n    redirect_to @post                    # render: redirect\n  end\n\n  private\n  # Strong params: guard mass-assignment\n  def post_params\n    params.require(:post).permit(:title, :body)\n  end\nend\n# Controllers are THIN: find, authorize, delegate, render. No business logic.\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Controllers Basics in a real project?",
        "How would you compare Controllers Basics with an alternative?",
        "What is a common mistake when using Controllers Basics?"
      ],
      "seo": {
        "metaTitle": "Rails Controllers — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What does a Rails controller do? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "rails-basics-controllers-basics-q001",
      "slug": "rails-basics-controllers-basics-when-to-use",
      "question": "When would you use Controllers Basics in a real project?",
      "title": "When would you use Controllers Basics in a real project",
      "direct_answer": "I would use What does a Rails controller do when the problem matches its main benefit. A controller is the traffic cop of MVC: it receives a request (with params), asks the model for data, and renders a response. In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain What does a Rails controller do in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about What does a Rails controller do without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining What does a Rails controller do in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. A controller is the traffic cop of MVC: it receives a request (with params), asks the model for data, and renders a response. A controller is a class inheriting from ApplicationController, with one method per action (index, show, create); each action sets up instance variables (@post = Post.find(params[:id])) and renders (explicitly or by convention — render :show by default). Controllers hold NO business logic — that lives in models or service objects; the controller orchestrates (find, authorize, call a service, render). Strong params (params.require(:post).permit(:title)) guard mass-assignment. Keep controllers thin: find, authorize, delegate, render."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use What does a Rails controller do when its rule makes the code safer, clearer, or more efficient for the actual problem. A controller is the traffic cop of MVC: it receives a request (with params), asks the model for data, and renders a response. A controller is a class inheriting from ApplicationController, with one method per action (index, show, create); each action sets up instance variables (@post = Post.find(params[:id])) and renders (explicitly or by convention — render :show by default). Controllers hold NO business logic — that lives in models or service objects; the controller orchestrates (find, authorize, call a service, render). Strong params (params.require(:post).permit(:title)) guard mass-assignment. Keep controllers thin: find, authorize, delegate, render. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "A controller is the traffic cop of MVC: it receives a request (with params), asks the model for data, and renders a response.\n\nA controller is a class inheriting from ApplicationController, with one method per action (index, show, create); each action sets up instance variables (@post = Post.find(params[:id])) and renders (explicitly or by convention — render :show by default).\n\nControllers hold NO business logic — that lives in models or service objects; the controller orchestrates (find, authorize, call a service, render). Strong params (params.require(:post).permit(:title)) guard mass-assignment. Keep controllers thin: find, authorize, delegate, render.\n\nPutting business logic in controllers (they should delegate to models/services), or forgetting strong params (mass-assignment risk)."
          },
          {
            "type": "code_example",
            "title": "A Rails controller",
            "content": "```ruby\nclass PostsController < ApplicationController\n  # GET /posts (params: ?page=)\n  def index\n    @posts = Post.page(params[:page])   # find (model)\n    # render :index by convention (HTML) OR:\n    render json: @posts                  # API\n  end\n\n  # GET /posts/:id\n  def show\n    @post = Post.find(params[:id])       # find\n    # render :show by convention\n  end\n\n  # POST /posts (params: { post: { title, body } })\n  def create\n    @post = Post.create!(post_params)    # delegate to model\n    redirect_to @post                    # render: redirect\n  end\n\n  private\n  # Strong params: guard mass-assignment\n  def post_params\n    params.require(:post).permit(:title, :body)\n  end\nend\n# Controllers are THIN: find, authorize, delegate, render. No business logic.\n```"
          }
        ]
      },
      "followup_questions": [
        "What does a Rails controller do?",
        "How would you compare Controllers Basics with an alternative?",
        "What is a common mistake when using Controllers Basics?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Controllers Basics in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Controllers Basics in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "rails-basics-controllers-basics-q002",
      "slug": "rails-basics-controllers-basics-common-mistake",
      "question": "What is a common mistake when using Controllers Basics?",
      "title": "a common mistake when using Controllers Basics",
      "direct_answer": "A common mistake with What does a Rails controller do is using the rule without checking its boundary conditions. A controller is the traffic cop of MVC: it receives a request (with params), asks the model for data, and renders a response. That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain What does a Rails controller do in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about What does a Rails controller do without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of What does a Rails controller do. A controller is the traffic cop of MVC: it receives a request (with params), asks the model for data, and renders a response. A controller is a class inheriting from ApplicationController, with one method per action (index, show, create); each action sets up instance variables (@post = Post.find(params[:id])) and renders (explicitly or by convention — render :show by default). Controllers hold NO business logic — that lives in models or service objects; the controller orchestrates (find, authorize, call a service, render). Strong params (params.require(:post).permit(:title)) guard mass-assignment. Keep controllers thin: find, authorize, delegate, render."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with What does a Rails controller do is assuming the happy path is the whole rule. A controller is the traffic cop of MVC: it receives a request (with params), asks the model for data, and renders a response. A controller is a class inheriting from ApplicationController, with one method per action (index, show, create); each action sets up instance variables (@post = Post.find(params[:id])) and renders (explicitly or by convention — render :show by default). Controllers hold NO business logic — that lives in models or service objects; the controller orchestrates (find, authorize, call a service, render). Strong params (params.require(:post).permit(:title)) guard mass-assignment. Keep controllers thin: find, authorize, delegate, render. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "A controller is the traffic cop of MVC: it receives a request (with params), asks the model for data, and renders a response.\n\nA controller is a class inheriting from ApplicationController, with one method per action (index, show, create); each action sets up instance variables (@post = Post.find(params[:id])) and renders (explicitly or by convention — render :show by default).\n\nControllers hold NO business logic — that lives in models or service objects; the controller orchestrates (find, authorize, call a service, render). Strong params (params.require(:post).permit(:title)) guard mass-assignment. Keep controllers thin: find, authorize, delegate, render.\n\nPutting business logic in controllers (they should delegate to models/services), or forgetting strong params (mass-assignment risk)."
          },
          {
            "type": "code_example",
            "title": "A Rails controller",
            "content": "```ruby\nclass PostsController < ApplicationController\n  # GET /posts (params: ?page=)\n  def index\n    @posts = Post.page(params[:page])   # find (model)\n    # render :index by convention (HTML) OR:\n    render json: @posts                  # API\n  end\n\n  # GET /posts/:id\n  def show\n    @post = Post.find(params[:id])       # find\n    # render :show by convention\n  end\n\n  # POST /posts (params: { post: { title, body } })\n  def create\n    @post = Post.create!(post_params)    # delegate to model\n    redirect_to @post                    # render: redirect\n  end\n\n  private\n  # Strong params: guard mass-assignment\n  def post_params\n    params.require(:post).permit(:title, :body)\n  end\nend\n# Controllers are THIN: find, authorize, delegate, render. No business logic.\n```"
          }
        ]
      },
      "followup_questions": [
        "What does a Rails controller do?",
        "When would you use Controllers Basics in a real project?",
        "How would you compare Controllers Basics with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Controllers Basics — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Controllers Basics? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "rails-basics-controllers-basics-q003",
      "slug": "rails-basics-controllers-basics-compare",
      "question": "How would you compare Controllers Basics with an alternative?",
      "title": "compare Controllers Basics with an alternative",
      "direct_answer": "I compare What does a Rails controller do with the simpler alternative by looking at correctness first, then time, memory, and readability. A controller is the traffic cop of MVC: it receives a request (with params), asks the model for data, and renders a response. The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain What does a Rails controller do in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about What does a Rails controller do without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. A controller is the traffic cop of MVC: it receives a request (with params), asks the model for data, and renders a response. A controller is a class inheriting from ApplicationController, with one method per action (index, show, create); each action sets up instance variables (@post = Post.find(params[:id])) and renders (explicitly or by convention — render :show by default). Controllers hold NO business logic — that lives in models or service objects; the controller orchestrates (find, authorize, call a service, render). Strong params (params.require(:post).permit(:title)) guard mass-assignment. Keep controllers thin: find, authorize, delegate, render."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare What does a Rails controller do with the straightforward alternative on the same example. A controller is the traffic cop of MVC: it receives a request (with params), asks the model for data, and renders a response. A controller is a class inheriting from ApplicationController, with one method per action (index, show, create); each action sets up instance variables (@post = Post.find(params[:id])) and renders (explicitly or by convention — render :show by default). Controllers hold NO business logic — that lives in models or service objects; the controller orchestrates (find, authorize, call a service, render). Strong params (params.require(:post).permit(:title)) guard mass-assignment. Keep controllers thin: find, authorize, delegate, render. If What does a Rails controller do removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "A controller is the traffic cop of MVC: it receives a request (with params), asks the model for data, and renders a response.\n\nA controller is a class inheriting from ApplicationController, with one method per action (index, show, create); each action sets up instance variables (@post = Post.find(params[:id])) and renders (explicitly or by convention — render :show by default).\n\nControllers hold NO business logic — that lives in models or service objects; the controller orchestrates (find, authorize, call a service, render). Strong params (params.require(:post).permit(:title)) guard mass-assignment. Keep controllers thin: find, authorize, delegate, render.\n\nPutting business logic in controllers (they should delegate to models/services), or forgetting strong params (mass-assignment risk)."
          },
          {
            "type": "code_example",
            "title": "A Rails controller",
            "content": "```ruby\nclass PostsController < ApplicationController\n  # GET /posts (params: ?page=)\n  def index\n    @posts = Post.page(params[:page])   # find (model)\n    # render :index by convention (HTML) OR:\n    render json: @posts                  # API\n  end\n\n  # GET /posts/:id\n  def show\n    @post = Post.find(params[:id])       # find\n    # render :show by convention\n  end\n\n  # POST /posts (params: { post: { title, body } })\n  def create\n    @post = Post.create!(post_params)    # delegate to model\n    redirect_to @post                    # render: redirect\n  end\n\n  private\n  # Strong params: guard mass-assignment\n  def post_params\n    params.require(:post).permit(:title, :body)\n  end\nend\n# Controllers are THIN: find, authorize, delegate, render. No business logic.\n```"
          }
        ]
      },
      "followup_questions": [
        "What does a Rails controller do?",
        "When would you use Controllers Basics in a real project?",
        "What is a common mistake when using Controllers Basics?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Controllers Basics with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Controllers Basics with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "rails-basics-controllers-basics-q004",
      "slug": "rails-basics-controllers-basics-scenario",
      "question": "How would you debug a problem involving Controllers Basics?",
      "title": "debug a problem involving Controllers Basics",
      "direct_answer": "I would debug a problem involving What does a Rails controller do by reproducing it with the smallest input first. A controller is the traffic cop of MVC: it receives a request (with params), asks the model for data, and renders a response. Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain What does a Rails controller do in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about What does a Rails controller do without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what What does a Rails controller do is supposed to guarantee. A controller is the traffic cop of MVC: it receives a request (with params), asks the model for data, and renders a response. A controller is a class inheriting from ApplicationController, with one method per action (index, show, create); each action sets up instance variables (@post = Post.find(params[:id])) and renders (explicitly or by convention — render :show by default). Controllers hold NO business logic — that lives in models or service objects; the controller orchestrates (find, authorize, call a service, render). Strong params (params.require(:post).permit(:title)) guard mass-assignment. Keep controllers thin: find, authorize, delegate, render."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the What does a Rails controller do bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: A controller is the traffic cop of MVC: it receives a request (with params), asks the model for data, and renders a response. A controller is a class inheriting from ApplicationController, with one method per action (index, show, create); each action sets up instance variables (@post = Post.find(params[:id])) and renders (explicitly or by convention — render :show by default). Controllers hold NO business logic — that lives in models or service objects; the controller orchestrates (find, authorize, call a service, render). Strong params (params.require(:post).permit(:title)) guard mass-assignment. Keep controllers thin: find, authorize, delegate, render. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "A controller is the traffic cop of MVC: it receives a request (with params), asks the model for data, and renders a response.\n\nA controller is a class inheriting from ApplicationController, with one method per action (index, show, create); each action sets up instance variables (@post = Post.find(params[:id])) and renders (explicitly or by convention — render :show by default).\n\nControllers hold NO business logic — that lives in models or service objects; the controller orchestrates (find, authorize, call a service, render). Strong params (params.require(:post).permit(:title)) guard mass-assignment. Keep controllers thin: find, authorize, delegate, render.\n\nPutting business logic in controllers (they should delegate to models/services), or forgetting strong params (mass-assignment risk)."
          },
          {
            "type": "code_example",
            "title": "A Rails controller",
            "content": "```ruby\nclass PostsController < ApplicationController\n  # GET /posts (params: ?page=)\n  def index\n    @posts = Post.page(params[:page])   # find (model)\n    # render :index by convention (HTML) OR:\n    render json: @posts                  # API\n  end\n\n  # GET /posts/:id\n  def show\n    @post = Post.find(params[:id])       # find\n    # render :show by convention\n  end\n\n  # POST /posts (params: { post: { title, body } })\n  def create\n    @post = Post.create!(post_params)    # delegate to model\n    redirect_to @post                    # render: redirect\n  end\n\n  private\n  # Strong params: guard mass-assignment\n  def post_params\n    params.require(:post).permit(:title, :body)\n  end\nend\n# Controllers are THIN: find, authorize, delegate, render. No business logic.\n```"
          }
        ]
      },
      "followup_questions": [
        "What does a Rails controller do?",
        "When would you use Controllers Basics in a real project?",
        "How would you compare Controllers Basics with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Controllers Basics — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Controllers Basics? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

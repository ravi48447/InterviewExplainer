{
  "topic": "Control Flow",
  "topicSlug": "control-flow",
  "questions": [
    {
      "id": "control-flow-q001",
      "slug": "ruby-core-control-flow",
      "question": "How does control flow work in Ruby?",
      "title": "Control Flow in Ruby",
      "direct_answer": "Conditionals: if/elsif/else/end, unless (= if not), case/when (case x; when 1..5 then 'small'; when String then 'str'; else 'other'; end), and the modifier form (puts 'hi' if verbose). Loops: while, until, but Ruby prefers iterators — 10.times { |i| ... }, array.each { |x| ... }, 1.upto(5) { |n| ... }, map, select. break exits a loop; next skips to the next iteration; redo retries the current; retry (in rescue) re-runs the block. return exits the method (implicit return = last expression). Exceptions: raise throws, begin/rescue/else/ensure/end catches (rescue StandardError => e), ensure always runs. Use iterators over while counters; use case/when over long if/elsif chains; use rescue for expected errors, not normal flow.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know if/unless/case, that Ruby prefers iterators over while, and break/next/redo plus raise/rescue/ensure.",
        "common_mistake": "Using a while counter when each/times would do, or rescue for normal control flow (it's for errors).",
        "to_stand_out": "Control flow: if/elsif/else, unless, case/when; iterators (each/times/upto) over while; break/next/redo; raise/rescue/else/ensure."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define Control Flow in Ruby simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "Conditionals: if/elsif/else, unless (= if not), case/when, modifier form.",
              "Loops: while/until, but Ruby prefers iterators (each/times/upto/map).",
              "break exits; next skips; redo retries the current; retry (in rescue) re-runs.",
              "return exits the method; implicit return = the last expression.",
              "raise throws; begin/rescue/else/ensure/end catches; ensure always runs."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "Conditionals: if, elsif, else, end, unless (equals if not), case when (case x; when 1 to 5 then small; when String then str; else other; end), and the modifier form (puts hi if verbose). Loops: while, until, but Ruby prefers iterators — 10 dot times, array dot each, 1 dot upto 5, map, select. break exits a loop; next skips to the next iteration; redo retries the current; retry (in rescue) re-runs the block. return exits the method (implicit return equals last expression). Exceptions: raise throws, begin rescue else ensure end catches (rescue StandardError e), ensure always runs. Use iterators over while counters; use case when over long if elsif chains; use rescue for expected errors, not normal flow. A common mistake is using a while counter when each or times would do, and using rescue for normal control flow, when it's for expected errors."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Conditionals: if/elsif/else/end, unless (= if not), case/when (case x; when 1..5 then 'small'; when String then 'str'; else 'other'; end), and the modifier form (puts 'hi' if verbose).\n\nLoops: while, until, but Ruby prefers iterators — 10.times { |i| ... }, array.each { |x| ... }, 1.upto(5) { |n| ... }, map, select.\n\nbreak exits a loop; next skips to the next iteration; redo retries the current; retry (in rescue) re-runs the block. return exits the method (implicit return = last expression). Exceptions: raise throws, begin/rescue/else/ensure/end catches (rescue StandardError => e), ensure always runs. Use iterators over while counters; use case/when over long if/elsif chains; use rescue for expected errors, not normal flow.\n\nA common mistake is using a while counter when each/times would do, or rescue for normal control flow (it's for errors)."
          },
          {
            "type": "flow_diagram",
            "title": "Control Flow",
            "content": "if/unless/case; iterators over while; break/next/redo; raise/rescue/ensure.\n\n```mermaid\nflowchart LR\n  C[conditionals] --> I[if/elsif/else]\n  C --> Ca[case/when]\n  L[loops] --> It[iterators each/times]\n  L --> B[break/next/redo]\n  E[errors] --> R[raise/rescue/ensure]\n```"
          },
          {
            "type": "code_example",
            "title": "Control flow",
            "content": "```ruby\n# Conditionals\nif score > 90; 'A'; elsif score > 80; 'B'; else; 'C'; end\nputs 'done' unless running        # unless = if NOT\nputs 'hi' if verbose               # modifier form\ncase x\nwhen 1..5     then 'small'\nwhen String   then 'string'\nelse 'other'\nend\n\n# Loops (prefer ITERATORS over while)\n3.times { |i| puts i }             # 0 1 2\n[10, 20].each { |x| puts x }       # 10 20\n1.upto(5) { |n| puts n }           # 1..5\nnums.map { |n| n * 2 }             # transform\n\n# while/until (less idiomatic):\ni = 0\nwhile i < 3; puts i; i += 1; end\n\n# break (exit), next (skip), redo (retry current)\n[1,2,3].each do |n|\n  next if n == 2      # skip 2\n  break if n > 10     # stop\n  redo                 # retry this iteration (careful!)\nend\n\n# Exceptions\nbegin\n  risky\nrescue StandardError => e\n  log(e.message)\nelse\n  puts 'no error'\nensure\n  cleanup             # ALWAYS runs\nend\n\n# raise throws:  raise 'bad input'\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Control Flow in a real project?",
        "How would you compare Control Flow with an alternative?",
        "What is a common mistake when using Control Flow?"
      ],
      "seo": {
        "metaTitle": "Control Flow in Ruby — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How does control flow work in Ruby? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "ruby-language-core-basics-control-flow-q001",
      "slug": "ruby-language-core-basics-control-flow-when-to-use",
      "question": "When would you use Control Flow in a real project?",
      "title": "When would you use Control Flow in a real project",
      "direct_answer": "I would use How does control flow work in Ruby when the problem matches its main benefit. Conditionals: if/elsif/else/end, unless (= if not), case/when (case x; when 1..5 then 'small'; when String then 'str'; else 'other'; end), and the modifier form (puts 'hi' if verbose). In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How does control flow work in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How does control flow work in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining How does control flow work in Ruby in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. Conditionals: if/elsif/else/end, unless (= if not), case/when (case x; when 1..5 then 'small'; when String then 'str'; else 'other'; end), and the modifier form (puts 'hi' if verbose). Loops: while, until, but Ruby prefers iterators — 10.times { |i| ... }, array.each { |x| ... }, 1.upto(5) { |n| ... }, map, select. break exits a loop; next skips to the next iteration; redo retries the current; retry (in rescue) re-runs the block. return exits the method (implicit return = last expression). Exceptions: raise throws, begin/rescue/else/ensure/end catches (rescue StandardError => e), ensure always runs. Use iterators over while counters; use case/when over long if/elsif chains; use rescue for expected errors, not normal flow."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use How does control flow work in Ruby when its rule makes the code safer, clearer, or more efficient for the actual problem. Conditionals: if/elsif/else/end, unless (= if not), case/when (case x; when 1..5 then 'small'; when String then 'str'; else 'other'; end), and the modifier form (puts 'hi' if verbose). Loops: while, until, but Ruby prefers iterators — 10.times { |i| ... }, array.each { |x| ... }, 1.upto(5) { |n| ... }, map, select. break exits a loop; next skips to the next iteration; redo retries the current; retry (in rescue) re-runs the block. return exits the method (implicit return = last expression). Exceptions: raise throws, begin/rescue/else/ensure/end catches (rescue StandardError => e), ensure always runs. Use iterators over while counters; use case/when over long if/elsif chains; use rescue for expected errors, not normal flow. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Conditionals: if/elsif/else/end, unless (= if not), case/when (case x; when 1..5 then 'small'; when String then 'str'; else 'other'; end), and the modifier form (puts 'hi' if verbose).\n\nLoops: while, until, but Ruby prefers iterators — 10.times { |i| ... }, array.each { |x| ... }, 1.upto(5) { |n| ... }, map, select.\n\nbreak exits a loop; next skips to the next iteration; redo retries the current; retry (in rescue) re-runs the block. return exits the method (implicit return = last expression). Exceptions: raise throws, begin/rescue/else/ensure/end catches (rescue StandardError => e), ensure always runs. Use iterators over while counters; use case/when over long if/elsif chains; use rescue for expected errors, not normal flow.\n\nA common mistake is using a while counter when each/times would do, or rescue for normal control flow (it's for errors)."
          },
          {
            "type": "code_example",
            "title": "Control flow",
            "content": "```ruby\n# Conditionals\nif score > 90; 'A'; elsif score > 80; 'B'; else; 'C'; end\nputs 'done' unless running        # unless = if NOT\nputs 'hi' if verbose               # modifier form\ncase x\nwhen 1..5     then 'small'\nwhen String   then 'string'\nelse 'other'\nend\n\n# Loops (prefer ITERATORS over while)\n3.times { |i| puts i }             # 0 1 2\n[10, 20].each { |x| puts x }       # 10 20\n1.upto(5) { |n| puts n }           # 1..5\nnums.map { |n| n * 2 }             # transform\n\n# while/until (less idiomatic):\ni = 0\nwhile i < 3; puts i; i += 1; end\n\n# break (exit), next (skip), redo (retry current)\n[1,2,3].each do |n|\n  next if n == 2      # skip 2\n  break if n > 10     # stop\n  redo                 # retry this iteration (careful!)\nend\n\n# Exceptions\nbegin\n  risky\nrescue StandardError => e\n  log(e.message)\nelse\n  puts 'no error'\nensure\n  cleanup             # ALWAYS runs\nend\n\n# raise throws:  raise 'bad input'\n```"
          }
        ]
      },
      "followup_questions": [
        "How does control flow work in Ruby?",
        "How would you compare Control Flow with an alternative?",
        "What is a common mistake when using Control Flow?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Control Flow in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Control Flow in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-language-core-basics-control-flow-q002",
      "slug": "ruby-language-core-basics-control-flow-common-mistake",
      "question": "What is a common mistake when using Control Flow?",
      "title": "a common mistake when using Control Flow",
      "direct_answer": "A common mistake with How does control flow work in Ruby is using the rule without checking its boundary conditions. Conditionals: if/elsif/else/end, unless (= if not), case/when (case x; when 1..5 then 'small'; when String then 'str'; else 'other'; end), and the modifier form (puts 'hi' if verbose). That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How does control flow work in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How does control flow work in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of How does control flow work in Ruby. Conditionals: if/elsif/else/end, unless (= if not), case/when (case x; when 1..5 then 'small'; when String then 'str'; else 'other'; end), and the modifier form (puts 'hi' if verbose). Loops: while, until, but Ruby prefers iterators — 10.times { |i| ... }, array.each { |x| ... }, 1.upto(5) { |n| ... }, map, select. break exits a loop; next skips to the next iteration; redo retries the current; retry (in rescue) re-runs the block. return exits the method (implicit return = last expression). Exceptions: raise throws, begin/rescue/else/ensure/end catches (rescue StandardError => e), ensure always runs. Use iterators over while counters; use case/when over long if/elsif chains; use rescue for expected errors, not normal flow."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with How does control flow work in Ruby is assuming the happy path is the whole rule. Conditionals: if/elsif/else/end, unless (= if not), case/when (case x; when 1..5 then 'small'; when String then 'str'; else 'other'; end), and the modifier form (puts 'hi' if verbose). Loops: while, until, but Ruby prefers iterators — 10.times { |i| ... }, array.each { |x| ... }, 1.upto(5) { |n| ... }, map, select. break exits a loop; next skips to the next iteration; redo retries the current; retry (in rescue) re-runs the block. return exits the method (implicit return = last expression). Exceptions: raise throws, begin/rescue/else/ensure/end catches (rescue StandardError => e), ensure always runs. Use iterators over while counters; use case/when over long if/elsif chains; use rescue for expected errors, not normal flow. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "Conditionals: if/elsif/else/end, unless (= if not), case/when (case x; when 1..5 then 'small'; when String then 'str'; else 'other'; end), and the modifier form (puts 'hi' if verbose).\n\nLoops: while, until, but Ruby prefers iterators — 10.times { |i| ... }, array.each { |x| ... }, 1.upto(5) { |n| ... }, map, select.\n\nbreak exits a loop; next skips to the next iteration; redo retries the current; retry (in rescue) re-runs the block. return exits the method (implicit return = last expression). Exceptions: raise throws, begin/rescue/else/ensure/end catches (rescue StandardError => e), ensure always runs. Use iterators over while counters; use case/when over long if/elsif chains; use rescue for expected errors, not normal flow.\n\nA common mistake is using a while counter when each/times would do, or rescue for normal control flow (it's for errors)."
          },
          {
            "type": "code_example",
            "title": "Control flow",
            "content": "```ruby\n# Conditionals\nif score > 90; 'A'; elsif score > 80; 'B'; else; 'C'; end\nputs 'done' unless running        # unless = if NOT\nputs 'hi' if verbose               # modifier form\ncase x\nwhen 1..5     then 'small'\nwhen String   then 'string'\nelse 'other'\nend\n\n# Loops (prefer ITERATORS over while)\n3.times { |i| puts i }             # 0 1 2\n[10, 20].each { |x| puts x }       # 10 20\n1.upto(5) { |n| puts n }           # 1..5\nnums.map { |n| n * 2 }             # transform\n\n# while/until (less idiomatic):\ni = 0\nwhile i < 3; puts i; i += 1; end\n\n# break (exit), next (skip), redo (retry current)\n[1,2,3].each do |n|\n  next if n == 2      # skip 2\n  break if n > 10     # stop\n  redo                 # retry this iteration (careful!)\nend\n\n# Exceptions\nbegin\n  risky\nrescue StandardError => e\n  log(e.message)\nelse\n  puts 'no error'\nensure\n  cleanup             # ALWAYS runs\nend\n\n# raise throws:  raise 'bad input'\n```"
          }
        ]
      },
      "followup_questions": [
        "How does control flow work in Ruby?",
        "When would you use Control Flow in a real project?",
        "How would you compare Control Flow with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Control Flow — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Control Flow? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-language-core-basics-control-flow-q003",
      "slug": "ruby-language-core-basics-control-flow-compare",
      "question": "How would you compare Control Flow with an alternative?",
      "title": "compare Control Flow with an alternative",
      "direct_answer": "I compare How does control flow work in Ruby with the simpler alternative by looking at correctness first, then time, memory, and readability. Conditionals: if/elsif/else/end, unless (= if not), case/when (case x; when 1..5 then 'small'; when String then 'str'; else 'other'; end), and the modifier form (puts 'hi' if verbose). The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How does control flow work in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How does control flow work in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. Conditionals: if/elsif/else/end, unless (= if not), case/when (case x; when 1..5 then 'small'; when String then 'str'; else 'other'; end), and the modifier form (puts 'hi' if verbose). Loops: while, until, but Ruby prefers iterators — 10.times { |i| ... }, array.each { |x| ... }, 1.upto(5) { |n| ... }, map, select. break exits a loop; next skips to the next iteration; redo retries the current; retry (in rescue) re-runs the block. return exits the method (implicit return = last expression). Exceptions: raise throws, begin/rescue/else/ensure/end catches (rescue StandardError => e), ensure always runs. Use iterators over while counters; use case/when over long if/elsif chains; use rescue for expected errors, not normal flow."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare How does control flow work in Ruby with the straightforward alternative on the same example. Conditionals: if/elsif/else/end, unless (= if not), case/when (case x; when 1..5 then 'small'; when String then 'str'; else 'other'; end), and the modifier form (puts 'hi' if verbose). Loops: while, until, but Ruby prefers iterators — 10.times { |i| ... }, array.each { |x| ... }, 1.upto(5) { |n| ... }, map, select. break exits a loop; next skips to the next iteration; redo retries the current; retry (in rescue) re-runs the block. return exits the method (implicit return = last expression). Exceptions: raise throws, begin/rescue/else/ensure/end catches (rescue StandardError => e), ensure always runs. Use iterators over while counters; use case/when over long if/elsif chains; use rescue for expected errors, not normal flow. If How does control flow work in Ruby removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "Conditionals: if/elsif/else/end, unless (= if not), case/when (case x; when 1..5 then 'small'; when String then 'str'; else 'other'; end), and the modifier form (puts 'hi' if verbose).\n\nLoops: while, until, but Ruby prefers iterators — 10.times { |i| ... }, array.each { |x| ... }, 1.upto(5) { |n| ... }, map, select.\n\nbreak exits a loop; next skips to the next iteration; redo retries the current; retry (in rescue) re-runs the block. return exits the method (implicit return = last expression). Exceptions: raise throws, begin/rescue/else/ensure/end catches (rescue StandardError => e), ensure always runs. Use iterators over while counters; use case/when over long if/elsif chains; use rescue for expected errors, not normal flow.\n\nA common mistake is using a while counter when each/times would do, or rescue for normal control flow (it's for errors)."
          },
          {
            "type": "code_example",
            "title": "Control flow",
            "content": "```ruby\n# Conditionals\nif score > 90; 'A'; elsif score > 80; 'B'; else; 'C'; end\nputs 'done' unless running        # unless = if NOT\nputs 'hi' if verbose               # modifier form\ncase x\nwhen 1..5     then 'small'\nwhen String   then 'string'\nelse 'other'\nend\n\n# Loops (prefer ITERATORS over while)\n3.times { |i| puts i }             # 0 1 2\n[10, 20].each { |x| puts x }       # 10 20\n1.upto(5) { |n| puts n }           # 1..5\nnums.map { |n| n * 2 }             # transform\n\n# while/until (less idiomatic):\ni = 0\nwhile i < 3; puts i; i += 1; end\n\n# break (exit), next (skip), redo (retry current)\n[1,2,3].each do |n|\n  next if n == 2      # skip 2\n  break if n > 10     # stop\n  redo                 # retry this iteration (careful!)\nend\n\n# Exceptions\nbegin\n  risky\nrescue StandardError => e\n  log(e.message)\nelse\n  puts 'no error'\nensure\n  cleanup             # ALWAYS runs\nend\n\n# raise throws:  raise 'bad input'\n```"
          }
        ]
      },
      "followup_questions": [
        "How does control flow work in Ruby?",
        "When would you use Control Flow in a real project?",
        "What is a common mistake when using Control Flow?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Control Flow with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Control Flow with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-language-core-basics-control-flow-q004",
      "slug": "ruby-language-core-basics-control-flow-scenario",
      "question": "How would you debug a problem involving Control Flow?",
      "title": "debug a problem involving Control Flow",
      "direct_answer": "I would debug a problem involving How does control flow work in Ruby by reproducing it with the smallest input first. Conditionals: if/elsif/else/end, unless (= if not), case/when (case x; when 1..5 then 'small'; when String then 'str'; else 'other'; end), and the modifier form (puts 'hi' if verbose). Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How does control flow work in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How does control flow work in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what How does control flow work in Ruby is supposed to guarantee. Conditionals: if/elsif/else/end, unless (= if not), case/when (case x; when 1..5 then 'small'; when String then 'str'; else 'other'; end), and the modifier form (puts 'hi' if verbose). Loops: while, until, but Ruby prefers iterators — 10.times { |i| ... }, array.each { |x| ... }, 1.upto(5) { |n| ... }, map, select. break exits a loop; next skips to the next iteration; redo retries the current; retry (in rescue) re-runs the block. return exits the method (implicit return = last expression). Exceptions: raise throws, begin/rescue/else/ensure/end catches (rescue StandardError => e), ensure always runs. Use iterators over while counters; use case/when over long if/elsif chains; use rescue for expected errors, not normal flow."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the How does control flow work in Ruby bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: Conditionals: if/elsif/else/end, unless (= if not), case/when (case x; when 1..5 then 'small'; when String then 'str'; else 'other'; end), and the modifier form (puts 'hi' if verbose). Loops: while, until, but Ruby prefers iterators — 10.times { |i| ... }, array.each { |x| ... }, 1.upto(5) { |n| ... }, map, select. break exits a loop; next skips to the next iteration; redo retries the current; retry (in rescue) re-runs the block. return exits the method (implicit return = last expression). Exceptions: raise throws, begin/rescue/else/ensure/end catches (rescue StandardError => e), ensure always runs. Use iterators over while counters; use case/when over long if/elsif chains; use rescue for expected errors, not normal flow. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "Conditionals: if/elsif/else/end, unless (= if not), case/when (case x; when 1..5 then 'small'; when String then 'str'; else 'other'; end), and the modifier form (puts 'hi' if verbose).\n\nLoops: while, until, but Ruby prefers iterators — 10.times { |i| ... }, array.each { |x| ... }, 1.upto(5) { |n| ... }, map, select.\n\nbreak exits a loop; next skips to the next iteration; redo retries the current; retry (in rescue) re-runs the block. return exits the method (implicit return = last expression). Exceptions: raise throws, begin/rescue/else/ensure/end catches (rescue StandardError => e), ensure always runs. Use iterators over while counters; use case/when over long if/elsif chains; use rescue for expected errors, not normal flow.\n\nA common mistake is using a while counter when each/times would do, or rescue for normal control flow (it's for errors)."
          },
          {
            "type": "code_example",
            "title": "Control flow",
            "content": "```ruby\n# Conditionals\nif score > 90; 'A'; elsif score > 80; 'B'; else; 'C'; end\nputs 'done' unless running        # unless = if NOT\nputs 'hi' if verbose               # modifier form\ncase x\nwhen 1..5     then 'small'\nwhen String   then 'string'\nelse 'other'\nend\n\n# Loops (prefer ITERATORS over while)\n3.times { |i| puts i }             # 0 1 2\n[10, 20].each { |x| puts x }       # 10 20\n1.upto(5) { |n| puts n }           # 1..5\nnums.map { |n| n * 2 }             # transform\n\n# while/until (less idiomatic):\ni = 0\nwhile i < 3; puts i; i += 1; end\n\n# break (exit), next (skip), redo (retry current)\n[1,2,3].each do |n|\n  next if n == 2      # skip 2\n  break if n > 10     # stop\n  redo                 # retry this iteration (careful!)\nend\n\n# Exceptions\nbegin\n  risky\nrescue StandardError => e\n  log(e.message)\nelse\n  puts 'no error'\nensure\n  cleanup             # ALWAYS runs\nend\n\n# raise throws:  raise 'bad input'\n```"
          }
        ]
      },
      "followup_questions": [
        "How does control flow work in Ruby?",
        "When would you use Control Flow in a real project?",
        "How would you compare Control Flow with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Control Flow — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Control Flow? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

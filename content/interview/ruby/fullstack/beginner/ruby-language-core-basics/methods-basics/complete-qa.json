{
  "topic": "Methods Basics",
  "topicSlug": "methods-basics",
  "questions": [
    {
      "id": "methods-basics-q001",
      "slug": "ruby-core-methods-basics",
      "question": "What are methods in Ruby?",
      "title": "Methods in Ruby",
      "direct_answer": "A method is a reusable block of code: def greet(name); \"Hello, #{name}\"; end. Ruby returns the last expression by default (implicit return) — no `return` unless you exit early. Arguments: required (a), default (a=1), keyword (name:), splat (*args -> Array), double-splat (**opts -> Hash). Call with or without parens (puts 'hi' or puts('hi')). return exits immediately; without a value, returns nil. Methods end with end. Naming: snake_case; predicates end with ? (empty?), dangerous ones with ! (sort!). Every call returns something (nil if nothing else). Methods are objects' actions (42.even?, 'a'.upcase) — everything is an object, so everything has methods. Define them small and focused; one method, one job.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know def/end, implicit return, argument kinds (required/default/keyword/splat), and ?/! naming.",
        "common_mistake": "Adding explicit `return` everywhere (Ruby returns the last expression), or making methods do many things.",
        "to_stand_out": "Methods: def name(args)...end. Implicit return (last expression). Args: required, default (a=1), keyword (name:), *args, **opts. snake_case; ? predicates; ! dangerous."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define Methods in Ruby simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "A method is def name(args) ... end; Ruby returns the last expression (implicit return).",
              "`return` exits early; without a value, returns nil.",
              "Args: required (a), default (a=1), keyword (name:), splat (*args), double-splat (**opts).",
              "Call with or without parens; naming: snake_case; ? = predicate, ! = dangerous.",
              "Everything is an object; methods are its actions; keep methods small and focused."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "A method is a reusable block of code: def greet name; Hello name; end. Ruby returns the last expression by default (implicit return) — no return unless you exit early. Arguments: required (a), default (a 1), keyword (name), splat (star args to Array), double-splat (star star opts to Hash). Call with or without parens (puts hi or puts hi). return exits immediately; without a value, returns nil. Methods end with end. Naming: snake_case; predicates end with ? (empty), dangerous ones with ! (sort bang). Every call returns something (nil if nothing else). Methods are objects' actions (42 dot even, a dot upcase) — everything is an object, so everything has methods. Define them small and focused; one method, one job. A common mistake is adding explicit return everywhere, when Ruby returns the last expression, and making methods long with many jobs, when you should keep them small."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "A method is a reusable block of code: def greet(name); \"Hello, #{name}\"; end.\n\nRuby returns the last expression by default (implicit return) — no `return` unless you exit early.\n\nArguments: required (a), default (a=1), keyword (name:), splat (*args -> Array), double-splat (**opts -> Hash). Call with or without parens (puts 'hi' or puts('hi')). return exits immediately; without a value, returns nil. Methods end with end. Naming: snake_case; predicates end with ? (empty?), dangerous ones with ! (sort!). Every call returns something (nil if nothing else). Methods are objects' actions (42.even?, 'a'.upcase) — everything is an object, so everything has methods. Define them small and focused; one method, one job.\n\nAdding explicit `return` everywhere (Ruby returns the last expression), or making methods do many things."
          },
          {
            "type": "flow_diagram",
            "title": "Method Anatomy",
            "content": "def name(args)...end. Implicit return. Args: required/default/keyword/*args/**opts. snake_case; ?/! naming.\n\n```mermaid\nflowchart LR\n  D[def greet(name)] --> B[body: last expr = return]\n  B --> R[implicit return]\n  A[args] --> Re[required a]\n  A --> De[default a=1]\n  A --> K[keyword name:]\n  A --> S[splat *args / **opts]\n```"
          },
          {
            "type": "code_example",
            "title": "Methods",
            "content": "```ruby\n# Define a method\ndef greet(name)\n  \"Hello, #{name}\"      # last expression -> IMPLICIT return\nend\ngreet('Ada')            # 'Hello, Ada'\n\n# Implicit return: no `return` needed\ndef add(a, b)\n  a + b                 # returns a + b\nend\n\n# `return` exits EARLY:\ndef first_even(xs)\n  xs.each { |x| return x if x.even? }\n  nil\nend\n\n# Arguments:\ndef f(a, b = 1, name:, *args, **opts)\n  # a: required; b: default; name: keyword (required);\n  # *args: extras -> Array; **opts: keyword extras -> Hash\nend\n\n# Call with or without parens:\nputs 'hi'\nputs('hi')\n\n# Naming: snake_case; ? = predicate (bool); ! = dangerous/mutating\narr.empty?   # ? -> returns true/false\narr.sort!    # ! -> mutates in place\n\n# Everything is an object; methods are its actions:\n42.even?           # true\n'a'.upcase         # 'A'\n[1,2,3].sum        # 6\n\n# Keep methods small; one method, one job.\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Methods Basics in a real project?",
        "How would you compare Methods Basics with an alternative?",
        "What is a common mistake when using Methods Basics?"
      ],
      "seo": {
        "metaTitle": "Methods in Ruby — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What are methods in Ruby? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "ruby-language-core-basics-methods-basics-q001",
      "slug": "ruby-language-core-basics-methods-basics-when-to-use",
      "question": "When would you use Methods Basics in a real project?",
      "title": "When would you use Methods Basics in a real project",
      "direct_answer": "I would use methods in Ruby when the problem matches its main benefit. A method is a reusable block of code: def greet(name); \"Hello, #{name}\"; end. In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain methods in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about methods in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining methods in Ruby in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. A method is a reusable block of code: def greet(name); \"Hello, #{name}\"; end. Ruby returns the last expression by default (implicit return) — no `return` unless you exit early. Arguments: required (a), default (a=1), keyword (name:), splat (*args -> Array), double-splat (**opts -> Hash). Call with or without parens (puts 'hi' or puts('hi')). return exits immediately; without a value, returns nil. Methods end with end. Naming: snake_case; predicates end with ? (empty?), dangerous ones with ! (sort!). Every call returns something (nil if nothing else). Methods are objects' actions (42.even?, 'a'.upcase) — everything is an object, so everything has methods. Define them small and focused; one method, one job."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use methods in Ruby when its rule makes the code safer, clearer, or more efficient for the actual problem. A method is a reusable block of code: def greet(name); \"Hello, #{name}\"; end. Ruby returns the last expression by default (implicit return) — no `return` unless you exit early. Arguments: required (a), default (a=1), keyword (name:), splat (*args -> Array), double-splat (**opts -> Hash). Call with or without parens (puts 'hi' or puts('hi')). return exits immediately; without a value, returns nil. Methods end with end. Naming: snake_case; predicates end with ? (empty?), dangerous ones with ! (sort!). Every call returns something (nil if nothing else). Methods are objects' actions (42.even?, 'a'.upcase) — everything is an object, so everything has methods. Define them small and focused; one method, one job. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "A method is a reusable block of code: def greet(name); \"Hello, #{name}\"; end.\n\nRuby returns the last expression by default (implicit return) — no `return` unless you exit early.\n\nArguments: required (a), default (a=1), keyword (name:), splat (*args -> Array), double-splat (**opts -> Hash). Call with or without parens (puts 'hi' or puts('hi')). return exits immediately; without a value, returns nil. Methods end with end. Naming: snake_case; predicates end with ? (empty?), dangerous ones with ! (sort!). Every call returns something (nil if nothing else). Methods are objects' actions (42.even?, 'a'.upcase) — everything is an object, so everything has methods. Define them small and focused; one method, one job.\n\nAdding explicit `return` everywhere (Ruby returns the last expression), or making methods do many things."
          },
          {
            "type": "code_example",
            "title": "Methods",
            "content": "```ruby\n# Define a method\ndef greet(name)\n  \"Hello, #{name}\"      # last expression -> IMPLICIT return\nend\ngreet('Ada')            # 'Hello, Ada'\n\n# Implicit return: no `return` needed\ndef add(a, b)\n  a + b                 # returns a + b\nend\n\n# `return` exits EARLY:\ndef first_even(xs)\n  xs.each { |x| return x if x.even? }\n  nil\nend\n\n# Arguments:\ndef f(a, b = 1, name:, *args, **opts)\n  # a: required; b: default; name: keyword (required);\n  # *args: extras -> Array; **opts: keyword extras -> Hash\nend\n\n# Call with or without parens:\nputs 'hi'\nputs('hi')\n\n# Naming: snake_case; ? = predicate (bool); ! = dangerous/mutating\narr.empty?   # ? -> returns true/false\narr.sort!    # ! -> mutates in place\n\n# Everything is an object; methods are its actions:\n42.even?           # true\n'a'.upcase         # 'A'\n[1,2,3].sum        # 6\n\n# Keep methods small; one method, one job.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are methods in Ruby?",
        "How would you compare Methods Basics with an alternative?",
        "What is a common mistake when using Methods Basics?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Methods Basics in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Methods Basics in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-language-core-basics-methods-basics-q002",
      "slug": "ruby-language-core-basics-methods-basics-common-mistake",
      "question": "What is a common mistake when using Methods Basics?",
      "title": "a common mistake when using Methods Basics",
      "direct_answer": "A common mistake with methods in Ruby is using the rule without checking its boundary conditions. A method is a reusable block of code: def greet(name); \"Hello, #{name}\"; end. That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain methods in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about methods in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of methods in Ruby. A method is a reusable block of code: def greet(name); \"Hello, #{name}\"; end. Ruby returns the last expression by default (implicit return) — no `return` unless you exit early. Arguments: required (a), default (a=1), keyword (name:), splat (*args -> Array), double-splat (**opts -> Hash). Call with or without parens (puts 'hi' or puts('hi')). return exits immediately; without a value, returns nil. Methods end with end. Naming: snake_case; predicates end with ? (empty?), dangerous ones with ! (sort!). Every call returns something (nil if nothing else). Methods are objects' actions (42.even?, 'a'.upcase) — everything is an object, so everything has methods. Define them small and focused; one method, one job."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with methods in Ruby is assuming the happy path is the whole rule. A method is a reusable block of code: def greet(name); \"Hello, #{name}\"; end. Ruby returns the last expression by default (implicit return) — no `return` unless you exit early. Arguments: required (a), default (a=1), keyword (name:), splat (*args -> Array), double-splat (**opts -> Hash). Call with or without parens (puts 'hi' or puts('hi')). return exits immediately; without a value, returns nil. Methods end with end. Naming: snake_case; predicates end with ? (empty?), dangerous ones with ! (sort!). Every call returns something (nil if nothing else). Methods are objects' actions (42.even?, 'a'.upcase) — everything is an object, so everything has methods. Define them small and focused; one method, one job. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "A method is a reusable block of code: def greet(name); \"Hello, #{name}\"; end.\n\nRuby returns the last expression by default (implicit return) — no `return` unless you exit early.\n\nArguments: required (a), default (a=1), keyword (name:), splat (*args -> Array), double-splat (**opts -> Hash). Call with or without parens (puts 'hi' or puts('hi')). return exits immediately; without a value, returns nil. Methods end with end. Naming: snake_case; predicates end with ? (empty?), dangerous ones with ! (sort!). Every call returns something (nil if nothing else). Methods are objects' actions (42.even?, 'a'.upcase) — everything is an object, so everything has methods. Define them small and focused; one method, one job.\n\nAdding explicit `return` everywhere (Ruby returns the last expression), or making methods do many things."
          },
          {
            "type": "code_example",
            "title": "Methods",
            "content": "```ruby\n# Define a method\ndef greet(name)\n  \"Hello, #{name}\"      # last expression -> IMPLICIT return\nend\ngreet('Ada')            # 'Hello, Ada'\n\n# Implicit return: no `return` needed\ndef add(a, b)\n  a + b                 # returns a + b\nend\n\n# `return` exits EARLY:\ndef first_even(xs)\n  xs.each { |x| return x if x.even? }\n  nil\nend\n\n# Arguments:\ndef f(a, b = 1, name:, *args, **opts)\n  # a: required; b: default; name: keyword (required);\n  # *args: extras -> Array; **opts: keyword extras -> Hash\nend\n\n# Call with or without parens:\nputs 'hi'\nputs('hi')\n\n# Naming: snake_case; ? = predicate (bool); ! = dangerous/mutating\narr.empty?   # ? -> returns true/false\narr.sort!    # ! -> mutates in place\n\n# Everything is an object; methods are its actions:\n42.even?           # true\n'a'.upcase         # 'A'\n[1,2,3].sum        # 6\n\n# Keep methods small; one method, one job.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are methods in Ruby?",
        "When would you use Methods Basics in a real project?",
        "How would you compare Methods Basics with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Methods Basics — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Methods Basics? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-language-core-basics-methods-basics-q003",
      "slug": "ruby-language-core-basics-methods-basics-compare",
      "question": "How would you compare Methods Basics with an alternative?",
      "title": "compare Methods Basics with an alternative",
      "direct_answer": "I compare methods in Ruby with the simpler alternative by looking at correctness first, then time, memory, and readability. A method is a reusable block of code: def greet(name); \"Hello, #{name}\"; end. The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain methods in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about methods in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. A method is a reusable block of code: def greet(name); \"Hello, #{name}\"; end. Ruby returns the last expression by default (implicit return) — no `return` unless you exit early. Arguments: required (a), default (a=1), keyword (name:), splat (*args -> Array), double-splat (**opts -> Hash). Call with or without parens (puts 'hi' or puts('hi')). return exits immediately; without a value, returns nil. Methods end with end. Naming: snake_case; predicates end with ? (empty?), dangerous ones with ! (sort!). Every call returns something (nil if nothing else). Methods are objects' actions (42.even?, 'a'.upcase) — everything is an object, so everything has methods. Define them small and focused; one method, one job."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare methods in Ruby with the straightforward alternative on the same example. A method is a reusable block of code: def greet(name); \"Hello, #{name}\"; end. Ruby returns the last expression by default (implicit return) — no `return` unless you exit early. Arguments: required (a), default (a=1), keyword (name:), splat (*args -> Array), double-splat (**opts -> Hash). Call with or without parens (puts 'hi' or puts('hi')). return exits immediately; without a value, returns nil. Methods end with end. Naming: snake_case; predicates end with ? (empty?), dangerous ones with ! (sort!). Every call returns something (nil if nothing else). Methods are objects' actions (42.even?, 'a'.upcase) — everything is an object, so everything has methods. Define them small and focused; one method, one job. If methods in Ruby removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "A method is a reusable block of code: def greet(name); \"Hello, #{name}\"; end.\n\nRuby returns the last expression by default (implicit return) — no `return` unless you exit early.\n\nArguments: required (a), default (a=1), keyword (name:), splat (*args -> Array), double-splat (**opts -> Hash). Call with or without parens (puts 'hi' or puts('hi')). return exits immediately; without a value, returns nil. Methods end with end. Naming: snake_case; predicates end with ? (empty?), dangerous ones with ! (sort!). Every call returns something (nil if nothing else). Methods are objects' actions (42.even?, 'a'.upcase) — everything is an object, so everything has methods. Define them small and focused; one method, one job.\n\nAdding explicit `return` everywhere (Ruby returns the last expression), or making methods do many things."
          },
          {
            "type": "code_example",
            "title": "Methods",
            "content": "```ruby\n# Define a method\ndef greet(name)\n  \"Hello, #{name}\"      # last expression -> IMPLICIT return\nend\ngreet('Ada')            # 'Hello, Ada'\n\n# Implicit return: no `return` needed\ndef add(a, b)\n  a + b                 # returns a + b\nend\n\n# `return` exits EARLY:\ndef first_even(xs)\n  xs.each { |x| return x if x.even? }\n  nil\nend\n\n# Arguments:\ndef f(a, b = 1, name:, *args, **opts)\n  # a: required; b: default; name: keyword (required);\n  # *args: extras -> Array; **opts: keyword extras -> Hash\nend\n\n# Call with or without parens:\nputs 'hi'\nputs('hi')\n\n# Naming: snake_case; ? = predicate (bool); ! = dangerous/mutating\narr.empty?   # ? -> returns true/false\narr.sort!    # ! -> mutates in place\n\n# Everything is an object; methods are its actions:\n42.even?           # true\n'a'.upcase         # 'A'\n[1,2,3].sum        # 6\n\n# Keep methods small; one method, one job.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are methods in Ruby?",
        "When would you use Methods Basics in a real project?",
        "What is a common mistake when using Methods Basics?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Methods Basics with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Methods Basics with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-language-core-basics-methods-basics-q004",
      "slug": "ruby-language-core-basics-methods-basics-scenario",
      "question": "How would you debug a problem involving Methods Basics?",
      "title": "debug a problem involving Methods Basics",
      "direct_answer": "I would debug a problem involving methods in Ruby by reproducing it with the smallest input first. A method is a reusable block of code: def greet(name); \"Hello, #{name}\"; end. Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain methods in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about methods in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what methods in Ruby is supposed to guarantee. A method is a reusable block of code: def greet(name); \"Hello, #{name}\"; end. Ruby returns the last expression by default (implicit return) — no `return` unless you exit early. Arguments: required (a), default (a=1), keyword (name:), splat (*args -> Array), double-splat (**opts -> Hash). Call with or without parens (puts 'hi' or puts('hi')). return exits immediately; without a value, returns nil. Methods end with end. Naming: snake_case; predicates end with ? (empty?), dangerous ones with ! (sort!). Every call returns something (nil if nothing else). Methods are objects' actions (42.even?, 'a'.upcase) — everything is an object, so everything has methods. Define them small and focused; one method, one job."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the methods in Ruby bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: A method is a reusable block of code: def greet(name); \"Hello, #{name}\"; end. Ruby returns the last expression by default (implicit return) — no `return` unless you exit early. Arguments: required (a), default (a=1), keyword (name:), splat (*args -> Array), double-splat (**opts -> Hash). Call with or without parens (puts 'hi' or puts('hi')). return exits immediately; without a value, returns nil. Methods end with end. Naming: snake_case; predicates end with ? (empty?), dangerous ones with ! (sort!). Every call returns something (nil if nothing else). Methods are objects' actions (42.even?, 'a'.upcase) — everything is an object, so everything has methods. Define them small and focused; one method, one job. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "A method is a reusable block of code: def greet(name); \"Hello, #{name}\"; end.\n\nRuby returns the last expression by default (implicit return) — no `return` unless you exit early.\n\nArguments: required (a), default (a=1), keyword (name:), splat (*args -> Array), double-splat (**opts -> Hash). Call with or without parens (puts 'hi' or puts('hi')). return exits immediately; without a value, returns nil. Methods end with end. Naming: snake_case; predicates end with ? (empty?), dangerous ones with ! (sort!). Every call returns something (nil if nothing else). Methods are objects' actions (42.even?, 'a'.upcase) — everything is an object, so everything has methods. Define them small and focused; one method, one job.\n\nAdding explicit `return` everywhere (Ruby returns the last expression), or making methods do many things."
          },
          {
            "type": "code_example",
            "title": "Methods",
            "content": "```ruby\n# Define a method\ndef greet(name)\n  \"Hello, #{name}\"      # last expression -> IMPLICIT return\nend\ngreet('Ada')            # 'Hello, Ada'\n\n# Implicit return: no `return` needed\ndef add(a, b)\n  a + b                 # returns a + b\nend\n\n# `return` exits EARLY:\ndef first_even(xs)\n  xs.each { |x| return x if x.even? }\n  nil\nend\n\n# Arguments:\ndef f(a, b = 1, name:, *args, **opts)\n  # a: required; b: default; name: keyword (required);\n  # *args: extras -> Array; **opts: keyword extras -> Hash\nend\n\n# Call with or without parens:\nputs 'hi'\nputs('hi')\n\n# Naming: snake_case; ? = predicate (bool); ! = dangerous/mutating\narr.empty?   # ? -> returns true/false\narr.sort!    # ! -> mutates in place\n\n# Everything is an object; methods are its actions:\n42.even?           # true\n'a'.upcase         # 'A'\n[1,2,3].sum        # 6\n\n# Keep methods small; one method, one job.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are methods in Ruby?",
        "When would you use Methods Basics in a real project?",
        "How would you compare Methods Basics with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Methods Basics — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Methods Basics? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

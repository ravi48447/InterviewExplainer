{
  "topic": "Unit vs Integration",
  "topicSlug": "unit-vs-integration",
  "questions": [
    {
      "id": "unit-vs-integration-q001",
      "slug": "ruby-unit-vs-integration",
      "question": "What's the difference between unit and integration tests in Rails?",
      "title": "Unit vs Integration Tests",
      "direct_answer": "Unit tests check one piece in isolation (a method, a model's validation): `UserTest` asserts `User.new(name: '').valid?` is false — fast, no DB/HTTP, pinpoint failures. Integration tests check multiple parts working together: a request spec hits `post '/users', params: ...` and asserts the DB changed and the response is 302 — slower (full stack: routing, controller, model, DB, views) but catches wiring bugs unit tests miss. Rails layers: model tests (unit), controller/request tests (integration), system tests (full browser, JS). Pyramid: many fast unit tests at the base, fewer integration, very few system (slow). Unit tests tell you WHAT broke precisely; integration tests tell you the WIRING works. Both matter: unit for fast feedback on logic, integration for confidence the pieces connect. Don't write only integration (slow, hard to localize) or only unit (misses wiring); balance them.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know unit tests check one piece in isolation (fast, pinpoint), integration tests check parts together (slow, catches wiring), and you balance the pyramid.",
        "common_mistake": "Only integration tests (slow, hard to localize) or only unit (misses wiring bugs) — balance the pyramid.",
        "to_stand_out": "Unit = one piece in isolation (fast, pinpoint). Integration = parts together (routing+controller+model+DB, catches wiring). Balance the pyramid."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define Unit vs Integration Tests simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "Unit tests check one piece in isolation (a method, a model's validation) — fast, pinpoint failures.",
              "Integration tests check parts together (routing, controller, model, DB, view) — slower, catches wiring.",
              "Rails layers: model (unit) -> request (integration) -> system (full browser, JS).",
              "Pyramid: many fast unit tests, fewer integration, very few system (slow).",
              "Unit tells you WHAT broke; integration tells you the WIRING works. Balance them."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "Unit tests check one piece in isolation (a method, a model's validation): User Test asserts User new name empty valid is false — fast, no DB or HTTP, pinpoint failures. Integration tests check multiple parts working together: a request spec hits post slash users params dot dot dot and asserts the DB changed and the response is 302 — slower (full stack: routing, controller, model, DB, views) but catches wiring bugs unit tests miss. Rails layers: model tests (unit), controller or request tests (integration), system tests (full browser, JS). Pyramid: many fast unit tests at the base, fewer integration, very few system (slow). Unit tests tell you WHAT broke precisely; integration tests tell you the WIRING works. Both matter: unit for fast feedback on logic, integration for confidence the pieces connect. Don't write only integration (slow, hard to localize) or only unit (misses wiring); balance them. A common mistake is only integration tests, which are slow and hard to localize, or only unit tests, which misses wiring, when you should balance the pyramid."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "Unit tests check one piece in isolation (a method, a model's validation): `UserTest` asserts `User.new(name: '').valid?` is false — fast, no DB/HTTP, pinpoint failures.\n\nIntegration tests check multiple parts working together: a request spec hits `post '/users', params: ...` and asserts the DB changed and the response is 302 — slower (full stack: routing, controller, model, DB, views) but catches wiring bugs unit tests miss.\n\nRails layers: model tests (unit), controller/request tests (integration), system tests (full browser, JS). Pyramid: many fast unit tests at the base, fewer integration, very few system (slow). Unit tests tell you WHAT broke precisely; integration tests tell you the WIRING works. Both matter: unit for fast feedback on logic, integration for confidence the pieces connect. Don't write only integration (slow, hard to localize) or only unit (misses wiring); balance them."
          },
          {
            "type": "flow_diagram",
            "title": "Unit vs Integration",
            "content": "unit = one piece in isolation (fast, pinpoint). integration = parts together (routing+controller+model+DB, catches wiring). Balance pyramid.\n\n```mermaid\nflowchart TD\n  S[System tests: few, slow, browser] --> I[Integration tests: fewer]\n  I --> U[Unit tests: many, fast, isolated]\n```"
          },
          {
            "type": "code_example",
            "title": "Unit vs integration",
            "content": "```ruby\n# UNIT test: one piece in isolation (fast, no DB/HTTP)\n# test/models/user_test.rb (Minitest)\nclass UserTest < ActiveSupport::TestCase\n  test 'invalid without a name' do\n    user = User.new(name: '')\n    refute user.valid?                     # just the validation logic\n    assert_equal [\"can't be blank\"], user.errors[:name]\n  end\nend\n# spec/models/user_spec.rb (RSpec)\nRSpec.describe User, type: :model do\n  it 'is invalid without a name' do\n    expect(User.new(name: '')).not_to be_valid\n  end\nend\n# Fast; pinpoint: tells you the validation logic broke.\n\n# INTEGRATION test: parts together (routing -> controller -> model -> DB -> view)\n# spec/requests/users_spec.rb (request spec)\nRSpec.describe 'Users', type: :request do\n  it 'creates a user' do\n    post '/users', params: { user: { name: 'Al' } }\n    expect(response).to have_http_status(:found)   # 302 redirect\n    expect(User.count).to eq(1)                    # DB changed\n    expect(User.last.name).to eq('Al')\n  end\nend\n# test/system/users_test.rb (system test: full browser, JS)\nclass UsersTest < ActionDispatch::SystemTestCase\n  test 'create via form' do\n    visit '/users/new'\n    fill_in 'Name', with: 'Al'\n    click_on 'Create'\n    assert_text 'Al'\n  end\nend\n\n# Rails layers:    model (unit) -> request (integration) -> system (full browser/JS)\n# Pyramid:         many unit (fast) / fewer integration / very few system (slow).\n# Unit tests    -> what broke precisely (logic).\n# Integration  -> the wiring works (parts connect).\n# System       -> the user flow works in a real browser (incl. JS).\n# Balance: don't write ONLY integration (slow, hard to localize) or ONLY unit (misses wiring).\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Unit Vs Integration in a real project?",
        "What is a common mistake when using Unit Vs Integration?",
        "How would you compare Unit Vs Integration with an alternative?"
      ],
      "seo": {
        "metaTitle": "Unit vs Integration Tests — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What's the difference between unit and integration tests in Rails? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "testing-basics-unit-vs-integration-q001",
      "slug": "testing-basics-unit-vs-integration-when-to-use",
      "question": "When would you use Unit Vs Integration in a real project?",
      "title": "When would you use Unit Vs Integration in a real project",
      "direct_answer": "I would use What's the difference between unit and integration tests in Rails when the problem matches its main benefit. Unit tests check one piece in isolation (a method, a model's validation): `UserTest` asserts `User.new(name: '').valid?` is false — fast, no DB/HTTP, pinpoint failures. In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain What's the difference between unit and integration tests in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about What's the difference between unit and integration tests in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining What's the difference between unit and integration tests in Rails in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. Unit tests check one piece in isolation (a method, a model's validation): `UserTest` asserts `User.new(name: '').valid?` is false — fast, no DB/HTTP, pinpoint failures. Integration tests check multiple parts working together: a request spec hits `post '/users', params: ...` and asserts the DB changed and the response is 302 — slower (full stack: routing, controller, model, DB, views) but catches wiring bugs unit tests miss. Rails layers: model tests (unit), controller/request tests (integration), system tests (full browser, JS). Pyramid: many fast unit tests at the base, fewer integration, very few system (slow). Unit tests tell you WHAT broke precisely; integration tests tell you the WIRING works. Both matter: unit for fast feedback on logic, integration for confidence the pieces connect. Don't write only integration (slow, hard to localize) or only unit (misses wiring); balance them."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use What's the difference between unit and integration tests in Rails when its rule makes the code safer, clearer, or more efficient for the actual problem. Unit tests check one piece in isolation (a method, a model's validation): `UserTest` asserts `User.new(name: '').valid?` is false — fast, no DB/HTTP, pinpoint failures. Integration tests check multiple parts working together: a request spec hits `post '/users', params: ...` and asserts the DB changed and the response is 302 — slower (full stack: routing, controller, model, DB, views) but catches wiring bugs unit tests miss. Rails layers: model tests (unit), controller/request tests (integration), system tests (full browser, JS). Pyramid: many fast unit tests at the base, fewer integration, very few system (slow). Unit tests tell you WHAT broke precisely; integration tests tell you the WIRING works. Both matter: unit for fast feedback on logic, integration for confidence the pieces connect. Don't write only integration (slow, hard to localize) or only unit (misses wiring); balance them. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "Unit tests check one piece in isolation (a method, a model's validation): `UserTest` asserts `User.new(name: '').valid?` is false — fast, no DB/HTTP, pinpoint failures.\n\nIntegration tests check multiple parts working together: a request spec hits `post '/users', params: ...` and asserts the DB changed and the response is 302 — slower (full stack: routing, controller, model, DB, views) but catches wiring bugs unit tests miss.\n\nRails layers: model tests (unit), controller/request tests (integration), system tests (full browser, JS). Pyramid: many fast unit tests at the base, fewer integration, very few system (slow). Unit tests tell you WHAT broke precisely; integration tests tell you the WIRING works. Both matter: unit for fast feedback on logic, integration for confidence the pieces connect. Don't write only integration (slow, hard to localize) or only unit (misses wiring); balance them."
          },
          {
            "type": "code_example",
            "title": "Unit vs integration",
            "content": "```ruby\n# UNIT test: one piece in isolation (fast, no DB/HTTP)\n# test/models/user_test.rb (Minitest)\nclass UserTest < ActiveSupport::TestCase\n  test 'invalid without a name' do\n    user = User.new(name: '')\n    refute user.valid?                     # just the validation logic\n    assert_equal [\"can't be blank\"], user.errors[:name]\n  end\nend\n# spec/models/user_spec.rb (RSpec)\nRSpec.describe User, type: :model do\n  it 'is invalid without a name' do\n    expect(User.new(name: '')).not_to be_valid\n  end\nend\n# Fast; pinpoint: tells you the validation logic broke.\n\n# INTEGRATION test: parts together (routing -> controller -> model -> DB -> view)\n# spec/requests/users_spec.rb (request spec)\nRSpec.describe 'Users', type: :request do\n  it 'creates a user' do\n    post '/users', params: { user: { name: 'Al' } }\n    expect(response).to have_http_status(:found)   # 302 redirect\n    expect(User.count).to eq(1)                    # DB changed\n    expect(User.last.name).to eq('Al')\n  end\nend\n# test/system/users_test.rb (system test: full browser, JS)\nclass UsersTest < ActionDispatch::SystemTestCase\n  test 'create via form' do\n    visit '/users/new'\n    fill_in 'Name', with: 'Al'\n    click_on 'Create'\n    assert_text 'Al'\n  end\nend\n\n# Rails layers:    model (unit) -> request (integration) -> system (full browser/JS)\n# Pyramid:         many unit (fast) / fewer integration / very few system (slow).\n# Unit tests    -> what broke precisely (logic).\n# Integration  -> the wiring works (parts connect).\n# System       -> the user flow works in a real browser (incl. JS).\n# Balance: don't write ONLY integration (slow, hard to localize) or ONLY unit (misses wiring).\n```"
          }
        ]
      },
      "followup_questions": [
        "What's the difference between unit and integration tests in Rails?",
        "What is a common mistake when using Unit Vs Integration?",
        "How would you compare Unit Vs Integration with an alternative?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Unit Vs Integration in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Unit Vs Integration in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "testing-basics-unit-vs-integration-q002",
      "slug": "testing-basics-unit-vs-integration-common-mistake",
      "question": "What is a common mistake when using Unit Vs Integration?",
      "title": "a common mistake when using Unit Vs Integration",
      "direct_answer": "A common mistake with What's the difference between unit and integration tests in Rails is using the rule without checking its boundary conditions. Unit tests check one piece in isolation (a method, a model's validation): `UserTest` asserts `User.new(name: '').valid?` is false — fast, no DB/HTTP, pinpoint failures. That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain What's the difference between unit and integration tests in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about What's the difference between unit and integration tests in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of What's the difference between unit and integration tests in Rails. Unit tests check one piece in isolation (a method, a model's validation): `UserTest` asserts `User.new(name: '').valid?` is false — fast, no DB/HTTP, pinpoint failures. Integration tests check multiple parts working together: a request spec hits `post '/users', params: ...` and asserts the DB changed and the response is 302 — slower (full stack: routing, controller, model, DB, views) but catches wiring bugs unit tests miss. Rails layers: model tests (unit), controller/request tests (integration), system tests (full browser, JS). Pyramid: many fast unit tests at the base, fewer integration, very few system (slow). Unit tests tell you WHAT broke precisely; integration tests tell you the WIRING works. Both matter: unit for fast feedback on logic, integration for confidence the pieces connect. Don't write only integration (slow, hard to localize) or only unit (misses wiring); balance them."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with What's the difference between unit and integration tests in Rails is assuming the happy path is the whole rule. Unit tests check one piece in isolation (a method, a model's validation): `UserTest` asserts `User.new(name: '').valid?` is false — fast, no DB/HTTP, pinpoint failures. Integration tests check multiple parts working together: a request spec hits `post '/users', params: ...` and asserts the DB changed and the response is 302 — slower (full stack: routing, controller, model, DB, views) but catches wiring bugs unit tests miss. Rails layers: model tests (unit), controller/request tests (integration), system tests (full browser, JS). Pyramid: many fast unit tests at the base, fewer integration, very few system (slow). Unit tests tell you WHAT broke precisely; integration tests tell you the WIRING works. Both matter: unit for fast feedback on logic, integration for confidence the pieces connect. Don't write only integration (slow, hard to localize) or only unit (misses wiring); balance them. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "Unit tests check one piece in isolation (a method, a model's validation): `UserTest` asserts `User.new(name: '').valid?` is false — fast, no DB/HTTP, pinpoint failures.\n\nIntegration tests check multiple parts working together: a request spec hits `post '/users', params: ...` and asserts the DB changed and the response is 302 — slower (full stack: routing, controller, model, DB, views) but catches wiring bugs unit tests miss.\n\nRails layers: model tests (unit), controller/request tests (integration), system tests (full browser, JS). Pyramid: many fast unit tests at the base, fewer integration, very few system (slow). Unit tests tell you WHAT broke precisely; integration tests tell you the WIRING works. Both matter: unit for fast feedback on logic, integration for confidence the pieces connect. Don't write only integration (slow, hard to localize) or only unit (misses wiring); balance them."
          },
          {
            "type": "code_example",
            "title": "Unit vs integration",
            "content": "```ruby\n# UNIT test: one piece in isolation (fast, no DB/HTTP)\n# test/models/user_test.rb (Minitest)\nclass UserTest < ActiveSupport::TestCase\n  test 'invalid without a name' do\n    user = User.new(name: '')\n    refute user.valid?                     # just the validation logic\n    assert_equal [\"can't be blank\"], user.errors[:name]\n  end\nend\n# spec/models/user_spec.rb (RSpec)\nRSpec.describe User, type: :model do\n  it 'is invalid without a name' do\n    expect(User.new(name: '')).not_to be_valid\n  end\nend\n# Fast; pinpoint: tells you the validation logic broke.\n\n# INTEGRATION test: parts together (routing -> controller -> model -> DB -> view)\n# spec/requests/users_spec.rb (request spec)\nRSpec.describe 'Users', type: :request do\n  it 'creates a user' do\n    post '/users', params: { user: { name: 'Al' } }\n    expect(response).to have_http_status(:found)   # 302 redirect\n    expect(User.count).to eq(1)                    # DB changed\n    expect(User.last.name).to eq('Al')\n  end\nend\n# test/system/users_test.rb (system test: full browser, JS)\nclass UsersTest < ActionDispatch::SystemTestCase\n  test 'create via form' do\n    visit '/users/new'\n    fill_in 'Name', with: 'Al'\n    click_on 'Create'\n    assert_text 'Al'\n  end\nend\n\n# Rails layers:    model (unit) -> request (integration) -> system (full browser/JS)\n# Pyramid:         many unit (fast) / fewer integration / very few system (slow).\n# Unit tests    -> what broke precisely (logic).\n# Integration  -> the wiring works (parts connect).\n# System       -> the user flow works in a real browser (incl. JS).\n# Balance: don't write ONLY integration (slow, hard to localize) or ONLY unit (misses wiring).\n```"
          }
        ]
      },
      "followup_questions": [
        "What's the difference between unit and integration tests in Rails?",
        "When would you use Unit Vs Integration in a real project?",
        "How would you compare Unit Vs Integration with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Unit Vs Integration — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Unit Vs Integration? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "testing-basics-unit-vs-integration-q003",
      "slug": "testing-basics-unit-vs-integration-compare",
      "question": "How would you compare Unit Vs Integration with an alternative?",
      "title": "compare Unit Vs Integration with an alternative",
      "direct_answer": "I compare What's the difference between unit and integration tests in Rails with the simpler alternative by looking at correctness first, then time, memory, and readability. Unit tests check one piece in isolation (a method, a model's validation): `UserTest` asserts `User.new(name: '').valid?` is false — fast, no DB/HTTP, pinpoint failures. The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain What's the difference between unit and integration tests in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about What's the difference between unit and integration tests in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. Unit tests check one piece in isolation (a method, a model's validation): `UserTest` asserts `User.new(name: '').valid?` is false — fast, no DB/HTTP, pinpoint failures. Integration tests check multiple parts working together: a request spec hits `post '/users', params: ...` and asserts the DB changed and the response is 302 — slower (full stack: routing, controller, model, DB, views) but catches wiring bugs unit tests miss. Rails layers: model tests (unit), controller/request tests (integration), system tests (full browser, JS). Pyramid: many fast unit tests at the base, fewer integration, very few system (slow). Unit tests tell you WHAT broke precisely; integration tests tell you the WIRING works. Both matter: unit for fast feedback on logic, integration for confidence the pieces connect. Don't write only integration (slow, hard to localize) or only unit (misses wiring); balance them."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare What's the difference between unit and integration tests in Rails with the straightforward alternative on the same example. Unit tests check one piece in isolation (a method, a model's validation): `UserTest` asserts `User.new(name: '').valid?` is false — fast, no DB/HTTP, pinpoint failures. Integration tests check multiple parts working together: a request spec hits `post '/users', params: ...` and asserts the DB changed and the response is 302 — slower (full stack: routing, controller, model, DB, views) but catches wiring bugs unit tests miss. Rails layers: model tests (unit), controller/request tests (integration), system tests (full browser, JS). Pyramid: many fast unit tests at the base, fewer integration, very few system (slow). Unit tests tell you WHAT broke precisely; integration tests tell you the WIRING works. Both matter: unit for fast feedback on logic, integration for confidence the pieces connect. Don't write only integration (slow, hard to localize) or only unit (misses wiring); balance them. If What's the difference between unit and integration tests in Rails removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "Unit tests check one piece in isolation (a method, a model's validation): `UserTest` asserts `User.new(name: '').valid?` is false — fast, no DB/HTTP, pinpoint failures.\n\nIntegration tests check multiple parts working together: a request spec hits `post '/users', params: ...` and asserts the DB changed and the response is 302 — slower (full stack: routing, controller, model, DB, views) but catches wiring bugs unit tests miss.\n\nRails layers: model tests (unit), controller/request tests (integration), system tests (full browser, JS). Pyramid: many fast unit tests at the base, fewer integration, very few system (slow). Unit tests tell you WHAT broke precisely; integration tests tell you the WIRING works. Both matter: unit for fast feedback on logic, integration for confidence the pieces connect. Don't write only integration (slow, hard to localize) or only unit (misses wiring); balance them."
          },
          {
            "type": "code_example",
            "title": "Unit vs integration",
            "content": "```ruby\n# UNIT test: one piece in isolation (fast, no DB/HTTP)\n# test/models/user_test.rb (Minitest)\nclass UserTest < ActiveSupport::TestCase\n  test 'invalid without a name' do\n    user = User.new(name: '')\n    refute user.valid?                     # just the validation logic\n    assert_equal [\"can't be blank\"], user.errors[:name]\n  end\nend\n# spec/models/user_spec.rb (RSpec)\nRSpec.describe User, type: :model do\n  it 'is invalid without a name' do\n    expect(User.new(name: '')).not_to be_valid\n  end\nend\n# Fast; pinpoint: tells you the validation logic broke.\n\n# INTEGRATION test: parts together (routing -> controller -> model -> DB -> view)\n# spec/requests/users_spec.rb (request spec)\nRSpec.describe 'Users', type: :request do\n  it 'creates a user' do\n    post '/users', params: { user: { name: 'Al' } }\n    expect(response).to have_http_status(:found)   # 302 redirect\n    expect(User.count).to eq(1)                    # DB changed\n    expect(User.last.name).to eq('Al')\n  end\nend\n# test/system/users_test.rb (system test: full browser, JS)\nclass UsersTest < ActionDispatch::SystemTestCase\n  test 'create via form' do\n    visit '/users/new'\n    fill_in 'Name', with: 'Al'\n    click_on 'Create'\n    assert_text 'Al'\n  end\nend\n\n# Rails layers:    model (unit) -> request (integration) -> system (full browser/JS)\n# Pyramid:         many unit (fast) / fewer integration / very few system (slow).\n# Unit tests    -> what broke precisely (logic).\n# Integration  -> the wiring works (parts connect).\n# System       -> the user flow works in a real browser (incl. JS).\n# Balance: don't write ONLY integration (slow, hard to localize) or ONLY unit (misses wiring).\n```"
          }
        ]
      },
      "followup_questions": [
        "What's the difference between unit and integration tests in Rails?",
        "When would you use Unit Vs Integration in a real project?",
        "What is a common mistake when using Unit Vs Integration?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Unit Vs Integration with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Unit Vs Integration with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "testing-basics-unit-vs-integration-q004",
      "slug": "testing-basics-unit-vs-integration-scenario",
      "question": "How would you debug a problem involving Unit Vs Integration?",
      "title": "debug a problem involving Unit Vs Integration",
      "direct_answer": "I would debug a problem involving What's the difference between unit and integration tests in Rails by reproducing it with the smallest input first. Unit tests check one piece in isolation (a method, a model's validation): `UserTest` asserts `User.new(name: '').valid?` is false — fast, no DB/HTTP, pinpoint failures. Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain What's the difference between unit and integration tests in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about What's the difference between unit and integration tests in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what What's the difference between unit and integration tests in Rails is supposed to guarantee. Unit tests check one piece in isolation (a method, a model's validation): `UserTest` asserts `User.new(name: '').valid?` is false — fast, no DB/HTTP, pinpoint failures. Integration tests check multiple parts working together: a request spec hits `post '/users', params: ...` and asserts the DB changed and the response is 302 — slower (full stack: routing, controller, model, DB, views) but catches wiring bugs unit tests miss. Rails layers: model tests (unit), controller/request tests (integration), system tests (full browser, JS). Pyramid: many fast unit tests at the base, fewer integration, very few system (slow). Unit tests tell you WHAT broke precisely; integration tests tell you the WIRING works. Both matter: unit for fast feedback on logic, integration for confidence the pieces connect. Don't write only integration (slow, hard to localize) or only unit (misses wiring); balance them."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the What's the difference between unit and integration tests in Rails bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: Unit tests check one piece in isolation (a method, a model's validation): `UserTest` asserts `User.new(name: '').valid?` is false — fast, no DB/HTTP, pinpoint failures. Integration tests check multiple parts working together: a request spec hits `post '/users', params: ...` and asserts the DB changed and the response is 302 — slower (full stack: routing, controller, model, DB, views) but catches wiring bugs unit tests miss. Rails layers: model tests (unit), controller/request tests (integration), system tests (full browser, JS). Pyramid: many fast unit tests at the base, fewer integration, very few system (slow). Unit tests tell you WHAT broke precisely; integration tests tell you the WIRING works. Both matter: unit for fast feedback on logic, integration for confidence the pieces connect. Don't write only integration (slow, hard to localize) or only unit (misses wiring); balance them. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "Unit tests check one piece in isolation (a method, a model's validation): `UserTest` asserts `User.new(name: '').valid?` is false — fast, no DB/HTTP, pinpoint failures.\n\nIntegration tests check multiple parts working together: a request spec hits `post '/users', params: ...` and asserts the DB changed and the response is 302 — slower (full stack: routing, controller, model, DB, views) but catches wiring bugs unit tests miss.\n\nRails layers: model tests (unit), controller/request tests (integration), system tests (full browser, JS). Pyramid: many fast unit tests at the base, fewer integration, very few system (slow). Unit tests tell you WHAT broke precisely; integration tests tell you the WIRING works. Both matter: unit for fast feedback on logic, integration for confidence the pieces connect. Don't write only integration (slow, hard to localize) or only unit (misses wiring); balance them."
          },
          {
            "type": "code_example",
            "title": "Unit vs integration",
            "content": "```ruby\n# UNIT test: one piece in isolation (fast, no DB/HTTP)\n# test/models/user_test.rb (Minitest)\nclass UserTest < ActiveSupport::TestCase\n  test 'invalid without a name' do\n    user = User.new(name: '')\n    refute user.valid?                     # just the validation logic\n    assert_equal [\"can't be blank\"], user.errors[:name]\n  end\nend\n# spec/models/user_spec.rb (RSpec)\nRSpec.describe User, type: :model do\n  it 'is invalid without a name' do\n    expect(User.new(name: '')).not_to be_valid\n  end\nend\n# Fast; pinpoint: tells you the validation logic broke.\n\n# INTEGRATION test: parts together (routing -> controller -> model -> DB -> view)\n# spec/requests/users_spec.rb (request spec)\nRSpec.describe 'Users', type: :request do\n  it 'creates a user' do\n    post '/users', params: { user: { name: 'Al' } }\n    expect(response).to have_http_status(:found)   # 302 redirect\n    expect(User.count).to eq(1)                    # DB changed\n    expect(User.last.name).to eq('Al')\n  end\nend\n# test/system/users_test.rb (system test: full browser, JS)\nclass UsersTest < ActionDispatch::SystemTestCase\n  test 'create via form' do\n    visit '/users/new'\n    fill_in 'Name', with: 'Al'\n    click_on 'Create'\n    assert_text 'Al'\n  end\nend\n\n# Rails layers:    model (unit) -> request (integration) -> system (full browser/JS)\n# Pyramid:         many unit (fast) / fewer integration / very few system (slow).\n# Unit tests    -> what broke precisely (logic).\n# Integration  -> the wiring works (parts connect).\n# System       -> the user flow works in a real browser (incl. JS).\n# Balance: don't write ONLY integration (slow, hard to localize) or ONLY unit (misses wiring).\n```"
          }
        ]
      },
      "followup_questions": [
        "What's the difference between unit and integration tests in Rails?",
        "When would you use Unit Vs Integration in a real project?",
        "What is a common mistake when using Unit Vs Integration?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Unit Vs Integration — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Unit Vs Integration? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

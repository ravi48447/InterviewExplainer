{
  "topic": "Attr Accessor",
  "topicSlug": "attr-accessor",
  "questions": [
    {
      "id": "attr-accessor-q001",
      "slug": "ruby-attr-accessor-basics",
      "question": "What are attr_accessor and friends in Ruby?",
      "title": "attr_accessor and Friends",
      "direct_answer": "`attr_accessor` generates getters and setters for instance variables: `attr_accessor :name` creates `name` (getter) and `name=` (setter). `attr_reader` = getter only (read-only); `attr_writer` = setter only (write-only); `attr_accessor` = both. They're meta-programming — Ruby defines the methods for you: def name; @name; end; def name=(v); @name = v; end is what attr_accessor :name expands to. Instance variables (@name) are private by default; attr_* exposes them. Use attr_reader by default (don't expose setters you don't need); use attr_accessor only when both make sense. You can pass multiple names: attr_accessor :name, :age. They're not fields — they're method definitions; you can override them later in the class if you need custom logic.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know attr_reader (getter), attr_writer (setter), attr_accessor (both), and that they generate methods from instance variables.",
        "common_mistake": "Over-exposing with attr_accessor when attr_reader would do (don't expose setters you don't need).",
        "to_stand_out": "attr_accessor = getter + setter; attr_reader = getter only; attr_writer = setter only. Generate methods from @vars. Prefer attr_reader by default."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define attr_accessor and Friends simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "attr_accessor generates a getter + setter; attr_reader = getter only; attr_writer = setter only.",
              "attr_accessor :name expands to def name; @name; end and def name=(v); @name = v; end.",
              "Instance variables (@name) are private by default; attr_* exposes them.",
              "Pass multiple names: attr_accessor :name, :age.",
              "Prefer attr_reader by default; add setters only when you need them."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "attr_accessor generates getters and setters for instance variables: attr_accessor name creates name (getter) and name equals (setter). attr_reader equals getter only (read-only); attr_writer equals setter only (write-only); attr_accessor equals both. They're meta-programming — Ruby defines the methods for you: def name; name var; def name equals v; name var equals v; end is what attr_accessor name expands to. Instance variables (name var) are private by default; attr star exposes them. Use attr_reader by default (don't expose setters you don't need); use attr_accessor only when both make sense. You can pass multiple names: attr_accessor name age. They're not fields — they're method definitions; you can override them later in the class if you need custom logic. A common mistake is over-exposing with attr_accessor when attr_reader would do, exposing setters you don't need."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "`attr_accessor` generates getters and setters for instance variables: `attr_accessor :name` creates `name` (getter) and `name=` (setter).\n\n`attr_reader` = getter only (read-only); `attr_writer` = setter only (write-only); `attr_accessor` = both.\n\nThey're meta-programming — Ruby defines the methods for you: def name; @name; end; def name=(v); @name = v; end is what attr_accessor :name expands to. Instance variables (@name) are private by default; attr_* exposes them. Use attr_reader by default (don't expose setters you don't need); use attr_accessor only when both make sense. You can pass multiple names: attr_accessor :name, :age. They're not fields — they're method definitions; you can override them later in the class if you need custom logic.\n\nOver-exposing with attr_accessor when attr_reader would do (don't expose setters you don't need)."
          },
          {
            "type": "flow_diagram",
            "title": "attr_*",
            "content": "attr_reader=getter; attr_writer=setter; attr_accessor=both. Generate from @vars. Prefer attr_reader.\n\n```mermaid\nflowchart LR\n  R[attr_reader :name] --> G[name -> @name]\n  W[attr_writer :name] --> S[name= -> @name=]\n  A[attr_accessor :name] --> B[both]\n```"
          },
          {
            "type": "code_example",
            "title": "attr_accessor",
            "content": "```ruby\nclass Person\n  attr_accessor :name, :age     # getter + setter for both\n  attr_reader :id               # getter ONLY (read-only)\n  attr_writer :secret           # setter ONLY (write-only)\n\n  def initialize(id, name, age)\n    @id, @name, @age = id, name, age\n    @secret = nil\n  end\nend\n\np = Person.new(1, 'Ada', 30)\n\n# attr_accessor :name, :age -> getter + setter\np.name            # 'Ada'  (getter)\np.name = 'Bob'    # setter\np.age = 31\n\n# attr_reader :id -> getter only\np.id              # 1\n# p.id = 2        # NoMethodError (no setter)\n\n# attr_writer :secret -> setter only\np.secret = 'x'\n# p.secret        # NoMethodError (no getter)\n\n# What attr_accessor :name EXPANDS to:\n#   def name;     @name; end   # getter\n#   def name=(v); @name = v; end # setter\n\n# Prefer attr_reader by default; add setters only when needed.\n# Multiple names: attr_accessor :a, :b, :c\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Attr Accessor in a real project?",
        "How would you compare Attr Accessor with an alternative?",
        "What is a common mistake when using Attr Accessor?"
      ],
      "seo": {
        "metaTitle": "attr_accessor and Friends — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What are attr_accessor and friends in Ruby? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "ruby-oop-basics-attr-accessor-q001",
      "slug": "ruby-oop-basics-attr-accessor-when-to-use",
      "question": "When would you use Attr Accessor in a real project?",
      "title": "When would you use Attr Accessor in a real project",
      "direct_answer": "I would use attr_accessor and friends in Ruby when the problem matches its main benefit. `attr_accessor` generates getters and setters for instance variables: `attr_accessor :name` creates `name` (getter) and `name=` (setter). In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain attr_accessor and friends in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about attr_accessor and friends in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining attr_accessor and friends in Ruby in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. `attr_accessor` generates getters and setters for instance variables: `attr_accessor :name` creates `name` (getter) and `name=` (setter). `attr_reader` = getter only (read-only); `attr_writer` = setter only (write-only); `attr_accessor` = both. They're meta-programming — Ruby defines the methods for you: def name; @name; end; def name=(v); @name = v; end is what attr_accessor :name expands to. Instance variables (@name) are private by default; attr_* exposes them. Use attr_reader by default (don't expose setters you don't need); use attr_accessor only when both make sense. You can pass multiple names: attr_accessor :name, :age. They're not fields — they're method definitions; you can override them later in the class if you need custom logic."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use attr_accessor and friends in Ruby when its rule makes the code safer, clearer, or more efficient for the actual problem. `attr_accessor` generates getters and setters for instance variables: `attr_accessor :name` creates `name` (getter) and `name=` (setter). `attr_reader` = getter only (read-only); `attr_writer` = setter only (write-only); `attr_accessor` = both. They're meta-programming — Ruby defines the methods for you: def name; @name; end; def name=(v); @name = v; end is what attr_accessor :name expands to. Instance variables (@name) are private by default; attr_* exposes them. Use attr_reader by default (don't expose setters you don't need); use attr_accessor only when both make sense. You can pass multiple names: attr_accessor :name, :age. They're not fields — they're method definitions; you can override them later in the class if you need custom logic. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "`attr_accessor` generates getters and setters for instance variables: `attr_accessor :name` creates `name` (getter) and `name=` (setter).\n\n`attr_reader` = getter only (read-only); `attr_writer` = setter only (write-only); `attr_accessor` = both.\n\nThey're meta-programming — Ruby defines the methods for you: def name; @name; end; def name=(v); @name = v; end is what attr_accessor :name expands to. Instance variables (@name) are private by default; attr_* exposes them. Use attr_reader by default (don't expose setters you don't need); use attr_accessor only when both make sense. You can pass multiple names: attr_accessor :name, :age. They're not fields — they're method definitions; you can override them later in the class if you need custom logic.\n\nOver-exposing with attr_accessor when attr_reader would do (don't expose setters you don't need)."
          },
          {
            "type": "code_example",
            "title": "attr_accessor",
            "content": "```ruby\nclass Person\n  attr_accessor :name, :age     # getter + setter for both\n  attr_reader :id               # getter ONLY (read-only)\n  attr_writer :secret           # setter ONLY (write-only)\n\n  def initialize(id, name, age)\n    @id, @name, @age = id, name, age\n    @secret = nil\n  end\nend\n\np = Person.new(1, 'Ada', 30)\n\n# attr_accessor :name, :age -> getter + setter\np.name            # 'Ada'  (getter)\np.name = 'Bob'    # setter\np.age = 31\n\n# attr_reader :id -> getter only\np.id              # 1\n# p.id = 2        # NoMethodError (no setter)\n\n# attr_writer :secret -> setter only\np.secret = 'x'\n# p.secret        # NoMethodError (no getter)\n\n# What attr_accessor :name EXPANDS to:\n#   def name;     @name; end   # getter\n#   def name=(v); @name = v; end # setter\n\n# Prefer attr_reader by default; add setters only when needed.\n# Multiple names: attr_accessor :a, :b, :c\n```"
          }
        ]
      },
      "followup_questions": [
        "What are attr_accessor and friends in Ruby?",
        "How would you compare Attr Accessor with an alternative?",
        "What is a common mistake when using Attr Accessor?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Attr Accessor in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Attr Accessor in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-oop-basics-attr-accessor-q002",
      "slug": "ruby-oop-basics-attr-accessor-common-mistake",
      "question": "What is a common mistake when using Attr Accessor?",
      "title": "a common mistake when using Attr Accessor",
      "direct_answer": "A common mistake with attr_accessor and friends in Ruby is using the rule without checking its boundary conditions. `attr_accessor` generates getters and setters for instance variables: `attr_accessor :name` creates `name` (getter) and `name=` (setter). That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain attr_accessor and friends in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about attr_accessor and friends in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of attr_accessor and friends in Ruby. `attr_accessor` generates getters and setters for instance variables: `attr_accessor :name` creates `name` (getter) and `name=` (setter). `attr_reader` = getter only (read-only); `attr_writer` = setter only (write-only); `attr_accessor` = both. They're meta-programming — Ruby defines the methods for you: def name; @name; end; def name=(v); @name = v; end is what attr_accessor :name expands to. Instance variables (@name) are private by default; attr_* exposes them. Use attr_reader by default (don't expose setters you don't need); use attr_accessor only when both make sense. You can pass multiple names: attr_accessor :name, :age. They're not fields — they're method definitions; you can override them later in the class if you need custom logic."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with attr_accessor and friends in Ruby is assuming the happy path is the whole rule. `attr_accessor` generates getters and setters for instance variables: `attr_accessor :name` creates `name` (getter) and `name=` (setter). `attr_reader` = getter only (read-only); `attr_writer` = setter only (write-only); `attr_accessor` = both. They're meta-programming — Ruby defines the methods for you: def name; @name; end; def name=(v); @name = v; end is what attr_accessor :name expands to. Instance variables (@name) are private by default; attr_* exposes them. Use attr_reader by default (don't expose setters you don't need); use attr_accessor only when both make sense. You can pass multiple names: attr_accessor :name, :age. They're not fields — they're method definitions; you can override them later in the class if you need custom logic. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "`attr_accessor` generates getters and setters for instance variables: `attr_accessor :name` creates `name` (getter) and `name=` (setter).\n\n`attr_reader` = getter only (read-only); `attr_writer` = setter only (write-only); `attr_accessor` = both.\n\nThey're meta-programming — Ruby defines the methods for you: def name; @name; end; def name=(v); @name = v; end is what attr_accessor :name expands to. Instance variables (@name) are private by default; attr_* exposes them. Use attr_reader by default (don't expose setters you don't need); use attr_accessor only when both make sense. You can pass multiple names: attr_accessor :name, :age. They're not fields — they're method definitions; you can override them later in the class if you need custom logic.\n\nOver-exposing with attr_accessor when attr_reader would do (don't expose setters you don't need)."
          },
          {
            "type": "code_example",
            "title": "attr_accessor",
            "content": "```ruby\nclass Person\n  attr_accessor :name, :age     # getter + setter for both\n  attr_reader :id               # getter ONLY (read-only)\n  attr_writer :secret           # setter ONLY (write-only)\n\n  def initialize(id, name, age)\n    @id, @name, @age = id, name, age\n    @secret = nil\n  end\nend\n\np = Person.new(1, 'Ada', 30)\n\n# attr_accessor :name, :age -> getter + setter\np.name            # 'Ada'  (getter)\np.name = 'Bob'    # setter\np.age = 31\n\n# attr_reader :id -> getter only\np.id              # 1\n# p.id = 2        # NoMethodError (no setter)\n\n# attr_writer :secret -> setter only\np.secret = 'x'\n# p.secret        # NoMethodError (no getter)\n\n# What attr_accessor :name EXPANDS to:\n#   def name;     @name; end   # getter\n#   def name=(v); @name = v; end # setter\n\n# Prefer attr_reader by default; add setters only when needed.\n# Multiple names: attr_accessor :a, :b, :c\n```"
          }
        ]
      },
      "followup_questions": [
        "What are attr_accessor and friends in Ruby?",
        "When would you use Attr Accessor in a real project?",
        "How would you compare Attr Accessor with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Attr Accessor — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Attr Accessor? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-oop-basics-attr-accessor-q003",
      "slug": "ruby-oop-basics-attr-accessor-compare",
      "question": "How would you compare Attr Accessor with an alternative?",
      "title": "compare Attr Accessor with an alternative",
      "direct_answer": "I compare attr_accessor and friends in Ruby with the simpler alternative by looking at correctness first, then time, memory, and readability. `attr_accessor` generates getters and setters for instance variables: `attr_accessor :name` creates `name` (getter) and `name=` (setter). The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain attr_accessor and friends in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about attr_accessor and friends in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. `attr_accessor` generates getters and setters for instance variables: `attr_accessor :name` creates `name` (getter) and `name=` (setter). `attr_reader` = getter only (read-only); `attr_writer` = setter only (write-only); `attr_accessor` = both. They're meta-programming — Ruby defines the methods for you: def name; @name; end; def name=(v); @name = v; end is what attr_accessor :name expands to. Instance variables (@name) are private by default; attr_* exposes them. Use attr_reader by default (don't expose setters you don't need); use attr_accessor only when both make sense. You can pass multiple names: attr_accessor :name, :age. They're not fields — they're method definitions; you can override them later in the class if you need custom logic."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare attr_accessor and friends in Ruby with the straightforward alternative on the same example. `attr_accessor` generates getters and setters for instance variables: `attr_accessor :name` creates `name` (getter) and `name=` (setter). `attr_reader` = getter only (read-only); `attr_writer` = setter only (write-only); `attr_accessor` = both. They're meta-programming — Ruby defines the methods for you: def name; @name; end; def name=(v); @name = v; end is what attr_accessor :name expands to. Instance variables (@name) are private by default; attr_* exposes them. Use attr_reader by default (don't expose setters you don't need); use attr_accessor only when both make sense. You can pass multiple names: attr_accessor :name, :age. They're not fields — they're method definitions; you can override them later in the class if you need custom logic. If attr_accessor and friends in Ruby removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "`attr_accessor` generates getters and setters for instance variables: `attr_accessor :name` creates `name` (getter) and `name=` (setter).\n\n`attr_reader` = getter only (read-only); `attr_writer` = setter only (write-only); `attr_accessor` = both.\n\nThey're meta-programming — Ruby defines the methods for you: def name; @name; end; def name=(v); @name = v; end is what attr_accessor :name expands to. Instance variables (@name) are private by default; attr_* exposes them. Use attr_reader by default (don't expose setters you don't need); use attr_accessor only when both make sense. You can pass multiple names: attr_accessor :name, :age. They're not fields — they're method definitions; you can override them later in the class if you need custom logic.\n\nOver-exposing with attr_accessor when attr_reader would do (don't expose setters you don't need)."
          },
          {
            "type": "code_example",
            "title": "attr_accessor",
            "content": "```ruby\nclass Person\n  attr_accessor :name, :age     # getter + setter for both\n  attr_reader :id               # getter ONLY (read-only)\n  attr_writer :secret           # setter ONLY (write-only)\n\n  def initialize(id, name, age)\n    @id, @name, @age = id, name, age\n    @secret = nil\n  end\nend\n\np = Person.new(1, 'Ada', 30)\n\n# attr_accessor :name, :age -> getter + setter\np.name            # 'Ada'  (getter)\np.name = 'Bob'    # setter\np.age = 31\n\n# attr_reader :id -> getter only\np.id              # 1\n# p.id = 2        # NoMethodError (no setter)\n\n# attr_writer :secret -> setter only\np.secret = 'x'\n# p.secret        # NoMethodError (no getter)\n\n# What attr_accessor :name EXPANDS to:\n#   def name;     @name; end   # getter\n#   def name=(v); @name = v; end # setter\n\n# Prefer attr_reader by default; add setters only when needed.\n# Multiple names: attr_accessor :a, :b, :c\n```"
          }
        ]
      },
      "followup_questions": [
        "What are attr_accessor and friends in Ruby?",
        "When would you use Attr Accessor in a real project?",
        "What is a common mistake when using Attr Accessor?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Attr Accessor with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Attr Accessor with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "ruby-oop-basics-attr-accessor-q004",
      "slug": "ruby-oop-basics-attr-accessor-scenario",
      "question": "How would you debug a problem involving Attr Accessor?",
      "title": "debug a problem involving Attr Accessor",
      "direct_answer": "I would debug a problem involving attr_accessor and friends in Ruby by reproducing it with the smallest input first. `attr_accessor` generates getters and setters for instance variables: `attr_accessor :name` creates `name` (getter) and `name=` (setter). Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain attr_accessor and friends in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about attr_accessor and friends in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what attr_accessor and friends in Ruby is supposed to guarantee. `attr_accessor` generates getters and setters for instance variables: `attr_accessor :name` creates `name` (getter) and `name=` (setter). `attr_reader` = getter only (read-only); `attr_writer` = setter only (write-only); `attr_accessor` = both. They're meta-programming — Ruby defines the methods for you: def name; @name; end; def name=(v); @name = v; end is what attr_accessor :name expands to. Instance variables (@name) are private by default; attr_* exposes them. Use attr_reader by default (don't expose setters you don't need); use attr_accessor only when both make sense. You can pass multiple names: attr_accessor :name, :age. They're not fields — they're method definitions; you can override them later in the class if you need custom logic."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the attr_accessor and friends in Ruby bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: `attr_accessor` generates getters and setters for instance variables: `attr_accessor :name` creates `name` (getter) and `name=` (setter). `attr_reader` = getter only (read-only); `attr_writer` = setter only (write-only); `attr_accessor` = both. They're meta-programming — Ruby defines the methods for you: def name; @name; end; def name=(v); @name = v; end is what attr_accessor :name expands to. Instance variables (@name) are private by default; attr_* exposes them. Use attr_reader by default (don't expose setters you don't need); use attr_accessor only when both make sense. You can pass multiple names: attr_accessor :name, :age. They're not fields — they're method definitions; you can override them later in the class if you need custom logic. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "`attr_accessor` generates getters and setters for instance variables: `attr_accessor :name` creates `name` (getter) and `name=` (setter).\n\n`attr_reader` = getter only (read-only); `attr_writer` = setter only (write-only); `attr_accessor` = both.\n\nThey're meta-programming — Ruby defines the methods for you: def name; @name; end; def name=(v); @name = v; end is what attr_accessor :name expands to. Instance variables (@name) are private by default; attr_* exposes them. Use attr_reader by default (don't expose setters you don't need); use attr_accessor only when both make sense. You can pass multiple names: attr_accessor :name, :age. They're not fields — they're method definitions; you can override them later in the class if you need custom logic.\n\nOver-exposing with attr_accessor when attr_reader would do (don't expose setters you don't need)."
          },
          {
            "type": "code_example",
            "title": "attr_accessor",
            "content": "```ruby\nclass Person\n  attr_accessor :name, :age     # getter + setter for both\n  attr_reader :id               # getter ONLY (read-only)\n  attr_writer :secret           # setter ONLY (write-only)\n\n  def initialize(id, name, age)\n    @id, @name, @age = id, name, age\n    @secret = nil\n  end\nend\n\np = Person.new(1, 'Ada', 30)\n\n# attr_accessor :name, :age -> getter + setter\np.name            # 'Ada'  (getter)\np.name = 'Bob'    # setter\np.age = 31\n\n# attr_reader :id -> getter only\np.id              # 1\n# p.id = 2        # NoMethodError (no setter)\n\n# attr_writer :secret -> setter only\np.secret = 'x'\n# p.secret        # NoMethodError (no getter)\n\n# What attr_accessor :name EXPANDS to:\n#   def name;     @name; end   # getter\n#   def name=(v); @name = v; end # setter\n\n# Prefer attr_reader by default; add setters only when needed.\n# Multiple names: attr_accessor :a, :b, :c\n```"
          }
        ]
      },
      "followup_questions": [
        "What are attr_accessor and friends in Ruby?",
        "When would you use Attr Accessor in a real project?",
        "How would you compare Attr Accessor with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Attr Accessor — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Attr Accessor? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

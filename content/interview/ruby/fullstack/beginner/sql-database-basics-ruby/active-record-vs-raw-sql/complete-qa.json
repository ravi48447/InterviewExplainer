{
  "topic": "Active Record vs Raw SQL",
  "topicSlug": "active-record-vs-raw-sql",
  "questions": [
    {
      "id": "active-record-vs-raw-sql-q001",
      "slug": "ruby-active-record-vs-raw-sql",
      "question": "When would you use Active Record vs raw SQL in Rails?",
      "title": "Active Record vs Raw SQL",
      "direct_answer": "Active Record (Rails ORM) maps tables to Ruby classes: User.find(1), User.where(active: true) generate SQL for you — readable, safe (parameterized), and schema-aware. Use it for normal CRUD and simple queries. Use raw SQL (find_by_sql, exec_query, connection.execute) when you need something AR can't express efficiently: complex joins/aggregations, window functions, a query that AR turns into N+1, or bulk operations. Even then, bind parameters (find_by_sql(['... WHERE id = ?', id]); connection.query_to_select) to avoid injection. Trade-off: AR is faster to write and easier to maintain but slower for complex queries; raw SQL is fast and expressive but loses AR's safety/conveniences and must be hand-bound. Rule: reach for AR first; drop to raw SQL only when AR is awkward or too slow, and always parameterize.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know AR is the default (readable/safe) and raw SQL is for complex/perf-critical queries — but must be parameterized.",
        "common_mistake": "Using raw SQL with string interpolation (injection) — always bind; or avoiding raw SQL even when N+1 hits.",
        "to_stand_out": "AR for normal CRUD/simple queries (readable, safe). Raw SQL for complex joins/aggregates/bulk where AR is awkward/slow — but parameterize."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define Active Record vs Raw SQL simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "Active Record maps tables to Ruby classes; generates safe, parameterized SQL.",
              "Use AR for normal CRUD and simple queries (readable, safe, schema-aware).",
              "Use raw SQL for complex joins/aggregates, window functions, bulk ops, or N+1 cases.",
              "With raw SQL, ALWAYS bind parameters (['... WHERE id = ?', id]) to avoid injection.",
              "AR is faster to write; raw SQL is faster to run for complex queries — reach for AR first."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "Active Record (Rails ORM) maps tables to Ruby classes: User find 1, User where active true generate SQL for you — readable, safe (parameterized), and schema-aware. Use it for normal CRUD and simple queries. Use raw SQL (find by sql, exec query, connection execute) when you need something AR can't express efficiently: complex joins or aggregates, window functions, a query that AR turns into N+1, or bulk operations. Even then, bind parameters (find by sql with a WHERE id question mark id; or exec query with binds) to avoid injection. Trade-off: AR is faster to write and easier to maintain but slower for complex queries; raw SQL is fast and expressive but loses AR's safety and conveniences and must be hand-bound. Rule: reach for AR first; drop to raw SQL only when AR is awkward or too slow, and always parameterize. A common mistake is using raw SQL with string interpolation, an injection risk, when you should always bind parameters."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "Active Record (Rails ORM) maps tables to Ruby classes: User.find(1), User.where(active: true) generate SQL for you — readable, safe (parameterized), and schema-aware.\n\nUse it for normal CRUD and simple queries.\n\nUse raw SQL (find_by_sql, exec_query, connection.execute) when you need something AR can't express efficiently: complex joins/aggregations, window functions, a query that AR turns into N+1, or bulk operations. Even then, bind parameters (find_by_sql(['... WHERE id = ?', id]); connection.query_to_select) to avoid injection. Trade-off: AR is faster to write and easier to maintain but slower for complex queries; raw SQL is fast and expressive but loses AR's safety/conveniences and must be hand-bound. Rule: reach for AR first; drop to raw SQL only when AR is awkward or too slow, and always parameterize.\n\nA common mistake is using raw SQL with string interpolation (injection) — always bind; or avoiding raw SQL even when N+1 hits."
          },
          {
            "type": "flow_diagram",
            "title": "AR vs Raw SQL",
            "content": "AR for normal CRUD (readable, safe). Raw SQL for complex/bulk where AR is awkward/slow — but parameterize.\n\n```mermaid\nflowchart LR\n  N[normal CRUD] --> AR[Active Record]\n  AR --> S[safe, readable]\n  C[complex joins / aggregates] --> R[raw SQL]\n  B[bulk / perf-critical] --> R\n  R --> P[MUST bind params]\n```"
          },
          {
            "type": "code_example",
            "title": "Active Record vs raw SQL",
            "content": "```ruby\n# Active Record (default): maps tables to Ruby classes, generates safe SQL\nUser.find(1)                          # SELECT * FROM users WHERE id = 1\nUser.where(active: true)              # ... WHERE active = TRUE (parameterized)\nUser.joins(:posts).where(posts: {published: true})\nUser.order(created_at: :desc).limit(10)\nUser.count                            # SELECT COUNT(*)\n# Readable, safe (parameterized), schema-aware. Use for normal CRUD.\n\n# Raw SQL: when AR is awkward or too slow.\n# 1) Complex joins / aggregates AR can't express well:\nUser.find_by_sql(['SELECT users.*, COUNT(p.id) AS pc\n                    FROM users LEFT JOIN posts p ON p.user_id = users.id\n                    GROUP BY users.id HAVING COUNT(p.id) > ?', 10])\n\n# 2) Window functions / DB-specific features:\nUser.connection.execute(\"SELECT id, RANK() OVER (ORDER BY score DESC) FROM users\")\n\n# 3) Bulk updates (faster than updating records one-by-one):\nUser.update_all('last_seen = NOW()')        # AR bulk (still AR)\nUser.connection.update(\"UPDATE users SET status = 1 WHERE id IN (1,2,3)\")\n\n# 4) Reporting queries (read a few columns, many rows):\nresult = User.connection.exec_query(\"SELECT DATE(created_at) d, COUNT(*) c FROM signups GROUP BY d\")\nresult.to_a  # [{\"d\"=>..., \"c\"=>...}, ...]\n\n# ALWAYS BIND PARAMETERS with raw SQL (avoid injection):\n#   BAD:  \"... WHERE name = '#{name}'\"          # injection\n#   GOOD: ['... WHERE name = ?', name]            # bound\n# Trade-off: AR = faster to write, safe, easy; raw = fast, expressive, must hand-bind.\n# Rule: AR first; raw SQL only when AR is awkward/slow; always parameterize.\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Active Record Vs Raw Sql in a real project?",
        "What is a common mistake when using Active Record Vs Raw Sql?",
        "How would you compare Active Record Vs Raw Sql with an alternative?"
      ],
      "seo": {
        "metaTitle": "Active Record vs Raw SQL — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Active Record vs raw SQL in Rails? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "sql-database-basics-ruby-active-record-vs-raw-sql-q001",
      "slug": "sql-database-basics-ruby-active-record-vs-raw-sql-when-to-use",
      "question": "When would you use Active Record Vs Raw Sql in a real project?",
      "title": "When would you use Active Record Vs Raw Sql in a real project",
      "direct_answer": "I would use When would you use Active Record vs raw SQL in Rails when the problem matches its main benefit. Active Record (Rails ORM) maps tables to Ruby classes: User.find(1), User.where(active: true) generate SQL for you — readable, safe (parameterized), and schema-aware. In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain When would you use Active Record vs raw SQL in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about When would you use Active Record vs raw SQL in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining When would you use Active Record vs raw SQL in Rails in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. Active Record (Rails ORM) maps tables to Ruby classes: User.find(1), User.where(active: true) generate SQL for you — readable, safe (parameterized), and schema-aware. Use it for normal CRUD and simple queries. Use raw SQL (find_by_sql, exec_query, connection.execute) when you need something AR can't express efficiently: complex joins/aggregations, window functions, a query that AR turns into N+1, or bulk operations. Even then, bind parameters (find_by_sql(['... WHERE id = ?', id]); connection.query_to_select) to avoid injection. Trade-off: AR is faster to write and easier to maintain but slower for complex queries; raw SQL is fast and expressive but loses AR's safety/conveniences and must be hand-bound. Rule: reach for AR first; drop to raw SQL only when AR is awkward or too slow, and always parameterize."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use When would you use Active Record vs raw SQL in Rails when its rule makes the code safer, clearer, or more efficient for the actual problem. Active Record (Rails ORM) maps tables to Ruby classes: User.find(1), User.where(active: true) generate SQL for you — readable, safe (parameterized), and schema-aware. Use it for normal CRUD and simple queries. Use raw SQL (find_by_sql, exec_query, connection.execute) when you need something AR can't express efficiently: complex joins/aggregations, window functions, a query that AR turns into N+1, or bulk operations. Even then, bind parameters (find_by_sql(['... WHERE id = ?', id]); connection.query_to_select) to avoid injection. Trade-off: AR is faster to write and easier to maintain but slower for complex queries; raw SQL is fast and expressive but loses AR's safety/conveniences and must be hand-bound. Rule: reach for AR first; drop to raw SQL only when AR is awkward or too slow, and always parameterize. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "Active Record (Rails ORM) maps tables to Ruby classes: User.find(1), User.where(active: true) generate SQL for you — readable, safe (parameterized), and schema-aware.\n\nUse it for normal CRUD and simple queries.\n\nUse raw SQL (find_by_sql, exec_query, connection.execute) when you need something AR can't express efficiently: complex joins/aggregations, window functions, a query that AR turns into N+1, or bulk operations. Even then, bind parameters (find_by_sql(['... WHERE id = ?', id]); connection.query_to_select) to avoid injection. Trade-off: AR is faster to write and easier to maintain but slower for complex queries; raw SQL is fast and expressive but loses AR's safety/conveniences and must be hand-bound. Rule: reach for AR first; drop to raw SQL only when AR is awkward or too slow, and always parameterize.\n\nA common mistake is using raw SQL with string interpolation (injection) — always bind; or avoiding raw SQL even when N+1 hits."
          },
          {
            "type": "code_example",
            "title": "Active Record vs raw SQL",
            "content": "```ruby\n# Active Record (default): maps tables to Ruby classes, generates safe SQL\nUser.find(1)                          # SELECT * FROM users WHERE id = 1\nUser.where(active: true)              # ... WHERE active = TRUE (parameterized)\nUser.joins(:posts).where(posts: {published: true})\nUser.order(created_at: :desc).limit(10)\nUser.count                            # SELECT COUNT(*)\n# Readable, safe (parameterized), schema-aware. Use for normal CRUD.\n\n# Raw SQL: when AR is awkward or too slow.\n# 1) Complex joins / aggregates AR can't express well:\nUser.find_by_sql(['SELECT users.*, COUNT(p.id) AS pc\n                    FROM users LEFT JOIN posts p ON p.user_id = users.id\n                    GROUP BY users.id HAVING COUNT(p.id) > ?', 10])\n\n# 2) Window functions / DB-specific features:\nUser.connection.execute(\"SELECT id, RANK() OVER (ORDER BY score DESC) FROM users\")\n\n# 3) Bulk updates (faster than updating records one-by-one):\nUser.update_all('last_seen = NOW()')        # AR bulk (still AR)\nUser.connection.update(\"UPDATE users SET status = 1 WHERE id IN (1,2,3)\")\n\n# 4) Reporting queries (read a few columns, many rows):\nresult = User.connection.exec_query(\"SELECT DATE(created_at) d, COUNT(*) c FROM signups GROUP BY d\")\nresult.to_a  # [{\"d\"=>..., \"c\"=>...}, ...]\n\n# ALWAYS BIND PARAMETERS with raw SQL (avoid injection):\n#   BAD:  \"... WHERE name = '#{name}'\"          # injection\n#   GOOD: ['... WHERE name = ?', name]            # bound\n# Trade-off: AR = faster to write, safe, easy; raw = fast, expressive, must hand-bind.\n# Rule: AR first; raw SQL only when AR is awkward/slow; always parameterize.\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Active Record vs raw SQL in Rails?",
        "What is a common mistake when using Active Record Vs Raw Sql?",
        "How would you compare Active Record Vs Raw Sql with an alternative?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Active Record Vs Raw Sql in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Active Record Vs Raw Sql in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "sql-database-basics-ruby-active-record-vs-raw-sql-q002",
      "slug": "sql-database-basics-ruby-active-record-vs-raw-sql-common-mistake",
      "question": "What is a common mistake when using Active Record Vs Raw Sql?",
      "title": "a common mistake when using Active Record Vs Raw Sql",
      "direct_answer": "A common mistake with When would you use Active Record vs raw SQL in Rails is using the rule without checking its boundary conditions. Active Record (Rails ORM) maps tables to Ruby classes: User.find(1), User.where(active: true) generate SQL for you — readable, safe (parameterized), and schema-aware. That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain When would you use Active Record vs raw SQL in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about When would you use Active Record vs raw SQL in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of When would you use Active Record vs raw SQL in Rails. Active Record (Rails ORM) maps tables to Ruby classes: User.find(1), User.where(active: true) generate SQL for you — readable, safe (parameterized), and schema-aware. Use it for normal CRUD and simple queries. Use raw SQL (find_by_sql, exec_query, connection.execute) when you need something AR can't express efficiently: complex joins/aggregations, window functions, a query that AR turns into N+1, or bulk operations. Even then, bind parameters (find_by_sql(['... WHERE id = ?', id]); connection.query_to_select) to avoid injection. Trade-off: AR is faster to write and easier to maintain but slower for complex queries; raw SQL is fast and expressive but loses AR's safety/conveniences and must be hand-bound. Rule: reach for AR first; drop to raw SQL only when AR is awkward or too slow, and always parameterize."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with When would you use Active Record vs raw SQL in Rails is assuming the happy path is the whole rule. Active Record (Rails ORM) maps tables to Ruby classes: User.find(1), User.where(active: true) generate SQL for you — readable, safe (parameterized), and schema-aware. Use it for normal CRUD and simple queries. Use raw SQL (find_by_sql, exec_query, connection.execute) when you need something AR can't express efficiently: complex joins/aggregations, window functions, a query that AR turns into N+1, or bulk operations. Even then, bind parameters (find_by_sql(['... WHERE id = ?', id]); connection.query_to_select) to avoid injection. Trade-off: AR is faster to write and easier to maintain but slower for complex queries; raw SQL is fast and expressive but loses AR's safety/conveniences and must be hand-bound. Rule: reach for AR first; drop to raw SQL only when AR is awkward or too slow, and always parameterize. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "Active Record (Rails ORM) maps tables to Ruby classes: User.find(1), User.where(active: true) generate SQL for you — readable, safe (parameterized), and schema-aware.\n\nUse it for normal CRUD and simple queries.\n\nUse raw SQL (find_by_sql, exec_query, connection.execute) when you need something AR can't express efficiently: complex joins/aggregations, window functions, a query that AR turns into N+1, or bulk operations. Even then, bind parameters (find_by_sql(['... WHERE id = ?', id]); connection.query_to_select) to avoid injection. Trade-off: AR is faster to write and easier to maintain but slower for complex queries; raw SQL is fast and expressive but loses AR's safety/conveniences and must be hand-bound. Rule: reach for AR first; drop to raw SQL only when AR is awkward or too slow, and always parameterize.\n\nA common mistake is using raw SQL with string interpolation (injection) — always bind; or avoiding raw SQL even when N+1 hits."
          },
          {
            "type": "code_example",
            "title": "Active Record vs raw SQL",
            "content": "```ruby\n# Active Record (default): maps tables to Ruby classes, generates safe SQL\nUser.find(1)                          # SELECT * FROM users WHERE id = 1\nUser.where(active: true)              # ... WHERE active = TRUE (parameterized)\nUser.joins(:posts).where(posts: {published: true})\nUser.order(created_at: :desc).limit(10)\nUser.count                            # SELECT COUNT(*)\n# Readable, safe (parameterized), schema-aware. Use for normal CRUD.\n\n# Raw SQL: when AR is awkward or too slow.\n# 1) Complex joins / aggregates AR can't express well:\nUser.find_by_sql(['SELECT users.*, COUNT(p.id) AS pc\n                    FROM users LEFT JOIN posts p ON p.user_id = users.id\n                    GROUP BY users.id HAVING COUNT(p.id) > ?', 10])\n\n# 2) Window functions / DB-specific features:\nUser.connection.execute(\"SELECT id, RANK() OVER (ORDER BY score DESC) FROM users\")\n\n# 3) Bulk updates (faster than updating records one-by-one):\nUser.update_all('last_seen = NOW()')        # AR bulk (still AR)\nUser.connection.update(\"UPDATE users SET status = 1 WHERE id IN (1,2,3)\")\n\n# 4) Reporting queries (read a few columns, many rows):\nresult = User.connection.exec_query(\"SELECT DATE(created_at) d, COUNT(*) c FROM signups GROUP BY d\")\nresult.to_a  # [{\"d\"=>..., \"c\"=>...}, ...]\n\n# ALWAYS BIND PARAMETERS with raw SQL (avoid injection):\n#   BAD:  \"... WHERE name = '#{name}'\"          # injection\n#   GOOD: ['... WHERE name = ?', name]            # bound\n# Trade-off: AR = faster to write, safe, easy; raw = fast, expressive, must hand-bind.\n# Rule: AR first; raw SQL only when AR is awkward/slow; always parameterize.\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Active Record vs raw SQL in Rails?",
        "When would you use Active Record Vs Raw Sql in a real project?",
        "How would you compare Active Record Vs Raw Sql with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Active Record Vs Raw Sql — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Active Record Vs Raw Sql? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "sql-database-basics-ruby-active-record-vs-raw-sql-q003",
      "slug": "sql-database-basics-ruby-active-record-vs-raw-sql-compare",
      "question": "How would you compare Active Record Vs Raw Sql with an alternative?",
      "title": "compare Active Record Vs Raw Sql with an alternative",
      "direct_answer": "I compare When would you use Active Record vs raw SQL in Rails with the simpler alternative by looking at correctness first, then time, memory, and readability. Active Record (Rails ORM) maps tables to Ruby classes: User.find(1), User.where(active: true) generate SQL for you — readable, safe (parameterized), and schema-aware. The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain When would you use Active Record vs raw SQL in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about When would you use Active Record vs raw SQL in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. Active Record (Rails ORM) maps tables to Ruby classes: User.find(1), User.where(active: true) generate SQL for you — readable, safe (parameterized), and schema-aware. Use it for normal CRUD and simple queries. Use raw SQL (find_by_sql, exec_query, connection.execute) when you need something AR can't express efficiently: complex joins/aggregations, window functions, a query that AR turns into N+1, or bulk operations. Even then, bind parameters (find_by_sql(['... WHERE id = ?', id]); connection.query_to_select) to avoid injection. Trade-off: AR is faster to write and easier to maintain but slower for complex queries; raw SQL is fast and expressive but loses AR's safety/conveniences and must be hand-bound. Rule: reach for AR first; drop to raw SQL only when AR is awkward or too slow, and always parameterize."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare When would you use Active Record vs raw SQL in Rails with the straightforward alternative on the same example. Active Record (Rails ORM) maps tables to Ruby classes: User.find(1), User.where(active: true) generate SQL for you — readable, safe (parameterized), and schema-aware. Use it for normal CRUD and simple queries. Use raw SQL (find_by_sql, exec_query, connection.execute) when you need something AR can't express efficiently: complex joins/aggregations, window functions, a query that AR turns into N+1, or bulk operations. Even then, bind parameters (find_by_sql(['... WHERE id = ?', id]); connection.query_to_select) to avoid injection. Trade-off: AR is faster to write and easier to maintain but slower for complex queries; raw SQL is fast and expressive but loses AR's safety/conveniences and must be hand-bound. Rule: reach for AR first; drop to raw SQL only when AR is awkward or too slow, and always parameterize. If When would you use Active Record vs raw SQL in Rails removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "Active Record (Rails ORM) maps tables to Ruby classes: User.find(1), User.where(active: true) generate SQL for you — readable, safe (parameterized), and schema-aware.\n\nUse it for normal CRUD and simple queries.\n\nUse raw SQL (find_by_sql, exec_query, connection.execute) when you need something AR can't express efficiently: complex joins/aggregations, window functions, a query that AR turns into N+1, or bulk operations. Even then, bind parameters (find_by_sql(['... WHERE id = ?', id]); connection.query_to_select) to avoid injection. Trade-off: AR is faster to write and easier to maintain but slower for complex queries; raw SQL is fast and expressive but loses AR's safety/conveniences and must be hand-bound. Rule: reach for AR first; drop to raw SQL only when AR is awkward or too slow, and always parameterize.\n\nA common mistake is using raw SQL with string interpolation (injection) — always bind; or avoiding raw SQL even when N+1 hits."
          },
          {
            "type": "code_example",
            "title": "Active Record vs raw SQL",
            "content": "```ruby\n# Active Record (default): maps tables to Ruby classes, generates safe SQL\nUser.find(1)                          # SELECT * FROM users WHERE id = 1\nUser.where(active: true)              # ... WHERE active = TRUE (parameterized)\nUser.joins(:posts).where(posts: {published: true})\nUser.order(created_at: :desc).limit(10)\nUser.count                            # SELECT COUNT(*)\n# Readable, safe (parameterized), schema-aware. Use for normal CRUD.\n\n# Raw SQL: when AR is awkward or too slow.\n# 1) Complex joins / aggregates AR can't express well:\nUser.find_by_sql(['SELECT users.*, COUNT(p.id) AS pc\n                    FROM users LEFT JOIN posts p ON p.user_id = users.id\n                    GROUP BY users.id HAVING COUNT(p.id) > ?', 10])\n\n# 2) Window functions / DB-specific features:\nUser.connection.execute(\"SELECT id, RANK() OVER (ORDER BY score DESC) FROM users\")\n\n# 3) Bulk updates (faster than updating records one-by-one):\nUser.update_all('last_seen = NOW()')        # AR bulk (still AR)\nUser.connection.update(\"UPDATE users SET status = 1 WHERE id IN (1,2,3)\")\n\n# 4) Reporting queries (read a few columns, many rows):\nresult = User.connection.exec_query(\"SELECT DATE(created_at) d, COUNT(*) c FROM signups GROUP BY d\")\nresult.to_a  # [{\"d\"=>..., \"c\"=>...}, ...]\n\n# ALWAYS BIND PARAMETERS with raw SQL (avoid injection):\n#   BAD:  \"... WHERE name = '#{name}'\"          # injection\n#   GOOD: ['... WHERE name = ?', name]            # bound\n# Trade-off: AR = faster to write, safe, easy; raw = fast, expressive, must hand-bind.\n# Rule: AR first; raw SQL only when AR is awkward/slow; always parameterize.\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Active Record vs raw SQL in Rails?",
        "When would you use Active Record Vs Raw Sql in a real project?",
        "What is a common mistake when using Active Record Vs Raw Sql?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Active Record Vs Raw Sql with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Active Record Vs Raw Sql with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "sql-database-basics-ruby-active-record-vs-raw-sql-q004",
      "slug": "sql-database-basics-ruby-active-record-vs-raw-sql-scenario",
      "question": "How would you debug a problem involving Active Record Vs Raw Sql?",
      "title": "debug a problem involving Active Record Vs Raw Sql",
      "direct_answer": "I would debug a problem involving When would you use Active Record vs raw SQL in Rails by reproducing it with the smallest input first. Active Record (Rails ORM) maps tables to Ruby classes: User.find(1), User.where(active: true) generate SQL for you — readable, safe (parameterized), and schema-aware. Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain When would you use Active Record vs raw SQL in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about When would you use Active Record vs raw SQL in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what When would you use Active Record vs raw SQL in Rails is supposed to guarantee. Active Record (Rails ORM) maps tables to Ruby classes: User.find(1), User.where(active: true) generate SQL for you — readable, safe (parameterized), and schema-aware. Use it for normal CRUD and simple queries. Use raw SQL (find_by_sql, exec_query, connection.execute) when you need something AR can't express efficiently: complex joins/aggregations, window functions, a query that AR turns into N+1, or bulk operations. Even then, bind parameters (find_by_sql(['... WHERE id = ?', id]); connection.query_to_select) to avoid injection. Trade-off: AR is faster to write and easier to maintain but slower for complex queries; raw SQL is fast and expressive but loses AR's safety/conveniences and must be hand-bound. Rule: reach for AR first; drop to raw SQL only when AR is awkward or too slow, and always parameterize."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the When would you use Active Record vs raw SQL in Rails bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: Active Record (Rails ORM) maps tables to Ruby classes: User.find(1), User.where(active: true) generate SQL for you — readable, safe (parameterized), and schema-aware. Use it for normal CRUD and simple queries. Use raw SQL (find_by_sql, exec_query, connection.execute) when you need something AR can't express efficiently: complex joins/aggregations, window functions, a query that AR turns into N+1, or bulk operations. Even then, bind parameters (find_by_sql(['... WHERE id = ?', id]); connection.query_to_select) to avoid injection. Trade-off: AR is faster to write and easier to maintain but slower for complex queries; raw SQL is fast and expressive but loses AR's safety/conveniences and must be hand-bound. Rule: reach for AR first; drop to raw SQL only when AR is awkward or too slow, and always parameterize. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "Active Record (Rails ORM) maps tables to Ruby classes: User.find(1), User.where(active: true) generate SQL for you — readable, safe (parameterized), and schema-aware.\n\nUse it for normal CRUD and simple queries.\n\nUse raw SQL (find_by_sql, exec_query, connection.execute) when you need something AR can't express efficiently: complex joins/aggregations, window functions, a query that AR turns into N+1, or bulk operations. Even then, bind parameters (find_by_sql(['... WHERE id = ?', id]); connection.query_to_select) to avoid injection. Trade-off: AR is faster to write and easier to maintain but slower for complex queries; raw SQL is fast and expressive but loses AR's safety/conveniences and must be hand-bound. Rule: reach for AR first; drop to raw SQL only when AR is awkward or too slow, and always parameterize.\n\nA common mistake is using raw SQL with string interpolation (injection) — always bind; or avoiding raw SQL even when N+1 hits."
          },
          {
            "type": "code_example",
            "title": "Active Record vs raw SQL",
            "content": "```ruby\n# Active Record (default): maps tables to Ruby classes, generates safe SQL\nUser.find(1)                          # SELECT * FROM users WHERE id = 1\nUser.where(active: true)              # ... WHERE active = TRUE (parameterized)\nUser.joins(:posts).where(posts: {published: true})\nUser.order(created_at: :desc).limit(10)\nUser.count                            # SELECT COUNT(*)\n# Readable, safe (parameterized), schema-aware. Use for normal CRUD.\n\n# Raw SQL: when AR is awkward or too slow.\n# 1) Complex joins / aggregates AR can't express well:\nUser.find_by_sql(['SELECT users.*, COUNT(p.id) AS pc\n                    FROM users LEFT JOIN posts p ON p.user_id = users.id\n                    GROUP BY users.id HAVING COUNT(p.id) > ?', 10])\n\n# 2) Window functions / DB-specific features:\nUser.connection.execute(\"SELECT id, RANK() OVER (ORDER BY score DESC) FROM users\")\n\n# 3) Bulk updates (faster than updating records one-by-one):\nUser.update_all('last_seen = NOW()')        # AR bulk (still AR)\nUser.connection.update(\"UPDATE users SET status = 1 WHERE id IN (1,2,3)\")\n\n# 4) Reporting queries (read a few columns, many rows):\nresult = User.connection.exec_query(\"SELECT DATE(created_at) d, COUNT(*) c FROM signups GROUP BY d\")\nresult.to_a  # [{\"d\"=>..., \"c\"=>...}, ...]\n\n# ALWAYS BIND PARAMETERS with raw SQL (avoid injection):\n#   BAD:  \"... WHERE name = '#{name}'\"          # injection\n#   GOOD: ['... WHERE name = ?', name]            # bound\n# Trade-off: AR = faster to write, safe, easy; raw = fast, expressive, must hand-bind.\n# Rule: AR first; raw SQL only when AR is awkward/slow; always parameterize.\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Active Record vs raw SQL in Rails?",
        "When would you use Active Record Vs Raw Sql in a real project?",
        "What is a common mistake when using Active Record Vs Raw Sql?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Active Record Vs Raw Sql — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Active Record Vs Raw Sql? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

{
  "topic": "N+1 Query Intro",
  "topicSlug": "n-plus-one-intro",
  "questions": [
    {
      "id": "n-plus-one-intro-q001",
      "slug": "ruby-n-plus-one-intro",
      "question": "What is the N+1 query problem and how do you fix it in Rails?",
      "title": "N+1 Query Intro",
      "direct_answer": "N+1: you fetch N records, then trigger 1 query PER record for an association, instead of 1 query for all. `User.all.each { |u| u.posts.count }` runs 1 query for users, then 1 PER user for posts count = N+1 queries. With 100 users that's 101 queries instead of 2. Fix: eager-load with `includes` — `User.all.includes(:posts)` loads posts in 1-2 queries (a JOIN or a second IN query) for ALL users. Detect N+1: the bullet gem warns in dev/test, or watch the log for many identical queries in a loop. Variants: `preload` (2 queries, no join — good when you don't filter on the association), `eager_load` (a JOIN — when you WHERE on the association), `includes` (Rails picks). For counts alone, counter caches (`posts_count`) avoid the query. The cost of N+1: many small queries are far slower than one batched query, especially over the network. Rule: if you loop and touch an association, eager-load it.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know N+1 = 1 query per record for an association, fixed with includes/preload/eager_load, detectable with bullet.",
        "common_mistake": "Looping over records and touching an association without includes (1 query per record).",
        "to_stand_out": "N+1 = 1 query per record for an association (N+1 total). Fix with includes/preload/eager_load. Detect with bullet. Use counter caches for counts."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define N+1 Query Intro simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "N+1 = fetching N records then 1 query per record for an association (N+1 total).",
              "Fix: eager-load with includes/preload/eager_load (loads all in 1-2 queries).",
              "preload = 2 queries (no join); eager_load = JOIN; includes = Rails picks.",
              "Detect N+1 with the bullet gem or by watching the log for many identical queries.",
              "For counts, use a counter cache (posts_count); rule: loop + association => eager-load."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "N+1: you fetch N records, then trigger 1 query PER record for an association, instead of 1 query for all. User all each do u u posts count end runs 1 query for users, then 1 PER user for posts count = N+1 queries. With 100 users that's 101 queries instead of 2. Fix: eager-load with includes — User all includes posts loads posts in 1 to 2 queries (a JOIN or a second IN query) for ALL users. Detect N+1: the bullet gem warns in dev/test, or watch the log for many identical queries in a loop. Variants: preload (2 queries, no join — good when you don't filter on the association), eager load (a JOIN — when you WHERE on the association), includes (Rails picks). For counts alone, counter caches (posts count) avoid the query. The cost of N+1: many small queries are far slower than one batched query, especially over the network. Rule: if you loop and touch an association, eager-load it. A common mistake is looping over records and touching an association without includes, an N+1 problem, when you should eager-load."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "N+1: you fetch N records, then trigger 1 query PER record for an association, instead of 1 query for all.\n\n`User.all.each { |u| u.posts.count }` runs 1 query for users, then 1 PER user for posts count = N+1 queries.\n\nWith 100 users that's 101 queries instead of 2. Fix: eager-load with `includes` — `User.all.includes(:posts)` loads posts in 1-2 queries (a JOIN or a second IN query) for ALL users. Detect N+1: the bullet gem warns in dev/test, or watch the log for many identical queries in a loop. Variants: `preload` (2 queries, no join — good when you don't filter on the association), `eager_load` (a JOIN — when you WHERE on the association), `includes` (Rails picks). For counts alone, counter caches (`posts_count`) avoid the query.\n\nLooping over records and touching an association without includes (1 query per record)."
          },
          {
            "type": "flow_diagram",
            "title": "N+1",
            "content": "1 query per record for an association (N+1 total). Fix with includes/preload/eager_load. Detect with bullet.\n\n```mermaid\nflowchart LR\n  U[User.all] --> E[each: u.posts.count]\n  E --> Q1[q1: users] --> QN[q2..qN: posts per user]\n  QN --> X[N+1 queries]\n  I[User.all.includes(:posts)] --> B[1-2 queries]\n  B --> Y[fast]\n```"
          },
          {
            "type": "code_example",
            "title": "N+1 query",
            "content": "```ruby\n# N+1: fetch N records, then 1 query PER record for an association\nusers = User.all                      # 1 query (users)\nusers.each { |u| puts u.posts.count }  # 1 query PER user (posts)  -> N+1\n# 100 users -> 101 queries (slow).\n\n# FIX: eager-load with `includes`\nusers = User.all.includes(:posts)      # 1-2 queries total (JOIN or IN)\nusers.each { |u| puts u.posts.size }   # posts already loaded; no extra queries\n\n# Variants of eager loading:\n#   preload      -> 2 queries (users, then posts WHERE user_id IN (...))\n#                  good when you DON'T filter on the association\n#   eager_load   -> a JOIN (1 query) -- when you WHERE on the association\n#   includes     -> Rails picks (preload usually; eager_load if you WHERE on it)\nUser.preload(:posts)                   # 2 queries\nUser.eager_load(:posts).where(posts: {published: true})  # JOIN\nUser.includes(:posts).where(posts: {published: true})   # Rails -> JOIN\n\n# Detect N+1:\n#   - 'bullet' gem warns in dev/test (logs N+1)\n#   - watch the log: many identical queries in a loop = N+1\n\n# For counts alone, use a COUNTER CACHE (no query at all):\n#   belongs_to :user, counter_cache: true\n#   user.posts_count  # stored on user; no COUNT(*) query\n\n# Cost: N small queries >> 1 batched query (network round-trips).\n# Rule: if you loop and touch an association, EAGER-LOAD it.\nUser.all.includes(:posts, :comments).each { ... }   # multiple associations\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use N Plus One Intro in a real project?",
        "How would you compare N Plus One Intro with an alternative?",
        "What is a common mistake when using N Plus One Intro?"
      ],
      "seo": {
        "metaTitle": "N+1 Query Intro — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is the N+1 query problem and how do you fix it in Rails? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "sql-database-basics-ruby-n-plus-one-intro-q001",
      "slug": "sql-database-basics-ruby-n-plus-one-intro-when-to-use",
      "question": "When would you use N Plus One Intro in a real project?",
      "title": "When would you use N Plus One Intro in a real project",
      "direct_answer": "I would use the N+1 query problem and how do you fix it in Rails when the problem matches its main benefit. N+1: you fetch N records, then trigger 1 query PER record for an association, instead of 1 query for all. In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain the N+1 query problem and how do you fix it in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about the N+1 query problem and how do you fix it in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining the N+1 query problem and how do you fix it in Rails in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. N+1: you fetch N records, then trigger 1 query PER record for an association, instead of 1 query for all. `User.all.each { |u| u.posts.count }` runs 1 query for users, then 1 PER user for posts count = N+1 queries. With 100 users that's 101 queries instead of 2. Fix: eager-load with `includes` — `User.all.includes(:posts)` loads posts in 1-2 queries (a JOIN or a second IN query) for ALL users. Detect N+1: the bullet gem warns in dev/test, or watch the log for many identical queries in a loop. Variants: `preload` (2 queries, no join — good when you don't filter on the association), `eager_load` (a JOIN — when you WHERE on the association), `includes` (Rails picks). For counts alone, counter caches (`posts_count`) avoid the query. The cost of N+1: many small queries are far slower than one batched query, especially over the network. Rule: if you loop and touch an association, eager-load it."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use the N+1 query problem and how do you fix it in Rails when its rule makes the code safer, clearer, or more efficient for the actual problem. N+1: you fetch N records, then trigger 1 query PER record for an association, instead of 1 query for all. `User.all.each { |u| u.posts.count }` runs 1 query for users, then 1 PER user for posts count = N+1 queries. With 100 users that's 101 queries instead of 2. Fix: eager-load with `includes` — `User.all.includes(:posts)` loads posts in 1-2 queries (a JOIN or a second IN query) for ALL users. Detect N+1: the bullet gem warns in dev/test, or watch the log for many identical queries in a loop. Variants: `preload` (2 queries, no join — good when you don't filter on the association), `eager_load` (a JOIN — when you WHERE on the association), `includes` (Rails picks). For counts alone, counter caches (`posts_count`) avoid the query. The cost of N+1: many small queries are far slower than one batched query, especially over the network. Rule: if you loop and touch an association, eager-load it. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "N+1: you fetch N records, then trigger 1 query PER record for an association, instead of 1 query for all.\n\n`User.all.each { |u| u.posts.count }` runs 1 query for users, then 1 PER user for posts count = N+1 queries.\n\nWith 100 users that's 101 queries instead of 2. Fix: eager-load with `includes` — `User.all.includes(:posts)` loads posts in 1-2 queries (a JOIN or a second IN query) for ALL users. Detect N+1: the bullet gem warns in dev/test, or watch the log for many identical queries in a loop. Variants: `preload` (2 queries, no join — good when you don't filter on the association), `eager_load` (a JOIN — when you WHERE on the association), `includes` (Rails picks). For counts alone, counter caches (`posts_count`) avoid the query.\n\nLooping over records and touching an association without includes (1 query per record)."
          },
          {
            "type": "code_example",
            "title": "N+1 query",
            "content": "```ruby\n# N+1: fetch N records, then 1 query PER record for an association\nusers = User.all                      # 1 query (users)\nusers.each { |u| puts u.posts.count }  # 1 query PER user (posts)  -> N+1\n# 100 users -> 101 queries (slow).\n\n# FIX: eager-load with `includes`\nusers = User.all.includes(:posts)      # 1-2 queries total (JOIN or IN)\nusers.each { |u| puts u.posts.size }   # posts already loaded; no extra queries\n\n# Variants of eager loading:\n#   preload      -> 2 queries (users, then posts WHERE user_id IN (...))\n#                  good when you DON'T filter on the association\n#   eager_load   -> a JOIN (1 query) -- when you WHERE on the association\n#   includes     -> Rails picks (preload usually; eager_load if you WHERE on it)\nUser.preload(:posts)                   # 2 queries\nUser.eager_load(:posts).where(posts: {published: true})  # JOIN\nUser.includes(:posts).where(posts: {published: true})   # Rails -> JOIN\n\n# Detect N+1:\n#   - 'bullet' gem warns in dev/test (logs N+1)\n#   - watch the log: many identical queries in a loop = N+1\n\n# For counts alone, use a COUNTER CACHE (no query at all):\n#   belongs_to :user, counter_cache: true\n#   user.posts_count  # stored on user; no COUNT(*) query\n\n# Cost: N small queries >> 1 batched query (network round-trips).\n# Rule: if you loop and touch an association, EAGER-LOAD it.\nUser.all.includes(:posts, :comments).each { ... }   # multiple associations\n```"
          }
        ]
      },
      "followup_questions": [
        "How would you compare N Plus One Intro with an alternative?",
        "What is a common mistake when using N Plus One Intro?",
        "What is the N+1 query problem and how do you fix it in Rails?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use N Plus One Intro in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use N Plus One Intro in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "sql-database-basics-ruby-n-plus-one-intro-q002",
      "slug": "sql-database-basics-ruby-n-plus-one-intro-common-mistake",
      "question": "What is a common mistake when using N Plus One Intro?",
      "title": "a common mistake when using N Plus One Intro",
      "direct_answer": "A common mistake with the N+1 query problem and how do you fix it in Rails is using the rule without checking its boundary conditions. N+1: you fetch N records, then trigger 1 query PER record for an association, instead of 1 query for all. That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain the N+1 query problem and how do you fix it in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about the N+1 query problem and how do you fix it in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of the N+1 query problem and how do you fix it in Rails. N+1: you fetch N records, then trigger 1 query PER record for an association, instead of 1 query for all. `User.all.each { |u| u.posts.count }` runs 1 query for users, then 1 PER user for posts count = N+1 queries. With 100 users that's 101 queries instead of 2. Fix: eager-load with `includes` — `User.all.includes(:posts)` loads posts in 1-2 queries (a JOIN or a second IN query) for ALL users. Detect N+1: the bullet gem warns in dev/test, or watch the log for many identical queries in a loop. Variants: `preload` (2 queries, no join — good when you don't filter on the association), `eager_load` (a JOIN — when you WHERE on the association), `includes` (Rails picks). For counts alone, counter caches (`posts_count`) avoid the query. The cost of N+1: many small queries are far slower than one batched query, especially over the network. Rule: if you loop and touch an association, eager-load it."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with the N+1 query problem and how do you fix it in Rails is assuming the happy path is the whole rule. N+1: you fetch N records, then trigger 1 query PER record for an association, instead of 1 query for all. `User.all.each { |u| u.posts.count }` runs 1 query for users, then 1 PER user for posts count = N+1 queries. With 100 users that's 101 queries instead of 2. Fix: eager-load with `includes` — `User.all.includes(:posts)` loads posts in 1-2 queries (a JOIN or a second IN query) for ALL users. Detect N+1: the bullet gem warns in dev/test, or watch the log for many identical queries in a loop. Variants: `preload` (2 queries, no join — good when you don't filter on the association), `eager_load` (a JOIN — when you WHERE on the association), `includes` (Rails picks). For counts alone, counter caches (`posts_count`) avoid the query. The cost of N+1: many small queries are far slower than one batched query, especially over the network. Rule: if you loop and touch an association, eager-load it. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "N+1: you fetch N records, then trigger 1 query PER record for an association, instead of 1 query for all.\n\n`User.all.each { |u| u.posts.count }` runs 1 query for users, then 1 PER user for posts count = N+1 queries.\n\nWith 100 users that's 101 queries instead of 2. Fix: eager-load with `includes` — `User.all.includes(:posts)` loads posts in 1-2 queries (a JOIN or a second IN query) for ALL users. Detect N+1: the bullet gem warns in dev/test, or watch the log for many identical queries in a loop. Variants: `preload` (2 queries, no join — good when you don't filter on the association), `eager_load` (a JOIN — when you WHERE on the association), `includes` (Rails picks). For counts alone, counter caches (`posts_count`) avoid the query.\n\nLooping over records and touching an association without includes (1 query per record)."
          },
          {
            "type": "code_example",
            "title": "N+1 query",
            "content": "```ruby\n# N+1: fetch N records, then 1 query PER record for an association\nusers = User.all                      # 1 query (users)\nusers.each { |u| puts u.posts.count }  # 1 query PER user (posts)  -> N+1\n# 100 users -> 101 queries (slow).\n\n# FIX: eager-load with `includes`\nusers = User.all.includes(:posts)      # 1-2 queries total (JOIN or IN)\nusers.each { |u| puts u.posts.size }   # posts already loaded; no extra queries\n\n# Variants of eager loading:\n#   preload      -> 2 queries (users, then posts WHERE user_id IN (...))\n#                  good when you DON'T filter on the association\n#   eager_load   -> a JOIN (1 query) -- when you WHERE on the association\n#   includes     -> Rails picks (preload usually; eager_load if you WHERE on it)\nUser.preload(:posts)                   # 2 queries\nUser.eager_load(:posts).where(posts: {published: true})  # JOIN\nUser.includes(:posts).where(posts: {published: true})   # Rails -> JOIN\n\n# Detect N+1:\n#   - 'bullet' gem warns in dev/test (logs N+1)\n#   - watch the log: many identical queries in a loop = N+1\n\n# For counts alone, use a COUNTER CACHE (no query at all):\n#   belongs_to :user, counter_cache: true\n#   user.posts_count  # stored on user; no COUNT(*) query\n\n# Cost: N small queries >> 1 batched query (network round-trips).\n# Rule: if you loop and touch an association, EAGER-LOAD it.\nUser.all.includes(:posts, :comments).each { ... }   # multiple associations\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use N Plus One Intro in a real project?",
        "How would you compare N Plus One Intro with an alternative?",
        "What is the N+1 query problem and how do you fix it in Rails?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using N Plus One Intro — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using N Plus One Intro? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "sql-database-basics-ruby-n-plus-one-intro-q003",
      "slug": "sql-database-basics-ruby-n-plus-one-intro-compare",
      "question": "How would you compare N Plus One Intro with an alternative?",
      "title": "compare N Plus One Intro with an alternative",
      "direct_answer": "I compare the N+1 query problem and how do you fix it in Rails with the simpler alternative by looking at correctness first, then time, memory, and readability. N+1: you fetch N records, then trigger 1 query PER record for an association, instead of 1 query for all. The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain the N+1 query problem and how do you fix it in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about the N+1 query problem and how do you fix it in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. N+1: you fetch N records, then trigger 1 query PER record for an association, instead of 1 query for all. `User.all.each { |u| u.posts.count }` runs 1 query for users, then 1 PER user for posts count = N+1 queries. With 100 users that's 101 queries instead of 2. Fix: eager-load with `includes` — `User.all.includes(:posts)` loads posts in 1-2 queries (a JOIN or a second IN query) for ALL users. Detect N+1: the bullet gem warns in dev/test, or watch the log for many identical queries in a loop. Variants: `preload` (2 queries, no join — good when you don't filter on the association), `eager_load` (a JOIN — when you WHERE on the association), `includes` (Rails picks). For counts alone, counter caches (`posts_count`) avoid the query. The cost of N+1: many small queries are far slower than one batched query, especially over the network. Rule: if you loop and touch an association, eager-load it."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare the N+1 query problem and how do you fix it in Rails with the straightforward alternative on the same example. N+1: you fetch N records, then trigger 1 query PER record for an association, instead of 1 query for all. `User.all.each { |u| u.posts.count }` runs 1 query for users, then 1 PER user for posts count = N+1 queries. With 100 users that's 101 queries instead of 2. Fix: eager-load with `includes` — `User.all.includes(:posts)` loads posts in 1-2 queries (a JOIN or a second IN query) for ALL users. Detect N+1: the bullet gem warns in dev/test, or watch the log for many identical queries in a loop. Variants: `preload` (2 queries, no join — good when you don't filter on the association), `eager_load` (a JOIN — when you WHERE on the association), `includes` (Rails picks). For counts alone, counter caches (`posts_count`) avoid the query. The cost of N+1: many small queries are far slower than one batched query, especially over the network. Rule: if you loop and touch an association, eager-load it. If the N+1 query problem and how do you fix it in Rails removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "N+1: you fetch N records, then trigger 1 query PER record for an association, instead of 1 query for all.\n\n`User.all.each { |u| u.posts.count }` runs 1 query for users, then 1 PER user for posts count = N+1 queries.\n\nWith 100 users that's 101 queries instead of 2. Fix: eager-load with `includes` — `User.all.includes(:posts)` loads posts in 1-2 queries (a JOIN or a second IN query) for ALL users. Detect N+1: the bullet gem warns in dev/test, or watch the log for many identical queries in a loop. Variants: `preload` (2 queries, no join — good when you don't filter on the association), `eager_load` (a JOIN — when you WHERE on the association), `includes` (Rails picks). For counts alone, counter caches (`posts_count`) avoid the query.\n\nLooping over records and touching an association without includes (1 query per record)."
          },
          {
            "type": "code_example",
            "title": "N+1 query",
            "content": "```ruby\n# N+1: fetch N records, then 1 query PER record for an association\nusers = User.all                      # 1 query (users)\nusers.each { |u| puts u.posts.count }  # 1 query PER user (posts)  -> N+1\n# 100 users -> 101 queries (slow).\n\n# FIX: eager-load with `includes`\nusers = User.all.includes(:posts)      # 1-2 queries total (JOIN or IN)\nusers.each { |u| puts u.posts.size }   # posts already loaded; no extra queries\n\n# Variants of eager loading:\n#   preload      -> 2 queries (users, then posts WHERE user_id IN (...))\n#                  good when you DON'T filter on the association\n#   eager_load   -> a JOIN (1 query) -- when you WHERE on the association\n#   includes     -> Rails picks (preload usually; eager_load if you WHERE on it)\nUser.preload(:posts)                   # 2 queries\nUser.eager_load(:posts).where(posts: {published: true})  # JOIN\nUser.includes(:posts).where(posts: {published: true})   # Rails -> JOIN\n\n# Detect N+1:\n#   - 'bullet' gem warns in dev/test (logs N+1)\n#   - watch the log: many identical queries in a loop = N+1\n\n# For counts alone, use a COUNTER CACHE (no query at all):\n#   belongs_to :user, counter_cache: true\n#   user.posts_count  # stored on user; no COUNT(*) query\n\n# Cost: N small queries >> 1 batched query (network round-trips).\n# Rule: if you loop and touch an association, EAGER-LOAD it.\nUser.all.includes(:posts, :comments).each { ... }   # multiple associations\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use N Plus One Intro in a real project?",
        "What is a common mistake when using N Plus One Intro?",
        "What is the N+1 query problem and how do you fix it in Rails?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare N Plus One Intro with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare N Plus One Intro with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "sql-database-basics-ruby-n-plus-one-intro-q004",
      "slug": "sql-database-basics-ruby-n-plus-one-intro-scenario",
      "question": "How would you debug a problem involving N Plus One Intro?",
      "title": "debug a problem involving N Plus One Intro",
      "direct_answer": "I would debug a problem involving the N+1 query problem and how do you fix it in Rails by reproducing it with the smallest input first. N+1: you fetch N records, then trigger 1 query PER record for an association, instead of 1 query for all. Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain the N+1 query problem and how do you fix it in Rails in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about the N+1 query problem and how do you fix it in Rails without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what the N+1 query problem and how do you fix it in Rails is supposed to guarantee. N+1: you fetch N records, then trigger 1 query PER record for an association, instead of 1 query for all. `User.all.each { |u| u.posts.count }` runs 1 query for users, then 1 PER user for posts count = N+1 queries. With 100 users that's 101 queries instead of 2. Fix: eager-load with `includes` — `User.all.includes(:posts)` loads posts in 1-2 queries (a JOIN or a second IN query) for ALL users. Detect N+1: the bullet gem warns in dev/test, or watch the log for many identical queries in a loop. Variants: `preload` (2 queries, no join — good when you don't filter on the association), `eager_load` (a JOIN — when you WHERE on the association), `includes` (Rails picks). For counts alone, counter caches (`posts_count`) avoid the query. The cost of N+1: many small queries are far slower than one batched query, especially over the network. Rule: if you loop and touch an association, eager-load it."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the the N+1 query problem and how do you fix it in Rails bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: N+1: you fetch N records, then trigger 1 query PER record for an association, instead of 1 query for all. `User.all.each { |u| u.posts.count }` runs 1 query for users, then 1 PER user for posts count = N+1 queries. With 100 users that's 101 queries instead of 2. Fix: eager-load with `includes` — `User.all.includes(:posts)` loads posts in 1-2 queries (a JOIN or a second IN query) for ALL users. Detect N+1: the bullet gem warns in dev/test, or watch the log for many identical queries in a loop. Variants: `preload` (2 queries, no join — good when you don't filter on the association), `eager_load` (a JOIN — when you WHERE on the association), `includes` (Rails picks). For counts alone, counter caches (`posts_count`) avoid the query. The cost of N+1: many small queries are far slower than one batched query, especially over the network. Rule: if you loop and touch an association, eager-load it. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "N+1: you fetch N records, then trigger 1 query PER record for an association, instead of 1 query for all.\n\n`User.all.each { |u| u.posts.count }` runs 1 query for users, then 1 PER user for posts count = N+1 queries.\n\nWith 100 users that's 101 queries instead of 2. Fix: eager-load with `includes` — `User.all.includes(:posts)` loads posts in 1-2 queries (a JOIN or a second IN query) for ALL users. Detect N+1: the bullet gem warns in dev/test, or watch the log for many identical queries in a loop. Variants: `preload` (2 queries, no join — good when you don't filter on the association), `eager_load` (a JOIN — when you WHERE on the association), `includes` (Rails picks). For counts alone, counter caches (`posts_count`) avoid the query.\n\nLooping over records and touching an association without includes (1 query per record)."
          },
          {
            "type": "code_example",
            "title": "N+1 query",
            "content": "```ruby\n# N+1: fetch N records, then 1 query PER record for an association\nusers = User.all                      # 1 query (users)\nusers.each { |u| puts u.posts.count }  # 1 query PER user (posts)  -> N+1\n# 100 users -> 101 queries (slow).\n\n# FIX: eager-load with `includes`\nusers = User.all.includes(:posts)      # 1-2 queries total (JOIN or IN)\nusers.each { |u| puts u.posts.size }   # posts already loaded; no extra queries\n\n# Variants of eager loading:\n#   preload      -> 2 queries (users, then posts WHERE user_id IN (...))\n#                  good when you DON'T filter on the association\n#   eager_load   -> a JOIN (1 query) -- when you WHERE on the association\n#   includes     -> Rails picks (preload usually; eager_load if you WHERE on it)\nUser.preload(:posts)                   # 2 queries\nUser.eager_load(:posts).where(posts: {published: true})  # JOIN\nUser.includes(:posts).where(posts: {published: true})   # Rails -> JOIN\n\n# Detect N+1:\n#   - 'bullet' gem warns in dev/test (logs N+1)\n#   - watch the log: many identical queries in a loop = N+1\n\n# For counts alone, use a COUNTER CACHE (no query at all):\n#   belongs_to :user, counter_cache: true\n#   user.posts_count  # stored on user; no COUNT(*) query\n\n# Cost: N small queries >> 1 batched query (network round-trips).\n# Rule: if you loop and touch an association, EAGER-LOAD it.\nUser.all.includes(:posts, :comments).each { ... }   # multiple associations\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use N Plus One Intro in a real project?",
        "How would you compare N Plus One Intro with an alternative?",
        "What is a common mistake when using N Plus One Intro?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving N Plus One Intro — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving N Plus One Intro? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

{
  "topic": "Migrations and Schema",
  "topicSlug": "migrations-and-schema",
  "questions": [
    {
      "id": "migrations-and-schema-q001",
      "slug": "ruby-migrations-and-schema",
      "question": "What are Rails migrations and why use them?",
      "title": "Migrations and Schema",
      "direct_answer": "Migrations are versioned, Ruby files that change the DB schema over time. `rails g migration CreateUsers name:string age:integer` makes a file with `create_table :users do |t| t.string :name; t.integer :age; t.timestamps end`. Run `rails db:migrate` to apply; the schema is recorded in db/schema.rb (the current state). Migrations let you evolve the schema safely (not by hand-editing the DB): they're ordered (timestamps), reversible (db:rollback), and shareable (committed to git so every dev has the same schema). Why not just run SQL? Because migrations are tracked (schema_migrations table), so they run once and in order on every environment; you can roll back; and they work across adapters (Postgres, MySQL, SQLite). Create a table, add/remove columns, add indexes (t.index / add_index for query speed), add references (t.references :user = foreign key). Never edit a migration that's been pushed — make a new one (otherwise other devs' DBs diverge).",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know migrations are versioned schema-change files tracked in schema_migrations, reversible, shareable, and you never edit pushed ones.",
        "common_mistake": "Editing a migration that's been pushed (other devs' DBs diverge) — make a new migration to change.",
        "to_stand_out": "Migrations: versioned Ruby files changing the DB schema. rails db:migrate applies; db/schema.rb records state; reversible; never edit pushed ones."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define Migrations and Schema simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "Migrations are versioned Ruby files that change the DB schema over time.",
              "rails db:migrate applies them (tracked in schema_migrations; run once, in order).",
              "db/schema.rb records the current state (source of truth for the schema).",
              "They're reversible (db:rollback) and work across DB adapters (PG/MySQL/SQLite).",
              "Never edit a migration that's been pushed — make a new one so DBs don't diverge."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "Migrations are versioned, Ruby files that change the DB schema over time. rails g migration CreateUsers name string age integer makes a file with create table users do t; t string name; t integer age; t timestamps end. Run rails db migrate to apply; the schema is recorded in db schema rb (the current state). Migrations let you evolve the schema safely (not by hand-editing the DB): they're ordered (timestamps), reversible (db rollback), and shareable (committed to git so every dev has the same schema). Why not just run SQL? Because migrations are tracked (schema migrations table), so they run once and in order on every environment; you can roll back; and they work across adapters (Postgres, MySQL, SQLite). Create a table, add or remove columns, add indexes (t index or add index for query speed), add references (t references user = foreign key). Never edit a migration that's been pushed — make a new one (otherwise other devs' DBs diverge). A common mistake is editing a migration that's been pushed, when other devs' DBs diverge, and you should make a new migration."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Migrations are versioned, Ruby files that change the DB schema over time.\n\n`rails g migration CreateUsers name:string age:integer` makes a file with `create_table :users do |t| t.string :name; t.integer :age; t.timestamps end`.\n\nRun `rails db:migrate` to apply; the schema is recorded in db/schema.rb (the current state). Migrations let you evolve the schema safely (not by hand-editing the DB): they're ordered (timestamps), reversible (db:rollback), and shareable (committed to git so every dev has the same schema). Why not just run SQL? Because migrations are tracked (schema_migrations table), so they run once and in order on every environment; you can roll back; and they work across adapters (Postgres, MySQL, SQLite).\n\nEditing a migration that's been pushed (other devs' DBs diverge) — make a new migration to change."
          },
          {
            "type": "flow_diagram",
            "title": "Migrations",
            "content": "versioned Ruby files changing the schema. db:migrate applies; schema.rb records state; reversible; never edit pushed.\n\n```mermaid\nflowchart LR\n  G[rails g migration] --> F[file with timestamp]\n  F --> M[db:migrate applies once]\n  M --> S[schema_migrations tracks it]\n  M --> SC[db/schema.rb = current state]\n  R[db:rollback] --> RE[reverses]\n```"
          },
          {
            "type": "code_example",
            "title": "Migrations and schema",
            "content": "```ruby\n# Generate a migration (creates a versioned file in db/migrate/)\n# rails g migration CreateUsers name:string age:integer\nclass CreateUsers < ActiveRecord::Migration[7.0]\n  def change                       # reversible (self.up / self.down auto-derived)\n    create_table :users do |t|\n      t.string :name\n      t.integer :age\n      t.references :account          # foreign key (account_id)\n      t.timestamps                   # created_at, updated_at\n    end\n    add_index :users, :email, unique: true   # speed up lookups\n  end\nend\n\n# Apply it (runs once; recorded in schema_migrations table):\n#   rails db:migrate\n# Roll back the last one:\n#   rails db:rollback\n# Current schema is recorded in db/schema.rb (the source of truth for state).\n\n# Common migration methods:\n#   add_column    :users, :role, :string\n#   remove_column :users, :age\n#   rename_column :users, :name, :full_name\n#   add_index     :users, :email, unique: true\n#   add_reference :posts, :user, foreign_key: true\n#   change_column :users, :age, :integer, default: 0\n\n# They're reversible (db:rollback) and work across adapters (PG/MySQL/SQLite).\n# Shareable: commit migrations to git; every dev runs db:migrate to catch up.\n\n# NEVER edit a migration that's been pushed to git/merged.\n#   (other devs already ran it -> their schema_migrations won't match.)\n#   Make a NEW migration to change it.\n\n# Schema loading vs migrating:\n#   db:migrate   -> run migrations in order (use in prod)\n#   db:schema:load -> load db/schema.rb directly (fresh DB)\n# db/schema.rb is the current state; don't hand-edit it either.\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Migrations And Schema in a real project?",
        "How would you compare Migrations And Schema with an alternative?",
        "What is a common mistake when using Migrations And Schema?"
      ],
      "seo": {
        "metaTitle": "Migrations and Schema — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What are Rails migrations and why use them? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "sql-database-basics-ruby-migrations-and-schema-q001",
      "slug": "sql-database-basics-ruby-migrations-and-schema-when-to-use",
      "question": "When would you use Migrations And Schema in a real project?",
      "title": "When would you use Migrations And Schema in a real project",
      "direct_answer": "I would use Rails migrations and why use them when the problem matches its main benefit. Migrations are versioned, Ruby files that change the DB schema over time. In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain Rails migrations and why use them in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about Rails migrations and why use them without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining Rails migrations and why use them in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. Migrations are versioned, Ruby files that change the DB schema over time. `rails g migration CreateUsers name:string age:integer` makes a file with `create_table :users do |t| t.string :name; t.integer :age; t.timestamps end`. Run `rails db:migrate` to apply; the schema is recorded in db/schema.rb (the current state). Migrations let you evolve the schema safely (not by hand-editing the DB): they're ordered (timestamps), reversible (db:rollback), and shareable (committed to git so every dev has the same schema). Why not just run SQL? Because migrations are tracked (schema_migrations table), so they run once and in order on every environment; you can roll back; and they work across adapters (Postgres, MySQL, SQLite). Create a table, add/remove columns, add indexes (t.index / add_index for query speed), add references (t.references :user = foreign key). Never edit a migration that's been pushed — make a new one (otherwise other devs' DBs diverge)."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use Rails migrations and why use them when its rule makes the code safer, clearer, or more efficient for the actual problem. Migrations are versioned, Ruby files that change the DB schema over time. `rails g migration CreateUsers name:string age:integer` makes a file with `create_table :users do |t| t.string :name; t.integer :age; t.timestamps end`. Run `rails db:migrate` to apply; the schema is recorded in db/schema.rb (the current state). Migrations let you evolve the schema safely (not by hand-editing the DB): they're ordered (timestamps), reversible (db:rollback), and shareable (committed to git so every dev has the same schema). Why not just run SQL? Because migrations are tracked (schema_migrations table), so they run once and in order on every environment; you can roll back; and they work across adapters (Postgres, MySQL, SQLite). Create a table, add/remove columns, add indexes (t.index / add_index for query speed), add references (t.references :user = foreign key). Never edit a migration that's been pushed — make a new one (otherwise other devs' DBs diverge). I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Migrations are versioned, Ruby files that change the DB schema over time.\n\n`rails g migration CreateUsers name:string age:integer` makes a file with `create_table :users do |t| t.string :name; t.integer :age; t.timestamps end`.\n\nRun `rails db:migrate` to apply; the schema is recorded in db/schema.rb (the current state). Migrations let you evolve the schema safely (not by hand-editing the DB): they're ordered (timestamps), reversible (db:rollback), and shareable (committed to git so every dev has the same schema). Why not just run SQL? Because migrations are tracked (schema_migrations table), so they run once and in order on every environment; you can roll back; and they work across adapters (Postgres, MySQL, SQLite).\n\nEditing a migration that's been pushed (other devs' DBs diverge) — make a new migration to change."
          },
          {
            "type": "code_example",
            "title": "Migrations and schema",
            "content": "```ruby\n# Generate a migration (creates a versioned file in db/migrate/)\n# rails g migration CreateUsers name:string age:integer\nclass CreateUsers < ActiveRecord::Migration[7.0]\n  def change                       # reversible (self.up / self.down auto-derived)\n    create_table :users do |t|\n      t.string :name\n      t.integer :age\n      t.references :account          # foreign key (account_id)\n      t.timestamps                   # created_at, updated_at\n    end\n    add_index :users, :email, unique: true   # speed up lookups\n  end\nend\n\n# Apply it (runs once; recorded in schema_migrations table):\n#   rails db:migrate\n# Roll back the last one:\n#   rails db:rollback\n# Current schema is recorded in db/schema.rb (the source of truth for state).\n\n# Common migration methods:\n#   add_column    :users, :role, :string\n#   remove_column :users, :age\n#   rename_column :users, :name, :full_name\n#   add_index     :users, :email, unique: true\n#   add_reference :posts, :user, foreign_key: true\n#   change_column :users, :age, :integer, default: 0\n\n# They're reversible (db:rollback) and work across adapters (PG/MySQL/SQLite).\n# Shareable: commit migrations to git; every dev runs db:migrate to catch up.\n\n# NEVER edit a migration that's been pushed to git/merged.\n#   (other devs already ran it -> their schema_migrations won't match.)\n#   Make a NEW migration to change it.\n\n# Schema loading vs migrating:\n#   db:migrate   -> run migrations in order (use in prod)\n#   db:schema:load -> load db/schema.rb directly (fresh DB)\n# db/schema.rb is the current state; don't hand-edit it either.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are Rails migrations and why use them?",
        "How would you compare Migrations And Schema with an alternative?",
        "What is a common mistake when using Migrations And Schema?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Migrations And Schema in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Migrations And Schema in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "sql-database-basics-ruby-migrations-and-schema-q002",
      "slug": "sql-database-basics-ruby-migrations-and-schema-common-mistake",
      "question": "What is a common mistake when using Migrations And Schema?",
      "title": "a common mistake when using Migrations And Schema",
      "direct_answer": "A common mistake with Rails migrations and why use them is using the rule without checking its boundary conditions. Migrations are versioned, Ruby files that change the DB schema over time. That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain Rails migrations and why use them in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about Rails migrations and why use them without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of Rails migrations and why use them. Migrations are versioned, Ruby files that change the DB schema over time. `rails g migration CreateUsers name:string age:integer` makes a file with `create_table :users do |t| t.string :name; t.integer :age; t.timestamps end`. Run `rails db:migrate` to apply; the schema is recorded in db/schema.rb (the current state). Migrations let you evolve the schema safely (not by hand-editing the DB): they're ordered (timestamps), reversible (db:rollback), and shareable (committed to git so every dev has the same schema). Why not just run SQL? Because migrations are tracked (schema_migrations table), so they run once and in order on every environment; you can roll back; and they work across adapters (Postgres, MySQL, SQLite). Create a table, add/remove columns, add indexes (t.index / add_index for query speed), add references (t.references :user = foreign key). Never edit a migration that's been pushed — make a new one (otherwise other devs' DBs diverge)."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with Rails migrations and why use them is assuming the happy path is the whole rule. Migrations are versioned, Ruby files that change the DB schema over time. `rails g migration CreateUsers name:string age:integer` makes a file with `create_table :users do |t| t.string :name; t.integer :age; t.timestamps end`. Run `rails db:migrate` to apply; the schema is recorded in db/schema.rb (the current state). Migrations let you evolve the schema safely (not by hand-editing the DB): they're ordered (timestamps), reversible (db:rollback), and shareable (committed to git so every dev has the same schema). Why not just run SQL? Because migrations are tracked (schema_migrations table), so they run once and in order on every environment; you can roll back; and they work across adapters (Postgres, MySQL, SQLite). Create a table, add/remove columns, add indexes (t.index / add_index for query speed), add references (t.references :user = foreign key). Never edit a migration that's been pushed — make a new one (otherwise other devs' DBs diverge). For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "Migrations are versioned, Ruby files that change the DB schema over time.\n\n`rails g migration CreateUsers name:string age:integer` makes a file with `create_table :users do |t| t.string :name; t.integer :age; t.timestamps end`.\n\nRun `rails db:migrate` to apply; the schema is recorded in db/schema.rb (the current state). Migrations let you evolve the schema safely (not by hand-editing the DB): they're ordered (timestamps), reversible (db:rollback), and shareable (committed to git so every dev has the same schema). Why not just run SQL? Because migrations are tracked (schema_migrations table), so they run once and in order on every environment; you can roll back; and they work across adapters (Postgres, MySQL, SQLite).\n\nEditing a migration that's been pushed (other devs' DBs diverge) — make a new migration to change."
          },
          {
            "type": "code_example",
            "title": "Migrations and schema",
            "content": "```ruby\n# Generate a migration (creates a versioned file in db/migrate/)\n# rails g migration CreateUsers name:string age:integer\nclass CreateUsers < ActiveRecord::Migration[7.0]\n  def change                       # reversible (self.up / self.down auto-derived)\n    create_table :users do |t|\n      t.string :name\n      t.integer :age\n      t.references :account          # foreign key (account_id)\n      t.timestamps                   # created_at, updated_at\n    end\n    add_index :users, :email, unique: true   # speed up lookups\n  end\nend\n\n# Apply it (runs once; recorded in schema_migrations table):\n#   rails db:migrate\n# Roll back the last one:\n#   rails db:rollback\n# Current schema is recorded in db/schema.rb (the source of truth for state).\n\n# Common migration methods:\n#   add_column    :users, :role, :string\n#   remove_column :users, :age\n#   rename_column :users, :name, :full_name\n#   add_index     :users, :email, unique: true\n#   add_reference :posts, :user, foreign_key: true\n#   change_column :users, :age, :integer, default: 0\n\n# They're reversible (db:rollback) and work across adapters (PG/MySQL/SQLite).\n# Shareable: commit migrations to git; every dev runs db:migrate to catch up.\n\n# NEVER edit a migration that's been pushed to git/merged.\n#   (other devs already ran it -> their schema_migrations won't match.)\n#   Make a NEW migration to change it.\n\n# Schema loading vs migrating:\n#   db:migrate   -> run migrations in order (use in prod)\n#   db:schema:load -> load db/schema.rb directly (fresh DB)\n# db/schema.rb is the current state; don't hand-edit it either.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are Rails migrations and why use them?",
        "When would you use Migrations And Schema in a real project?",
        "How would you compare Migrations And Schema with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Migrations And Schema — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Migrations And Schema? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "sql-database-basics-ruby-migrations-and-schema-q003",
      "slug": "sql-database-basics-ruby-migrations-and-schema-compare",
      "question": "How would you compare Migrations And Schema with an alternative?",
      "title": "compare Migrations And Schema with an alternative",
      "direct_answer": "I compare Rails migrations and why use them with the simpler alternative by looking at correctness first, then time, memory, and readability. Migrations are versioned, Ruby files that change the DB schema over time. The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain Rails migrations and why use them in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about Rails migrations and why use them without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. Migrations are versioned, Ruby files that change the DB schema over time. `rails g migration CreateUsers name:string age:integer` makes a file with `create_table :users do |t| t.string :name; t.integer :age; t.timestamps end`. Run `rails db:migrate` to apply; the schema is recorded in db/schema.rb (the current state). Migrations let you evolve the schema safely (not by hand-editing the DB): they're ordered (timestamps), reversible (db:rollback), and shareable (committed to git so every dev has the same schema). Why not just run SQL? Because migrations are tracked (schema_migrations table), so they run once and in order on every environment; you can roll back; and they work across adapters (Postgres, MySQL, SQLite). Create a table, add/remove columns, add indexes (t.index / add_index for query speed), add references (t.references :user = foreign key). Never edit a migration that's been pushed — make a new one (otherwise other devs' DBs diverge)."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare Rails migrations and why use them with the straightforward alternative on the same example. Migrations are versioned, Ruby files that change the DB schema over time. `rails g migration CreateUsers name:string age:integer` makes a file with `create_table :users do |t| t.string :name; t.integer :age; t.timestamps end`. Run `rails db:migrate` to apply; the schema is recorded in db/schema.rb (the current state). Migrations let you evolve the schema safely (not by hand-editing the DB): they're ordered (timestamps), reversible (db:rollback), and shareable (committed to git so every dev has the same schema). Why not just run SQL? Because migrations are tracked (schema_migrations table), so they run once and in order on every environment; you can roll back; and they work across adapters (Postgres, MySQL, SQLite). Create a table, add/remove columns, add indexes (t.index / add_index for query speed), add references (t.references :user = foreign key). Never edit a migration that's been pushed — make a new one (otherwise other devs' DBs diverge). If Rails migrations and why use them removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "Migrations are versioned, Ruby files that change the DB schema over time.\n\n`rails g migration CreateUsers name:string age:integer` makes a file with `create_table :users do |t| t.string :name; t.integer :age; t.timestamps end`.\n\nRun `rails db:migrate` to apply; the schema is recorded in db/schema.rb (the current state). Migrations let you evolve the schema safely (not by hand-editing the DB): they're ordered (timestamps), reversible (db:rollback), and shareable (committed to git so every dev has the same schema). Why not just run SQL? Because migrations are tracked (schema_migrations table), so they run once and in order on every environment; you can roll back; and they work across adapters (Postgres, MySQL, SQLite).\n\nEditing a migration that's been pushed (other devs' DBs diverge) — make a new migration to change."
          },
          {
            "type": "code_example",
            "title": "Migrations and schema",
            "content": "```ruby\n# Generate a migration (creates a versioned file in db/migrate/)\n# rails g migration CreateUsers name:string age:integer\nclass CreateUsers < ActiveRecord::Migration[7.0]\n  def change                       # reversible (self.up / self.down auto-derived)\n    create_table :users do |t|\n      t.string :name\n      t.integer :age\n      t.references :account          # foreign key (account_id)\n      t.timestamps                   # created_at, updated_at\n    end\n    add_index :users, :email, unique: true   # speed up lookups\n  end\nend\n\n# Apply it (runs once; recorded in schema_migrations table):\n#   rails db:migrate\n# Roll back the last one:\n#   rails db:rollback\n# Current schema is recorded in db/schema.rb (the source of truth for state).\n\n# Common migration methods:\n#   add_column    :users, :role, :string\n#   remove_column :users, :age\n#   rename_column :users, :name, :full_name\n#   add_index     :users, :email, unique: true\n#   add_reference :posts, :user, foreign_key: true\n#   change_column :users, :age, :integer, default: 0\n\n# They're reversible (db:rollback) and work across adapters (PG/MySQL/SQLite).\n# Shareable: commit migrations to git; every dev runs db:migrate to catch up.\n\n# NEVER edit a migration that's been pushed to git/merged.\n#   (other devs already ran it -> their schema_migrations won't match.)\n#   Make a NEW migration to change it.\n\n# Schema loading vs migrating:\n#   db:migrate   -> run migrations in order (use in prod)\n#   db:schema:load -> load db/schema.rb directly (fresh DB)\n# db/schema.rb is the current state; don't hand-edit it either.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are Rails migrations and why use them?",
        "When would you use Migrations And Schema in a real project?",
        "What is a common mistake when using Migrations And Schema?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Migrations And Schema with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Migrations And Schema with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "sql-database-basics-ruby-migrations-and-schema-q004",
      "slug": "sql-database-basics-ruby-migrations-and-schema-scenario",
      "question": "How would you debug a problem involving Migrations And Schema?",
      "title": "debug a problem involving Migrations And Schema",
      "direct_answer": "I would debug a problem involving Rails migrations and why use them by reproducing it with the smallest input first. Migrations are versioned, Ruby files that change the DB schema over time. Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain Rails migrations and why use them in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about Rails migrations and why use them without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what Rails migrations and why use them is supposed to guarantee. Migrations are versioned, Ruby files that change the DB schema over time. `rails g migration CreateUsers name:string age:integer` makes a file with `create_table :users do |t| t.string :name; t.integer :age; t.timestamps end`. Run `rails db:migrate` to apply; the schema is recorded in db/schema.rb (the current state). Migrations let you evolve the schema safely (not by hand-editing the DB): they're ordered (timestamps), reversible (db:rollback), and shareable (committed to git so every dev has the same schema). Why not just run SQL? Because migrations are tracked (schema_migrations table), so they run once and in order on every environment; you can roll back; and they work across adapters (Postgres, MySQL, SQLite). Create a table, add/remove columns, add indexes (t.index / add_index for query speed), add references (t.references :user = foreign key). Never edit a migration that's been pushed — make a new one (otherwise other devs' DBs diverge)."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the Rails migrations and why use them bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: Migrations are versioned, Ruby files that change the DB schema over time. `rails g migration CreateUsers name:string age:integer` makes a file with `create_table :users do |t| t.string :name; t.integer :age; t.timestamps end`. Run `rails db:migrate` to apply; the schema is recorded in db/schema.rb (the current state). Migrations let you evolve the schema safely (not by hand-editing the DB): they're ordered (timestamps), reversible (db:rollback), and shareable (committed to git so every dev has the same schema). Why not just run SQL? Because migrations are tracked (schema_migrations table), so they run once and in order on every environment; you can roll back; and they work across adapters (Postgres, MySQL, SQLite). Create a table, add/remove columns, add indexes (t.index / add_index for query speed), add references (t.references :user = foreign key). Never edit a migration that's been pushed — make a new one (otherwise other devs' DBs diverge). Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "Migrations are versioned, Ruby files that change the DB schema over time.\n\n`rails g migration CreateUsers name:string age:integer` makes a file with `create_table :users do |t| t.string :name; t.integer :age; t.timestamps end`.\n\nRun `rails db:migrate` to apply; the schema is recorded in db/schema.rb (the current state). Migrations let you evolve the schema safely (not by hand-editing the DB): they're ordered (timestamps), reversible (db:rollback), and shareable (committed to git so every dev has the same schema). Why not just run SQL? Because migrations are tracked (schema_migrations table), so they run once and in order on every environment; you can roll back; and they work across adapters (Postgres, MySQL, SQLite).\n\nEditing a migration that's been pushed (other devs' DBs diverge) — make a new migration to change."
          },
          {
            "type": "code_example",
            "title": "Migrations and schema",
            "content": "```ruby\n# Generate a migration (creates a versioned file in db/migrate/)\n# rails g migration CreateUsers name:string age:integer\nclass CreateUsers < ActiveRecord::Migration[7.0]\n  def change                       # reversible (self.up / self.down auto-derived)\n    create_table :users do |t|\n      t.string :name\n      t.integer :age\n      t.references :account          # foreign key (account_id)\n      t.timestamps                   # created_at, updated_at\n    end\n    add_index :users, :email, unique: true   # speed up lookups\n  end\nend\n\n# Apply it (runs once; recorded in schema_migrations table):\n#   rails db:migrate\n# Roll back the last one:\n#   rails db:rollback\n# Current schema is recorded in db/schema.rb (the source of truth for state).\n\n# Common migration methods:\n#   add_column    :users, :role, :string\n#   remove_column :users, :age\n#   rename_column :users, :name, :full_name\n#   add_index     :users, :email, unique: true\n#   add_reference :posts, :user, foreign_key: true\n#   change_column :users, :age, :integer, default: 0\n\n# They're reversible (db:rollback) and work across adapters (PG/MySQL/SQLite).\n# Shareable: commit migrations to git; every dev runs db:migrate to catch up.\n\n# NEVER edit a migration that's been pushed to git/merged.\n#   (other devs already ran it -> their schema_migrations won't match.)\n#   Make a NEW migration to change it.\n\n# Schema loading vs migrating:\n#   db:migrate   -> run migrations in order (use in prod)\n#   db:schema:load -> load db/schema.rb directly (fresh DB)\n# db/schema.rb is the current state; don't hand-edit it either.\n```"
          }
        ]
      },
      "followup_questions": [
        "What are Rails migrations and why use them?",
        "When would you use Migrations And Schema in a real project?",
        "How would you compare Migrations And Schema with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Migrations And Schema — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Migrations And Schema? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

{
  "topic": "Time Complexity Basics",
  "topicSlug": "time-complexity-basics",
  "questions": [
    {
      "id": "time-complexity-basics-q001",
      "slug": "ruby-time-complexity-basics",
      "question": "What is time complexity and how do you reason about it in Ruby?",
      "title": "Time Complexity Basics",
      "direct_answer": "Time complexity describes how runtime grows with input size n. The big ones: O(1) constant (hash lookup, array index), O(log n) logarithmic (binary search), O(n) linear (each over an array), O(n log n) linearithmic (sort, sort_by), O(n^2) quadratic (nested each). You reason about Ruby code by counting loops: a single each is O(n); nested eaches are O(n^2); a hash lookup inside a loop keeps it O(n) (O(1) per lookup); sort is O(n log n). The goal is to pick the lowest complexity that fits — a hash turns an O(n^2) pair-finding loop into O(n).",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you can name O(1)/O(log n)/O(n)/O(n log n)/O(n^2), count loops in Ruby, and pick the lower-complexity structure (hash for O(1)).",
        "common_mistake": "Counting only outer loops (miss nested each as O(n^2)), or calling sort O(n) (it's O(n log n)).",
        "to_stand_out": "Time complexity = how runtime grows with n: O(1) hash lookup, O(log n) binary search, O(n) each, O(n log n) sort, O(n^2) nested each. Pick the lower-complexity structure."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define Time Complexity Basics simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "Time complexity = how runtime grows with input size n.",
              "O(1) hash lookup/index; O(log n) binary search; O(n) each; O(n log n) sort; O(n^2) nested each.",
              "Count loops: one each is O(n); nested eaches are O(n^2).",
              "A hash lookup is O(1) — it turns an O(n^2) pair loop into O(n).",
              "sort/sort_by is O(n log n), not O(n) — the dominant cost."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "Time complexity describes how runtime grows with input size n. The big ones: O(1) constant — hash lookup, array index; O(log n) logarithmic — binary search; O(n) linear — each over an array; O(n log n) linearithmic — sort, sort_by; O(n^2) quadratic — nested each. You reason about Ruby code by counting loops: a single each is O(n); nested eaches are O(n^2); a hash lookup inside a loop keeps it O(n) because each lookup is O(1); sort is O(n log n). The goal is to pick the lowest complexity that fits — a hash turns an O(n^2) pair-finding nested loop into O(n) by giving O(1) lookup, which is the single most common optimization. A common mistake is counting only the outer loop and missing a nested each, which is O(n^2), and calling sort O(n), when it's O(n log n), the dominant cost."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Time complexity describes how runtime grows with input size n.\n\nThe big ones: O(1) constant (hash lookup, array index), O(log n) logarithmic (binary search), O(n) linear (each over an array), O(n log n) linearithmic (sort, sort_by), O(n^2) quadratic (nested each).\n\nYou reason about Ruby code by counting loops: a single each is O(n); nested eaches are O(n^2); a hash lookup inside a loop keeps it O(n) (O(1) per lookup); sort is O(n log n). The goal is to pick the lowest complexity that fits — a hash turns an O(n^2) pair-finding loop into O(n).\n\nCounting only outer loops (miss nested each as O(n^2)), or calling sort O(n) (it's O(n log n))."
          },
          {
            "type": "flow_diagram",
            "title": "Complexity Ladder",
            "content": "O(1) < O(log n) < O(n) < O(n log n) < O(n^2).\n\n```mermaid\nflowchart LR\n  C1[O(1) hash lookup] --> L[O(log n) binary search]\n  L --> N[O(n) each]\n  N --> NL[O(n log n) sort]\n  NL --> Q[O(n^2) nested each]\n```"
          },
          {
            "type": "code_example",
            "title": "Reasoning about Ruby code",
            "content": "```ruby\n# O(1) - hash lookup / array index\nh = { a: 1 }; h[:a]          # constant time\narr = [1,2,3]; arr[0]        # constant time\n\n# O(log n) - binary search (array must be sorted)\n# arr.bsearch { |x| x >= target }\n\n# O(n) - one each over the array\narr.each { |x| puts x }      # linear in arr.length\narr.sum                      # O(n)\n\n# O(n log n) - sort / sort_by\narr.sort                    # n log n\narr.sort_by { |x| x.len }   # n log n\n\n# O(n^2) - nested each (avoid!)\narr.each { |a| arr.each { |b| puts a + b } }   # n * n\n\n# Hash turns O(n^2) pair-find into O(n):\n# nested loop: for each x, scan for target-x  -> O(n^2)\n# with seen hash: one pass, O(1) lookup each   -> O(n)\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Time Complexity Basics in a real project?",
        "How would you compare Time Complexity Basics with an alternative?",
        "What is a common mistake when using Time Complexity Basics?"
      ],
      "seo": {
        "metaTitle": "Time Complexity Basics — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is time complexity and how do you reason about it in Ruby? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "dsa-ruby-basics-time-complexity-basics-q001",
      "slug": "dsa-ruby-basics-time-complexity-basics-when-to-use",
      "question": "When would you use Time Complexity Basics in a real project?",
      "title": "When would you use Time Complexity Basics in a real project",
      "direct_answer": "I would use time complexity and how do you reason about it in Ruby when the problem matches its main benefit. Time complexity describes how runtime grows with input size n. In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain time complexity and how do you reason about it in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about time complexity and how do you reason about it in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining time complexity and how do you reason about it in Ruby in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. Time complexity describes how runtime grows with input size n. The big ones: O(1) constant (hash lookup, array index), O(log n) logarithmic (binary search), O(n) linear (each over an array), O(n log n) linearithmic (sort, sort_by), O(n^2) quadratic (nested each). You reason about Ruby code by counting loops: a single each is O(n); nested eaches are O(n^2); a hash lookup inside a loop keeps it O(n) (O(1) per lookup); sort is O(n log n). The goal is to pick the lowest complexity that fits — a hash turns an O(n^2) pair-finding loop into O(n)."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use time complexity and how do you reason about it in Ruby when its rule makes the code safer, clearer, or more efficient for the actual problem. Time complexity describes how runtime grows with input size n. The big ones: O(1) constant (hash lookup, array index), O(log n) logarithmic (binary search), O(n) linear (each over an array), O(n log n) linearithmic (sort, sort_by), O(n^2) quadratic (nested each). You reason about Ruby code by counting loops: a single each is O(n); nested eaches are O(n^2); a hash lookup inside a loop keeps it O(n) (O(1) per lookup); sort is O(n log n). The goal is to pick the lowest complexity that fits — a hash turns an O(n^2) pair-finding loop into O(n). I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Time complexity describes how runtime grows with input size n.\n\nThe big ones: O(1) constant (hash lookup, array index), O(log n) logarithmic (binary search), O(n) linear (each over an array), O(n log n) linearithmic (sort, sort_by), O(n^2) quadratic (nested each).\n\nYou reason about Ruby code by counting loops: a single each is O(n); nested eaches are O(n^2); a hash lookup inside a loop keeps it O(n) (O(1) per lookup); sort is O(n log n). The goal is to pick the lowest complexity that fits — a hash turns an O(n^2) pair-finding loop into O(n).\n\nCounting only outer loops (miss nested each as O(n^2)), or calling sort O(n) (it's O(n log n))."
          },
          {
            "type": "code_example",
            "title": "Reasoning about Ruby code",
            "content": "```ruby\n# O(1) - hash lookup / array index\nh = { a: 1 }; h[:a]          # constant time\narr = [1,2,3]; arr[0]        # constant time\n\n# O(log n) - binary search (array must be sorted)\n# arr.bsearch { |x| x >= target }\n\n# O(n) - one each over the array\narr.each { |x| puts x }      # linear in arr.length\narr.sum                      # O(n)\n\n# O(n log n) - sort / sort_by\narr.sort                    # n log n\narr.sort_by { |x| x.len }   # n log n\n\n# O(n^2) - nested each (avoid!)\narr.each { |a| arr.each { |b| puts a + b } }   # n * n\n\n# Hash turns O(n^2) pair-find into O(n):\n# nested loop: for each x, scan for target-x  -> O(n^2)\n# with seen hash: one pass, O(1) lookup each   -> O(n)\n```"
          }
        ]
      },
      "followup_questions": [
        "What is time complexity and how do you reason about it in Ruby?",
        "How would you compare Time Complexity Basics with an alternative?",
        "What is a common mistake when using Time Complexity Basics?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Time Complexity Basics in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Time Complexity Basics in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "dsa-ruby-basics-time-complexity-basics-q002",
      "slug": "dsa-ruby-basics-time-complexity-basics-common-mistake",
      "question": "What is a common mistake when using Time Complexity Basics?",
      "title": "a common mistake when using Time Complexity Basics",
      "direct_answer": "A common mistake with time complexity and how do you reason about it in Ruby is using the rule without checking its boundary conditions. Time complexity describes how runtime grows with input size n. That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain time complexity and how do you reason about it in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about time complexity and how do you reason about it in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of time complexity and how do you reason about it in Ruby. Time complexity describes how runtime grows with input size n. The big ones: O(1) constant (hash lookup, array index), O(log n) logarithmic (binary search), O(n) linear (each over an array), O(n log n) linearithmic (sort, sort_by), O(n^2) quadratic (nested each). You reason about Ruby code by counting loops: a single each is O(n); nested eaches are O(n^2); a hash lookup inside a loop keeps it O(n) (O(1) per lookup); sort is O(n log n). The goal is to pick the lowest complexity that fits — a hash turns an O(n^2) pair-finding loop into O(n)."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with time complexity and how do you reason about it in Ruby is assuming the happy path is the whole rule. Time complexity describes how runtime grows with input size n. The big ones: O(1) constant (hash lookup, array index), O(log n) logarithmic (binary search), O(n) linear (each over an array), O(n log n) linearithmic (sort, sort_by), O(n^2) quadratic (nested each). You reason about Ruby code by counting loops: a single each is O(n); nested eaches are O(n^2); a hash lookup inside a loop keeps it O(n) (O(1) per lookup); sort is O(n log n). The goal is to pick the lowest complexity that fits — a hash turns an O(n^2) pair-finding loop into O(n). For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "Time complexity describes how runtime grows with input size n.\n\nThe big ones: O(1) constant (hash lookup, array index), O(log n) logarithmic (binary search), O(n) linear (each over an array), O(n log n) linearithmic (sort, sort_by), O(n^2) quadratic (nested each).\n\nYou reason about Ruby code by counting loops: a single each is O(n); nested eaches are O(n^2); a hash lookup inside a loop keeps it O(n) (O(1) per lookup); sort is O(n log n). The goal is to pick the lowest complexity that fits — a hash turns an O(n^2) pair-finding loop into O(n).\n\nCounting only outer loops (miss nested each as O(n^2)), or calling sort O(n) (it's O(n log n))."
          },
          {
            "type": "code_example",
            "title": "Reasoning about Ruby code",
            "content": "```ruby\n# O(1) - hash lookup / array index\nh = { a: 1 }; h[:a]          # constant time\narr = [1,2,3]; arr[0]        # constant time\n\n# O(log n) - binary search (array must be sorted)\n# arr.bsearch { |x| x >= target }\n\n# O(n) - one each over the array\narr.each { |x| puts x }      # linear in arr.length\narr.sum                      # O(n)\n\n# O(n log n) - sort / sort_by\narr.sort                    # n log n\narr.sort_by { |x| x.len }   # n log n\n\n# O(n^2) - nested each (avoid!)\narr.each { |a| arr.each { |b| puts a + b } }   # n * n\n\n# Hash turns O(n^2) pair-find into O(n):\n# nested loop: for each x, scan for target-x  -> O(n^2)\n# with seen hash: one pass, O(1) lookup each   -> O(n)\n```"
          }
        ]
      },
      "followup_questions": [
        "What is time complexity and how do you reason about it in Ruby?",
        "When would you use Time Complexity Basics in a real project?",
        "How would you compare Time Complexity Basics with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Time Complexity Basics — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Time Complexity Basics? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "dsa-ruby-basics-time-complexity-basics-q003",
      "slug": "dsa-ruby-basics-time-complexity-basics-compare",
      "question": "How would you compare Time Complexity Basics with an alternative?",
      "title": "compare Time Complexity Basics with an alternative",
      "direct_answer": "I compare time complexity and how do you reason about it in Ruby with the simpler alternative by looking at correctness first, then time, memory, and readability. Time complexity describes how runtime grows with input size n. The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain time complexity and how do you reason about it in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about time complexity and how do you reason about it in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. Time complexity describes how runtime grows with input size n. The big ones: O(1) constant (hash lookup, array index), O(log n) logarithmic (binary search), O(n) linear (each over an array), O(n log n) linearithmic (sort, sort_by), O(n^2) quadratic (nested each). You reason about Ruby code by counting loops: a single each is O(n); nested eaches are O(n^2); a hash lookup inside a loop keeps it O(n) (O(1) per lookup); sort is O(n log n). The goal is to pick the lowest complexity that fits — a hash turns an O(n^2) pair-finding loop into O(n)."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare time complexity and how do you reason about it in Ruby with the straightforward alternative on the same example. Time complexity describes how runtime grows with input size n. The big ones: O(1) constant (hash lookup, array index), O(log n) logarithmic (binary search), O(n) linear (each over an array), O(n log n) linearithmic (sort, sort_by), O(n^2) quadratic (nested each). You reason about Ruby code by counting loops: a single each is O(n); nested eaches are O(n^2); a hash lookup inside a loop keeps it O(n) (O(1) per lookup); sort is O(n log n). The goal is to pick the lowest complexity that fits — a hash turns an O(n^2) pair-finding loop into O(n). If time complexity and how do you reason about it in Ruby removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "Time complexity describes how runtime grows with input size n.\n\nThe big ones: O(1) constant (hash lookup, array index), O(log n) logarithmic (binary search), O(n) linear (each over an array), O(n log n) linearithmic (sort, sort_by), O(n^2) quadratic (nested each).\n\nYou reason about Ruby code by counting loops: a single each is O(n); nested eaches are O(n^2); a hash lookup inside a loop keeps it O(n) (O(1) per lookup); sort is O(n log n). The goal is to pick the lowest complexity that fits — a hash turns an O(n^2) pair-finding loop into O(n).\n\nCounting only outer loops (miss nested each as O(n^2)), or calling sort O(n) (it's O(n log n))."
          },
          {
            "type": "code_example",
            "title": "Reasoning about Ruby code",
            "content": "```ruby\n# O(1) - hash lookup / array index\nh = { a: 1 }; h[:a]          # constant time\narr = [1,2,3]; arr[0]        # constant time\n\n# O(log n) - binary search (array must be sorted)\n# arr.bsearch { |x| x >= target }\n\n# O(n) - one each over the array\narr.each { |x| puts x }      # linear in arr.length\narr.sum                      # O(n)\n\n# O(n log n) - sort / sort_by\narr.sort                    # n log n\narr.sort_by { |x| x.len }   # n log n\n\n# O(n^2) - nested each (avoid!)\narr.each { |a| arr.each { |b| puts a + b } }   # n * n\n\n# Hash turns O(n^2) pair-find into O(n):\n# nested loop: for each x, scan for target-x  -> O(n^2)\n# with seen hash: one pass, O(1) lookup each   -> O(n)\n```"
          }
        ]
      },
      "followup_questions": [
        "What is time complexity and how do you reason about it in Ruby?",
        "When would you use Time Complexity Basics in a real project?",
        "What is a common mistake when using Time Complexity Basics?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Time Complexity Basics with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Time Complexity Basics with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "dsa-ruby-basics-time-complexity-basics-q004",
      "slug": "dsa-ruby-basics-time-complexity-basics-scenario",
      "question": "How would you debug a problem involving Time Complexity Basics?",
      "title": "debug a problem involving Time Complexity Basics",
      "direct_answer": "I would debug a problem involving time complexity and how do you reason about it in Ruby by reproducing it with the smallest input first. Time complexity describes how runtime grows with input size n. Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain time complexity and how do you reason about it in Ruby in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about time complexity and how do you reason about it in Ruby without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what time complexity and how do you reason about it in Ruby is supposed to guarantee. Time complexity describes how runtime grows with input size n. The big ones: O(1) constant (hash lookup, array index), O(log n) logarithmic (binary search), O(n) linear (each over an array), O(n log n) linearithmic (sort, sort_by), O(n^2) quadratic (nested each). You reason about Ruby code by counting loops: a single each is O(n); nested eaches are O(n^2); a hash lookup inside a loop keeps it O(n) (O(1) per lookup); sort is O(n log n). The goal is to pick the lowest complexity that fits — a hash turns an O(n^2) pair-finding loop into O(n)."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the time complexity and how do you reason about it in Ruby bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: Time complexity describes how runtime grows with input size n. The big ones: O(1) constant (hash lookup, array index), O(log n) logarithmic (binary search), O(n) linear (each over an array), O(n log n) linearithmic (sort, sort_by), O(n^2) quadratic (nested each). You reason about Ruby code by counting loops: a single each is O(n); nested eaches are O(n^2); a hash lookup inside a loop keeps it O(n) (O(1) per lookup); sort is O(n log n). The goal is to pick the lowest complexity that fits — a hash turns an O(n^2) pair-finding loop into O(n). Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "Time complexity describes how runtime grows with input size n.\n\nThe big ones: O(1) constant (hash lookup, array index), O(log n) logarithmic (binary search), O(n) linear (each over an array), O(n log n) linearithmic (sort, sort_by), O(n^2) quadratic (nested each).\n\nYou reason about Ruby code by counting loops: a single each is O(n); nested eaches are O(n^2); a hash lookup inside a loop keeps it O(n) (O(1) per lookup); sort is O(n log n). The goal is to pick the lowest complexity that fits — a hash turns an O(n^2) pair-finding loop into O(n).\n\nCounting only outer loops (miss nested each as O(n^2)), or calling sort O(n) (it's O(n log n))."
          },
          {
            "type": "code_example",
            "title": "Reasoning about Ruby code",
            "content": "```ruby\n# O(1) - hash lookup / array index\nh = { a: 1 }; h[:a]          # constant time\narr = [1,2,3]; arr[0]        # constant time\n\n# O(log n) - binary search (array must be sorted)\n# arr.bsearch { |x| x >= target }\n\n# O(n) - one each over the array\narr.each { |x| puts x }      # linear in arr.length\narr.sum                      # O(n)\n\n# O(n log n) - sort / sort_by\narr.sort                    # n log n\narr.sort_by { |x| x.len }   # n log n\n\n# O(n^2) - nested each (avoid!)\narr.each { |a| arr.each { |b| puts a + b } }   # n * n\n\n# Hash turns O(n^2) pair-find into O(n):\n# nested loop: for each x, scan for target-x  -> O(n^2)\n# with seen hash: one pass, O(1) lookup each   -> O(n)\n```"
          }
        ]
      },
      "followup_questions": [
        "What is time complexity and how do you reason about it in Ruby?",
        "When would you use Time Complexity Basics in a real project?",
        "How would you compare Time Complexity Basics with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Time Complexity Basics — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Time Complexity Basics? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

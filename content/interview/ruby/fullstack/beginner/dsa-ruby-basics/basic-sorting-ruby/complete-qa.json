{
  "topic": "Basic Sorting",
  "topicSlug": "basic-sorting-ruby",
  "questions": [
    {
      "id": "basic-sorting-ruby-q001",
      "slug": "ruby-basic-sorting-basics",
      "question": "How do you sort in Ruby and what's the time complexity?",
      "title": "Basic Sorting in Ruby",
      "direct_answer": "Ruby's sort (and sort_by) uses a fast built-in (quicksort/merge, O(n log n) average). You call arr.sort (natural order) or arr.sort { |a, b| a <=> b } (custom), and sort_by { |x| x.len } for sorting by a key (stable and preferred for complex keys). For small or learning cases you may write bubble/insertion/selection sort by hand (all O(n^2)) to show you understand them, but in real code you use the built-in sort — it's O(n log n), tested, and faster than a hand-written one. Know the O(n^2) ones for interviews; use sort_by in code.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you know Ruby's sort is O(n log n) built-in, sort_by for a key, and can name the O(n^2) sorts (bubble/insertion/selection).",
        "common_mistake": "Writing a hand-rolled O(n^2) sort in real code (use the built-in sort_by), or confusing sort (a<=>b) with sort_by (key).",
        "to_stand_out": "Ruby's sort/sort_by is O(n log n) built-in; sort_by { |x| key } for a key. O(n^2) sorts (bubble/insertion/selection) for interviews; use sort_by in code."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define Basic Sorting in Ruby simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "Ruby's sort/sort_by is a fast built-in (quicksort/merge), O(n log n) average.",
              "Use sort_by { |x| key } for sorting by a key (stable; preferred for complex keys).",
              "sort uses a comparator (a <=> b); sort_by uses a key function (x.len).",
              "Bubble, insertion, selection are O(n^2) — know them for interviews, not real code.",
              "In real code, use the built-in sort_by — tested and faster than hand-rolled."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "Ruby's sort, and sort_by, uses a fast built-in — quicksort or mergesort, O(n log n) average. You call arr dot sort for the natural order or arr dot sort with a b a spaceship b for a custom comparator, and sort_by with x x dot len for sorting by a key, which is stable and preferred for complex keys. For small or learning cases you may write bubble, insertion, or selection sort by hand — all O(n^2) — to show you understand them, but in real code you use the built-in sort because it's O(n log n), tested, and faster than a hand-written one. Know the O(n^2) ones for interviews; use sort_by in code. A common mistake is writing a hand-rolled O(n^2) sort in real code when you should use the built-in sort_by, and confusing sort, which uses a comparator, with sort_by, which uses a key function."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Ruby's sort (and sort_by) uses a fast built-in (quicksort/merge, O(n log n) average).\n\nYou call arr.sort (natural order) or arr.sort { |a, b| a <=> b } (custom), and sort_by { |x| x.len } for sorting by a key (stable and preferred for complex keys).\n\nFor small or learning cases you may write bubble/insertion/selection sort by hand (all O(n^2)) to show you understand them, but in real code you use the built-in sort — it's O(n log n), tested, and faster than a hand-written one. Know the O(n^2) ones for interviews; use sort_by in code.\n\nWriting a hand-rolled O(n^2) sort in real code (use the built-in sort_by), or confusing sort (a<=>b) with sort_by (key)."
          },
          {
            "type": "flow_diagram",
            "title": "Sort Choices",
            "content": "Built-in O(n log n) for real code; know the O(n^2) ones for interviews.\n\n```mermaid\nflowchart LR\n  N[arr.sort / sort_by] -->|built-in| F[n log n]\n  B[bubble / insertion / selection] -->|hand-rolled| S[n squared]\n```"
          },
          {
            "type": "code_example",
            "title": "Sorting in Ruby",
            "content": "```ruby\n# Built-in sort (O(n log n))\n[3, 1, 2].sort                         # [1, 2, 3]\n[3, 1, 2].sort { |a, b| b <=> a }      # [3, 2, 1] (descending)\n\n# sort_by a key (preferred for complex keys, stable)\nwords = %w[apple fig kiwi]\nwords.sort_by(&:length)               # ['fig', 'kiwi', 'apple']\npeople.sort_by { |p| [p.age, p.name] }  # by age then name\n\n# Hand-rolled bubble sort (O(n^2)) — for learning/interviews\ndef bubble(arr)\n  n = arr.length\n  loop do\n    swapped = false\n    (1...n).each do |i|\n      if arr[i - 1] > arr[i]\n        arr[i - 1], arr[i] = arr[i], arr[i - 1]\n        swapped = true\n      end\n    end\n    break unless swapped\n  end\n  arr\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Basic Sorting Ruby in a real project?",
        "How would you compare Basic Sorting Ruby with an alternative?",
        "What is a common mistake when using Basic Sorting Ruby?"
      ],
      "seo": {
        "metaTitle": "Basic Sorting in Ruby — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How do you sort in Ruby and what's the time complexity? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "dsa-ruby-basics-basic-sorting-ruby-q001",
      "slug": "dsa-ruby-basics-basic-sorting-ruby-when-to-use",
      "question": "When would you use Basic Sorting Ruby in a real project?",
      "title": "When would you use Basic Sorting Ruby in a real project",
      "direct_answer": "I would use How do you sort in Ruby and what's the time complexity when the problem matches its main benefit. Ruby's sort (and sort_by) uses a fast built-in (quicksort/merge, O(n log n) average). In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How do you sort in Ruby and what's the time complexity in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do you sort in Ruby and what's the time complexity without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining How do you sort in Ruby and what's the time complexity in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. Ruby's sort (and sort_by) uses a fast built-in (quicksort/merge, O(n log n) average). You call arr.sort (natural order) or arr.sort { |a, b| a <=> b } (custom), and sort_by { |x| x.len } for sorting by a key (stable and preferred for complex keys). For small or learning cases you may write bubble/insertion/selection sort by hand (all O(n^2)) to show you understand them, but in real code you use the built-in sort — it's O(n log n), tested, and faster than a hand-written one. Know the O(n^2) ones for interviews; use sort_by in code."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use How do you sort in Ruby and what's the time complexity when its rule makes the code safer, clearer, or more efficient for the actual problem. Ruby's sort (and sort_by) uses a fast built-in (quicksort/merge, O(n log n) average). You call arr.sort (natural order) or arr.sort { |a, b| a <=> b } (custom), and sort_by { |x| x.len } for sorting by a key (stable and preferred for complex keys). For small or learning cases you may write bubble/insertion/selection sort by hand (all O(n^2)) to show you understand them, but in real code you use the built-in sort — it's O(n log n), tested, and faster than a hand-written one. Know the O(n^2) ones for interviews; use sort_by in code. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Ruby's sort (and sort_by) uses a fast built-in (quicksort/merge, O(n log n) average).\n\nYou call arr.sort (natural order) or arr.sort { |a, b| a <=> b } (custom), and sort_by { |x| x.len } for sorting by a key (stable and preferred for complex keys).\n\nFor small or learning cases you may write bubble/insertion/selection sort by hand (all O(n^2)) to show you understand them, but in real code you use the built-in sort — it's O(n log n), tested, and faster than a hand-written one. Know the O(n^2) ones for interviews; use sort_by in code.\n\nWriting a hand-rolled O(n^2) sort in real code (use the built-in sort_by), or confusing sort (a<=>b) with sort_by (key)."
          },
          {
            "type": "code_example",
            "title": "Sorting in Ruby",
            "content": "```ruby\n# Built-in sort (O(n log n))\n[3, 1, 2].sort                         # [1, 2, 3]\n[3, 1, 2].sort { |a, b| b <=> a }      # [3, 2, 1] (descending)\n\n# sort_by a key (preferred for complex keys, stable)\nwords = %w[apple fig kiwi]\nwords.sort_by(&:length)               # ['fig', 'kiwi', 'apple']\npeople.sort_by { |p| [p.age, p.name] }  # by age then name\n\n# Hand-rolled bubble sort (O(n^2)) — for learning/interviews\ndef bubble(arr)\n  n = arr.length\n  loop do\n    swapped = false\n    (1...n).each do |i|\n      if arr[i - 1] > arr[i]\n        arr[i - 1], arr[i] = arr[i], arr[i - 1]\n        swapped = true\n      end\n    end\n    break unless swapped\n  end\n  arr\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "How do you sort in Ruby and what's the time complexity?",
        "How would you compare Basic Sorting Ruby with an alternative?",
        "What is a common mistake when using Basic Sorting Ruby?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Basic Sorting Ruby in a real project — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Basic Sorting Ruby in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "dsa-ruby-basics-basic-sorting-ruby-q002",
      "slug": "dsa-ruby-basics-basic-sorting-ruby-common-mistake",
      "question": "What is a common mistake when using Basic Sorting Ruby?",
      "title": "a common mistake when using Basic Sorting Ruby",
      "direct_answer": "A common mistake with How do you sort in Ruby and what's the time complexity is using the rule without checking its boundary conditions. Ruby's sort (and sort_by) uses a fast built-in (quicksort/merge, O(n log n) average). That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How do you sort in Ruby and what's the time complexity in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do you sort in Ruby and what's the time complexity without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of How do you sort in Ruby and what's the time complexity. Ruby's sort (and sort_by) uses a fast built-in (quicksort/merge, O(n log n) average). You call arr.sort (natural order) or arr.sort { |a, b| a <=> b } (custom), and sort_by { |x| x.len } for sorting by a key (stable and preferred for complex keys). For small or learning cases you may write bubble/insertion/selection sort by hand (all O(n^2)) to show you understand them, but in real code you use the built-in sort — it's O(n log n), tested, and faster than a hand-written one. Know the O(n^2) ones for interviews; use sort_by in code."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with How do you sort in Ruby and what's the time complexity is assuming the happy path is the whole rule. Ruby's sort (and sort_by) uses a fast built-in (quicksort/merge, O(n log n) average). You call arr.sort (natural order) or arr.sort { |a, b| a <=> b } (custom), and sort_by { |x| x.len } for sorting by a key (stable and preferred for complex keys). For small or learning cases you may write bubble/insertion/selection sort by hand (all O(n^2)) to show you understand them, but in real code you use the built-in sort — it's O(n log n), tested, and faster than a hand-written one. Know the O(n^2) ones for interviews; use sort_by in code. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "Ruby's sort (and sort_by) uses a fast built-in (quicksort/merge, O(n log n) average).\n\nYou call arr.sort (natural order) or arr.sort { |a, b| a <=> b } (custom), and sort_by { |x| x.len } for sorting by a key (stable and preferred for complex keys).\n\nFor small or learning cases you may write bubble/insertion/selection sort by hand (all O(n^2)) to show you understand them, but in real code you use the built-in sort — it's O(n log n), tested, and faster than a hand-written one. Know the O(n^2) ones for interviews; use sort_by in code.\n\nWriting a hand-rolled O(n^2) sort in real code (use the built-in sort_by), or confusing sort (a<=>b) with sort_by (key)."
          },
          {
            "type": "code_example",
            "title": "Sorting in Ruby",
            "content": "```ruby\n# Built-in sort (O(n log n))\n[3, 1, 2].sort                         # [1, 2, 3]\n[3, 1, 2].sort { |a, b| b <=> a }      # [3, 2, 1] (descending)\n\n# sort_by a key (preferred for complex keys, stable)\nwords = %w[apple fig kiwi]\nwords.sort_by(&:length)               # ['fig', 'kiwi', 'apple']\npeople.sort_by { |p| [p.age, p.name] }  # by age then name\n\n# Hand-rolled bubble sort (O(n^2)) — for learning/interviews\ndef bubble(arr)\n  n = arr.length\n  loop do\n    swapped = false\n    (1...n).each do |i|\n      if arr[i - 1] > arr[i]\n        arr[i - 1], arr[i] = arr[i], arr[i - 1]\n        swapped = true\n      end\n    end\n    break unless swapped\n  end\n  arr\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "How do you sort in Ruby and what's the time complexity?",
        "When would you use Basic Sorting Ruby in a real project?",
        "How would you compare Basic Sorting Ruby with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Basic Sorting Ruby — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Basic Sorting Ruby? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "dsa-ruby-basics-basic-sorting-ruby-q003",
      "slug": "dsa-ruby-basics-basic-sorting-ruby-compare",
      "question": "How would you compare Basic Sorting Ruby with an alternative?",
      "title": "compare Basic Sorting Ruby with an alternative",
      "direct_answer": "I compare How do you sort in Ruby and what's the time complexity with the simpler alternative by looking at correctness first, then time, memory, and readability. Ruby's sort (and sort_by) uses a fast built-in (quicksort/merge, O(n log n) average). The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How do you sort in Ruby and what's the time complexity in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do you sort in Ruby and what's the time complexity without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. Ruby's sort (and sort_by) uses a fast built-in (quicksort/merge, O(n log n) average). You call arr.sort (natural order) or arr.sort { |a, b| a <=> b } (custom), and sort_by { |x| x.len } for sorting by a key (stable and preferred for complex keys). For small or learning cases you may write bubble/insertion/selection sort by hand (all O(n^2)) to show you understand them, but in real code you use the built-in sort — it's O(n log n), tested, and faster than a hand-written one. Know the O(n^2) ones for interviews; use sort_by in code."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare How do you sort in Ruby and what's the time complexity with the straightforward alternative on the same example. Ruby's sort (and sort_by) uses a fast built-in (quicksort/merge, O(n log n) average). You call arr.sort (natural order) or arr.sort { |a, b| a <=> b } (custom), and sort_by { |x| x.len } for sorting by a key (stable and preferred for complex keys). For small or learning cases you may write bubble/insertion/selection sort by hand (all O(n^2)) to show you understand them, but in real code you use the built-in sort — it's O(n log n), tested, and faster than a hand-written one. Know the O(n^2) ones for interviews; use sort_by in code. If How do you sort in Ruby and what's the time complexity removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "Ruby's sort (and sort_by) uses a fast built-in (quicksort/merge, O(n log n) average).\n\nYou call arr.sort (natural order) or arr.sort { |a, b| a <=> b } (custom), and sort_by { |x| x.len } for sorting by a key (stable and preferred for complex keys).\n\nFor small or learning cases you may write bubble/insertion/selection sort by hand (all O(n^2)) to show you understand them, but in real code you use the built-in sort — it's O(n log n), tested, and faster than a hand-written one. Know the O(n^2) ones for interviews; use sort_by in code.\n\nWriting a hand-rolled O(n^2) sort in real code (use the built-in sort_by), or confusing sort (a<=>b) with sort_by (key)."
          },
          {
            "type": "code_example",
            "title": "Sorting in Ruby",
            "content": "```ruby\n# Built-in sort (O(n log n))\n[3, 1, 2].sort                         # [1, 2, 3]\n[3, 1, 2].sort { |a, b| b <=> a }      # [3, 2, 1] (descending)\n\n# sort_by a key (preferred for complex keys, stable)\nwords = %w[apple fig kiwi]\nwords.sort_by(&:length)               # ['fig', 'kiwi', 'apple']\npeople.sort_by { |p| [p.age, p.name] }  # by age then name\n\n# Hand-rolled bubble sort (O(n^2)) — for learning/interviews\ndef bubble(arr)\n  n = arr.length\n  loop do\n    swapped = false\n    (1...n).each do |i|\n      if arr[i - 1] > arr[i]\n        arr[i - 1], arr[i] = arr[i], arr[i - 1]\n        swapped = true\n      end\n    end\n    break unless swapped\n  end\n  arr\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "How do you sort in Ruby and what's the time complexity?",
        "When would you use Basic Sorting Ruby in a real project?",
        "What is a common mistake when using Basic Sorting Ruby?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Basic Sorting Ruby with an alternative — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Basic Sorting Ruby with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "dsa-ruby-basics-basic-sorting-ruby-q004",
      "slug": "dsa-ruby-basics-basic-sorting-ruby-scenario",
      "question": "How would you debug a problem involving Basic Sorting Ruby?",
      "title": "debug a problem involving Basic Sorting Ruby",
      "direct_answer": "I would debug a problem involving How do you sort in Ruby and what's the time complexity by reproducing it with the smallest input first. Ruby's sort (and sort_by) uses a fast built-in (quicksort/merge, O(n log n) average). Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How do you sort in Ruby and what's the time complexity in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do you sort in Ruby and what's the time complexity without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what How do you sort in Ruby and what's the time complexity is supposed to guarantee. Ruby's sort (and sort_by) uses a fast built-in (quicksort/merge, O(n log n) average). You call arr.sort (natural order) or arr.sort { |a, b| a <=> b } (custom), and sort_by { |x| x.len } for sorting by a key (stable and preferred for complex keys). For small or learning cases you may write bubble/insertion/selection sort by hand (all O(n^2)) to show you understand them, but in real code you use the built-in sort — it's O(n log n), tested, and faster than a hand-written one. Know the O(n^2) ones for interviews; use sort_by in code."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the How do you sort in Ruby and what's the time complexity bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: Ruby's sort (and sort_by) uses a fast built-in (quicksort/merge, O(n log n) average). You call arr.sort (natural order) or arr.sort { |a, b| a <=> b } (custom), and sort_by { |x| x.len } for sorting by a key (stable and preferred for complex keys). For small or learning cases you may write bubble/insertion/selection sort by hand (all O(n^2)) to show you understand them, but in real code you use the built-in sort — it's O(n log n), tested, and faster than a hand-written one. Know the O(n^2) ones for interviews; use sort_by in code. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "Ruby's sort (and sort_by) uses a fast built-in (quicksort/merge, O(n log n) average).\n\nYou call arr.sort (natural order) or arr.sort { |a, b| a <=> b } (custom), and sort_by { |x| x.len } for sorting by a key (stable and preferred for complex keys).\n\nFor small or learning cases you may write bubble/insertion/selection sort by hand (all O(n^2)) to show you understand them, but in real code you use the built-in sort — it's O(n log n), tested, and faster than a hand-written one. Know the O(n^2) ones for interviews; use sort_by in code.\n\nWriting a hand-rolled O(n^2) sort in real code (use the built-in sort_by), or confusing sort (a<=>b) with sort_by (key)."
          },
          {
            "type": "code_example",
            "title": "Sorting in Ruby",
            "content": "```ruby\n# Built-in sort (O(n log n))\n[3, 1, 2].sort                         # [1, 2, 3]\n[3, 1, 2].sort { |a, b| b <=> a }      # [3, 2, 1] (descending)\n\n# sort_by a key (preferred for complex keys, stable)\nwords = %w[apple fig kiwi]\nwords.sort_by(&:length)               # ['fig', 'kiwi', 'apple']\npeople.sort_by { |p| [p.age, p.name] }  # by age then name\n\n# Hand-rolled bubble sort (O(n^2)) — for learning/interviews\ndef bubble(arr)\n  n = arr.length\n  loop do\n    swapped = false\n    (1...n).each do |i|\n      if arr[i - 1] > arr[i]\n        arr[i - 1], arr[i] = arr[i], arr[i - 1]\n        swapped = true\n      end\n    end\n    break unless swapped\n  end\n  arr\nend\n```"
          }
        ]
      },
      "followup_questions": [
        "How do you sort in Ruby and what's the time complexity?",
        "When would you use Basic Sorting Ruby in a real project?",
        "How would you compare Basic Sorting Ruby with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Basic Sorting Ruby — ruby backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Basic Sorting Ruby? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

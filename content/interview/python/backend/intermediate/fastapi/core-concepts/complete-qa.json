{
  "topic": "FastAPI Core Concepts",
  "topicSlug": "fastapi-core-concepts",
  "lang": "python",
  "track": "backend",
  "level": "intermediate",
  "stack": "fastapi",
  "subcategory": "core-concepts",
  "version": "1",
  "lastUpdated": "2025-04",
  "questions": [
    {
      "id": "fastapi-dependency-injection",
      "slug": "fastapi-dependency-injection",
      "title": "FastAPI Dependency Injection",
      "question": "How does FastAPI's dependency injection system work, and what makes it powerful for building APIs?",
      "difficulty": "medium",
      "importance": "critical",
      "tags": [
        "fastapi",
        "dependency-injection",
        "python",
        "async"
      ],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "content": "They want to hear that you understand `Depends()`, can compose dependencies, and understand why DI is better than global state for things like DB sessions, auth, and config."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **`Depends()` resolves a function tree** — FastAPI calls your dependency, injects the result, and resolves nested dependencies automatically\n- **`yield` for setup/teardown** — code before `yield` runs on request, code after runs on response; cleanup executes even if the handler raises\n- **Dependencies are cached per request** — `get_db` referenced in two places gives the same session instance, avoiding duplicate connections\n- **`dependency_overrides` for testing** — swap any dependency with a mock at the app level, no monkey-patching or global state needed\n- **Class-based dependencies** — implement `__call__` for configurable, reusable dependencies with constructor parameters"
          },
          {
            "type": "speakable_answer",
            "content": "FastAPI's dependency injection uses Python's type hints and the `Depends()` function to automatically resolve and inject values into route handlers. When you declare a parameter with `Depends(some_function)`, FastAPI calls `some_function` before your handler, injects the result, and handles teardown via `yield` dependencies.\n\nThe power is composability. A `get_current_user` dependency can depend on `get_db` (which provides a database session), which itself depends on `get_settings` (which reads environment variables). FastAPI resolves this tree automatically, caches repeated dependencies within a request, and handles cleanup when the response is sent.\n\nThis makes testing trivial — override any dependency in tests with `app.dependency_overrides[real_dep] = mock_dep`. No monkey-patching, no global state."
          },
          {
            "type": "important_points",
            "content": "- **`yield` dependencies** — everything before `yield` is setup, after is teardown. Runs even if handler raises.\n- **Dependencies are cached per request** — `get_db` called in two dependencies gives the same session\n- **`use_cache=False`** — to get a fresh dependency instance each time\n- **Class-based dependencies** — use `__call__` for configurable, stateful dependencies\n- **Dependency overrides in tests** — the single best feature for testability"
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "The rule: use `async def` only when all I/O operations inside it are awaited using async libraries.\n\nUse `def` for CPU-bound work or when using synchronous libraries like `psycopg2` or `requests`.\n\nFastAPI runs async routes on the main event loop — if you block the event loop with a synchronous call (like `requests.get()` or a synchronous DB query), you block all other concurrent requests. FastAPI runs sync `def` routes in a thread pool automatically, which is actually safer for blocking code. The correct async stack: `httpx.AsyncClient` instead of `requests`, `asyncpg` or SQLAlchemy with async session instead of psycopg2, `aioredis` instead of redis-py. If your DB library is synchronous, use sync `def` routes — FastAPI handles the threading."
          },
          {
            "type": "practice_prompt",
            "content": "Write a FastAPI dependency that enforces rate limiting: 100 requests per minute per user (or IP for anonymous), using Redis to store counters. The dependency should work on any route with `Depends(rate_limit)`."
          }
        ]
      },
      "speakable_v2": {
        "archetype": "C",
        "pillar": "P01",
        "hook": "FastAPI's dependency injection uses Python's type hints and the Depends() function to automatically resolve and inject values into route handlers.",
        "beats": [
          {
            "kind": "mental_model",
            "layout": "paragraph",
            "text": ""
          },
          {
            "kind": "mechanism",
            "layout": "ordered_list",
            "steps": [
              ""
            ]
          },
          {
            "kind": "edge_cases",
            "layout": "bullets",
            "items": [
              "Depends() resolves a function tree — FastAPI calls your dependency, injects the result, and resolves nested dependencies automatically yield for setup/teardown — code before yield runs on request, code after runs on response; cleanup executes even if the handler raises Dependencies are cached per request — get_db referenced in two places gives the same session instance, avoiding duplicate connections dependency_overrides for testing — swap any dependency with a mock at the app level, no monkey-patching or global state needed Class-based dependencies — implement __call__ for configurable, reusable dependencies with constructor parameters"
            ]
          },
          {
            "kind": "failure_mode",
            "layout": "callout",
            "text": "The production failure mode here is unchecked state mutation under concurrent access."
          }
        ],
        "cap": "No monkey-patching, no global state.",
        "familiarity_anchors": [],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": []
      },
      "followup_questions": [
        "How does Pydantic work in FastAPI, and how do you handle complex validation, custom validators, and nested models?",
        "When should you use `async def` vs `def` in FastAPI routes, and what are the performance implications?"
      ]
    },
    {
      "id": "fastapi-async-performance",
      "slug": "fastapi-async-vs-sync",
      "title": "FastAPI Async vs Sync Endpoints",
      "question": "When should you use `async def` vs `def` in FastAPI routes, and what are the performance implications?",
      "difficulty": "hard",
      "importance": "critical",
      "tags": [
        "fastapi",
        "async",
        "python",
        "performance",
        "asyncio"
      ],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "content": "This is a trap question for many engineers. FastAPI doesn't automatically make everything faster just by adding `async`. They want to hear you understand when async actually helps (I/O-bound operations with async libraries) vs when it hurts (blocking sync code in an async function)."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **`async def` + sync I/O = blocked event loop** — synchronous calls like `requests.get()` freeze all concurrent requests\n- **`def` routes run in a thread pool** — FastAPI automatically handles threading, making sync code safe without manual executor calls\n- **Full async stack required** — `httpx.AsyncClient`, `asyncpg`, and `aioredis` replace their sync counterparts for true concurrency\n- **`run_in_threadpool` for mixed code** — offload CPU-heavy or unavoidably sync operations without blocking the event loop\n- **Profile before assuming async is faster** — measure with `locust` or `k6`; thread-pool sync often matches async for DB-bound workloads"
          },
          {
            "type": "speakable_answer",
            "content": "The rule: use `async def` only when all I/O operations inside it are awaited using async libraries. Use `def` for CPU-bound work or when using synchronous libraries like `psycopg2` or `requests`.\n\nFastAPI runs async routes on the main event loop — if you block the event loop with a synchronous call (like `requests.get()` or a synchronous DB query), you block all other concurrent requests. FastAPI runs sync `def` routes in a thread pool automatically, which is actually safer for blocking code.\n\nThe correct async stack: `httpx.AsyncClient` instead of `requests`, `asyncpg` or SQLAlchemy with async session instead of psycopg2, `aioredis` instead of redis-py. If your DB library is synchronous, use sync `def` routes — FastAPI handles the threading."
          },
          {
            "type": "important_points",
            "content": "- **`async def` with sync library = worse than sync `def`** — you get the overhead of async with none of the benefits\n- **FastAPI runs `def` routes in a thread pool** — safe for synchronous, blocking code\n- **Use `httpx` not `requests`** for outbound HTTP in async routes\n- **`asyncpg` or SQLAlchemy async** for PostgreSQL in async routes\n- **Profile with `locust` or `k6`** — measure actual throughput, don't assume async is faster"
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "The rule: use `async def` only when all I/O operations inside it are awaited using async libraries.\n\nUse `def` for CPU-bound work or when using synchronous libraries like `psycopg2` or `requests`.\n\nFastAPI runs async routes on the main event loop — if you block the event loop with a synchronous call (like `requests.get()` or a synchronous DB query), you block all other concurrent requests. FastAPI runs sync `def` routes in a thread pool automatically, which is actually safer for blocking code. The correct async stack: `httpx.AsyncClient` instead of `requests`, `asyncpg` or SQLAlchemy with async session instead of psycopg2, `aioredis` instead of redis-py. If your DB library is synchronous, use sync `def` routes — FastAPI handles the threading."
          }
        ]
      },
      "speakable_v2": {
        "archetype": "B",
        "pillar": "P01",
        "hook": "The rule: use async def only when all I/O operations inside it are awaited using async libraries.",
        "beats": [
          {
            "kind": "what_each_is",
            "layout": "paragraph",
            "text": ""
          },
          {
            "kind": "differences",
            "layout": "bullets",
            "items": [
              "async def + sync I/O = blocked event loop — synchronous calls like requests",
              "get() freeze all concurrent requests def routes run in a thread pool — FastAPI automatically handles threading, making sync code safe without manual executor calls Full async stack required — httpx",
              "AsyncClient, asyncpg, and aioredis replace their sync counterparts for true concurrency run_in_threadpool for mixed code — offload CPU-heavy or unavoidably sync operations without blocking the event loop Profile before assuming async is faster — measure with locust or k6; thread-pool sync often matches async for DB-bound workloads"
            ]
          },
          {
            "kind": "when_to_pick",
            "layout": "paragraph",
            "text": ""
          },
          {
            "kind": "tiny_example",
            "layout": "paragraph",
            "text": ""
          }
        ],
        "cap": "If your DB library is synchronous, use sync def routes — FastAPI handles the threading.",
        "familiarity_anchors": [],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": []
      },
      "followup_questions": [
        "How does FastAPI's dependency injection system work, and what makes it powerful for building APIs?",
        "How does Pydantic work in FastAPI, and how do you handle complex validation, custom validators, and nested models?"
      ]
    },
    {
      "id": "fastapi-pydantic-validation",
      "slug": "fastapi-pydantic-validation",
      "title": "Pydantic Validation in FastAPI",
      "question": "How does Pydantic work in FastAPI, and how do you handle complex validation, custom validators, and nested models?",
      "difficulty": "medium",
      "importance": "high",
      "tags": [
        "fastapi",
        "pydantic",
        "validation",
        "python",
        "models"
      ],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "content": "They want to see you can write production Pydantic models with custom validators, understand the separation between input models, output models, and DB models, and know how to handle validation errors cleanly."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Separate input, output, and DB models** — never return ORM objects directly; `response_model` controls exactly what the client sees\n- **`@field_validator` for business rules** — password strength, phone format, and email validation run before the handler executes\n- **`@model_validator(mode='after')` for cross-field checks** — compare password and confirm_password after individual field validation passes\n- **`from_attributes = True`** — Pydantic v2 setting that lets models read directly from SQLAlchemy object attributes\n- **`exclude_unset=True` for PATCH** — `.model_dump(exclude_unset=True)` returns only fields the client sent, enabling partial updates"
          },
          {
            "type": "speakable_answer",
            "content": "Pydantic is FastAPI's data validation backbone. Every request body, query parameter, and response body is a Pydantic model. FastAPI uses the model's type hints to validate incoming data, generate OpenAPI docs, and serialize responses — automatically.\n\nFor production APIs: separate input models from output models from database models. Never return your SQLAlchemy model directly — create a Pydantic response model that explicitly lists what's exposed. Use `@field_validator` for business rule validation like phone number format or password strength. Use `model_validator` for cross-field validation like confirming password matches confirm_password."
          },
          {
            "type": "important_points",
            "content": "- **Always use `response_model`** — prevents accidentally exposing internal fields like password hashes\n- **`from_attributes = True`** (Pydantic v2) — needed to create Pydantic models from ORM objects\n- **`model_validator(mode='after')`** — cross-field validation after individual fields are validated\n- **`Optional[X] = None` vs `X | None`** — both work in Pydantic v2; the latter is more Pythonic\n- **`exclude_unset=True`** in `.model_dump()` — for PATCH endpoints, only update fields the client sent"
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "The rule: use `async def` only when all I/O operations inside it are awaited using async libraries.\n\nUse `def` for CPU-bound work or when using synchronous libraries like `psycopg2` or `requests`.\n\nFastAPI runs async routes on the main event loop — if you block the event loop with a synchronous call (like `requests.get()` or a synchronous DB query), you block all other concurrent requests. FastAPI runs sync `def` routes in a thread pool automatically, which is actually safer for blocking code. The correct async stack: `httpx.AsyncClient` instead of `requests`, `asyncpg` or SQLAlchemy with async session instead of psycopg2, `aioredis` instead of redis-py. If your DB library is synchronous, use sync `def` routes — FastAPI handles the threading."
          }
        ]
      },
      "speakable_v2": {
        "archetype": "C",
        "pillar": "P01",
        "hook": "Pydantic is FastAPI's data validation backbone.",
        "beats": [
          {
            "kind": "mental_model",
            "layout": "paragraph",
            "text": ""
          },
          {
            "kind": "mechanism",
            "layout": "ordered_list",
            "steps": [
              ""
            ]
          },
          {
            "kind": "edge_cases",
            "layout": "bullets",
            "items": [
              "Separate input, output, and DB models — never return ORM objects directly; response_model controls exactly what the client sees @field_validator for business rules — password strength, phone format, and email validation run before the handler executes @model_validator(mode='after') for cross-field checks — compare password and confirm_password after individual field validation passes from_attributes = True — Pydantic v2 setting that lets models read directly from SQLAlchemy object attributes exclude_unset=True for PATCH —",
              "model_dump(exclude_unset=True) returns only fields the client sent, enabling partial updates"
            ]
          },
          {
            "kind": "failure_mode",
            "layout": "callout",
            "text": "The production failure mode here is unchecked state mutation under concurrent access."
          }
        ],
        "cap": "Use model_validator for cross-field validation like confirming password matches confirm_password.",
        "familiarity_anchors": [],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": []
      },
      "followup_questions": [
        "How does FastAPI's dependency injection system work, and what makes it powerful for building APIs?",
        "When should you use `async def` vs `def` in FastAPI routes, and what are the performance implications?"
      ]
    }
  ]
}

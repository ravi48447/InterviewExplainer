{
  "topic": "mypy Basics",
  "topicSlug": "mypy-basics",
  "questions": [
    {
      "id": "mypy-basics-q001",
      "slug": "python-mypy-static-type-checking",
      "question": "What is mypy and how do you use it for static type checking in Python?",
      "title": "mypy: Static Type Checking for Python",
      "direct_answer": "mypy is a static type checker for Python: it reads your type annotations and reports type errors before you run the code, catching bugs that Python's runtime ignores (passing a str where an int is expected, calling a missing method, returning the wrong type). You run it on your files (mypy file.py) or package (mypy mypackage/); it reports errors like 'Argument 1 has incompatible type.' mypy doesn't change runtime behavior — Python still ignores annotations at runtime; mypy checks them statically. Use it in CI and your editor to get the safety of static typing without giving up Python's dynamic nature.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 7,
      "last_updated": "2026-08-08",
      "interviewer_intent": {
        "testing": "Whether you understand mypy as a STATIC checker (reads annotations, reports errors before running) and that Python ignores annotations at runtime — mypy is the enforcement layer.",
        "common_mistake": "Thinking annotations are enforced at runtime, or running mypy once and ignoring its output (it only helps if you fix the errors).",
        "to_stand_out": "mypy is a static type checker: reads annotations, reports type errors before running (Python ignores them at runtime). Run in CI/editor for safety without losing Python's dynamic nature."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Define mypy: Static Type Checking for Python simply, show a small example, and mention the main trade-off or edge case."
          },
          {
            "type": "key_points",
            "title": "Key Takeaways",
            "items": [
              "mypy is a static type checker: it reads annotations and reports type errors BEFORE running.",
              "Python ignores annotations at runtime — mypy is the static enforcement layer.",
              "Run it on files/packages (mypy file.py, mypy mypackage/); use --strict for new code.",
              "Integrate mypy in CI (every commit) and your editor (live checks) for early safety.",
              "Fix the errors it reports — annotations only help if the flagged errors are fixed."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Spoken Answer",
            "content": "mypy is a static type checker for Python. It reads your type annotations and reports type errors before you run the code, catching bugs that Python's runtime ignores — passing a str where an int is expected, calling a method that doesn't exist, returning the wrong type. You run it on a file, mypy file.py, or a package, mypy mypackage, and it reports errors like 'Argument 1 has incompatible type.' mypy doesn't change runtime behavior — Python still ignores annotations at runtime; mypy is the static layer that enforces them. Use it in CI, running on every commit, and in your editor for live checks, to get the safety of static typing without giving up Python's dynamic nature. The common mistakes are thinking annotations are enforced at runtime, which they're not — mypy is the enforcement layer, running mypy once and ignoring its output, because it only helps if you fix the errors it flags, and not configuring mypy — the defaults are lenient, so use --strict for new code to catch more."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "mypy is a static type checker for Python: it reads your type annotations and reports type errors before you run the code, catching bugs that Python's runtime ignores (passing a str where an int is expected, calling a missing method, returning the wrong type).\n\nYou run it on your files (mypy file.py) or package (mypy mypackage/); it reports errors like 'Argument 1 has incompatible type.' mypy doesn't change runtime behavior — Python still ignores annotations at runtime; mypy checks them statically.\n\nUse it in CI and your editor to get the safety of static typing without giving up Python's dynamic nature.\n\nThinking annotations are enforced at runtime, or running mypy once and ignoring its output (it only helps if you fix the errors)."
          },
          {
            "type": "flow_diagram",
            "title": "mypy's Place in the Workflow",
            "content": "mypy reads annotations and reports errors; Python ignores them at runtime. Run it in CI and the editor.\n\n```mermaid\nflowchart LR\n  S[annotated source] --> M[mypy: static check]\n  M -->|errors| E[fix before running]\n  S -->|runtime| R[Python: ignores annotations]\n  E --> R\n```"
          },
          {
            "type": "code_example",
            "title": "Running mypy and an error it catches",
            "content": "```python\n# example.py\ndef greet(name: str) -> str:\n    return f\"Hi, {name}!\"\n\ngreet(123)        # BUG: passing an int where str is expected\n\n# --- Run mypy ---\n# $ mypy example.py\n#   example.py:4: error: Argument 1 to \"greet\" has incompatible type \"int\";\n#   expected \"str\"  [arg-type]\n#\n# Python RUNS this fine (no runtime error) — mypy catches it BEFORE running.\n#\n# Common mypy commands:\n#   mypy file.py             # check one file\n#   mypy mypackage/          # check a package\n#   mypy --strict mypackage/ # stricter checks (recommended for new code)\n#   mypy --ignore-missing-imports  # don't fail on untyped third-party libs\n```"
          }
        ]
      },
      "followup_questions": [
        "When would you use Mypy Basics in a real project?",
        "How would you compare Mypy Basics with an alternative?",
        "What is a common mistake when using Mypy Basics?"
      ],
      "seo": {
        "metaTitle": "mypy: Static Type Checking for Python — python backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is mypy and how do you use it for static type checking in Python? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1
    },
    {
      "id": "type-hints-basics-mypy-basics-q001",
      "slug": "type-hints-basics-mypy-basics-when-to-use",
      "question": "When would you use Mypy Basics in a real project?",
      "title": "When would you use Mypy Basics in a real project",
      "direct_answer": "I would use mypy and how do you use it for static type checking in Python when the problem matches its main benefit. mypy is a static type checker for Python: it reads your type annotations and reports type errors before you run the code, catching bugs that Python's runtime ignores (passing a str where an int is expected, calling a missing method, returning the wrong type). In a real project, I would confirm that benefit with the input shape, correctness rules, and expected load before choosing it. I would keep the use local and explain the decision in the code review.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain mypy and how do you use it for static type checking in Python in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about mypy and how do you use it for static type checking in Python without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining mypy and how do you use it for static type checking in Python in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. mypy is a static type checker for Python: it reads your type annotations and reports type errors before you run the code, catching bugs that Python's runtime ignores (passing a str where an int is expected, calling a missing method, returning the wrong type). You run it on your files (mypy file.py) or package (mypy mypackage/); it reports errors like 'Argument 1 has incompatible type.' mypy doesn't change runtime behavior — Python still ignores annotations at runtime; mypy checks them statically. Use it in CI and your editor to get the safety of static typing without giving up Python's dynamic nature."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use mypy and how do you use it for static type checking in Python when its rule makes the code safer, clearer, or more efficient for the actual problem. mypy is a static type checker for Python: it reads your type annotations and reports type errors before you run the code, catching bugs that Python's runtime ignores (passing a str where an int is expected, calling a missing method, returning the wrong type). You run it on your files (mypy file.py) or package (mypy mypackage/); it reports errors like 'Argument 1 has incompatible type.' mypy doesn't change runtime behavior — Python still ignores annotations at runtime; mypy checks them statically. Use it in CI and your editor to get the safety of static typing without giving up Python's dynamic nature. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "mypy is a static type checker for Python: it reads your type annotations and reports type errors before you run the code, catching bugs that Python's runtime ignores (passing a str where an int is expected, calling a missing method, returning the wrong type).\n\nYou run it on your files (mypy file.py) or package (mypy mypackage/); it reports errors like 'Argument 1 has incompatible type.' mypy doesn't change runtime behavior — Python still ignores annotations at runtime; mypy checks them statically.\n\nUse it in CI and your editor to get the safety of static typing without giving up Python's dynamic nature.\n\nThinking annotations are enforced at runtime, or running mypy once and ignoring its output (it only helps if you fix the errors)."
          },
          {
            "type": "code_example",
            "title": "Running mypy and an error it catches",
            "content": "```python\n# example.py\ndef greet(name: str) -> str:\n    return f\"Hi, {name}!\"\n\ngreet(123)        # BUG: passing an int where str is expected\n\n# --- Run mypy ---\n# $ mypy example.py\n#   example.py:4: error: Argument 1 to \"greet\" has incompatible type \"int\";\n#   expected \"str\"  [arg-type]\n#\n# Python RUNS this fine (no runtime error) — mypy catches it BEFORE running.\n#\n# Common mypy commands:\n#   mypy file.py             # check one file\n#   mypy mypackage/          # check a package\n#   mypy --strict mypackage/ # stricter checks (recommended for new code)\n#   mypy --ignore-missing-imports  # don't fail on untyped third-party libs\n```"
          }
        ]
      },
      "followup_questions": [
        "What is mypy and how do you use it for static type checking in Python?",
        "How would you compare Mypy Basics with an alternative?",
        "What is a common mistake when using Mypy Basics?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Mypy Basics in a real project — python backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Mypy Basics in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "type-hints-basics-mypy-basics-q002",
      "slug": "type-hints-basics-mypy-basics-common-mistake",
      "question": "What is a common mistake when using Mypy Basics?",
      "title": "a common mistake when using Mypy Basics",
      "direct_answer": "A common mistake with mypy and how do you use it for static type checking in Python is using the rule without checking its boundary conditions. mypy is a static type checker for Python: it reads your type annotations and reports type errors before you run the code, catching bugs that Python's runtime ignores (passing a str where an int is expected, calling a missing method, returning the wrong type). That can make a valid-looking result fail for an empty input, a nil value, a duplicate, or a different ordering. I prevent it by naming the assumption, testing the boundary, and checking the result against a small hand-worked example.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain mypy and how do you use it for static type checking in Python in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about mypy and how do you use it for static type checking in Python without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only “be careful”; connect the mistake to the behaviour of mypy and how do you use it for static type checking in Python. mypy is a static type checker for Python: it reads your type annotations and reports type errors before you run the code, catching bugs that Python's runtime ignores (passing a str where an int is expected, calling a missing method, returning the wrong type). You run it on your files (mypy file.py) or package (mypy mypackage/); it reports errors like 'Argument 1 has incompatible type.' mypy doesn't change runtime behavior — Python still ignores annotations at runtime; mypy checks them statically. Use it in CI and your editor to get the safety of static typing without giving up Python's dynamic nature."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with mypy and how do you use it for static type checking in Python is assuming the happy path is the whole rule. mypy is a static type checker for Python: it reads your type annotations and reports type errors before you run the code, catching bugs that Python's runtime ignores (passing a str where an int is expected, calling a missing method, returning the wrong type). You run it on your files (mypy file.py) or package (mypy mypackage/); it reports errors like 'Argument 1 has incompatible type.' mypy doesn't change runtime behavior — Python still ignores annotations at runtime; mypy checks them statically. Use it in CI and your editor to get the safety of static typing without giving up Python's dynamic nature. For example, the normal case may work while the zero, nil, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "mypy is a static type checker for Python: it reads your type annotations and reports type errors before you run the code, catching bugs that Python's runtime ignores (passing a str where an int is expected, calling a missing method, returning the wrong type).\n\nYou run it on your files (mypy file.py) or package (mypy mypackage/); it reports errors like 'Argument 1 has incompatible type.' mypy doesn't change runtime behavior — Python still ignores annotations at runtime; mypy checks them statically.\n\nUse it in CI and your editor to get the safety of static typing without giving up Python's dynamic nature.\n\nThinking annotations are enforced at runtime, or running mypy once and ignoring its output (it only helps if you fix the errors)."
          },
          {
            "type": "code_example",
            "title": "Running mypy and an error it catches",
            "content": "```python\n# example.py\ndef greet(name: str) -> str:\n    return f\"Hi, {name}!\"\n\ngreet(123)        # BUG: passing an int where str is expected\n\n# --- Run mypy ---\n# $ mypy example.py\n#   example.py:4: error: Argument 1 to \"greet\" has incompatible type \"int\";\n#   expected \"str\"  [arg-type]\n#\n# Python RUNS this fine (no runtime error) — mypy catches it BEFORE running.\n#\n# Common mypy commands:\n#   mypy file.py             # check one file\n#   mypy mypackage/          # check a package\n#   mypy --strict mypackage/ # stricter checks (recommended for new code)\n#   mypy --ignore-missing-imports  # don't fail on untyped third-party libs\n```"
          }
        ]
      },
      "followup_questions": [
        "What is mypy and how do you use it for static type checking in Python?",
        "When would you use Mypy Basics in a real project?",
        "How would you compare Mypy Basics with an alternative?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Mypy Basics — python backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Mypy Basics? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "type-hints-basics-mypy-basics-q003",
      "slug": "type-hints-basics-mypy-basics-compare",
      "question": "How would you compare Mypy Basics with an alternative?",
      "title": "compare Mypy Basics with an alternative",
      "direct_answer": "I compare mypy and how do you use it for static type checking in Python with the simpler alternative by looking at correctness first, then time, memory, and readability. mypy is a static type checker for Python: it reads your type annotations and reports type errors before you run the code, catching bugs that Python's runtime ignores (passing a str where an int is expected, calling a missing method, returning the wrong type). The better choice depends on the input and the failure modes, so I would state that trade-off instead of claiming one option is always best.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain mypy and how do you use it for static type checking in Python in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about mypy and how do you use it for static type checking in Python without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. mypy is a static type checker for Python: it reads your type annotations and reports type errors before you run the code, catching bugs that Python's runtime ignores (passing a str where an int is expected, calling a missing method, returning the wrong type). You run it on your files (mypy file.py) or package (mypy mypackage/); it reports errors like 'Argument 1 has incompatible type.' mypy doesn't change runtime behavior — Python still ignores annotations at runtime; mypy checks them statically. Use it in CI and your editor to get the safety of static typing without giving up Python's dynamic nature."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare mypy and how do you use it for static type checking in Python with the straightforward alternative on the same example. mypy is a static type checker for Python: it reads your type annotations and reports type errors before you run the code, catching bugs that Python's runtime ignores (passing a str where an int is expected, calling a missing method, returning the wrong type). You run it on your files (mypy file.py) or package (mypy mypackage/); it reports errors like 'Argument 1 has incompatible type.' mypy doesn't change runtime behavior — Python still ignores annotations at runtime; mypy checks them statically. Use it in CI and your editor to get the safety of static typing without giving up Python's dynamic nature. If mypy and how do you use it for static type checking in Python removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "mypy is a static type checker for Python: it reads your type annotations and reports type errors before you run the code, catching bugs that Python's runtime ignores (passing a str where an int is expected, calling a missing method, returning the wrong type).\n\nYou run it on your files (mypy file.py) or package (mypy mypackage/); it reports errors like 'Argument 1 has incompatible type.' mypy doesn't change runtime behavior — Python still ignores annotations at runtime; mypy checks them statically.\n\nUse it in CI and your editor to get the safety of static typing without giving up Python's dynamic nature.\n\nThinking annotations are enforced at runtime, or running mypy once and ignoring its output (it only helps if you fix the errors)."
          },
          {
            "type": "code_example",
            "title": "Running mypy and an error it catches",
            "content": "```python\n# example.py\ndef greet(name: str) -> str:\n    return f\"Hi, {name}!\"\n\ngreet(123)        # BUG: passing an int where str is expected\n\n# --- Run mypy ---\n# $ mypy example.py\n#   example.py:4: error: Argument 1 to \"greet\" has incompatible type \"int\";\n#   expected \"str\"  [arg-type]\n#\n# Python RUNS this fine (no runtime error) — mypy catches it BEFORE running.\n#\n# Common mypy commands:\n#   mypy file.py             # check one file\n#   mypy mypackage/          # check a package\n#   mypy --strict mypackage/ # stricter checks (recommended for new code)\n#   mypy --ignore-missing-imports  # don't fail on untyped third-party libs\n```"
          }
        ]
      },
      "followup_questions": [
        "What is mypy and how do you use it for static type checking in Python?",
        "When would you use Mypy Basics in a real project?",
        "What is a common mistake when using Mypy Basics?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Mypy Basics with an alternative — python backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Mypy Basics with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "type-hints-basics-mypy-basics-q004",
      "slug": "type-hints-basics-mypy-basics-scenario",
      "question": "How would you debug a problem involving Mypy Basics?",
      "title": "debug a problem involving Mypy Basics",
      "direct_answer": "I would debug a problem involving mypy and how do you use it for static type checking in Python by reproducing it with the smallest input first. mypy is a static type checker for Python: it reads your type annotations and reports type errors before you run the code, catching bugs that Python's runtime ignores (passing a str where an int is expected, calling a missing method, returning the wrong type). Then I would inspect the values at the boundary where the result changes, add a regression test, and check an empty, nil, duplicate, or maximum-size case as appropriate. This keeps the investigation evidence-based instead of guessing.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain mypy and how do you use it for static type checking in Python in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about mypy and how do you use it for static type checking in Python without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what mypy and how do you use it for static type checking in Python is supposed to guarantee. mypy is a static type checker for Python: it reads your type annotations and reports type errors before you run the code, catching bugs that Python's runtime ignores (passing a str where an int is expected, calling a missing method, returning the wrong type). You run it on your files (mypy file.py) or package (mypy mypackage/); it reports errors like 'Argument 1 has incompatible type.' mypy doesn't change runtime behavior — Python still ignores annotations at runtime; mypy checks them statically. Use it in CI and your editor to get the safety of static typing without giving up Python's dynamic nature."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the mypy and how do you use it for static type checking in Python bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: mypy is a static type checker for Python: it reads your type annotations and reports type errors before you run the code, catching bugs that Python's runtime ignores (passing a str where an int is expected, calling a missing method, returning the wrong type). You run it on your files (mypy file.py) or package (mypy mypackage/); it reports errors like 'Argument 1 has incompatible type.' mypy doesn't change runtime behavior — Python still ignores annotations at runtime; mypy checks them statically. Use it in CI and your editor to get the safety of static typing without giving up Python's dynamic nature. Once I find the first incorrect state, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Reason through the problem",
            "content": "mypy is a static type checker for Python: it reads your type annotations and reports type errors before you run the code, catching bugs that Python's runtime ignores (passing a str where an int is expected, calling a missing method, returning the wrong type).\n\nYou run it on your files (mypy file.py) or package (mypy mypackage/); it reports errors like 'Argument 1 has incompatible type.' mypy doesn't change runtime behavior — Python still ignores annotations at runtime; mypy checks them statically.\n\nUse it in CI and your editor to get the safety of static typing without giving up Python's dynamic nature.\n\nThinking annotations are enforced at runtime, or running mypy once and ignoring its output (it only helps if you fix the errors)."
          },
          {
            "type": "code_example",
            "title": "Running mypy and an error it catches",
            "content": "```python\n# example.py\ndef greet(name: str) -> str:\n    return f\"Hi, {name}!\"\n\ngreet(123)        # BUG: passing an int where str is expected\n\n# --- Run mypy ---\n# $ mypy example.py\n#   example.py:4: error: Argument 1 to \"greet\" has incompatible type \"int\";\n#   expected \"str\"  [arg-type]\n#\n# Python RUNS this fine (no runtime error) — mypy catches it BEFORE running.\n#\n# Common mypy commands:\n#   mypy file.py             # check one file\n#   mypy mypackage/          # check a package\n#   mypy --strict mypackage/ # stricter checks (recommended for new code)\n#   mypy --ignore-missing-imports  # don't fail on untyped third-party libs\n```"
          }
        ]
      },
      "followup_questions": [
        "What is mypy and how do you use it for static type checking in Python?",
        "When would you use Mypy Basics in a real project?",
        "How would you compare Mypy Basics with an alternative?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Mypy Basics — python backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Mypy Basics? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

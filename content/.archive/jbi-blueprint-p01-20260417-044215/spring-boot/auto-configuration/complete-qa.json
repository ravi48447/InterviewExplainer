{
  "topic": "Auto Configuration",
  "topicSlug": "auto-configuration",
  "questions": [
    {
      "id": "sbo-001",
      "slug": "spring-boot-auto-configuration-internals",
      "question": "How does Spring Boot auto-configuration work internally?",
      "title": "Spring Boot Auto-Configuration \u2014 How It Works Internally",
      "direct_answer": "Spring Boot auto-configuration uses @EnableAutoConfiguration to scan META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports (or spring.factories in older versions) for candidate configuration classes. Each candidate is conditionally applied using @ConditionalOn* annotations that check for the presence of classes, beans, or properties. This lets Spring Boot configure sensible defaults without requiring explicit XML or Java configuration from the developer.",
      "layout_type": "internals-deep-dive",
      "interviewer_intent": {
        "testing": "Whether the candidate understands the mechanism behind Spring Boot's 'magic' rather than just using it as a black box.",
        "common_mistake": "Candidates often say 'Spring Boot just auto-configures things' without explaining how conditions and imports files drive the process.",
        "to_stand_out": "Mention the difference between spring.factories (pre-2.7) and AutoConfiguration.imports (2.7+), and how @AutoConfigureAfter/@AutoConfigureBefore control ordering."
      },
      "company_tags": [
        "Amazon",
        "Google",
        "Netflix",
        "Pivotal",
        "ThoughtWorks"
      ],
      "reading_time_minutes": 6,
      "last_updated": "2025-01-15",
      "difficulty": "intermediate",
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Auto-Configuration Isn't Magic \u2014 It's a Pipeline of Conditional @Configuration Classes",
            "content": "Most developers describe Spring Boot auto-configuration as \"it just figures out what you need.\" That's the experience, not the mechanism. Under the hood, auto-configuration is a deterministic pipeline: Boot discovers a list of candidate `@Configuration` classes from a well-known file on the classpath, evaluates a set of `@Conditional` annotations on each one, and only registers the beans from classes whose conditions all pass.\n\n**There is no scanning, no guessing, no AI.** Every auto-configuration class is a regular `@Configuration` class with `@Bean` methods \u2014 the same kind you'd write yourself. The only difference is that each one is guarded by conditions like `@ConditionalOnClass` (\"only if this class is on the classpath\"), `@ConditionalOnMissingBean` (\"only if the developer hasn't already defined this bean\"), and `@ConditionalOnProperty` (\"only if this property is set\"). When you add `spring-boot-starter-data-jpa` to your POM, Boot doesn't \"detect\" that you want JPA \u2014 it finds `DataSourceAutoConfiguration` in its imports file, checks that `javax.sql.DataSource` is on the classpath, confirms you haven't defined your own, and wires one with HikariCP defaults."
          },
          {
            "type": "phase",
            "title": "Discovery \u2014 How Boot Finds 150+ Candidate Configuration Classes",
            "content": "When `@SpringBootApplication` triggers `@EnableAutoConfiguration`, Spring imports `AutoConfigurationImportSelector`, which reads a file that lists every auto-configuration class Boot ships. **In Spring Boot 2.7+**, this file is `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` \u2014 one fully-qualified class name per line. In older versions, the same list lived in `META-INF/spring.factories` under the `EnableAutoConfiguration` key.\n\nThe migration happened because `spring.factories` was overloaded \u2014 it handled auto-configuration, listeners, initializers, and failure analyzers all in one file. The new dedicated imports file is faster to parse and makes it obvious which classes are auto-configuration candidates.\n\nSpring Boot 3.x ships roughly **150 auto-configuration classes** in this file. Every one of them is loaded as a candidate \u2014 but loading the class doesn't mean its beans get registered. That decision happens in the next phase. The key insight is that discovery is exhaustive and condition evaluation is selective. Boot loads all candidates and then filters, rather than trying to discover only the relevant ones."
          },
          {
            "type": "phase",
            "title": "Condition Evaluation \u2014 The Chain of @ConditionalOn* Annotations That Decides Everything",
            "content": "Each auto-configuration class is decorated with one or more `@Conditional` annotations. Spring evaluates them in a specific order optimized for speed \u2014 cheap checks first, expensive checks last.\n\nThe most common conditions form a decision chain. `@ConditionalOnClass` checks whether a class exists on the classpath without actually loading it \u2014 this is the cheapest filter and eliminates most candidates immediately. If you don't have `javax.jms.ConnectionFactory` on your classpath, `ActiveMQAutoConfiguration` is skipped before any bean definitions are read.\n\nNext, `@ConditionalOnProperty` checks application properties. A class guarded by `@ConditionalOnProperty(name = \"spring.data.redis.repositories.enabled\", havingValue = \"true\", matchIfMissing = true)` will activate by default but can be disabled with a single property. This is how you turn off auto-configuration selectively without using `exclude`.\n\nThe most important condition for developers is **`@ConditionalOnMissingBean`**. This is what makes Boot non-invasive \u2014 it checks whether a bean of a specific type already exists in the context. If you define your own `DataSource` bean, Boot's auto-configured one backs off silently. This is the mechanism behind \"convention over configuration\" \u2014 Boot provides the convention, your explicit bean is the configuration that overrides it.\n\nWhen an auto-configuration doesn't behave as expected, run with `--debug` or set `debug=true` in `application.properties`. Boot prints a **conditions evaluation report** listing every candidate class and exactly which condition passed or failed. This report is the single most useful debugging tool for auto-configuration issues."
          },
          {
            "type": "phase",
            "title": "Ordering and Exclusion \u2014 Why @AutoConfigureAfter Exists and How to Disable What You Don't Want",
            "content": "Auto-configuration classes don't run in random order \u2014 they declare dependencies on each other using `@AutoConfigureAfter` and `@AutoConfigureBefore`. For example, `JpaRepositoriesAutoConfiguration` declares `@AutoConfigureAfter(DataSourceAutoConfiguration.class)` because JPA repositories need a DataSource to already exist.\n\n**Getting the ordering wrong in a custom auto-configuration is a common source of `BeanCreationException`** at startup. If your auto-configuration creates a bean that depends on another auto-configured bean, you must declare the ordering explicitly \u2014 Spring won't infer it from the bean dependency graph at the auto-configuration level.\n\nTo exclude an auto-configuration entirely, use `@SpringBootApplication(exclude = DataSourceAutoConfiguration.class)` or set `spring.autoconfigure.exclude` in properties. The exclude approach is cleaner than fighting conditions with dummy beans.\n\nThe combination of discovery, conditions, and ordering produces a fully deterministic startup. Given the same classpath, the same properties, and the same user-defined beans, Boot will always produce the same application context. When something changes \u2014 a new JAR on the classpath, a removed property \u2014 you can trace exactly which condition flipped and which beans appeared or disappeared."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **`@EnableAutoConfiguration` loads candidate classes from `AutoConfiguration.imports`** \u2014 Spring Boot 2.7+ uses `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`; pre-2.7 used `META-INF/spring.factories`.\n- **Every auto-config class is `@Conditional` \u2014 never truly unconditional** \u2014 `@ConditionalOnClass` checks the classpath; `@ConditionalOnMissingBean` checks the registry; user-defined beans always take precedence.\n- **Auto-configuration runs after user configuration** \u2014 `@Configuration` classes from your packages are processed first; auto-config is effectively the fallback.\n- **`--debug` prints the Conditions Evaluation Report** \u2014 three sections: positive (matched and applied), negative (checked and skipped), exclusions; the only reliable way to know why an auto-config fired or did not.\n- **`spring.autoconfigure.exclude` removes a class entirely** \u2014 prevents an auto-config from being evaluated even if its conditions would match; useful when a starter pulls in something you do not want."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "When Spring Boot 'just works' without any configuration, auto-configuration is what is happening behind the scenes. It is not magic \u2014 it is conditional bean registration driven by what is on your classpath and what beans you have already defined.\n\n**How it is discovered**\n- `@SpringBootApplication` includes `@EnableAutoConfiguration`, which reads `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` (Spring Boot 2.7+) listing hundreds of candidate auto-configuration classes\n- Each class is evaluated for its `@Conditional` annotations before any beans are created\n\n**How conditions decide what registers**\n- `@ConditionalOnClass(HikariDataSource.class)` \u2014 only apply this config if HikariCP is on the classpath\n- `@ConditionalOnMissingBean(DataSource.class)` \u2014 only apply if you have not already defined a `DataSource`; this is how your custom bean overrides the default\n- Your `@Configuration` classes are processed before auto-configuration \u2014 auto-config is always the fallback, never the override\n\n**Debugging**\n- Add `--debug` to the JVM arguments and Spring prints the Conditions Evaluation Report at startup\n- Three sections: positive matches (auto-config that ran), negative matches (auto-config that was skipped and why), and exclusions\n- `spring.autoconfigure.exclude=org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration` removes a class entirely when a starter pulls in something unwanted"
          }
        ]
      },
      "followup_questions": [
        "How would you debug which auto-configurations are being applied at startup?",
        "How do you write and register your own auto-configuration for an internal library?",
        "What is the difference between @Configuration and @AutoConfiguration?"
      ],
      "seo": {
        "metaTitle": "Spring Boot Auto-Configuration Internals | Java Intermediate Interview",
        "metaDescription": "Deep dive into how Spring Boot auto-configuration works: AutoConfiguration.imports, @ConditionalOn* annotations, ordering, and user-bean priority \u2014 explained for intermediate Java interviews."
      },
      "speakable_v2": {
        "archetype": "C",
        "pillar": "P02",
        "hook": "When Spring Boot 'just works' without any configuration, auto-configuration is what is happening behind the scenes.",
        "beats": [
          {
            "kind": "mental_model",
            "layout": "paragraph",
            "text": "Most developers describe Spring Boot auto-configuration as \"it just figures out what you need.\" That's the experience, not the mechanism. Under the hood, auto-configuration is a deterministic pipeline: Boot discovers a list of candidate @Configuration classes from a well-known file on the classpath, evaluates a set of @Conditional annotations on each one, and only registers the beans from classes whose conditions all pass. There is no scanning, no guessing, no AI. Every auto-configuration class is a regular @Configuration class with @Bean methods \u2014 the same kind you'd write yourself. The."
          },
          {
            "kind": "mechanism",
            "layout": "ordered_list",
            "steps": [
              "When @SpringBootApplication triggers @EnableAutoConfiguration, Spring imports AutoConfigurationImportSelector, which reads a file that lists every auto-configuration class Boot ships.",
              "In Spring Boot 2.7+, this file is META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports \u2014 one fully-qualified class name per line.",
              "In older versions, the same list lived in META-INF/spring.factories under the EnableAutoConfiguration key.",
              "The migration happened because spring.factories was overloaded \u2014 it handled auto-configuration, listeners, initializers, and failure analyzers all in one file.",
              "The new dedicated imports file is faster to parse and makes it obvious which classes are auto-configuration candidates.",
              "Spring Boot 3.x ships roughly 150 auto-configuration classes in this file."
            ]
          },
          {
            "kind": "edge_cases",
            "layout": "bullets",
            "items": [
              "@EnableAutoConfiguration loads candidate classes from AutoConfiguration",
              "imports \u2014 Spring Boot 2",
              "7+ uses META-INF/spring/org",
              "springframework",
              "autoconfigure"
            ]
          },
          {
            "kind": "failure_mode",
            "layout": "callout",
            "text": "Candidates often say 'Spring Boot just auto-configures things' without explaining how conditions and imports files drive the process."
          }
        ],
        "cap": "How it is discovered @SpringBootApplication includes @EnableAutoConfiguration, which reads META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports (Spring Boot 2.7+) listing h",
        "familiarity_anchors": [
          "Spring",
          "EnableAutoConfiguration",
          "AutoConfiguration"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": [
          "How would you debug which auto-configurations are being applied at startup?",
          "How do you write and register your own auto-configuration for an internal library?",
          "What is the difference between @Configuration and @AutoConfiguration?"
        ]
      }
    },
    {
      "id": "sbo-002",
      "slug": "spring-boot-startup-sequence",
      "question": "What are the key phases of the Spring Boot startup sequence when SpringApplication.run() is called?",
      "title": "Spring Boot Startup Sequence (SpringApplication.run Phases)",
      "direct_answer": "SpringApplication.run() orchestrates a multi-phase startup: it prepares the environment, creates and refreshes the ApplicationContext, then calls runners and publishes a final ready event. Each phase is observable via SpringApplicationRunListeners and ApplicationEvents. Understanding this sequence is critical for troubleshooting slow startups and for hooking in custom initialization logic.",
      "layout_type": "lifecycle-timeline",
      "interviewer_intent": {
        "testing": "Whether the candidate can trace the lifecycle beyond 'the context starts' and explain where things like property loading and bean initialization happen.",
        "common_mistake": "Confusing ApplicationRunner/CommandLineRunner (called after context is ready) with @PostConstruct (called during bean initialization inside context refresh).",
        "to_stand_out": "Mention SpringApplicationRunListeners and the exact ApplicationEvent types (ApplicationStartingEvent, ApplicationEnvironmentPreparedEvent, etc.) and where to hook custom logic safely."
      },
      "company_tags": [
        "Spotify",
        "LinkedIn",
        "Zalando",
        "Booking.com"
      ],
      "reading_time_minutes": 5,
      "last_updated": "2025-01-15",
      "difficulty": "intermediate",
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Why Knowing the Startup Sequence Matters More Than Memorizing Event Names",
            "content": "Most developers think of `SpringApplication.run()` as \"it starts the app.\" But this single method orchestrates a multi-phase lifecycle where **the order of operations determines where your custom logic can safely run.** The most common bug from misunderstanding this sequence is putting initialization code in `@PostConstruct` that depends on the full application context being ready \u2014 it isn't. `@PostConstruct` runs during bean initialization inside context refresh, before runners, before the server port is bound, and before `ApplicationReadyEvent` fires.\n\nThe startup sequence is observable through `SpringApplicationRunListeners`, which broadcast typed events at each phase boundary. These aren't just debugging aids \u2014 they're the hook points where you can inject custom behavior: registering metrics before the context exists, modifying the environment before property binding, or running migration scripts after the context is fully wired but before the app accepts traffic.\n\nUnderstanding the sequence also explains why startup is slow. Each phase has a measurable cost, and Spring Boot 3.x added startup tracking via `ApplicationStartup` to let you pinpoint exactly which phase is consuming time."
          },
          {
            "type": "phase",
            "title": "Environment and Context Creation \u2014 From System Properties to a Wired ApplicationContext",
            "content": "The first thing `SpringApplication.run()` does is create a `DefaultBootstrapContext` and notify `SpringApplicationRunListeners` with an `ApplicationStartingEvent`. At this point, **no beans exist, no properties are loaded, and no ApplicationContext has been created.** The only things running are listeners registered via `spring.factories` \u2014 this is where logging framework initialization happens.\n\nNext comes environment preparation. Spring builds the `Environment` object by layering property sources in a specific precedence order: system properties override `application.yml`, which overrides `application.properties`, which overrides defaults. **`ApplicationEnvironmentPreparedEvent`** fires here \u2014 it's the last safe point to programmatically add or modify property sources before anything reads them. If you need to inject secrets from a vault, this is where you do it.\n\nWith the environment ready, Spring creates the `ApplicationContext` \u2014 `AnnotationConfigServletWebServerApplicationContext` for web apps, `AnnotationConfigApplicationContext` for non-web. The context type is inferred from the classpath (presence of `Servlet` API vs. reactive `WebFlux`). The context is created but **not yet refreshed** \u2014 no bean definitions have been processed."
          },
          {
            "type": "phase",
            "title": "Context Refresh \u2014 Where Bean Instantiation, @PostConstruct, and Auto-Configuration All Happen",
            "content": "Context refresh is the heaviest phase and where most startup time is spent. `ApplicationPreparedEvent` fires just before refresh begins \u2014 the context exists, the main configuration class is registered, but no beans have been instantiated yet.\n\nDuring refresh, Spring processes all `@Configuration` classes (including auto-configurations filtered by `@Conditional` annotations), creates bean definitions, and then instantiates singletons. **This is when `@PostConstruct` methods run** \u2014 during individual bean initialization, inside the refresh cycle. A common mistake is assuming `@PostConstruct` has access to the complete application context. It doesn't \u2014 other singletons may not have been created yet, and the web server hasn't started.\n\nFor web applications, context refresh also creates and starts the embedded server. The server binds to its port during refresh, which is why a port conflict causes `ApplicationContext` refresh failure, not a separate error. `ContextRefreshedEvent` fires when refresh completes \u2014 all singleton beans are instantiated, all `@PostConstruct` methods have run, and the server is listening.\n\nThe critical insight for interviews: **everything inside refresh happens before the application is considered \"started.\"** If your initialization depends on all beans being ready, refresh is too early. You need the next phase."
          },
          {
            "type": "phase",
            "title": "Runners and Ready \u2014 The Safe Zone for Application-Level Initialization",
            "content": "After context refresh completes, Boot fires `ApplicationStartedEvent` and calls all `CommandLineRunner` and `ApplicationRunner` beans. This is the first point where **every singleton bean is guaranteed to exist, every `@PostConstruct` has completed, and the server is bound to its port.** Runners are the correct place for one-time initialization tasks: loading caches, running data migrations, or verifying external service connectivity.\n\nThe difference between the two runner types is small but matters. `CommandLineRunner` receives raw `String[] args`, while `ApplicationRunner` receives an `ApplicationArguments` object that parses option arguments (like `--server.port=8081`) from non-option arguments. Use `@Order` to control runner execution sequence when you have multiple.\n\nFinally, `ApplicationReadyEvent` fires \u2014 this is the signal that the application is fully initialized and ready to accept requests. Health check endpoints like `/actuator/health` should return UP only after this event. In Kubernetes, this maps to the **readiness probe** \u2014 your pod should not receive traffic until `ApplicationReadyEvent` has fired.\n\nIf any runner throws an exception, the startup fails and `ApplicationFailedEvent` fires instead of `ApplicationReadyEvent`. This is the clean distinction: `ApplicationStartedEvent` means \"context is refreshed,\" `ApplicationReadyEvent` means \"everything succeeded, accept traffic.\""
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **`@PostConstruct` fires before runners; runners fire after the context is fully ready** \u2014 the order is: instantiate beans \u2192 inject \u2192 `@PostConstruct` \u2192 `ApplicationStartedEvent` \u2192 runners \u2192 `ApplicationReadyEvent`.\n- **`CommandLineRunner` and `ApplicationRunner` execute after all beans are created** \u2014 safe to call any injected service; `@PostConstruct` is not safe for logic that depends on the full context being operational.\n- **Multiple runners use `@Order` or `Ordered`** \u2014 lower value = higher priority; unordered runners execute in registration order.\n- **`SpringApplicationRunListeners` vs `ApplicationListener`** \u2014 `SpringApplicationRunListeners` fire before the `ApplicationContext` is created (too early for `@Bean`); `ApplicationListener` or `@EventListener` fires after.\n- **`ApplicationFailedEvent` fires if startup fails** \u2014 use it to emit metrics or alerts before the JVM exits; not fired on graceful shutdown, only on exception during startup."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "Spring Boot's startup follows a precise sequence that determines when each hook is safe to use. Getting the order wrong means initialization code runs before its dependencies are ready.\n\n**The sequence**\n- Spring creates and injects all beans, then calls `@PostConstruct` methods \u2014 dependencies are available but the web server has not started yet\n- `ApplicationStartedEvent` fires after the context is refreshed and the embedded server is started\n- `CommandLineRunner` and `ApplicationRunner` beans execute next \u2014 this is the last point before `ApplicationReadyEvent` marks the application as ready for traffic\n- `ApplicationReadyEvent` fires when the application is fully started and health checks are passing\n\n**What goes where**\n- `@PostConstruct`: initialization that depends only on injected collaborators \u2014 cache warmup, connection validation\n- Runners: startup logic that should run after the full context is ready \u2014 data seeding, integration checks, feature flag registration\n- `@EventListener(ApplicationReadyEvent.class)`: same timing as runners but declared as an event listener rather than implementing an interface\n\nMultiple runners execute in `@Order` sequence \u2014 lower value runs first. `CommandLineRunner` receives raw `String[] args`; `ApplicationRunner` receives a typed `ApplicationArguments` object \u2014 the only practical difference between the two."
          }
        ]
      },
      "followup_questions": [
        "Where is the best place to run logic that needs the full application context wired but should complete before the app accepts traffic?",
        "How would you measure which beans are slowest to initialize during startup?",
        "What is the difference between ApplicationStartedEvent and ApplicationReadyEvent?"
      ],
      "seo": {
        "metaTitle": "Spring Boot Startup Sequence Explained | Java Intermediate Interview",
        "metaDescription": "Trace every phase of SpringApplication.run(): environment prep, context refresh, ApplicationRunner, and ApplicationReadyEvent \u2014 with lifecycle diagram for Java intermediate interviews."
      },
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P02",
        "hook": "Spring Boot's startup follows a precise sequence that determines when each hook is safe to use.",
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "Most developers think of SpringApplication.run() as \"it starts the app.\" But this single method orchestrates a multi-phase lifecycle where the order of operations determines where your custom logic can safely run. The most common bug from misunderstanding this sequence is putting initialization code in @PostConstruct that depends on the full application context being ready \u2014 it isn't. @PostConstruct runs during bean initialization inside context refresh, before runners, before the server port is bound, and before ApplicationReadyEvent fires. The startup sequence."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "Whether the candidate can trace the lifecycle beyond 'the context starts' and explain where things like property loading and bean initialization happen."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "@PostConstruct fires before runners; runners fire after the context is fully ready \u2014 the order is: instantiate beans \u2192 inject \u2192 @PostConstruct \u2192 ApplicationStartedEvent \u2192 runners \u2192 ApplicationReadyEvent",
              "CommandLineRunner and ApplicationRunner execute after all beans are created \u2014 safe to call any injected service; @PostConstruct is not safe for logic that depends on the full context being operational",
              "Multiple runners use @Order or Ordered \u2014 lower value = higher priority; unordered runners execute in registration order",
              "SpringApplicationRunListeners vs ApplicationListener \u2014 SpringApplicationRunListeners fire before the ApplicationContext is created (too early for @Bean); ApplicationListener or @EventListener fires after",
              "ApplicationFailedEvent fires if startup fails \u2014 use it to emit metrics or alerts before the JVM exits; not fired on graceful shutdown, only on exception during startup"
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "Most developers think of SpringApplication.run() as \"it starts the app.\" But this single method orchestrates a multi-phase lifecycle where the order of operations determines where your custom logic can safely run. The most common bug from misunderstanding this sequence is putting initialization code in @PostConstruct that depends on the full application context being ready \u2014 it isn't. @PostConstruct runs during bean initialization inside context refresh, before runners, before the server port is bound, and before ApplicationReadyEvent fires. The startup sequence is observable through SpringApplicationRunListeners, which broadcast typed events at each phase boundary. These aren't just debugging aids \u2014 they're the hook points where you can inject custom behavior: registering metrics before the context exists, modifying the environment before property binding."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Confusing ApplicationRunner/CommandLineRunner (called after context is ready) with @PostConstruct (called during bean initialization inside context refresh)"
            ]
          }
        ],
        "cap": "CommandLineRunner receives raw String[] args; ApplicationRunner receives a typed ApplicationArguments object \u2014 the only practical difference between the two.",
        "familiarity_anchors": [
          "SpringApplication",
          "ApplicationContext",
          "SpringApplicationRunListeners"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": [
          "Where is the best place to run logic that needs the full application context wired but should complete before the app accepts traffic?",
          "How would you measure which beans are slowest to initialize during startup?",
          "What is the difference between ApplicationStartedEvent and ApplicationReadyEvent?"
        ]
      }
    },
    {
      "id": "sbo-003",
      "slug": "spring-boot-application-annotation",
      "question": "What does @SpringBootApplication do and what three annotations does it combine?",
      "title": "@SpringBootApplication \u2014 What It Combines",
      "direct_answer": "@SpringBootApplication is a convenience meta-annotation that combines @SpringBootConfiguration (a specialised @Configuration), @EnableAutoConfiguration (triggers auto-configuration scanning), and @ComponentScan (scans the package of the annotated class and its sub-packages for Spring components). Placing it on your main class is equivalent to annotating it with all three separately.",
      "layout_type": "concept-explainer",
      "interviewer_intent": {
        "testing": "Whether the candidate understands component scanning scope and can explain why placing the main class in the root package matters.",
        "common_mistake": "Placing the main class inside a sub-package so that sibling packages are missed by @ComponentScan, causing beans not to be found.",
        "to_stand_out": "Explain that you can narrow or widen the scan with scanBasePackages/scanBasePackageClasses, and that @SpringBootConfiguration differs from @Configuration because only one is allowed per application context."
      },
      "company_tags": [
        "Amazon",
        "Adobe",
        "Infosys",
        "Capgemini"
      ],
      "reading_time_minutes": 4,
      "last_updated": "2025-01-15",
      "difficulty": "intermediate",
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Three Annotations in a Trenchcoat \u2014 And Why Package Placement Is the Hidden Gotcha",
            "content": "`@SpringBootApplication` is a meta-annotation that combines three annotations into one. Knowing the three names is the easy part \u2014 understanding **what each one actually triggers and the subtle constraint that catches most teams** is what interviewers are really testing.\n\nThe three annotations are `@SpringBootConfiguration` (a specialized `@Configuration` that marks this class as the application's primary configuration source), `@EnableAutoConfiguration` (triggers the auto-configuration pipeline that scans `AutoConfiguration.imports` for conditional configuration classes), and `@ComponentScan` (scans the package of the annotated class and all sub-packages for `@Component`, `@Service`, `@Repository`, and `@Controller` beans).\n\nThe critical detail is `@ComponentScan`'s default behavior. It scans **from the package of the annotated class downward.** If your main class lives in `com.example.app.config` and your controllers are in `com.example.app.web`, both get scanned. But if your main class is in `com.example.app.config` and your service is in `com.example.service`, that service package is a sibling, not a child \u2014 **it will never be found.** This is the most common reason for \"bean not found\" errors in new Spring Boot projects."
          },
          {
            "type": "step",
            "title": "What Each Annotation Controls and How to Override the Defaults",
            "content": "**`@SpringBootConfiguration`** differs from plain `@Configuration` in one important way: Spring Boot enforces that only one `@SpringBootConfiguration` exists per application context. This prevents accidental duplicate context creation in tests. When `@SpringBootTest` searches for the application's configuration, it walks up the package hierarchy looking for exactly one `@SpringBootConfiguration` \u2014 if you have two, the test fails with an ambiguous configuration error.\n\n**`@EnableAutoConfiguration`** is the trigger for Boot's auto-configuration pipeline. You can selectively disable auto-configurations using the `exclude` attribute: `@SpringBootApplication(exclude = DataSourceAutoConfiguration.class)` removes database auto-configuration entirely. This is cleaner than adding dummy beans to satisfy conditions.\n\n**`@ComponentScan`** is the one you'll customize most. When your package structure doesn't fit the \"main class at the root\" convention, use `scanBasePackages` to explicitly declare which packages to scan. The `scanBasePackageClasses` variant accepts a class reference instead of a string \u2014 this survives refactoring because the compiler catches renamed packages.\n\nA typical override looks like `@SpringBootApplication(scanBasePackages = {\"com.example.core\", \"com.example.web\"})`. This is safer than moving your main class to the root package, especially in multi-module projects where the root package might be too broad and accidentally scan transitive dependencies."
          },
          {
            "type": "when_to_use",
            "title": "When to Override Scan Defaults \u2014 Multi-Module Projects and the Test Discovery Rule",
            "content": "**In single-module projects**, the convention is simple: place the main class in the root package (`com.example.app`) and let `@ComponentScan` discover everything beneath it. This is the path of least resistance, and any deviation requires explicit configuration.\n\n**In multi-module projects**, the root-package assumption breaks quickly. A common setup has a `core` module (`com.example.core`) and a `web` module (`com.example.web`) where the web module depends on core. If the main class lives in `com.example.web`, beans from `com.example.core` are in a sibling package \u2014 they will not be scanned. The fix is either `scanBasePackages = {\"com.example\"}` (scanning the shared parent) or `@Import(CoreConfiguration.class)` in the main class. The `@Import` approach is more surgical: you explicitly declare which configurations you depend on rather than accidentally scanning every class in the parent package.\n\n**The test discovery rule** trips up teams regularly. When you run `@SpringBootTest`, Spring walks up the package tree from the test class to find the nearest `@SpringBootConfiguration`. If the test is in `com.example.web.controller` and the main class is in `com.example.web`, it's found correctly. But if you've placed the main class in a different package hierarchy, `@SpringBootTest` fails with `Unable to find a @SpringBootConfiguration`. The safest rule: keep test classes in a package that is the same as or a sub-package of the application's root package, or use `@SpringBootTest(classes = Application.class)` to point directly at the configuration class \u2014 bypassing the auto-discovery entirely.\n\nFinally, the `scanBasePackageClasses` attribute is strictly better than `scanBasePackages` for refactoring safety. Instead of `scanBasePackages = {\"com.example.core\"}`, write `scanBasePackageClasses = {CoreMarker.class}` where `CoreMarker` is a placeholder interface in the `com.example.core` package. When the package is renamed, the compiler catches the reference \u2014 a string-based package name silently becomes wrong after a rename with no startup error."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **`@SpringBootApplication` = `@Configuration` + `@EnableAutoConfiguration` + `@ComponentScan`** \u2014 decompose it when you only need part of the behavior.\n- **Main class package determines the default scan boundary** \u2014 classes in `com.example.common` are invisible if the main class is in `com.example.app`; move the main class to the root package or use `scanBasePackages`.\n- **`scanBasePackageClasses` is refactoring-safe; `scanBasePackages` (strings) is not** \u2014 renaming a package breaks the string reference silently; referencing a marker class fails at compile time.\n- **`exclude = {DataSourceAutoConfiguration.class}` prevents unwanted auto-config** \u2014 cleaner than `spring.autoconfigure.exclude` property because it is type-safe and visible at the annotation site.\n- **Do not place `@SpringBootApplication` inside a sub-package** \u2014 the scan covers the annotated class's package and all sub-packages; a main class in `com.example.app.main` will not scan `com.example.app.service` if it is a sibling, not a descendant."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "`@SpringBootApplication` is a single annotation that composes three: `@Configuration` (this class is a bean source), `@EnableAutoConfiguration` (activate Spring Boot's auto-configuration), and `@ComponentScan` (scan from this class's package downward).\n\n**The scan boundary rule**\n- The component scan starts from the package containing the `@SpringBootApplication` class\n- Place the main class in the root package (`com.example`) so every sub-package (`com.example.service`, `com.example.repository`) is automatically discovered\n- A main class buried inside a sub-package means sibling packages are outside the scan boundary \u2014 beans there are invisible\n\n**Customizing the scan**\n- `@SpringBootApplication(scanBasePackages = \"com.example\")` extends the boundary, but string-based \u2014 renaming the package breaks it silently\n- `scanBasePackageClasses = {ServiceMarker.class}` is refactoring-safe \u2014 a compile error tells you immediately when the marker class moves\n\n**Excluding auto-configurations**\n- `@SpringBootApplication(exclude = {DataSourceAutoConfiguration.class})` prevents that auto-config from running \u2014 useful when a starter pulls in database config you do not want\n- Type-safe and visible at the annotation site; prefer this over the string-based property approach"
          }
        ]
      },
      "followup_questions": [
        "What happens if you have two classes annotated with @SpringBootConfiguration in the same context?",
        "How would you configure component scanning to include a package outside the main application package?",
        "Can you use @SpringBootApplication on a class that is not the entry point main class?"
      ],
      "seo": {
        "metaTitle": "@SpringBootApplication Explained | Java Intermediate Interview",
        "metaDescription": "Learn what @SpringBootApplication combines \u2014 @SpringBootConfiguration, @EnableAutoConfiguration, and @ComponentScan \u2014 and why it matters for component scanning scope."
      },
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P02",
        "hook": "@SpringBootApplication is a single annotation that composes three: @Configuration (this class is a bean source), @EnableAutoConfiguration (activate Spring Boot's auto-configuration), and @ComponentScan (scan from this class's package downward).",
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "@SpringBootApplication is a meta-annotation that combines three annotations into one. Knowing the three names is the easy part \u2014 understanding what each one actually triggers and the subtle constraint that catches most teams is what interviewers are really testing. The three annotations are @SpringBootConfiguration (a specialized @Configuration that marks this class as the application's primary configuration source), @EnableAutoConfiguration (triggers the auto-configuration pipeline that scans AutoConfiguration.imports for conditional configuration classes), and @ComponentScan (scans the package of the annotated class and all."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "Whether the candidate understands component scanning scope and can explain why placing the main class in the root package matters."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "@SpringBootApplication = @Configuration + @EnableAutoConfiguration + @ComponentScan \u2014 decompose it when you only need part of the behavior",
              "Main class package determines the default scan boundary \u2014 classes in com",
              "common are invisible if the main class is in com",
              "app; move the main class to the root package or use scanBasePackages",
              "scanBasePackageClasses is refactoring-safe; scanBasePackages (strings) is not \u2014 renaming a package breaks the string reference silently; referencing a marker class fails at compile time"
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "@SpringBootApplication is a meta-annotation that combines three annotations into one. Knowing the three names is the easy part \u2014 understanding what each one actually triggers and the subtle constraint that catches most teams is what interviewers are really testing. The three annotations are @SpringBootConfiguration (a specialized @Configuration that marks this class as the application's primary configuration source), @EnableAutoConfiguration (triggers the auto-configuration pipeline that scans AutoConfiguration.imports for conditional configuration classes), and @ComponentScan (scans the package of the annotated class and all sub-packages for @Component, @Service, @Repository, and @Controller beans). The critical detail is @ComponentScan's default behavior. It scans from the package of the annotated class downward. If your main class lives in com.example.app.config and your controllers are in com.example.app.web, both get."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Placing the main class inside a sub-package so that sibling packages are missed by @ComponentScan, causing beans not to be found"
            ]
          }
        ],
        "cap": "The scan boundary rule The component scan starts from the package containing the @SpringBootApplication class Place the main class in the root package (com.example) so every sub-package (com.example.s",
        "familiarity_anchors": [
          "SpringBootApplication",
          "SpringBootConfiguration",
          "Configuration"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": [
          "What happens if you have two classes annotated with @SpringBootConfiguration in the same context?",
          "How would you configure component scanning to include a package outside the main application package?",
          "Can you use @SpringBootApplication on a class that is not the entry point main class?"
        ]
      }
    },
    {
      "id": "sbo-009",
      "slug": "custom-auto-configuration-spring-factories",
      "question": "How do you write and register a custom Spring Boot auto-configuration?",
      "title": "Custom Auto-Configuration (spring.factories / AutoConfiguration.imports)",
      "direct_answer": "Create a @AutoConfiguration class with @ConditionalOn* guards, implement your @Bean methods, then register the class in META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports (Spring Boot 2.7+) so Spring Boot discovers it at startup. This pattern is used by every Spring Boot starter to provide zero-configuration library integration.",
      "layout_type": "recipe-builder",
      "interviewer_intent": {
        "testing": "Whether the candidate understands the registration mechanism and knows how to write safe conditions so the auto-configuration backs off correctly.",
        "common_mistake": "Forgetting to add the registration file, so the @AutoConfiguration class is never discovered, or using @Configuration instead of @AutoConfiguration which bypasses ordering guarantees.",
        "to_stand_out": "Describe the full starter module layout (autoconfigure JAR + starter POM), explain how @AutoConfigureAfter/@AutoConfigureBefore work, and show how spring-boot-configuration-processor generates IDE metadata from @ConfigurationProperties."
      },
      "company_tags": [
        "Pivotal",
        "Red Hat",
        "Netflix",
        "Zalando"
      ],
      "reading_time_minutes": 6,
      "last_updated": "2025-01-15",
      "difficulty": "intermediate",
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "The Registration File Most Developers Forget \u2014 And Why Their Auto-Configuration Silently Does Nothing",
            "content": "The number one mistake when writing custom auto-configuration is forgetting the registration file. You create a perfectly valid `@AutoConfiguration` class with all the right conditions, but Spring Boot never discovers it because **auto-configuration classes are not found by component scanning.** They must be explicitly listed in `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` (Boot 2.7+) or `META-INF/spring.factories` (pre-2.7).\n\nThis is intentional. Auto-configuration classes live in shared libraries \u2014 JARs consumed by other applications. Component scanning would only find them if the consumer happened to scan the right package, making the behavior dependent on the consumer's package structure. The registration file makes discovery explicit and package-independent.\n\nThe standard pattern for production library auto-configuration is a **two-module layout**: an `autoconfigure` module containing the `@AutoConfiguration` class, its `@ConfigurationProperties` POJO, and the registration file; and a `starter` module that depends on the autoconfigure module plus all required transitive dependencies. **The starter POM has no Java code** \u2014 it's a dependency aggregator. Consumers add a single `<dependency>` to their POM, and the transitive dependency brings in the autoconfigure module where the actual logic lives.\n\nThis separation matters because it lets consumers who need fine-grained control depend only on the `autoconfigure` module directly, bypassing the starter's default transitive dependencies. Spring's own starters follow this exact pattern \u2014 `spring-boot-starter-data-jpa` is just a POM that pulls in `spring-boot-autoconfigure` (among others). Understanding this structure is what interviewers test when they ask you to design a reusable library: you should immediately reach for this two-module layout rather than bundling configuration inside the same module as the library API."
          },
          {
            "type": "step",
            "title": "Writing the Auto-Configuration Class \u2014 @AutoConfiguration, Conditions, and Ordering",
            "content": "In Spring Boot 3.x, use `@AutoConfiguration` instead of `@Configuration`. This dedicated annotation signals Boot's infrastructure that this class participates in auto-configuration ordering, and it supports `after` and `before` attributes directly.\n\nEvery auto-configuration class should be guarded by at least one condition. The minimum is `@ConditionalOnClass` \u2014 which prevents the class from activating when its required dependency isn't on the classpath. Without this guard, your auto-configuration throws `NoClassDefFoundError` in applications that don't use your library.\n\n**The most important condition is `@ConditionalOnMissingBean`** on each `@Bean` method. This allows consumers to define their own bean and have your auto-configured one back off. Without it, consumers get `BeanDefinitionOverrideException` when they try to customize behavior.\n\n```java\n@AutoConfiguration(after = DataSourceAutoConfiguration.class)\n@ConditionalOnClass(MetricsService.class)\n@EnableConfigurationProperties(MetricsProperties.class)\npublic class MetricsAutoConfiguration {\n\n    @Bean\n    @ConditionalOnMissingBean\n    public MetricsService metricsService(MetricsProperties props) {\n        return new MetricsService(props.getEndpoint(), props.getInterval());\n    }\n}\n```\n\nThe `@AutoConfiguration(after = DataSourceAutoConfiguration.class)` ensures this class runs after the DataSource is available. Getting the ordering wrong means your bean definition references a bean that doesn't exist yet, causing a startup failure.\n\nA subtle ordering issue appears when two auto-configuration classes have circular after/before declarations \u2014 Spring detects this and throws `IllegalStateException: @AutoConfiguration after/before cycle detected` at startup. The fix is to declare ordering only where there's a genuine dependency, not defensively.\n\nFor `@ConfigurationProperties` classes paired with auto-configuration, add `@EnableConfigurationProperties(MetricsProperties.class)` to the auto-configuration class rather than relying on `@ConfigurationPropertiesScan`. This makes the properties binding explicit and contained \u2014 consumers can see exactly which properties your auto-configuration activates without inspecting the classpath scan results."
          },
          {
            "type": "step",
            "title": "The Registration File and Testing \u2014 Proving Your Auto-Configuration Actually Works",
            "content": "Create the registration file at exactly `src/main/resources/META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` with one fully-qualified class name per line. No comments, no blank lines at the end \u2014 just the class name.\n\n```\ncom.example.metrics.MetricsAutoConfiguration\n```\n\n**Testing is where most tutorials stop and most production issues start.** Use `ApplicationContextRunner` to test your auto-configuration in isolation without starting a full application. It lets you simulate different classpath and property combinations.\n\n```java\nprivate final ApplicationContextRunner runner = new ApplicationContextRunner()\n    .withConfiguration(AutoConfigurations.of(MetricsAutoConfiguration.class));\n\n@Test\nvoid createsServiceWhenClassPresent() {\n    runner.withPropertyValues(\"metrics.endpoint=http://localhost:9090\")\n        .run(ctx -> assertThat(ctx).hasSingleBean(MetricsService.class));\n}\n\n@Test\nvoid backsOffWhenUserDefinesOwnBean() {\n    runner.withBean(MetricsService.class, () -> new MetricsService(\"custom\", 5))\n        .run(ctx -> assertThat(ctx.getBean(MetricsService.class)\n            .getEndpoint()).isEqualTo(\"custom\"));\n}\n```\n\nThe second test is critical \u2014 it proves `@ConditionalOnMissingBean` works correctly. Without this test, a consumer who defines their own `MetricsService` discovers the conflict only at their application startup, not during your library's CI. Writing this test is the contract you make with your library's consumers: \"you can always override what we provide.\"\n\nA third test you should always include is the negative case \u2014 verifying that the auto-configuration does nothing when required classes are absent. Use `withClassLoader(new FilteredClassLoader(MetricsService.class))` on the `ApplicationContextRunner` to simulate the class not being on the classpath. This proves `@ConditionalOnClass` is wired to the right class and that the auto-configuration truly does not activate for consumers who don't have your library.\n\nFor **IDE metadata**, add `spring-boot-configuration-processor` as an annotation processor in your autoconfigure module's `pom.xml`. It generates `META-INF/spring-configuration-metadata.json` at compile time from your `@ConfigurationProperties` class. This file powers IDE autocomplete \u2014 consumers typing `metrics.` in `application.yml` see `endpoint` and `interval` suggested with their types and your Javadoc descriptions. Without this file, consumer IDEs show yellow \"unknown property\" warnings for every property your library defines."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Auto-configuration class must be registered to be discovered** \u2014 Spring Boot 2.7+: list in `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports`; pre-2.7: list in `META-INF/spring.factories` under `EnableAutoConfiguration`.\n- **Starter layout: two modules** \u2014 `my-feature-autoconfigure` (contains `@AutoConfiguration` + `@Conditional` logic); `my-feature-spring-boot-starter` (POM-only, depends on autoconfigure + feature library).\n- **User beans always win \u2014 put `@ConditionalOnMissingBean` on every auto-configured bean** \u2014 this lets users override any default by defining their own bean of that type.\n- **`@AutoConfiguration(after = DataSourceAutoConfiguration.class)` controls ordering** \u2014 if your auto-config depends on a `DataSource` bean existing first, declare the dependency explicitly.\n- **Test auto-configuration with `ApplicationContextRunner`** \u2014 creates a minimal application context without starting Spring Boot; call `.withConfiguration(AutoConfigurations.of(YourAutoConfig.class))` and assert which beans are registered."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "Writing a custom auto-configuration is how you package reusable Spring Boot behavior into a library that others use by simply adding the dependency \u2014 no manual bean registration required.\n\n**The registration file**\n- Spring Boot 2.7+: create `META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports` listing the fully-qualified class name of your `@AutoConfiguration` class\n- Without this file, Spring Boot never discovers your class \u2014 the most common mistake when writing the first custom auto-configuration\n\n**Starter module layout**\n- `my-feature-autoconfigure` JAR: contains the `@AutoConfiguration` class with `@Conditional` logic and bean definitions\n- `my-feature-spring-boot-starter` POM: depends on `autoconfigure` + the feature library; users add only the starter dependency and get everything\n\n**The override contract**\n- Every auto-configured bean must have `@ConditionalOnMissingBean` \u2014 if the user defines their own bean of that type, yours is skipped\n- Use `@AutoConfiguration(after = DataSourceAutoConfiguration.class)` when your beans depend on a `DataSource` being available\n\n**Testing**\n- `ApplicationContextRunner` is the standard testing tool \u2014 creates a minimal context, applies your auto-configuration, and lets you assert which beans registered\n- No need to start the full application; tests run in milliseconds"
          }
        ]
      },
      "followup_questions": [
        "What is the difference between a Spring Boot starter and the autoconfigure module?",
        "How do you test that your auto-configuration backs off correctly when the user defines their own bean?",
        "How do @AutoConfigureAfter and @AutoConfigureBefore prevent ordering problems between auto-configurations?"
      ],
      "seo": {
        "metaTitle": "Custom Spring Boot Auto-Configuration Guide | Java Intermediate Interview",
        "metaDescription": "Step-by-step guide to writing and registering custom Spring Boot auto-configuration with @AutoConfiguration, @ConditionalOn*, AutoConfiguration.imports, and ApplicationContextRunner tests."
      },
      "speakable_v2": {
        "archetype": "C",
        "pillar": "P02",
        "hook": "Writing a custom auto-configuration is how you package reusable Spring Boot behavior into a library that others use by simply adding the dependency \u2014 no manual bean registration required.",
        "beats": [
          {
            "kind": "mental_model",
            "layout": "paragraph",
            "text": "The number one mistake when writing custom auto-configuration is forgetting the registration file. You create a perfectly valid @AutoConfiguration class with all the right conditions, but Spring Boot never discovers it because auto-configuration classes are not found by component scanning. They must be explicitly listed in META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports (Boot 2.7+) or META-INF/spring.factories (pre-2.7). This is intentional. Auto-configuration classes live in shared libraries \u2014 JARs consumed by other applications. Component scanning would only find them if the consumer happened to scan the right package, making the behavior dependent on the consumer's package."
          },
          {
            "kind": "mechanism",
            "layout": "ordered_list",
            "steps": [
              "In Spring Boot 3.x, use @AutoConfiguration instead of @Configuration.",
              "This dedicated annotation signals Boot's infrastructure that this class participates in auto-configuration ordering, and it supports after and before attributes directly.",
              "Every auto-configuration class should be guarded by at least one condition.",
              "The minimum is @ConditionalOnClass \u2014 which prevents the class from activating when its required dependency isn't on the classpath.",
              "Without this guard, your auto-configuration throws NoClassDefFoundError in applications that don't use your library.",
              "The most important condition is @ConditionalOnMissingBean on each @Bean method."
            ]
          },
          {
            "kind": "edge_cases",
            "layout": "bullets",
            "items": [
              "Auto-configuration class must be registered to be discovered \u2014 Spring Boot 2",
              "7+: list in META-INF/spring/org",
              "springframework",
              "autoconfigure",
              "AutoConfiguration"
            ]
          },
          {
            "kind": "failure_mode",
            "layout": "callout",
            "text": "Forgetting to add the registration file, so the @AutoConfiguration class is never discovered, or using @Configuration instead of @AutoConfiguration which bypasses ordering guarantees."
          }
        ],
        "cap": "The registration file Spring Boot 2.7+: create META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports listing the fully-qualified class name of your @AutoConfiguration class",
        "familiarity_anchors": [
          "Create",
          "AutoConfiguration",
          "ConditionalOn"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": [
          "What is the difference between a Spring Boot starter and the autoconfigure module?",
          "How do you test that your auto-configuration backs off correctly when the user defines their own bean?",
          "How do @AutoConfigureAfter and @AutoConfigureBefore prevent ordering problems between auto-configurations?"
        ]
      }
    },
    {
      "id": "sbo-banner-startup",
      "slug": "spring-boot-startup-customization",
      "question": "How do you customize Spring Boot startup \u2014 banners, listeners, and FailureAnalyzer?",
      "title": "Customizing Spring Boot Startup \u2014 Banner, Listeners, and FailureAnalyzer",
      "direct_answer": "Spring Boot's startup is customizable at three levels: the banner (visual branding via banner.txt or a Banner bean), application listeners (reacting to lifecycle events before the context exists), and FailureAnalyzer (converting cryptic startup exceptions into human-readable messages with fix suggestions). FailureAnalyzer is the most production-relevant \u2014 it turns a 200-line stack trace into a clear message like 'Port 8080 is already in use.'",
      "layout_type": "default",
      "difficulty": "intermediate",
      "reading_time_minutes": 5,
      "last_updated": "2026-04-12",
      "interviewer_intent": {
        "testing": "Whether the candidate knows about startup hooks beyond basic configuration and can explain FailureAnalyzer.",
        "common_mistake": "Confusing ApplicationListener with @EventListener \u2014 ApplicationListener registered via spring.factories works before the context exists, while @EventListener requires a bean and only works after context creation.",
        "to_stand_out": "Explain writing a custom FailureAnalyzer to provide team-specific error messages for common deployment issues like missing environment variables."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Beyond Configuration \u2014 The Three Startup Hooks Most Developers Never Use",
            "content": "Most Spring Boot developers know `application.yml` and `@SpringBootApplication`. But the startup sequence has three customization points that solve real problems: **banner customization** for identifying which version is running in logs, **application listeners** for reacting to lifecycle events before the ApplicationContext exists, and **FailureAnalyzer** for turning cryptic startup exceptions into human-readable messages.\n\nOf these three, `FailureAnalyzer` is the most production-relevant but least known. When Spring Boot fails to start, the default behavior is a 200-line stack trace that buries the actual cause. FailureAnalyzers intercept specific exceptions and produce clear messages like \"Port 8080 is already in use\" with a suggested action. **You can write custom FailureAnalyzers** for your team's common deployment issues \u2014 missing environment variables, incompatible database schemas, or unreachable configuration servers.\n\nThe key distinction with listeners is that `ApplicationListener` registered via `spring.factories` works **before the ApplicationContext exists** \u2014 unlike `@EventListener`, which requires a bean in the context. This makes listeners the only way to react to early startup events like `ApplicationEnvironmentPreparedEvent`.\n\nThe most production-relevant of these three mechanisms is `FailureAnalyzer`. When your application fails to start, every minute a developer spends decoding a stack trace is a minute of deployment delay. Standard Spring Boot FailureAnalyzers already handle port conflicts, missing beans, and JPA configuration errors. **Writing custom FailureAnalyzers for your team's common deployment issues** \u2014 missing environment variables, incompatible database schema versions, misconfigured API keys \u2014 can save hours of debugging time across a large team."
          },
          {
            "type": "step",
            "title": "Writing a Custom FailureAnalyzer \u2014 Clear Error Messages for Your Team",
            "content": "A `FailureAnalyzer` converts a specific exception type into a `FailureAnalysis` containing a description, action, and optional cause. Spring Boot ships analyzers for port conflicts, missing beans, and database connection failures. Writing your own takes three steps.\n\n```java\npublic class MissingApiKeyAnalyzer extends AbstractFailureAnalyzer<ApiKeyMissingException> {\n    @Override\n    protected FailureAnalysis analyze(Throwable rootFailure, ApiKeyMissingException cause) {\n        return new FailureAnalysis(\n            \"Required API key 'payment.gateway.key' is not configured.\",\n            \"Set PAYMENT_GATEWAY_KEY environment variable or add payment.gateway.key to application.yml\",\n            cause\n        );\n    }\n}\n```\n\nRegister it in `META-INF/spring.factories` (or the Boot 3.x equivalent). When `ApiKeyMissingException` is thrown during startup, instead of a stack trace, the developer sees a formatted message with the exact property name and how to set it. **This is especially valuable for onboarding** \u2014 new team members get actionable error messages instead of debugging stack traces.\n\nThe `AbstractFailureAnalyzer<T>` generic type determines which exception type this analyzer handles. Spring's exception chain walking finds the cause of type `T` anywhere in the exception chain \u2014 not just the immediate cause. This means your analyzer fires even when the real exception is wrapped in a `BeanCreationException` several layers deep, which is the typical way Spring Boot surfaces startup failures. The `rootFailure` parameter gives you the outermost exception (usually `BeanCreationException`), and `cause` gives you the specific typed exception you declared.\n\nFor Boot 3.x, register `FailureAnalyzer` implementations in `META-INF/spring/org.springframework.boot.diagnostics.FailureAnalyzer` (one class name per line), consistent with how auto-configurations are registered."
          },
          {
            "type": "step",
            "title": "Banners and Listeners \u2014 Quick Customizations That Add Operational Value",
            "content": "The banner \u2014 the ASCII art at startup \u2014 seems trivial but has **operational value in log files.** When searching through aggregated logs from multiple services, seeing the application name and version in the banner immediately identifies which service produced the log.\n\nPlace a `banner.txt` in `src/main/resources` with `${spring-boot.version}` and `${application.title}` placeholders, or implement the `Banner` interface for dynamic content. Use `spring.main.banner-mode=off` to disable it if the team finds it noisy.\n\n**Application listeners** are the pre-context hook. Register them in `spring.factories` under `org.springframework.context.ApplicationListener` to react to events like `ApplicationEnvironmentPreparedEvent` (last chance to modify properties) or `ApplicationFailedEvent` (custom cleanup on startup failure). The critical difference from `@EventListener` is that listeners registered via `spring.factories` are instantiated by Spring directly \u2014 no constructor injection, no dependency on the context. If your listener needs beans, use `@EventListener` on a `@Component` and accept that it only handles events after context creation.\n\nA practical use of `ApplicationEnvironmentPreparedEvent` listeners is **secret injection from a vault**. Before any bean reads the environment (which happens during context refresh), this listener can reach out to HashiCorp Vault or AWS Secrets Manager, fetch the secrets, and add them as a high-precedence property source. This pattern is how Spring Cloud Vault integrates \u2014 it adds a `PropertySource` containing vault secrets that takes priority over `application.yml` but arrives before any bean sees the environment. Without this listener hook, you'd have to bootstrap a separate Vault connection before `SpringApplication.run()`, losing all of Spring's lifecycle management.\n\nFor `ApplicationFailedEvent` listeners, use cases include: alerting a Slack channel when a deployment fails, updating a deployment status dashboard, or cleaning up partially initialized external resources (like a service registry entry that was created before the context failed). These listeners run even when the context close fails \u2014 they're the last resort cleanup mechanism before the JVM exits."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **`ApplicationListener` vs `@EventListener`** \u2014 `ApplicationListener<E>` registered as a bean fires for events after the `ApplicationContext` is created; `SpringApplication.addListeners()` fires for events before the context exists (e.g., `ApplicationStartingEvent`).\n- **`ApplicationContextInitializer` runs before bean refresh** \u2014 modify the `ConfigurableApplicationContext` before any beans are created; used for adding property sources, setting active profiles programmatically.\n- **`EnvironmentPostProcessor` adds properties before the context starts** \u2014 loads external secrets, vault config, or custom property formats before `@Value` injection; registered in `spring.factories`.\n- **`FailureAnalyzer` produces human-readable startup failure messages** \u2014 implement to replace cryptic stack traces with actionable diagnostics; Spring Boot ships 20+ built-in analyzers for common errors.\n- **`Banner` customization is cosmetic \u2014 disable in production** \u2014 `spring.main.banner-mode=off` suppresses it; enables cleaner log output in CI and container environments."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "Spring Boot's startup pipeline has extension points at every phase \u2014 before the context exists, before beans are created, after everything is ready, and when startup fails. Knowing which hook fires when determines which customization goes where.\n\n**Before the context is created**\n- `SpringApplication.addListeners(new MyListener())` registers listeners that fire for early events like `ApplicationStartingEvent` and `ApplicationEnvironmentPreparedEvent`\n- `EnvironmentPostProcessor` \u2014 add property sources from external systems (Vault, AWS Secrets Manager) before `@Value` injection evaluates them\n- `ApplicationContextInitializer` \u2014 set active profiles, add property sources, or modify the context before beans are registered\n\n**After the context is ready**\n- `@EventListener(ApplicationReadyEvent.class)` \u2014 fires when the application is fully started; the same timing as `CommandLineRunner` and `ApplicationRunner`\n- `@EventListener(ApplicationStartedEvent.class)` \u2014 fires after context refresh but before runners\n\n**When startup fails**\n- `FailureAnalyzer` \u2014 implement this interface to produce a human-readable message explaining a startup failure; Spring Boot ships analyzers for port-in-use, missing beans, and failed conditions\n- Your analyzer catches a specific exception type and returns an explanation with a suggested action \u2014 replace stack traces with actionable diagnostics for your team"
          }
        ]
      },
      "followup_questions": [],
      "seo": {
        "metaTitle": "Customizing Spring Boot Startup \u2014 Banner, Listeners, and FailureAnalyzer",
        "metaDescription": ""
      },
      "speakable_v2": {
        "archetype": "C",
        "pillar": "P02",
        "hook": "Spring Boot's startup pipeline has extension points at every phase \u2014 before the context exists, before beans are created, after everything is ready, and when startup fails.",
        "beats": [
          {
            "kind": "mental_model",
            "layout": "paragraph",
            "text": "Most Spring Boot developers know application.yml and @SpringBootApplication. But the startup sequence has three customization points that solve real problems: banner customization for identifying which version is running in logs, application listeners for reacting to lifecycle events before the ApplicationContext exists, and FailureAnalyzer for turning cryptic startup exceptions into human-readable messages. Of these three, FailureAnalyzer is the most production-relevant but least known. When Spring Boot fails to start, the default behavior is a 200-line stack trace that buries the actual cause. FailureAnalyzers intercept specific exceptions and produce clear messages like."
          },
          {
            "kind": "mechanism",
            "layout": "ordered_list",
            "steps": [
              "A FailureAnalyzer converts a specific exception type into a FailureAnalysis containing a description, action, and optional cause.",
              "Spring Boot ships analyzers for port conflicts, missing beans, and database connection failures.",
              "Writing your own takes three steps.",
              "``java public class MissingApiKeyAnalyzer extends AbstractFailureAnalyzer<ApiKeyMissingException> { @Override protected FailureAnalysis analyze(Throwable rootFailure, ApiKeyMissingException cause) { return new FailureAnalysis( \"Required API key 'payment.gateway.key' is not configured.\", \"Set PAYMENT_GATEWAY_KEY environment variable or add payment.gateway.key to application.yml\", cause ); } } ` Register it in META-INF/spring.factories (or the Boot 3.x equivalent).",
              "When ApiKeyMissingException is thrown during startup, instead of a stack trace, the developer sees a formatted message with the exact property name and how to set it.",
              "This is especially valuable for onboarding \u2014 new team members get actionable error messages instead of debugging stack traces."
            ]
          },
          {
            "kind": "edge_cases",
            "layout": "bullets",
            "items": [
              "ApplicationListener vs @EventListener \u2014 ApplicationListener<E> registered as a bean fires for events after the ApplicationContext is created; SpringApplication",
              "addListeners() fires for events before the context exists (e",
              ", ApplicationStartingEvent)",
              "ApplicationContextInitializer runs before bean refresh \u2014 modify the ConfigurableApplicationContext before any beans are created; used for adding property sources, setting active profiles programmatically",
              "EnvironmentPostProcessor adds properties before the context starts \u2014 loads external secrets, vault config, or custom property formats before @Value injection; registered in spring"
            ]
          },
          {
            "kind": "failure_mode",
            "layout": "callout",
            "text": "Confusing ApplicationListener with @EventListener \u2014 ApplicationListener registered via spring.factories works before the context exists, while @EventListener requires a bean and only works after context creation."
          }
        ],
        "cap": "Before the context is created SpringApplication.addListeners(new MyListener()) registers listeners that fire for early events like ApplicationStartingEvent and ApplicationEnvironmentPreparedEvent Envi",
        "familiarity_anchors": [
          "Spring",
          "Banner",
          "FailureAnalyzer"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": []
      }
    }
  ]
}
{
  "topic": "Multi Tenancy",
  "topicSlug": "multi-tenancy",
  "questions": [
    {
      "id": "jpa-multi-datasource",
      "slug": "multi-datasource-spring-boot",
      "question": "How do you configure multiple DataSources in a Spring Boot application with routing?",
      "title": "Multiple DataSources in Spring Boot \u2014 Configuration and Routing",
      "direct_answer": "Spring Boot auto-configures a single DataSource. For multiple databases, manually define each DataSource as a @Bean, create separate EntityManagerFactory and TransactionManager per DataSource, and use @Qualifier to inject the correct one. AbstractRoutingDataSource dynamically routes queries to different databases based on a context key (e.g., tenant ID or read/write split).",
      "layout_type": "default",
      "difficulty": "intermediate",
      "reading_time_minutes": 5,
      "last_updated": "2026-04-12",
      "interviewer_intent": {
        "testing": "Whether the candidate can configure multiple databases and understands transaction manager isolation.",
        "common_mistake": "Forgetting that each DataSource needs its own EntityManagerFactory and TransactionManager \u2014 mixing them causes entities to be managed by the wrong persistence context.",
        "to_stand_out": "Explain AbstractRoutingDataSource for read/write splitting or multi-tenancy, and discuss how @Transactional must specify which transaction manager to use."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "One DataSource Is Auto-Configured \u2014 Two Requires Manual Wiring of Everything",
            "content": "Spring Boot's auto-configuration assumes a single database. Add `spring.datasource.url` and everything works. **The moment you need a second database**, auto-configuration can't decide which DataSource to use for which entities, so you must configure everything manually: two `DataSource` beans, two `LocalContainerEntityManagerFactoryBean` instances (each scanning different entity packages), and two `PlatformTransactionManager` beans.\n\nThe critical mistake is sharing a single `EntityManagerFactory` across both databases. Each `EntityManagerFactory` must know which entities it manages and which `DataSource` to use. If they overlap, entities end up in the wrong persistence context, and queries hit the wrong database.\n\n**`AbstractRoutingDataSource`** is the alternative for dynamic routing \u2014 a single logical DataSource that delegates to different physical databases based on a context key. Use it for **read/write splitting** (route reads to replicas, writes to primary) or **multi-tenancy** (route each tenant to their database). The context key is typically stored in a `ThreadLocal` set by an interceptor or filter."
          },
          {
            "type": "step",
            "title": "AbstractRoutingDataSource for Read/Write Splitting \u2014 The Production Pattern",
            "content": "The routing DataSource determines the target at query time by calling `determineCurrentLookupKey()`. A `ThreadLocal<String>` holds the context \u2014 set to \"primary\" for writes and \"replica\" for reads.\n\n**`@Transactional(readOnly = true)` is the natural split signal.** A custom `AbstractRoutingDataSource` checks the current transaction's read-only flag and routes accordingly. Read-only transactions go to the replica; write transactions go to the primary.\n\nThe timing subtlety: **the DataSource is selected when the connection is acquired** \u2014 at the start of the transaction. If you set the routing key inside the transaction, it's too late. The routing key must be set before `@Transactional` begins, typically in an AOP aspect that runs before the transaction advice.\n\n**Each transaction manager must specify which DataSource it uses.** With `@Transactional(\"primaryTxManager\")`, you explicitly name the manager. Without the qualifier, Spring uses the `@Primary` transaction manager, which can silently route operations to the wrong database."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Each `DataSource` needs its own `EntityManagerFactory` and `TransactionManager`** \u2014 Spring Boot auto-configures these for one datasource; for multiple, define all three beans explicitly per datasource.\n- **Mark one `TransactionManager` as `@Primary`** \u2014 `@Transactional` without a qualifier uses the primary one; use `@Transactional(\"secondaryTransactionManager\")` for the secondary.\n- **`@EnableJpaRepositories(entityManagerFactoryRef, transactionManagerRef, basePackages)`** \u2014 each datasource's repository package must be configured separately to use the correct `EntityManagerFactory`.\n- **`AbstractRoutingDataSource` enables dynamic routing at runtime** \u2014 set the routing key in a thread-local; `determineCurrentLookupKey()` returns which datasource to use; used for read/write splitting and multi-tenancy.\n- **XA transactions (two-phase commit) span multiple datasources atomically** \u2014 requires a JTA transaction manager (Atomikos, Bitronix); significant overhead; prefer eventual consistency with outbox pattern instead."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "Multiple datasources are required when your application reads from multiple databases \u2014 primary/replica separation, microservice-owned databases, or multi-tenant schemas. Spring Boot auto-configuration handles one datasource; you wire multiple manually.\n\n**What you must configure per datasource**\n- `DataSource` bean \u2014 the connection pool (HikariCP)\n- `EntityManagerFactory` bean \u2014 the JPA context for that datasource's entity classes\n- `PlatformTransactionManager` bean \u2014 the transaction manager that manages transactions for that `EntityManagerFactory`\n- `@EnableJpaRepositories(basePackages = \"...\", entityManagerFactoryRef = \"...\", transactionManagerRef = \"...\")` \u2014 routes repository interfaces to the correct infrastructure\n\n**Primary bean designation**\n- `@Primary` on one `DataSource`, one `EntityManagerFactory`, one `TransactionManager`\n- `@Transactional` without a qualifier uses the primary transaction manager\n- `@Transactional(\"replicaTransactionManager\")` explicitly uses the secondary\n\n**`AbstractRoutingDataSource` for dynamic routing**\n- Override `determineCurrentLookupKey()` to return a key from a thread-local\n- Before a read-only `@Transactional(readOnly = true)` method, set key to `REPLICA`; before a write transaction, set to `PRIMARY`\n- Use an `@Aspect` to automate the thread-local based on `@Transactional(readOnly)`"
          }
        ]
      },
      "followup_questions": [],
      "seo": {
        "metaTitle": "Multiple DataSources in Spring Boot \u2014 Configuration and Routing",
        "metaDescription": ""
      },
      "speakable_v2": {
        "archetype": "C",
        "pillar": "P02",
        "hook": "Multiple datasources are required when your application reads from multiple databases \u2014 primary/replica separation, microservice-owned databases, or multi-tenant schemas.",
        "beats": [
          {
            "kind": "mental_model",
            "layout": "paragraph",
            "text": "Spring Boot's auto-configuration assumes a single database. Add spring.datasource.url and everything works. The moment you need a second database, auto-configuration can't decide which DataSource to use for which entities, so you must configure everything manually: two DataSource beans, two LocalContainerEntityManagerFactoryBean instances (each scanning different entity packages), and two PlatformTransactionManager beans. The critical mistake is sharing a single EntityManagerFactory across both databases. Each EntityManagerFactory must know which entities it manages and which DataSource to use. If they overlap, entities end up in the wrong persistence context, and queries hit the wrong."
          },
          {
            "kind": "mechanism",
            "layout": "ordered_list",
            "steps": [
              "The routing DataSource determines the target at query time by calling determineCurrentLookupKey().",
              "A ThreadLocal<String> holds the context \u2014 set to \"primary\" for writes and \"replica\" for reads.",
              "@Transactional(readOnly = true) is the natural split signal.",
              "A custom AbstractRoutingDataSource checks the current transaction's read-only flag and routes accordingly.",
              "Read-only transactions go to the replica; write transactions go to the primary.",
              "The timing subtlety: the DataSource is selected when the connection is acquired \u2014 at the start of the transaction."
            ]
          },
          {
            "kind": "edge_cases",
            "layout": "bullets",
            "items": [
              "Each DataSource needs its own EntityManagerFactory and TransactionManager \u2014 Spring Boot auto-configures these for one datasource; for multiple, define all three beans explicitly per datasource",
              "Mark one TransactionManager as @Primary \u2014 @Transactional without a qualifier uses the primary one; use @Transactional(\"secondaryTransactionManager\") for the secondary",
              "@EnableJpaRepositories(entityManagerFactoryRef, transactionManagerRef, basePackages) \u2014 each datasource's repository package must be configured separately to use the correct EntityManagerFactory",
              "AbstractRoutingDataSource enables dynamic routing at runtime \u2014 set the routing key in a thread-local; determineCurrentLookupKey() returns which datasource to use; used for read/write splitting and multi-tenancy",
              "XA transactions (two-phase commit) span multiple datasources atomically \u2014 requires a JTA transaction manager (Atomikos, Bitronix); significant overhead; prefer eventual consistency with outbox pattern instead"
            ]
          },
          {
            "kind": "failure_mode",
            "layout": "callout",
            "text": "Forgetting that each DataSource needs its own EntityManagerFactory and TransactionManager \u2014 mixing them causes entities to be managed by the wrong persistence context."
          }
        ],
        "cap": "What you must configure per datasource DataSource bean \u2014 the connection pool (HikariCP) EntityManagerFactory bean \u2014 the JPA context for that datasource's entity classes PlatformTransactionManager bean",
        "familiarity_anchors": [
          "Spring",
          "DataSource",
          "EntityManagerFactory"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": []
      }
    }
  ]
}
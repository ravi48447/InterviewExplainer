{
  "questions": [
    {
      "id": "statement-types-001",
      "slug": "jdbc-statement-types-statement-preparedstatement-callablestatement",
      "question": "What are the three types of statements in JDBC?",
      "title": "JDBC Statement Types — Statement, PreparedStatement, CallableStatement",
      "direct_answer": "JDBC has three statement types: **Statement** (static SQL, no parameters), **PreparedStatement** (parameterized + precompiled, prevents SQL injection — the default choice), and **CallableStatement** (calls stored procedures). All three send SQL; they differ in parameterization and safety.",
      "layout_type": "concept-explainer",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-08",
      "interviewer_intent": {
        "testing": "Whether you can name the three statement types and when each applies.",
        "common_mistake": "Defaulting to Statement and building SQL with string concatenation.",
        "to_stand_out": "State PreparedStatement is the default for any SQL with input — safer and faster."
      },
      "company_tags": [
        "amazon",
        "tcs",
        "infosys",
        "microsoft"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "The Three JDBC Statement Types",
            "content": "JDBC offers three interfaces for sending SQL:\n\n- **`Statement`** — executes **static** SQL with no parameters. Building it with user input via string concatenation is **unsafe** (SQL injection).\n- **`PreparedStatement`** — **parameterized** SQL with `?` placeholders, **precompiled** by the DB, and **injection-safe**. This is the default for any query involving input.\n- **`CallableStatement`** — calls **stored procedures** (`{call proc(?, ?)}`), supporting IN/OUT parameters.\n\n`PreparedStatement` extends `Statement`, and `CallableStatement` extends `PreparedStatement`."
          },
          {
            "type": "concept_map",
            "title": "Statement Types at a Glance",
            "content": "rose|Statement|~static SQL|no parameters|injection risk if concatenated|rare use\nemerald|PreparedStatement|~parameterized|? placeholders|precompiled + injection-safe|the default\nblue|CallableStatement|~stored procedures|{call proc(?,?)}|IN/OUT params|for DB procedures\namber|Hierarchy|~inheritance|Statement <- PreparedStatement|<- CallableStatement|"
          },
          {
            "type": "comparison_table",
            "title": "Statement vs PreparedStatement vs CallableStatement",
            "content": "| Aspect | `Statement` | `PreparedStatement` | `CallableStatement` |\n|---|---|---|---|\n| **Parameters** | none — static SQL | `?` placeholders | IN / OUT / INOUT |\n| **Compilation** | parsed every call | precompiled once, plan reused | precompiled (extends PreparedStatement) |\n| **SQL injection** | unsafe if input concatenated | safe — values bound as data | safe |\n| **Created with** | `createStatement()` | `prepareStatement(sql)` | `prepareCall(\"{call proc(?)}\")` |\n| **Use for** | one-off static/DDL SQL | every query with input (the default) | stored procedures/functions |\n\nThe three form an inheritance chain — `Statement` ← `PreparedStatement` ← `CallableStatement` — so each adds capability on top of the previous."
          },
          {
            "type": "code_example",
            "title": "All three types",
            "content": "```java\n// Statement — static SQL only (no input)\ntry (Statement st = con.createStatement();\n     ResultSet rs = st.executeQuery(\"SELECT COUNT(*) FROM users\")) { /* ... */ }\n\n// PreparedStatement — parameterized, safe (the default)\ntry (PreparedStatement ps = con.prepareStatement(\"SELECT * FROM users WHERE id = ?\")) {\n    ps.setInt(1, 42);\n    try (ResultSet rs = ps.executeQuery()) { /* ... */ }\n}\n\n// CallableStatement — stored procedure\ntry (CallableStatement cs = con.prepareCall(\"{call get_user(?)}\")) {\n    cs.setInt(1, 42);\n    cs.execute();\n}\n```"
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Using `Statement` with concatenated input**: opens SQL injection — use `PreparedStatement`.\n- **Thinking they're unrelated**: they form an inheritance chain.\n- **Reaching for `Statement` by habit**: prefer `PreparedStatement` for nearly everything.\n- **Using `CallableStatement` for plain queries**: it's for stored procedures."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Statement** — static SQL, no params (risky with input).\n- **PreparedStatement** — parameterized, precompiled, injection-safe (default).\n- **CallableStatement** — calls stored procedures (IN/OUT params).\n- **Inheritance**: Statement ← PreparedStatement ← CallableStatement.\n- **Use PreparedStatement** for any SQL with input."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "JDBC gives you three types of statements for sending SQL, and they differ mainly in parameterization and safety. The first is Statement, which executes static SQL with no parameters. The problem is that if you build a Statement by concatenating user input into the SQL string, you open yourself up to SQL injection, so plain Statement is rarely the right choice when input is involved. The second is PreparedStatement, which uses question-mark placeholders for parameters, is precompiled by the database, and is safe against SQL injection because the values are sent separately from the SQL. This is the default I reach for in almost every case. The third is CallableStatement, which is used to call stored procedures, with a syntax like curly-brace call proc with question marks, and it supports both input and output parameters. It's also worth knowing they form an inheritance chain: PreparedStatement extends Statement, and CallableStatement extends PreparedStatement, so each adds capability on top of the previous. So the short version is: Statement for static SQL with no input, PreparedStatement for any parameterized query, which is the safe and fast default, and CallableStatement specifically for stored procedures."
          }
        ]
      },
      "followup_questions": [
        "Why is PreparedStatement preferred over Statement?",
        "When would you use a CallableStatement?",
        "How do the three statement types relate by inheritance?"
      ],
      "seo": {
        "metaTitle": "JDBC Statement Types — Statement, PreparedStatement, CallableStatement — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What are the three types of statements in JDBC? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1,
      "speakable_v2": {
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "B",
        "hook": "Three ways to send SQL, but one rule sorts them out. If input touches the query, it's PreparedStatement.",
        "standard_example": "Java 17 app connecting to a MySQL database with JDBC to query an orders table",
        "familiarity_anchors": [
          "SQL injection",
          "stored procedure",
          "placeholder",
          "precompiled"
        ],
        "beats": [
          {
            "kind": "what_each_is",
            "layout": "bullets",
            "items": [
              "Statement runs plain static SQL. It takes no parameters.",
              "PreparedStatement uses placeholders, and the database precompiles it.",
              "CallableStatement calls stored procedures. It handles output values too."
            ]
          },
          {
            "kind": "differences",
            "layout": "mini_table",
            "columns": [
              "Statement",
              "PreparedStatement",
              "CallableStatement"
            ],
            "rows": [
              {
                "axis": "Parameters",
                "values": [
                  "None.",
                  "Placeholders.",
                  "IN and OUT."
                ]
              },
              {
                "axis": "Safety",
                "values": [
                  "Risky.",
                  "Safe.",
                  "Safe."
                ]
              },
              {
                "axis": "Best use",
                "values": [
                  "Static SQL.",
                  "Most queries.",
                  "Procedures."
                ]
              }
            ]
          },
          {
            "kind": "when_to_pick",
            "layout": "bullets",
            "items": [
              "Pick PreparedStatement for almost everything an app does.",
              "Pick Statement only for fixed one-off SQL like a CREATE TABLE.",
              "Pick CallableStatement when you call a stored procedure."
            ]
          },
          {
            "kind": "tiny_example",
            "layout": "paragraph",
            "text": "Say you fetch a user by id. Use PreparedStatement with a placeholder and call setInt. The gotcha's plain Statement plus string building. That one habit opens the door to injection."
          }
        ],
        "cap": "So PreparedStatement's your default. Keep plain Statement for static admin SQL, and CallableStatement for procedures.",
        "followup_handoff": [
          "Why should you never use Statement with user input?",
          "What is the performance benefit of PreparedStatement over Statement?"
        ],
        "tts_overrides": {},
        "speakable_status": "approved"
      }
    },
    {
      "id": "statement-types-002",
      "slug": "sql-injection-risk-with-jdbc-statement",
      "question": "Why should you avoid using Statement with string concatenation for user input?",
      "title": "SQL Injection Risk with JDBC Statement",
      "direct_answer": "Concatenating user input into a `Statement` lets attackers inject SQL — e.g. `' OR '1'='1` turns a login check into 'always true'. **PreparedStatement** with `?` placeholders sends values **separately** from the SQL, so input is treated as data, never executable code.",
      "layout_type": "comparison-arena",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-08",
      "interviewer_intent": {
        "testing": "Whether you understand SQL injection from concatenation and the PreparedStatement fix.",
        "common_mistake": "Building SQL by string concatenation and trusting input is harmless.",
        "to_stand_out": "Explain that parameters are bound as data, so the query structure can't be altered."
      },
      "company_tags": [
        "amazon",
        "microsoft",
        "tcs",
        "infosys"
      ],
      "answer": {
        "sections": [
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Concatenation** makes input part of the executable SQL → injection.\n- **`' OR '1'='1`** can turn a check into 'always true'.\n- **PreparedStatement** binds values separately as data.\n- **The query is parsed first**, so structure can't change.\n- **Parameterize** any SQL that touches input."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "Concatenating user input into a Statement is dangerous because the input becomes part of the executable SQL, and that's exactly how SQL injection works. Suppose I build a login query by concatenating the username into the SQL string. An attacker enters something like quote space OR space quote one quote equals quote one, and now the WHERE clause becomes name equals empty string OR one equals one, which is always true, so the query returns every user, or worse, an attacker could append a statement that drops a table. The fix is PreparedStatement with question-mark placeholders. Instead of building the SQL with the value inside it, you write the SQL with a placeholder and then bind the value separately using a setter like setString. The crucial difference is timing and separation: the database parses the SQL query first, with the placeholder, and only then plugs in the parameter value as pure data. Because the structure of the query is already fixed before the value arrives, the input can never add new clauses or change the logic, no matter what special characters it contains. So even that classic quote OR one equals one payload is treated as a literal name string and simply doesn't match anyone. The rule I follow is simple: any time user-controlled input reaches SQL, and that's basically every real query, I parameterize with PreparedStatement; I only use a plain Statement for static SQL with no input at all."
          },
          {
            "type": "deep_explanation",
            "title": "Why the mistake happens",
            "content": "Use **`executeQuery()`** for SELECT — it returns a `ResultSet`.\n\nUse **`executeUpdate()`** for INSERT/UPDATE/DELETE/DDL — it returns an **int** row count.\n\nWhen you build SQL by **concatenating** user input into a `Statement`, the input becomes part of the **executable query**. An attacker can supply `' OR '1'='1` (or `'; DROP TABLE users;--`) and rewrite your query's logic — that's **SQL injection**, a top security risk. A **PreparedStatement** uses `?` placeholders and **binds values separately** from the SQL text. The database parses the query *first*, then plugs in parameters as **data**, so input can never change the query's structure — no matter what characters it contains.\n\nConcatenating user input into a `Statement` lets attackers inject SQL — e.g. `' OR '1'='1` turns a login check into 'always true'. **PreparedStatement** with `?` placeholders sends values **separately** from the SQL, so input is treated as data, never executable code."
          },
          {
            "type": "concept_map",
            "title": "Injection vs Parameterization",
            "content": "rose|Concatenation|~input becomes code|' OR '1'='1|query logic rewritten|SQL injection\nemerald|PreparedStatement|~input stays data|? placeholders|bound separately|structure fixed\nblue|Why safe|~parse then bind|SQL parsed first|params can't add clauses|special chars harmless\namber|Rule|~never concatenate|input -> always bind|even 'trusted' input|"
          },
          {
            "type": "before_code",
            "title": "Vulnerable: concatenated Statement",
            "content": "```java\n// NEVER do this — input is part of the SQL\nString name = request.getParameter(\"name\");   // attacker: ' OR '1'='1\nString sql = \"SELECT * FROM users WHERE name = '\" + name + \"'\";\nStatement st = con.createStatement();\nResultSet rs = st.executeQuery(sql);\n// becomes: SELECT * FROM users WHERE name = '' OR '1'='1'  -> returns everyone\n```"
          },
          {
            "type": "after_code",
            "title": "Safe: parameterized PreparedStatement",
            "content": "```java\nString name = request.getParameter(\"name\");\ntry (PreparedStatement ps =\n        con.prepareStatement(\"SELECT * FROM users WHERE name = ?\")) {\n    ps.setString(1, name);   // bound as DATA, never as SQL\n    try (ResultSet rs = ps.executeQuery()) { /* ... */ }\n}\n// even ' OR '1'='1 is treated as a literal name string -> no injection\n```\n\n*The query is parsed before the value is bound, so input can't alter its structure.*"
          },
          {
            "type": "when_to_use",
            "title": "When This Matters",
            "content": "Any time **user-controlled input** reaches SQL — form fields, query params, headers, file contents — you must parameterize. That's essentially every real query.\n\nStatic SQL with **no input** (e.g. `SELECT COUNT(*) FROM users`) is safe with a `Statement`, but the moment a value is involved, switch to `PreparedStatement`."
          },
          {
            "type": "tradeoffs",
            "title": "Important trade-off",
            "content": "Use **`executeQuery()`** for SELECT — it returns a `ResultSet`. Use **`executeUpdate()`** for INSERT/UPDATE/DELETE/DDL — it returns an **int** row count. Use **`execute()`** when you don't know the type ahead of time — it returns a **boolean** (true = a ResultSet is available), and you fetch results accordingly."
          }
        ]
      },
      "followup_questions": [
        "What are the three types of statements in JDBC?",
        "What is the performance benefit of PreparedStatement over Statement?",
        "How does PreparedStatement's precompilation improve performance?"
      ],
      "seo": {
        "metaTitle": "SQL Injection Risk with JDBC Statement — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for Why should you avoid using Statement with string concatenation for user input? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 2,
      "speakable_v2": {
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "A",
        "hook": "Quote OR one equals one can flip a login check to always true. Binding input as data shuts that door.",
        "standard_example": "Java 17 app connecting to a MySQL database with JDBC to query an orders table",
        "familiarity_anchors": [
          "SQL injection",
          "OWASP Top 10",
          "PreparedStatement",
          "placeholder"
        ],
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "SQL injection happens when user text slips into a SQL string and runs as code. The database can't tell your SQL from the user's input, so the input changes what the query does."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "A plain Statement glues the whole SQL string together and sends it as one piece. There's no line between your code and the user's data. So the data can act like code."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "Auth bypass: typing quote OR one equals one as a username makes the WHERE clause always true and returns every row.",
              "Data damage: typing a semicolon then DROP TABLE orders can wipe a table.",
              "The fix: PreparedStatement compiles the template first, then sends values as plain data that can't change the query."
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "Picture an order lookup building SQL with id equals plus orderId on a Statement. An attacker passes zero OR one equals one and grabs the whole orders table. Swap in a PreparedStatement with a placeholder and call setLong. Here's the non-obvious part. The attack text becomes a literal value, so the database finds no matching order and returns nothing."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Escaping quotes by hand feels safe but isn't. One missed edge case lets injection through.",
              "Guarding only the login form. Any endpoint that puts user data in a query can leak."
            ]
          }
        ],
        "cap": "Never glue user data into SQL. Use PreparedStatement and placeholders every time. This one isn't negotiable.",
        "followup_handoff": [
          "Does PreparedStatement always prevent SQL injection?",
          "What is OWASP Top 10 and where does SQL injection rank?"
        ],
        "tts_overrides": {},
        "speakable_status": "approved"
      }
    },
    {
      "id": "statement-types-003",
      "slug": "preparedstatement-performance-benefits",
      "question": "What is the performance benefit of PreparedStatement over Statement?",
      "title": "PreparedStatement Performance Benefits",
      "direct_answer": "**PreparedStatement** is faster for repeated execution because the database **parses and builds the execution plan once** (precompiles), then reuses it for every call with new parameters. A plain **Statement** re-parses and re-plans the SQL on **every** execution, wasting work in loops and batches.",
      "layout_type": "comparison-arena",
      "difficulty": "medium",
      "importance": "medium",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-08",
      "interviewer_intent": {
        "testing": "Whether you know PreparedStatement amortizes parsing/planning via precompilation.",
        "common_mistake": "Thinking PreparedStatement is only about security, not performance.",
        "to_stand_out": "Mention server-side statement caching and batch execution amplifying the gain."
      },
      "company_tags": [
        "amazon",
        "tcs",
        "infosys",
        "flipkart"
      ],
      "answer": {
        "sections": [
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **PreparedStatement precompiles** — parse + plan once, reuse.\n- **Statement re-parses/re-plans** on every execution.\n- **Biggest win** in loops, bulk ops, and high-volume queries.\n- **Batching** (`addBatch`/`executeBatch`) multiplies the gain.\n- **Also injection-safe** — two benefits in one."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "People often think PreparedStatement is only about security, but it's also a real performance win, and the reason is precompilation. Whenever the database runs SQL, it has to parse the text, build an execution plan, and then execute. A plain Statement redoes the parsing and planning on every single call. That's fine if you run it once, but if you're running the same query in a loop, you're paying that parse-and-plan cost over and over. A PreparedStatement, by contrast, is precompiled: the database parses and plans it once, often caching the plan, and then each execution just rebinds the new parameter values and runs. So for something like inserting ten thousand rows, or a lookup query that runs on every request at high volume, the reuse adds up to a significant speedup. And you can amplify it further with batching, where you queue many parameter sets with addBatch and send them all at once with executeBatch, which dramatically cuts the number of network round-trips. For a single one-off query the difference is negligible, so this matters most for repeated execution. The nice thing is you don't have to choose between safety and speed, because PreparedStatement gives you both: it prevents SQL injection and it's faster for repeated queries, which is why it's the sensible default."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Use **`executeQuery()`** for SELECT — it returns a `ResultSet`.\n\nUse **`executeUpdate()`** for INSERT/UPDATE/DELETE/DDL — it returns an **int** row count.\n\nUse **`execute()`** when you don't know the type ahead of time — it returns a **boolean** (true = a ResultSet is available), and you fetch results accordingly.\n\nConcatenating user input into a `Statement` lets attackers inject SQL — e.g. `' OR '1'='1` turns a login check into 'always true'. **PreparedStatement** with `?` placeholders sends values **separately** from the SQL, so input is treated as data, never executable code."
          },
          {
            "type": "concept_map",
            "title": "PreparedStatement Performance",
            "content": "rose|Statement|~re-parse each call|parse + plan every time|wasteful in loops|no reuse\nemerald|PreparedStatement|~precompiled|parse + plan once|rebind params + run|cached plan\namber|Batching|~amplifies gain|addBatch/executeBatch|fewer round-trips|big bulk speedup\nblue|Bonus|~also safer|injection-safe|two wins in one|the default"
          },
          {
            "type": "before_code",
            "title": "Statement in a loop — re-parsed every time",
            "content": "```java\ntry (Statement st = con.createStatement()) {\n    for (int i = 0; i < 10_000; i++) {\n        // the DB parses + plans this SQL 10,000 times\n        st.executeUpdate(\"INSERT INTO logs (msg) VALUES ('row \" + i + \"')\");\n    }\n}\n```"
          },
          {
            "type": "after_code",
            "title": "PreparedStatement + batch — compiled once",
            "content": "```java\ntry (PreparedStatement ps =\n        con.prepareStatement(\"INSERT INTO logs (msg) VALUES (?)\")) {\n    for (int i = 0; i < 10_000; i++) {\n        ps.setString(1, \"row \" + i);\n        ps.addBatch();                 // queue\n    }\n    ps.executeBatch();                 // one round-trip, plan reused\n}\n```\n\n*Parsed and planned once, parameters rebound each iteration, and batched into far fewer round-trips.*"
          },
          {
            "type": "when_to_use",
            "title": "When the Speedup Matters Most",
            "content": "The gain is biggest for **repeated execution**: bulk inserts/updates, queries run per request at high volume, and anything you can **batch**. For a single one-off query the difference is negligible.\n\nSince `PreparedStatement` also prevents injection, it's the right default even when performance isn't the main concern."
          }
        ]
      },
      "followup_questions": [
        "What are the three types of statements in JDBC?",
        "How does PreparedStatement's precompilation improve performance?",
        "What is the difference between execute(), executeQuery(), and executeUpdate() in JDBC?"
      ],
      "seo": {
        "metaTitle": "PreparedStatement Performance Benefits — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is the performance benefit of PreparedStatement over Statement? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 3,
      "speakable_v2": {
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "A",
        "hook": "A Statement in a loop makes the database re-plan the same SQL again and again. PreparedStatement plans once.",
        "standard_example": "Java 17 app connecting to a MySQL database with JDBC to query an orders table",
        "familiarity_anchors": [
          "execution plan",
          "plan cache",
          "batching"
        ],
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "When you create a PreparedStatement, the database parses the SQL and builds an execution plan. It caches that plan. Every later call with new values skips the parse and plan steps and jumps straight to running."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "Parsing SQL and picking a plan costs real CPU. A query that runs thousands of times a second only changes its values, not its shape. Redoing that work each call wastes effort on both servers."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "A Statement pays for parse, validate, optimize, and execute on every single call.",
              "A PreparedStatement does parse, validate, and optimize once, then caches the plan.",
              "Later PreparedStatement calls just bind new values and run the cached plan.",
              "Batching with addBatch and executeBatch sends many value sets in one round trip."
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "The orders app runs SELECT id, total FROM orders WHERE customer id equals placeholder thousands of times a minute. The first call builds the plan. Every later call reuses it. Here's the non-obvious part. With a plain Statement, each new id becomes a brand-new SQL string, so the database can't match it to the old plan and re-plans every time."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Creating a fresh PreparedStatement inside the loop throws away the caching. Build it once outside.",
              "Thinking plan caching only helps huge tables. It helps any query shape that runs a lot."
            ]
          }
        ],
        "cap": "PreparedStatement is faster because parse and optimize run once. A Statement pays that cost on every call.",
        "followup_handoff": [
          "What is statement caching in a connection pool?",
          "How does PreparedStatement batching work?"
        ],
        "tts_overrides": {},
        "speakable_status": "approved"
      }
    },
    {
      "id": "statement-types-005",
      "slug": "execute-vs-executequery-vs-executeupdate-in-jdbc",
      "question": "What is the difference between execute(), executeQuery(), and executeUpdate() in JDBC?",
      "title": "execute() vs executeQuery() vs executeUpdate() in JDBC",
      "direct_answer": "Use **`executeQuery()`** for SELECT — it returns a `ResultSet`. Use **`executeUpdate()`** for INSERT/UPDATE/DELETE/DDL — it returns an **int** row count. Use **`execute()`** when you don't know the type ahead of time — it returns a **boolean** (true = a ResultSet is available), and you fetch results accordingly.",
      "layout_type": "comparison-arena",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 2,
      "last_updated": "2026-06-08",
      "interviewer_intent": {
        "testing": "Whether you can match each execute method to the right SQL and return type.",
        "common_mistake": "Using executeQuery for an UPDATE or vice versa.",
        "to_stand_out": "Note execute() is for dynamic/unknown SQL or statements returning multiple results."
      },
      "company_tags": [
        "tcs",
        "infosys",
        "amazon",
        "wipro"
      ],
      "answer": {
        "sections": [
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **`executeQuery()`** → SELECT → returns `ResultSet`.\n- **`executeUpdate()`** → INSERT/UPDATE/DELETE/DDL → returns `int`.\n- **`execute()`** → unknown/multiple → returns `boolean`.\n- **Match the method** to the SQL or it fails.\n- **`execute`** is for dynamic SQL and mixed results."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "JDBC gives you three execute methods, and you pick based on what the SQL does and what you want back. executeQuery is for SELECT statements; it returns a ResultSet that you iterate over to read rows. executeUpdate is for data-changing statements, INSERT, UPDATE, and DELETE, as well as DDL like CREATE; it returns an int, the number of rows affected, which is zero for DDL. And execute is the general-purpose one, used when you don't know the statement type ahead of time or it might return multiple results; it returns a boolean, where true means the first result is a ResultSet, and then you call getResultSet or getUpdateCount to retrieve the actual results. The mistakes to avoid are mixing them up: calling executeQuery on an update, or executeUpdate on a select, is wrong because the return types don't match the operation. In day-to-day code I'm almost always using executeQuery for reads and executeUpdate for writes, and I rarely need the generic execute. The execute method really comes into play in things like database tools, dynamically generated SQL where you genuinely don't know if it's a query or an update, or stored procedures that can return a ResultSet or an update count, sometimes several in sequence."
          },
          {
            "type": "deep_explanation",
            "title": "Understand the decision",
            "content": "Use **`executeQuery()`** for SELECT — it returns a `ResultSet`.\n\nUse **`executeUpdate()`** for INSERT/UPDATE/DELETE/DDL — it returns an **int** row count.\n\nUse **`execute()`** when you don't know the type ahead of time — it returns a **boolean** (true = a ResultSet is available), and you fetch results accordingly.\n\nConcatenating user input into a `Statement` lets attackers inject SQL — e.g. `' OR '1'='1` turns a login check into 'always true'. **PreparedStatement** with `?` placeholders sends values **separately** from the SQL, so input is treated as data, never executable code."
          },
          {
            "type": "concept_map",
            "title": "Execute Methods at a Glance",
            "content": "emerald|executeQuery|~SELECT|returns ResultSet|read rows|never for updates\nblue|executeUpdate|~INSERT/UPDATE/DELETE|returns int row count|0 for DDL|never for SELECT\namber|execute|~unknown/dynamic SQL|returns boolean|true = ResultSet|getResultSet/getUpdateCount\nrose|Mistake|~wrong method|query for update fails|update for query fails|match to SQL"
          },
          {
            "type": "comparison_table",
            "title": "Execute Methods",
            "content": "| Method | For | Returns |\n|---|---|---|\n| `executeQuery()` | SELECT | `ResultSet` |\n| `executeUpdate()` | INSERT/UPDATE/DELETE/DDL | `int` (rows affected) |\n| `execute()` | unknown / multiple results | `boolean` |"
          },
          {
            "type": "before_code",
            "title": "Wrong method for the SQL",
            "content": "```java\n// WRONG: executeQuery on an UPDATE\nps.executeQuery(\"UPDATE users SET active = false\");  // throws / misuse\n\n// WRONG: executeUpdate on a SELECT\nps.executeUpdate(\"SELECT * FROM users\");             // wrong return type\n```"
          },
          {
            "type": "after_code",
            "title": "Right method per statement",
            "content": "```java\n// SELECT -> executeQuery -> ResultSet\ntry (ResultSet rs = psSelect.executeQuery()) { while (rs.next()) { /* ... */ } }\n\n// UPDATE -> executeUpdate -> int\nint affected = psUpdate.executeUpdate();\nSystem.out.println(affected + \" rows updated\");\n\n// unknown -> execute -> boolean, then branch\nboolean hasResultSet = psAny.execute();\nif (hasResultSet) { try (ResultSet rs = psAny.getResultSet()) { /* ... */ } }\nelse { int count = psAny.getUpdateCount(); }\n```"
          },
          {
            "type": "when_to_use",
            "title": "When to Use Which",
            "content": "Use **`executeQuery`** whenever you're reading data (SELECT). Use **`executeUpdate`** for any write or DDL when you care about how many rows changed.\n\nUse **`execute`** only when you don't know the statement type in advance — dynamic SQL, tools, or procedures that may return a ResultSet *or* an update count (or several)."
          }
        ]
      },
      "followup_questions": [
        "What are the three types of statements in JDBC?",
        "What is the performance benefit of PreparedStatement over Statement?",
        "How does PreparedStatement's precompilation improve performance?"
      ],
      "seo": {
        "metaTitle": "execute() vs executeQuery() vs executeUpdate() in JDBC — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is the difference between execute(), executeQuery(), and executeUpdate() in JDBC? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 4,
      "speakable_v2": {
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "B",
        "hook": "Three execute methods, three return types. The trick is matching the method to what the SQL actually does.",
        "standard_example": "Java 17 app connecting to a MySQL database with JDBC to query an orders table",
        "familiarity_anchors": [
          "ResultSet",
          "row count",
          "DDL"
        ],
        "beats": [
          {
            "kind": "what_each_is",
            "layout": "bullets",
            "items": [
              "executeQuery runs a SELECT. It hands back a ResultSet.",
              "executeUpdate runs an INSERT, UPDATE, DELETE, or DDL. It returns a row count.",
              "execute runs any SQL and returns a boolean."
            ]
          },
          {
            "kind": "differences",
            "layout": "mini_table",
            "columns": [
              "Use case",
              "Returns"
            ],
            "rows": [
              {
                "axis": "executeQuery",
                "values": [
                  "SELECT.",
                  "A ResultSet."
                ]
              },
              {
                "axis": "executeUpdate",
                "values": [
                  "Writes and DDL.",
                  "An int row count."
                ]
              },
              {
                "axis": "execute",
                "values": [
                  "Unknown SQL.",
                  "A boolean."
                ]
              }
            ]
          },
          {
            "kind": "when_to_pick",
            "layout": "bullets",
            "items": [
              "Use executeQuery whenever you read rows from a SELECT.",
              "Use executeUpdate for writes and schema scripts when the row count matters.",
              "Use execute only in dynamic SQL tools where you don't know the type up front."
            ]
          },
          {
            "kind": "tiny_example",
            "layout": "paragraph",
            "text": "Insert an order and executeUpdate returns one. One row landed. Read it back and executeQuery returns a ResultSet. The gotcha's calling executeQuery on that insert. It throws, since an insert makes no ResultSet."
          }
        ],
        "cap": "Use executeQuery to read and executeUpdate to write. Save execute for dynamic SQL tools.",
        "followup_handoff": [
          "What is returned by executeUpdate() for a DDL statement?",
          "What happens if you call executeQuery() on an INSERT statement?"
        ],
        "tts_overrides": {},
        "speakable_status": "approved"
      }
    },
    {
      "id": "statement-types-006",
      "slug": "when-to-use-statement-vs-preparedstatement",
      "question": "When would you use a Statement instead of PreparedStatement?",
      "title": "When to Use Statement vs PreparedStatement",
      "direct_answer": "Use a plain **Statement** only for **static SQL with no parameters** that runs **once** — like one-off DDL (`CREATE TABLE`), admin/migration scripts, or simple constant queries. The moment any user input or repetition is involved, switch to **PreparedStatement** for safety and performance.",
      "layout_type": "concept-explainer",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-08",
      "interviewer_intent": {
        "testing": "Whether you know the narrow legitimate uses of Statement.",
        "common_mistake": "Using Statement for parameterized or repeated queries.",
        "to_stand_out": "Note Statement avoids the precompile overhead for truly one-off static SQL."
      },
      "company_tags": [
        "tcs",
        "infosys",
        "wipro",
        "capgemini"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "When a Plain Statement Is Acceptable",
            "content": "`PreparedStatement` is the default, but a plain **`Statement`** is fine in a few narrow cases: **static SQL with no input** that you run **once**. Examples:\n\n- **One-off DDL** — `CREATE TABLE`, `ALTER TABLE` in a migration.\n- **Admin/maintenance scripts** — fixed queries with no parameters.\n- **Constant queries** — `SELECT COUNT(*) FROM users` with no variable parts.\n\nFor a single execution of fixed SQL, `Statement` skips the precompile setup. But add **any** parameter or run it **repeatedly**, and `PreparedStatement` wins on both **safety** and **performance**."
          },
          {
            "type": "concept_map",
            "title": "When to Use Statement",
            "content": "emerald|OK for|~static, one-off|DDL: CREATE/ALTER|admin scripts|constant queries\nrose|Not for|~input or repetition|user-supplied values|loops/batches|use PreparedStatement\nblue|Why OK|~no params|nothing to inject|run-once|no precompile benefit lost\namber|Switch when|~any variable|input present|executed many times|safety + speed"
          },
          {
            "type": "comparison_table",
            "title": "Statement vs PreparedStatement — when each fits",
            "content": "| Situation | `Statement` | `PreparedStatement` |\n|---|---|---|\n| **Has user input** | unsafe (injection) | safe — bound as data |\n| **Runs in a loop / batch** | re-parsed every call | precompiled once, reused |\n| **One-off static DDL** | fine — no precompile overhead | works, but no benefit |\n| **Day-to-day app queries** | avoid | the default |\n\nThe only place `Statement` clearly wins is **fixed, parameterless SQL you run once** — everywhere else `PreparedStatement` is safer and faster."
          },
          {
            "type": "code_example",
            "title": "OK: static, parameterless DDL run once",
            "content": "```java\n// Statement is fine here: no input, executed a single time\ntry (Statement st = con.createStatement()) {\n    st.execute(\"CREATE TABLE audit (id INT PRIMARY KEY, msg VARCHAR(200))\");\n}\n```"
          },
          {
            "type": "before_code",
            "title": "Wrong: Statement with input in a loop",
            "content": "```java\n// repeated parse + injection risk from concatenated input\ntry (Statement st = con.createStatement()) {\n    for (String msg : messages) {                 // runs many times\n        // the DB re-parses this SQL on every iteration\n        st.executeUpdate(\"INSERT INTO audit (msg) VALUES ('\" + msg + \"')\");\n    }\n}\n```"
          },
          {
            "type": "after_code",
            "title": "Right: PreparedStatement, prepared once",
            "content": "```java\n// parameterized + precompiled: safe and fast\ntry (PreparedStatement ps =\n        con.prepareStatement(\"INSERT INTO audit (msg) VALUES (?)\")) {\n    for (String msg : messages) {\n        ps.setString(1, msg);   // bound as DATA, no injection\n        ps.addBatch();          // queue for one round-trip\n    }\n    ps.executeBatch();          // plan reused across all rows\n}\n```\n\n*Prepared once, values bound as data, and batched into far fewer round-trips.*"
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Statement with input**: any user value → injection risk; parameterize.\n- **Statement in a loop**: repeated parsing wastes time — PreparedStatement is precompiled.\n- **Over-thinking it**: for fixed one-off DDL, Statement is perfectly fine.\n- **Assuming Statement is always faster**: only for true single-run static SQL."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Statement** suits static, parameterless, run-once SQL.\n- **Good for** one-off DDL and admin scripts.\n- **Avoid** Statement with input or in loops.\n- **PreparedStatement** wins on safety and on repeated execution.\n- **Default to PreparedStatement** unless it's clearly static one-off SQL."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "Even though PreparedStatement is the default, there are a few narrow situations where a plain Statement is perfectly acceptable, and the common thread is static SQL with no parameters that you run once. The clearest example is one-off DDL, like a CREATE TABLE or ALTER TABLE in a migration script; there's no user input and you run it a single time, so a Statement is fine. The same goes for admin or maintenance scripts with fixed queries, or a constant query like SELECT COUNT star FROM users that has no variable parts. In those cases a Statement actually avoids the small overhead of precompiling that a PreparedStatement sets up, since you're only running the SQL once. But the moment any of two things is true, I switch to PreparedStatement. The first is user input: if any value comes from outside, concatenating it into a Statement risks SQL injection, so it must be parameterized. The second is repetition: if you're running the same query many times, say in a loop or a batch, a PreparedStatement is precompiled once and reused, so it's faster, whereas a Statement re-parses every time. So my rule is to default to PreparedStatement and only fall back to Statement for clearly static, parameterless, run-once SQL like DDL."
          }
        ]
      },
      "followup_questions": [
        "Why is PreparedStatement faster for repeated queries?",
        "Is Statement safe for DDL?",
        "What is the overhead of PreparedStatement for a single run?"
      ],
      "seo": {
        "metaTitle": "When to Use Statement vs PreparedStatement — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use a Statement instead of PreparedStatement? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 5,
      "speakable_v2": {
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "B",
        "hook": "Statement earns its keep in one spot only. Static, no-parameter SQL you run once, like a CREATE TABLE.",
        "standard_example": "Java 17 app connecting to a MySQL database with JDBC to query an orders table",
        "familiarity_anchors": [
          "DDL",
          "SQL injection",
          "PreparedStatement"
        ],
        "beats": [
          {
            "kind": "what_each_is",
            "layout": "bullets",
            "items": [
              "Statement sends fixed SQL with no parameters, usually DDL or admin scripts.",
              "PreparedStatement uses placeholders for any value from a variable, config, or user input."
            ]
          },
          {
            "kind": "differences",
            "layout": "mini_table",
            "columns": [
              "Statement",
              "PreparedStatement"
            ],
            "rows": [
              {
                "axis": "Safety",
                "values": [
                  "Injection risk.",
                  "Injection-safe."
                ]
              },
              {
                "axis": "Speed",
                "values": [
                  "Re-parsed each run.",
                  "Plan cached and reused."
                ]
              },
              {
                "axis": "Good for",
                "values": [
                  "One-off DDL.",
                  "All app queries."
                ]
              }
            ]
          },
          {
            "kind": "when_to_pick",
            "layout": "bullets",
            "items": [
              "Reach for PreparedStatement in every controller, service, or repository method.",
              "Reach for Statement only in a schema migration or a one-off admin script with fixed SQL."
            ]
          },
          {
            "kind": "tiny_example",
            "layout": "paragraph",
            "text": "Creating an audit table once? A plain Statement works fine. Inserting rows into it? Switch to PreparedStatement. The gotcha's a Statement inside a loop. It re-parses the SQL every pass and invites injection."
          }
        ],
        "cap": "In app code, PreparedStatement's always right. Keep Statement for fixed DDL scripts only.",
        "followup_handoff": [
          "Can you use PreparedStatement for DDL like CREATE TABLE?",
          "What security principle does always defaulting to PreparedStatement follow?"
        ],
        "tts_overrides": {},
        "speakable_status": "approved"
      }
    },
    {
      "id": "statement-types-007",
      "slug": "preparedstatement-precompilation-and-performance",
      "question": "How does PreparedStatement's precompilation improve performance?",
      "title": "PreparedStatement Precompilation and Performance",
      "direct_answer": "**Precompilation** means the database parses the SQL, validates it, and builds an **execution plan once** when the `PreparedStatement` is prepared. Each execution then skips straight to **binding parameters and running** the cached plan — avoiding repeated parse/optimize work, which is the performance gain for repeated queries.",
      "layout_type": "concept-explainer",
      "difficulty": "medium",
      "importance": "medium",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-08",
      "interviewer_intent": {
        "testing": "Whether you can explain what precompilation actually does step by step.",
        "common_mistake": "Saying 'it's compiled' without explaining parse/plan reuse.",
        "to_stand_out": "Distinguish client-side prepare from server-side prepared statements/plan caching."
      },
      "company_tags": [
        "amazon",
        "flipkart",
        "microsoft",
        "tcs"
      ],
      "answer": {
        "sections": [
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Prepare** = parse + validate + build execution plan, once.\n- **Execute** = bind parameters + run the cached plan.\n- **Parse/optimize cost** is paid once, reused many times.\n- **Prepare once, execute many** — don't re-prepare in a loop.\n- **Server-side vs client-side** prepare varies by driver/DB."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "Precompilation is what makes a PreparedStatement faster for repeated queries, so it helps to walk through what actually happens. When you prepare a PreparedStatement, the database does the expensive work up front and just once: it parses the SQL to check the syntax, validates it by confirming the tables and columns exist, and builds an execution plan, which is the strategy for how it'll fetch the data, like which index to use. That compiled, planned form gets cached. Then, on each execution, the database skips the parsing and planning entirely and goes straight to binding your parameter values into the placeholders and running the cached plan. So the costly parse-and-optimize step is paid once and then amortized across every execution. That's why running the same query many times, say looking up orders for a list of customer ids, is much faster with a PreparedStatement: it's parsed and planned once and reused for each id. A couple of clarifications I'd add. This is not compilation to machine code; it's parsing and plan caching. And the exact behaviour depends on the driver and database: some do a true server-side prepared statement where the plan is cached in the database, while others prepare more on the client side. The practical takeaway is to prepare once outside your loop and execute many times inside it, rather than re-preparing each iteration."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Use **`executeQuery()`** for SELECT — it returns a `ResultSet`.\n\nUse **`executeUpdate()`** for INSERT/UPDATE/DELETE/DDL — it returns an **int** row count.\n\nUse **`execute()`** when you don't know the type ahead of time — it returns a **boolean** (true = a ResultSet is available), and you fetch results accordingly.\n\nConcatenating user input into a `Statement` lets attackers inject SQL — e.g. `' OR '1'='1` turns a login check into 'always true'. **PreparedStatement** with `?` placeholders sends values **separately** from the SQL, so input is treated as data, never executable code."
          },
          {
            "type": "concept_map",
            "title": "Precompilation at a Glance",
            "content": "blue|Prepare phase|~one-time cost|parse SQL|validate tables/columns|build execution plan\nemerald|Execute phase|~cheap, repeated|bind parameters|run cached plan|skip parse/plan\namber|Net effect|~amortized cost|pay parse once|reuse many times|fast in loops\nrose|Server-side|~real prepared stmt|plan cached in DB|vs client-side prepare|depends on driver/DB"
          },
          {
            "type": "code_example",
            "title": "Prepare once, execute many",
            "content": "```java\n// prepare(): parse + validate + plan happen here, once\ntry (PreparedStatement ps =\n        con.prepareStatement(\"SELECT * FROM orders WHERE customer_id = ?\")) {\n    for (int id : customerIds) {     // many executions\n        ps.setInt(1, id);            // just bind...\n        try (ResultSet rs = ps.executeQuery()) {  // ...and run the cached plan\n            while (rs.next()) { /* ... */ }\n        }\n    }\n}\n// the SQL is parsed and planned once, then reused for every id\n```"
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes",
            "content": "- **Re-preparing inside the loop**: defeats the purpose — prepare once, execute many.\n- **Saying 'compiled to machine code'**: it's parse + plan caching, not native compilation.\n- **Assuming all DBs cache server-side identically**: some prepare client-side; behaviour varies.\n- **Expecting a gain for a single run**: precompilation pays off with repetition."
          }
        ]
      },
      "followup_questions": [
        "What are the three types of statements in JDBC?",
        "What is the performance benefit of PreparedStatement over Statement?",
        "What is the difference between execute(), executeQuery(), and executeUpdate() in JDBC?"
      ],
      "seo": {
        "metaTitle": "PreparedStatement Precompilation and Performance — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How does PreparedStatement's precompilation improve performance? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 6,
      "speakable_v2": {
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "archetype": "A",
        "hook": "Precompilation isn't magic compiling to machine code. The database just runs parse, validate, and plan once, then reuses that work.",
        "standard_example": "Java 17 app connecting to a MySQL database with JDBC to query an orders table",
        "familiarity_anchors": [
          "execution plan",
          "plan cache",
          "parse and optimize"
        ],
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "Precompilation means the database works through the SQL template once. It parses, validates, optimizes, and builds an execution plan. Then it caches that plan. Every later call with new parameters skips ahead to execution."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "For a hot query like find order by customer id, the SQL shape never changes. Only the id changes. Rebuilding the plan every time burns database CPU. Caching it once and reusing it is an easy win."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "A Statement runs all four stages, parse, validate, optimize, execute, on every call.",
              "A PreparedStatement's first call does parse, validate, optimize, then caches the plan.",
              "Later calls only bind parameters and execute on the cached plan, so just two stages.",
              "A query that runs exactly once gets no caching benefit at all."
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "The orders app runs a SELECT by customer id many times a minute. The first run compiles the plan. Each later call passes a different id and rides the same plan. Here's the non-obvious part. With a Statement, every call carries a unique string like customer id equals forty-two, so the database can't match the shape and recompiles."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Building a new PreparedStatement on each loop pass. You pay the compile cost again and lose the cache.",
              "Treating precompilation as a tiny tweak. Under heavy load, skipping parse and optimize lifts throughput a lot."
            ]
          }
        ],
        "cap": "Create a PreparedStatement once and reuse it. The cache payoff grows with how often the query runs.",
        "followup_handoff": [
          "What are the stages of query processing in a relational database?",
          "Does a PreparedStatement created in one request get reused in another request?"
        ],
        "tts_overrides": {},
        "speakable_status": "approved"
      }
    }
  ]
}

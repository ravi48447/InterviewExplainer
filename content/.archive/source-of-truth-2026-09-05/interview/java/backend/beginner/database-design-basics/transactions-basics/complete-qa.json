{
  "questions": [
    {
      "id": "jbf-dbdesign-transactions-001",
      "slug": "what-are-acid-properties-database",
      "question": "What are the ACID properties of a database transaction?",
      "title": "ACID Properties of Database Transactions",
      "direct_answer": "ACID stands for Atomicity, Consistency, Isolation, and Durability — the four properties that guarantee database transactions are processed reliably. Atomicity ensures all-or-nothing execution, Consistency maintains database rules, Isolation prevents concurrent transaction interference, and Durability guarantees committed data survives system failures.",
      "layout_type": "concept-explainer",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-08",
      "interviewer_intent": {
        "testing": "Whether you understand what each ACID property means in practice and can explain why each matters with concrete examples.",
        "common_mistake": "Memorizing the four-letter acronym without understanding scenarios where each property prevents data corruption or loss.",
        "to_stand_out": "Use the bank transfer example to illustrate all four properties simultaneously, showing how removing any one property breaks data integrity."
      },
      "company_tags": [
        "amazon",
        "google",
        "microsoft",
        "tcs"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "What ACID Properties Guarantee in Database Transactions",
            "content": "ACID properties are the foundation of reliable database transactions. **Atomicity** means a transaction executes completely or not at all — no partial updates. **Consistency** ensures the database moves from one valid state to another, respecting all constraints and rules. **Isolation** prevents concurrent transactions from interfering with each other, so each transaction sees a consistent view of the data. **Durability** guarantees that once a transaction commits, the changes survive system crashes or power failures. Think of ACID like a bank transfer: atomicity ensures both debit and credit happen or neither does, consistency ensures account balances never go negative if that rule exists, isolation prevents two transfers from reading the same balance simultaneously, and durability means your deposited money survives even if the server crashes one second later."
          },
          {
            "type": "concept_map",
            "title": "ACID at a Glance",
            "content": "amber|Atomicity|~All or nothing|every step or none|rollback on failure|debit and credit together\nblue|Consistency|~Valid to valid state|constraints enforced|no rule broken|balance never negative\nemerald|Isolation|~Concurrent safety|transactions don't interfere|isolation levels|no dirty reads\nviolet|Durability|~Survives crashes|committed equals permanent|write-ahead log|power-failure safe"
          },
          {
            "type": "code_example",
            "title": "Spring Transaction with ACID Properties in Action",
            "content": "@Service\npublic class BankTransferService {\n    @Autowired\n    private AccountRepository accountRepo;\n\n    @Transactional  // Spring enforces ACID properties\n    public void transferMoney(Long fromId, Long toId, BigDecimal amount) {\n        var fromAccount = accountRepo.findById(fromId)\n            .orElseThrow(() -> new AccountNotFoundException(fromId));\n        var toAccount = accountRepo.findById(toId)\n            .orElseThrow(() -> new AccountNotFoundException(toId));\n\n        // Atomicity: both operations succeed or both fail\n        fromAccount.debit(amount);  // throws if insufficient funds\n        toAccount.credit(amount);\n\n        accountRepo.save(fromAccount);  // Consistency: balance constraints checked\n        accountRepo.save(toAccount);    // Isolation: other transactions see old or new state, not in-between\n        // Durability: after commit, changes survive crashes\n    }\n}"
          },
          {
            "type": "code_example",
            "title": "The Same Transaction in Raw SQL (BEGIN, COMMIT, ROLLBACK, SAVEPOINT)",
            "content": "```sql\n-- A money transfer as one atomic unit of work.\nBEGIN;                                          -- start the transaction\n\nUPDATE accounts SET balance = balance - 100\n WHERE id = 1;                                  -- debit the sender\n\nSAVEPOINT after_debit;                           -- optional checkpoint\n\nUPDATE accounts SET balance = balance + 100\n WHERE id = 2;                                  -- credit the receiver\n\n-- If the credit looked wrong, undo just that step:\n--   ROLLBACK TO SAVEPOINT after_debit;\n\nCOMMIT;                                          -- make both changes permanent\n\n-- If anything had failed before COMMIT, we would run:\n--   ROLLBACK;   -- the database stays exactly as it was before BEGIN\n```\n\n*Everything between `BEGIN` and `COMMIT` is one all-or-nothing unit. `SAVEPOINT` lets you undo part of the work without abandoning the whole transaction.*"
          },
          {
            "type": "phase",
            "title": "Why Atomicity Needs an Explicit Boundary",
            "content": "A single SQL statement is already atomic on its own. The danger appears when **two statements must agree** — a debit and a credit, an order and a stock decrement. Without a transaction boundary, each statement commits the instant it runs. If the process dies between them, the first change is permanent and the second never happens, leaving the data **inconsistent with no error reported**.\n\nA transaction draws a line around the group: every statement inside it commits together, or none of them do. The before-and-after below shows the same two updates without and with that boundary."
          },
          {
            "type": "before_code",
            "title": "Without a transaction — a crash leaves money missing",
            "content": "```sql\n-- No BEGIN/COMMIT: each UPDATE commits on its own.\nUPDATE accounts SET balance = balance - 100 WHERE id = 1;  -- committed immediately\n-- *** application crashes / connection drops here ***\nUPDATE accounts SET balance = balance + 100 WHERE id = 2;  -- never runs\n\n-- Result: sender lost 100, receiver gained nothing. Money vanished.\n```"
          },
          {
            "type": "after_code",
            "title": "Wrapped in one transaction — all or nothing",
            "content": "```sql\nBEGIN;\nUPDATE accounts SET balance = balance - 100 WHERE id = 1;  -- not yet permanent\nUPDATE accounts SET balance = balance + 100 WHERE id = 2;  -- not yet permanent\nCOMMIT;                                                     -- both land together\n\n-- A crash before COMMIT changes nothing — the database rolls back to its prior state.\n```\n\n*In Spring you rarely type `BEGIN`/`COMMIT` — `@Transactional` on the service method draws this exact boundary for you, committing on success and rolling back on a runtime exception.*"
          },
          {
            "type": "when_to_use",
            "title": "When You Need ACID Transaction Guarantees",
            "content": "Use transactions with ACID guarantees when **multiple database operations must succeed or fail together**. Bank transfers between accounts require atomicity so money isn't lost or duplicated. E-commerce order placement needs consistency to prevent selling more inventory than exists. Multi-user applications need isolation so concurrent updates don't overwrite each other. Financial records need durability so committed transactions survive hardware failures. Any operation where partial completion corrupts your data requires ACID properties."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes Freshers Make with ACID",
            "content": "- **Thinking ACID is automatic for all database operations**: Without explicit transaction boundaries in Spring, each repository call is a separate transaction, so multi-step operations aren't atomic.\n- **Not understanding isolation trade-offs**: Freshers often don't realize that higher isolation levels prevent more anomalies but reduce concurrency and performance.\n- **Ignoring durability assumptions**: Thinking committed data is safe without database backups — durability protects against crashes but not disk failures or accidental deletes."
          },
          {
            "type": "key_points",
            "title": "Key Points About ACID Properties",
            "content": [
              "**Atomicity**: All operations in a transaction succeed or all fail — no partial updates leave data inconsistent",
              "**Consistency**: Database moves from one valid state to another, all constraints and rules are enforced",
              "**Isolation**: Concurrent transactions don't interfere — each sees a consistent snapshot of data",
              "**Durability**: Committed changes survive crashes, power failures, and system restarts",
              "**Spring enforcement**: at-Transactional annotation ensures ACID properties are applied to service methods",
              "**Trade-offs**: Higher isolation levels provide stronger consistency but reduce performance and concurrency",
              "**Real-world necessity**: Financial operations, inventory management, and multi-step workflows all depend on ACID"
            ]
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer in an Interview",
            "content": "ACID is four properties that make database transactions reliable: Atomicity, Consistency, Isolation, and Durability. The easiest way to explain them is a bank transfer. Atomicity means all or nothing. You debit one account and credit another, and either both happen or neither does, so money can't vanish because the debit succeeded but the credit failed. Consistency means the database keeps its rules. If a balance can't go negative, a transfer that would break that rule won't commit. Isolation means concurrent transactions don't step on each other. Two transfers from the same account each see a consistent view instead of a half-updated balance. Durability means once you commit, the change survives. Even if the server loses power a second later, the committed transfer is still there. In SQL you wrap the steps between BEGIN and COMMIT, and you ROLLBACK if anything fails. In Spring Boot, the @Transactional annotation draws that same boundary for you and rolls back on a runtime exception. The thing freshers miss is that ACID isn't automatic for multi-step work. Without an explicit transaction, each statement commits on its own, so a crash in the middle leaves your data half-written. That's why ACID matters for transfers, order processing, and anything where a partial result would corrupt your data."
          }
        ]
      },
      "followup_questions": [
        "What is the difference between a commit and a rollback?",
        "What happens if an exception is thrown inside a @Transactional method?"
      ],
      "seo": {
        "metaTitle": "ACID Properties of Database Transactions — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What are the ACID properties of a database transaction? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 1,
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "a bank transfer in SQL with BEGIN, two UPDATE accounts, and COMMIT",
        "familiarity_anchors": [
          "Atomic — all or nothing",
          "BEGIN and COMMIT",
          "ROLLBACK",
          "bank transfer"
        ],
        "hook": "ACID is four letters, and each one blocks a different way your data can break.",
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "ACID is the promise a database makes about a transaction. It names four properties. Atomic — all or nothing. Consistency keeps your rules valid. Isolation keeps concurrent transactions apart. Durability means committed data sticks."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "Without these guarantees, half-finished work can wreck your data. Say a transfer debits one account but the credit fails. Money just vanishes. That's the mess ACID exists to stop."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "Atomicity: every step commits together, or they all roll back.",
              "Consistency: constraints hold, so a balance can't break its own rules.",
              "Isolation: two transactions don't see each other's half-done work.",
              "Durability: once it's committed, a crash won't undo it."
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "Picture a bank transfer in SQL. You start with BEGIN. Then one UPDATE accounts debits the sender. Another UPDATE accounts credits the receiver. Then COMMIT makes it final. If a step fails first, you ROLLBACK and nothing changed. Here's the non-obvious part. The database might crash right after COMMIT. Durability still keeps your change safe."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Thinking every statement is already one transaction. Multi-step work needs an explicit boundary.",
              "Expecting a checked exception to roll back a Spring transaction. By default it won't, only a runtime exception does."
            ]
          }
        ],
        "cap": "Get all four letters and you'll know exactly what a transaction protects, and what breaks without it.",
        "followup_handoff": [
          "What happens if an exception is thrown inside a @Transactional method?",
          "What is transaction propagation in Spring?"
        ],
        "tts_overrides": {},
        "speakable_status": "approved"
      }
    },
    {
      "id": "jbf-dbdesign-transactions-002",
      "slug": "spring-transactional-annotation",
      "question": "How does Spring's @Transactional annotation work?",
      "title": "How @Transactional Works in Spring",
      "direct_answer": "The at-Transactional annotation tells Spring to wrap a method in a database transaction, automatically starting the transaction before the method executes and committing on success or rolling back if an unchecked exception is thrown. Spring uses AOP proxies to intercept method calls and inject transaction management code.",
      "layout_type": "concept-explainer",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-08",
      "interviewer_intent": {
        "testing": "Practical Spring knowledge — at-Transactional is used in almost every Spring Data application, so you must understand its behavior.",
        "common_mistake": "Thinking at-Transactional works when called from within the same class — Spring's AOP proxy doesn't intercept self-invocation, so the transaction never starts.",
        "to_stand_out": "Mention the self-invocation caveat and explain that at-Transactional on private methods also doesn't work — only public methods on Spring beans are proxied."
      },
      "company_tags": [
        "amazon",
        "flipkart",
        "tcs"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "How Spring's @Transactional Annotation Manages Database Transactions",
            "content": "Spring's **at-Transactional annotation** uses AOP (Aspect-Oriented Programming) to automatically wrap methods in database transactions. When you call a transactional method, Spring's **proxy** intercepts the call, starts a database transaction before entering the method, and commits the transaction if the method completes successfully. If an **unchecked exception** (RuntimeException or Error) is thrown, Spring automatically rolls back the transaction. This declarative approach eliminates boilerplate try-catch-commit-rollback code.\n\nThink of it like a bodyguard: the proxy stands between the caller and your method, handling all the transaction lifecycle events (begin, commit, rollback) so your business logic stays clean.\n\nThe key is understanding that this only works through Spring proxies — calling a transactional method from within the same class bypasses the proxy entirely."
          },
          {
            "type": "flow_diagram",
            "title": "The @Transactional Commit Lifecycle",
            "content": "```mermaid\nflowchart TD\n    A[Caller invokes method<br/>through Spring proxy] --> B[Proxy opens<br/>a transaction]\n    B --> C[Your method body runs<br/>repository calls execute]\n    C --> D{Method outcome?}\n    D -->|returns normally| E[Proxy COMMITs<br/>all changes persist]\n    D -->|RuntimeException thrown| F[Proxy ROLLBACKs<br/>all changes undone]\n    D -->|checked Exception thrown| G[Proxy COMMITs by default<br/>use rollbackFor to change]\n    E --> H[Connection returned to pool]\n    F --> H\n    G --> H\n```\n\n*The proxy — not your code — runs begin, commit, and rollback. By default only a RuntimeException triggers a rollback.*"
          },
          {
            "type": "code_example",
            "title": "Using @Transactional in a Spring Service",
            "content": "@Service\npublic class OrderService {\n    @Autowired\n    private OrderRepository orderRepo;\n    @Autowired\n    private InventoryService inventoryService;\n\n    @Transactional  // Spring starts transaction here\n    public Order createOrder(OrderRequest request) {\n        var order = new Order(request.customerId(), request.items());\n        orderRepo.save(order);  // persisted in transaction\n\n        inventoryService.reserveStock(request.items());  // also in same transaction\n\n        return order;  // Spring commits transaction on successful return\n    }\n\n    @Transactional(readOnly = true)  // optimization for read-only operations\n    public Optional<Order> findOrder(Long orderId) {\n        return orderRepo.findById(orderId);  // read-only transaction\n    }\n    // If createOrder throws RuntimeException, Spring rolls back automatically\n}"
          },
          {
            "type": "before_code",
            "title": "Without @Transactional — the second write can orphan the first",
            "content": "```java\n@Service\npublic class OrderService {\n    private final OrderRepository orderRepo;\n    private final InventoryRepository inventoryRepo;\n\n    // No @Transactional: each save() is its own transaction.\n    public void placeOrder(Order order) {\n        orderRepo.save(order);                 // committed immediately\n        inventoryRepo.decrementStock(order);   // if THIS throws...\n        // ...the order is already saved, but stock was never reduced.\n        // Result: an order exists for inventory you didn't deduct.\n    }\n}\n```"
          },
          {
            "type": "after_code",
            "title": "With @Transactional — both writes share one boundary",
            "content": "```java\n@Service\npublic class OrderService {\n    private final OrderRepository orderRepo;\n    private final InventoryRepository inventoryRepo;\n\n    @Transactional  // one transaction wraps both writes\n    public void placeOrder(Order order) {\n        orderRepo.save(order);                 // not yet committed\n        inventoryRepo.decrementStock(order);   // not yet committed\n    }   // proxy commits here on success, or rolls back if either throws\n}\n```\n\n*If `decrementStock` throws a `RuntimeException`, Spring rolls back the order insert too — there is no orphaned order.*"
          },
          {
            "type": "when_to_use",
            "title": "When to Apply @Transactional Annotation",
            "content": "Use **at-Transactional on service layer methods** that perform multiple database operations that must succeed or fail together.\n\nApply it when you're coordinating changes across multiple entities or repositories — order creation with inventory updates, user registration with profile setup, payment processing with status updates.\n\nUse **readOnly = true** for query methods to optimize performance. Don't use at-Transactional on repository methods (they're already transactional) or on methods that don't touch the database. Place it on public methods of Spring-managed beans — private methods and self-invoked methods won't be proxied."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes with @Transactional",
            "content": "- **Self-invocation doesn't work**: Calling a transactional method from another method in the same class bypasses the Spring proxy, so no transaction starts. If methodA calls methodB and both have at-Transactional, methodB's annotation is ignored.\n- **Using it on private methods**: Spring AOP only proxies public methods, so at-Transactional on private or protected methods is silently ignored.\n- **Forgetting checked exceptions roll back differently**: By default, only unchecked exceptions trigger rollback — checked exceptions commit unless you specify rollbackFor."
          },
          {
            "type": "key_points",
            "title": "Key Points About @Transactional",
            "content": [
              "**Proxy-based**: Spring creates a proxy around your bean that intercepts method calls to manage transactions",
              "**Automatic lifecycle**: Transaction starts on method entry, commits on success, rolls back on unchecked exceptions",
              "**Self-invocation caveat**: Calling transactional methods from within the same class bypasses the proxy",
              "**Public methods only**: Private, protected, or package-private methods aren't proxied, so at-Transactional has no effect",
              "**readOnly optimization**: Mark query methods with readOnly equals true for better performance",
              "**Propagation control**: Nested transactional calls participate in existing transactions by default (REQUIRED propagation)",
              "**Exception handling**: Unchecked exceptions roll back automatically, checked exceptions don't unless configured"
            ]
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer in an Interview",
            "content": "The at-Transactional annotation tells Spring to wrap a method in a database transaction automatically. Spring uses AOP proxies to intercept calls to transactional methods. When you call the method, the proxy starts a transaction before entering your code, and when the method returns successfully, the proxy commits the transaction. If an unchecked exception like RuntimeException is thrown, Spring automatically rolls back the transaction. This declarative approach is much cleaner than writing manual transaction management code. Here's the critical caveat freshers often miss: at-Transactional only works when the method is called from outside the class. If you call a transactional method from another method in the same class, Spring's proxy isn't involved, so no transaction starts. That's called self-invocation and it's a common source of bugs. Also, the annotation only works on public methods of Spring beans — private methods aren't proxied. In a real application, you'd put at-Transactional on service layer methods that coordinate multiple repository calls, like creating an order and updating inventory. That ensures both operations succeed or both fail together. You can also use readOnly equals true on query methods to optimize performance. Understanding how the proxy mechanism works is key to using at-Transactional correctly and avoiding subtle transaction bugs."
          }
        ]
      },
      "followup_questions": [
        "What is transaction propagation in Spring?",
        "Why doesn't @Transactional work on private methods?"
      ],
      "seo": {
        "metaTitle": "How @Transactional Works in Spring — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How does Spring's @Transactional annotation work? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 2,
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "a service method that inserts an order and reserves stock under one @Transactional",
        "familiarity_anchors": [
          "@Transactional",
          "proxy",
          "commit and rollback",
          "self-invocation"
        ],
        "hook": "@Transactional looks like one line, but two quiet rules decide whether it even runs.",
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "@Transactional tells Spring to wrap a method in a database transaction. Spring builds a proxy around your bean. The proxy starts the transaction, runs your code, then commits. If a runtime exception escapes, it rolls back instead."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "Without it, each repository save runs in its own transaction. If the second save fails, the first one's already stored. Now your data's half-written, with no error to warn you."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "Commit: the method returns normally, so Spring saves every change.",
              "Rollback: a runtime exception escapes, so Spring undoes every change.",
              "No rollback: a checked exception escapes, and the changes stay committed by default."
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "Say a service method inserts an order, then reserves stock. One @Transactional ties both into a single unit. If reserving stock throws, the order insert rolls back too. Here's the non-obvious part. If that method calls another @Transactional method in the same class, Spring skips the proxy. So the second annotation does nothing."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Putting @Transactional on a private method. The proxy can't wrap it, so Spring ignores it.",
              "Calling it from inside the same class. The call bypasses the proxy, so no new transaction starts."
            ]
          }
        ],
        "cap": "@Transactional is handy, but it only works through the proxy. Private methods and self-calls slip right past it.",
        "followup_handoff": [
          "Why doesn't @Transactional work on private methods?",
          "How do you handle the self-invocation problem with @Transactional?"
        ],
        "tts_overrides": {},
        "speakable_status": "approved"
      }
    },
    {
      "id": "jbf-dbdesign-transactions-005",
      "slug": "when-transactions-matter-practical",
      "question": "In practice, when do you actually need a database transaction?",
      "title": "When Do You Need a Database Transaction?",
      "direct_answer": "You need a database transaction whenever two or more operations must succeed or fail together to maintain data consistency. Any multi-step operation where a partial update would leave your data in an inconsistent state requires a transaction. Single operations that don't depend on other operations typically don't need explicit transactions.",
      "layout_type": "concept-explainer",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 2,
      "last_updated": "2026-06-08",
      "interviewer_intent": {
        "testing": "Practical judgment — knowing when to use transactions versus when a single operation doesn't need explicit transaction management.",
        "common_mistake": "Wrapping every single database call in at-Transactional even when there's only one operation — unnecessary overhead and code clutter.",
        "to_stand_out": "Name specific real-world scenarios: order creation with inventory updates, money transfers, user registration with email verification records — and explain why each requires a transaction."
      },
      "company_tags": [
        "amazon",
        "tcs",
        "infosys"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Deciding When Database Transactions Are Necessary",
            "content": "A database **transaction** is necessary when multiple operations must execute as an atomic unit — all succeed or all fail together. The decision hinges on one question: would partial completion leave your data in an **inconsistent state**? If updating table A without updating table B breaks a business rule or corrupts data integrity, you need a transaction. Single operations like saving one entity or running one query typically don't need explicit transactions because each database statement is already atomic. However, coordinating changes across multiple tables, entities, or repositories requires wrapping operations in a transaction.\n\nThink of it like a recipe: if you're making a single dish that doesn't depend on anything else, you don't need coordination. But if you're preparing a multi-course meal where each course must be ready simultaneously, you need orchestration — that's when transactions matter."
          },
          {
            "type": "concept_map",
            "title": "When You Actually Need a Transaction",
            "content": "amber|Multiple writes, one unit|~Two+ tables must agree|order + inventory + payment|all or nothing|the classic case\nblue|Money / inventory|~Correctness is critical|no partial debit|no oversell|always wrap\nemerald|Read-modify-write|~Avoid lost updates|read balance, then update|concurrent users|needs isolation\nviolet|Not needed|~Single-row insert/update|one atomic statement|already atomic|don't over-wrap"
          },
          {
            "type": "code_example",
            "title": "Wrapping a Multi-Step Operation",
            "content": "```java\n@Service\npublic class OrderService {\n    // Both writes must succeed together, or neither should persist\n    @Transactional\n    public void placeOrder(Order order) {\n        inventoryRepo.decrementStock(order.getItemId(), order.getQty());\n        orderRepo.save(order);\n        // If save() throws, decrementStock() is rolled back automatically\n    }\n}\n```\n\nYou need a transaction whenever two or more writes must either all succeed or all fail as a single unit of work."
          },
          {
            "type": "step",
            "title": "Step 1: Identify Multi-Step Operations Requiring Atomicity",
            "content": "Start by asking: does this operation involve multiple writes that depend on each other? **Order creation** typically requires inserting an order record, updating inventory quantities, and creating order line items — all three must succeed or the system is inconsistent. **Money transfers** require debiting one account and crediting another — you can't have money disappear or duplicate. **User registration** might involve creating a user record, saving a profile, and inserting an email verification token — partial completion leaves orphaned data. If the answer is yes, you need a transaction. Single writes like saving one user or fetching one order don't need explicit transactions."
          },
          {
            "type": "step",
            "title": "Step 2: Evaluate the Cost of Inconsistency",
            "content": "Ask: what happens if this operation partially succeeds? If the consequence is **data corruption**, **financial loss**, or **broken business rules**, use a transaction. For example, charging a customer's credit card but not creating their order is a critical failure requiring a transaction. If the consequence is minor and recoverable — like a cache miss or a delayed log entry — a transaction might be overkill. Also consider **concurrency**: if two users updating the same data simultaneously could cause conflicts, you need transaction isolation. Real-time inventory updates under heavy traffic require transactions to prevent overselling."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes Deciding When to Use Transactions",
            "content": "- **Over-transacting**: Wrapping every repository method in at-Transactional even when there's only one database operation — this adds unnecessary overhead and makes code harder to read. Spring Data repositories are already transactional for single operations.\n- **Under-transacting**: Not using transactions for multi-step workflows like order processing, leading to inconsistent data when one step fails.\n- **Ignoring read-only scenarios**: Not marking read-only methods with readOnly equals true, missing performance optimizations for queries that don't modify data."
          },
          {
            "type": "key_points",
            "title": "Key Points About When to Use Transactions",
            "content": [
              "**Multi-step coordination**: Use transactions when two or more operations must succeed or fail together to maintain consistency",
              "**Single operations don't need them**: One repository save or one query doesn't require explicit at-Transactional — already atomic",
              "**Financial operations**: Money transfers, payments, refunds always need transactions to prevent loss or duplication",
              "**Inventory management**: Stock updates coordinated with order creation require transactions to prevent overselling",
              "**User workflows**: Registration, profile setup, permission assignment — multi-table changes need transactions",
              "**Concurrency protection**: High-traffic scenarios where simultaneous updates could conflict need transaction isolation",
              "**Read-only optimization**: Mark query methods with readOnly equals true for better performance, even if single operation"
            ]
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer in an Interview",
            "content": "You need a database transaction whenever several operations have to succeed or fail together to keep your data consistent. The question to ask is simple: would a partial result break something? Let me give concrete cases. Placing an order usually needs one. You save the order, decrement stock, and maybe create line items, and if one step fails the others must not stick. A money transfer needs one, because a debit and a credit have to move together. User registration often needs one, since saving a user but failing to create their profile leaves orphaned data. On the other side, single operations usually don't need an explicit transaction. A single insert or a single read is already atomic, and Spring Data repositories handle that for you. Wrapping every single call in @Transactional is a common fresher mistake, because it adds overhead and holds database connections for no reason. A practical rule: if you call several repository methods and all the changes must be visible together or not at all, use a transaction. If you're doing one independent thing, skip it. Also think about concurrency, since high-traffic updates to the same row may need a transaction to avoid race conditions. In short, use transactions for multi-step writes where consistency matters, and leave plain reads and single writes alone."
          }
        ]
      },
      "followup_questions": [
        "Should @Transactional be on the service layer or the repository layer?"
      ],
      "seo": {
        "metaTitle": "When Do You Need a Database Transaction? — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for In practice, when do you actually need a database transaction? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 5,
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "placing an order: decrement stock and save the order row as one unit",
        "familiarity_anchors": [
          "multi-step write",
          "partial failure",
          "service method",
          "@Transactional"
        ],
        "hook": "The test for a transaction is simple. Could half-finished work corrupt your data?",
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "You need a transaction when several operations must succeed or fail as one. The deciding question is whether a partial result breaks your data. If half-done work leaves things inconsistent, wrap it. If not, you don't need one."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "A single insert or read is already atomic on its own. The risk shows up the moment one action depends on another. That's when a partial failure quietly corrupts your state."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "Multiple writes that must agree, like an order plus its stock update.",
              "Money movement, where a debit and a credit can't drift apart.",
              "Read-modify-write under load, where two users mustn't lose each other's update.",
              "Not needed for one simple insert, update, or read."
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "Take placing an order. You decrement stock, then save the order row. One @Transactional makes both land together, or neither does. Here's the non-obvious part. A plain GET endpoint needs none of this. Wrapping every read in a transaction just holds connections for no reason."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Wrapping single reads in a transaction. It adds overhead and holds connections.",
              "Skipping a transaction on two related writes. A mid-way failure leaves a broken record."
            ]
          }
        ],
        "cap": "Ask what a partial success looks like. If that's a data problem, use a transaction.",
        "followup_handoff": [
          "Should @Transactional go on the service layer or the repository layer?",
          "What is the overhead of a database transaction?"
        ],
        "tts_overrides": {},
        "speakable_status": "approved"
      }
    },
    {
      "id": "jbf-dbdesign-transactions-003",
      "slug": "transaction-isolation-levels",
      "question": "What are the four database transaction isolation levels and what read anomalies do they prevent?",
      "title": "Transaction Isolation Levels Explained",
      "direct_answer": "The four isolation levels are READ UNCOMMITTED, READ COMMITTED, REPEATABLE READ, and SERIALIZABLE. Each prevents progressively more read anomalies: dirty reads, non-repeatable reads, and phantom reads. Higher isolation means better consistency but lower concurrency and performance.",
      "layout_type": "concept-explainer",
      "difficulty": "medium",
      "importance": "high",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-08",
      "interviewer_intent": {
        "testing": "Whether you understand the trade-off between consistency and performance in concurrent database access and can explain what anomalies each level prevents.",
        "common_mistake": "Thinking SERIALIZABLE is always best — it prevents all anomalies but serializes concurrent transactions, severely hurting throughput.",
        "to_stand_out": "Know that MySQL's default is REPEATABLE READ and PostgreSQL's default is READ COMMITTED — and be able to say which anomalies each prevents with examples."
      },
      "company_tags": [
        "amazon",
        "google",
        "microsoft"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Understanding Database Isolation Levels and Read Anomalies",
            "content": "Transaction **isolation levels** define how much one transaction can see changes made by other concurrent transactions. The four standard levels — **READ UNCOMMITTED**, **READ COMMITTED**, **REPEATABLE READ**, and **SERIALIZABLE** — form a spectrum from weakest to strongest isolation.\n\nEach level prevents specific **read anomalies**: dirty reads (reading uncommitted changes), non-repeatable reads (reading different values in the same transaction), and phantom reads (seeing new rows appear mid-transaction). Higher isolation provides stronger consistency guarantees but reduces concurrency because transactions must wait for locks. Lower isolation allows more concurrent access but risks anomalies. Most databases default to READ COMMITTED (PostgreSQL, Oracle) or REPEATABLE READ (MySQL) as a practical balance between consistency and performance."
          },
          {
            "type": "concept_map",
            "title": "Isolation Levels and Anomalies",
            "content": "amber|READ UNCOMMITTED|~Lowest|allows dirty reads|sees uncommitted data|rarely used\nblue|READ COMMITTED|~Postgres default|stops dirty reads|non-repeatable reads possible|solid default\nemerald|REPEATABLE READ|~MySQL default|stops non-repeatable reads|phantoms still possible|stronger\nviolet|SERIALIZABLE|~Highest|stops phantom reads too|acts fully serial|slowest, most locking"
          },
          {
            "type": "code_example",
            "title": "Choosing an Isolation Level",
            "content": "```sql\n-- Higher levels prevent more anomalies at the cost of concurrency\nSET TRANSACTION ISOLATION LEVEL REPEATABLE READ;\n\nSTART TRANSACTION;\nSELECT balance FROM accounts WHERE id = 1;  -- read 1\n-- ... other work ...\nSELECT balance FROM accounts WHERE id = 1;  -- read 2: same value guaranteed\nCOMMIT;\n```\n\nREAD UNCOMMITTED allows dirty reads; READ COMMITTED stops them; REPEATABLE READ also stops non-repeatable reads; SERIALIZABLE stops phantoms too."
          },
          {
            "type": "step",
            "title": "Step 1: Understand the Three Read Anomalies",
            "content": "**Dirty reads** happen when you read uncommitted data from another transaction that might roll back — your data is based on changes that never actually happened. **Non-repeatable reads** occur when you read the same row twice in your transaction and get different values because another transaction modified and committed it in between. **Phantom reads** are when you run the same query twice and see different rows because another transaction inserted or deleted rows matching your criteria. Understanding these anomalies is the foundation for choosing isolation levels — each level prevents a different subset of these problems."
          },
          {
            "type": "step",
            "title": "Step 2: Match Isolation Level to Anomaly Prevention Needs",
            "content": "**READ UNCOMMITTED** prevents nothing — allows all three anomalies but offers maximum concurrency. **READ COMMITTED** prevents dirty reads only — you see only committed data but repeated reads can return different values. **REPEATABLE READ** prevents dirty and non-repeatable reads — the same row always returns the same value within your transaction. **SERIALIZABLE** prevents all three anomalies — transactions execute as if they ran one after another sequentially. Choose based on your consistency requirements: report generation might tolerate non-repeatable reads for speed, while financial calculations might need REPEATABLE READ or SERIALIZABLE to ensure accurate totals."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes with Isolation Levels",
            "content": "- **Defaulting to SERIALIZABLE for everything**: Freshers think the highest isolation is always safest, but SERIALIZABLE can severely hurt performance by forcing transactions to wait, causing timeouts and deadlocks in high-concurrency scenarios.\n- **Not knowing database defaults**: MySQL defaults to REPEATABLE READ while PostgreSQL defaults to READ COMMITTED — assuming one behavior across databases leads to bugs.\n- **Ignoring the performance cost**: Higher isolation levels require more locking, which reduces throughput and can cause contention bottlenecks under load."
          },
          {
            "type": "key_points",
            "title": "Key Points About Isolation Levels",
            "content": [
              "**READ UNCOMMITTED**: No isolation, allows all anomalies, rarely used in practice due to dirty read risks",
              "**READ COMMITTED**: Prevents dirty reads, default for PostgreSQL and Oracle, good balance for most applications",
              "**REPEATABLE READ**: Prevents dirty and non-repeatable reads, default for MySQL, ensures consistent row values",
              "**SERIALIZABLE**: Prevents all anomalies, strongest consistency, but lowest concurrency and performance",
              "**Trade-off principle**: Higher isolation equals better consistency but worse performance and concurrency",
              "**Database differences**: MySQL and PostgreSQL have different defaults and implement levels differently",
              "**Spring configuration**: Set isolation in at-Transactional with isolation equals Isolation.REPEATABLE_READ"
            ]
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer in an Interview",
            "content": "The four isolation levels are READ UNCOMMITTED, READ COMMITTED, REPEATABLE READ, and SERIALIZABLE. They control how much one transaction sees of another's uncommitted changes, and each one blocks more read anomalies. There are three anomalies to know. A dirty read is reading data another transaction hasn't committed yet, data that might get rolled back. A non-repeatable read is reading the same row twice and getting different values because another transaction changed it in between. A phantom read is running the same query twice and seeing new rows appear because another transaction inserted matching data. Now the levels. READ UNCOMMITTED blocks nothing, so it's rarely used. READ COMMITTED stops dirty reads and is PostgreSQL's default. REPEATABLE READ also stops non-repeatable reads and is MySQL's default. SERIALIZABLE stops all three, including phantoms, by making transactions behave as if they ran one at a time. The trade-off freshers miss is that stronger isolation costs throughput, because it holds more locks. So you don't reach for SERIALIZABLE by default. Stick with your database's default for normal work, and only raise the level once you've confirmed a real anomaly, like a phantom row breaking a count. And always know your database's default, since MySQL and PostgreSQL differ."
          }
        ]
      },
      "followup_questions": [
        "What is a dirty read?",
        "What is a phantom read?"
      ],
      "seo": {
        "metaTitle": "Transaction Isolation Levels Explained — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What are the four database transaction isolation levels and what read anomalies do they prevent? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 3,
      "speakable_v2": {
        "archetype": "B",
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "a phantom read scenario at REPEATABLE_READ that only SERIALIZABLE prevents",
        "familiarity_anchors": [
          "dirty read",
          "non-repeatable read",
          "phantom read",
          "SERIALIZABLE prevents all of those"
        ],
        "hook": "Isolation levels are a dial. Turn it up for safety, down for speed.",
        "beats": [
          {
            "kind": "what_each_is",
            "layout": "paragraph",
            "text": "An isolation level sets how much one transaction sees of another's uncommitted work. There are four. READ UNCOMMITTED is the loosest. SERIALIZABLE prevents all of those anomalies and is the strictest."
          },
          {
            "kind": "differences",
            "layout": "mini_table",
            "columns": [
              "Stops these anomalies",
              "Throughput"
            ],
            "rows": [
              {
                "axis": "READ UNCOMMITTED",
                "values": [
                  "Nothing.",
                  "Highest."
                ]
              },
              {
                "axis": "READ COMMITTED",
                "values": [
                  "Dirty reads.",
                  "High."
                ]
              },
              {
                "axis": "REPEATABLE READ",
                "values": [
                  "Dirty and non-repeatable reads.",
                  "Medium."
                ]
              },
              {
                "axis": "SERIALIZABLE",
                "values": [
                  "All anomalies, even phantoms.",
                  "Lowest."
                ]
              }
            ]
          },
          {
            "kind": "when_to_pick",
            "layout": "bullets",
            "items": [
              "Stick with your database default for normal work. It's READ COMMITTED in PostgreSQL, REPEATABLE READ in MySQL.",
              "Move up to SERIALIZABLE only when a phantom row would cause a real bug."
            ]
          },
          {
            "kind": "tiny_example",
            "layout": "paragraph",
            "text": "Run the same SELECT twice in one transaction. At REPEATABLE_READ each row reads the same. But a phantom read can still slip in when another transaction inserts a matching row. The gotcha: only SERIALIZABLE closes that gap."
          }
        ],
        "cap": "Know the four levels and their anomalies. Then raise isolation only once you've proven you need it.",
        "followup_handoff": [
          "What is a phantom read and which isolation level prevents it?",
          "How do you set isolation level in Spring @Transactional?"
        ],
        "tts_overrides": {},
        "speakable_status": "approved"
      }
    },
    {
      "id": "jbf-dbdesign-transactions-004",
      "slug": "transaction-propagation-required-requires-new",
      "question": "What is transaction propagation in Spring and what is the difference between REQUIRED and REQUIRES_NEW?",
      "title": "Spring Transaction Propagation: REQUIRED vs REQUIRES_NEW",
      "direct_answer": "Transaction propagation defines how a transactional method behaves when called from within another transaction. REQUIRED, the default, joins the existing transaction if one is active or starts a new one if not. REQUIRES_NEW always suspends the current transaction and starts a completely independent new transaction.",
      "layout_type": "comparison-arena",
      "difficulty": "medium",
      "importance": "medium",
      "reading_time_minutes": 3,
      "last_updated": "2026-06-08",
      "interviewer_intent": {
        "testing": "Understanding of transaction propagation — shows you know that at-Transactional has behavior beyond just wrapping a method in begin/commit/rollback.",
        "common_mistake": "Not knowing that REQUIRED is the default, or thinking every at-Transactional always creates a new transaction regardless of context.",
        "to_stand_out": "Give a use case for REQUIRES_NEW: audit logging that should be committed even if the main transaction rolls back, or processing individual batch items independently."
      },
      "company_tags": [
        "amazon",
        "google"
      ],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "How Transaction Propagation Controls Nested Transaction Behavior",
            "content": "**Transaction propagation** determines what happens when a transactional method calls another transactional method. Spring supports seven propagation types, but **REQUIRED** and **REQUIRES_NEW** are the most common. **REQUIRED** (the default) means the method participates in the existing transaction if one is active, or starts a new one if not — this creates seamless transaction boundaries across method calls. **REQUIRES_NEW** always suspends the current transaction and starts a completely independent new transaction — changes in the inner transaction commit or roll back independently of the outer transaction.\n\nThink of REQUIRED like carpooling: if someone's already driving (a transaction exists), you join that ride. REQUIRES_NEW is like taking your own car: you're independent regardless of whether others are traveling."
          },
          {
            "type": "concept_map",
            "title": "REQUIRED vs REQUIRES_NEW",
            "content": "amber|REQUIRED|~The default|join the caller's transaction|or start one if none|one shared unit of work\nblue|Rollback link|~Shared fate|inner failure rolls back everything|all-or-nothing across calls|usually what you want\nemerald|REQUIRES_NEW|~Always a new transaction|suspends the caller's|commits independently|separate unit of work\nviolet|Use REQUIRES_NEW for|~Audit logs / notifications|must persist even if outer rolls back|isolated side effects|costs an extra connection"
          },
          {
            "type": "code_example",
            "title": "REQUIRED vs REQUIRES_NEW",
            "content": "```java\n@Service\npublic class PaymentService {\n    // Joins the caller's transaction (or starts one) - shared rollback\n    @Transactional(propagation = Propagation.REQUIRED)\n    public void pay(Order order) {\n        charge(order);\n        auditService.log(order);  // see below\n    }\n}\n\n@Service\npublic class AuditService {\n    // Always its own transaction: the log survives even if pay() rolls back\n    @Transactional(propagation = Propagation.REQUIRES_NEW)\n    public void log(Order order) {\n        auditRepo.save(new AuditEntry(order));\n    }\n}\n```\n\nREQUIRED shares one transaction (and one rollback); REQUIRES_NEW suspends the outer transaction and commits the inner work independently."
          },
          {
            "type": "comparison_table",
            "title": "REQUIRED vs REQUIRES_NEW Propagation",
            "content": {
              "headers": [
                "Aspect",
                "REQUIRED",
                "REQUIRES_NEW"
              ],
              "rows": [
                [
                  "Existing transaction",
                  "Joins the existing transaction",
                  "Suspends existing, starts new one"
                ],
                [
                  "Rollback scope",
                  "Rolls back entire outer transaction",
                  "Rolls back only its own transaction"
                ],
                [
                  "Commit timing",
                  "Commits with outer transaction",
                  "Commits independently, immediately"
                ],
                [
                  "Use case",
                  "Normal coordinated business logic",
                  "Audit logs, independent batch items"
                ],
                [
                  "Default behavior",
                  "Yes, default for at-Transactional",
                  "No, must specify explicitly"
                ]
              ]
            }
          },
          {
            "type": "when_to_use",
            "title": "When to Use Each Propagation Type",
            "content": "Use **REQUIRED** (the default) for normal business logic where all operations should succeed or fail together — order creation calling inventory updates, user registration calling email sending. The entire workflow shares one transaction.\n\nUse **REQUIRES_NEW** when you need an operation to commit independently regardless of the outer transaction's fate. Classic examples: audit logging that must be saved even if the main operation fails, processing batch items where each item's success or failure shouldn't affect others, or recording error details when a transaction is already marked for rollback. REQUIRES_NEW is the exception, not the rule — most service methods should use REQUIRED."
          },
          {
            "type": "common_mistakes",
            "title": "Common Mistakes with Transaction Propagation",
            "content": "- **Not knowing REQUIRED is the default**: Freshers think every at-Transactional starts a new transaction, but by default methods join existing transactions, which is usually what you want.\n- **Overusing REQUIRES_NEW**: Creating independent transactions everywhere breaks atomicity — if orderService calls inventoryService with REQUIRES_NEW, the inventory update commits even if the order creation fails.\n- **Ignoring self-invocation**: Propagation only applies when Spring's proxy intercepts the call — calling a REQUIRES_NEW method from within the same class doesn't create a new transaction."
          },
          {
            "type": "key_points",
            "title": "Key Points About Transaction Propagation",
            "content": [
              "**REQUIRED default**: Most methods use REQUIRED propagation, joining the existing transaction or creating one if needed",
              "**REQUIRES_NEW independence**: Creates a new transaction and suspends the current one, commits and rolls back independently",
              "**Audit logging pattern**: REQUIRES_NEW is perfect for audit logs that must be saved even if the main transaction fails",
              "**Batch processing use case**: Process individual items in REQUIRES_NEW so one failure doesn't roll back all items",
              "**Rollback scope difference**: REQUIRED shares rollback with outer transaction, REQUIRES_NEW isolates its rollback",
              "**Configuration syntax**: Specify with at-Transactional(propagation equals Propagation.REQUIRES_NEW)",
              "**Performance consideration**: REQUIRES_NEW has overhead — it suspends and resumes transactions, use it only when truly needed"
            ]
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer in an Interview",
            "content": "Transaction propagation controls what happens when one transactional method calls another. The two you need are REQUIRED and REQUIRES_NEW. REQUIRED is the default. If a transaction is already running, the method joins it. If not, it starts a new one. So everything runs as one unit, and if any part fails, the whole thing rolls back together. That's what you want most of the time. For example, an order service that saves an order and updates inventory should share one transaction, so both succeed or both fail. REQUIRES_NEW is different. It always suspends the current transaction and starts a fresh, independent one. The inner transaction commits or rolls back on its own, no matter what happens to the outer one. The classic use case is audit logging. Say you want to record every order attempt, even the failed ones. You put REQUIRES_NEW on the logging method, so when it commits, the audit record stays even if the outer order transaction rolls back. Another case is batch processing, where one bad item shouldn't undo the good ones. The mistakes freshers make are not knowing REQUIRED is the default, and overusing REQUIRES_NEW, which quietly breaks atomicity. And remember propagation only works through Spring's proxy, so a self-call inside the same class won't start a new transaction at all."
          }
        ]
      },
      "followup_questions": [
        "What happens to the outer transaction if REQUIRES_NEW inner transaction fails?"
      ],
      "seo": {
        "metaTitle": "Spring Transaction Propagation: REQUIRED vs REQUIRES_NEW — java backend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is transaction propagation in Spring and what is the difference between REQUIRED and REQUIRES_NEW? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "order": 4,
      "speakable_v2": {
        "archetype": "B",
        "pillar": "P03",
        "audience_assumption": "beginner",
        "voice": "friendly",
        "standard_example": "audit logging with REQUIRES_NEW that commits even when the outer transaction rolls back",
        "familiarity_anchors": [
          "REQUIRED",
          "REQUIRES_NEW",
          "audit logging",
          "self-invocation"
        ],
        "hook": "Propagation decides if a nested call joins your transaction or spins up its own.",
        "beats": [
          {
            "kind": "what_each_is",
            "layout": "paragraph",
            "text": "Propagation is what a transactional method does when it's called inside another transaction. REQUIRED is the default. REQUIRES_NEW is the main alternative. They differ in whether the inner work shares the outer's fate."
          },
          {
            "kind": "differences",
            "layout": "mini_table",
            "columns": [
              "REQUIRED",
              "REQUIRES_NEW"
            ],
            "rows": [
              {
                "axis": "Transaction",
                "values": [
                  "Joins the existing one.",
                  "Always starts a new one."
                ]
              },
              {
                "axis": "Outer rollback",
                "values": [
                  "Rolls the inner back too.",
                  "Leaves the inner committed."
                ]
              },
              {
                "axis": "Best for",
                "values": [
                  "One shared operation.",
                  "Independent side effects."
                ]
              }
            ]
          },
          {
            "kind": "when_to_pick",
            "layout": "bullets",
            "items": [
              "Pick REQUIRED for work that belongs together. Publishing a post and bumping the author's count should share one transaction.",
              "Pick REQUIRES_NEW when the inner work must survive on its own. Audit logging a failed attempt is the classic case."
            ]
          },
          {
            "kind": "tiny_example",
            "layout": "paragraph",
            "text": "Say publishing a post fails and rolls back. If the audit log used REQUIRES_NEW, that record still commits. So you keep a trail of the attempt. The gotcha: a self-call skips the proxy, so propagation won't even apply."
          }
        ],
        "cap": "REQUIRED fits almost everything. Reach for REQUIRES_NEW only when an operation must commit no matter what.",
        "followup_handoff": [
          "What happens to the outer transaction if REQUIRES_NEW inner transaction throws an exception?",
          "What is the NESTED propagation type?"
        ],
        "tts_overrides": {},
        "speakable_status": "approved"
      }
    }
  ]
}

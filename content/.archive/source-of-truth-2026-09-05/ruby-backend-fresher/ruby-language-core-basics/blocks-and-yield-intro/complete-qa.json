{
  "topic": "Blocks and Yield",
  "topicSlug": "blocks-and-yield-intro",
  "questions": [
    {
      "id": "blocks-and-yield-intro-q001",
      "slug": "ruby-blocks-and-yield-intro",
      "question": "What are blocks and yield in Ruby?",
      "title": "Blocks and Yield in Ruby",
      "direct_answer": "A Ruby block is executable code passed with a method call, written with `{ ... }` or `do ... end`. The receiving method runs it with `yield` and may pass values into it. Blocks power iterators, resource helpers, callbacks, and DSLs. Use `block_given?` when the block is optional; capture it as a `Proc` with `&block` only when you need to store or forward it.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 6,
      "last_updated": "2026-09-07",
      "interviewer_intent": {
        "testing": "Whether you understand how a method receives and invokes a block, including arguments, return values, closure behavior, and optional blocks.",
        "common_mistake": "Calling `yield` when no block was supplied, or changing `{}` to `do...end` without considering precedence.",
        "to_stand_out": "Explain that the normal block is implicit; `&block` converts it to a `Proc` only when the method must store, inspect, or forward it."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "Learning goal",
            "content": "Follow the handoff from caller to method: the caller supplies behavior, the method decides when to run it, and `yield` transfers control to that behavior."
          },
          {
            "type": "key_points",
            "title": "Quick revision",
            "items": [
              "A method call can receive one implicit block written as `{ ... }` or `do ... end`.",
              "`yield(value)` runs the block with `value` and returns the block's result.",
              "`block_given?` tells a method whether the caller supplied a block.",
              "Blocks are closures, so they can read variables from the surrounding scope.",
              "`&block` turns the implicit block into a `Proc` when it must be stored or forwarded."
            ]
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "- A block is a piece of behavior attached to a method call. The caller writes it with braces or `do...end`, and the method controls when and how often that code runs. This is why `each` can own the iteration while the caller supplies only what to do for each item.\n\n- Inside the method, `yield` transfers control to the block. `yield 10` passes `10` to the block parameter, and the value produced by the block becomes the result of that `yield` expression. The method can use that result or yield again.\n\n- For example, a method `transform` can call `yield 10`; `transform { |number| number * 2 }` then produces `20`. The method provides the workflow, while the block supplies the changing part.\n\n- A block is optional only if the method handles that case. Calling `yield` without a supplied block raises `LocalJumpError`, so an optional workflow checks `block_given?` or returns an enumerator.\n\n- Blocks are closures, which means they can use surrounding local variables. Ruby uses this naturally for iterators, callbacks, transactions, file handling, and DSL configuration.\n\n- I use plain `yield` when the method only needs to run the block. I capture `&block` as a `Proc` when I need to pass it to another method, store it, or call Proc methods. Braces bind more tightly than `do...end`, so in a chained call I make the block's owner explicit."
          },
          {
            "type": "deep_explanation",
            "title": "The method owns the workflow",
            "content": "A block lets a method keep the stable part of an operation and ask the caller for the variable part. `each` owns traversal, `File.open` owns opening and closing a file, and a transaction helper owns commit or rollback. The attached block says what should happen inside that workflow.\n\nThe receiving method chooses when to yield, what arguments to pass, and what to do with the returned value."
          },
          {
            "type": "flow_diagram",
            "title": "Control moves to the block and back",
            "content": "```mermaid\nsequenceDiagram\n  participant C as Caller\n  participant M as Method\n  participant B as Block\n  C->>M: transform with a block\n  M->>B: yield 10\n  B-->>M: 20\n  M-->>C: Result: 20\n```\n\nThe block receives `10`, uses any captured values, and returns `20` to the point where the method yielded."
          },
          {
            "type": "comparison_table",
            "title": "Block, yield, and Proc are different roles",
            "content": "| Term | Role | Example |\n|---|---|---|\n| Block | Behavior attached to a method call | `numbers.map { |n| n * 2 }` |\n| `yield` | Runs the implicit block from inside the method | `result = yield(10)` |\n| `block_given?` | Checks whether a block exists | `return unless block_given?` |\n| `&block` | Captures the block as a `Proc` object | `items.each(&block)` |"
          },
          {
            "type": "code_example",
            "title": "Build a small block-driven method",
            "content": "```ruby\ndef transform(value)\n  return enum_for(__method__, value) unless block_given?\n\n  result = yield(value)\n  \"Result: #{result}\"\nend\n\nfactor = 2\nputs transform(10) { |number| number * factor }\n# Result: 20\n```\n\n`factor` comes from the caller's surrounding scope, so the block is a closure. `yield(value)` passes `10` in, the block returns `20`, and the method wraps that result."
          },
          {
            "type": "common_mistakes",
            "title": "The precedence trap",
            "content": "Braces bind more tightly to the method immediately before them, while `do...end` has lower precedence. In a chained call, `puts numbers.map { |n| n * 2 }` passes the block to `map`, but changing the block form without parentheses can attach it to a different call. Use parentheses or split the expression when ownership is not obvious."
          },
          {
            "type": "practice_prompt",
            "title": "Check the control flow",
            "content": "In `wrap(5) { |n| n + 1 }`, if `wrap` executes `value = yield(5)`, what is `value`? **6**. The argument travels from `yield` into `n`, and the block's last expression travels back as the result of `yield`."
          }
        ]
      },
      "followup_questions": [
        "What happens when a method calls `yield` without receiving a block?",
        "When should a method use `yield` instead of capturing `&block`?",
        "How do `{}` and `do...end` differ when a call contains several methods?"
      ],
      "seo": {
        "metaTitle": "Ruby Blocks and yield Explained with Examples | InterviewExplainer",
        "metaDescription": "Learn how Ruby blocks, yield, block_given?, closures, and &block work with a control-flow diagram, examples, and an interview-ready explanation."
      },
      "order": 1
    }
  ]
}

{
  "topic": "Scenario Based",
  "topicSlug": "scenario-based",
  "questions": [
    {
      "id": "con-012",
      "slug": "parallel-streams-when-they-help-vs-hurt",
      "title": "Parallel Streams \u2014 When They Help vs Hurt",
      "question": "When do Java parallel streams improve performance, and when do they actually hurt it?",
      "direct_answer": "Parallel streams split a data source across the ForkJoinPool.commonPool() and merge results, giving a speedup only when the data set is large, the work per element is significant (CPU-bound), and the source supports efficient splitting. They hurt performance when the overhead of task decomposition, merging, and thread coordination outweighs the savings \u2014 particularly for small collections, I/O-bound tasks, sequential data sources like LinkedList, or operations with shared mutable state.",
      "layout_type": "concept-explainer",
      "difficulty": "intermediate",
      "reading_time_minutes": 5,
      "last_updated": "2025-01-15",
      "company_tags": [
        "Amazon",
        "Google",
        "Palantir",
        "Databricks"
      ],
      "interviewer_intent": {
        "testing": "Whether the candidate understands the conditions under which parallelism actually pays off, rather than assuming parallelStream() is always faster.",
        "common_mistake": "Adding parallelStream() to small collections or I/O operations, adding overhead with no benefit \u2014 or worse, blocking the shared ForkJoinPool with I/O and starving other tasks.",
        "to_stand_out": "Mention the NQ model (N = elements, Q = work per element), the SIZED and SUBSIZED spliterator characteristics required for efficient splitting, and how to use a custom ForkJoinPool to isolate parallel work from the common pool."
      },
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Parallel Streams Use the Common ForkJoinPool \u2014 And Blocking Operations There Starve Your Entire Application",
            "content": "Calling `.parallel()` on a stream splits the workload across multiple threads using the **common ForkJoinPool**. This is the same pool used by CompletableFuture's default async methods. A slow parallel stream operation can **starve** all other concurrent work sharing this pool.\n\nParallel streams improve throughput only when three conditions are met: the data source **splits cheaply** (arrays and ArrayLists split in O(1); LinkedLists require O(n) traversal to find the midpoint), the per-element operation is **CPU-intensive** enough to justify the overhead (thread management, task splitting, result merging), and there are **enough elements** to amortize the fixed costs.\n\nFor small collections (under ~10,000 elements), the overhead of splitting and merging typically exceeds the parallelism benefit. For I/O-bound operations (HTTP calls, database queries), parallel streams are **actively harmful** \u2014 they block common pool threads, reducing available parallelism for the entire JVM.\n\nThe ordering guarantee is another hidden cost. `parallelStream().forEachOrdered()` forces sequential ordering of results, eliminating most parallelism benefit. Similarly, `limit()` and `findFirst()` on parallel streams perform poorly because they must coordinate across threads to maintain encounter order."
          },
          {
            "type": "step",
            "title": "When Parallel Streams Help and When They Hurt: The Decision Checklist",
            "content": "Before using `.parallel()`, check four conditions.\n\n**1. Data source splits cheaply.** Arrays and ArrayLists split in O(1) \u2014 the array is divided by index. LinkedList requires O(n) traversal. HashSet and TreeSet split reasonably. Streams from `Files.lines()` or `BufferedReader` split poorly.\n\n**2. Operation is CPU-bound.** Math-heavy transformations, encryption, compression, complex object construction benefit from parallelism. Simple field access, string concatenation, or list addition do not \u2014 the overhead exceeds the benefit.\n\n**3. Collection is large enough.** Below ~10,000 elements, the fixed cost of thread management and result merging dominates. Profile to find the breakeven for your specific operation.\n\n**4. No shared mutable state.** Parallel stream operations must be stateless and non-interfering. Accumulating into a shared ArrayList without synchronization causes data corruption \u2014 use `collect()` with thread-safe collectors instead.\n\n```java\n// Good: CPU-intensive on large array\nlong sum = LongStream.rangeClosed(1, 10_000_000)\n    .parallel()\n    .map(n -> expensiveComputation(n))\n    .sum();\n\n// Bad: I/O-bound \u2014 blocks common pool threads\nlist.parallelStream()\n    .map(id -> httpClient.fetch(id))  // blocks pool threads\n    .collect(toList());\n```\n\nTo isolate parallel stream work from the common pool, submit the stream operation to a **custom ForkJoinPool**: `new ForkJoinPool(4).submit(() -> stream.parallel()...collect()).join()`."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Splits work across the common ForkJoinPool** \u2014 the same pool shared with CompletableFuture; a slow stream starves other async work.\n- **Three requirements: large dataset, CPU-intensive, splittable source** \u2014 10,000+ elements; no I/O; ArrayList not LinkedList.\n- **`sorted()` and `limit()` force sequential coordination** \u2014 negate most of the parallelism benefit.\n- **I/O in parallel streams blocks pool threads** \u2014 use CompletableFuture with a dedicated executor instead.\n- **For isolation: `new ForkJoinPool(4).submit(() -> stream.parallel()...)`**."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "Parallel streams split work across the common ForkJoinPool \u2014 the same pool shared with CompletableFuture, making them dangerous when used carelessly.\n\n**Three conditions for parallel streams to help:**\n- Data source splits cheaply: arrays and ArrayLists are O(1); LinkedList is O(n) \u2014 terrible for parallel splitting\n- Operation must be CPU-intensive enough to justify overhead \u2014 roughly 10,000+ elements minimum\n- No I/O \u2014 blocking pool threads without CPU benefit actively harms throughput\n\n**Common pitfalls:**\n- `forEachOrdered()` and `limit()` force sequential coordination \u2014 kills most of the parallelism benefit\n- I/O-bound operations in parallel streams block pool threads \u2014 use CompletableFuture with a dedicated executor instead\n\n**Isolation from common pool:**\n- `new ForkJoinPool(4).submit(() -> stream.parallel()...collect()).join()`\n- Prevents a slow parallel stream from starving all other async work in the JVM\n\nI recommend parallel streams only for CPU-heavy computation on large arrays or ArrayLists. For I/O-bound work, CompletableFuture with explicit thread pools is safer and more controllable."
          }
        ]
      },
      "followup_questions": [
        "How does ForkJoinPool work-stealing enable parallel stream splitting?",
        "What Spliterator characteristics are needed for efficient parallel splitting?",
        "How do you benchmark a parallel stream correctly using JMH?"
      ],
      "seo": {
        "metaTitle": "Java Parallel Streams When to Use | Concurrency Interview Guide",
        "metaDescription": "Learn when Java parallel streams help vs hurt: NQ model, good vs bad use cases, ForkJoinPool, and I/O pitfalls. Intermediate concurrency interview prep."
      },
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P01",
        "hook": "Parallel streams split work across the common ForkJoinPool \u2014 the same pool shared with CompletableFuture, making them dangerous when used carelessly.",
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "Calling .parallel() on a stream splits the workload across multiple threads using the common ForkJoinPool. This is the same pool used by CompletableFuture's default async methods. A slow parallel stream operation can starve all other concurrent work sharing this pool. Parallel streams improve throughput only when three conditions are met: the data source splits cheaply (arrays and ArrayLists split in O(1); LinkedLists require O(n) traversal to find the midpoint), the per-element operation is CPU-intensive enough to justify the overhead (thread."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "Whether the candidate understands the conditions under which parallelism actually pays off, rather than assuming parallelStream() is always faster."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "Splits work across the common ForkJoinPool \u2014 the same pool shared with CompletableFuture; a slow stream starves other async work",
              "Three requirements: large dataset, CPU-intensive, splittable source \u2014 10,000+ elements; no I/O; ArrayList not LinkedList",
              "sorted() and limit() force sequential coordination \u2014 negate most of the parallelism benefit",
              "I/O in parallel streams blocks pool threads \u2014 use CompletableFuture with a dedicated executor instead",
              "For isolation: new ForkJoinPool(4)"
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "Calling .parallel() on a stream splits the workload across multiple threads using the common ForkJoinPool. This is the same pool used by CompletableFuture's default async methods. A slow parallel stream operation can starve all other concurrent work sharing this pool. Parallel streams improve throughput only when three conditions are met: the data source splits cheaply (arrays and ArrayLists split in O(1); LinkedLists require O(n) traversal to find the midpoint), the per-element operation is CPU-intensive enough to justify the overhead (thread management, task splitting, result merging), and there are enough elements to amortize the fixed costs. For small collections (under ~10,000 elements), the overhead of splitting and merging typically exceeds the parallelism benefit. For I/O-bound operations (HTTP calls, database queries), parallel."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Adding parallelStream() to small collections or I/O operations, adding overhead with no benefit \u2014 or worse, blocking the shared ForkJoinPool with I/O and starving other tasks"
            ]
          }
        ],
        "cap": "For I/O-bound work, CompletableFuture with explicit thread pools is safer and more controllable.",
        "familiarity_anchors": [
          "Parallel",
          "ForkJoinPool",
          "LinkedList"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": [
          "How does ForkJoinPool work-stealing enable parallel stream splitting?",
          "What Spliterator characteristics are needed for efficient parallel splitting?",
          "How do you benchmark a parallel stream correctly using JMH?"
        ]
      }
    },
    {
      "id": "con-011",
      "slug": "race-condition-identify-and-fix",
      "title": "Race Condition \u2014 How to Identify and Fix",
      "question": "What is a race condition in Java, how do you identify one, and what are the standard ways to fix it?",
      "direct_answer": "A race condition occurs when a program's correctness depends on the relative timing of thread executions, producing inconsistent results that vary non-deterministically between runs. They arise whenever multiple threads access shared mutable state and at least one access is a write, without proper synchronisation. Fixes include synchronisation, atomic variables, immutable data, or confining state to a single thread.",
      "layout_type": "problem-detective",
      "difficulty": "intermediate",
      "reading_time_minutes": 6,
      "last_updated": "2025-01-15",
      "company_tags": [
        "Amazon",
        "Stripe",
        "Twilio",
        "Cloudflare"
      ],
      "interviewer_intent": {
        "testing": "Whether the candidate can recognise the check-then-act and read-modify-write patterns as race condition triggers.",
        "common_mistake": "Fixing only one access point (adding synchronized to the write but not the read, or vice versa), leaving the race condition partially intact.",
        "to_stand_out": "Mention that races are easiest to find with thread sanitizers or tools like Java PathFinder, Helgrind, or careful code review for non-atomic compound operations."
      },
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Race Conditions Pass Every Test and Fail in Production \u2014 Because They Depend on Timing You Cannot Control",
            "content": "A **race condition** occurs when the correctness of a program depends on the relative timing of two or more threads \u2014 and the timing is not controlled by synchronization. The program produces different results depending on which thread reaches a critical section first.\n\nThe most insidious property of race conditions is that they are **non-deterministic**. They pass unit tests reliably because single-threaded test execution has consistent timing. They fail intermittently in production because CPU scheduling, GC pauses, and load variations change the thread interleaving.\n\nThe two most common patterns: **check-then-act** (check a condition, then act on it \u2014 another thread changes the condition between check and act) and **read-modify-write** (read a value, compute a new value, write it \u2014 another thread writes between your read and write).\n\nCheck-then-act: `if (map.containsKey(key)) { return map.get(key); }` \u2014 between `containsKey()` and `get()`, another thread can remove the key. The fix: use `map.getOrDefault(key, defaultValue)` or `computeIfAbsent()`.\n\nRead-modify-write: `counter++` \u2014 read 5, compute 6, write 6. Two threads both read 5, both write 6 \u2014 one increment is lost. The fix: `AtomicInteger.incrementAndGet()` or `synchronized`."
          },
          {
            "type": "step",
            "title": "Identifying Race Conditions: Code Review Patterns and Tool-Based Detection",
            "content": "Race conditions are difficult to reproduce but have recognizable code patterns.\n\n**Pattern 1: Compound operations without synchronization.** Any sequence of `get()` followed by `put()` on a shared map, `isEmpty()` followed by `remove()` on a shared queue, or `size()` followed by conditional logic. Each operation is individually atomic, but the pair is not.\n\n**Pattern 2: Lazy initialization without synchronization.** `if (instance == null) { instance = new Singleton(); }` \u2014 two threads see null, both create instances. Fix with double-checked locking (volatile + synchronized) or holder class idiom.\n\n**Pattern 3: Publishing mutable state without happens-before.** Storing an object in a field visible to other threads without volatile or synchronized. The reading thread may see a partially constructed object due to instruction reordering.\n\nTools for detection: **ThreadSanitizer** (compile-time instrumentation for C/JNI), **Java Concurrency Stress Tests (jcstress)** \u2014 a framework that runs code with aggressive thread scheduling to expose races. **FindBugs/SpotBugs** static analysis detects common patterns like unsynchronized access to shared fields.\n\n```java\n// Race condition: check-then-act\nif (!map.containsKey(key)) {\n    map.put(key, computeValue()); // another thread may put between check and act\n}\n// Fix: atomic operation\nmap.computeIfAbsent(key, k -> computeValue());\n```\n\nIn code reviews, flag any shared mutable state accessed without synchronization, atomic operations, or volatile. The absence of these is a race condition until proven otherwise."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Race condition = correctness depends on uncontrolled thread timing** \u2014 passes tests, fails in production under load.\n- **Check-then-act pattern** \u2014 `containsKey()` then `get()` races; fix with `computeIfAbsent()`.\n- **Read-modify-write** \u2014 `counter++` is three non-atomic steps; fix with `AtomicInteger.incrementAndGet()`.\n- **`volatile` doesn't fix compound operations** \u2014 ensures visibility, not atomicity; `counter++` on volatile still races.\n- **Detection: `jcstress` for aggressive scheduling tests; SpotBugs for static analysis**."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "A race condition means program correctness depends on thread timing that synchronization doesn't control \u2014 they pass tests and silently fail in production.\n\n**Two main patterns:**\n- Check-then-act: `containsKey()` then `get()` \u2014 another thread removes between the two calls; fix with `computeIfAbsent()`\n- Read-modify-write: `counter++` reads, increments, writes in three non-atomic steps \u2014 two threads can interleave; fix with `AtomicInteger.incrementAndGet()`\n\n**volatile doesn't fix it:**\n- `volatile` ensures visibility but not atomicity \u2014 `counter++` on a volatile int still loses updates under concurrent access\n\n**Detection:**\n- `jcstress`: tests with aggressive scheduling to surface races in controlled environments\n- SpotBugs: static analysis for unsynchronized field access patterns\n- Code review: flag any shared mutable state accessed without synchronization, atomic, or single-writer guarantee\n\nI recommend treating shared mutable state as guilty until proven safe \u2014 every access needs synchronization, an atomic operation, or a volatile with a single-writer guarantee."
          }
        ]
      },
      "followup_questions": [
        "What is the difference between a race condition and a data race?",
        "How would you write a stress test to expose a race condition reliably?",
        "Can immutable objects eliminate race conditions entirely?"
      ],
      "seo": {
        "metaTitle": "Java Race Condition Identify and Fix | Concurrency Interview",
        "metaDescription": "Learn to identify and fix Java race conditions: check-then-act, read-modify-write patterns, synchronized, AtomicInteger, and single-thread confinement fixes."
      },
      "speakable_v2": {
        "archetype": "C",
        "pillar": "P01",
        "hook": "A race condition means program correctness depends on thread timing that synchronization doesn't control \u2014 they pass tests and silently fail in production.",
        "beats": [
          {
            "kind": "mental_model",
            "layout": "paragraph",
            "text": "A race condition occurs when the correctness of a program depends on the relative timing of two or more threads \u2014 and the timing is not controlled by synchronization. The program produces different results depending on which thread reaches a critical section first. The most insidious property of race conditions is that they are non-deterministic. They pass unit tests reliably because single-threaded test execution has consistent timing. They fail intermittently in production because CPU scheduling, GC pauses, and load variations change the thread interleaving. The two most common patterns: check-then-act."
          },
          {
            "kind": "mechanism",
            "layout": "ordered_list",
            "steps": [
              "Race conditions are difficult to reproduce but have recognizable code patterns.",
              "Pattern 1: Compound operations without synchronization.",
              "Any sequence of get() followed by put() on a shared map, isEmpty() followed by remove() on a shared queue, or size() followed by conditional logic.",
              "Each operation is individually atomic, but the pair is not.",
              "Pattern 2: Lazy initialization without synchronization.",
              "if (instance == null) { instance = new Singleton(); } \u2014 two threads see null, both create instances."
            ]
          },
          {
            "kind": "edge_cases",
            "layout": "bullets",
            "items": [
              "Race condition = correctness depends on uncontrolled thread timing \u2014 passes tests, fails in production under load",
              "Check-then-act pattern \u2014 containsKey() then get() races; fix with computeIfAbsent()",
              "Read-modify-write \u2014 counter++ is three non-atomic steps; fix with AtomicInteger",
              "incrementAndGet()",
              "volatile doesn't fix compound operations \u2014 ensures visibility, not atomicity; counter++ on volatile still races"
            ]
          },
          {
            "kind": "failure_mode",
            "layout": "callout",
            "text": "Fixing only one access point (adding synchronized to the write but not the read, or vice versa), leaving the race condition partially intact."
          }
        ],
        "cap": "Two main patterns: Check-then-act: containsKey() then get() \u2014 another thread removes between the two calls; fix with computeIfAbsent() Read-modify-write: counter++ reads, increments, writes in three n",
        "familiarity_anchors": [
          "Fixes"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": [
          "What is the difference between a race condition and a data race?",
          "How would you write a stress test to expose a race condition reliably?",
          "Can immutable objects eliminate race conditions entirely?"
        ]
      }
    },
    {
      "id": "cmt-011",
      "slug": "liveness-hazards-deadlock-livelock-starvation",
      "title": "Liveness Hazards: Deadlock, Livelock, and Starvation",
      "question": "Explain deadlock, livelock, and starvation in Java concurrency. How do you detect and prevent each?",
      "difficulty": "intermediate",
      "reading_time_minutes": 5,
      "last_updated": "2025-01-15",
      "layout_type": "concept-explainer",
      "company_tags": [
        "amazon",
        "microsoft",
        "jpmorgan",
        "google"
      ],
      "interviewer_intent": {
        "testing": "Ability to identify and reason about liveness failures \u2014 a sign of production concurrency experience.",
        "common_mistake": "Conflating deadlock (stuck forever) with livelock (threads keep running but make no progress). Both prevent progress but have very different root causes and detection strategies.",
        "to_stand_out": "Mention lock ordering as the canonical deadlock prevention strategy, jstack/ThreadMXBean for runtime detection, and that tryLock() with timeout can break deadlocks at the cost of retry logic."
      },
      "direct_answer": "Deadlock: threads wait forever in a cycle of lock dependencies. Livelock: threads keep changing state responding to each other but never progress. Starvation: a thread never gets CPU time or a lock because others always take priority. All three prevent useful work without necessarily crashing the JVM.",
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Deadlock Is Two Threads Each Holding What the Other Needs \u2014 But Livelock and Starvation Are Harder to Diagnose",
            "content": "Two threads, each holding what the other needs \u2014 **deadlock** is the most recognizable liveness hazard, but not the only one. Thread A holds Lock 1 and waits for Lock 2. Thread B holds Lock 2 and waits for Lock 1. Neither can proceed. The JVM detects this in thread dumps \u2014 look for \"waiting to lock\" cycles.\n\n**Livelock** is more subtle: threads are not blocked, they are actively executing \u2014 but they keep **changing state in response to each other** without making progress. Two people in a hallway stepping left and right simultaneously to avoid each other. The threads consume CPU but accomplish nothing, and no thread dump reveals the problem because no thread is BLOCKED or WAITING.\n\n**Starvation** means a thread is perpetually denied the resource it needs. With unfair locks, a continuously arriving stream of threads can barge ahead of a waiting thread indefinitely. The starved thread is technically in a WAITING state but never progresses. Starvation is especially dangerous because the system appears to work \u2014 all other threads are making progress.\n\nDeadlock requires four conditions simultaneously: **mutual exclusion** (resources are non-shareable), **hold and wait** (thread holds one resource while waiting for another), **no preemption** (resources cannot be forcibly taken), and **circular wait** (A waits for B, B waits for A). Breaking any one condition prevents deadlock."
          },
          {
            "type": "step",
            "title": "Prevention Strategies: Lock Ordering, Timeout, and Detection",
            "content": "Each liveness hazard has a different prevention strategy.\n\n**Deadlock prevention**: enforce a **global lock ordering**. If all threads always acquire Lock 1 before Lock 2, circular wait is impossible. For dynamic lock ordering (e.g., transferring between two bank accounts), use `System.identityHashCode()` to establish a consistent order.\n\n```java\npublic void transfer(Account a, Account b, int amount) {\n    Account first = System.identityHashCode(a) < System.identityHashCode(b) ? a : b;\n    Account second = first == a ? b : a;\n    synchronized (first) {\n        synchronized (second) {\n            a.debit(amount);\n            b.credit(amount);\n        }\n    }\n}\n```\n\n**Deadlock detection**: use `ReentrantLock.tryLock(timeout)` instead of `synchronized`. If the lock cannot be acquired within the timeout, release all held locks and retry \u2014 breaking the \"hold and wait\" condition.\n\n**Livelock prevention**: introduce **randomized backoff**. When a thread detects conflict, it waits a random duration before retrying \u2014 breaking the synchronized stepping pattern.\n\n**Starvation prevention**: use **fair locks** (`new ReentrantLock(true)`) that guarantee FIFO ordering. The trade-off is reduced throughput because every acquisition checks the wait queue instead of allowing barging.\n\nFor deadlock diagnosis, use `jstack <pid>` or `ManagementFactory.getThreadMXBean().findDeadlockedThreads()`. Thread dumps show the lock dependency cycle explicitly."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Deadlock** \u2014 circular wait; Thread A holds Lock 1, waits for Lock 2; Thread B vice versa; neither proceeds.\n- **Fix: lock ordering** \u2014 all threads acquire locks in the same global order; circular wait condition can't form.\n- **Livelock** \u2014 threads actively running but reacting to each other without progress; fix with randomized backoff.\n- **Starvation** \u2014 thread perpetually denied resources by unfair lock; fix with fair mode (FIFO ordering).\n- **`jstack` detects deadlock cycles automatically** \u2014 prints \"Found N deadlock\" section at the bottom of the dump."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "Deadlock, livelock, and starvation are three distinct failure modes \u2014 they differ in what the threads are doing, how you diagnose them, and how you prevent them.\n\n**Deadlock:**\n- Two threads each holding what the other needs \u2014 Thread A holds Lock 1 and waits for Lock 2; Thread B holds Lock 2 and waits for Lock 1; neither proceeds\n- Fix with lock ordering: all threads acquire locks in the same global order \u2014 circular wait condition can't form\n- Diagnosis: `jstack` automatically detects monitor cycles and prints \"Found N deadlock\" at the bottom\n\n**Livelock:**\n- Threads are not blocked \u2014 they're actively running but keep reacting to each other without making progress\n- Like two people stepping left and right in a hallway, each trying to let the other pass\n- Fix with randomized backoff \u2014 break the symmetry\n\n**Starvation:**\n- A thread is perpetually denied resources \u2014 unfair locks let new arrivals barge ahead of waiting threads indefinitely\n- Fix with fair locks or explicit priority management \u2014 trades throughput for guaranteed FIFO ordering\n\n**Prevention tools:**\n- `tryLock(timeout)` breaks hold-and-wait by releasing locks on timeout \u2014 essential for dynamic locking scenarios\n- `ThreadMXBean.findDeadlockedThreads()` for programmatic detection in health-check endpoints\n\nI recommend lock ordering as the primary prevention strategy and `tryLock` with timeouts as the fallback."
          }
        ]
      },
      "followup_questions": [
        "What are the four Coffman conditions for deadlock, and which is easiest to break in practice?",
        "How does tryLock() with a timeout help avoid deadlock? What are its drawbacks?",
        "How can priority inversion cause starvation, and what is the classic solution?"
      ],
      "seo": {
        "meta_title": "Java Deadlock Livelock Starvation Explained | Concurrency Interview",
        "meta_description": "Understand deadlock, livelock, and starvation in Java with code examples, prevention strategies, and runtime detection using ThreadMXBean.",
        "keywords": [
          "java deadlock livelock starvation",
          "java concurrency liveness hazards",
          "detect deadlock java threadmxbean"
        ]
      },
      "speakable_v2": {
        "archetype": "C",
        "pillar": "P01",
        "hook": "Deadlock, livelock, and starvation are three distinct failure modes \u2014 they differ in what the threads are doing, how you diagnose them, and how you prevent them.",
        "beats": [
          {
            "kind": "mental_model",
            "layout": "paragraph",
            "text": "Two threads, each holding what the other needs \u2014 deadlock is the most recognizable liveness hazard, but not the only one. Thread A holds Lock 1 and waits for Lock 2. Thread B holds Lock 2 and waits for Lock 1. Neither can proceed. The JVM detects this in thread dumps \u2014 look for \"waiting to lock\" cycles. Livelock is more subtle: threads are not blocked, they are actively executing \u2014 but they keep changing state in response to each other without making progress. Two people in a hallway stepping."
          },
          {
            "kind": "mechanism",
            "layout": "ordered_list",
            "steps": [
              "Each liveness hazard has a different prevention strategy.",
              "Deadlock prevention: enforce a global lock ordering.",
              "If all threads always acquire Lock 1 before Lock 2, circular wait is impossible.",
              "For dynamic lock ordering (e.g., transferring between two bank accounts), use System.identityHashCode() to establish a consistent order.",
              "``java public void transfer(Account a, Account b, int amount) { Account first = System.identityHashCode(a) < System.identityHashCode(b) ?",
              "a : b; Account second = first == a ?"
            ]
          },
          {
            "kind": "edge_cases",
            "layout": "bullets",
            "items": [
              "Deadlock \u2014 circular wait; Thread A holds Lock 1, waits for Lock 2; Thread B vice versa; neither proceeds",
              "Fix: lock ordering \u2014 all threads acquire locks in the same global order; circular wait condition can't form",
              "Livelock \u2014 threads actively running but reacting to each other without progress; fix with randomized backoff",
              "Starvation \u2014 thread perpetually denied resources by unfair lock; fix with fair mode (FIFO ordering)",
              "jstack detects deadlock cycles automatically \u2014 prints \"Found N deadlock\" section at the bottom of the dump"
            ]
          },
          {
            "kind": "failure_mode",
            "layout": "callout",
            "text": "Conflating deadlock (stuck forever) with livelock (threads keep running but make no progress). Both prevent progress but have very different root causes and detection strategies."
          }
        ],
        "cap": "Deadlock: Two threads each holding what the other needs \u2014 Thread A holds Lock 1 and waits for Lock 2; Thread B holds Lock 2 and waits for Lock 1; neither proceeds Fix with lock.",
        "familiarity_anchors": [
          "Deadlock",
          "Livelock",
          "Starvation"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": [
          "What are the four Coffman conditions for deadlock, and which is easiest to break in practice?",
          "How does tryLock() with a timeout help avoid deadlock? What are its drawbacks?",
          "How can priority inversion cause starvation, and what is the classic solution?"
        ]
      }
    }
  ]
}
{
  "topic": "Grpc Basics",
  "topicSlug": "grpc-basics",
  "questions": [
    {
      "id": "grpc-003",
      "slug": "grpc-service-types-java",
      "question": "What are the four gRPC service types and when do you use each?",
      "title": "gRPC Service Types \u2014 Unary, Server Streaming, Client Streaming, Bidirectional",
      "direct_answer": "gRPC supports four RPC patterns: unary (one request, one response \u2014 like a normal function call), server streaming (one request, stream of responses \u2014 e.g. live updates), client streaming (stream of requests, one response \u2014 e.g. file upload), and bidirectional streaming (both sides stream concurrently \u2014 e.g. chat or real-time trading feeds). All four are expressed in the `.proto` file using the `stream` keyword.",
      "layout_type": "concept-explainer",
      "interviewer_intent": "Checks whether the candidate understands gRPC beyond simple request-reply and can design streaming APIs for real-world use cases like live feeds, uploads, or chat.",
      "company_tags": [
        "Google",
        "Uber",
        "Twitch",
        "Bloomberg"
      ],
      "reading_time_minutes": 5,
      "last_updated": "2025-01-15",
      "difficulty": "intermediate",
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Why Request-Response Isn't Enough \u2014 The Problem That Created Streaming RPCs",
            "content": "REST APIs give you one pattern: send a request, wait for a response. When your order-tracking UI needs live updates, you poll every 2 seconds \u2014 30 requests per minute per user, most returning \"no change.\" When your IoT gateway receives 10,000 sensor readings per second, each one becomes a separate HTTP request with full headers, TLS handshake overhead, and connection setup. The request-response model wasn't designed for continuous data flow.\n\ngRPC solves this with four RPC patterns built on HTTP/2's native stream support. The `stream` keyword in your `.proto` file controls the direction of data flow. No `stream` keyword means unary \u2014 one request, one response. Add `stream` to the response and the server can push multiple messages on a single connection. Add it to the request and the client streams data to the server. Add it to both and you get a full-duplex channel.\n\nThe design insight is that all four patterns share the same connection. HTTP/2 multiplexes streams over a single TCP connection, so a server-streaming RPC and a unary RPC to the same service share one connection \u2014 no additional TCP setup. In Java, all four patterns map to the same abstraction: `StreamObserver<T>`. Unary and server-streaming methods receive a `StreamObserver<Response>` to push results. Client-streaming and bidirectional methods return a `StreamObserver<Request>` that the framework writes incoming messages to. The difference is how many times you call `onNext()` and from which side."
          },
          {
            "type": "step",
            "title": "Unary and Server Streaming \u2014 The Patterns You'll Use 90% of the Time",
            "content": "**Unary** is the bread and butter \u2014 one request, one response, analogous to a REST GET or POST. Use it for CRUD operations, lookups, and any interaction where the client expects a single answer. In Java, it looks identical to a normal method call wrapped in `StreamObserver`.\n\n**Server streaming** is where gRPC first differentiates from REST. The client sends one request and the server pushes back a stream of responses \u2014 the server calls `onNext()` multiple times before calling `onCompleted()`. This is the pattern for live order tracking, log tailing, streaming query results, or pushing price updates to a trading UI.\n\n```java\n// Proto: rpc ListUpdates (OrderRequest) returns (stream OrderEvent);\n@Override\npublic void listUpdates(OrderRequest req,\n                        StreamObserver<OrderEvent> responseObserver) {\n  for (OrderEvent event : eventStore.getEvents(req.getOrderId())) {\n    if (Context.current().isCancelled()) {\n      responseObserver.onError(Status.CANCELLED.asRuntimeException());\n      return;                         // stop if client disconnected\n    }\n    responseObserver.onNext(event);   // push each event to the client\n  }\n  responseObserver.onCompleted();     // signal end of stream\n}\n```\n\nThe critical detail is cancellation checking inside the loop. Without `Context.current().isCancelled()`, a disconnected client's stream keeps producing events that nobody receives \u2014 wasting CPU and potentially holding database cursors open."
          },
          {
            "type": "step",
            "title": "Client Streaming and Bidirectional \u2014 When Both Sides Need to Push Data",
            "content": "**Client streaming** flips the direction: the client sends a sequence of messages and gets one response after the server has received everything. The canonical use case is chunked file uploads \u2014 the client streams file chunks, and the server returns an upload summary after processing them all. In Java, the method returns a `StreamObserver<Request>` instead of receiving one.\n\n**Bidirectional streaming** opens a full-duplex channel where both sides send messages independently \u2014 neither waits for the other. Chat applications, real-time collaborative editing, and trading feeds with acknowledgments all use this pattern.\n\n```java\n// Proto: rpc Chat (stream ChatMessage) returns (stream ChatMessage);\n@Override\npublic StreamObserver<ChatMessage> chat(\n    StreamObserver<ChatMessage> responseObserver) {\n  return new StreamObserver<>() {\n    @Override\n    public void onNext(ChatMessage msg) {\n      // Process incoming message and push a response\n      responseObserver.onNext(ChatMessage.newBuilder()\n          .setContent(\"Echo: \" + msg.getContent()).build());\n    }\n    @Override\n    public void onError(Throwable t) {\n      responseObserver.onError(t);    // propagate error to client\n    }\n    @Override\n    public void onCompleted() {\n      responseObserver.onCompleted(); // close when client closes\n    }\n  };\n}\n```\n\nThe lifecycle management trap is that both `onError()` and `onCompleted()` are terminal \u2014 calling either one after the other throws `IllegalStateException`. In streaming RPCs, always handle `onError()` from the peer gracefully. If the client disconnects unexpectedly, the server's `onError()` callback fires \u2014 use it to clean up resources like open database connections or pending timers."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **`stream` keyword controls direction** \u2014 no `stream` = unary, `stream` on response = server streaming, `stream` on request = client streaming, both =.\n- **Server streaming replaces polling** \u2014 push updates on a single connection instead of clients polling every 2 seconds.\n- **`StreamObserver` is the universal abstraction** \u2014 unary receives it, streaming returns it.\n- **`onError()` and `onCompleted()` are terminal** \u2014 calling either one after the other throws `IllegalStateException`.\n- **Start with unary, add streaming when justified** \u2014 streaming adds lifecycle complexity."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "gRPC gives you four RPC patterns built on HTTP/2 streaming, and the `stream` keyword in the `.proto` file controls which one you get. Each pattern suits different communication needs.\n\n**Unary \u2014 the default for most calls:**\n- One request, one response \u2014 exactly like REST; use it for 90% of service calls including lookups, CRUD operations, and anything request-response\n- The simplest lifecycle: call the method, get a result, done; no streaming complexity or cancellation management required\n\n**Server streaming \u2014 replacing polling:**\n- The client sends one request and the server pushes a stream of responses \u2014 live order tracking, log tailing, streaming large query results\n- Instead of clients polling every 2 seconds with full request overhead each time, the server pushes updates on a single connection at any rate; the single connection eliminates repeated TLS and HTTP setup costs\n\n**Client streaming:**\n- The client sends a sequence of messages and gets one response at the end; natural for chunked file uploads and telemetry batching where you send many small pieces and get a summary confirmation\n\n**Bidirectional streaming:**\n- Both sides push independently over a full-duplex channel; chat applications, collaborative editing, and trading feeds with acknowledgments all use this pattern\n- The most complex lifecycle: both sides can send or close at any time, so error handling and cancellation require careful design\n\n**Lifecycle safety rules:**\n- `onError` and `onCompleted` are both terminal on `StreamObserver` \u2014 call either twice and you get `IllegalStateException`\n- Always check `Context.current().isCancelled()` in server-streaming loops or you'll keep producing data for disconnected clients\n\nI recommend starting with unary and only adding streaming when polling overhead or per-call latency is a measured problem, not a theoretical one \u2014 streaming adds real lifecycle complexity."
          }
        ]
      },
      "followup_questions": [
        "How do you handle backpressure in a server-streaming gRPC call in Java?",
        "What happens to an open streaming RPC when the server restarts?",
        "Can you convert a bidirectional streaming RPC to use reactive streams (Project Reactor or RxJava)?"
      ],
      "seo": {
        "meta_title": "gRPC Service Types Java \u2014 Unary, Streaming, Bidirectional",
        "meta_description": "Understand all four gRPC RPC patterns in Java: unary, server streaming, client streaming, and bidirectional streaming with proto and code examples.",
        "keywords": [
          "grpc service types java",
          "grpc streaming java",
          "bidirectional streaming grpc",
          "grpc StreamObserver"
        ]
      },
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P01",
        "hook": "gRPC gives you four RPC patterns built on HTTP/2 streaming, and the stream keyword in the .proto file controls which one you get.",
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "REST APIs give you one pattern: send a request, wait for a response. When your order-tracking UI needs live updates, you poll every 2 seconds \u2014 30 requests per minute per user, most returning \"no change.\" When your IoT gateway receives 10,000 sensor readings per second, each one becomes a separate HTTP request with full headers, TLS handshake overhead, and connection setup. The request-response model wasn't designed for continuous data flow. gRPC solves this with four RPC patterns built on."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "REST APIs give you one pattern: send a request, wait for a response. When your order-tracking UI needs live updates, you poll every 2 seconds \u2014 30 requests per minute per user, most returning \"no change.\" When your IoT gateway receives 10,000 sensor readings per second, each one becomes a separate HTTP request with full headers, TLS handshake overhead, and connection setup. The request-response model wasn't designed for continuous data."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "stream keyword controls direction \u2014 no stream = unary, stream on response = server streaming, stream on request = client streaming, both =",
              "Server streaming replaces polling \u2014 push updates on a single connection instead of clients polling every 2 seconds",
              "StreamObserver is the universal abstraction \u2014 unary receives it, streaming returns it",
              "onError() and onCompleted() are terminal \u2014 calling either one after the other throws IllegalStateException",
              "Start with unary, add streaming when justified \u2014 streaming adds lifecycle complexity"
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "REST APIs give you one pattern: send a request, wait for a response. When your order-tracking UI needs live updates, you poll every 2 seconds \u2014 30 requests per minute per user, most returning \"no change.\" When your IoT gateway receives 10,000 sensor readings per second, each one becomes a separate HTTP request with full headers, TLS handshake overhead, and connection setup. The request-response model wasn't designed for continuous data flow. gRPC solves this with four RPC patterns built on HTTP/2's native stream support. The stream keyword in your .proto file controls the direction of data flow. No stream keyword means unary \u2014 one request, one response. Add stream to the response and the server can push multiple messages on."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "REST APIs give you one pattern: send a request, wait for a response",
              "When your order-tracking UI needs live updates, you poll every 2 seconds \u2014 30 requests per minute per user, most returning \"no change",
              "\" When your IoT gateway receives 10,000 sensor readings per second, each one becomes a separate HTTP request with full headers, TLS handshake overhead, and connection setup",
              "The request-response model wasn't designed for continuous data flow",
              "gRPC solves this with four RPC patterns built on HTTP/2's native stream support"
            ]
          }
        ],
        "cap": "Unary \u2014 the default for most calls: One request, one response \u2014 exactly like REST; use it for 90% of service calls including lookups, CRUD operations, and anything request-response The simplest lifecy",
        "familiarity_anchors": [],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": [
          "How do you handle backpressure in a server-streaming gRPC call in Java?",
          "What happens to an open streaming RPC when the server restarts?",
          "Can you convert a bidirectional streaming RPC to use reactive streams (Project Reactor or RxJava)?"
        ]
      }
    },
    {
      "id": "grpc-004",
      "slug": "grpc-server-spring-boot-setup",
      "question": "How do you set up a gRPC server in Spring Boot using grpc-spring-boot-starter?",
      "title": "Setting Up a gRPC Server in Spring Boot",
      "direct_answer": "Add the `grpc-spring-boot-starter` dependency (by LogNet or net.devh), place your `.proto` files under `src/main/proto`, configure the plugin to generate Java stubs, annotate your service implementation with `@GrpcService`, and configure the port in `application.yml`. Spring Boot auto-configures the gRPC server lifecycle, interceptor wiring, and health checks.",
      "layout_type": "recipe-builder",
      "interviewer_intent": "Evaluates practical experience integrating gRPC into the Spring Boot ecosystem. Interviewers want to see you know the full setup \u2014 dependencies, code generation, annotation, and configuration \u2014 not just the theory.",
      "company_tags": [
        "Pivotal",
        "Salesforce",
        "SAP",
        "Deutsche Bank"
      ],
      "reading_time_minutes": 5,
      "last_updated": "2025-01-15",
      "difficulty": "intermediate",
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "The Problem with Raw gRPC Setup \u2014 And Why the Starter Exists",
            "content": "Setting up a gRPC server without Spring integration means hand-wiring `ServerBuilder`, manually registering services, managing the server lifecycle alongside your application context, and writing your own interceptor plumbing. In a Spring Boot world where `@RestController` auto-registers HTTP endpoints with zero boilerplate, that manual ceremony feels wrong.\n\nThe `grpc-spring-boot-starter` from `net.devh` bridges gRPC's runtime with Spring Boot's conventions. It scans for beans annotated with `@GrpcService`, registers them with a Netty-based gRPC `Server` instance, and starts that server on a configurable port (default 9090) alongside Spring Boot's HTTP server. Your gRPC service implementation is a regular Spring bean \u2014 constructor injection, `@Value` properties, `@Transactional` methods all work exactly as you'd expect.\n\nThe architectural consequence is that your application now runs **two servers in one process** \u2014 the servlet-based HTTP server on port 8080 and the gRPC server on port 9090. This means two health checks, two sets of TLS certificates if you're doing mTLS, and two port mappings in your Kubernetes service definition. Understanding this dual-server model is the key to avoiding production surprises \u2014 from forgotten port mappings to health probes that only check one protocol while the other is unresponsive."
          },
          {
            "type": "step",
            "title": "Step 1 \u2014 Add the Starter and Protobuf Dependencies",
            "content": "The `grpc-spring-boot-starter` bundles `grpc-netty-shaded` as the transport layer, so you don't need to manage Netty dependencies separately. You still need the `protobuf-java` runtime and the `protobuf-maven-plugin` for code generation from your `.proto` files. The starter version tracks Spring Boot's release cadence, so check the compatibility matrix before upgrading.\n\n```xml\n<dependencies>\n  <dependency>\n    <groupId>net.devh</groupId>\n    <artifactId>grpc-spring-boot-starter</artifactId>\n    <version>2.15.0.RELEASE</version>\n  </dependency>\n  <dependency>\n    <groupId>com.google.protobuf</groupId>\n    <artifactId>protobuf-java</artifactId>\n    <version>3.25.1</version>\n  </dependency>\n</dependencies>\n```\n\nThe production footgun is version compatibility \u2014 using Spring Boot 3.2 with a starter built for 2.7 produces cryptic `NoSuchMethodError` exceptions at startup because the starter references Spring APIs that moved between Boot 2 and Boot 3. Always verify the starter version against the project README's compatibility matrix before upgrading Spring Boot."
          },
          {
            "type": "step",
            "title": "Step 2 \u2014 Configure the gRPC Server Port and Properties",
            "content": "By default, the gRPC server binds to port 9090, separate from Spring's HTTP server on 8080. Override this in `application.yml` along with TLS and keepalive settings.\n\n```yaml\ngrpc:\n  server:\n    port: 9090                    # dedicated gRPC port\n    # security:                   # uncomment for mTLS\n    #   certificate-chain: classpath:server.crt\n    #   private-key: classpath:server.key\n\nspring:\n  application:\n    name: order-service\n```\n\nRunning gRPC on a separate port means your Kubernetes `Service` needs two port definitions, your Ingress or load balancer needs a separate listener for HTTP/2 traffic, and your network policies must allow traffic on both ports. This is the most commonly forgotten step when migrating a REST-only service to dual REST+gRPC \u2014 the gRPC port works locally but is unreachable in the cluster because the `Service` manifest only exposes port 8080.\n\nFor tests, always set `grpc.server.port: 0` to get a random available port. Hardcoded test ports cause CI failures when two test suites run in parallel and both try to bind 9090."
          },
          {
            "type": "step",
            "title": "Step 3 \u2014 Annotate Your Service and Optionally Wire a Client",
            "content": "Mark your service implementation with `@GrpcService` \u2014 the starter discovers it at startup and registers it with the gRPC server. The service is a full Spring bean, so you can inject any Spring-managed dependency.\n\n```java\nimport net.devh.boot.grpc.server.service.GrpcService;\nimport io.grpc.stub.StreamObserver;\n\n@GrpcService\npublic class OrderServiceImpl\n    extends OrderServiceGrpc.OrderServiceImplBase {\n\n  private final OrderRepository repository;\n\n  public OrderServiceImpl(OrderRepository repository) {\n    this.repository = repository;  // constructor injection works normally\n  }\n\n  @Override\n  public void getOrder(OrderRequest req,\n                       StreamObserver<OrderReply> obs) {\n    Order order = repository.findById(req.getOrderId())\n        .orElseThrow(() -> Status.NOT_FOUND\n            .withDescription(\"Order not found\")\n            .asRuntimeException());\n    obs.onNext(toProto(order));\n    obs.onCompleted();\n  }\n}\n```\n\nFor the client side, the starter provides `@GrpcClient` to inject managed stubs. Configure the target address in YAML under `grpc.client.<name>.address` and the starter handles channel lifecycle, load balancing, and TLS negotiation.\n\n```yaml\ngrpc:\n  client:\n    order-service:\n      address: static://localhost:9090\n      negotiation-type: plaintext\n```\n\n```java\n@Service\npublic class OrderClient {\n  @GrpcClient(\"order-service\")\n  private OrderServiceGrpc.OrderServiceBlockingStub stub;\n\n  public String getOrder(String id) {\n    return stub.getOrder(OrderRequest.newBuilder()\n        .setOrderId(id).build()).getStatus();\n  }\n}\n```\n\nThe `@GrpcClient` annotation supports three stub types: `BlockingStub` for synchronous calls, `Stub` for async callbacks, and `FutureStub` for `ListenableFuture`-based responses. Use blocking stubs in tests and simple consumers; use async stubs in production services that need to handle concurrent requests without thread-per-call blocking."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Dual-server model** \u2014 the app runs HTTP on port 8080 and gRPC on port 9090 simultaneously.\n- **`@GrpcService` = auto-registration** \u2014 the starter discovers annotated beans, registers them with the Netty gRPC server, and manages startup/shutdown.\n- **`grpc.server.port: 0` in tests** \u2014 always use random ports in test properties to avoid CI failures from parallel test suites binding the same port.\n- **`@GrpcClient` + YAML addresses** \u2014 logical service names map to addresses and TLS negotiation per environment without recompiling.\n- **Starter version must match Spring Boot version** \u2014 Boot 2.x and 3.x have different Spring APIs."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "Setting up a gRPC server in Spring Boot with `grpc-spring-boot-starter` is clean once you understand the dual-server model. Your application runs two servers simultaneously, and both need to be configured correctly.\n\n**The basic setup:**\n- Add `net.devh:grpc-spring-boot-starter` dependency, set `grpc.server.port` in `application.yml` \u2014 typically 9090 \u2014 and annotate your service implementation with `@GrpcService`\n- The starter handles Netty server startup, service registration, and lifecycle management alongside the Spring context; your service class is a regular Spring bean with full support for constructor injection, `@Transactional`, and configuration properties\n- On the client side, `@GrpcClient` injects a managed stub with a logical service name configured in YAML \u2014 addresses can switch from `static://` for dev to `dns://` for production service discovery without recompiling\n\n**The dual-server model \u2014 the critical detail:**\n- Your application runs HTTP on port 8080 and gRPC on port 9090; that means two health checks, potentially two TLS certificate sets for mTLS, and two port entries in your Kubernetes Service definition\n- A common deployment mistake: the K8s manifest exposes only port 8080, so gRPC is unreachable in the cluster with no obvious error \u2014 the service starts fine but all gRPC clients get connection refused\n\n**Test configuration:**\n- Always set `grpc.server.port: 0` in test properties to use a random port; parallel test suites binding the same port is a frequent CI failure source that's hard to debug\n\nI recommend verifying both ports appear in your Kubernetes Service manifest before the first cluster deployment \u2014 and confirming the starter version matches your Spring Boot major version, since Boot 2.x and 3.x have different APIs that cause `NoSuchMethodError` on startup if mismatched."
          }
        ]
      },
      "followup_questions": [
        "How do you enable TLS for the gRPC server in Spring Boot?",
        "How does grpc-spring-boot-starter integrate with Spring Security?",
        "What is the difference between blocking stubs, async stubs, and future stubs?"
      ],
      "seo": {
        "meta_title": "gRPC Spring Boot Setup \u2014 grpc-spring-boot-starter Guide",
        "meta_description": "Step-by-step guide to setting up a gRPC server and client in Spring Boot using net.devh grpc-spring-boot-starter with @GrpcService and @GrpcClient.",
        "keywords": [
          "grpc spring boot setup",
          "grpc-spring-boot-starter",
          "GrpcService annotation",
          "spring boot grpc server"
        ]
      },
      "speakable_v2": {
        "archetype": "C",
        "pillar": "P01",
        "hook": "Setting up a gRPC server in Spring Boot with grpc-spring-boot-starter is clean once you understand the dual-server model.",
        "beats": [
          {
            "kind": "mental_model",
            "layout": "paragraph",
            "text": "Setting up a gRPC server without Spring integration means hand-wiring ServerBuilder, manually registering services, managing the server lifecycle alongside your application context, and writing your own interceptor plumbing. In a Spring Boot world where @RestController auto-registers HTTP endpoints with zero boilerplate, that manual ceremony feels wrong. The grpc-spring-boot-starter from net.devh bridges gRPC's runtime with Spring Boot's conventions. It scans for beans annotated with @GrpcService, registers them with a Netty-based gRPC Server instance, and starts that server on a configurable port (default 9090) alongside Spring Boot's HTTP server. Your gRPC service."
          },
          {
            "kind": "mechanism",
            "layout": "ordered_list",
            "steps": [
              "The grpc-spring-boot-starter bundles grpc-netty-shaded as the transport layer, so you don't need to manage Netty dependencies separately.",
              "You still need the protobuf-java runtime and the protobuf-maven-plugin for code generation from your .proto files.",
              "The starter version tracks Spring Boot's release cadence, so check the compatibility matrix before upgrading.",
              "``xml <dependencies> <dependency> <groupId>net.devh</groupId> <artifactId>grpc-spring-boot-starter</artifactId> <version>2.15.0.RELEASE</version> </dependency> <dependency> <groupId>com.google.protobuf</groupId> <artifactId>protobuf-java</artifactId> <version>3.25.1</version> </dependency> </dependencies> ` The production footgun is version compatibility \u2014 using Spring Boot 3.2 with a starter built for 2.7 produces cryptic NoSuchMethodError` exceptions at startup because the starter references Spring APIs that moved between Boot 2 and Boot 3.",
              "Always verify the starter version against the project README's compatibility matrix before upgrading Spring Boot.",
              "By default, the gRPC server binds to port 9090, separate from Spring's HTTP server on 8080."
            ]
          },
          {
            "kind": "edge_cases",
            "layout": "bullets",
            "items": [
              "Dual-server model \u2014 the app runs HTTP on port 8080 and gRPC on port 9090 simultaneously",
              "@GrpcService = auto-registration \u2014 the starter discovers annotated beans, registers them with the Netty gRPC server, and manages startup/shutdown",
              "port: 0 in tests \u2014 always use random ports in test properties to avoid CI failures from parallel test suites binding the same port",
              "@GrpcClient + YAML addresses \u2014 logical service names map to addresses and TLS negotiation per environment without recompiling",
              "Starter version must match Spring Boot version \u2014 Boot 2"
            ]
          },
          {
            "kind": "failure_mode",
            "layout": "callout",
            "text": "The production failure mode here is unchecked state mutation under concurrent access."
          }
        ],
        "cap": "The basic setup: Add net.devh:grpc-spring-boot-starter dependency, set grpc.server.port in application.yml \u2014 typically 9090 \u2014 and annotate your service implementation with @GrpcService The starter han",
        "familiarity_anchors": [
          "LogNet",
          "GrpcService",
          "Spring"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": [
          "How do you enable TLS for the gRPC server in Spring Boot?",
          "How does grpc-spring-boot-starter integrate with Spring Security?",
          "What is the difference between blocking stubs, async stubs, and future stubs?"
        ]
      }
    },
    {
      "id": "grpc-005",
      "slug": "grpc-interceptors-auth-logging",
      "question": "How do you implement gRPC interceptors for authentication and logging in Spring Boot?",
      "title": "gRPC Interceptors for Auth and Logging",
      "direct_answer": "Implement `ServerInterceptor` (or `ClientInterceptor`) and register it with `@GrpcGlobalServerInterceptor` or per-service via `@GrpcService(interceptors = ...)`. Inside the interceptor, access the `Metadata` (headers) for tokens, call `Context` for propagating values, and delegate to the next handler or abort with a `Status.UNAUTHENTICATED` error.",
      "layout_type": "recipe-builder",
      "interviewer_intent": "Tests cross-cutting concern design in gRPC \u2014 equivalent to Servlet filters or Spring AOP. Interviewers want to see you can centralise auth and observability without cluttering service logic.",
      "company_tags": [
        "Netflix",
        "Stripe",
        "Lyft",
        "Coinbase"
      ],
      "reading_time_minutes": 5,
      "last_updated": "2025-01-15",
      "difficulty": "intermediate",
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Why Cross-Cutting Concerns Can't Live in Service Methods",
            "content": "Imagine adding JWT validation to a gRPC service with 15 RPC methods. Without interceptors, every method starts with the same 10 lines \u2014 extract the token from metadata, validate it, check roles, abort if invalid. When the auth logic changes (new token format, different header name, additional claims check), you update 15 methods. Miss one, and that endpoint is unauthenticated in production.\n\ngRPC interceptors solve this the same way servlet filters solve it for HTTP \u2014 they wrap every RPC call in a chain that executes before and after the service method. A `ServerInterceptor` receives three things: the `ServerCall` (representing the RPC), the `Metadata` (gRPC's equivalent of HTTP headers), and a `ServerCallHandler` (the next interceptor or the service itself). The interceptor can inspect headers, reject the call entirely, modify the request, or attach values to the `Context` for downstream use.\n\nThe `grpc-spring-boot-starter` makes registration trivial: annotate an interceptor bean with `@GrpcGlobalServerInterceptor` to apply it to every service, or pass specific interceptor classes to `@GrpcService(interceptors = ...)` for per-service scoping. The critical difference from Spring's `SecurityContextHolder` is context propagation \u2014 gRPC uses explicit `Context` objects instead of `ThreadLocal`, which is essential because streaming RPCs can process messages across different threads. Using `ThreadLocal` for auth state in gRPC silently breaks the moment you handle a streaming call."
          },
          {
            "type": "step",
            "title": "Step 1 \u2014 Build a Logging Interceptor That Captures Entry and Exit",
            "content": "The logging interceptor wraps the `ServerCall` using `ForwardingServerCall` to capture the status code when the call closes. This gives you structured request-response logging \u2014 method name at entry, status code at exit \u2014 without touching any service method.\n\n```java\nimport io.grpc.*;\nimport net.devh.boot.grpc.server.interceptor.GrpcGlobalServerInterceptor;\nimport org.slf4j.Logger;\nimport org.slf4j.LoggerFactory;\n\n@GrpcGlobalServerInterceptor\npublic class LoggingInterceptor implements ServerInterceptor {\n\n  private static final Logger log =\n      LoggerFactory.getLogger(LoggingInterceptor.class);\n\n  @Override\n  public <Req, Resp> ServerCall.Listener<Req> interceptCall(\n      ServerCall<Req, Resp> call, Metadata headers,\n      ServerCallHandler<Req, Resp> next) {\n\n    String method = call.getMethodDescriptor().getFullMethodName();\n    long start = System.nanoTime();\n    log.info(\"gRPC call started: {}\", method);\n\n    ServerCall<Req, Resp> wrapped =\n        new ForwardingServerCall.SimpleForwardingServerCall<>(call) {\n          @Override\n          public void close(Status status, Metadata trailers) {\n            long elapsed = (System.nanoTime() - start) / 1_000_000;\n            log.info(\"gRPC call completed: {} status={} time={}ms\",\n                method, status.getCode(), elapsed);\n            super.close(status, trailers);\n          }\n        };\n    return next.startCall(wrapped, headers);\n  }\n}\n```\n\nThe `ForwardingServerCall` pattern is important \u2014 it delegates all methods to the original call and only overrides `close()`. This avoids accidentally breaking flow control, cancellation, or message delivery by intercepting methods you didn't intend to."
          },
          {
            "type": "step",
            "title": "Step 2 \u2014 Build an Auth Interceptor That Validates JWT and Propagates Identity",
            "content": "The auth interceptor reads the `Authorization` header from `Metadata`, validates the JWT, and either rejects the call immediately with `Status.UNAUTHENTICATED` or attaches the validated principal to a `Context.Key`. The `Context` mechanism is gRPC's way of propagating request-scoped values without thread-local state.\n\n```java\n@GrpcGlobalServerInterceptor\npublic class AuthInterceptor implements ServerInterceptor {\n\n  static final Metadata.Key<String> AUTH_HEADER =\n      Metadata.Key.of(\"authorization\", Metadata.ASCII_STRING_MARSHALLER);\n  static final Context.Key<String> USER_CTX = Context.key(\"user\");\n\n  private final JwtDecoder jwtDecoder;\n\n  public AuthInterceptor(JwtDecoder jwtDecoder) {\n    this.jwtDecoder = jwtDecoder;\n  }\n\n  @Override\n  public <Req, Resp> ServerCall.Listener<Req> interceptCall(\n      ServerCall<Req, Resp> call, Metadata headers,\n      ServerCallHandler<Req, Resp> next) {\n\n    String token = headers.get(AUTH_HEADER);\n    if (token == null || !token.startsWith(\"Bearer \")) {\n      call.close(Status.UNAUTHENTICATED\n          .withDescription(\"Missing Bearer token\"), new Metadata());\n      return new ServerCall.Listener<>() {};  // no-op \u2014 service never executes\n    }\n\n    try {\n      String userId = jwtDecoder.decode(token.substring(7)).getSubject();\n      Context ctx = Context.current().withValue(USER_CTX, userId);\n      return Contexts.interceptCall(ctx, call, headers, next);\n    } catch (Exception e) {\n      call.close(Status.UNAUTHENTICATED\n          .withDescription(\"Invalid token\"), new Metadata());\n      return new ServerCall.Listener<>() {};\n    }\n  }\n}\n```\n\nTwo critical details: the no-op `ServerCall.Listener` after closing the call prevents the service method from executing. And `Contexts.interceptCall()` attaches the `Context` to the call so that streaming message handlers \u2014 which may run on different threads \u2014 still see the authenticated user via `USER_CTX.get()`. If you used `ThreadLocal` instead, the value would be visible on the interceptor thread but invisible in the `StreamObserver.onNext()` callback thread."
          },
          {
            "type": "step",
            "title": "Step 3 \u2014 Access the Authenticated User in Service Methods",
            "content": "Inside any `@GrpcService` method, retrieve the authenticated user from the `Context` without any dependency on the auth mechanism.\n\n```java\n@GrpcService\npublic class OrderServiceImpl\n    extends OrderServiceGrpc.OrderServiceImplBase {\n\n  @Override\n  public void getOrder(OrderRequest req,\n                       StreamObserver<OrderReply> obs) {\n    String userId = AuthInterceptor.USER_CTX.get();\n    // userId is available regardless of which thread handles this call\n    Order order = repository.findByIdAndOwner(\n        req.getOrderId(), userId);\n    if (order == null) {\n      obs.onError(Status.NOT_FOUND\n          .withDescription(\"Order not found\").asRuntimeException());\n      return;\n    }\n    obs.onNext(toProto(order));\n    obs.onCompleted();\n  }\n}\n```\n\nThe service has no knowledge of JWT parsing, `Metadata` keys, or the interceptor implementation. It only knows about the `Context.Key` \u2014 a clean separation that lets you swap JWT for mTLS or API keys by changing the interceptor alone. The `Context.Key` acts as the interface contract between auth infrastructure and business logic, not unlike Spring Security's `SecurityContextHolder` \u2014 but explicitly propagated through gRPC's threading model instead of relying on `ThreadLocal`."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **`Context.Key` over `ThreadLocal`** \u2014 gRPC streaming RPCs process messages across threads.\n- **No-op `ServerCall.Listener` after rejection** \u2014 return `new ServerCall.Listener<>() {}` after calling `call.close(UNAUTHENTICATED)` to prevent the.\n- **`@GrpcGlobalServerInterceptor` vs per-service** \u2014 global applies to all services automatically.\n- **`ForwardingServerCall` for wrapping** \u2014 override only the methods you need (typically `close()` for logging).\n- **Interceptor ordering matters** \u2014 auth should run before logging so rejected calls still get logged."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "Interceptors are gRPC's equivalent of servlet filters \u2014 they wrap every RPC call so you can centralize logging, authentication, and metrics without touching service methods. Implement `ServerInterceptor`, annotate with `@GrpcGlobalServerInterceptor`, and the starter applies it to every service automatically.\n\n**Logging interceptor:**\n- Wrap the `ServerCall` with `ForwardingServerCall` to capture the method name at entry and the status code at exit; override only the methods you need \u2014 typically `close()` for response logging \u2014 and delegate everything else to avoid breaking flow control or cancellation propagation\n- This gives structured request-response logs without modifying any service code\n\n**Auth interceptor:**\n- Read the `Authorization` header from gRPC's `Metadata`, validate the JWT, and either close the call with `Status.UNAUTHENTICATED` or attach the principal to a `Context.Key` using `Contexts.interceptCall()`\n- Return a no-op `new ServerCall.Listener<>() {}` after calling `call.close()` to prevent the service method from executing on rejected calls \u2014 without this, the service still runs after rejection\n\n**The context propagation model \u2014 the most important detail:**\n- Unlike Spring Security's `SecurityContextHolder` which uses `ThreadLocal`, gRPC's `Context` is explicitly propagated through `Contexts.interceptCall()`\n- If you use `ThreadLocal` for auth state, it works fine for unary calls but silently breaks on streaming RPCs where message handlers run on different threads \u2014 `Context.Key` propagates correctly across all gRPC execution models\n\nI recommend always using `Context.Key` for anything request-scoped in gRPC, and setting `@Order` on interceptors so auth runs before logging \u2014 rejected calls should still appear in access logs."
          }
        ]
      },
      "followup_questions": [
        "How do you apply an interceptor to only one specific gRPC service?",
        "How do you integrate gRPC interceptors with Spring Security's SecurityContextHolder?",
        "How do you propagate OpenTelemetry trace context through gRPC interceptors?"
      ],
      "seo": {
        "meta_title": "gRPC Interceptors Spring Boot \u2014 Auth and Logging",
        "meta_description": "Learn how to implement gRPC server interceptors in Spring Boot for JWT authentication, logging, and context propagation using grpc-spring-boot-starter.",
        "keywords": [
          "grpc interceptor spring boot",
          "grpc authentication java",
          "grpc server interceptor",
          "grpc logging interceptor"
        ]
      },
      "speakable_v2": {
        "archetype": "C",
        "pillar": "P01",
        "hook": "Interceptors are gRPC's equivalent of servlet filters \u2014 they wrap every RPC call so you can centralize logging, authentication, and metrics without touching service methods.",
        "beats": [
          {
            "kind": "mental_model",
            "layout": "paragraph",
            "text": "Imagine adding JWT validation to a gRPC service with 15 RPC methods. Without interceptors, every method starts with the same 10 lines \u2014 extract the token from metadata, validate it, check roles, abort if invalid. When the auth logic changes (new token format, different header name, additional claims check), you update 15 methods. Miss one, and that endpoint is unauthenticated in production. gRPC interceptors solve this the same way servlet filters solve it for HTTP \u2014 they wrap every RPC call in a chain that executes before and after the."
          },
          {
            "kind": "mechanism",
            "layout": "ordered_list",
            "steps": [
              "The logging interceptor wraps the ServerCall using ForwardingServerCall to capture the status code when the call closes.",
              "This gives you structured request-response logging \u2014 method name at entry, status code at exit \u2014 without touching any service method.",
              "``java import io.grpc.*; import net.devh.boot.grpc.server.interceptor.GrpcGlobalServerInterceptor; import org.slf4j.Logger; import org.slf4j.LoggerFactory; @GrpcGlobalServerInterceptor public class LoggingInterceptor implements ServerInterceptor { private static final Logger log = LoggerFactory.getLogger(LoggingInterceptor.class); @Override public <Req, Resp> ServerCall.Listener<Req> interceptCall( ServerCall<Req, Resp> call, Metadata headers, ServerCallHandler<Req, Resp> next) { String method = call.getMethodDescriptor().getFullMethodName(); long start = System.nanoTime(); log.info(\"gRPC call started: {}\", method); ServerCall<Req, Resp> wrapped = new ForwardingServerCall.SimpleForwardingServerCall<>(call) { @Override public void close(Status status, Metadata trailers) { long elapsed = (System.nanoTime() - start) / 1_000_000; log.info(\"gRPC call completed: {} status={} time={}ms\", method, status.getCode(), elapsed); super.close(status, trailers); } }; return next.startCall(wrapped, headers); } } ` The ForwardingServerCall pattern is important \u2014 it delegates all."
            ]
          },
          {
            "kind": "edge_cases",
            "layout": "bullets",
            "items": [
              "Key over ThreadLocal \u2014 gRPC streaming RPCs process messages across threads",
              "No-op ServerCall",
              "Listener after rejection \u2014 return new ServerCall",
              "Listener<>() {} after calling call",
              "close(UNAUTHENTICATED) to prevent the"
            ]
          },
          {
            "kind": "failure_mode",
            "layout": "callout",
            "text": "The production failure mode here is unchecked state mutation under concurrent access."
          }
        ],
        "cap": "Logging interceptor: Wrap the ServerCall with ForwardingServerCall to capture the method name at entry and the status code at exit; override only the methods you need \u2014 typically close() for response",
        "familiarity_anchors": [
          "Implement",
          "ServerInterceptor",
          "ClientInterceptor"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": [
          "How do you apply an interceptor to only one specific gRPC service?",
          "How do you integrate gRPC interceptors with Spring Security's SecurityContextHolder?",
          "How do you propagate OpenTelemetry trace context through gRPC interceptors?"
        ]
      }
    },
    {
      "id": "grpc-006",
      "slug": "grpc-error-handling-status-codes",
      "question": "How does gRPC error handling work and what are the key Status codes?",
      "title": "gRPC Error Handling with Status Codes",
      "direct_answer": "gRPC uses a well-defined set of `Status` codes (0\u201316) instead of HTTP status codes. The server signals errors by calling `responseObserver.onError(StatusRuntimeException)` or by closing the call with a non-OK `Status`. The client receives a `StatusRuntimeException` that it can catch and inspect. You should always map your domain exceptions to meaningful gRPC status codes rather than letting raw Java exceptions propagate.",
      "layout_type": "concept-explainer",
      "interviewer_intent": "Checks that you understand gRPC's error model rather than assuming it works like HTTP. Interviewers want to see you can communicate rich error information to clients and handle errors gracefully.",
      "company_tags": [
        "Google",
        "Lyft",
        "Stripe",
        "Spotify"
      ],
      "reading_time_minutes": 5,
      "last_updated": "2025-01-15",
      "difficulty": "intermediate",
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "gRPC Has Its Own Error Language \u2014 And It's Not HTTP Status Codes",
            "content": "The most common mistake developers make when starting with gRPC is assuming errors work like REST. In REST, a 404 means \"not found,\" a 401 means \"unauthenticated,\" and your HTTP client framework parses these automatically. gRPC runs over HTTP/2, but it **does not use HTTP status codes for application errors**. Every gRPC call returns HTTP 200 at the transport layer \u2014 even failures. The actual result lives in the `grpc-status` and `grpc-message` trailers appended to the HTTP/2 response.\n\ngRPC defines 17 canonical status codes (0 through 16), each with precise semantics. `OK` (0) means success. `NOT_FOUND` (5) means the requested entity doesn't exist. `INVALID_ARGUMENT` (3) means the client sent bad input. `UNAVAILABLE` (14) means the service is temporarily down \u2014 **safe to retry**. `INTERNAL` (13) means an unexpected server error \u2014 retrying won't help.\n\nThe distinction between `UNAVAILABLE` and `INTERNAL` is critical for production clients. Client retry logic branches on the status code \u2014 `UNAVAILABLE` triggers exponential backoff retries, while `INTERNAL` surfaces the error to the caller immediately. If your server returns `INTERNAL` for a transient database connection timeout instead of `UNAVAILABLE`, clients won't retry and users see errors that would have resolved on the next attempt.\n\nIn Java, servers signal errors by calling `responseObserver.onError()` with a `StatusRuntimeException`. Clients catch `StatusRuntimeException` and inspect `getStatus().getCode()`. The pattern is clean, but the trap is letting raw Java exceptions (NPE, IllegalState) propagate \u2014 gRPC wraps them as `UNKNOWN` with the exception message as the description, potentially leaking internal class names and stack traces to the client."
          },
          {
            "type": "step",
            "title": "Server-Side Error Mapping \u2014 Inline and Centralized Approaches",
            "content": "The simplest approach maps domain errors to gRPC status codes directly in the service method. This works for small services but creates duplication across methods as the service grows.\n\n```java\n@GrpcService\npublic class UserServiceImpl\n    extends UserServiceGrpc.UserServiceImplBase {\n\n  @Override\n  public void getUser(UserRequest req,\n                      StreamObserver<UserResponse> obs) {\n    if (req.getUserId().isBlank()) {\n      obs.onError(Status.INVALID_ARGUMENT\n          .withDescription(\"userId must not be blank\")\n          .asRuntimeException());\n      return;\n    }\n    User user = repository.findById(req.getUserId()).orElse(null);\n    if (user == null) {\n      obs.onError(Status.NOT_FOUND\n          .withDescription(\"User \" + req.getUserId() + \" not found\")\n          .asRuntimeException());\n      return;\n    }\n    obs.onNext(toProto(user));\n    obs.onCompleted();\n  }\n}\n```\n\nFor larger services, a centralized exception-mapping interceptor catches domain exceptions and translates them to status codes uniformly. Service methods throw normal Java exceptions, and the interceptor handles translation \u2014 identical to Spring's `@ControllerAdvice` pattern.\n\n```java\n@GrpcGlobalServerInterceptor\npublic class ExceptionMapper implements ServerInterceptor {\n  @Override\n  public <Req, Resp> ServerCall.Listener<Req> interceptCall(\n      ServerCall<Req, Resp> call, Metadata headers,\n      ServerCallHandler<Req, Resp> next) {\n    return new ForwardingServerCallListener.SimpleForwardingServerCallListener<>(\n        next.startCall(call, headers)) {\n      @Override\n      public void onHalfClose() {\n        try {\n          super.onHalfClose();\n        } catch (EntityNotFoundException e) {\n          call.close(Status.NOT_FOUND.withDescription(e.getMessage()),\n              new Metadata());\n        } catch (IllegalArgumentException e) {\n          call.close(Status.INVALID_ARGUMENT.withDescription(e.getMessage()),\n              new Metadata());\n        } catch (Exception e) {\n          call.close(Status.INTERNAL.withDescription(\"Unexpected error\"),\n              new Metadata());   // never leak class names or stack traces\n        }\n      }\n    };\n  }\n}\n```\n\nThe catch-all for `Exception` intentionally uses a generic description. Leaking `NullPointerException at UserServiceImpl.java:47` to clients is both a security risk (internal implementation exposure) and useless for the client. Log the full stack trace server-side and return a safe description."
          },
          {
            "type": "step",
            "title": "Client-Side Handling \u2014 Branching on Status Codes for Retry Decisions",
            "content": "Clients catch `StatusRuntimeException` and branch on the status code to determine behavior. The retry/no-retry distinction is the most important decision.\n\n```java\ntry {\n  UserResponse user = stub.getUser(request);\n} catch (StatusRuntimeException e) {\n  switch (e.getStatus().getCode()) {\n    case NOT_FOUND ->\n        log.warn(\"User not found: {}\", e.getStatus().getDescription());\n    case INVALID_ARGUMENT ->\n        throw new BadRequestException(e.getStatus().getDescription());\n    case UNAVAILABLE -> {\n        log.warn(\"Service temporarily unavailable, retrying...\");\n        retryWithBackoff(() -> stub.getUser(request));\n    }\n    case DEADLINE_EXCEEDED ->\n        log.error(\"Request timed out\");\n    default -> throw e;  // propagate unexpected status codes\n  }\n}\n```\n\n`UNAVAILABLE` signals a transient failure \u2014 the server is temporarily overloaded or a network glitch occurred. Retry with exponential backoff. `INTERNAL` signals a bug \u2014 the server threw an unexpected exception. Retrying won't fix the bug. `DEADLINE_EXCEEDED` means the client's deadline passed before the server responded \u2014 check whether the deadline was too aggressive or the server is genuinely slow.\n\nFor structured error details beyond a string description, gRPC supports the `google.rpc.Status` proto with an `Any`-typed `details` field. This lets servers attach domain-specific payloads \u2014 field validation errors, retry-after hints \u2014 that clients can deserialize. The `ErrorInfo`, `BadRequest`, and `RetryInfo` protos from `google/rpc/error_details.proto` provide standardized structures."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Every gRPC call returns HTTP 200** \u2014 the real status lives in `grpc-status` and `grpc-message` trailers.\n- **`UNAVAILABLE` = retry, `INTERNAL` = don't** \u2014 using the wrong code causes clients to either retry hopelessly or give up on transient failures.\n- **Raw exceptions propagate as `UNKNOWN`** \u2014 an uncaught `NullPointerException` becomes `UNKNOWN` with the exception message, leaking internal class names.\n- **Centralized exception-mapping interceptor** \u2014 catches domain exceptions and translates to gRPC status codes uniformly, like Spring's.\n- **`StatusRuntimeException.getStatus().getCode()`** \u2014 the client-side API for inspecting errors."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "gRPC uses its own 17-status-code system for application errors, not HTTP status codes. Every gRPC call returns HTTP 200 at the transport layer \u2014 the actual result lives in the `grpc-status` trailer. Understanding these codes is essential for both server-side error mapping and client-side retry decisions.\n\n**Key status codes and their semantics:**\n- `NOT_FOUND` for missing entities, `INVALID_ARGUMENT` for bad input, `PERMISSION_DENIED` for authorization failures, `UNAUTHENTICATED` for missing credentials\n- `UNAVAILABLE` means transient failure \u2014 safe to retry with backoff; `INTERNAL` means something is genuinely broken \u2014 retrying won't help; using the wrong one causes clients to either retry hopelessly or give up on recoverable failures\n- `DEADLINE_EXCEEDED` signals the client's deadline passed before the server responded \u2014 the correct signal for timeout scenarios\n\n**Centralized error mapping:**\n- On the server, signal errors by calling `responseObserver.onError()` with a `StatusRuntimeException`; for larger services, centralize this with a global interceptor that catches domain exceptions and maps them to status codes \u2014 the same concept as Spring's `@ControllerAdvice`\n- The production rule: never let raw Java exceptions propagate to clients; an uncaught `NullPointerException` becomes `UNKNOWN` with the stack trace in the description \u2014 a security risk and useless to clients\n\n**Client-side error handling:**\n- `StatusRuntimeException.getStatus().getCode()` is the client-side API; always switch on the code enum, not the description string which is human-readable and may change\n\nI recommend logging the full stack trace server-side while keeping error descriptions informative but safe for clients \u2014 the goal is a client that can react programmatically to the code, not one that parses error messages."
          }
        ]
      },
      "followup_questions": [
        "How do you attach structured error details to a gRPC error using `google.rpc.Status` and `ErrorInfo`?",
        "What is the difference between `UNAVAILABLE` and `INTERNAL` status codes in terms of retry behaviour?",
        "How do you propagate a gRPC error code across service boundaries in a microservices architecture?"
      ],
      "seo": {
        "meta_title": "gRPC Error Handling Java \u2014 Status Codes Guide",
        "meta_description": "Learn gRPC status codes and error handling patterns in Java. Server-side status throwing, centralised exception mapping, and client-side StatusRuntimeException handling.",
        "keywords": [
          "grpc error handling java",
          "grpc status codes",
          "StatusRuntimeException",
          "grpc NOT_FOUND INVALID_ARGUMENT"
        ]
      },
      "speakable_v2": {
        "archetype": "C",
        "pillar": "P01",
        "hook": "gRPC uses its own 17-status-code system for application errors, not HTTP status codes.",
        "beats": [
          {
            "kind": "mental_model",
            "layout": "paragraph",
            "text": "The most common mistake developers make when starting with gRPC is assuming errors work like REST. In REST, a 404 means \"not found,\" a 401 means \"unauthenticated,\" and your HTTP client framework parses these automatically. gRPC runs over HTTP/2, but it does not use HTTP status codes for application errors. Every gRPC call returns HTTP 200 at the transport layer \u2014 even failures. The actual result lives in the grpc-status and grpc-message trailers appended to the HTTP/2 response. gRPC defines 17 canonical status codes (0 through 16), each with precise."
          },
          {
            "kind": "mechanism",
            "layout": "ordered_list",
            "steps": [
              "The simplest approach maps domain errors to gRPC status codes directly in the service method.",
              "This works for small services but creates duplication across methods as the service grows.",
              "``java @GrpcService public class UserServiceImpl extends UserServiceGrpc.UserServiceImplBase { @Override public void getUser(UserRequest req, StreamObserver<UserResponse> obs) { if (req.getUserId().isBlank()) { obs.onError(Status.INVALID_ARGUMENT .withDescription(\"userId must not be blank\") .asRuntimeException()); return; } User user = repository.findById(req.getUserId()).orElse(null); if (user == null) { obs.onError(Status.NOT_FOUND .withDescription(\"User \" + req.getUserId() + \" not found\") .asRuntimeException()); return; } obs.onNext(toProto(user)); obs.onCompleted(); } } ` For larger services, a centralized exception-mapping interceptor catches domain exceptions and translates them to status codes uniformly.",
              "Service methods throw normal Java exceptions, and the interceptor handles translation \u2014 identical to Spring's @ControllerAdvice pattern.",
              "`java @GrpcGlobalServerInterceptor public class ExceptionMapper implements ServerInterceptor { @Override public <Req, Resp> ServerCall.Listener<Req> interceptCall( ServerCall<Req, Resp> call, Metadata headers, ServerCallHandler<Req, Resp> next) {."
            ]
          },
          {
            "kind": "edge_cases",
            "layout": "bullets",
            "items": [
              "Every gRPC call returns HTTP 200 \u2014 the real status lives in grpc-status and grpc-message trailers",
              "UNAVAILABLE = retry, INTERNAL = don't \u2014 using the wrong code causes clients to either retry hopelessly or give up on transient failures",
              "Raw exceptions propagate as UNKNOWN \u2014 an uncaught NullPointerException becomes UNKNOWN with the exception message, leaking internal class names",
              "Centralized exception-mapping interceptor \u2014 catches domain exceptions and translates to gRPC status codes uniformly, like Spring's",
              "StatusRuntimeException"
            ]
          },
          {
            "kind": "failure_mode",
            "layout": "callout",
            "text": "The production failure mode here is unchecked state mutation under concurrent access."
          }
        ],
        "cap": "Key status codes and their semantics: NOT_FOUND for missing entities, INVALID_ARGUMENT for bad input, PERMISSION_DENIED for authorization failures, UNAUTHENTICATED for missing credentials UNAVAILABLE",
        "familiarity_anchors": [
          "Status",
          "StatusRuntimeException"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": [
          "How do you attach structured error details to a gRPC error using `google.rpc.Status` and `ErrorInfo`?",
          "What is the difference between `UNAVAILABLE` and `INTERNAL` status codes in terms of retry behaviour?",
          "How do you propagate a gRPC error code across service boundaries in a microservices architecture?"
        ]
      }
    },
    {
      "id": "grpc-007",
      "slug": "grpc-vs-rest-performance",
      "question": "Why is gRPC generally faster than REST, and when does that advantage matter?",
      "title": "gRPC vs REST Performance Comparison",
      "direct_answer": "gRPC is typically 5\u201310x faster than REST/JSON for the same payload because it uses Protocol Buffers (binary, compact, no parsing overhead) over HTTP/2 (multiplexed, header compression, persistent connection). The performance advantage is most significant in high-throughput internal microservice communication, latency-sensitive paths, and mobile/IoT scenarios. For low-traffic public APIs the difference is often negligible.",
      "layout_type": "comparison-arena",
      "interviewer_intent": "Probes whether the candidate can quantify gRPC's performance story and articulate when it actually matters in production, rather than just saying 'gRPC is faster'.",
      "company_tags": [
        "Google",
        "Netflix",
        "Uber",
        "Twitter",
        "Cloudflare"
      ],
      "reading_time_minutes": 4,
      "last_updated": "2025-01-15",
      "difficulty": "intermediate",
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Two Independent Performance Layers That Compound Under Load",
            "content": "Saying \"gRPC is faster than REST\" without quantifying where the speed comes from is the kind of answer that makes interviewers stop listening. The performance gap exists for two independent reasons, and each one matters at a different scale.\n\n**Layer 1: Serialization.** Protocol Buffers produces binary messages that are 3\u201310\u00d7 smaller than equivalent JSON. JSON repeats field names in every single object \u2014 a list of 1,000 users includes the string `\"username\"` 1,000 times. Protobuf uses field numbers (1 byte each) instead. JSON requires runtime parsing \u2014 the deserializer scans characters, handles string escaping, infers types dynamically. Protobuf's schema is compiled at build time, so deserialization is a direct memory copy with known offsets \u2014 no reflection, no type guessing. On a service processing 10K messages per second, the CPU savings from avoiding JSON parsing alone can free 20\u201330% of a core.\n\n**Layer 2: Transport.** gRPC runs over HTTP/2, which multiplexes all RPC calls on a single TCP connection and compresses headers with HPACK. REST over HTTP/1.1 requires a new TCP connection per request (or careful connection pooling) and repeats headers verbatim on every call. The TLS handshake alone \u2014 1\u20132 round trips, 50\u2013100ms \u2014 disappears when a single persistent HTTP/2 connection handles all calls. HPACK header compression is particularly effective for gRPC because most calls to the same service carry identical metadata \u2014 after the first request establishes the header table, subsequent requests encode headers in as few as 5 bytes.\n\nThese advantages compound in a service mesh. When a single user request fans out to 5 downstream services, each saving 5ms of serialization overhead and avoiding per-request connection setup, the aggregate latency reduction reaches 25ms \u2014 visible in p99 latency dashboards. Teams switching internal hot-path REST calls to gRPC commonly report 60\u201370% latency reductions.\n\nBut context matters. For a low-traffic admin API serving 100 requests per minute, the difference is unmeasurable \u2014 and REST's ability to debug with curl, test with Postman, and cache with CDNs is worth more than the microseconds you'd save. The performance advantage is real but situational \u2014 it pays off on high-RPS internal paths and is irrelevant at the edge."
          },
          {
            "type": "comparison_table",
            "title": "Where Each Millisecond Comes From \u2014 gRPC vs REST/JSON",
            "content": "| Performance Factor | gRPC/Protobuf | REST/JSON |\n|---|---|---|\n| Payload size (typical message) | ~50\u2013100 bytes (binary) | ~500\u20131000 bytes (text + field names) |\n| Serialization CPU | Compiled schema, direct memory copy | Runtime parsing, string escaping, type inference |\n| Connection setup | One persistent HTTP/2 connection per peer | New connection per request or managed pool |\n| Header overhead | HPACK compressed (first request ~full, subsequent ~5 bytes) | Full headers repeated every request (~500 bytes) |\n| Concurrent calls | Multiplexed on single connection | One request per connection (HTTP/1.1) or multiplexed (HTTP/2) |\n| TLS handshake | Once per connection lifecycle | Once per connection, but more connections = more handshakes |\n| Debugging ease | Requires grpcurl / Bloom RPC | curl, Postman, browser DevTools |\n| When advantage matters | >1K RPS, service mesh fan-out, mobile/IoT | <100 RPS, external APIs, developer workflows |"
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Protobuf payloads are 3\u201310\u00d7 smaller than JSON** \u2014 field numbers replace repeated field names.\n- **Serialization CPU savings reach 20\u201330% of a core at 10K msg/sec** \u2014 protobuf's compiled schema uses direct memory offsets.\n- **HTTP/2 multiplexing eliminates per-request TCP/TLS setup** \u2014 one persistent connection carries all concurrent RPCs.\n- **Advantages compound in service mesh fan-out** \u2014 5ms saved per hop \u00d7 5 downstream services = 25ms aggregate improvement on a single user request.\n- **Below ~1K RPS the difference is negligible** \u2014 REST's debuggability (curl, Postman, browser) outweighs microsecond savings for low-traffic or."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "gRPC is faster than REST for internal service calls for two compounding reasons: smaller payloads from Protocol Buffers and connection efficiency from HTTP/2. At scale, these savings compound in ways that REST's simplicity cannot offset.\n\n**Protocol Buffers \u2014 the payload advantage:**\n- Binary encoding with field numbers instead of repeated key names produces payloads 3\u201310\u00d7 smaller than JSON; a list of 1,000 objects avoids repeating field names like \"orderId\", \"customerId\", \"amount\" for every element\n- The compiled schema means deserialization is a direct memory copy instead of runtime character scanning, string escaping, and type inference; at 10K messages per second, the CPU savings alone are measurable \u2014 roughly 20\u201330% of a CPU core\n\n**HTTP/2 multiplexing \u2014 the connection advantage:**\n- All RPCs share one persistent TCP connection with full multiplexing \u2014 multiple in-flight requests without head-of-line blocking\n- REST over HTTP/1.1 needs a new connection per request or connection pooling to approach similar efficiency; every new connection repeats the TCP and TLS handshake cost\n\n**Where the advantages compound:**\n- In service mesh scenarios with multiple hops, saving 5ms per hop across five services produces 25ms aggregate improvement on a single user request \u2014 the deeper the call chain, the larger the total payoff\n- Teams have reported cutting internal latency by 60\u201370% switching high-frequency REST calls to gRPC on measured hot paths\n\n**When gRPC's advantages don't matter:**\n- Below roughly 1,000 requests per second, the performance difference is unmeasurable; REST's debuggability with curl, Postman, and browser tooling is a real advantage that gRPC cannot match\n\nI recommend gRPC for east-west traffic on high-RPS internal paths where the performance savings are measurable, and REST at the edge and for low-traffic internal endpoints where debuggability matters more than microseconds."
          }
        ]
      },
      "followup_questions": [
        "How would you benchmark gRPC vs REST for your specific use case?",
        "Does HTTP/2 bring any performance benefits to REST that narrow the gap?",
        "What tools can you use to profile protobuf serialisation overhead in a Java application?"
      ],
      "seo": {
        "meta_title": "gRPC vs REST Performance Java \u2014 Why gRPC is Faster",
        "meta_description": "Deep dive into why gRPC outperforms REST/JSON: Protocol Buffers vs JSON serialisation, HTTP/2 vs HTTP/1.1, and when the performance difference actually matters.",
        "keywords": [
          "grpc vs rest performance",
          "grpc faster than rest",
          "protobuf vs json speed",
          "grpc http2 performance java"
        ]
      },
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P01",
        "hook": "gRPC is faster than REST for internal service calls for two compounding reasons: smaller payloads from Protocol Buffers and connection efficiency from HTTP/2.",
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "Saying \"gRPC is faster than REST\" without quantifying where the speed comes from is the kind of answer that makes interviewers stop listening. The performance gap exists for two independent reasons, and each one matters at a different scale. Layer 1: Serialization. Protocol Buffers produces binary messages that are 3\u201310\u00d7 smaller than equivalent JSON. JSON repeats field names in every single object \u2014 a list of 1,000 users includes the string \"username\" 1,000 times. Protobuf uses field numbers (1 byte."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "Saying \"gRPC is faster than REST\" without quantifying where the speed comes from is the kind of answer that makes interviewers stop listening. The performance gap exists for two independent reasons, and each one matters at a different scale. Layer 1: Serialization. Protocol Buffers produces binary messages that are 3\u201310\u00d7 smaller than equivalent JSON. JSON repeats field names in every single object \u2014 a list of 1,000 users includes the."
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "Protobuf payloads are 3\u201310\u00d7 smaller than JSON \u2014 field numbers replace repeated field names",
              "Serialization CPU savings reach 20\u201330% of a core at 10K msg/sec \u2014 protobuf's compiled schema uses direct memory offsets",
              "HTTP/2 multiplexing eliminates per-request TCP/TLS setup \u2014 one persistent connection carries all concurrent RPCs",
              "Advantages compound in service mesh fan-out \u2014 5ms saved per hop \u00d7 5 downstream services = 25ms aggregate improvement on a single user request",
              "Below ~1K RPS the difference is negligible \u2014 REST's debuggability (curl, Postman, browser) outweighs microsecond savings for low-traffic or"
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "Saying \"gRPC is faster than REST\" without quantifying where the speed comes from is the kind of answer that makes interviewers stop listening. The performance gap exists for two independent reasons, and each one matters at a different scale. Layer 1: Serialization. Protocol Buffers produces binary messages that are 3\u201310\u00d7 smaller than equivalent JSON. JSON repeats field names in every single object \u2014 a list of 1,000 users includes the string \"username\" 1,000 times. Protobuf uses field numbers (1 byte each) instead. JSON requires runtime parsing \u2014 the deserializer scans characters, handles string escaping, infers types dynamically. Protobuf's schema is compiled at build time, so deserialization is a direct memory copy with known offsets \u2014 no reflection, no type guessing."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Saying \"gRPC is faster than REST\" without quantifying where the speed comes from is the kind of answer that makes interviewers stop listening",
              "The performance gap exists for two independent reasons, and each one matters at a different scale",
              "Layer 1: Serialization",
              "Protocol Buffers produces binary messages that are 3\u201310\u00d7 smaller than equivalent JSON",
              "JSON repeats field names in every single object \u2014 a list of 1,000 users includes the string \"username\" 1,000 times"
            ]
          }
        ],
        "cap": "Protocol Buffers \u2014 the payload advantage: Binary encoding with field numbers instead of repeated key names produces payloads 3\u201310\u00d7 smaller than JSON; a list of 1,000 objects avoids repeating field nam",
        "familiarity_anchors": [
          "Protocol",
          "Buffers"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": [
          "How would you benchmark gRPC vs REST for your specific use case?",
          "Does HTTP/2 bring any performance benefits to REST that narrow the gap?",
          "What tools can you use to profile protobuf serialisation overhead in a Java application?"
        ]
      }
    },
    {
      "id": "grpc-008",
      "slug": "grpc-deadlines-cancellation-java",
      "question": "How do deadlines and cancellation work in gRPC, and why are they important?",
      "title": "Deadlines and Cancellation in gRPC",
      "direct_answer": "A gRPC deadline is a point in time by which a client expects a response. It is propagated through the entire call chain as part of the context. If the deadline expires, gRPC cancels the call with `Status.DEADLINE_EXCEEDED` on the client and sets the context as cancelled on the server. Every service should respect deadlines \u2014 checking `Context.current().isCancelled()` \u2014 to avoid doing unnecessary work after the client has already given up.",
      "layout_type": "concept-explainer",
      "interviewer_intent": "Tests understanding of resilience patterns. Deadlines are critical to preventing cascade failures \u2014 if a downstream service hangs, a client without a deadline will hold a thread or connection indefinitely, causing resource exhaustion upstream.",
      "company_tags": [
        "Google",
        "Netflix",
        "Lyft",
        "Square"
      ],
      "reading_time_minutes": 4,
      "last_updated": "2025-01-15",
      "difficulty": "intermediate",
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "The Cascade Failure That Deadlines Exist to Prevent",
            "content": "Picture a common microservice call chain: the API gateway calls the order service, which calls the inventory service, which calls the warehouse database. The warehouse database starts running slow \u2014 queries that normally take 50ms now take 30 seconds. Without deadlines, every layer waits. The inventory service holds a thread for 30 seconds. The order service holds a thread for 30 seconds. The gateway holds a thread for 30 seconds. At 100 concurrent requests, that's 300 blocked threads across three services \u2014 and none of them can serve new traffic.\n\nThis is the **cascade failure** that deadlines prevent. A deadline is an **absolute wall-clock timestamp** \u2014 not a relative timeout \u2014 by which the client expects a response. The critical distinction matters: if Service A sets a 500ms deadline and spends 300ms doing local work before calling Service B, Service B inherits only 200ms of remaining budget. A naive per-hop timeout of 500ms would give B a fresh window, masking the fact that A's client has already waited 300ms.\n\ngRPC transmits the deadline as the `grpc-timeout` header on every hop, and each downstream service sees the shrinking budget. When the deadline expires, two things happen simultaneously: the client receives `Status.DEADLINE_EXCEEDED` and stops waiting, and the server's `Context` is marked as cancelled. The server is responsible for checking `Context.current().isCancelled()` \u2014 gRPC doesn't kill server threads automatically. Without that check, the server finishes processing a request whose result nobody will ever read, wasting CPU, database connections, and queue capacity."
          },
          {
            "type": "step",
            "title": "Setting Deadlines on the Client \u2014 And What Happens When They Expire",
            "content": "Clients set deadlines with `withDeadlineAfter()` on the stub. gRPC converts the relative duration to an absolute timestamp on the wire. If the deadline passes before the server responds, the client receives a `StatusRuntimeException` with code `DEADLINE_EXCEEDED`.\n\n```java\ntry {\n  UserResponse response = stub\n      .withDeadlineAfter(500, TimeUnit.MILLISECONDS)\n      .getUser(UserRequest.newBuilder()\n          .setUserId(\"u-123\").build());\n} catch (StatusRuntimeException e) {\n  if (e.getStatus().getCode() == Status.Code.DEADLINE_EXCEEDED) {\n    log.warn(\"getUser timed out after 500ms\");\n    return fallbackResponse();       // degrade gracefully\n  }\n  throw e;\n}\n```\n\nThe choice of deadline value depends on the call's expected latency plus a safety margin. A service that typically responds in 50ms might get a 200ms deadline \u2014 enough headroom for occasional GC pauses or slow queries, but tight enough that a genuinely stuck service doesn't hold threads for minutes. Setting deadlines too aggressively causes spurious failures during normal load spikes; setting them too loosely defeats the purpose.\n\nWhen chaining downstream calls, the deadline propagates automatically through the `Context`. You can also set a tighter sub-deadline with `withDeadlineAfter()` on the downstream stub to reserve budget for your own post-processing. If you don't set an explicit sub-deadline, the downstream call inherits whatever budget remains from the original client deadline."
          },
          {
            "type": "step",
            "title": "Cooperative Cancellation \u2014 The Server's Responsibility",
            "content": "When a deadline expires, gRPC cancels the client side immediately but does **not** interrupt the server thread. The server must cooperate by checking cancellation inside loops or before expensive operations.\n\n```java\n@GrpcService\npublic class ReportServiceImpl\n    extends ReportServiceGrpc.ReportServiceImplBase {\n\n  @Override\n  public void generateReport(ReportRequest req,\n      StreamObserver<ReportChunk> obs) {\n\n    for (DataPage page : dataSource.pages(req.getQuery())) {\n      if (Context.current().isCancelled()) {\n        obs.onError(Status.CANCELLED\n            .withDescription(\"Client disconnected\")\n            .asRuntimeException());\n        return;                      // stop immediately\n      }\n      obs.onNext(processPage(page)); // send processed chunk\n    }\n    obs.onCompleted();\n  }\n}\n```\n\nWithout the `isCancelled()` check, a report generation that processes 100 pages would complete all 100 even if the client timed out after page 3 \u2014 burning CPU and database I/O for 97 pages of results that nobody receives. At scale, this wasted work consumes 30\u201340% of server capacity during peak load when deadlines are firing frequently.\n\nFor non-streaming RPCs that make a single expensive call (database query, external API), the cancellation check should happen before the call starts. If the remaining deadline budget is less than the call's expected latency, fail fast with `DEADLINE_EXCEEDED` rather than starting work you know will time out. You can read the remaining budget with `Context.current().getDeadline().timeRemaining(TimeUnit.MILLISECONDS)` and compare it against the operation's expected duration. This preemptive check is especially important for database queries \u2014 starting a 2-second query with only 500ms of budget remaining wastes a database connection and returns a timeout that could have been reported immediately."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Deadline = absolute timestamp, not relative timeout** \u2014 `grpc-timeout` propagates one wall-clock budget across all hops.\n- **Budget subtraction at each hop** \u2014 if Service A sets 500ms and uses 300ms before calling B, Service B inherits only 200ms.\n- **Server threads keep running after cancellation** \u2014 gRPC marks the Context as cancelled but does not interrupt the thread.\n- **Wasted work burns 30\u201340% of capacity at peak** \u2014 without cancellation checks, servers complete processing for requests whose results are discarded.\n- **An RPC without a deadline is a resource leak** \u2014 a hung downstream service holds threads indefinitely, causing thread pool exhaustion that cascades to."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "Deadlines are gRPC's primary resilience mechanism and are fundamentally different from per-hop timeouts. A deadline is an absolute wall-clock timestamp, not a relative duration \u2014 this distinction drives every cascading behavior in a distributed call chain.\n\n**How deadlines propagate:**\n- When you call `stub.withDeadlineAfter(500, MILLISECONDS)`, gRPC converts that to a wall-clock timestamp and propagates it in the `grpc-timeout` header to every downstream hop in the call chain\n- Budget subtraction: if Service A sets a 500ms deadline and spends 300ms doing local work before calling Service B, Service B inherits only 200ms \u2014 not a fresh 500ms window; this prevents the common pattern where each service gives a fresh timeout and the client's patience is exhausted invisibly\n\n**Server-side cancellation checking:**\n- gRPC marks the context as cancelled when the deadline expires but does not interrupt the server thread; without explicit checks, the server continues processing for results nobody will receive\n- Always check `Context.current().isCancelled()` inside loops and before expensive operations like database queries; without this, servers waste 30\u201340% of compute at peak completing work for disconnected clients\n\n**The cascade failure scenario:**\n- Without deadlines, a hung downstream service holds threads in every upstream service indefinitely \u2014 thread pool exhaustion cascades across the entire call chain\n- An RPC without a deadline is a resource leak; every thread waiting for a response that will never come is capacity unavailable for legitimate requests\n\nI recommend establishing a rule that every gRPC stub call gets a deadline \u2014 no exceptions \u2014 and enforcing it through code review and architecture standards rather than leaving it to individual developer discipline."
          }
        ]
      },
      "followup_questions": [
        "What is the difference between a deadline and a retry timeout?",
        "How do you implement deadline propagation when mixing gRPC and REST calls in the same request path?",
        "How does `Context.withCancellation()` differ from a deadline in gRPC?"
      ],
      "seo": {
        "meta_title": "gRPC Deadlines and Cancellation Java \u2014 Best Practices",
        "meta_description": "Learn how gRPC deadlines and cancellation work in Java. Setting withDeadlineAfter, propagating deadlines across services, and checking Context cancellation on the server.",
        "keywords": [
          "grpc deadline java",
          "grpc cancellation",
          "grpc withDeadlineAfter",
          "grpc DEADLINE_EXCEEDED",
          "grpc context cancellation"
        ]
      },
      "speakable_v2": {
        "archetype": "C",
        "pillar": "P01",
        "hook": "Deadlines are gRPC's primary resilience mechanism and are fundamentally different from per-hop timeouts.",
        "beats": [
          {
            "kind": "mental_model",
            "layout": "paragraph",
            "text": "Picture a common microservice call chain: the API gateway calls the order service, which calls the inventory service, which calls the warehouse database. The warehouse database starts running slow \u2014 queries that normally take 50ms now take 30 seconds. Without deadlines, every layer waits. The inventory service holds a thread for 30 seconds. The order service holds a thread for 30 seconds. The gateway holds a thread for 30 seconds. At 100 concurrent requests, that's 300 blocked threads across three services \u2014 and none of them can serve new traffic."
          },
          {
            "kind": "mechanism",
            "layout": "ordered_list",
            "steps": [
              "Clients set deadlines with withDeadlineAfter() on the stub.",
              "gRPC converts the relative duration to an absolute timestamp on the wire.",
              "If the deadline passes before the server responds, the client receives a StatusRuntimeException with code DEADLINE_EXCEEDED.",
              "``java try { UserResponse response = stub .withDeadlineAfter(500, TimeUnit.MILLISECONDS) .getUser(UserRequest.newBuilder() .setUserId(\"u-123\").build()); } catch (StatusRuntimeException e) { if (e.getStatus().getCode() == Status.Code.DEADLINE_EXCEEDED) { log.warn(\"getUser timed out after 500ms\"); return fallbackResponse(); // degrade gracefully } throw e; } ` The choice of deadline value depends on the call's expected latency plus a safety margin.",
              "A service that typically responds in 50ms might get a 200ms deadline \u2014 enough headroom for occasional GC pauses or slow queries, but tight enough that a genuinely stuck service doesn't hold threads for minutes.",
              "Setting deadlines too aggressively causes spurious failures during normal load spikes; setting them too loosely defeats the purpose."
            ]
          },
          {
            "kind": "edge_cases",
            "layout": "bullets",
            "items": [
              "Deadline = absolute timestamp, not relative timeout \u2014 grpc-timeout propagates one wall-clock budget across all hops",
              "Budget subtraction at each hop \u2014 if Service A sets 500ms and uses 300ms before calling B, Service B inherits only 200ms",
              "Server threads keep running after cancellation \u2014 gRPC marks the Context as cancelled but does not interrupt the thread",
              "Wasted work burns 30\u201340% of capacity at peak \u2014 without cancellation checks, servers complete processing for requests whose results are discarded",
              "An RPC without a deadline is a resource leak \u2014 a hung downstream service holds threads indefinitely, causing thread pool exhaustion that cascades to"
            ]
          },
          {
            "kind": "failure_mode",
            "layout": "callout",
            "text": "The production failure mode here is unchecked state mutation under concurrent access."
          }
        ],
        "cap": "How deadlines propagate: When you call stub.withDeadlineAfter(500, MILLISECONDS), gRPC converts that to a wall-clock timestamp and propagates it in the grpc-timeout header to every downstream hop in t",
        "familiarity_anchors": [
          "Status",
          "Every",
          "Context"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": [
          "What is the difference between a deadline and a retry timeout?",
          "How do you implement deadline propagation when mixing gRPC and REST calls in the same request path?",
          "How does `Context.withCancellation()` differ from a deadline in gRPC?"
        ]
      }
    },
    {
      "id": "grpc-009",
      "slug": "grpc-health-checking-protocol",
      "question": "What is the gRPC health checking protocol and how do you implement it in Spring Boot?",
      "title": "gRPC Health Checking Protocol",
      "direct_answer": "The gRPC Health Checking Protocol is a standard gRPC service defined by Google (`grpc.health.v1.Health`) that exposes a `Check` (unary) and `Watch` (server streaming) RPC for liveness and readiness probes. Load balancers and orchestrators (Kubernetes, Envoy) call `Check` to determine whether a service is ready to accept traffic. The `grpc-spring-boot-starter` auto-configures this service and integrates it with Spring's `HealthIndicator` beans.",
      "layout_type": "concept-explainer",
      "interviewer_intent": "Checks production awareness. Running gRPC in Kubernetes without health checks means the orchestrator cannot detect unhealthy pods. Interviewers want to see you know both the protocol standard and the Spring Boot integration.",
      "company_tags": [
        "Google",
        "Kubernetes",
        "HashiCorp",
        "Lyft"
      ],
      "reading_time_minutes": 4,
      "last_updated": "2025-01-15",
      "difficulty": "intermediate",
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Why HTTP Health Endpoints Don't Work for gRPC Services",
            "content": "When Kubernetes needs to check if a REST service is healthy, it sends an HTTP GET to `/actuator/health` and checks for a 200 response. Simple. But a gRPC server speaks HTTP/2 with binary protobuf \u2014 an HTTP/1.1 GET request to a gRPC port returns garbage or a connection error. The orchestrator thinks the service is dead when it's perfectly healthy.\n\nBefore Kubernetes 1.24, teams worked around this by running a sidecar HTTP health endpoint or using `grpc_health_probe` as a custom exec command. Both approaches were fragile \u2014 the sidecar could be healthy while the gRPC server was crashed, and the exec probe had a cold-start cost on every check. Kubernetes 1.24 added **native gRPC probes** that speak the protocol directly, eliminating these workarounds.\n\nThe gRPC Health Checking Protocol is defined in `grpc.health.v1.Health` \u2014 a standard `.proto` file that all gRPC implementations understand. It exposes two RPCs: `Check` returns the current health status as one of `SERVING`, `NOT_SERVING`, `UNKNOWN`, or `SERVICE_UNKNOWN`. `Watch` is a server-streaming RPC that pushes status changes in real time \u2014 useful for load balancers like Envoy that want to react to health transitions without constant polling.\n\nThe protocol supports **per-service granularity** through the `service` field in the request. An empty string checks overall health, but you can report different statuses for different services on the same server \u2014 the `UserService` is healthy while the `PaymentService` is degraded because its downstream database is slow. This lets the load balancer route around a specific degraded service without marking the entire pod as unhealthy."
          },
          {
            "type": "step",
            "title": "Step 1 \u2014 Auto-Configuration in Spring Boot and Actuator Integration",
            "content": "The `grpc-spring-boot-starter` registers the `grpc.health.v1.Health` service automatically \u2014 no code needed. It bridges Spring Actuator's `HealthIndicator` beans to gRPC health status: when Spring reports DOWN (database unreachable, Redis timeout), the gRPC health service flips to `NOT_SERVING`, and Kubernetes stops routing traffic to that pod.\n\n```yaml\ngrpc:\n  server:\n    port: 9090\n    health-service-enabled: true    # default is true\n\nmanagement:\n  endpoint:\n    health:\n      probes:\n        enabled: true               # enable liveness/readiness separation\n```\n\nYou can verify the health service is responding using `grpcurl` from the command line \u2014 this is the gRPC equivalent of curling an HTTP health endpoint.\n\n```bash\n# Check overall health\ngrpcurl -plaintext localhost:9090 grpc.health.v1.Health/Check\n# Response: { \"status\": \"SERVING\" }\n\n# Check a specific service\ngrpcurl -plaintext -d '{\"service\": \"OrderService\"}' \\\n  localhost:9090 grpc.health.v1.Health/Check\n```\n\nIf your application has custom health requirements beyond Spring Actuator's defaults \u2014 for example, verifying that a Kafka consumer is caught up or that a cache is warm \u2014 implement a `HealthIndicator` bean. The starter picks it up automatically and includes it in the gRPC health response."
          },
          {
            "type": "step",
            "title": "Step 2 \u2014 Configure Kubernetes Native gRPC Probes",
            "content": "Kubernetes 1.24+ supports gRPC probes directly in the pod spec. The kubelet calls the `Check` RPC on the configured port, and the pod transitions to unready if the response is anything other than `SERVING`.\n\n```yaml\napiVersion: v1\nkind: Pod\nspec:\n  containers:\n  - name: order-service\n    image: myregistry/order-service:2.1\n    ports:\n    - containerPort: 9090\n    livenessProbe:\n      grpc:\n        port: 9090\n      initialDelaySeconds: 10\n      periodSeconds: 10\n    readinessProbe:\n      grpc:\n        port: 9090\n        service: \"\"              # empty = overall health\n      initialDelaySeconds: 5\n      periodSeconds: 5\n```\n\nThe critical production rule is **separating liveness from readiness**. Liveness checks whether the process itself is healthy \u2014 a stuck thread, a deadlock, an out-of-memory condition. If liveness fails, Kubernetes restarts the pod. Readiness checks whether the pod can accept traffic \u2014 database connected, caches warm, downstream dependencies available. If readiness fails, Kubernetes removes the pod from the Service's endpoint list but doesn't restart it.\n\nConfusing the two causes cascading restarts. If a database goes slow and you report that as a liveness failure, Kubernetes restarts every pod in the deployment. The restarted pods reconnect to the same slow database, fail liveness again, and restart in a loop \u2014 an outage that amplifies the original problem. Use readiness for dependency failures (stop traffic, let the dependency recover) and liveness only for process-level problems (the JVM is stuck)."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **HTTP health checks don't work on gRPC ports** \u2014 an HTTP/1.1 GET to a gRPC server returns garbage.\n- **`grpc-spring-boot-starter` auto-registers the health service** \u2014 bridges Spring `HealthIndicator` beans to gRPC status.\n- **Per-service granularity via the `service` field** \u2014 an empty string checks overall health.\n- **Liveness \u2260 readiness** \u2014 liveness failure restarts the pod; readiness failure stops traffic without restart.\n- **`Watch` RPC for reactive health monitoring** \u2014 server-streaming RPC pushes status transitions to Envoy in real time without constant polling overhead."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "The gRPC Health Checking Protocol is a standard service every production gRPC deployment needs from day one. HTTP health endpoints don't work on gRPC ports \u2014 an HTTP GET to a gRPC port returns garbage because gRPC speaks HTTP/2 with binary protobuf framing.\n\n**The protocol:**\n- The standard service at `grpc.health.v1.Health` exposes two RPCs: `Check` returns the current status \u2014 `SERVING` or `NOT_SERVING` \u2014 and `Watch` is a server-streaming RPC that pushes status changes in real time to load balancers without constant polling\n- The `service` field provides per-service granularity \u2014 an empty string checks overall health; a specific service name lets load balancers route around degraded services without marking the entire pod unhealthy\n\n**Spring Boot integration:**\n- With `grpc-spring-boot-starter`, the health service is auto-configured and wired to Spring Actuator's health indicators; if a database connection fails, Actuator reports DOWN, the gRPC health flips to `NOT_SERVING`, and Kubernetes removes the pod from traffic rotation\n- Kubernetes 1.24+ supports native gRPC probes \u2014 configure `grpc.port: 9090` in `livenessProbe` and `readinessProbe` directly; no workarounds needed\n\n**The liveness vs readiness distinction \u2014 the most important rule:**\n- Liveness failure triggers a pod restart; readiness failure stops traffic without restarting\n- A slow database should fail readiness (drain traffic) but not liveness (don't restart the pod); confusing the two causes cascading restarts that amplify an existing outage \u2014 every restart triggers a reconnect storm that makes a struggling database worse\n\nI recommend wiring health checks to your database and external dependency indicators from day one, and always keeping liveness and readiness as separate probes with different thresholds."
          }
        ]
      },
      "followup_questions": [
        "How do you report per-service health (e.g. only the OrderService is NOT_SERVING) using the health checking protocol?",
        "How does Envoy use the Watch RPC for endpoint health in a service mesh?",
        "What happens to in-flight gRPC calls when Kubernetes sends SIGTERM to a pod?"
      ],
      "seo": {
        "meta_title": "gRPC Health Checking Spring Boot \u2014 Kubernetes Probes",
        "meta_description": "Implement the gRPC Health Checking Protocol in Spring Boot. Auto-configure GrpcHealthService, integrate with Actuator health indicators, and set up Kubernetes gRPC probes.",
        "keywords": [
          "grpc health check spring boot",
          "grpc health checking protocol",
          "kubernetes grpc probe",
          "grpc.health.v1.Health"
        ]
      },
      "speakable_v2": {
        "archetype": "C",
        "pillar": "P01",
        "hook": "The gRPC Health Checking Protocol is a standard service every production gRPC deployment needs from day one.",
        "beats": [
          {
            "kind": "mental_model",
            "layout": "paragraph",
            "text": "When Kubernetes needs to check if a REST service is healthy, it sends an HTTP GET to /actuator/health and checks for a 200 response. Simple. But a gRPC server speaks HTTP/2 with binary protobuf \u2014 an HTTP/1.1 GET request to a gRPC port returns garbage or a connection error. The orchestrator thinks the service is dead when it's perfectly healthy. Before Kubernetes 1.24, teams worked around this by running a sidecar HTTP health endpoint or using grpc_health_probe as a custom exec command. Both approaches were fragile \u2014 the sidecar could."
          },
          {
            "kind": "mechanism",
            "layout": "ordered_list",
            "steps": [
              "The grpc-spring-boot-starter registers the grpc.health.v1.Health service automatically \u2014 no code needed.",
              "It bridges Spring Actuator's HealthIndicator beans to gRPC health status: when Spring reports DOWN (database unreachable, Redis timeout), the gRPC health service flips to NOT_SERVING, and Kubernetes stops routing traffic to that pod.",
              "``yaml grpc: server: port: 9090 health-service-enabled: true # default is true management: endpoint: health: probes: enabled: true # enable liveness/readiness separation ` You can verify the health service is responding using grpcurl from the command line \u2014 this is the gRPC equivalent of curling an HTTP health endpoint.",
              "`bash Check overall health grpcurl -plaintext localhost:9090 grpc.health.v1.Health/Check Response: { \"status\": \"SERVING\" } Check a specific service grpcurl -plaintext -d '{\"service\": \"OrderService\"}' \\ localhost:9090 grpc.health.v1.Health/Check ` If your application has custom health requirements beyond Spring Actuator's defaults \u2014 for example, verifying that a Kafka consumer is caught up."
            ]
          },
          {
            "kind": "edge_cases",
            "layout": "bullets",
            "items": [
              "HTTP health checks don't work on gRPC ports \u2014 an HTTP/1",
              "1 GET to a gRPC server returns garbage",
              "grpc-spring-boot-starter auto-registers the health service \u2014 bridges Spring HealthIndicator beans to gRPC status",
              "Per-service granularity via the service field \u2014 an empty string checks overall health",
              "Liveness \u2260 readiness \u2014 liveness failure restarts the pod; readiness failure stops traffic without restart"
            ]
          },
          {
            "kind": "failure_mode",
            "layout": "callout",
            "text": "The production failure mode here is unchecked state mutation under concurrent access."
          }
        ],
        "cap": "The protocol: The standard service at grpc.health.v1.Health exposes two RPCs: Check returns the current status \u2014 SERVING or NOT_SERVING \u2014 and Watch is a server-streaming RPC that pushes status changes",
        "familiarity_anchors": [
          "Health",
          "Checking",
          "Protocol"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": [
          "How do you report per-service health (e.g. only the OrderService is NOT_SERVING) using the health checking protocol?",
          "How does Envoy use the Watch RPC for endpoint health in a service mesh?",
          "What happens to in-flight gRPC calls when Kubernetes sends SIGTERM to a pod?"
        ]
      }
    },
    {
      "id": "grpc-010",
      "slug": "testing-grpc-services-java",
      "question": "How do you test gRPC services in Java \u2014 unit tests, integration tests, and end-to-end tests?",
      "title": "Testing gRPC Services in Java",
      "direct_answer": "Test gRPC services at three levels: unit tests that mock `StreamObserver` and call the service implementation directly; integration tests that use `grpc-spring-boot-starter`'s `@SpringBootTest` with an in-process server; and end-to-end tests that start a real server and call it via a generated blocking stub. The `grpc-testing` library provides `GrpcCleanupRule` (JUnit 4) and `GrpcCleanupExtension` (JUnit 5) to manage in-process channel lifecycle.",
      "layout_type": "recipe-builder",
      "interviewer_intent": "Tests whether the candidate knows the full testing pyramid for gRPC and can write deterministic, fast tests without needing a live network. Interviewers want to see awareness of the gRPC testing utilities, not just 'I curl it'.",
      "company_tags": [
        "Google",
        "Lyft",
        "Stripe",
        "Shopify"
      ],
      "reading_time_minutes": 5,
      "last_updated": "2025-01-15",
      "difficulty": "intermediate",
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Why MockMvc Can't Test gRPC \u2014 And What Replaces It",
            "content": "In a REST world, `MockMvc` lets you test controllers without starting a real HTTP server \u2014 it simulates the entire request pipeline in-memory. When you migrate to gRPC, that tool disappears. `MockMvc` speaks HTTP/1.1 with JSON bodies; gRPC speaks HTTP/2 with binary protobuf and a completely different header and trailer model. Your first instinct \u2014 \"I'll just curl the gRPC port in a test\" \u2014 creates slow, flaky tests that depend on port availability and network behavior.\n\ngRPC's testing toolchain replaces `MockMvc` with two mechanisms. For unit tests, the service implementation is a plain Java class \u2014 instantiate it directly, pass a mocked `StreamObserver`, and assert on what gets sent. No server, no network, no Spring context. Tests run in single-digit milliseconds.\n\nFor integration tests, `InProcessServerBuilder` and `InProcessChannelBuilder` from the `grpc-testing` library start a real gRPC server and client inside the same JVM with no TCP socket. The server processes requests through the full protobuf serialization path, interceptor chain, and `StreamObserver` lifecycle \u2014 catching bugs that unit tests miss (metadata propagation, interceptor ordering, serialization edge cases) \u2014 while remaining fast and deterministic.\n\nThe testing pyramid still applies: many fast unit tests for business logic, fewer integration tests for wiring and interceptor behavior, and a small number of end-to-end tests that exercise the full Spring Boot context including `@GrpcService` auto-configuration and `@GrpcClient` injection."
          },
          {
            "type": "step",
            "title": "Step 1 \u2014 Unit Test: Mock StreamObserver and Call the Service Directly",
            "content": "The service implementation is a plain Java class that extends a generated base class. Instantiate it directly \u2014 no Spring context, no gRPC server, no network. Mock the `StreamObserver` with Mockito, call the method, and use `ArgumentCaptor` to grab the response.\n\n```java\nimport io.grpc.stub.StreamObserver;\nimport org.junit.jupiter.api.Test;\nimport org.mockito.ArgumentCaptor;\nimport static org.assertj.core.api.Assertions.assertThat;\nimport static org.mockito.Mockito.*;\n\nclass OrderServiceImplTest {\n\n  private final OrderServiceImpl service =\n      new OrderServiceImpl(new FakeOrderRepository());\n\n  @Test\n  void getOrder_returnsOrder() {\n    OrderRequest request = OrderRequest.newBuilder()\n        .setOrderId(\"ord-1\").build();\n\n    @SuppressWarnings(\"unchecked\")\n    StreamObserver<OrderReply> observer = mock(StreamObserver.class);\n    ArgumentCaptor<OrderReply> captor =\n        ArgumentCaptor.forClass(OrderReply.class);\n\n    service.getOrder(request, observer);\n\n    verify(observer).onNext(captor.capture());\n    verify(observer).onCompleted();\n    verify(observer, never()).onError(any());\n    assertThat(captor.getValue().getStatus()).isEqualTo(\"CONFIRMED\");\n  }\n}\n```\n\nThis pattern runs in single-digit milliseconds and verifies three things: `onNext` was called with the expected response, `onCompleted` was called to close the stream, and `onError` was never called. These are the three `StreamObserver` contract guarantees that every gRPC service method must satisfy. Use fake repositories or Mockito stubs to isolate business logic from infrastructure \u2014 the goal is testing your code, not protobuf serialization."
          },
          {
            "type": "step",
            "title": "Step 2 \u2014 Integration Test: In-Process Server Without Network",
            "content": "The `grpc-testing` library provides `InProcessServerBuilder` and `InProcessChannelBuilder` that start a real gRPC server and client inside the same JVM. No TCP socket, no port allocation, no flaky network. The `directExecutor()` option runs everything on the test thread for deterministic sequencing.\n\n```java\nimport io.grpc.ManagedChannel;\nimport io.grpc.inprocess.InProcessChannelBuilder;\nimport io.grpc.inprocess.InProcessServerBuilder;\nimport io.grpc.testing.GrpcCleanupRule;\nimport org.junit.Rule;\nimport org.junit.Test;\nimport static org.assertj.core.api.Assertions.assertThat;\n\npublic class OrderServiceIntegrationTest {\n\n  @Rule\n  public final GrpcCleanupRule grpcCleanup = new GrpcCleanupRule();\n\n  @Test\n  public void getOrder_throughFullStack() throws Exception {\n    String name = InProcessServerBuilder.generateName();\n\n    grpcCleanup.register(\n        InProcessServerBuilder.forName(name)\n            .directExecutor()\n            .addService(new OrderServiceImpl(new RealRepository()))\n            .addService(new AuthInterceptor())    // test interceptors too\n            .build().start());\n\n    ManagedChannel channel = grpcCleanup.register(\n        InProcessChannelBuilder.forName(name)\n            .directExecutor().build());\n\n    OrderServiceGrpc.OrderServiceBlockingStub stub =\n        OrderServiceGrpc.newBlockingStub(channel);\n\n    OrderReply reply = stub.getOrder(\n        OrderRequest.newBuilder().setOrderId(\"ord-1\").build());\n\n    assertThat(reply.getStatus()).isEqualTo(\"CONFIRMED\");\n  }\n}\n```\n\nThis level exercises the full protobuf serialization, interceptor chain, and status code mapping. `GrpcCleanupRule` registers shutdown hooks so channels and servers close deterministically after each test \u2014 without it, leaked channels accumulate threads and eventually cause `OutOfMemoryError` in long test suites. For JUnit 5, use the equivalent extension or `@AfterEach` manual cleanup."
          },
          {
            "type": "step",
            "title": "Step 3 \u2014 Spring Boot Test: Full Context with In-Process Transport",
            "content": "Use `@SpringBootTest` with test properties that route the gRPC server and client through in-process transport. This boots the entire Spring context \u2014 dependency injection, `@GrpcService` auto-discovery, interceptors, health service \u2014 and validates that the starter's auto-configuration wires everything correctly.\n\n```java\nimport net.devh.boot.grpc.client.inject.GrpcClient;\nimport org.junit.jupiter.api.Test;\nimport org.springframework.boot.test.context.SpringBootTest;\nimport org.springframework.test.annotation.DirtiesContext;\nimport static org.assertj.core.api.Assertions.*;\n\n@SpringBootTest(properties = {\n    \"grpc.server.port=-1\",\n    \"grpc.server.in-process-name=test\",\n    \"grpc.client.order.address=in-process:test\"\n})\n@DirtiesContext\nclass OrderServiceSpringTest {\n\n  @GrpcClient(\"order\")\n  private OrderServiceGrpc.OrderServiceBlockingStub stub;\n\n  @Test\n  void getOrder_springContext() {\n    OrderReply reply = stub.getOrder(\n        OrderRequest.newBuilder().setOrderId(\"ord-1\").build());\n    assertThat(reply.getStatus()).isEqualTo(\"CONFIRMED\");\n  }\n\n  @Test\n  void getOrder_notFound_returnsCorrectStatus() {\n    assertThatThrownBy(() -> stub.getOrder(\n        OrderRequest.newBuilder().setOrderId(\"missing\").build()))\n      .isInstanceOf(StatusRuntimeException.class)\n      .satisfies(ex -> assertThat(\n          ((StatusRuntimeException) ex).getStatus().getCode())\n          .isEqualTo(Status.Code.NOT_FOUND));\n  }\n}\n```\n\nSetting `grpc.server.port=-1` disables the TCP server entirely \u2014 all traffic flows through the in-process transport, eliminating port conflicts in CI. The error path test is critical: clients depend on specific status codes for retry decisions and error display. If your `.proto` documents error conditions, your tests must verify them. Untested error paths are the first thing that breaks in production when real clients encounter edge cases the happy-path tests never exercised."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **`MockMvc` doesn't work for gRPC** \u2014 it speaks HTTP/1.1+JSON; gRPC requires direct service invocation (unit) or in-process server/channel (integration).\n- **Mocked `StreamObserver` unit tests run in <10ms** \u2014 verify `onNext`, `onCompleted`, and `onError` contract without any server or Spring context.\n- **`InProcessServerBuilder` exercises the full protobuf path** \u2014 serialization, interceptor chain, and status code mapping without TCP sockets.\n- **`grpc.server.port=-1` disables TCP in Spring tests** \u2014 combined with `in-process-name`, routes all traffic through in-process transport.\n- **Error path tests are non-negotiable** \u2014 assert specific `Status.Code` values on the thrown `StatusRuntimeException`."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "The key difference from REST testing is that `MockMvc` doesn't work for gRPC \u2014 it speaks HTTP/1.1 with JSON, and gRPC needs HTTP/2 with protobuf. Test at three levels with the right tools for each.\n\n**Unit tests \u2014 fastest and simplest:**\n- Instantiate the service directly as a plain Java object, pass a mocked `StreamObserver`, and assert that `onNext` was called with the expected response and `onCompleted` was called; no Spring, no server, no network\n- Use `ArgumentCaptor` to inspect the response object's fields; these run in under 10 milliseconds and give fast feedback on business logic\n\n**Integration tests \u2014 full gRPC pipeline without TCP:**\n- Use `InProcessServerBuilder` from the `grpc-testing` library to start a real gRPC server inside the JVM with no TCP socket; this exercises full protobuf serialization and the interceptor chain while staying fast and deterministic\n- `GrpcCleanupRule` handles channel and server shutdown so you don't leak threads or channels across test runs\n\n**Spring Boot tests \u2014 full context, no port binding:**\n- Use `@SpringBootTest` with `grpc.server.port=-1` and `in-process-name` to route everything through in-process transport; the full Spring context boots \u2014 `@GrpcService` discovery, interceptors, health service \u2014 but without binding a real port, eliminating CI port conflicts\n\n**Error path tests \u2014 non-negotiable:**\n- Assert that the stub throws `StatusRuntimeException` with the correct status code for every error scenario \u2014 `NOT_FOUND` for missing entities, `INVALID_ARGUMENT` for bad input, `UNAUTHENTICATED` for missing tokens\n- Untested error paths are the first thing that breaks when real clients hit edge cases; the test `assertThrows(StatusRuntimeException.class, () -> stub.callMethod(...)).getStatus().getCode()` pattern is straightforward\n\nI recommend unit tests for every service method, integration tests for every interceptor behavior, and at least one error path test per distinct `Status.Code` the service can return."
          }
        ]
      },
      "followup_questions": [
        "How do you test gRPC interceptors in isolation?",
        "How do you mock a downstream gRPC service dependency in a Spring Boot integration test?",
        "How do you test server-streaming or bidirectional-streaming RPCs with JUnit?"
      ],
      "seo": {
        "meta_title": "Testing gRPC Services Java \u2014 Unit, Integration, Spring Boot",
        "meta_description": "Complete guide to testing gRPC services in Java: mock StreamObserver unit tests, InProcessServerBuilder integration tests, and @SpringBootTest with grpc-spring-boot-starter.",
        "keywords": [
          "testing grpc java",
          "grpc unit test mockito",
          "InProcessServerBuilder junit",
          "grpc spring boot test",
          "grpc integration test"
        ]
      },
      "speakable_v2": {
        "archetype": "C",
        "pillar": "P01",
        "hook": "The key difference from REST testing is that MockMvc doesn't work for gRPC \u2014 it speaks HTTP/1.1 with JSON, and gRPC needs HTTP/2 with protobuf.",
        "beats": [
          {
            "kind": "mental_model",
            "layout": "paragraph",
            "text": "In a REST world, MockMvc lets you test controllers without starting a real HTTP server \u2014 it simulates the entire request pipeline in-memory. When you migrate to gRPC, that tool disappears. MockMvc speaks HTTP/1.1 with JSON bodies; gRPC speaks HTTP/2 with binary protobuf and a completely different header and trailer model. Your first instinct \u2014 \"I'll just curl the gRPC port in a test\" \u2014 creates slow, flaky tests that depend on port availability and network behavior. gRPC's testing toolchain replaces MockMvc with two mechanisms. For unit tests, the service."
          },
          {
            "kind": "mechanism",
            "layout": "ordered_list",
            "steps": [
              "The service implementation is a plain Java class that extends a generated base class.",
              "Instantiate it directly \u2014 no Spring context, no gRPC server, no network.",
              "Mock the StreamObserver with Mockito, call the method, and use ArgumentCaptor to grab the response.",
              "``java import io.grpc.stub.StreamObserver; import org.junit.jupiter.api.Test; import org.mockito.ArgumentCaptor; import static org.assertj.core.api.Assertions.assertThat; import static org.mockito.Mockito.*; class OrderServiceImplTest { private final OrderServiceImpl service = new OrderServiceImpl(new FakeOrderRepository()); @Test void getOrder_returnsOrder() { OrderRequest request = OrderRequest.newBuilder() .setOrderId(\"ord-1\").build(); @SuppressWarnings(\"unchecked\") StreamObserver<OrderReply> observer = mock(StreamObserver.class); ArgumentCaptor<OrderReply> captor = ArgumentCaptor.forClass(OrderReply.class); service.getOrder(request, observer); verify(observer).onNext(captor.capture()); verify(observer).onCompleted(); verify(observer, never()).onError(any()); assertThat(captor.getValue().getStatus()).isEqualTo(\"CONFIRMED\"); } } ` This pattern runs in single-digit milliseconds and verifies three things: onNext was called with the expected response, onCompleted was called to close the stream, and onError was never called.",
              "These are the three StreamObserver` contract guarantees that every gRPC service method must satisfy.",
              "Use fake repositories or."
            ]
          },
          {
            "kind": "edge_cases",
            "layout": "bullets",
            "items": [
              "MockMvc doesn't work for gRPC \u2014 it speaks HTTP/1",
              "1+JSON; gRPC requires direct service invocation (unit) or in-process server/channel (integration)",
              "Mocked StreamObserver unit tests run in <10ms \u2014 verify onNext, onCompleted, and onError contract without any server or Spring context",
              "InProcessServerBuilder exercises the full protobuf path \u2014 serialization, interceptor chain, and status code mapping without TCP sockets",
              "port=-1 disables TCP in Spring tests \u2014 combined with in-process-name, routes all traffic through in-process transport"
            ]
          },
          {
            "kind": "failure_mode",
            "layout": "callout",
            "text": "The production failure mode here is unchecked state mutation under concurrent access."
          }
        ],
        "cap": "Unit tests \u2014 fastest and simplest: Instantiate the service directly as a plain Java object, pass a mocked StreamObserver, and assert that onNext was called with the expected response and onCompleted w",
        "familiarity_anchors": [
          "StreamObserver",
          "SpringBootTest",
          "GrpcCleanupRule"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": [
          "How do you test gRPC interceptors in isolation?",
          "How do you mock a downstream gRPC service dependency in a Spring Boot integration test?",
          "How do you test server-streaming or bidirectional-streaming RPCs with JUnit?"
        ]
      }
    }
  ]
}
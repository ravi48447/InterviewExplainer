{
  "topic": "Comparisons",
  "topicSlug": "comparisons",
  "questions": [
    {
      "id": "spring-boot-vs-spring-framework",
      "slug": "spring-boot-vs-spring-framework-difference",
      "question": "What is the difference between Spring Framework and Spring Boot?",
      "title": "Spring Framework vs Spring Boot \u2014 What's the Difference?",
      "direct_answer": "Spring Boot is not a replacement for Spring Framework \u2014 it's an opinionated layer on top that eliminates boilerplate configuration. Spring provides the ecosystem (IoC, AOP, MVC, Data, Security), while Spring Boot provides auto-configuration, embedded servers, starter dependencies, and production-ready defaults so you can go from zero to running application in minutes instead of hours.",
      "layout_type": "comparison-arena",
      "interviewer_intent": {
        "testing": "Whether the candidate understands that Spring Boot builds on top of Spring Framework rather than replacing it.",
        "common_mistake": "Saying 'Spring Boot is a different framework' or treating them as competitors rather than layers in the same stack.",
        "to_stand_out": "Explain that understanding raw Spring is still essential for debugging auto-configuration issues, and mention the --debug flag for the conditions evaluation report."
      },
      "company_tags": [
        "Amazon",
        "Google",
        "Infosys",
        "Wipro",
        "TCS"
      ],
      "reading_time_minutes": 5,
      "last_updated": "2026-04-12",
      "difficulty": "intermediate",
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Boot Is Not a Separate Framework \u2014 It's the Configuration You'd Write Yourself",
            "content": "The most common way candidates fail this question is by framing Spring Boot as a competitor to Spring Framework. In reality, **every Spring Boot application is a Spring Framework application** \u2014 Boot is an opinionated configuration layer that sits on top of the same IoC container, the same AOP proxy infrastructure, and the same MVC dispatcher you'd wire manually.\n\nWhat Boot actually replaces is ceremony, not capability. In a raw Spring project, you write `@Configuration` classes to define your `DataSource`, register your `ViewResolver`, configure your `DispatcherServlet`, and manually align dozens of transitive dependency versions. A single version mismatch between Hibernate and Spring Data JPA can cost you a full day of debugging `NoSuchMethodError` stack traces that reveal nothing about the actual problem.\n\n**Spring Boot eliminates that configuration tax** through three mechanisms: auto-configuration classes that detect what's on your classpath and apply sensible defaults, starter POMs that bundle pre-tested dependency versions, and an embedded server that turns deployment from a multi-step WAR packaging process into `java -jar app.jar`. The framework underneath hasn't changed \u2014 you've just stopped writing the boilerplate that connects it."
          },
          {
            "type": "comparison_table",
            "title": "What Boot Automates vs What Spring Framework Still Controls",
            "content": "The table below isn't a feature list \u2014 it maps each pain point in raw Spring to the specific Boot mechanism that eliminates it.\n\n| Concern | Spring Framework (Manual) | Spring Boot (Auto-Configured) |\n|---|---|---|\n| **Bean configuration** | Write `@Configuration` classes for every integration | `@ConditionalOnClass` auto-detects and wires beans |\n| **Server deployment** | Package as WAR, deploy to external Tomcat or JBoss | Embedded Tomcat starts from `main()` \u2014 one JAR, no server install |\n| **Dependency versions** | Manually align 30+ transitive JARs | Starter POMs with a tested version matrix managed by the Spring team |\n| **DataSource setup** | Define `HikariDataSource` bean with explicit URL and pool config | Add `spring.datasource.url` to `application.yml` \u2014 HikariCP auto-configured |\n| **Production monitoring** | Build custom health checks and metrics endpoints from scratch | Actuator exposes `/health`, `/metrics`, `/info` out of the box |\n| **Property management** | `PropertyPlaceholderConfigurer` with manual file loading | `application.yml` loaded automatically with profile support built in |\n\nNotice the pattern: **the right column is what Boot does for you, the middle column is what you debug when it does the wrong thing.** That middle column is why understanding raw Spring still matters \u2014 every auto-configuration decision Boot makes maps directly to a manual configuration step you'd take yourself."
          },
          {
            "type": "when_to_use",
            "title": "When Auto-Configuration Does Something Unexpected \u2014 The --debug Flag That Most Developers Don't Know",
            "content": "Every Spring Boot auto-configuration class is guarded by `@Conditional` annotations \u2014 `@ConditionalOnClass` checks if a class exists on the classpath, `@ConditionalOnMissingBean` backs off when you define your own bean, and `@ConditionalOnProperty` checks for specific property values. This conditional mechanism is what makes Boot feel like magic, and it's exactly what you need to understand when that magic breaks.\n\nThe most common production surprise is a bean you expected to be auto-configured that isn't \u2014 or one you didn't expect that is. When this happens, **run your application with the `--debug` flag** (or set `debug=true` in `application.properties`). Spring Boot prints a conditions evaluation report showing every auto-configuration class that was matched or excluded, with the exact condition that decided it.\n\n```\n=== CONDITIONS EVALUATION REPORT ===\nPositive matches:\n   DataSourceAutoConfiguration matched:\n      - @ConditionalOnClass found 'javax.sql.DataSource'\n\nNegative matches:\n   ActiveMQAutoConfiguration:\n      Did not match:\n         - @ConditionalOnClass did not find 'javax.jms.ConnectionFactory'\n```\n\nThis report tells you exactly which decisions Boot made and why. When a `DataSource` isn't being created despite having the right dependency, the report will show which `@Conditional` check failed. **Being able to explain this debugging workflow** in an interview is what separates a developer who uses Boot from one who understands it. The right mental model isn't \"Spring Boot is simpler\" \u2014 it's \"Spring Boot automates the decisions I'd make manually, and I know how to audit those decisions when they go wrong.\""
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Spring Boot is not a replacement \u2014 it is Spring Framework with conventions applied** \u2014 auto-configuration, embedded servers, and starter POMs eliminate XML setup; the underlying IoC container, AOP, and data layers are unchanged.\n- **Auto-configuration is always conditional** \u2014 every auto-config class uses `@ConditionalOnClass`, `@ConditionalOnMissingBean`; override any default by providing your own bean.\n- **`@SpringBootApplication` = `@Configuration` + `@EnableAutoConfiguration` + `@ComponentScan`** \u2014 knowing which composed annotation provides which behavior is a common interview checkpoint.\n- **Raw Spring knowledge is still required for debugging** \u2014 when auto-configuration activates the wrong bean, understanding `BeanPostProcessor`, `BeanFactoryPostProcessor`, and condition evaluation is the only path to the root cause.\n- **Spring Boot 3.x requires Java 17+ and Jakarta EE 9+ namespace** \u2014 `javax.*` is replaced by `jakarta.*`; a common migration trap for teams upgrading from Boot 2.x."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "Spring Boot is not a different framework \u2014 it is Spring Framework with decisions already made for you. The same `ApplicationContext`, the same IoC container, the same AOP proxies. What Spring Boot adds is conventions, auto-configuration, embedded servers, and starter POMs that wire everything together automatically.\n\n**What Spring Boot adds**\n- Auto-configuration: Spring Boot reads `@ConditionalOnClass`, `@ConditionalOnMissingBean` conditions at startup and registers beans only when the right dependencies are on the classpath and you have not already defined your own\n- Embedded server: Tomcat, Jetty, or Netty ships inside the JAR \u2014 no deployment to an external container required\n- Starter POMs: `spring-boot-starter-web` pulls in exactly the right set of dependencies at compatible versions\n\n**What stays the same**\n- The IoC container is identical \u2014 `ApplicationContext`, `BeanPostProcessor`, `@Autowired`, `@Transactional` all work exactly as in raw Spring\n- Debugging an auto-configuration issue requires understanding raw Spring: which `BeanPostProcessor` runs, what condition failed, why two beans of the same type conflict\n\n**When to use raw Spring**\n- Embedding Spring into a non-boot environment (legacy app servers, custom tooling)\n- Writing a Spring Boot starter or auto-configuration itself\n\n`@SpringBootApplication` is three annotations composed: `@Configuration` (this class defines beans), `@EnableAutoConfiguration` (activate auto-config), `@ComponentScan` (scan this package). Understanding the composition means you can decompose it when you only need part of it."
          }
        ]
      },
      "followup_questions": [
        "How does Spring Boot auto-configuration work internally?",
        "What is the purpose of starter dependencies in Spring Boot?",
        "How do you override Spring Boot's default auto-configuration?"
      ],
      "seo": {
        "metaTitle": "Spring Framework vs Spring Boot Difference | Java Interview Guide",
        "metaDescription": "Understand the difference between Spring Framework and Spring Boot \u2014 auto-configuration, embedded servers, starters, and when you still need raw Spring knowledge."
      },
      "speakable_v2": {
        "archetype": "B",
        "pillar": "P02",
        "hook": "Spring Boot is not a different framework \u2014 it is Spring Framework with decisions already made for you.",
        "beats": [
          {
            "kind": "what_each_is",
            "layout": "paragraph",
            "text": "The most common way candidates fail this question is by framing Spring Boot as a competitor to Spring Framework. In reality, every Spring Boot application is a Spring Framework application \u2014 Boot is an opinionated configuration layer that sits on top of the same IoC container, the same AOP proxy infrastructure, and the same MVC dispatcher you'd wire manually. What Boot actually replaces is ceremony, not capability. In a raw."
          },
          {
            "kind": "differences",
            "layout": "bullets",
            "items": [
              "The table below isn't a feature list \u2014 it maps each pain point in raw Spring to the specific Boot mechanism that eliminates it",
              "| Concern | Spring Framework (Manual) | Spring Boot (Auto-Configured) | |---|---|---| | Bean configuration | Write @Configuration classes for every integration | @ConditionalOnClass auto-detects and wires beans | | Server deployment | Package as WAR, deploy to external Tomcat or JBoss | Embedded Tomcat starts from main() \u2014 one JAR, no server install | | Dependency versions | Manually align 30+ transitive JARs | Starter POMs with a tested version matrix managed by the Spring team | | DataSource setup | Define HikariDataSource bean with explicit URL and pool config | Add spring",
              "url to application",
              "yml \u2014 HikariCP auto-configured | | Production monitoring | Build custom health checks and metrics endpoints from scratch | Actuator exposes /health, /metrics, /info out of the box | | Property management | PropertyPlaceholderConfigurer with manual file loading | application",
              "yml loaded automatically with profile support built in | Notice the pattern: the right column is what Boot does for you, the middle column is what you debug when it does the wrong thing"
            ]
          },
          {
            "kind": "when_to_pick",
            "layout": "paragraph",
            "text": "Every Spring Boot auto-configuration class is guarded by @Conditional annotations \u2014 @ConditionalOnClass checks if a class exists on the classpath, @ConditionalOnMissingBean backs off when you define your own bean, and @ConditionalOnProperty checks for specific property values. This conditional mechanism is what makes Boot feel like magic, and it's exactly what you need to understand when that magic breaks. The most common production surprise is a bean you expected to be auto-configured that isn't \u2014 or one you didn't expect that is. When this happens, run your application with the --debug."
          },
          {
            "kind": "tiny_example",
            "layout": "paragraph",
            "text": "The most common way candidates fail this question is by framing Spring Boot as a competitor to Spring Framework. In reality, every Spring Boot application is a Spring Framework application \u2014 Boot is an opinionated configuration layer that sits on top of the same IoC container, the same AOP proxy infrastructure, and the same MVC dispatcher you'd wire manually. What."
          }
        ],
        "cap": "Understanding the composition means you can decompose it when you only need part of it.",
        "familiarity_anchors": [
          "Spring",
          "Framework",
          "Security"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": [
          "How does Spring Boot auto-configuration work internally?",
          "What is the purpose of starter dependencies in Spring Boot?",
          "How do you override Spring Boot's default auto-configuration?"
        ]
      }
    }
  ]
}
{
  "topic": "Generics Wildcards",
  "topicSlug": "generics-wildcards",
  "questions": [
    {
      "id": "java-generics-type-erasure",
      "slug": "java-generics-type-erasure-explained",
      "question": "What are Java generics and what is type erasure?",
      "title": "Java Generics and Type Erasure",
      "direct_answer": "Generics let you parameterize classes and methods with type variables (e.g., List<T>) so the compiler enforces type safety at compile time. Type erasure means the compiler removes those type parameters at bytecode level, replacing them with Object (or their bounds), so generic information does not exist at runtime.",
      "layout_type": "concept-explainer",
      "interviewer_intent": {
        "testing": "Whether you understand why generics exist, what guarantees they give, and what the erasure tradeoff costs you at runtime",
        "common_mistake": "Confusing compile-time safety with runtime information \u2014 claiming you can do instanceof checks on generic types or create generic arrays",
        "to_stand_out": "Explain why List<String> and List<Integer> share the same Class object at runtime, describe how to work around erasure with Class<T> tokens and super type tokens, and mention bounded wildcards for API design"
      },
      "company_tags": [
        "google",
        "amazon",
        "oracle",
        "linkedin",
        "palantir"
      ],
      "reading_time_minutes": 6,
      "last_updated": "2025-01-15",
      "difficulty": "medium",
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Why List<String> and List<Integer> Are the Same Class at Runtime",
            "content": "Most developers think of generics as \"compile-time type checking for collections.\" That's correct but incomplete \u2014 the interesting part is what happens *after* compilation, and why that limitation shapes how you design APIs.\n\nBefore Java 5, collections stored `Object`. You put a `String` in, you got an `Object` out, and you cast it. If someone put an `Integer` into your `List`, you wouldn't know until a `ClassCastException` at runtime \u2014 potentially far from the insertion point. **Generics** moved this error to compile time: `List<String>` refuses to accept an `Integer` at the point of insertion, not at the point of retrieval.\n\nThe tradeoff Java made to achieve this without breaking existing bytecode is **type erasure**. The compiler verifies all generic types, inserts the necessary casts, and then *erases* the type parameters from the bytecode. At runtime, `List<String>` and `List<Integer>` are both just `List`. They share the same `Class` object \u2014 `new ArrayList<String>().getClass() == new ArrayList<Integer>().getClass()` is `true`.\n\nThis erasure has concrete consequences that trip up experienced developers: you can't do `instanceof List<String>`, you can't create `new T[]`, and you can't call `T.class`. Understanding *why* these limitations exist \u2014 not just memorizing them \u2014 is what the interviewer is evaluating."
          },
          {
            "type": "phase",
            "title": "What the Compiler Does: Type Checking, Cast Insertion, and Erasure",
            "content": "The compiler performs three operations with generics, and all three happen before a single line of bytecode is generated.\n\nFirst, **type checking**. When you write `List<String> names = new ArrayList<>()`, the compiler enforces that only `String` values can be added. `names.add(42)` is a compile error \u2014 no runtime check needed.\n\nSecond, **cast insertion**. When you write `String name = names.get(0)`, the compiler inserts a `(String)` cast into the bytecode. You don't see it in source code, but the bytecode literally contains `checkcast java/lang/String`. Before generics, you wrote this cast yourself \u2014 generics just made the compiler do it for you.\n\nThird, **erasure**. The compiler replaces all type parameters with their bounds. `List<String>` becomes `List` (bound is `Object`). `Comparable<T>` becomes `Comparable`. A bounded type like `<T extends Number>` erases to `Number`, not `Object` \u2014 this is why bounded generics preserve more type information in the bytecode than unbounded ones.\n\nAfter these three steps, the resulting bytecode is identical to what you'd write without generics \u2014 just with the casts guaranteed to be safe by the compiler's type analysis."
          },
          {
            "type": "phase",
            "title": "The Five Things Erasure Prevents \u2014 and the Workarounds That Exist",
            "content": "Type erasure creates specific limitations that every Java developer encounters eventually. Knowing the workaround for each one is what separates a textbook answer from a practical one.\n\n**No `instanceof` on generic types.** You can't write `if (list instanceof List<String>)` because `List<String>` doesn't exist at runtime \u2014 it's just `List`. The workaround is checking the element type: store a `Class<T>` token and use `clazz.isInstance(element)`.\n\n**No `new T()` or `new T[]`.** The JVM doesn't know what `T` is at runtime, so it can't allocate the right type. The workaround is passing a `Class<T>` parameter \u2014 the factory pattern: `T instance = clazz.getDeclaredConstructor().newInstance()`.\n\n**No generic exceptions.** You can't write `catch (MyException<T>)` because the JVM's exception handling works on erased types. This is rarely a problem in practice but occasionally surprises framework developers.\n\n**Bridge methods in inheritance.** When a generic class is subclassed with a concrete type, the compiler generates **bridge methods** to maintain polymorphism. A class implementing `Comparable<MyClass>` gets both `compareTo(MyClass)` and a synthetic `compareTo(Object)` that casts and delegates. These show up in reflection with `isBridge() == true`.\n\n**No overloading by type parameter alone.** `void process(List<String> s)` and `void process(List<Integer> i)` have the same erasure \u2014 `process(List)` \u2014 so they can't coexist in the same class."
          },
          {
            "type": "step",
            "title": "Bounded Wildcards: The API Design Tool Most Developers Underuse",
            "content": "Wildcards (`? extends T` and `? super T`) are the generic feature that matters most for API design, and the one most developers learn last. The rule is captured by **PECS: Producer Extends, Consumer Super**.\n\nWhen a method *reads* from a collection (the collection produces values), use `? extends T`. This lets the caller pass `List<Integer>`, `List<Double>`, or any subtype of `Number`.\n\n```java\npublic double sum(List<? extends Number> numbers) {\n    double total = 0;\n    for (Number n : numbers) total += n.doubleValue();\n    return total;\n}\n```\n\nWhen a method *writes* to a collection (the collection consumes values), use `? super T`. This lets the caller pass `List<Number>`, `List<Object>`, or any supertype.\n\n```java\npublic void addIntegers(List<? super Integer> target) {\n    target.add(1);\n    target.add(2);\n}\n```\n\nWithout wildcards, `sum(List<Integer>)` would not accept a `List<Integer>` if the parameter was typed as `List<Number>` \u2014 because `List<Integer>` is *not* a subtype of `List<Number>` (this is called **invariance**, and it's a direct consequence of erasure and type safety). Wildcards restore the flexibility that erasure takes away."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Generics = compile-time type safety** \u2014 `List<String>` won't accept `Integer`; the compiler inserts casts automatically.\n- **Type erasure: all generic types become raw `List` in bytecode** \u2014 no `instanceof List<String>` or `new T[]` at runtime.\n- **Bounded types erase to their bound** \u2014 `<T extends Number>` becomes `Number` in bytecode, not `Object`.\n- **Bridge methods preserve polymorphism** \u2014 compiler generates synthetic methods when a generic class is subclassed with a concrete type.\n- **PECS: Producer Extends, Consumer Super** \u2014 `List<? extends Number>` for reading; `List<? super Integer>` for writing."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "Generics give you compile-time type safety \u2014 `List<String>` won't accept an `Integer` and the compiler inserts casts for you. The tradeoff is **type erasure**: after compilation, `List<String>` and `List<Integer>` are both just `List`.\n\n**Consequences of erasure:**\n- No `instanceof List<String>` \u2014 the type parameter doesn't exist at runtime\n- No `new T[]` or `new T()` \u2014 the JVM doesn't know what `T` is at runtime\n- Workaround: pass a `Class<T>` token and use `Array.newInstance(clazz, length)` or reflection\n\n**What survives erasure:**\n- Bounded types erase to their bound: `<T extends Number>` becomes `Number` in bytecode, not `Object`\n- Bridge methods are generated when a generic class is subclassed with a concrete type, maintaining polymorphism\n\n**Wildcards (PECS):**\n- `List<? extends Number>` accepts `List<Integer>` or `List<Double>` \u2014 for reading (Producer Extends)\n- `List<? super Integer>` accepts `List<Number>` or `List<Object>` \u2014 for writing (Consumer Super)\n- Generic collections are invariant without wildcards: `List<Integer>` is NOT a `List<Number>`\n\nI recommend following PECS for any generic API that accepts or returns collections."
          }
        ]
      },
      "followup_questions": [
        "What is the difference between ? extends T and ? super T (PECS)?",
        "Why can't you create a generic array in Java?",
        "How does Jackson's TypeReference work around type erasure?"
      ],
      "seo": {
        "metaTitle": "Java Generics and Type Erasure Explained | Java Interview | InterviewExplainer",
        "metaDescription": "Learn how Java generics provide compile-time type safety and how type erasure removes generic information at runtime \u2014 with code examples and interview tips."
      },
      "speakable_v2": {
        "archetype": "A",
        "pillar": "P01",
        "hook": "Generics give you compile-time type safety \u2014 List<String> won't accept an Integer and the compiler inserts casts for you.",
        "beats": [
          {
            "kind": "definition",
            "layout": "paragraph",
            "text": "Most developers think of generics as \"compile-time type checking for collections.\" That's correct but incomplete \u2014 the interesting part is what happens after compilation, and why that limitation shapes how you design APIs. Before Java 5, collections stored Object. You put a String in, you got an Object out, and you cast it. If someone put an Integer into your List, you wouldn't know until a ClassCastException at runtime \u2014 potentially far from the insertion point. Generics moved this error."
          },
          {
            "kind": "why_exists",
            "layout": "paragraph",
            "text": "Whether you understand why generics exist, what guarantees they give, and what the erasure tradeoff costs you at runtime"
          },
          {
            "kind": "parts_or_states",
            "layout": "bullets",
            "items": [
              "Generics = compile-time type safety \u2014 List<String> won't accept Integer; the compiler inserts casts automatically",
              "Type erasure: all generic types become raw List in bytecode \u2014 no instanceof List<String> or new T[] at runtime",
              "Bounded types erase to their bound \u2014 <T extends Number> becomes Number in bytecode, not Object",
              "Bridge methods preserve polymorphism \u2014 compiler generates synthetic methods when a generic class is subclassed with a concrete type",
              "PECS: Producer Extends, Consumer Super \u2014 List<? extends Number> for reading; List<? super Integer> for writing"
            ]
          },
          {
            "kind": "example",
            "layout": "paragraph",
            "text": "Most developers think of generics as \"compile-time type checking for collections.\" That's correct but incomplete \u2014 the interesting part is what happens after compilation, and why that limitation shapes how you design APIs. Before Java 5, collections stored Object. You put a String in, you got an Object out, and you cast it. If someone put an Integer into your List, you wouldn't know until a ClassCastException at runtime \u2014 potentially far from the insertion point. Generics moved this error to compile time: List<String> refuses to accept an Integer at the point of insertion, not at the point of retrieval. The tradeoff Java made to achieve this without breaking existing bytecode is type erasure. The compiler verifies all generic types."
          },
          {
            "kind": "pitfalls",
            "layout": "bullets",
            "items": [
              "Confusing compile-time safety with runtime information \u2014 claiming you can do instanceof checks on generic types or create generic arrays"
            ]
          }
        ],
        "cap": "Consequences of erasure: No instanceof List<String> \u2014 the type parameter doesn't exist at runtime No new T[] or new T() \u2014 the JVM doesn't know what T is at runtime Workaround: pass a Class<T> token.",
        "familiarity_anchors": [
          "Generics",
          "Object"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": [
          "What is the difference between ? extends T and ? super T (PECS)?",
          "Why can't you create a generic array in Java?",
          "How does Jackson's TypeReference work around type erasure?"
        ]
      }
    }
  ]
}
{
  "topic": "Database Migrations",
  "topicSlug": "database-migrations",
  "questions": [
    {
      "id": "jpa-flyway-liquibase",
      "slug": "flyway-liquibase-database-migrations",
      "question": "How do you manage database schema migrations with Flyway or Liquibase in a Spring Boot application?",
      "title": "Database Migrations with Flyway and Liquibase in Spring Boot",
      "direct_answer": "Flyway and Liquibase manage database schema changes as versioned migration scripts that run automatically at application startup. Flyway uses SQL files (V1__create_users.sql) with a naming convention. Liquibase uses XML/YAML/JSON changelogs. Both track applied migrations in a metadata table to ensure each migration runs exactly once.",
      "layout_type": "default",
      "difficulty": "intermediate",
      "reading_time_minutes": 5,
      "last_updated": "2026-04-12",
      "interviewer_intent": {
        "testing": "Whether the candidate understands schema migration versioning and its role in CI/CD pipelines.",
        "common_mistake": "Modifying an already-applied migration script instead of creating a new migration \u2014 Flyway detects the checksum mismatch and refuses to start.",
        "to_stand_out": "Discuss repeatable migrations in Flyway, rollback limitations, and how to handle migration failures in production (out-of-order migrations, baseline)."
      },
      "company_tags": [],
      "answer": {
        "sections": [
          {
            "type": "overview",
            "title": "Editing an Applied Migration Breaks Your Application at Startup \u2014 Always Create New Migrations",
            "content": "The most common Flyway mistake is editing a migration script that's already been applied. Flyway stores a **checksum of every applied migration** in its metadata table (`flyway_schema_history`). If the checksum of a file on disk doesn't match the stored checksum, Flyway refuses to start the application. This is a safety mechanism \u2014 applied migrations represent the actual state of the database.\n\nBoth Flyway and Liquibase solve the same problem: **making database schema changes repeatable, versioned, and automated.** Instead of manually running ALTER TABLE scripts on each environment, migration files are part of the codebase, versioned in Git, and run automatically at startup.\n\nFlyway is simpler \u2014 SQL files with a naming convention (`V1__create_users.sql`, `V2__add_email_column.sql`). Liquibase is more powerful \u2014 XML/YAML changelogs with database-agnostic change types and automatic rollback generation. **Spring Boot auto-configures both** \u2014 add the dependency, put migration files in the right directory, and they run at startup."
          },
          {
            "type": "step",
            "title": "Flyway Naming Convention, Repeatable Migrations, and Production Recovery",
            "content": "Flyway migration files follow a strict naming convention: **`V{version}__{description}.sql`** \u2014 two underscores between version and description. Versions can be numeric (`V1`, `V2`) or semantic (`V1.1`, `V2.0.1`). Files are applied in version order.\n\n**Repeatable migrations** (`R__create_views.sql`) run every time their checksum changes. Use them for views, stored procedures, or seed data that needs to be recreated when modified \u2014 they always run after versioned migrations.\n\nWhen a migration fails in production, the database is in a **partially migrated state.** Flyway marks the migration as failed. Fix the issue manually, then use `flyway repair` to update the metadata table. In CI/CD pipelines, run `flyway validate` before deployment to catch checksum mismatches early.\n\n**Liquibase's advantage** is automatic rollback generation for simple changes (add column \u2192 drop column) and database-agnostic changeset definitions. Flyway has no built-in rollback \u2014 you write undo migrations manually. For most Spring Boot projects, Flyway's simplicity wins unless you need multi-database support or automatic rollbacks."
          },
          {
            "type": "key_points",
            "title": "Key Points",
            "content": "- **Never modify a migration script that has already been applied** \u2014 Flyway checksums every script; a change to `V1__create_orders.sql` causes `FlywayException: Checksum mismatch`; create a new version instead.\n- **Liquibase uses XML/YAML changesets with an `id` and `author`** \u2014 each changeset is uniquely identified; the `DATABASECHANGELOG` table tracks what has been applied.\n- **Flyway runs on application startup by default in Spring Boot** \u2014 `spring.flyway.enabled=true`; migrations run before the `ApplicationContext` is ready; a failed migration prevents the application from starting.\n- **Repeatable migrations in Flyway use `R__` prefix** \u2014 re-run on every startup if the content changes (checksum-driven); use for views, stored procedures, and seed data.\n- **`spring.flyway.out-of-order=false` (default) rejects out-of-order migrations** \u2014 if `V3__` is added after `V4__` has already run, Flyway fails startup; set `out-of-order=true` for feature branch workflows."
          },
          {
            "type": "speakable_answer",
            "title": "How to Answer This Verbally",
            "content": "Database migration tools manage schema changes as versioned scripts applied in order. They ensure every environment \u2014 developer machine, CI, production \u2014 runs the same schema evolution.\n\n**Flyway \u2014 version-based**\n- Scripts named `V1__create_orders.sql`, `V2__add_index.sql` \u2014 the number is the version\n- Flyway tracks applied migrations in `flyway_schema_history`; at startup it applies all pending scripts in version order\n- Once applied, a script is immutable \u2014 Flyway stores a checksum; any modification causes a startup failure\n- Create a new version to undo or modify what a previous migration did\n\n**Liquibase \u2014 changeset-based**\n- Changes are defined in changelog files (XML, YAML, SQL, JSON)\n- Each changeset has a unique `id` + `author` combination; tracked in `DATABASECHANGELOG`\n- Supports rollback with `<rollback>` tags \u2014 Flyway does not have built-in rollback\n- Better for teams that need rollback capability or generate changelogs from entity diff\n\n**Spring Boot integration**\n- Both integrate automatically: migrations run at startup, before the JPA schema validation\n- A failed migration prevents the application from starting \u2014 safe for catching problems early\n- In CI, run migrations against the real test database; in local dev, Flyway + Testcontainers gives each developer a clean schema"
          }
        ]
      },
      "followup_questions": [],
      "seo": {
        "metaTitle": "Database Migrations with Flyway and Liquibase in Spring Boot",
        "metaDescription": ""
      },
      "speakable_v2": {
        "archetype": "C",
        "pillar": "P02",
        "hook": "Database migration tools manage schema changes as versioned scripts applied in order.",
        "beats": [
          {
            "kind": "mental_model",
            "layout": "paragraph",
            "text": "The most common Flyway mistake is editing a migration script that's already been applied. Flyway stores a checksum of every applied migration in its metadata table (flyway_schema_history). If the checksum of a file on disk doesn't match the stored checksum, Flyway refuses to start the application. This is a safety mechanism \u2014 applied migrations represent the actual state of the database. Both Flyway and Liquibase solve the same problem: making database schema changes repeatable, versioned, and automated. Instead of manually running ALTER TABLE scripts on each environment, migration files are."
          },
          {
            "kind": "mechanism",
            "layout": "ordered_list",
            "steps": [
              "Flyway migration files follow a strict naming convention: V{version}__{description}.sql \u2014 two underscores between version and description.",
              "Versions can be numeric (V1, V2) or semantic (V1.1, V2.0.1).",
              "Files are applied in version order.",
              "Repeatable migrations (R__create_views.sql) run every time their checksum changes.",
              "Use them for views, stored procedures, or seed data that needs to be recreated when modified \u2014 they always run after versioned migrations.",
              "When a migration fails in production, the database is in a partially migrated state."
            ]
          },
          {
            "kind": "edge_cases",
            "layout": "bullets",
            "items": [
              "Never modify a migration script that has already been applied \u2014 Flyway checksums every script; a change to V1__create_orders",
              "sql causes FlywayException: Checksum mismatch; create a new version instead",
              "Liquibase uses XML/YAML changesets with an id and author \u2014 each changeset is uniquely identified; the DATABASECHANGELOG table tracks what has been applied",
              "Flyway runs on application startup by default in Spring Boot \u2014 spring",
              "enabled=true; migrations run before the ApplicationContext is ready; a failed migration prevents the application from starting"
            ]
          },
          {
            "kind": "failure_mode",
            "layout": "callout",
            "text": "Modifying an already-applied migration script instead of creating a new migration \u2014 Flyway detects the checksum mismatch and refuses to start."
          }
        ],
        "cap": "Flyway \u2014 version-based Scripts named V1__create_orders.sql, V2__add_index.sql \u2014 the number is the version Flyway tracks applied migrations in flyway_schema_history; at startup it applies all pending s",
        "familiarity_anchors": [
          "Flyway",
          "Liquibase"
        ],
        "tts_overrides": {},
        "speakable_status": "pending_review",
        "followup_handoff": []
      }
    }
  ]
}
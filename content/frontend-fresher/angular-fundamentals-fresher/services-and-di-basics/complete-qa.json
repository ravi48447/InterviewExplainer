{
  "topic": "Services And Di Basics",
  "topicSlug": "services-and-di-basics",
  "questions": [
    {
      "id": "angular-fundamentals-fresher-services-and-di-basics-q001",
      "slug": "angular-fundamentals-fresher-services-and-di-basics-di",
      "question": "How do Angular services and dependency injection work — providers, injectors, and inject()?",
      "title": "Services And DI",
      "direct_answer": "A service is a plain class decorated @Injectable() that holds logic/state; DI delivers instances to components without them constructing anything. The provider registry decides instance identity: providedIn: 'root' = one app-wide singleton (the default and best choice); providers: [Service] on a component = a fresh instance for that subtree. The injector tree mirrors the component tree — child injectors look up by token, nearest provider wins, so a component-level provider SHADOWS the root one. Two injection forms: constructor injection (constructor(private http: HttpClient)) and the modern inject() function (private http = inject(HttpClient)) which works in field initializers and must run inside an injection context (construction time). providedIn: 'root' services are also tree-shakable — unused ones drop from the bundle.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 6,
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "Quick Revision",
            "content": "A service is a plain class decorated @Injectable() that holds logic/state; DI delivers instances to components without them constructing anything. The provider registry decides instance identity: providedIn: 'root' = one app-wide singleton (the default and best choice); providers: [Service] on a component = a fresh instance for that subtree. The injector tree mirrors the component tree — child injectors look up by token, nearest provider wins, so a component-level provider SHADOWS the root one. Two injection forms: constructor injection (constructor(private http: HttpClient)) and the modern inject() function (private http = inject(HttpClient)) which works in field initializers and must run inside an injection context (construction time). providedIn: 'root' services are also tree-shakable — unused ones drop from the bundle."
          },
          {
            "type": "speakable_answer",
            "title": "Interview Answer",
            "content": "The full DI picture, verified:\n\n```ts\n@Injectable({ providedIn: 'root' })   // ONE app-wide instance, tree-shakable\nexport class CartService {\n  private items: Item[] = [];\n  add(item: Item) { this.items.push(item); }\n  get count() { return this.items.length; }\n}\n\n@Component({ /* ... */ })\nexport class Checkout {\n  // modern form: field initializer, shorter with TS\n  private cart = inject(CartService);\n  // classic form: constructor parameter\n  constructor(private notifier: NotifierService) {}\n}\n\n// component-scoped: every Checkout gets its OWN CartService\n@Component({\n  providers: [CartService],   // shadows root for this subtree only\n})\nexport class Checkout {}\n```\n\nThe injector tree (the interview core): there is an injector per component level, rooted at the platform injector. When Checkout asks for CartService, Angular walks UP from the component's injector — if a provider exists at the component level, that instance wins (shadowing root); otherwise it reaches the root injector's provider. That's the whole scope model: 'root' = everyone shares one; component providers = that subtree shares one (and its children see the NEAREST one up their chain).\n\nWhy services at all (the design answer): components are UI-shaped and short-lived; services hold reusable logic and shared state so components stay thin. A cart in a component's field dies with the component — in a root service it survives navigation. The testability bonus: DI means every service arrives through the constructor, so a test provides a fake (TestBed.provide(HttpClient, useValue: fakeHttp)) instead of hitting a network — DI is what makes Angular components testable at all.\n\nWhen to scope a service to a component (the judgment layer): per-feature state — a dialog stack owned by one route, an editor session that must reset on close. Everything shared/stateless (API clients, stores, auth) goes to root. A smell worth naming: providing at a component purely to avoid import cycles is a misuse — fix the cycle, not the scope."
          },
          {
            "type": "flow_diagram",
            "title": "Mental Model",
            "content": "```mermaid\nflowchart TD\n    S['@Injectable() service class'] --> PRV[provider registration]\n    PRV --> R['providedIn: root — one app singleton']\n    PRV --> C['component providers — one per subtree']\n    R --> TS[tree-shakable: unused = dropped from bundle]\n    INJ[injector tree mirrors component tree] --> L[lookup by token, nearest provider wins]\n    L --> SHADOW[component provider shadows root for its subtree]\n    INJCT['inject(Token) — modern, field initializer'] -.needs.-> CTX[injection context: construction time only]\n    TR['constructor(private s: Service) — classic'] --> SAME[same injector chain]\n    TST[tests provide fakes] --> TEST[why DI = testability]\n```"
          },
          {
            "type": "deep_explanation",
            "title": "Deep Dive",
            "content": "The injection-context rule (the inject() gotcha interviewers now ask): inject() must run while an injection context is active — that means during class construction (field initializers and constructor body). Calling it inside a setTimeout, an event callback, or a method invoked later throws the NG0203 runtime error ('inject() must be called from an injection context'). The escapes: capture the service in a field first (private cart = inject(CartService); then use this.cart everywhere), or use Injector.runWithinInjectionContext for late needs. The reverse trap with constructor injection: parameter properties are sugar, but DI resolves by TYPE TOKEN at design time — the decorator metadata carries the tokens, which is why emitDecoratorMetadata and TS strict settings interplay here.\n\nThe token model (the layer beneath classes): DI keys are InjectionTokens, not classes per se — a class provider uses the class AS the token ({ provide: CartService, useClass: CartService }). The other provider recipes cover the remaining cases: useValue (a config object or mock: { provide: API_URL, useValue: '...' } — needs an InjectionToken<string> since a primitive can't key), useFactory (compose: create the service with dependencies chosen at runtime, deps: [Auth]), useExisting (alias one token to another — the redirect pattern). Tree-shaking only works for providedIn root because the decorator is statically analyzable; providers arrays in components are always reachable by construction and never shake.\n\nThe modern state direction (name it and the answer reads current): where teams used BehaviorSubject services for shared state, signals in services (signal(), computed()) are the 2024+ pattern — a CartService exposing readonly count = computed(() => this.items().length) gives components fine-grained reactivity without RxJS subscription management, and provideState/injection functions compose it with DI. RxJS still owns async flows (HTTP, timers, events); signals own synchronous derived state. 'Services hold state, DI wires them, signals make them reactive' is the accurate one-line architecture."
          }
        ]
      },
      "followup_questions": [
        "When would you use Services And Di Basics in a real project?",
        "How would you compare Services And Di Basics with an alternative?",
        "What is a common mistake when using Services And Di Basics?"
      ],
      "order": 1,
      "seo": {
        "metaTitle": "Services And DI — frontend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How do Angular services and dependency injection work — providers, injectors, and inject()? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "interviewer_intent": {
        "testing": "Whether you understand Angular services and DI beyond memorized syntax.",
        "common_mistake": "Giving a definition without explaining a practical trade-off.",
        "to_stand_out": "Connect the concept to a small production example and state its failure mode."
      },
      "priority": "must-prepare",
      "role": "anchor",
      "interviewStage": "framework",
      "answerProfile": "mechanism"
    },
    {
      "id": "angular-fundamentals-fresher-services-and-di-basics-q002",
      "slug": "angular-fundamentals-fresher-services-and-di-basics-when-to-use",
      "question": "When would you use Services And Di Basics in a real project?",
      "title": "When would you use Services And Di Basics in a real project",
      "direct_answer": "I would use How do Angular services and dependency injection work — providers, injectors, and inject()? when the problem matches its main benefit. Services are injected classes; provider scope (root vs component) decides singleton vs per-subtree identity via the injector tree, and inject() is the modern retrieval form. In a real project, I would confirm that benefit on a small example before applying it broadly, and I would keep the simpler option whenever it is just as clear.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining How do Angular services and dependency injection work — providers, injectors, and inject()? in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. Services are injected classes; provider scope (root vs component) decides singleton vs per-subtree identity via the injector tree, and inject() is the modern retrieval form."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use How do Angular services and dependency injection work — providers, injectors, and inject()? when its rule makes the code safer, clearer, or more efficient for the actual problem. Services are injected classes; provider scope (root vs component) decides singleton vs per-subtree identity via the injector tree, and inject() is the modern retrieval form. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Services are injected classes; provider scope (root vs component) decides singleton vs per-subtree identity via the injector tree, and inject() is the modern retrieval form.\n\nProviding a service in BOTH root and a component and being surprised the subtree gets the shadow instance; and calling inject() outside construction (a callback/setTimeout) where no injection context exists."
          }
        ]
      },
      "followup_questions": [
        "When would you use Services And Di Basics in a real project?",
        "How would you compare Services And Di Basics with an alternative?",
        "What is a common mistake when using Services And Di Basics?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Services And Di Basics in a real project — frontend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Services And Di Basics in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "interviewer_intent": {
        "testing": "Whether you can explain How do Angular services and dependency injection work — providers, injectors, and inject()? in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do Angular services and dependency injection work — providers, injectors, and inject()? without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      }
    },
    {
      "id": "angular-fundamentals-fresher-services-and-di-basics-q003",
      "slug": "angular-fundamentals-fresher-services-and-di-basics-common-mistake",
      "question": "What is a common mistake when using Services And Di Basics?",
      "title": "a common mistake when using Services And Di Basics",
      "direct_answer": "A common mistake with How do Angular services and dependency injection work — providers, injectors, and inject()? is using the rule without checking its boundary conditions. Services are injected classes; provider scope (root vs component) decides singleton vs per-subtree identity via the injector tree, and inject() is the modern retrieval form. That can make a valid-looking answer wrong under an edge case, so I state the assumption, handle the edge case, and add a small regression test.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only \"be careful\"; connect the mistake to the behaviour of How do Angular services and dependency injection work — providers, injectors, and inject()?. Services are injected classes; provider scope (root vs component) decides singleton vs per-subtree identity via the injector tree, and inject() is the modern retrieval form."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with How do Angular services and dependency injection work — providers, injectors, and inject()? is assuming the happy path is the whole rule. Services are injected classes; provider scope (root vs component) decides singleton vs per-subtree identity via the injector tree, and inject() is the modern retrieval form. For example, the normal case may work while the zero, empty, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Services are injected classes; provider scope (root vs component) decides singleton vs per-subtree identity via the injector tree, and inject() is the modern retrieval form.\n\nProviding a service in BOTH root and a component and being surprised the subtree gets the shadow instance; and calling inject() outside construction (a callback/setTimeout) where no injection context exists."
          }
        ]
      },
      "followup_questions": [
        "When would you use Services And Di Basics in a real project?",
        "How would you compare Services And Di Basics with an alternative?",
        "What is a common mistake when using Services And Di Basics?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Services And Di Basics — frontend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Services And Di Basics? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "interviewer_intent": {
        "testing": "Whether you can explain How do Angular services and dependency injection work — providers, injectors, and inject()? in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do Angular services and dependency injection work — providers, injectors, and inject()? without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      }
    },
    {
      "id": "angular-fundamentals-fresher-services-and-di-basics-q004",
      "slug": "angular-fundamentals-fresher-services-and-di-basics-compare",
      "question": "How would you compare Services And Di Basics with an alternative?",
      "title": "compare Services And Di Basics with an alternative",
      "direct_answer": "I compare How do Angular services and dependency injection work — providers, injectors, and inject()? with the simpler alternative by looking at correctness first, then time, memory, and readability. Services are injected classes; provider scope (root vs component) decides singleton vs per-subtree identity via the injector tree, and inject() is the modern retrieval form. I normally choose the simplest option that meets the requirement, and I can say why the alternative is not needed for this case.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How do Angular services and dependency injection work — providers, injectors, and inject()? in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do Angular services and dependency injection work — providers, injectors, and inject()? without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. Services are injected classes; provider scope (root vs component) decides singleton vs per-subtree identity via the injector tree, and inject() is the modern retrieval form."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare How do Angular services and dependency injection work — providers, injectors, and inject()? with the straightforward alternative on the same example. Services are injected classes; provider scope (root vs component) decides singleton vs per-subtree identity via the injector tree, and inject() is the modern retrieval form. If it removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Services are injected classes; provider scope (root vs component) decides singleton vs per-subtree identity via the injector tree, and inject() is the modern retrieval form.\n\nProviding a service in BOTH root and a component and being surprised the subtree gets the shadow instance; and calling inject() outside construction (a callback/setTimeout) where no injection context exists."
          }
        ]
      },
      "followup_questions": [
        "When would you use Services And Di Basics in a real project?",
        "How would you compare Services And Di Basics with an alternative?",
        "What is a common mistake when using Services And Di Basics?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Services And Di Basics with an alternative — frontend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Services And Di Basics with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "angular-fundamentals-fresher-services-and-di-basics-q005",
      "slug": "angular-fundamentals-fresher-services-and-di-basics-scenario",
      "question": "How would you debug a problem involving Services And Di Basics?",
      "title": "debug a problem involving Services And Di Basics",
      "direct_answer": "I would debug a problem involving How do Angular services and dependency injection work — providers, injectors, and inject()? by reproducing it with the smallest input first. Services are injected classes; provider scope (root vs component) decides singleton vs per-subtree identity via the injector tree, and inject() is the modern retrieval form. Then I would inspect the intermediate values, compare them with the expected rule, fix the first incorrect step, and add a regression test.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How do Angular services and dependency injection work — providers, injectors, and inject()? in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do Angular services and dependency injection work — providers, injectors, and inject()? without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what How do Angular services and dependency injection work — providers, injectors, and inject()? is supposed to guarantee. Services are injected classes; provider scope (root vs component) decides singleton vs per-subtree identity via the injector tree, and inject() is the modern retrieval form."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: Services are injected classes; provider scope (root vs component) decides singleton vs per-subtree identity via the injector tree, and inject() is the modern retrieval form. Once I find the first incorrect value, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "Services are injected classes; provider scope (root vs component) decides singleton vs per-subtree identity via the injector tree, and inject() is the modern retrieval form.\n\nProviding a service in BOTH root and a component and being surprised the subtree gets the shadow instance; and calling inject() outside construction (a callback/setTimeout) where no injection context exists."
          }
        ]
      },
      "followup_questions": [
        "When would you use Services And Di Basics in a real project?",
        "How would you compare Services And Di Basics with an alternative?",
        "What is a common mistake when using Services And Di Basics?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Services And Di Basics — frontend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Services And Di Basics? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

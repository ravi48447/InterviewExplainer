{
  "topic": "Angular Scenario Based",
  "topicSlug": "scenario-based",
  "questions": [
    {
      "id": "angular-fundamentals-fresher-scenario-based-q001",
      "slug": "angular-fundamentals-fresher-scenario-based-scenario",
      "question": "Angular view shows stale data, a @for list misbehaves, or a component leaks subscriptions — what's the fix?",
      "title": "Angular Scenarios",
      "direct_answer": "The three classic Angular failure scenarios: (1) STALE DATA after mutation — with OnPush, mutating an object/array field keeps the same reference, so change detection skips the update; fix by replacing the reference (this.items = [...this.items, item]) or calling markForCheck() when mutation is unavoidable. (2) @for LIST MISBEHAVES on reorder — track keyed by index or a changing id causes node churn; track by a STABLE unique id so DOM nodes follow their items. (3) SUBSCRIPTION LEAKS — an interval or HTTP subscription in ngOnInit keeps running after the component is destroyed; fix with takeUntilDestroyed() (the modern one-liner), or the TakeUntil destroy$ pattern, or manual unsubscribe in ngOnDestroy; async pipe in templates unsubscribes automatically.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 6,
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "Quick Revision",
            "content": "The three classic Angular failure scenarios: (1) STALE DATA after mutation — with OnPush, mutating an object/array field keeps the same reference, so change detection skips the update; fix by replacing the reference (this.items = [...this.items, item]) or calling markForCheck() when mutation is unavoidable. (2) @for LIST MISBEHAVES on reorder — track keyed by index or a changing id causes node churn; track by a STABLE unique id so DOM nodes follow their items. (3) SUBSCRIPTION LEAKS — an interval or HTTP subscription in ngOnInit keeps running after the component is destroyed; fix with takeUntilDestroyed() (the modern one-liner), or the TakeUntil destroy$ pattern, or manual unsubscribe in ngOnDestroy; async pipe in templates unsubscribes automatically."
          },
          {
            "type": "speakable_answer",
            "title": "Interview Answer",
            "content": "Each scenario with its verified fix:\n\n```ts\n// 1. STALE DATA — OnPush + mutation = skipped render\n@Component({ changeDetection: ChangeDetectionStrategy.OnPush })\nexport class Cart {\n  items: Item[] = [];\n\n  addBad(item: Item)  { this.items.push(item); }      // WRONG: same reference → CD skips\n  addGood(item: Item) { this.items = [...this.items, item]; } // RIGHT: new reference\n}\n\n// 2. @for REORDER — track by stable id, never index\n@for (item of items; track item.id) { ... }   // node follows item across reorder\n@for (item of items; track $index) { ... }     // reorder = node churn (focus lost)\n\n// 3. SUBSCRIPTION LEAK — the modern one-liner\nexport class Feed implements OnInit {\n  private destroy = injectDestroyRef; // DestroyRef\n  ngOnInit() {\n    this.http.get('/feed').pipe(takeUntilDestroyed(this.destroyRef)).subscribe(...);\n    // or simpler — inside injection context, no arg needed:\n    interval(1000).pipe(takeUntilDestroyed()).subscribe(tick => this.now = tick);\n  }\n}\n```\n\nThe reasoning per scenario (the interview structure):\n1. OnPush means the component renders only when an @Input reference changes, a template event fires inside it, or markForCheck() runs. Mutation keeps the reference — Object.is still equal — so the binding is never re-checked. Immutable updates (new array/object) flip the reference and the render happens. markForCheck() is the honest escape when the mutation is genuinely the right model (third-party canvas libs, state machines) — but sprinkle it last, not first.\n2. track is @for's identity contract. A stable unique id means: item moves up → its DOM node MOVES (focus, animations, scroll position preserved). Index keying means: item removed at 0 → every node shifts its data — visually 'works' but focus is lost, animations misfire, and inputs inside rows show the wrong item's value during the update frame.\n3. Subscriptions outlive components unless closed. takeUntilDestroyed() both closes and expresses intent in one operator; the async pipe in templates ({{ feed$ | async }}) manages the whole lifecycle in the binding itself — subscribe-on-check, unsubscribe-on-destroy; often the cleanest fix is pushing data into a signal via toSignal(rx) and letting the framework own it."
          },
          {
            "type": "flow_diagram",
            "title": "Mental Model",
            "content": "```mermaid\nflowchart TD\n    SYM{symptom} -->|stale view under OnPush| F1[mutation kept the reference]\n    F1 --> FIX1[new array/object reference OR markForCheck as last resort]\n    SYM -->|list reorder breaks focus| F2[track keyed by index or unstable id]\n    F2 --> FIX2[track item.id — stable unique identity]\n    SYM -->|memory grows / code runs after route change| F3[unclosed subscription]\n    F3 --> FIX3[takeUntilDestroyed / TakeUntil destroy$ / async pipe / toSignal]\n    MODEL[immutable data flow] -.prevents.-> F1\n    ID[stable domain ids] -.prevents.-> F2\n```"
          },
          {
            "type": "deep_explanation",
            "title": "Deep Dive",
            "content": "The OnPush decision tree (the senior-level complete rules): a component with OnPush renders when (1) any @Input reference changes, (2) a template event binding inside IT fires (click handlers — but a PARENT method reassigning the same component's inputs doesn't count), (3) it or an ancestor calls markForCheck() (marks the path to root dirty), (4) it calls detectChanges() directly (local check — runs now, but doesn't mark ancestors), (5) a signal it reads in a template changes, or (6) an async pipe it hosts emits. The async-pipe point is subtle: the pipe itself calls markForCheck() on emission, which is why OnPush + observable | async 'just works' — the classic pattern was OnPush everywhere + async pipes for data flows, before signals subsumed it.\n\nSubscription lifecycle beyond takeUntilDestroyed (the full toolbox): (a) async pipe — template-owned; (b) takeUntil(this.destroy$) with a Subject blasted in ngOnDestroy — the classic pattern, still fine, one line more; (c) manual .unsubscribe() in ngOnDestroy for imperative subscriptions stored as fields; (d) takeUntilDestroyed() — the operator form of (b) with the DestroyRef injection token; and (e) FIRST-value semantics: take(1)/first() when the flow is a one-shot request (HTTP) — no need to unsubscribe a completed observable, completion cleans up automatically. The real leak sources are INTERVALS and WebSocket/event listeners, which never complete.\n\nThe signals reconciliation (why these scenarios are shrinking): signal-based state makes scenario 1 mostly disappear — a signal() holding an array is read in the template; any .set()/update() notifies exactly the bindings reading it regardless of OnPush, because fine-grained reactivity replaces the reference-equality gate. toSignal(observable) turns any RxJS flow into the same model — the leak in scenario 3 becomes framework-managed teardown. The honest interview line: 'know the OnPush/track/unsubscribe trio because every existing codebase has them; write signal state in new code so the whole class of issues never forms.'"
          }
        ]
      },
      "followup_questions": [
        "When would you use Angular Scenario Based in a real project?",
        "How would you compare Angular Scenario Based with an alternative?",
        "What is a common mistake when using Angular Scenario Based?"
      ],
      "order": 1,
      "seo": {
        "metaTitle": "Angular Scenarios — frontend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for Angular view shows stale data, a @for list misbehaves, or a component leaks subscriptions — what's the fix? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "interviewer_intent": {
        "testing": "Whether you understand Angular debugging scenarios beyond memorized syntax.",
        "common_mistake": "Giving a definition without explaining a practical trade-off.",
        "to_stand_out": "Connect the concept to a small production example and state its failure mode."
      },
      "priority": "must-prepare",
      "role": "anchor",
      "interviewStage": "practical",
      "answerProfile": "debugging"
    },
    {
      "id": "angular-fundamentals-fresher-scenario-based-q002",
      "slug": "angular-fundamentals-fresher-scenario-based-when-to-use",
      "question": "When would you use Angular Scenario Based in a real project?",
      "title": "When would you use Angular Scenario Based in a real project",
      "direct_answer": "I would use Angular view shows stale data, a @for list misbehaves, or a component leaks subscriptions — what's the fix? when the problem matches its main benefit. OnPush staleness → replace references; @for churn → stable track keys; subscription leaks → takeUntilDestroyed/async pipe. In a real project, I would confirm that benefit on a small example before applying it broadly, and I would keep the simpler option whenever it is just as clear.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining Angular view shows stale data, a @for list misbehaves, or a component leaks subscriptions — what's the fix? in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. OnPush staleness → replace references; @for churn → stable track keys; subscription leaks → takeUntilDestroyed/async pipe."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use Angular view shows stale data, a @for list misbehaves, or a component leaks subscriptions — what's the fix? when its rule makes the code safer, clearer, or more efficient for the actual problem. OnPush staleness → replace references; @for churn → stable track keys; subscription leaks → takeUntilDestroyed/async pipe. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "OnPush staleness → replace references; @for churn → stable track keys; subscription leaks → takeUntilDestroyed/async pipe.\n\nMutating arrays under OnPush and 'randomly' calling detectChanges everywhere instead of fixing the data flow; and forgetting track stability when the API re-creates ids."
          }
        ]
      },
      "followup_questions": [
        "When would you use Angular Scenario Based in a real project?",
        "How would you compare Angular Scenario Based with an alternative?",
        "What is a common mistake when using Angular Scenario Based?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Angular Scenario Based in a real project — frontend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Angular Scenario Based in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "interviewer_intent": {
        "testing": "Whether you can explain Angular view shows stale data, a @for list misbehaves, or a component leaks subscriptions — what's the fix? in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about Angular view shows stale data, a @for list misbehaves, or a component leaks subscriptions — what's the fix? without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      }
    },
    {
      "id": "angular-fundamentals-fresher-scenario-based-q003",
      "slug": "angular-fundamentals-fresher-scenario-based-common-mistake",
      "question": "What is a common mistake when using Angular Scenario Based?",
      "title": "a common mistake when using Angular Scenario Based",
      "direct_answer": "A common mistake with Angular view shows stale data, a @for list misbehaves, or a component leaks subscriptions — what's the fix? is using the rule without checking its boundary conditions. OnPush staleness → replace references; @for churn → stable track keys; subscription leaks → takeUntilDestroyed/async pipe. That can make a valid-looking answer wrong under an edge case, so I state the assumption, handle the edge case, and add a small regression test.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only \"be careful\"; connect the mistake to the behaviour of Angular view shows stale data, a @for list misbehaves, or a component leaks subscriptions — what's the fix?. OnPush staleness → replace references; @for churn → stable track keys; subscription leaks → takeUntilDestroyed/async pipe."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with Angular view shows stale data, a @for list misbehaves, or a component leaks subscriptions — what's the fix? is assuming the happy path is the whole rule. OnPush staleness → replace references; @for churn → stable track keys; subscription leaks → takeUntilDestroyed/async pipe. For example, the normal case may work while the zero, empty, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "OnPush staleness → replace references; @for churn → stable track keys; subscription leaks → takeUntilDestroyed/async pipe.\n\nMutating arrays under OnPush and 'randomly' calling detectChanges everywhere instead of fixing the data flow; and forgetting track stability when the API re-creates ids."
          }
        ]
      },
      "followup_questions": [
        "When would you use Angular Scenario Based in a real project?",
        "How would you compare Angular Scenario Based with an alternative?",
        "What is a common mistake when using Angular Scenario Based?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Angular Scenario Based — frontend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Angular Scenario Based? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "interviewer_intent": {
        "testing": "Whether you can explain Angular view shows stale data, a @for list misbehaves, or a component leaks subscriptions — what's the fix? in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about Angular view shows stale data, a @for list misbehaves, or a component leaks subscriptions — what's the fix? without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      }
    },
    {
      "id": "angular-fundamentals-fresher-scenario-based-q004",
      "slug": "angular-fundamentals-fresher-scenario-based-compare",
      "question": "How would you compare Angular Scenario Based with an alternative?",
      "title": "compare Angular Scenario Based with an alternative",
      "direct_answer": "I compare Angular view shows stale data, a @for list misbehaves, or a component leaks subscriptions — what's the fix? with the simpler alternative by looking at correctness first, then time, memory, and readability. OnPush staleness → replace references; @for churn → stable track keys; subscription leaks → takeUntilDestroyed/async pipe. I normally choose the simplest option that meets the requirement, and I can say why the alternative is not needed for this case.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain Angular view shows stale data, a @for list misbehaves, or a component leaks subscriptions — what's the fix? in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about Angular view shows stale data, a @for list misbehaves, or a component leaks subscriptions — what's the fix? without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. OnPush staleness → replace references; @for churn → stable track keys; subscription leaks → takeUntilDestroyed/async pipe."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare Angular view shows stale data, a @for list misbehaves, or a component leaks subscriptions — what's the fix? with the straightforward alternative on the same example. OnPush staleness → replace references; @for churn → stable track keys; subscription leaks → takeUntilDestroyed/async pipe. If it removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "OnPush staleness → replace references; @for churn → stable track keys; subscription leaks → takeUntilDestroyed/async pipe.\n\nMutating arrays under OnPush and 'randomly' calling detectChanges everywhere instead of fixing the data flow; and forgetting track stability when the API re-creates ids."
          }
        ]
      },
      "followup_questions": [
        "When would you use Angular Scenario Based in a real project?",
        "How would you compare Angular Scenario Based with an alternative?",
        "What is a common mistake when using Angular Scenario Based?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Angular Scenario Based with an alternative — frontend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Angular Scenario Based with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "angular-fundamentals-fresher-scenario-based-q005",
      "slug": "angular-fundamentals-fresher-scenario-based-scenario",
      "question": "How would you debug a problem involving Angular Scenario Based?",
      "title": "debug a problem involving Angular Scenario Based",
      "direct_answer": "I would debug a problem involving Angular view shows stale data, a @for list misbehaves, or a component leaks subscriptions — what's the fix? by reproducing it with the smallest input first. OnPush staleness → replace references; @for churn → stable track keys; subscription leaks → takeUntilDestroyed/async pipe. Then I would inspect the intermediate values, compare them with the expected rule, fix the first incorrect step, and add a regression test.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain Angular view shows stale data, a @for list misbehaves, or a component leaks subscriptions — what's the fix? in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about Angular view shows stale data, a @for list misbehaves, or a component leaks subscriptions — what's the fix? without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what Angular view shows stale data, a @for list misbehaves, or a component leaks subscriptions — what's the fix? is supposed to guarantee. OnPush staleness → replace references; @for churn → stable track keys; subscription leaks → takeUntilDestroyed/async pipe."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: OnPush staleness → replace references; @for churn → stable track keys; subscription leaks → takeUntilDestroyed/async pipe. Once I find the first incorrect value, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "OnPush staleness → replace references; @for churn → stable track keys; subscription leaks → takeUntilDestroyed/async pipe.\n\nMutating arrays under OnPush and 'randomly' calling detectChanges everywhere instead of fixing the data flow; and forgetting track stability when the API re-creates ids."
          }
        ]
      },
      "followup_questions": [
        "When would you use Angular Scenario Based in a real project?",
        "How would you compare Angular Scenario Based with an alternative?",
        "What is a common mistake when using Angular Scenario Based?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Angular Scenario Based — frontend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Angular Scenario Based? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

{
  "topic": "Forms Template Driven",
  "topicSlug": "forms-template-driven",
  "questions": [
    {
      "id": "angular-fundamentals-fresher-forms-template-driven-q001",
      "slug": "angular-fundamentals-fresher-forms-template-driven-td-forms",
      "question": "How do template-driven forms work — NgForm, ngModel registration, validation, and when to use them?",
      "title": "Template Driven Forms",
      "direct_answer": "Template-driven forms (TDF) put the form model in the TEMPLATE: FormsModule + ngModel on each field, and Angular assembles an NgForm/FormControl model by directive order — you rarely touch the class. Registration: <form #f=\"ngForm\"> exposes the form model to the template; [(ngModel)] binds data two-way; name=\"x\" is REQUIRED for ngModel to self-register into the form. Validation: built-in attributes (required, minlength, email, pattern) become validator functions on the control; state is readable as touched/dirty/invalid on the control (f.form.controls.email.invalid) and as form-level (f.invalid). Use TDF for simple forms — reactive forms (explicit FormBuilder model in the class) win for dynamic fields, complex validation, and unit-testability without the DOM.",
      "layout_type": "concept-explanation",
      "difficulty": "easy",
      "importance": "high",
      "reading_time_minutes": 6,
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "Quick Revision",
            "content": "Template-driven forms (TDF) put the form model in the TEMPLATE: FormsModule + ngModel on each field, and Angular assembles an NgForm/FormControl model by directive order — you rarely touch the class. Registration: <form #f=\"ngForm\"> exposes the form model to the template; [(ngModel)] binds data two-way; name=\"x\" is REQUIRED for ngModel to self-register into the form. Validation: built-in attributes (required, minlength, email, pattern) become validator functions on the control; state is readable as touched/dirty/invalid on the control (f.form.controls.email.invalid) and as form-level (f.invalid). Use TDF for simple forms — reactive forms (explicit FormBuilder model in the class) win for dynamic fields, complex validation, and unit-testability without the DOM."
          },
          {
            "type": "speakable_answer",
            "title": "Interview Answer",
            "content": "A working TDF with validation, verified:\n\n```html\n<form #f=\"ngForm\" (ngSubmit)=\"save(f)\">\n  <!-- name is MANDATORY: it's how ngModel registers into NgForm -->\n  <input name=\"email\" [(ngModel)]=\"model.email\" required email\n         #email=\"ngModel\" />\n  @if (email.invalid && email.touched) {\n    <p class=\"error\">\n      @if (email.errors?.['required']) { Email is required. }\n      @else if (email.errors?.['email']) { Enter a valid email. }\n    </p>\n  }\n\n  <input name=\"qty\" [(ngModel)]=\"model.qty\" required min=\"1\" max=\"9\"\n         #qty=\"ngModel\" />\n\n  <button [disabled]=\"f.invalid\">Submit</button>\n</form>\n```\n\nHow the model gets built (the interview core): unlike reactive forms where YOU build FormGroup/FormControl in the class, TDF builds the model from the template at directive-initialization time — each ngModel with a name registers a FormControl into the parent NgForm's FormGroup, in DOM order. #f=\"ngForm\" exports that auto-assembled model so the template can read it (f.invalid, f.value) and ngSubmit fires only on a valid submit by convention (you check f.invalid in the handler or guard with [disabled]).\n\nThe validation mapping: HTML attributes (required, minlength, email, pattern) are INTERCEPTED by Angular's directives and compiled into validator functions on the control — the DOM attribute is the authoring surface, but the runtime is the same Validators.required pipeline as reactive forms. State classes (ng-invalid/ng-touched) land on the element for CSS styling, and the errors object (control.errors) carries structured error keys you test in the template — same object as the reactive form.\n\nThe decision rule (say it unprompted): TDF for small, static, template-shaped forms — quick, readable, minimal code. Reactive forms for anything dynamic (fields added/removed at runtime), with cross-field validation, or needing to be unit-tested without rendering a template — the model exists in the class, so you test the model. Both run on the same underlying FormControl machinery; the difference is who declares it and where it lives."
          },
          {
            "type": "flow_diagram",
            "title": "Mental Model",
            "content": "```mermaid\nflowchart TD\n    F['FormsModule + <form #f=ngForm>'] --> M['ngModel per field + name attr']\n    M --> REG[auto-register FormControl into NgForm in DOM order]\n    REG --> V[validators from HTML attrs: required email min pattern]\n    V --> ST[state: pristine dirty touched invalid + ng-* classes]\n    ST --> UI[template shows errors when touched + invalid]\n    SUB[(ngSubmit)] --> H[handler receives f — guard on f.invalid]\n    SIMPLE[simple static form] --> TDF[use TDF]\n    DYN[dynamic fields cross-field validation testable model] --> RF[use reactive forms]\n```"
          },
          {
            "type": "deep_explanation",
            "title": "Deep Dive",
            "content": "The two-way binding + registration split (the mechanism people miss): [(ngModel)] without a name does two-way binding only — the field is NOT part of the form model and f.value won't include it. With name, ngModel BOTH binds the data and registers the control. standalone: true inside an ngForm boundary is the escape hatch for a field that binds but doesn't belong to the form. The #email=\"ngModel\" export gives the template the field's own FormControl — the per-field state (touched, dirty, errors) the error UI reads; without it you navigate f.form.controls['email'] which is untyped and clunky in templates.\n\nngSubmit vs form submit (the subtle contract): ngSubmit is Angular's own event that fires on submit AFTER ngNoForm/FORM_DIRECTIVES handling — and critically, TDF does NOT preventDefault for you in the old template binding form; the (ngSubmit) binding via NgForm DOES prevent the native submission, so the page-doesn't-reload guarantee comes from using NgForm's event, not from the browser. A disabled submit button ([disabled]=\"f.invalid\") is UX guidance, not a guarantee — always re-validate in the handler or server, since touched-state means users can still submit invalid forms via Enter key in some browsers.\n\nThe migration story (worth one line in any answer): form control value accessors (the bridge directives for non-input elements — select, custom components implementing ControlValueAccessor) work identically in both TDF and reactive forms — they're the CVA contract, the third layer below the two form styles. A custom form control (a rating widget, a masked input) implements ControlValueAccessor once and works in ngModel templates AND FormControl bindings; that's the layer interviewers drill when they ask 'how would you make our design-system input work with Angular forms.'"
          }
        ]
      },
      "followup_questions": [
        "When would you use Forms Template Driven in a real project?",
        "How would you compare Forms Template Driven with an alternative?",
        "What is a common mistake when using Forms Template Driven?"
      ],
      "order": 1,
      "seo": {
        "metaTitle": "Template Driven Forms — frontend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How do template-driven forms work — NgForm, ngModel registration, validation, and when to use them? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "interviewer_intent": {
        "testing": "Whether you understand template-driven Angular forms beyond memorized syntax.",
        "common_mistake": "Giving a definition without explaining a practical trade-off.",
        "to_stand_out": "Connect the concept to a small production example and state its failure mode."
      },
      "priority": "must-prepare",
      "role": "anchor",
      "interviewStage": "framework",
      "answerProfile": "implementation"
    },
    {
      "id": "angular-fundamentals-fresher-forms-template-driven-q002",
      "slug": "angular-fundamentals-fresher-forms-template-driven-when-to-use",
      "question": "When would you use Forms Template Driven in a real project?",
      "title": "When would you use Forms Template Driven in a real project",
      "direct_answer": "I would use How do template-driven forms work — NgForm, ngModel registration, validation, and when to use them? when the problem matches its main benefit. TDF = the template IS the form model: ngModel + name registers fields into NgForm, HTML validation attributes map to validators, and state flows through #f=\"ngForm\" — best for simple forms. In a real project, I would confirm that benefit on a small example before applying it broadly, and I would keep the simpler option whenever it is just as clear.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Start by defining How do template-driven forms work — NgForm, ngModel registration, validation, and when to use them? in one sentence, then connect it to a decision: what kind of input or behaviour makes it a good fit. The interviewer is looking for a practical reason, not a list of buzzwords. TDF = the template IS the form model: ngModel + name registers fields into NgForm, HTML validation attributes map to validators, and state flows through #f=\"ngForm\" — best for simple forms."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I use How do template-driven forms work — NgForm, ngModel registration, validation, and when to use them? when its rule makes the code safer, clearer, or more efficient for the actual problem. TDF = the template IS the form model: ngModel + name registers fields into NgForm, HTML validation attributes map to validators, and state flows through #f=\"ngForm\" — best for simple forms. I would first check the input and constraints, use a small example to confirm the behaviour, and then add a test for the boundary case. If the simpler option is just as clear and fast, I would choose that instead."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "TDF = the template IS the form model: ngModel + name registers fields into NgForm, HTML validation attributes map to validators, and state flows through #f=\"ngForm\" — best for simple forms.\n\nForgetting the name attribute (field silently doesn't register), checking pristine/touched state wrong, and building a complex dynamic form with TDF when reactive forms are the tool."
          }
        ]
      },
      "followup_questions": [
        "When would you use Forms Template Driven in a real project?",
        "How would you compare Forms Template Driven with an alternative?",
        "What is a common mistake when using Forms Template Driven?"
      ],
      "order": 2,
      "seo": {
        "metaTitle": "When would you use Forms Template Driven in a real project — frontend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for When would you use Forms Template Driven in a real project? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "interviewer_intent": {
        "testing": "Whether you can explain How do template-driven forms work — NgForm, ngModel registration, validation, and when to use them? in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do template-driven forms work — NgForm, ngModel registration, validation, and when to use them? without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      }
    },
    {
      "id": "angular-fundamentals-fresher-forms-template-driven-q003",
      "slug": "angular-fundamentals-fresher-forms-template-driven-common-mistake",
      "question": "What is a common mistake when using Forms Template Driven?",
      "title": "a common mistake when using Forms Template Driven",
      "direct_answer": "A common mistake with How do template-driven forms work — NgForm, ngModel registration, validation, and when to use them? is using the rule without checking its boundary conditions. TDF = the template IS the form model: ngModel + name registers fields into NgForm, HTML validation attributes map to validators, and state flows through #f=\"ngForm\" — best for simple forms. That can make a valid-looking answer wrong under an edge case, so I state the assumption, handle the edge case, and add a small regression test.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Name one mistake, show why it happens, and explain the guard you would add. Avoid saying only \"be careful\"; connect the mistake to the behaviour of How do template-driven forms work — NgForm, ngModel registration, validation, and when to use them?. TDF = the template IS the form model: ngModel + name registers fields into NgForm, HTML validation attributes map to validators, and state flows through #f=\"ngForm\" — best for simple forms."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "The mistake I watch for with How do template-driven forms work — NgForm, ngModel registration, validation, and when to use them? is assuming the happy path is the whole rule. TDF = the template IS the form model: ngModel + name registers fields into NgForm, HTML validation attributes map to validators, and state flows through #f=\"ngForm\" — best for simple forms. For example, the normal case may work while the zero, empty, duplicate, or error case behaves differently. I make the assumption explicit, add a focused test for it, and verify the result with a small input before shipping."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "TDF = the template IS the form model: ngModel + name registers fields into NgForm, HTML validation attributes map to validators, and state flows through #f=\"ngForm\" — best for simple forms.\n\nForgetting the name attribute (field silently doesn't register), checking pristine/touched state wrong, and building a complex dynamic form with TDF when reactive forms are the tool."
          }
        ]
      },
      "followup_questions": [
        "When would you use Forms Template Driven in a real project?",
        "How would you compare Forms Template Driven with an alternative?",
        "What is a common mistake when using Forms Template Driven?"
      ],
      "order": 3,
      "seo": {
        "metaTitle": "a common mistake when using Forms Template Driven — frontend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for What is a common mistake when using Forms Template Driven? with a clear explanation, practical example, common mistakes, and follow-up questions."
      },
      "interviewer_intent": {
        "testing": "Whether you can explain How do template-driven forms work — NgForm, ngModel registration, validation, and when to use them? in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do template-driven forms work — NgForm, ngModel registration, validation, and when to use them? without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      }
    },
    {
      "id": "angular-fundamentals-fresher-forms-template-driven-q004",
      "slug": "angular-fundamentals-fresher-forms-template-driven-compare",
      "question": "How would you compare Forms Template Driven with an alternative?",
      "title": "compare Forms Template Driven with an alternative",
      "direct_answer": "I compare How do template-driven forms work — NgForm, ngModel registration, validation, and when to use them? with the simpler alternative by looking at correctness first, then time, memory, and readability. TDF = the template IS the form model: ngModel + name registers fields into NgForm, HTML validation attributes map to validators, and state flows through #f=\"ngForm\" — best for simple forms. I normally choose the simplest option that meets the requirement, and I can say why the alternative is not needed for this case.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How do template-driven forms work — NgForm, ngModel registration, validation, and when to use them? in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do template-driven forms work — NgForm, ngModel registration, validation, and when to use them? without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "A strong comparison names the rule, the alternative, and the condition that changes the choice. Use the same input for both approaches so the trade-off is concrete. TDF = the template IS the form model: ngModel + name registers fields into NgForm, HTML validation attributes map to validators, and state flows through #f=\"ngForm\" — best for simple forms."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would compare How do template-driven forms work — NgForm, ngModel registration, validation, and when to use them? with the straightforward alternative on the same example. TDF = the template IS the form model: ngModel + name registers fields into NgForm, HTML validation attributes map to validators, and state flows through #f=\"ngForm\" — best for simple forms. If it removes repeated work or makes an important guarantee explicit, I would choose it when that benefit matters. Otherwise, I would keep the simpler version because fewer moving parts are easier to test and maintain. I would mention both complexity and the edge case that separates them."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "TDF = the template IS the form model: ngModel + name registers fields into NgForm, HTML validation attributes map to validators, and state flows through #f=\"ngForm\" — best for simple forms.\n\nForgetting the name attribute (field silently doesn't register), checking pristine/touched state wrong, and building a complex dynamic form with TDF when reactive forms are the tool."
          }
        ]
      },
      "followup_questions": [
        "When would you use Forms Template Driven in a real project?",
        "How would you compare Forms Template Driven with an alternative?",
        "What is a common mistake when using Forms Template Driven?"
      ],
      "order": 4,
      "seo": {
        "metaTitle": "compare Forms Template Driven with an alternative — frontend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you compare Forms Template Driven with an alternative? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    },
    {
      "id": "angular-fundamentals-fresher-forms-template-driven-q005",
      "slug": "angular-fundamentals-fresher-forms-template-driven-scenario",
      "question": "How would you debug a problem involving Forms Template Driven?",
      "title": "debug a problem involving Forms Template Driven",
      "direct_answer": "I would debug a problem involving How do template-driven forms work — NgForm, ngModel registration, validation, and when to use them? by reproducing it with the smallest input first. TDF = the template IS the form model: ngModel + name registers fields into NgForm, HTML validation attributes map to validators, and state flows through #f=\"ngForm\" — best for simple forms. Then I would inspect the intermediate values, compare them with the expected rule, fix the first incorrect step, and add a regression test.",
      "layout_type": "interview-practice",
      "difficulty": "easy",
      "importance": "medium",
      "reading_time_minutes": 6,
      "interviewer_intent": {
        "testing": "Whether you can explain How do template-driven forms work — NgForm, ngModel registration, validation, and when to use them? in plain language and apply it to a small example.",
        "common_mistake": "Giving a slogan about How do template-driven forms work — NgForm, ngModel registration, validation, and when to use them? without checking its scope, boundary cases, or trade-offs.",
        "to_stand_out": "State the rule, walk through one concrete example, and name the test or edge case you would verify."
      },
      "answer": {
        "sections": [
          {
            "type": "interviewer_expectation",
            "title": "What the interviewer wants",
            "content": "Explain a repeatable debugging sequence: reproduce, isolate, observe, fix, and prove the fix. Tie each step to what How do template-driven forms work — NgForm, ngModel registration, validation, and when to use them? is supposed to guarantee. TDF = the template IS the form model: ngModel + name registers fields into NgForm, HTML validation attributes map to validators, and state flows through #f=\"ngForm\" — best for simple forms."
          },
          {
            "type": "speakable_answer",
            "title": "Interview answer",
            "content": "I would start by reproducing the bug with the smallest input that still fails. I would log or inspect the state before and after the key operation, then compare it with the expected rule: TDF = the template IS the form model: ngModel + name registers fields into NgForm, HTML validation attributes map to validators, and state flows through #f=\"ngForm\" — best for simple forms. Once I find the first incorrect value, I would make the smallest fix, add a regression test, and run the boundary cases. I would also check the change under realistic load before calling it done."
          },
          {
            "type": "deep_explanation",
            "title": "Build the mental model",
            "content": "TDF = the template IS the form model: ngModel + name registers fields into NgForm, HTML validation attributes map to validators, and state flows through #f=\"ngForm\" — best for simple forms.\n\nForgetting the name attribute (field silently doesn't register), checking pristine/touched state wrong, and building a complex dynamic form with TDF when reactive forms are the tool."
          }
        ]
      },
      "followup_questions": [
        "When would you use Forms Template Driven in a real project?",
        "How would you compare Forms Template Driven with an alternative?",
        "What is a common mistake when using Forms Template Driven?"
      ],
      "order": 5,
      "seo": {
        "metaTitle": "debug a problem involving Forms Template Driven — frontend interview question | InterviewExplainer",
        "metaDescription": "Interview answer for How would you debug a problem involving Forms Template Driven? with a clear explanation, practical example, common mistakes, and follow-up questions."
      }
    }
  ]
}

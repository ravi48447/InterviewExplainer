# Graphs animation plan

34 canonical problems from `content/dsa/_index.json`. Each row names the primary reusable engine and the concrete explanation direction. Full storyboards and readiness metadata live in `../problem-animation-matrix.json`.

| Problem | Difficulty | Primary engine | Supporting engine | How we explain it | Learner action | Wave |
| --- | --- | --- | --- | --- | --- | --- |
| Bellman-Ford Shortest Path | medium | Bellman-Ford Sweeps | 1D DP State Conveyor | Teach the authored optimal approach, Bellman-Ford — V−1 Relaxation Passes + Cycle Check (Optimal), inside a weighted edge list beside a distance table organized by passes; sweep every edge, pulse successful relaxations, and compare the extra cycle pass. The decisive moment is why paths with at most k edges are solved after k passes. | run one full relaxation pass and predict whether another is needed | 1 |
| BFS: Shortest Reach in a Graph | medium | Graph Frontier | — | Teach the authored optimal approach, Plain BFS, Scale by 6 (Optimal), inside a fixed graph beside the queue or recursion stack that drives traversal; expand one edge at a time and stamp distance, parent, or component labels. The decisive moment is how frontier plus visited state prevents repeated work, and when queue layers imply shortest distance. | expand the next frontier node and predict newly discovered vertices | 2 |
| Castle on the Grid | medium | Grid Frontier | Graph Frontier | Teach the authored optimal approach, BFS with Slide Pruning (Optimal), inside a terrain grid with visited cells, frontier cells, and untouched cells distinct; expand DFS tendrils or BFS waves while keeping the frontier explicit. The decisive moment is the exact rule that turns neighboring cells into one component or shortest layer. | choose the next valid neighbor and watch the frontier update | 2 |
| Clone Graph | medium | Graph Frontier | Array + Memory Ledger | Teach the authored optimal approach, BFS with Queue and Clone Map, inside a fixed graph beside the queue or recursion stack that drives traversal; expand one edge at a time and stamp distance, parent, or component labels. The decisive moment is how frontier plus visited state prevents repeated work, and when queue layers imply shortest distance. | expand the next frontier node and predict newly discovered vertices | 2 |
| Components in a Graph | medium | Union-Find Merger | — | Teach the authored optimal approach, Union-Find with Size Tracking (Optimal), inside components, parent pointers, and rank/size values in one coordinated view; trace find paths, compress them, then merge roots by rank or size. The decisive moment is how compression changes future cost without changing membership. | connect two nodes and inspect the resulting parent forest | 2 |
| Course Schedule (Topological Sort) | medium | Topological Unlock | Graph Frontier | Teach the authored optimal approach, DFS with Three-Color Cycle Detection, inside a dependency graph with live indegree counters and an unlocked queue; remove a completed node, decrement outgoing neighbors, and unlock zeros. The decisive moment is why an empty queue with nodes remaining proves a cycle. | choose an unlocked node and update its dependents | 1 |
| Course Schedule II | medium | Topological Unlock | Graph Frontier | Teach the authored optimal approach, DFS Post-Order with 3-Color Cycle Detection, inside a dependency graph with live indegree counters and an unlocked queue; remove a completed node, decrement outgoing neighbors, and unlock zeros. The decisive moment is why an empty queue with nodes remaining proves a cycle. | choose an unlocked node and update its dependents | 2 |
| Detect Cycle in a Directed Graph | medium | Topological Unlock | Graph Frontier | Teach the authored optimal approach, Kahn's Algorithm — Indegree BFS (Iterative Alternative), inside a dependency graph with live indegree counters and an unlocked queue; remove a completed node, decrement outgoing neighbors, and unlock zeros. The decisive moment is why an empty queue with nodes remaining proves a cycle. | choose an unlocked node and update its dependents | 2 |
| Dijkstra: Shortest Reach 2 | medium | Dijkstra Distance Lab | Heap Arena | Teach the authored optimal approach, Min-Heap Dijkstra (Optimal), inside a weighted graph, tentative-distance table, and priority queue; extract the best candidate, relax edges, and visibly discard stale entries. The decisive moment is why a finalized shortest distance cannot improve with non-negative edges. | choose the next queue item and perform its relaxations | 2 |
| Dijkstra's Shortest Path | medium | Dijkstra Distance Lab | Heap Arena | Teach the authored optimal approach, Dijkstra — Min-Heap Greedy (Optimal), inside a weighted graph, tentative-distance table, and priority queue; extract the best candidate, relax edges, and visibly discard stale entries. The decisive moment is why a finalized shortest distance cannot improve with non-negative edges. | choose the next queue item and perform its relaxations | 2 |
| Even Tree | medium | Graph Frontier | Greedy Choice Frontier | Teach the authored optimal approach, Iterative DFS Subtree Sizes (Optimal), inside a fixed graph beside the queue or recursion stack that drives traversal; expand one edge at a time and stamp distance, parent, or component labels. The decisive moment is how frontier plus visited state prevents repeated work, and when queue layers imply shortest distance. | expand the next frontier node and predict newly discovered vertices | 2 |
| Find the Nearest Clone | medium | Graph Frontier | — | Teach the authored optimal approach, Multi-Source BFS with Source Tags (Optimal), inside a fixed graph beside the queue or recursion stack that drives traversal; expand one edge at a time and stamp distance, parent, or component labels. The decisive moment is how frontier plus visited state prevents repeated work, and when queue layers imply shortest distance. | expand the next frontier node and predict newly discovered vertices | 2 |
| Floyd-Warshall All-Pairs Shortest Path | medium | Floyd-Warshall Matrix | 1D DP State Conveyor | Teach the authored optimal approach, Floyd-Warshall — DP Over Intermediate Vertices (Optimal for Small Dense), inside a graph beside an all-pairs distance matrix; open one intermediate node k and update every i,j cell through it. The decisive moment is the DP meaning of allowing only the first k intermediates. | select k and inspect which matrix cells improve | 2 |
| Floyd: City of Blinding Lights | medium | Floyd-Warshall Matrix | 1D DP State Conveyor | Teach the authored optimal approach, Floyd-Warshall All-Pairs (Optimal), inside a graph beside an all-pairs distance matrix; open one intermediate node k and update every i,j cell through it. The decisive moment is the DP meaning of allowing only the first k intermediates. | select k and inspect which matrix cells improve | 2 |
| Friend Circle Queries | medium | Union-Find Merger | Array + Memory Ledger | Teach the authored optimal approach, Union-Find with Size (Optimal), inside components, parent pointers, and rank/size values in one coordinated view; trace find paths, compress them, then merge roots by rank or size. The decisive moment is how compression changes future cost without changing membership. | connect two nodes and inspect the resulting parent forest | 2 |
| Graph Valid Tree | medium | Union-Find Merger | Graph Frontier | Teach the authored optimal approach, DFS / BFS with Parent Tracking, inside components, parent pointers, and rank/size values in one coordinated view; trace find paths, compress them, then merge roots by rank or size. The decisive moment is how compression changes future cost without changing membership. | connect two nodes and inspect the resulting parent forest | 1 |
| Is Graph Bipartite? (Two-Coloring) | medium | Graph Coloring Studio | Graph Frontier | Teach the authored optimal approach, DFS Two-Coloring (Recursive Alternative), inside a graph with two color palettes and conflict edges; propagate the opposite color across each component and flag contradictions. The decisive moment is why an odd cycle makes two-coloring impossible. | color the next node and detect a conflict | 2 |
| Journey to the Moon | medium | Union-Find Merger | — | Teach the authored optimal approach, Union-Find Sizes + Running-Sum Accumulation (Optimal), inside components, parent pointers, and rank/size values in one coordinated view; trace find paths, compress them, then merge roots by rank or size. The decisive moment is how compression changes future cost without changing membership. | connect two nodes and inspect the resulting parent forest | 2 |
| Kruskal (MST): Really Special Subtree | medium | Minimum-Spanning-Tree Edge Market | Greedy Choice Frontier | Teach the authored optimal approach, Kruskal with Union-Find (Optimal), inside weighted candidate edges competing to join a growing forest; accept the cheapest safe edge and reject edges that create a cycle. The decisive moment is the cut/cycle reason a local edge choice stays globally safe. | approve or reject the next edge, then reveal the proof | 2 |
| Kruskal's Minimum Spanning Tree | medium | Minimum-Spanning-Tree Edge Market | Greedy Choice Frontier | Teach the authored optimal approach, Kruskal with Path Compression + Union by Rank (Optimal), inside weighted candidate edges competing to join a growing forest; accept the cheapest safe edge and reject edges that create a cycle. The decisive moment is the cut/cycle reason a local edge choice stays globally safe. | approve or reject the next edge, then reveal the proof | 2 |
| Minimum Genetic Mutation | medium | Graph Frontier | Array + Memory Ledger | Teach the authored optimal approach, BFS by Scanning the Bank for One-Away Neighbours, inside a fixed graph beside the queue or recursion stack that drives traversal; expand one edge at a time and stamp distance, parent, or component labels. The decisive moment is how frontier plus visited state prevents repeated work, and when queue layers imply shortest distance. | expand the next frontier node and predict newly discovered vertices | 2 |
| Number of Connected Components in an Undirected Graph | medium | Union-Find Merger | Graph Frontier | Teach the authored optimal approach, Union-Find with Path Compression + Union by Rank (Optimal), inside components, parent pointers, and rank/size values in one coordinated view; trace find paths, compress them, then merge roots by rank or size. The decisive moment is how compression changes future cost without changing membership. | connect two nodes and inspect the resulting parent forest | 2 |
| Number of Islands | medium | Grid Frontier | Graph Frontier | Teach the authored optimal approach, BFS Flood Fill, inside a terrain grid with visited cells, frontier cells, and untouched cells distinct; expand DFS tendrils or BFS waves while keeping the frontier explicit. The decisive moment is the exact rule that turns neighboring cells into one component or shortest layer. | choose the next valid neighbor and watch the frontier update | 1 |
| Pacific Atlantic Water Flow | medium | Grid Frontier | Graph Frontier | Teach the authored optimal approach, Reverse Multi-Source DFS From Both Oceans (Optimal), inside a terrain grid with visited cells, frontier cells, and untouched cells distinct; expand DFS tendrils or BFS waves while keeping the frontier explicit. The decisive moment is the exact rule that turns neighboring cells into one component or shortest layer. | choose the next valid neighbor and watch the frontier update | 2 |
| Prim's (MST): Special Subtree | medium | Minimum-Spanning-Tree Edge Market | Heap Arena | Teach the authored optimal approach, Min-Heap Prim (Optimal), inside weighted candidate edges competing to join a growing forest; accept the cheapest safe edge and reject edges that create a cycle. The decisive moment is the cut/cycle reason a local edge choice stays globally safe. | approve or reject the next edge, then reveal the proof | 2 |
| Prim's Minimum Spanning Tree | medium | Minimum-Spanning-Tree Edge Market | Heap Arena | Teach the authored optimal approach, Prim with Min-Heap (Optimal for Sparse/Moderate), inside weighted candidate edges competing to join a growing forest; accept the cheapest safe edge and reject edges that create a cycle. The decisive moment is the cut/cycle reason a local edge choice stays globally safe. | approve or reject the next edge, then reveal the proof | 2 |
| Roads and Libraries | medium | Union-Find Merger | Greedy Choice Frontier | Teach the authored optimal approach, Union-Find Component Sizing (Optimal), inside components, parent pointers, and rank/size values in one coordinated view; trace find paths, compress them, then merge roots by rank or size. The decisive moment is how compression changes future cost without changing membership. | connect two nodes and inspect the resulting parent forest | 2 |
| Rotting Oranges | medium | Grid Frontier | Graph Frontier | Teach the authored optimal approach, Multi-Source BFS (Optimal), inside a terrain grid with visited cells, frontier cells, and untouched cells distinct; expand DFS tendrils or BFS waves while keeping the frontier explicit. The decisive moment is the exact rule that turns neighboring cells into one component or shortest layer. | choose the next valid neighbor and watch the frontier update | 1 |
| Snakes and Ladders | medium | Graph Frontier | — | Teach the authored optimal approach, BFS with a Distance Array, inside a fixed graph beside the queue or recursion stack that drives traversal; expand one edge at a time and stamp distance, parent, or component labels. The decisive moment is how frontier plus visited state prevents repeated work, and when queue layers imply shortest distance. | expand the next frontier node and predict newly discovered vertices | 2 |
| Snakes and Ladders: The Quickest Way Up | medium | Graph Frontier | — | Teach the authored optimal approach, BFS over the Board Graph (Optimal), inside a fixed graph beside the queue or recursion stack that drives traversal; expand one edge at a time and stamp distance, parent, or component labels. The decisive moment is how frontier plus visited state prevents repeated work, and when queue layers imply shortest distance. | expand the next frontier node and predict newly discovered vertices | 2 |
| Surrounded Regions | medium | Grid Frontier | Graph Frontier | Teach the authored optimal approach, Union-Find with a Virtual Border Node, inside a terrain grid with visited cells, frontier cells, and untouched cells distinct; expand DFS tendrils or BFS waves while keeping the frontier explicit. The decisive moment is the exact rule that turns neighboring cells into one component or shortest layer. | choose the next valid neighbor and watch the frontier update | 2 |
| The Story of a Tree | hard | Rerooting Lens | Graph Frontier | Teach the authored optimal approach, Rerooting DP (Optimal), inside a tree with directed parent-child contribution arrows; compute downward answers, move the root across one edge, and repair contributions. The decisive moment is what changes locally and what can be reused when the root moves. | drag the root to a neighbor and recompute only affected state | 3 |
| Topological Sort (Kahn's Algorithm) | medium | Topological Unlock | Graph Frontier | Teach the authored optimal approach, Kahn's Algorithm — In-Degree BFS (Optimal), inside a dependency graph with live indegree counters and an unlocked queue; remove a completed node, decrement outgoing neighbors, and unlock zeros. The decisive moment is why an empty queue with nodes remaining proves a cycle. | choose an unlocked node and update its dependents | 2 |
| Word Ladder | hard | Graph Frontier | Array + Memory Ledger | Teach the authored optimal approach, Bidirectional BFS (Meet in the Middle), inside a fixed graph beside the queue or recursion stack that drives traversal; expand one edge at a time and stamp distance, parent, or component labels. The decisive moment is how frontier plus visited state prevents repeated work, and when queue layers imply shortest distance. | expand the next frontier node and predict newly discovered vertices | 3 |

# Binary Search animation plan

16 canonical problems from `content/dsa/_index.json`. Each row names the primary reusable engine and the concrete explanation direction. Full storyboards and readiness metadata live in `../problem-animation-matrix.json`.

| Problem | Difficulty | Primary engine | Supporting engine | How we explain it | Learner action | Wave |
| --- | --- | --- | --- | --- | --- | --- |
| Aggressive Cows | medium | Binary Search on the Answer | Binary-Search Spotlight | Teach the authored optimal approach, Binary Search on the Distance (Optimal), inside a numeric answer dial paired with a feasibility simulation; test a candidate, run the greedy checker, and tighten the answer range. The decisive moment is the monotonic yes/no boundary hidden inside an optimization problem. | guess a candidate answer and run the feasibility check | 2 |
| Allocate Minimum Number of Pages | medium | Binary Search on the Answer | Binary-Search Spotlight | Teach the authored optimal approach, Binary Search on the Page Cap (Optimal), inside a numeric answer dial paired with a feasibility simulation; test a candidate, run the greedy checker, and tighten the answer range. The decisive moment is the monotonic yes/no boundary hidden inside an optimization problem. | guess a candidate answer and run the feasibility check | 2 |
| Binary Search | easy | Binary-Search Spotlight | — | Teach the authored optimal approach, Binary Search — Inclusive Bounds (Optimal), inside an ordered search space with low, mid, and high markers; collapse the surviving interval while leaving rejected space visible but muted. The decisive moment is the predicate that makes half the search space impossible. | vote left/right/found before the interval collapses | 2 |
| Find K-th Smallest Pair Distance | hard | Binary-Search Spotlight | Two-Pointer Stage | Teach the authored optimal approach, Binary Search on the Answer + Two-Pointer Count (Optimal), inside an ordered search space with low, mid, and high markers; collapse the surviving interval while leaving rejected space visible but muted. The decisive moment is the predicate that makes half the search space impossible. | vote left/right/found before the interval collapses | 3 |
| Find Minimum in Rotated Sorted Array | medium | Binary-Search Spotlight | — | Teach the authored optimal approach, Binary Search for the Pivot (Optimal), inside an ordered search space with low, mid, and high markers; collapse the surviving interval while leaving rejected space visible but muted. The decisive moment is the predicate that makes half the search space impossible. | vote left/right/found before the interval collapses | 2 |
| Find Peak Element | medium | Binary-Search Spotlight | — | Teach the authored optimal approach, Binary Search Uphill (Optimal), inside an ordered search space with low, mid, and high markers; collapse the surviving interval while leaving rejected space visible but muted. The decisive moment is the predicate that makes half the search space impossible. | vote left/right/found before the interval collapses | 2 |
| Floor and Ceil in a Sorted Array | easy | Binary-Search Spotlight | — | Teach the authored optimal approach, Binary Search for the Boundary (Optimal), inside an ordered search space with low, mid, and high markers; collapse the surviving interval while leaving rejected space visible but muted. The decisive moment is the predicate that makes half the search space impossible. | vote left/right/found before the interval collapses | 2 |
| Koko Eating Bananas | medium | Binary Search on the Answer | Binary-Search Spotlight | Teach the authored optimal approach, Binary Search on the Answer (Optimal), inside a numeric answer dial paired with a feasibility simulation; test a candidate, run the greedy checker, and tighten the answer range. The decisive moment is the monotonic yes/no boundary hidden inside an optimization problem. | guess a candidate answer and run the feasibility check | 2 |
| Kth Smallest Element in an Array | medium | Heap Arena | Binary-Search Spotlight | Teach the authored optimal approach, Quickselect — Partition and Recurse One Side (Optimal), inside a heap tree synchronized with its array and candidate queue; insert/extract at the root and show every swap that repairs the invariant. The decisive moment is why only the best candidate needs to be immediately accessible. | choose the next candidate and repair the heap | 2 |
| Median of Two Sorted Arrays | hard | Binary-Search Spotlight | — | Teach the authored optimal approach, Binary Search the Partition (Optimal), inside an ordered search space with low, mid, and high markers; collapse the surviving interval while leaving rejected space visible but muted. The decisive moment is the predicate that makes half the search space impossible. | vote left/right/found before the interval collapses | 3 |
| Painter's Partition Problem | medium | Binary Search on the Answer | Binary-Search Spotlight | Teach the authored optimal approach, Binary Search on the Finishing Time (Optimal), inside a numeric answer dial paired with a feasibility simulation; test a candidate, run the greedy checker, and tighten the answer range. The decisive moment is the monotonic yes/no boundary hidden inside an optimization problem. | guess a candidate answer and run the feasibility check | 2 |
| Pairs | medium | Binary-Search Spotlight | Two-Pointer Stage | Teach the authored optimal approach, Sort + Binary Search (Optimal), inside an ordered search space with low, mid, and high markers; collapse the surviving interval while leaving rejected space visible but muted. The decisive moment is the predicate that makes half the search space impossible. | vote left/right/found before the interval collapses | 2 |
| Search a 2D Matrix | medium | Binary-Search Spotlight | — | Teach the authored optimal approach, Flatten to a Virtual 1D Array (Optimal), inside an ordered search space with low, mid, and high markers; collapse the surviving interval while leaving rejected space visible but muted. The decisive moment is the predicate that makes half the search space impossible. | vote left/right/found before the interval collapses | 2 |
| Search in Rotated Sorted Array | medium | Binary-Search Spotlight | — | Teach the authored optimal approach, One-Pass Binary Search — Which Half Is Sorted (Optimal), inside an ordered search space with low, mid, and high markers; collapse the surviving interval while leaving rejected space visible but muted. The decisive moment is the predicate that makes half the search space impossible. | vote left/right/found before the interval collapses | 1 |
| Search Insert Position | easy | Binary-Search Spotlight | — | Teach the authored optimal approach, Lower-Bound Binary Search (Optimal), inside an ordered search space with low, mid, and high markers; collapse the surviving interval while leaving rejected space visible but muted. The decisive moment is the predicate that makes half the search space impossible. | vote left/right/found before the interval collapses | 2 |
| Ternary Search on a Unimodal Function | easy | Unimodal Probe | Binary-Search Spotlight | Teach the authored optimal approach, Ternary Search (Optimal), inside a hill-shaped function with two probes splitting it into thirds; compare the probe heights and fade the slope that cannot contain the optimum. The decisive moment is why local slope reveals which side contains the peak. | move the probes and predict the discarded third | 2 |

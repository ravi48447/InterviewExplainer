# Backtracking animation plan

13 canonical problems from `content/dsa/_index.json`. Each row names the primary reusable engine and the concrete explanation direction. Full storyboards and readiness metadata live in `../problem-animation-matrix.json`.

| Problem | Difficulty | Primary engine | Supporting engine | How we explain it | Learner action | Wave |
| --- | --- | --- | --- | --- | --- | --- |
| Combination Sum | medium | Decision-Tree Backtracker | — | Teach the authored optimal approach, Backtracking with Start Index (Optimal), inside the concrete board/string plus a parallel choose-recurse-undo tree; commit one choice, descend, hit success/dead-end, then visibly undo. The decisive moment is the constraint that prunes an entire branch. | choose the next candidate and predict whether it survives | 2 |
| Crossword Puzzle | medium | Constraint Board | — | Teach the authored optimal approach, Slot Enumeration + Backtracking (Optimal), inside a board with candidate sets and row/column/region constraints; remove candidates as constraints propagate, then branch only when needed. The decisive moment is which constraint causes the decisive prune. | place a candidate and inspect all propagated consequences | 2 |
| Knight's Tour (Count / Locate) | hard | Decision-Tree Backtracker | Grid Frontier | Teach the authored optimal approach, Backtracking + Warnsdorff's Heuristic (Finds One Tour Fast), inside the concrete board/string plus a parallel choose-recurse-undo tree; commit one choice, descend, hit success/dead-end, then visibly undo. The decisive moment is the constraint that prunes an entire branch. | choose the next candidate and predict whether it survives | 3 |
| Letter Combinations of a Phone Number | medium | Decision-Tree Backtracker | — | Teach the authored optimal approach, Backtracking over Digit Positions (Optimal), inside the concrete board/string plus a parallel choose-recurse-undo tree; commit one choice, descend, hit success/dead-end, then visibly undo. The decisive moment is the constraint that prunes an entire branch. | choose the next candidate and predict whether it survives | 2 |
| N-Queens | hard | Constraint Board | — | Teach the authored optimal approach, Row-by-Row Backtracking with Conflict Sets (Optimal), inside a board with candidate sets and row/column/region constraints; remove candidates as constraints propagate, then branch only when needed. The decisive moment is which constraint causes the decisive prune. | place a candidate and inspect all propagated consequences | 1 |
| Palindrome Partitioning | medium | Decision-Tree Backtracker | 1D DP State Conveyor | Teach the authored optimal approach, Backtracking over Cut Points (Optimal), inside the concrete board/string plus a parallel choose-recurse-undo tree; commit one choice, descend, hit success/dead-end, then visibly undo. The decisive moment is the constraint that prunes an entire branch. | choose the next candidate and predict whether it survives | 2 |
| Permutations | medium | Decision-Tree Backtracker | — | Teach the authored optimal approach, Backtracking with a used[] Set (Optimal), inside the concrete board/string plus a parallel choose-recurse-undo tree; commit one choice, descend, hit success/dead-end, then visibly undo. The decisive moment is the constraint that prunes an entire branch. | choose the next candidate and predict whether it survives | 2 |
| Permutations of Strings | medium | Decision-Tree Backtracker | — | Teach the authored optimal approach, Sort + Backtracking with Duplicate Skip (Optimal), inside the concrete board/string plus a parallel choose-recurse-undo tree; commit one choice, descend, hit success/dead-end, then visibly undo. The decisive moment is the constraint that prunes an entire branch. | choose the next candidate and predict whether it survives | 2 |
| Rat in a Maze | medium | Decision-Tree Backtracker | Grid Frontier | Teach the authored optimal approach, Four-Direction Backtracking (Optimal General Case), inside the concrete board/string plus a parallel choose-recurse-undo tree; commit one choice, descend, hit success/dead-end, then visibly undo. The decisive moment is the constraint that prunes an entire branch. | choose the next candidate and predict whether it survives | 2 |
| Subsets | medium | Decision-Tree Backtracker | — | Teach the authored optimal approach, Bitmask Enumeration (Iterative), inside the concrete board/string plus a parallel choose-recurse-undo tree; commit one choice, descend, hit success/dead-end, then visibly undo. The decisive moment is the constraint that prunes an entire branch. | choose the next candidate and predict whether it survives | 2 |
| The Power Sum | medium | Decision-Tree Backtracker | — | Teach the authored optimal approach, Include/Exclude Recursion with Increasing Base (Optimal), inside the concrete board/string plus a parallel choose-recurse-undo tree; commit one choice, descend, hit success/dead-end, then visibly undo. The decisive moment is the constraint that prunes an entire branch. | choose the next candidate and predict whether it survives | 2 |
| Tower of Hanoi | easy | Decision-Tree Backtracker | — | Teach the authored optimal approach, Recursive Move Generation (Optimal Sequence), inside the concrete board/string plus a parallel choose-recurse-undo tree; commit one choice, descend, hit success/dead-end, then visibly undo. The decisive moment is the constraint that prunes an entire branch. | choose the next candidate and predict whether it survives | 2 |
| Word Search | medium | Decision-Tree Backtracker | Grid Frontier | Teach the authored optimal approach, DFS + Backtracking with In-Place Marking (Optimal), inside the concrete board/string plus a parallel choose-recurse-undo tree; commit one choice, descend, hit success/dead-end, then visibly undo. The decisive moment is the constraint that prunes an entire branch. | choose the next candidate and predict whether it survives | 1 |

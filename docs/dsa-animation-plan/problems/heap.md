# Heap animation plan

8 canonical problems from `content/dsa/_index.json`. Each row names the primary reusable engine and the concrete explanation direction. Full storyboards and readiness metadata live in `../problem-animation-matrix.json`.

| Problem | Difficulty | Primary engine | Supporting engine | How we explain it | Learner action | Wave |
| --- | --- | --- | --- | --- | --- | --- |
| Find K Pairs with Smallest Sums | medium | Heap Arena | — | Teach the authored optimal approach, Min-Heap over the Sorted Frontier (Optimal), inside a heap tree synchronized with its array and candidate queue; insert/extract at the root and show every swap that repairs the invariant. The decisive moment is why only the best candidate needs to be immediately accessible. | choose the next candidate and repair the heap | 2 |
| Find Median from Data Stream | hard | Two-Heap Balance | Heap Arena | Teach the authored optimal approach, Two Heaps — Max-Heap Low Half, Min-Heap High Half (Optimal), inside a max-heap and min-heap on opposite sides of a median fulcrum; route each incoming value, then rebalance across the fulcrum. The decisive moment is how two partial orderings expose a streaming median. | place the next value and decide whether rebalancing is required | 1 |
| Find the Running Median | hard | Two-Heap Balance | Heap Arena | Teach the authored optimal approach, Two Heaps — Max-Heap + Min-Heap (Optimal), inside a max-heap and min-heap on opposite sides of a median fulcrum; route each incoming value, then rebalance across the fulcrum. The decisive moment is how two partial orderings expose a streaming median. | place the next value and decide whether rebalancing is required | 3 |
| IPO | hard | Heap Arena | Greedy Choice Frontier | Teach the authored optimal approach, Sort by Capital + Max-Heap of Affordable Profits (Optimal), inside a heap tree synchronized with its array and candidate queue; insert/extract at the root and show every swap that repairs the invariant. The decisive moment is why only the best candidate needs to be immediately accessible. | choose the next candidate and repair the heap | 3 |
| K Closest Points to Origin | medium | Heap Arena | Merge-Sort Studio | Teach the authored optimal approach, Quickselect (Optimal Average O(n)), inside a heap tree synchronized with its array and candidate queue; insert/extract at the root and show every swap that repairs the invariant. The decisive moment is why only the best candidate needs to be immediately accessible. | choose the next candidate and repair the heap | 2 |
| Kth Largest Element in an Array | medium | Heap Arena | Merge-Sort Studio | Teach the authored optimal approach, Quickselect — Partition Toward Index n−k (Optimal Average), inside a heap tree synchronized with its array and candidate queue; insert/extract at the root and show every swap that repairs the invariant. The decisive moment is why only the best candidate needs to be immediately accessible. | choose the next candidate and repair the heap | 2 |
| Task Scheduler | medium | Heap Arena | Array + Memory Ledger | Teach the authored optimal approach, Gap-Filling Math Formula (Optimal O(1) Extra), inside a heap tree synchronized with its array and candidate queue; insert/extract at the root and show every swap that repairs the invariant. The decisive moment is why only the best candidate needs to be immediately accessible. | choose the next candidate and repair the heap | 2 |
| Top K Frequent Elements | medium | Heap Arena | Array + Memory Ledger | Teach the authored optimal approach, Bucket Sort by Frequency (Optimal O(n)), inside a heap tree synchronized with its array and candidate queue; insert/extract at the root and show every swap that repairs the invariant. The decisive moment is why only the best candidate needs to be immediately accessible. | choose the next candidate and repair the heap | 1 |

# Math Geometry animation plan

35 canonical problems from `content/dsa/_index.json`. Each row names the primary reusable engine and the concrete explanation direction. Full storyboards and readiness metadata live in `../problem-animation-matrix.json`.

| Problem | Difficulty | Primary engine | Supporting engine | How we explain it | Learner action | Wave |
| --- | --- | --- | --- | --- | --- | --- |
| Armstrong Number | easy | Math Derivation Canvas | — | Teach the authored optimal approach, Count Digits, Then Sum Powers (Standard), inside a concrete example beside a symbolic equation and geometric/counting model; morph repeated work into the closed form or recurrence. The decisive moment is where each term of the formula comes from. | change the input and verify the derivation still holds | 2 |
| Catalan Numbers | medium | 1D DP State Conveyor | — | Teach the authored optimal approach, Closed Form C(2n,n)/(n+1) (Optimal Single Value), inside state cells ordered by subproblem size with dependency arrows; compare choices, compute the recurrence, and commit one new state. The decisive moment is the smallest state that contains everything the future needs. | choose the recurrence inputs and commit the next cell | 2 |
| Chinese Remainder Theorem | hard | Number-Theory Workbench | — | Teach the authored optimal approach, Constructive CRT — Orthogonal Basis / Iterative Merge (Optimal), inside numbers decomposed into factors, residues, or recurrence states; apply one algebraic reduction while keeping the invariant equation visible. The decisive moment is the identity that turns a huge numeric task into repeated smaller ones. | perform the next reduction and verify the invariant | 3 |
| Collatz Sequence Length | easy | 1D DP State Conveyor | — | Teach the authored optimal approach, Memoized Step Counts (Optimal for many queries), inside state cells ordered by subproblem size with dependency arrows; compare choices, compute the recurrence, and commit one new state. The decisive moment is the smallest state that contains everything the future needs. | choose the recurrence inputs and commit the next cell | 2 |
| Day of the Programmer | easy | Simulation State Machine | — | Teach the authored optimal approach, Direct Branch — September Closed Form (Optimal), inside the problem world beside explicit control state and an event timeline; consume one event, apply the rule, and snapshot the resulting state. The decisive moment is the compact state that makes every transition deterministic. | trigger the next event and predict the transition | 2 |
| Drawing Book | easy | Math Derivation Canvas | — | Teach the authored optimal approach, Direct Formula — min(p/2, n/2 − p/2) (Optimal), inside a concrete example beside a symbolic equation and geometric/counting model; morph repeated work into the closed form or recurrence. The decisive moment is where each term of the formula comes from. | change the input and verify the derivation still holds | 2 |
| Euler's Totient Function | medium | Sieve Elimination Field | — | Teach the authored optimal approach, Totient Sieve for All Values up to N, inside integers on a grid with base primes and composite strike paths; start at the mathematically correct multiple and eliminate in waves. The decisive moment is why earlier primes already handled smaller multiples. | select a prime and predict its first new composite | 2 |
| Extended Euclidean Algorithm | medium | Euclidean Reduction | — | Teach the authored optimal approach, Extended Euclidean Recursion (Optimal), inside two number bars and the remainder equation they produce; replace (a,b) with (b,a mod b) and optionally replay substitutions backward. The decisive moment is why the set of common divisors is preserved. | compute the next remainder and back-substitute coefficients | 2 |
| Factorial of a Number | easy | Math Derivation Canvas | — | Teach the authored optimal approach, BigInteger / Arbitrary Precision (Optimal for large n), inside a concrete example beside a symbolic equation and geometric/counting model; morph repeated work into the closed form or recurrence. The decisive moment is where each term of the formula comes from. | change the input and verify the derivation still holds | 2 |
| Factorial Trailing Zeroes | medium | Math Derivation Canvas | — | Teach the authored optimal approach, Legendre's Formula — Divide by Powers of 5 (Optimal), inside a concrete example beside a symbolic equation and geometric/counting model; morph repeated work into the closed form or recurrence. The decisive moment is where each term of the formula comes from. | change the input and verify the derivation still holds | 2 |
| FFT Polynomial Multiplication | hard | FFT Butterfly | — | Teach the authored optimal approach, Iterative FFT + Pointwise Multiply (Optimal), inside coefficient and value representations connected by even/odd butterfly stages; split, combine roots-of-unity pairs, multiply pointwise, then invert. The decisive moment is why convolution becomes pointwise multiplication after evaluation. | run one butterfly stage and inspect phase/degree changes | 3 |
| Game of Life | medium | Matrix Simulation Studio | — | Teach the authored optimal approach, In-Place 2-Bit Encoding (Optimal, O(1) space), inside a coordinate grid with direction, boundary, and staged cell states; apply one spatial rule per frame and preserve simultaneous updates when required. The decisive moment is the boundary or encoding trick that prevents overwritten state. | advance one move/generation and inspect affected neighbors | 2 |
| GCD and LCM | easy | Euclidean Reduction | — | Teach the authored optimal approach, Euclidean Algorithm + LCM Identity (Optimal), inside two number bars and the remainder equation they produce; replace (a,b) with (b,a mod b) and optionally replay substitutions backward. The decisive moment is why the set of common divisors is preserved. | compute the next remainder and back-substitute coefficients | 2 |
| GCD of Two Numbers | easy | Euclidean Reduction | — | Teach the authored optimal approach, Euclidean Algorithm with Modulo (Optimal), inside two number bars and the remainder equation they produce; replace (a,b) with (b,a mod b) and optionally replay substitutions backward. The decisive moment is why the set of common divisors is preserved. | compute the next remainder and back-substitute coefficients | 2 |
| Handshake | easy | Number-Theory Workbench | — | Teach the authored optimal approach, Closed Form — C(n, 2) = n(n−1)/2 (Optimal), inside numbers decomposed into factors, residues, or recurrence states; apply one algebraic reduction while keeping the invariant equation visible. The decisive moment is the identity that turns a huge numeric task into repeated smaller ones. | perform the next reduction and verify the invariant | 2 |
| Happy Number | easy | Two-Pointer Stage | Array + Memory Ledger | Teach the authored optimal approach, Floyd's Fast/Slow Pointers (O(1) Space), inside a stable sequence with independently controlled left/right or slow/fast actors; move only the pointer justified by the invariant and dim the discarded region. The decisive moment is why one move safely removes many impossible candidates. | choose which pointer moves before revealing the rule | 2 |
| Lucas' Theorem | hard | Number-Theory Workbench | — | Teach the authored optimal approach, Lucas' Theorem (Optimal), inside numbers decomposed into factors, residues, or recurrence states; apply one algebraic reduction while keeping the invariant equation visible. The decisive moment is the identity that turns a huge numeric task into repeated smaller ones. | perform the next reduction and verify the invariant | 3 |
| Matrix Exponentiation (Fibonacci) | medium | Number-Theory Workbench | — | Teach the authored optimal approach, Matrix Fast Exponentiation (Optimal), inside numbers decomposed into factors, residues, or recurrence states; apply one algebraic reduction while keeping the invariant equation visible. The decisive moment is the identity that turns a huge numeric task into repeated smaller ones. | perform the next reduction and verify the invariant | 2 |
| Max Points on a Line | hard | Geometry Slope Lab | Array + Memory Ledger | Teach the authored optimal approach, Anchor + Hash Exact Slopes (Optimal), inside an anchor point with rays to other points and a ledger of normalized rational slopes; rotate through anchors, reduce each dx/dy pair by gcd, and grow the largest collinear bucket. The decisive moment is why normalized integer slope pairs avoid floating-point and sign errors. | normalize the next ray and place it into the matching slope bucket | 3 |
| Modular Exponentiation | medium | Number-Theory Workbench | — | Teach the authored optimal approach, Binary (Fast) Exponentiation (Optimal), inside numbers decomposed into factors, residues, or recurrence states; apply one algebraic reduction while keeping the invariant equation visible. The decisive moment is the identity that turns a huge numeric task into repeated smaller ones. | perform the next reduction and verify the invariant | 2 |
| nCr mod p | medium | Number-Theory Workbench | — | Teach the authored optimal approach, Precomputed Factorials + Fermat Inverse (Optimal), inside numbers decomposed into factors, residues, or recurrence states; apply one algebraic reduction while keeping the invariant equation visible. The decisive moment is the identity that turns a huge numeric task into repeated smaller ones. | perform the next reduction and verify the invariant | 2 |
| Nth Fibonacci Number | medium | 1D DP State Conveyor | — | Teach the authored optimal approach, Fast Doubling (Optimal, O(log n)), inside state cells ordered by subproblem size with dependency arrows; compare choices, compute the recurrence, and commit one new state. The decisive moment is the smallest state that contains everything the future needs. | choose the recurrence inputs and commit the next cell | 2 |
| Number Line Jumps | easy | Math Derivation Canvas | — | Teach the authored optimal approach, Divisibility Formula — Solve for n (Optimal), inside a concrete example beside a symbolic equation and geometric/counting model; morph repeated work into the closed form or recurrence. The decisive moment is where each term of the formula comes from. | change the input and verify the derivation still holds | 2 |
| Palindrome Number | easy | String Token Conveyor | — | Teach the authored optimal approach, Reverse Half the Number (Optimal), inside characters as addressable tokens with parser state and output buffer; consume one character, update the state machine, and emit only justified output. The decisive moment is the parsing state required to handle boundaries and malformed input. | feed the next character and choose the state transition | 2 |
| Plus One | easy | Math Derivation Canvas | — | Teach the authored optimal approach, Digit-by-Digit Carry (Optimal), inside a concrete example beside a symbolic equation and geometric/counting model; morph repeated work into the closed form or recurrence. The decisive moment is where each term of the formula comes from. | change the input and verify the derivation still holds | 2 |
| Pow(x, n) | medium | Math Derivation Canvas | — | Teach the authored optimal approach, Iterative Binary Exponentiation (Optimal), inside a concrete example beside a symbolic equation and geometric/counting model; morph repeated work into the closed form or recurrence. The decisive moment is where each term of the formula comes from. | change the input and verify the derivation still holds | 2 |
| Recursive Digit Sum | easy | Math Derivation Canvas | — | Teach the authored optimal approach, Digital Root — Closed Form (Optimal), inside a concrete example beside a symbolic equation and geometric/counting model; morph repeated work into the closed form or recurrence. The decisive moment is where each term of the formula comes from. | change the input and verify the derivation still holds | 2 |
| Rotate Image | medium | Matrix Simulation Studio | — | Teach the authored optimal approach, Transpose + Reverse Each Row (Optimal, In Place), inside a coordinate grid with direction, boundary, and staged cell states; apply one spatial rule per frame and preserve simultaneous updates when required. The decisive moment is the boundary or encoding trick that prevents overwritten state. | advance one move/generation and inspect affected neighbors | 2 |
| Segmented Sieve | medium | Sieve Elimination Field | — | Teach the authored optimal approach, Segmented Sieve (Optimal), inside integers on a grid with base primes and composite strike paths; start at the mathematically correct multiple and eliminate in waves. The decisive moment is why earlier primes already handled smaller multiples. | select a prime and predict its first new composite | 2 |
| Set Matrix Zeroes | medium | Matrix Simulation Studio | — | Teach the authored optimal approach, First Row / Column as Markers (Optimal, O(1) Space), inside a coordinate grid with direction, boundary, and staged cell states; apply one spatial rule per frame and preserve simultaneous updates when required. The decisive moment is the boundary or encoding trick that prevents overwritten state. | advance one move/generation and inspect affected neighbors | 2 |
| Sieve of Eratosthenes | easy | Sieve Elimination Field | — | Teach the authored optimal approach, Sieve of Eratosthenes (Optimal), inside integers on a grid with base primes and composite strike paths; start at the mathematically correct multiple and eliminate in waves. The decisive moment is why earlier primes already handled smaller multiples. | select a prime and predict its first new composite | 1 |
| Spiral Matrix | medium | Matrix Simulation Studio | — | Teach the authored optimal approach, Four Boundary Pointers (Optimal), inside a coordinate grid with direction, boundary, and staged cell states; apply one spatial rule per frame and preserve simultaneous updates when required. The decisive moment is the boundary or encoding trick that prevents overwritten state. | advance one move/generation and inspect affected neighbors | 2 |
| Sqrt(x) | easy | Binary-Search Spotlight | — | Teach the authored optimal approach, Binary Search on the Answer (Optimal), inside an ordered search space with low, mid, and high markers; collapse the surviving interval while leaving rejected space visible but muted. The decisive moment is the predicate that makes half the search space impossible. | vote left/right/found before the interval collapses | 2 |
| Sum of Digits (Digital Root) | easy | Number-Theory Workbench | — | Teach the authored optimal approach, Digital Root in O(1) (Optimal for the repeated sum), inside numbers decomposed into factors, residues, or recurrence states; apply one algebraic reduction while keeping the invariant equation visible. The decisive moment is the identity that turns a huge numeric task into repeated smaller ones. | perform the next reduction and verify the invariant | 2 |
| Time Complexity: Primality | easy | Number-Theory Workbench | — | Teach the authored optimal approach, Trial Division up to √n, Skipping Evens (Optimal), inside numbers decomposed into factors, residues, or recurrence states; apply one algebraic reduction while keeping the invariant equation visible. The decisive moment is the identity that turns a huge numeric task into repeated smaller ones. | perform the next reduction and verify the invariant | 2 |

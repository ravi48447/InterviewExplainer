# Trees animation plan

41 canonical problems from `content/dsa/_index.json`. Each row names the primary reusable engine and the concrete explanation direction. Full storyboards and readiness metadata live in `../problem-animation-matrix.json`.

| Problem | Difficulty | Primary engine | Supporting engine | How we explain it | Learner action | Wave |
| --- | --- | --- | --- | --- | --- | --- |
| Average of Levels in Binary Tree | easy | Tree Traversal Stage | Grid Frontier | Teach the authored optimal approach, DFS Carrying Depth with Per-Level (sum, count), inside a fixed tree with an explicit frontier, call stack, and return path; move focus without moving nodes; animate enter, recurse, and return separately. The decisive moment is where information flows upward versus downward. | step into a child or return a value to the parent | 2 |
| Balanced Binary Tree | easy | Tree Traversal Stage | Grid Frontier | Teach the authored optimal approach, Bottom-Up Height with -1 Sentinel (Optimal), inside a fixed tree with an explicit frontier, call stack, and return path; move focus without moving nodes; animate enter, recurse, and return separately. The decisive moment is where information flows upward versus downward. | step into a child or return a value to the parent | 2 |
| Binary Search Tree: Insertion | easy | BST Ordered Corridor | Binary-Search Spotlight | Teach the authored optimal approach, Iterative Insert — Track the Parent, inside a fixed search tree paired with its persistent inorder strip; project lower/upper bounds through the tree while narrowing the valid corridor. The decisive moment is how the global ordering rule constrains every subtree. | choose a branch and inspect the remaining value bounds | 2 |
| Binary Search Tree: Lowest Common Ancestor | easy | BST Ordered Corridor | Binary-Search Spotlight | Teach the authored optimal approach, BST Guided Descent — Split Point (Optimal), inside a fixed search tree paired with its persistent inorder strip; project lower/upper bounds through the tree while narrowing the valid corridor. The decisive moment is how the global ordering rule constrains every subtree. | choose a branch and inspect the remaining value bounds | 2 |
| Binary Tree Level Order Traversal | medium | Tree Traversal Stage | Grid Frontier | Teach the authored optimal approach, BFS with a Queue — Level by Level (Optimal), inside a fixed tree with an explicit frontier, call stack, and return path; move focus without moving nodes; animate enter, recurse, and return separately. The decisive moment is where information flows upward versus downward. | step into a child or return a value to the parent | 2 |
| Binary Tree Maximum Path Sum | hard | Tree-DP Message Passing | Grid Frontier | Teach the authored optimal approach, Single Post-Order DFS — Gain Up, Split Recorded (Optimal), inside a stable tree where each node sends a compact state vector to its parent; collect child messages, combine them, and optionally reroot the answer. The decisive moment is the minimal information a subtree must expose. | compose child states and compare take/skip or path choices | 3 |
| Binary Tree Right Side View | medium | Tree Traversal Stage | Grid Frontier | Teach the authored optimal approach, DFS Right-First — First Node Per Depth (Optimal Space), inside a fixed tree with an explicit frontier, call stack, and return path; move focus without moving nodes; animate enter, recurse, and return separately. The decisive moment is where information flows upward versus downward. | step into a child or return a value to the parent | 2 |
| Binary Tree Traversal (In/Pre/Post) | easy | Tree Traversal Stage | Grid Frontier | Teach the authored optimal approach, Iterative Inorder with an Explicit Stack (+ Morris), inside a fixed tree with an explicit frontier, call stack, and return path; move focus without moving nodes; animate enter, recurse, and return separately. The decisive moment is where information flows upward versus downward. | step into a child or return a value to the parent | 1 |
| Binary Tree Zigzag Level Order Traversal | medium | Tree Traversal Stage | Grid Frontier | Teach the authored optimal approach, Level-Order BFS Then Reverse Odd Rows, inside a fixed tree with an explicit frontier, call stack, and return path; move focus without moving nodes; animate enter, recurse, and return separately. The decisive moment is where information flows upward versus downward. | step into a child or return a value to the parent | 2 |
| Centroid of a Tree | hard | Tree-DP Message Passing | Grid Frontier | Teach the authored optimal approach, Subtree-Size DFS — Single Pass (Optimal), inside a stable tree where each node sends a compact state vector to its parent; collect child messages, combine them, and optionally reroot the answer. The decisive moment is the minimal information a subtree must expose. | compose child states and compare take/skip or path choices | 3 |
| Construct Binary Tree from Preorder and Inorder Traversal | medium | Tree Construction Workshop | Array + Memory Ledger | Teach the authored optimal approach, Hash Map of Inorder Indices + Preorder Pointer (Optimal), inside traversal sequences beside an incrementally assembled tree; consume the next root and split or attach the remaining region. The decisive moment is which sequence tells root order and which tells subtree boundaries. | place the next node before revealing its legal position | 2 |
| Count Complete Tree Nodes | easy | Tree Traversal Stage | Binary-Search Spotlight | Teach the authored optimal approach, Spine-Height Perfect-Subtree Shortcut (Optimal, O(log² n)), inside a fixed tree with an explicit frontier, call stack, and return path; move focus without moving nodes; animate enter, recurse, and return separately. The decisive moment is where information flows upward versus downward. | step into a child or return a value to the parent | 2 |
| Diameter of Binary Tree | medium | Tree-DP Message Passing | Grid Frontier | Teach the authored optimal approach, Single Post-Order DFS — Height Up, Split Recorded (Optimal), inside a stable tree where each node sends a compact state vector to its parent; collect child messages, combine them, and optionally reroot the answer. The decisive moment is the minimal information a subtree must expose. | compose child states and compare take/skip or path choices | 2 |
| Euler Tour of a Tree (Subtree Flattening) | medium | Tree Flattening Map | Grid Frontier | Teach the authored optimal approach, Euler Tour + Fenwick Tree (Optimal), inside a tree and its Euler-tour array shown side by side; stamp entry/exit times and turn a subtree into a contiguous range. The decisive moment is why hierarchical queries can become array range queries. | select a subtree and see its exact flattened interval | 2 |
| Fenwick Tree (Binary Indexed Tree) | medium | Fenwick Lowbit Ladder | — | Teach the authored optimal approach, Fenwick Tree — lowbit Jumps (Optimal), inside an array, covered ranges, and the implicit BIT parent links; jump by lowbit(i) for updates and strip lowbit(i) for prefix queries. The decisive moment is which range each Fenwick cell owns. | toggle index bits to reveal the next update/query jump | 2 |
| Flatten Binary Tree to Linked List | medium | Tree Traversal Stage | Grid Frontier | Teach the authored optimal approach, Morris-Style O(1) In-Place Stitching, inside a fixed tree with an explicit frontier, call stack, and return path; move focus without moving nodes; animate enter, recurse, and return separately. The decisive moment is where information flows upward versus downward. | step into a child or return a value to the parent | 2 |
| Heavy-Light Decomposition (Path Queries) | hard | Heavy-Light Path Map | Grid Frontier | Teach the authored optimal approach, Heavy-Light Decomposition + Segment Tree (Optimal), inside a tree colored into heavy chains beside its flattened segment-tree lanes; break a path into chain segments and execute one range query per segment. The decisive moment is why every light-edge jump at least doubles subtree size. | select two nodes and watch their path decompose | 3 |
| Invert Binary Tree | easy | Tree Traversal Stage | Grid Frontier | Teach the authored optimal approach, Iterative BFS/DFS — Swap with a Queue or Stack, inside a fixed tree with an explicit frontier, call stack, and return path; move focus without moving nodes; animate enter, recurse, and return separately. The decisive moment is where information flows upward versus downward. | step into a child or return a value to the parent | 2 |
| Is This a Binary Search Tree? | medium | BST Ordered Corridor | Binary-Search Spotlight | Teach the authored optimal approach, Top-Down Bounds — (low, high) Range (Optimal), inside a fixed search tree paired with its persistent inorder strip; project lower/upper bounds through the tree while narrowing the valid corridor. The decisive moment is how the global ordering rule constrains every subtree. | choose a branch and inspect the remaining value bounds | 2 |
| Kth Smallest Element in a BST | medium | BST Ordered Corridor | Grid Frontier | Teach the authored optimal approach, Iterative In-Order with Early Stop (Optimal), inside a fixed search tree paired with its persistent inorder strip; project lower/upper bounds through the tree while narrowing the valid corridor. The decisive moment is how the global ordering rule constrains every subtree. | choose a branch and inspect the remaining value bounds | 2 |
| Lowest Common Ancestor of a Binary Tree | medium | Tree Traversal Stage | Grid Frontier | Teach the authored optimal approach, Parent-Pointer Mapping + Ancestor-Set Intersection, inside a fixed tree with an explicit frontier, call stack, and return path; move focus without moving nodes; animate enter, recurse, and return separately. The decisive moment is where information flows upward versus downward. | step into a child or return a value to the parent | 2 |
| Lowest Common Ancestor of a BST | medium | BST Ordered Corridor | Grid Frontier | Teach the authored optimal approach, BST Descent — Walk to the Split Point (Optimal), inside a fixed search tree paired with its persistent inorder strip; project lower/upper bounds through the tree while narrowing the valid corridor. The decisive moment is how the global ordering rule constrains every subtree. | choose a branch and inspect the remaining value bounds | 2 |
| Lowest Common Ancestor with Binary Lifting | hard | Ancestor Jump Table | Grid Frontier | Teach the authored optimal approach, Binary Lifting — Sparse Ancestor Table (Optimal), inside a tree paired with a powers-of-two ancestor table; decompose a jump into binary leaps and highlight each table lookup. The decisive moment is how repeated doubling replaces linear climbing. | compose a k-step ancestor jump from powers of two | 3 |
| Maximum Depth of Binary Tree | easy | Tree Traversal Stage | Grid Frontier | Teach the authored optimal approach, Iterative BFS — Count the Levels, inside a fixed tree with an explicit frontier, call stack, and return path; move focus without moving nodes; animate enter, recurse, and return separately. The decisive moment is where information flows upward versus downward. | step into a child or return a value to the parent | 2 |
| Maximum Weight Independent Set on a Tree | hard | Tree-DP Message Passing | Grid Frontier | Teach the authored optimal approach, Tree DP — Two States per Node (Optimal), inside a stable tree where each node sends a compact state vector to its parent; collect child messages, combine them, and optionally reroot the answer. The decisive moment is the minimal information a subtree must expose. | compose child states and compare take/skip or path choices | 3 |
| Minimum Absolute Difference in BST | easy | BST Ordered Corridor | Grid Frontier | Teach the authored optimal approach, Iterative In-Order with an Explicit Stack, inside a fixed search tree paired with its persistent inorder strip; project lower/upper bounds through the tree while narrowing the valid corridor. The decisive moment is how the global ordering rule constrains every subtree. | choose a branch and inspect the remaining value bounds | 2 |
| Path Sum | easy | Tree Traversal Stage | Grid Frontier | Teach the authored optimal approach, Iterative DFS with an Explicit Stack, inside a fixed tree with an explicit frontier, call stack, and return path; move focus without moving nodes; animate enter, recurse, and return separately. The decisive moment is where information flows upward versus downward. | step into a child or return a value to the parent | 2 |
| Same Tree | easy | Tree Traversal Stage | Grid Frontier | Teach the authored optimal approach, Iterative — Stack/Queue of Node Pairs, inside a fixed tree with an explicit frontier, call stack, and return path; move focus without moving nodes; animate enter, recurse, and return separately. The decisive moment is where information flows upward versus downward. | step into a child or return a value to the parent | 2 |
| Segment Tree for Range Sum | medium | Segment-Tree Control Room | — | Teach the authored optimal approach, Segment Tree — Recursive Array Implementation (Optimal), inside an array under a hierarchical set of range aggregates; split queries by overlap and propagate updates back to the root. The decisive moment is why only logarithmically many nodes cover a query. | paint a query range and collect the minimal covering nodes | 2 |
| Subtree of Another Tree | easy | Tree Traversal Stage | Grid Frontier | Teach the authored optimal approach, Serialize + KMP Substring Search (Optimal), inside a fixed tree with an explicit frontier, call stack, and return path; move focus without moving nodes; animate enter, recurse, and return separately. The decisive moment is where information flows upward versus downward. | step into a child or return a value to the parent | 2 |
| Sum Root to Leaf Numbers | medium | Tree Traversal Stage | Grid Frontier | Teach the authored optimal approach, Iterative DFS with a Stack of (node, number) Pairs, inside a fixed tree with an explicit frontier, call stack, and return path; move focus without moving nodes; animate enter, recurse, and return separately. The decisive moment is where information flows upward versus downward. | step into a child or return a value to the parent | 2 |
| Swap Nodes [Algo] | medium | Tree Construction Workshop | Grid Frontier | Teach the authored optimal approach, Build + BFS Depth Buckets + Iterative Inorder, inside traversal sequences beside an incrementally assembled tree; consume the next root and split or attach the remaining region. The decisive moment is which sequence tells root order and which tells subtree boundaries. | place the next node before revealing its legal position | 2 |
| Swap Nodes [Algo] | medium | Tree Traversal Stage | Grid Frontier | Teach the authored optimal approach, Build + BFS Depth Buckets + Iterative Inorder, inside a fixed tree with an explicit frontier, call stack, and return path; move focus without moving nodes; animate enter, recurse, and return separately. The decisive moment is where information flows upward versus downward. | step into a child or return a value to the parent | 2 |
| Symmetric Tree | easy | Tree Traversal Stage | Grid Frontier | Teach the authored optimal approach, Iterative BFS with a Queue of Pairs, inside a fixed tree with an explicit frontier, call stack, and return path; move focus without moving nodes; animate enter, recurse, and return separately. The decisive moment is where information flows upward versus downward. | step into a child or return a value to the parent | 2 |
| Tree: Height of a Binary Tree | easy | Tree Traversal Stage | Grid Frontier | Teach the authored optimal approach, Iterative BFS — Count Levels Minus One, inside a fixed tree with an explicit frontier, call stack, and return path; move focus without moving nodes; animate enter, recurse, and return separately. The decisive moment is where information flows upward versus downward. | step into a child or return a value to the parent | 2 |
| Tree: Huffman Decoding | medium | Tree Traversal Stage | Greedy Choice Frontier | Teach the authored optimal approach, Prefix-Table Decode — Build code→char Map First, inside a fixed tree with an explicit frontier, call stack, and return path; move focus without moving nodes; animate enter, recurse, and return separately. The decisive moment is where information flows upward versus downward. | step into a child or return a value to the parent | 2 |
| Tree: Inorder Traversal | easy | Tree Traversal Stage | Grid Frontier | Teach the authored optimal approach, Iterative Inorder — Explicit Stack, inside a fixed tree with an explicit frontier, call stack, and return path; move focus without moving nodes; animate enter, recurse, and return separately. The decisive moment is where information flows upward versus downward. | step into a child or return a value to the parent | 2 |
| Tree: Level Order Traversal | easy | Tree Traversal Stage | Grid Frontier | Teach the authored optimal approach, BFS Grouped by Level — Size Snapshot, inside a fixed tree with an explicit frontier, call stack, and return path; move focus without moving nodes; animate enter, recurse, and return separately. The decisive moment is where information flows upward versus downward. | step into a child or return a value to the parent | 2 |
| Tree: Postorder Traversal | easy | Tree Traversal Stage | Grid Frontier | Teach the authored optimal approach, Iterative Postorder — Two Stacks (reverse of Node-Right-Left), inside a fixed tree with an explicit frontier, call stack, and return path; move focus without moving nodes; animate enter, recurse, and return separately. The decisive moment is where information flows upward versus downward. | step into a child or return a value to the parent | 2 |
| Tree: Preorder Traversal | easy | Tree Traversal Stage | Grid Frontier | Teach the authored optimal approach, Iterative Preorder — Explicit Stack, inside a fixed tree with an explicit frontier, call stack, and return path; move focus without moving nodes; animate enter, recurse, and return separately. The decisive moment is where information flows upward versus downward. | step into a child or return a value to the parent | 2 |
| Validate Binary Search Tree | medium | BST Ordered Corridor | Grid Frontier | Teach the authored optimal approach, Inorder Traversal — Strictly Increasing (Alternative), inside a fixed search tree paired with its persistent inorder strip; project lower/upper bounds through the tree while narrowing the valid corridor. The decisive moment is how the global ordering rule constrains every subtree. | choose a branch and inspect the remaining value bounds | 2 |

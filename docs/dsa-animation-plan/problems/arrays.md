# Arrays animation plan

67 canonical problems from `content/dsa/_index.json`. Each row names the primary reusable engine and the concrete explanation direction. Full storyboards and readiness metadata live in `../problem-animation-matrix.json`.

| Problem | Difficulty | Primary engine | Supporting engine | How we explain it | Learner action | Wave |
| --- | --- | --- | --- | --- | --- | --- |
| 2D Array - DS | medium | Sliding-Window Lens | — | Teach the authored optimal approach, Constant Pass — Closed-Form Over 16 Hourglasses (Optimal), inside a translucent window over a string or array with a persistent validity ledger; expand to acquire information and contract only when the invariant permits. The decisive moment is why each element enters and leaves at most once. | drag the right edge and decide when the left edge must move | 2 |
| 3Sum | medium | Two-Pointer Stage | Merge-Sort Studio | Teach the authored optimal approach, Sort + Two Pointers (Optimal), inside a stable sequence with independently controlled left/right or slow/fast actors; move only the pointer justified by the invariant and dim the discarded region. The decisive moment is why one move safely removes many impossible candidates. | choose which pointer moves before revealing the rule | 2 |
| Array Manipulation | medium | Difference-Array Wave | — | Teach the authored optimal approach, Difference Array + Prefix Sum (Optimal), inside range updates as start/end impulses followed by a reconstruction sweep; drop boundary deltas, then roll a cumulative wave across the final array. The decisive moment is why two writes can represent an entire range update. | paint a range and inspect the two delta cells it changes | 2 |
| Arrays: DS (Reverse an Array) | easy | Two-Pointer Stage | — | Teach the authored optimal approach, Two Pointers — In-Place Swap (Optimal), inside a stable sequence with independently controlled left/right or slow/fast actors; move only the pointer justified by the invariant and dim the discarded region. The decisive moment is why one move safely removes many impossible candidates. | choose which pointer moves before revealing the rule | 2 |
| Arrays: Left Rotation | easy | Two-Pointer Stage | — | Teach the authored optimal approach, Three Reversals — In-Place Rotation (Optimal, O(1) space), inside a stable sequence with independently controlled left/right or slow/fast actors; move only the pointer justified by the invariant and dim the discarded region. The decisive moment is why one move safely removes many impossible candidates. | choose which pointer moves before revealing the rule | 2 |
| Best Time to Buy and Sell Stock | easy | Sliding-Window Lens | Greedy Choice Frontier | Teach the authored optimal approach, One Pass — Min Price So Far (Optimal), inside a translucent window over a string or array with a persistent validity ledger; expand to acquire information and contract only when the invariant permits. The decisive moment is why each element enters and leaves at most once. | drag the right edge and decide when the left edge must move | 2 |
| Best Time to Buy and Sell Stock II | medium | 1D DP State Conveyor | Greedy Choice Frontier | Teach the authored optimal approach, DP State Machine — Hold vs Cash, inside state cells ordered by subproblem size with dependency arrows; compare choices, compute the recurrence, and commit one new state. The decisive moment is the smallest state that contains everything the future needs. | choose the recurrence inputs and commit the next cell | 2 |
| Bill Division (Bon Appétit) | easy | Math Derivation Canvas | — | Teach the authored optimal approach, Total Minus Item — Branchless Sum (Optimal), inside a concrete example beside a symbolic equation and geometric/counting model; morph repeated work into the closed form or recurrence. The decisive moment is where each term of the formula comes from. | change the input and verify the derivation still holds | 2 |
| Candy | hard | Greedy Choice Frontier | — | Teach the authored optimal approach, One Pass, O(1) Space (Slope Counting), inside sorted candidates approaching a best-so-far frontier; accept or reject each local choice while the safety invariant remains visible. The decisive moment is the exchange argument that proves the choice can be part of an optimum. | make the local choice before the proof is revealed | 3 |
| Closest Numbers | easy | Merge-Sort Studio | — | Teach the authored optimal approach, Sort + Adjacent Scan (Optimal), inside a stable array split into recursive lanes with a merge workbench; split ranges, compare two front elements, and stitch the sorted result. The decisive moment is where cross-half inversions are counted during merge. | choose the next merge element and update the counter | 2 |
| Container With Most Water | medium | Two-Pointer Stage | — | Teach the authored optimal approach, Two Pointers — Move the Shorter Wall (Optimal), inside a stable sequence with independently controlled left/right or slow/fast actors; move only the pointer justified by the invariant and dim the discarded region. The decisive moment is why one move safely removes many impossible candidates. | choose which pointer moves before revealing the rule | 2 |
| Contains Duplicate | easy | Array + Memory Ledger | — | Teach the authored optimal approach, Hash Set — One Pass (Optimal), inside stable array cells beside a live hash-map, set, or frequency ledger; move the cursor one cell at a time; animate lookup first and mutation second. The decisive moment is what remembering earlier values removes from the future search. | predict lookup result, then reveal the map mutation | 2 |
| Contains Duplicate II | easy | Sliding-Window Lens | Array + Memory Ledger | Teach the authored optimal approach, Sliding Window — Set of the Last k Values, inside a translucent window over a string or array with a persistent validity ledger; expand to acquire information and contract only when the invariant permits. The decisive moment is why each element enters and leaves at most once. | drag the right edge and decide when the left edge must move | 2 |
| Count Inversions | medium | Merge-Sort Studio | — | Teach the authored optimal approach, Modified Merge Sort (Optimal), inside a stable array split into recursive lanes with a merge workbench; split ranges, compare two front elements, and stitch the sorted result. The decisive moment is where cross-half inversions are counted during merge. | choose the next merge element and update the counter | 1 |
| Count Inversions in an Array | medium | Merge-Sort Studio | — | Teach the authored optimal approach, Modified Merge Sort (Optimal), inside a stable array split into recursive lanes with a merge workbench; split ranges, compare two front elements, and stitch the sorted result. The decisive moment is where cross-half inversions are counted during merge. | choose the next merge element and update the counter | 2 |
| Count Pairs with given sum | easy | Array + Memory Ledger | — | Teach the authored optimal approach, Frequency Map — One Pass (Optimal), inside stable array cells beside a live hash-map, set, or frequency ledger; move the cursor one cell at a time; animate lookup first and mutation second. The decisive moment is what remembering earlier values removes from the future search. | predict lookup result, then reveal the map mutation | 2 |
| Count Triplets | medium | Array + Memory Ledger | — | Teach the authored optimal approach, Two Frequency Maps — Fix the Middle (Optimal), inside stable array cells beside a live hash-map, set, or frequency ledger; move the cursor one cell at a time; animate lookup first and mutation second. The decisive moment is what remembering earlier values removes from the future search. | predict lookup result, then reveal the map mutation | 2 |
| Counting Sort 1 | easy | Counting-Bucket Factory | Array + Memory Ledger | Teach the authored optimal approach, Frequency Array (Bucket Tally), inside input tokens feeding frequency buckets and a stable output conveyor; count, prefix positions when needed, then emit values in order. The decisive moment is how a bounded key range replaces comparison sorting. | drop the next token into its bucket and rebuild the output | 2 |
| Counting Sort 2 | easy | Counting-Bucket Factory | Array + Memory Ledger | Teach the authored optimal approach, Stable Placement via Prefix Sums, inside input tokens feeding frequency buckets and a stable output conveyor; count, prefix positions when needed, then emit values in order. The decisive moment is how a bounded key range replaces comparison sorting. | drop the next token into its bucket and rebuild the output | 2 |
| Divisible Sum Pairs | easy | Number-Theory Workbench | Array + Memory Ledger | Teach the authored optimal approach, Remainder Buckets — One-Pass Complement Count (Optimal), inside numbers decomposed into factors, residues, or recurrence states; apply one algebraic reduction while keeping the invariant equation visible. The decisive moment is the identity that turns a huge numeric task into repeated smaller ones. | perform the next reduction and verify the invariant | 2 |
| Dynamic Array | easy | Simulation State Machine | — | Teach the authored optimal approach, Array of Lists — Direct Simulation (Optimal), inside the problem world beside explicit control state and an event timeline; consume one event, apply the rule, and snapshot the resulting state. The decisive moment is the compact state that makes every transition deterministic. | trigger the next event and predict the transition | 2 |
| Equilibrium Index | easy | Prefix-Sum Balance | — | Teach the authored optimal approach, Total Minus Running Prefix (Optimal), inside the original values and a second cumulative track aligned above them; grow the prefix total and connect two prefixes whose difference answers the query. The decisive moment is how a range becomes one subtraction or complement lookup. | select a range and watch its two boundary prefixes light up | 2 |
| Find the Duplicate Number | medium | Two-Pointer Stage | — | Teach the authored optimal approach, Floyd's Cycle Detection on the Array (Optimal), inside a stable sequence with independently controlled left/right or slow/fast actors; move only the pointer justified by the invariant and dim the discarded region. The decisive moment is why one move safely removes many impossible candidates. | choose which pointer moves before revealing the rule | 2 |
| Fraudulent Activity Notifications | medium | Sliding-Window Lens | Two-Pointer Stage | Teach the authored optimal approach, Counting-Sort Frequency Window (Optimal), inside a translucent window over a string or array with a persistent validity ledger; expand to acquire information and contract only when the invariant permits. The decisive moment is why each element enters and leaves at most once. | drag the right edge and decide when the left edge must move | 2 |
| Frequency Queries | medium | Array + Memory Ledger | — | Teach the authored optimal approach, Two Hash Maps — Frequency of Frequencies (Optimal), inside stable array cells beside a live hash-map, set, or frequency ledger; move the cursor one cell at a time; animate lookup first and mutation second. The decisive moment is what remembering earlier values removes from the future search. | predict lookup result, then reveal the map mutation | 2 |
| Gridland Metro | medium | Interval Timeline | Array + Memory Ledger | Teach the authored optimal approach, Group by Row + Merge Intervals (Optimal), inside interval bars on a shared time axis with endpoints and active resources visible; sort, sweep, merge, allocate, or discard at endpoint events. The decisive moment is which endpoint rule captures overlap and compatibility. | drag an interval onto the timeline and decide merge/room/reject | 2 |
| H-Index | medium | Array + Memory Ledger | Merge-Sort Studio | Teach the authored optimal approach, Counting Buckets (Optimal O(n)), inside stable array cells beside a live hash-map, set, or frequency ledger; move the cursor one cell at a time; animate lookup first and mutation second. The decisive moment is what remembering earlier values removes from the future search. | predict lookup result, then reveal the map mutation | 2 |
| Hash Tables: Ice Cream Parlor | easy | Array + Memory Ledger | — | Teach the authored optimal approach, Complement Hash Map — One Pass (Optimal), inside stable array cells beside a live hash-map, set, or frequency ledger; move the cursor one cell at a time; animate lookup first and mutation second. The decisive moment is what remembering earlier values removes from the future search. | predict lookup result, then reveal the map mutation | 2 |
| Ice Cream Parlor | easy | Array + Memory Ledger | — | Teach the authored optimal approach, Complement Hash Map — One Pass (Optimal), inside stable array cells beside a live hash-map, set, or frequency ledger; move the cursor one cell at a time; animate lookup first and mutation second. The decisive moment is what remembering earlier values removes from the future search. | predict lookup result, then reveal the map mutation | 2 |
| Insert Delete GetRandom O(1) | medium | Coordinated Data-Structure Design | Array + Memory Ledger | Teach the authored optimal approach, Dynamic Array + value→index Hash Map (Optimal), inside two synchronized representations such as a map plus list or array plus index map; run one public operation as a transaction across both structures. The decisive moment is the invariant that keeps O(1) operations correct. | call the API and verify both structures after every mutation | 2 |
| Jumping on the Clouds | easy | 1D DP State Conveyor | Greedy Choice Frontier | Teach the authored optimal approach, Dynamic Programming — Minimum Jumps Table, inside state cells ordered by subproblem size with dependency arrows; compare choices, compute the recurrence, and commit one new state. The decisive moment is the smallest state that contains everything the future needs. | choose the recurrence inputs and commit the next cell | 2 |
| Kth Largest Element via Quickselect | medium | Heap Arena | — | Teach the authored optimal approach, Quickselect — Hoare/Lomuto Partition (Optimal), inside a heap tree synchronized with its array and candidate queue; insert/extract at the root and show every swap that repairs the invariant. The decisive moment is why only the best candidate needs to be immediately accessible. | choose the next candidate and repair the heap | 2 |
| Longest Consecutive Sequence | medium | Array + Memory Ledger | — | Teach the authored optimal approach, Hash Set — Start-Only Walk (Optimal), inside stable array cells beside a live hash-map, set, or frequency ledger; move the cursor one cell at a time; animate lookup first and mutation second. The decisive moment is what remembering earlier values removes from the future search. | predict lookup result, then reveal the map mutation | 2 |
| Majority Element | easy | Candidate Cancellation | Array + Memory Ledger | Teach the authored optimal approach, Boyer-Moore Voting (Optimal), inside a current candidate on a podium beside a signed vote balance and uncancelled tokens; pair unlike values as cancellations, replace the candidate only when the balance reaches zero. The decisive moment is why cancelling different pairs cannot eliminate a true strict majority. | accept or cancel the next token and predict the surviving candidate | 2 |
| Maximum Hourglass Sum | medium | Sliding-Window Lens | — | Teach the authored optimal approach, Direct Mask Sum — Sliding Hourglass (Optimal), inside a translucent window over a string or array with a persistent validity ledger; expand to acquire information and contract only when the invariant permits. The decisive moment is why each element enters and leaves at most once. | drag the right edge and decide when the left edge must move | 2 |
| Maximum Subarray (Kadane's) | medium | 1D DP State Conveyor | Greedy Choice Frontier | Teach the authored optimal approach, Kadane's Algorithm — One-Pass Running Sum (Optimal), inside state cells ordered by subproblem size with dependency arrows; compare choices, compute the recurrence, and commit one new state. The decisive moment is the smallest state that contains everything the future needs. | choose the recurrence inputs and commit the next cell | 2 |
| Maximum Subarray Sum | hard | Prefix-Sum Balance | Binary-Search Spotlight | Teach the authored optimal approach, Prefix Sums + Ordered Set (Optimal), inside the original values and a second cumulative track aligned above them; grow the prefix total and connect two prefixes whose difference answers the query. The decisive moment is how a range becomes one subtraction or complement lookup. | select a range and watch its two boundary prefixes light up | 3 |
| Merge Sort: Counting Inversions | hard | Merge-Sort Studio | — | Teach the authored optimal approach, Merge Sort with Inversion Counting (Optimal), inside a stable array split into recursive lanes with a merge workbench; split ranges, compare two front elements, and stitch the sorted result. The decisive moment is where cross-half inversions are counted during merge. | choose the next merge element and update the counter | 3 |
| Merge Sorted Array | easy | Two-Pointer Stage | — | Teach the authored optimal approach, Three Pointers, Back to Front (Optimal), inside a stable sequence with independently controlled left/right or slow/fast actors; move only the pointer justified by the invariant and dim the discarded region. The decisive moment is why one move safely removes many impossible candidates. | choose which pointer moves before revealing the rule | 2 |
| Migratory Birds | easy | Counting-Bucket Factory | Array + Memory Ledger | Teach the authored optimal approach, Frequency Array — One-Pass Count + Argmax (Optimal), inside input tokens feeding frequency buckets and a stable output conveyor; count, prefix positions when needed, then emit values in order. The decisive moment is how a bounded key range replaces comparison sorting. | drop the next token into its bucket and rebuild the output | 2 |
| Minimum Absolute Difference in an Array | easy | Greedy Choice Frontier | Merge-Sort Studio | Teach the authored optimal approach, Sort + Adjacent Scan (Optimal), inside sorted candidates approaching a best-so-far frontier; accept or reject each local choice while the safety invariant remains visible. The decisive moment is the exchange argument that proves the choice can be part of an optimum. | make the local choice before the proof is revealed | 2 |
| Minimum Loss | medium | Greedy Choice Frontier | Merge-Sort Studio | Teach the authored optimal approach, Sort by Price + Adjacent Scan (Optimal), inside sorted candidates approaching a best-so-far frontier; accept or reject each local choice while the safety invariant remains visible. The decisive moment is the exchange argument that proves the choice can be part of an optimum. | make the local choice before the proof is revealed | 2 |
| Minimum Size Subarray Sum | medium | Sliding-Window Lens | Two-Pointer Stage | Teach the authored optimal approach, Variable-Size Sliding Window (Optimal), inside a translucent window over a string or array with a persistent validity ledger; expand to acquire information and contract only when the invariant permits. The decisive moment is why each element enters and leaves at most once. | drag the right edge and decide when the left edge must move | 2 |
| Minimum Swaps 2 | medium | Simulation State Machine | — | Teach the authored optimal approach, Cycle Decomposition (Optimal), inside the problem world beside explicit control state and an event timeline; consume one event, apply the rule, and snapshot the resulting state. The decisive moment is the compact state that makes every transition deterministic. | trigger the next event and predict the transition | 2 |
| Missing Numbers | medium | Array + Memory Ledger | — | Teach the authored optimal approach, Frequency-Count Difference (Optimal), inside stable array cells beside a live hash-map, set, or frequency ledger; move the cursor one cell at a time; animate lookup first and mutation second. The decisive moment is what remembering earlier values removes from the future search. | predict lookup result, then reveal the map mutation | 2 |
| Move Zeroes | easy | Two-Pointer Stage | — | Teach the authored optimal approach, Write Pointer with Swap (Optimal), inside a stable sequence with independently controlled left/right or slow/fast actors; move only the pointer justified by the invariant and dim the discarded region. The decisive moment is why one move safely removes many impossible candidates. | choose which pointer moves before revealing the rule | 2 |
| New Year Chaos | medium | Merge-Sort Studio | Greedy Choice Frontier | Teach the authored optimal approach, Windowed Count — Greedy Bound (Optimal), inside a stable array split into recursive lanes with a merge workbench; split ranges, compare two front elements, and stitch the sorted result. The decisive moment is where cross-half inversions are counted during merge. | choose the next merge element and update the counter | 2 |
| Picking Numbers | easy | Counting-Bucket Factory | Array + Memory Ledger | Teach the authored optimal approach, Frequency Array — Max Adjacent Bucket Sum (Optimal), inside input tokens feeding frequency buckets and a stable output conveyor; count, prefix positions when needed, then emit values in order. The decisive moment is how a bounded key range replaces comparison sorting. | drop the next token into its bucket and rebuild the output | 2 |
| Product of Array Except Self | medium | Prefix-Sum Balance | — | Teach the authored optimal approach, Prefix × Suffix in One Array (Optimal), inside the original values and a second cumulative track aligned above them; grow the prefix total and connect two prefixes whose difference answers the query. The decisive moment is how a range becomes one subtraction or complement lookup. | select a range and watch its two boundary prefixes light up | 1 |
| Quicksort 1 - Partition | easy | Partition / Selection Workbench | — | Teach the authored optimal approach, In-Place Lomuto Partition (Space-Optimised), inside an array with pivot, less/equal/greater regions, and swap arcs; classify one value at a time and grow the pivot's final region. The decisive moment is why one partition can discard an entire side for selection. | classify the focused value and perform the swap | 2 |
| Quicksort 2 - Sorting (Partition) | medium | Partition / Selection Workbench | Merge-Sort Studio | Teach the authored optimal approach, In-Place Lomuto Quicksort (Space-Optimised), inside an array with pivot, less/equal/greater regions, and swap arcs; classify one value at a time and grow the pivot's final region. The decisive moment is why one partition can discard an entire side for selection. | classify the focused value and perform the swap | 2 |
| Remove Duplicates from Sorted Array | easy | Two-Pointer Stage | — | Teach the authored optimal approach, Slow/Fast Two Pointers (Optimal), inside a stable sequence with independently controlled left/right or slow/fast actors; move only the pointer justified by the invariant and dim the discarded region. The decisive moment is why one move safely removes many impossible candidates. | choose which pointer moves before revealing the rule | 2 |
| Remove Duplicates from Sorted Array II | medium | Two-Pointer Stage | — | Teach the authored optimal approach, Write Pointer with Look-Back-by-2 (Optimal), inside a stable sequence with independently controlled left/right or slow/fast actors; move only the pointer justified by the invariant and dim the discarded region. The decisive moment is why one move safely removes many impossible candidates. | choose which pointer moves before revealing the rule | 2 |
| Remove Element | easy | Two-Pointer Stage | — | Teach the authored optimal approach, Swap With Last Element (Write-Minimizing), inside a stable sequence with independently controlled left/right or slow/fast actors; move only the pointer justified by the invariant and dim the discarded region. The decisive moment is why one move safely removes many impossible candidates. | choose which pointer moves before revealing the rule | 2 |
| Rotate Array | medium | Two-Pointer Stage | — | Teach the authored optimal approach, Three Reversals (Optimal, O(1) Space), inside a stable sequence with independently controlled left/right or slow/fast actors; move only the pointer justified by the invariant and dim the discarded region. The decisive moment is why one move safely removes many impossible candidates. | choose which pointer moves before revealing the rule | 2 |
| Sherlock and Array | easy | Prefix-Sum Balance | Two-Pointer Stage | Teach the authored optimal approach, Running Prefix Sum (Optimal), inside the original values and a second cumulative track aligned above them; grow the prefix total and connect two prefixes whose difference answers the query. The decisive moment is how a range becomes one subtraction or complement lookup. | select a range and watch its two boundary prefixes light up | 2 |
| Sock Merchant | easy | Array + Memory Ledger | — | Teach the authored optimal approach, Fixed-Size Count Array, inside stable array cells beside a live hash-map, set, or frequency ledger; move the cursor one cell at a time; animate lookup first and mutation second. The decisive moment is what remembering earlier values removes from the future search. | predict lookup result, then reveal the map mutation | 2 |
| Sorting: Bubble Sort | medium | Merge-Sort Studio | — | Teach the authored optimal approach, Count Inversions via Merge Sort (Optimal), inside a stable array split into recursive lanes with a merge workbench; split ranges, compare two front elements, and stitch the sorted result. The decisive moment is where cross-half inversions are counted during merge. | choose the next merge element and update the counter | 2 |
| Squares of a Sorted Array | easy | Two-Pointer Stage | — | Teach the authored optimal approach, Two Pointers Filling Back-to-Front (Optimal), inside a stable sequence with independently controlled left/right or slow/fast actors; move only the pointer justified by the invariant and dim the discarded region. The decisive moment is why one move safely removes many impossible candidates. | choose which pointer moves before revealing the rule | 2 |
| Stock Span | medium | Monotonic Skyline | — | Teach the authored optimal approach, Monotonic Decreasing Stack (Optimal), inside bars on a skyline beside a stack of still-useful candidates; let a stronger bar visibly evict dominated candidates. The decisive moment is why every popped item can never help a future answer. | predict how many candidates the new value will pop | 2 |
| Subarray Division (Lily's Chocolate) | easy | Prefix-Sum Balance | — | Teach the authored optimal approach, Fixed-Size Sliding Window — Running Sum (Optimal), inside the original values and a second cumulative track aligned above them; grow the prefix total and connect two prefixes whose difference answers the query. The decisive moment is how a range becomes one subtraction or complement lookup. | select a range and watch its two boundary prefixes light up | 2 |
| The Full Counting Sort | medium | Counting-Bucket Factory | — | Teach the authored optimal approach, Prefix-Sum Stable Placement, inside input tokens feeding frequency buckets and a stable output conveyor; count, prefix positions when needed, then emit values in order. The decisive moment is how a bounded key range replaces comparison sorting. | drop the next token into its bucket and rebuild the output | 2 |
| Trapping Rain Water | hard | Two-Pointer Stage | 1D DP State Conveyor | Teach the authored optimal approach, Two Pointers — Running Maxes (Optimal), inside a stable sequence with independently controlled left/right or slow/fast actors; move only the pointer justified by the invariant and dim the discarded region. The decisive moment is why one move safely removes many impossible candidates. | choose which pointer moves before revealing the rule | 3 |
| Triple Sum | medium | Binary-Search Spotlight | Merge-Sort Studio | Teach the authored optimal approach, Sort + Binary-Search Counting (Optimal), inside an ordered search space with low, mid, and high markers; collapse the surviving interval while leaving rejected space visible but muted. The decisive moment is the predicate that makes half the search space impossible. | vote left/right/found before the interval collapses | 2 |
| Two Sum | easy | Array + Memory Ledger | — | Teach the authored optimal approach, Hash Map — One Pass (Optimal), inside stable array cells beside a live hash-map, set, or frequency ledger; move the cursor one cell at a time; animate lookup first and mutation second. The decisive moment is what remembering earlier values removes from the future search. | predict lookup result, then reveal the map mutation | 1 |
| Two Sum II — Input Array Is Sorted | medium | Binary-Search Spotlight | Two-Pointer Stage | Teach the authored optimal approach, Opposite-End Two Pointers (Optimal), inside an ordered search space with low, mid, and high markers; collapse the surviving interval while leaving rejected space visible but muted. The decisive moment is the predicate that makes half the search space impossible. | vote left/right/found before the interval collapses | 2 |
| Valid Sudoku | medium | Constraint Board | Array + Memory Ledger | Teach the authored optimal approach, Single Pass — Row/Col/Box Sets (Optimal), inside a board with candidate sets and row/column/region constraints; remove candidates as constraints propagate, then branch only when needed. The decisive moment is which constraint causes the decisive prune. | place a candidate and inspect all propagated consequences | 2 |

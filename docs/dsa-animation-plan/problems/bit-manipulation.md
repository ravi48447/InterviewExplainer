# Bit Manipulation animation plan

20 canonical problems from `content/dsa/_index.json`. Each row names the primary reusable engine and the concrete explanation direction. Full storyboards and readiness metadata live in `../problem-animation-matrix.json`.

| Problem | Difficulty | Primary engine | Supporting engine | How we explain it | Learner action | Wave |
| --- | --- | --- | --- | --- | --- | --- |
| Add Binary | easy | Bitwise Circuit | — | Teach the authored optimal approach, Right-to-Left Carry Simulation (Optimal), inside aligned bit lanes flowing through XOR, AND, shift, and carry gates; transform one bit position at a time while decimal values stay visible. The decisive moment is the bit identity that removes branching or extra memory. | toggle input bits and predict the gate output | 2 |
| Bitwise AND of Numbers Range | medium | Bitwise Circuit | — | Teach the authored optimal approach, Brian Kernighan — Strip Right's Low Set Bits, inside aligned bit lanes flowing through XOR, AND, shift, and carry gates; transform one bit position at a time while decimal values stay visible. The decisive moment is the bit identity that removes branching or extra memory. | toggle input bits and predict the gate output | 2 |
| Cipher | medium | Sliding-Window Lens | — | Teach the authored optimal approach, Telescoping Recurrence — Adjacent-Window Difference (Optimal), inside a translucent window over a string or array with a persistent validity ledger; expand to acquire information and contract only when the invariant permits. The decisive moment is why each element enters and leaves at most once. | drag the right edge and decide when the left edge must move | 2 |
| Counter Game | medium | Bitwise Circuit | — | Teach the authored optimal approach, Bit Formula — Popcount and Trailing Zeros (Optimal), inside aligned bit lanes flowing through XOR, AND, shift, and carry gates; transform one bit position at a time while decimal values stay visible. The decisive moment is the bit identity that removes branching or extra memory. | toggle input bits and predict the gate output | 2 |
| Counting Bits | easy | 1D DP State Conveyor | — | Teach the authored optimal approach, DP — Reuse the Right-Shifted Result (Optimal), inside state cells ordered by subproblem size with dependency arrows; compare choices, compute the recurrence, and commit one new state. The decisive moment is the smallest state that contains everything the future needs. | choose the recurrence inputs and commit the next cell | 2 |
| Flipping Bits | easy | Bitwise Circuit | — | Teach the authored optimal approach, XOR with All-Ones Mask (Optimal), inside aligned bit lanes flowing through XOR, AND, shift, and carry gates; transform one bit position at a time while decimal values stay visible. The decisive moment is the bit identity that removes branching or extra memory. | toggle input bits and predict the gate output | 2 |
| Gray Code Generation | medium | Bitwise Circuit | — | Teach the authored optimal approach, Direct Formula — i ^ (i >> 1) (Optimal), inside aligned bit lanes flowing through XOR, AND, shift, and carry gates; transform one bit position at a time while decimal values stay visible. The decisive moment is the bit identity that removes branching or extra memory. | toggle input bits and predict the gate output | 2 |
| Lonely Integer | easy | Bitwise Circuit | — | Teach the authored optimal approach, XOR Fold — Pairs Cancel (Optimal), inside aligned bit lanes flowing through XOR, AND, shift, and carry gates; transform one bit position at a time while decimal values stay visible. The decisive moment is the bit identity that removes branching or extra memory. | toggle input bits and predict the gate output | 2 |
| Maximizing XOR | easy | Bitwise Circuit | — | Teach the authored optimal approach, Leading Differing Bit — Closed Form (Optimal), inside aligned bit lanes flowing through XOR, AND, shift, and carry gates; transform one bit position at a time while decimal values stay visible. The decisive moment is the bit identity that removes branching or extra memory. | toggle input bits and predict the gate output | 2 |
| Maximum Xor | medium | Trie Pathfinder | — | Teach the authored optimal approach, Binary Trie — Greedy Opposite-Bit Walk (Optimal), inside a prefix tree with shared prefixes kept visible; light one character edge at a time and prune dead branches when justified. The decisive moment is how shared prefixes eliminate repeated search work. | type a prefix and follow or backtrack the matching path | 2 |
| Missing Number | easy | Bitwise Circuit | — | Teach the authored optimal approach, XOR Indices With Values (Optimal), inside aligned bit lanes flowing through XOR, AND, shift, and carry gates; transform one bit position at a time while decimal values stay visible. The decisive moment is the bit identity that removes branching or extra memory. | toggle input bits and predict the gate output | 2 |
| Number of 1 Bits | easy | Bitwise Circuit | — | Teach the authored optimal approach, Brian Kernighan — Clear the Lowest Set Bit (Optimal), inside aligned bit lanes flowing through XOR, AND, shift, and carry gates; transform one bit position at a time while decimal values stay visible. The decisive moment is the bit identity that removes branching or extra memory. | toggle input bits and predict the gate output | 2 |
| Power of Two | easy | Bitwise Circuit | — | Teach the authored optimal approach, Clear Lowest Set Bit — n & (n - 1) (Optimal), inside aligned bit lanes flowing through XOR, AND, shift, and carry gates; transform one bit position at a time while decimal values stay visible. The decisive moment is the bit identity that removes branching or extra memory. | toggle input bits and predict the gate output | 2 |
| Reverse Bits | easy | Bitwise Circuit | — | Teach the authored optimal approach, Byte Cache — Reverse and Reorder Bytes (Optimal for Repeated Calls), inside aligned bit lanes flowing through XOR, AND, shift, and carry gates; transform one bit position at a time while decimal values stay visible. The decisive moment is the bit identity that removes branching or extra memory. | toggle input bits and predict the gate output | 2 |
| Sansa and XOR | easy | Bitwise Circuit | — | Teach the authored optimal approach, Parity Count — XOR Even Indices When n Is Odd (Optimal), inside aligned bit lanes flowing through XOR, AND, shift, and carry gates; transform one bit position at a time while decimal values stay visible. The decisive moment is the bit identity that removes branching or extra memory. | toggle input bits and predict the gate output | 2 |
| Single Number | easy | Bitwise Circuit | — | Teach the authored optimal approach, XOR All Elements (Optimal), inside aligned bit lanes flowing through XOR, AND, shift, and carry gates; transform one bit position at a time while decimal values stay visible. The decisive moment is the bit identity that removes branching or extra memory. | toggle input bits and predict the gate output | 1 |
| Sum of All Subset XOR Totals | medium | Bitwise Circuit | — | Teach the authored optimal approach, Per-Bit Counting — OR × 2ⁿ⁻¹ (Optimal), inside aligned bit lanes flowing through XOR, AND, shift, and carry gates; transform one bit position at a time while decimal values stay visible. The decisive moment is the bit identity that removes branching or extra memory. | toggle input bits and predict the gate output | 2 |
| Sum of Two Integers | medium | Bitwise Circuit | — | Teach the authored optimal approach, XOR Sum + Carry Loop (Optimal), inside aligned bit lanes flowing through XOR, AND, shift, and carry gates; transform one bit position at a time while decimal values stay visible. The decisive moment is the bit identity that removes branching or extra memory. | toggle input bits and predict the gate output | 2 |
| Sum Over Subsets (SOS DP) | hard | Bitmask State Cube | 1D DP State Conveyor | Teach the authored optimal approach, Sum Over Subsets DP — Bit by Bit (Optimal), inside subset states as lit bits linked to reachable next masks; toggle one choice bit and propagate values across the subset lattice. The decisive moment is how one integer represents an entire chosen set. | toggle a bit and inspect newly reachable states | 3 |
| Sum vs XOR | easy | Bitwise Circuit | — | Teach the authored optimal approach, Count Zero Bits — 2^zeros (Optimal), inside aligned bit lanes flowing through XOR, AND, shift, and carry gates; transform one bit position at a time while decimal values stay visible. The decisive moment is the bit identity that removes branching or extra memory. | toggle input bits and predict the gate output | 2 |

# Stack Queue animation plan

17 canonical problems from `content/dsa/_index.json`. Each row names the primary reusable engine and the concrete explanation direction. Full storyboards and readiness metadata live in `../problem-animation-matrix.json`.

| Problem | Difficulty | Primary engine | Supporting engine | How we explain it | Learner action | Wave |
| --- | --- | --- | --- | --- | --- | --- |
| Balanced Brackets | easy | Stack Theatre | — | Teach the authored optimal approach, Stack — One Pass (Optimal), inside tokens entering a visible vertical stack with the top always explicit; push, compare, and pop one causal event at a time. The decisive moment is which unresolved work must be remembered in LIFO order. | choose push/pop before the operation executes | 2 |
| Basic Calculator | hard | Stack Theatre | — | Teach the authored optimal approach, Single Pass — Running Result, Sign, and a Context Stack (Optimal), inside tokens entering a visible vertical stack with the top always explicit; push, compare, and pop one causal event at a time. The decisive moment is which unresolved work must be remembered in LIFO order. | choose push/pop before the operation executes | 3 |
| Daily Temperatures | medium | Monotonic Skyline | — | Teach the authored optimal approach, Monotonic Decreasing Stack (Optimal), inside bars on a skyline beside a stack of still-useful candidates; let a stronger bar visibly evict dominated candidates. The decisive moment is why every popped item can never help a future answer. | predict how many candidates the new value will pop | 1 |
| Equal Stacks | medium | Stack Theatre | Array + Memory Ledger | Teach the authored optimal approach, Suffix Sums + Lockstep Reduction (Optimal), inside tokens entering a visible vertical stack with the top always explicit; push, compare, and pop one causal event at a time. The decisive moment is which unresolved work must be remembered in LIFO order. | choose push/pop before the operation executes | 2 |
| Evaluate Reverse Polish Notation | medium | Stack Theatre | — | Teach the authored optimal approach, Stack of Operands (Optimal), inside tokens entering a visible vertical stack with the top always explicit; push, compare, and pop one causal event at a time. The decisive moment is which unresolved work must be remembered in LIFO order. | choose push/pop before the operation executes | 2 |
| Generate Parentheses | medium | Decision-Tree Backtracker | — | Teach the authored optimal approach, Backtracking with Count Pruning (Optimal), inside the concrete board/string plus a parallel choose-recurse-undo tree; commit one choice, descend, hit success/dead-end, then visibly undo. The decisive moment is the constraint that prunes an entire branch. | choose the next candidate and predict whether it survives | 2 |
| Largest Rectangle in Histogram | hard | Monotonic Skyline | — | Teach the authored optimal approach, Monotonic Increasing Stack (Optimal), inside bars on a skyline beside a stack of still-useful candidates; let a stronger bar visibly evict dominated candidates. The decisive moment is why every popped item can never help a future answer. | predict how many candidates the new value will pop | 3 |
| Maximum Element | easy | Stack Theatre | — | Teach the authored optimal approach, Auxiliary Max Stack (Optimal), inside tokens entering a visible vertical stack with the top always explicit; push, compare, and pop one causal event at a time. The decisive moment is which unresolved work must be remembered in LIFO order. | choose push/pop before the operation executes | 2 |
| Min Max Riddle | hard | Monotonic Skyline | — | Teach the authored optimal approach, Monotonic Stack — Span of Dominance (Optimal), inside bars on a skyline beside a stack of still-useful candidates; let a stronger bar visibly evict dominated candidates. The decisive moment is why every popped item can never help a future answer. | predict how many candidates the new value will pop | 3 |
| Min Stack | medium | Coordinated Data-Structure Design | — | Teach the authored optimal approach, Parallel Min Stack (Optimal), inside two synchronized representations such as a map plus list or array plus index map; run one public operation as a transaction across both structures. The decisive moment is the invariant that keeps O(1) operations correct. | call the API and verify both structures after every mutation | 2 |
| Poisonous Plants | hard | Monotonic Skyline | — | Teach the authored optimal approach, Monotonic Stack of Death Days (Optimal), inside bars on a skyline beside a stack of still-useful candidates; let a stronger bar visibly evict dominated candidates. The decisive moment is why every popped item can never help a future answer. | predict how many candidates the new value will pop | 3 |
| Queue Using Two Stacks | easy | Queue / Transfer Lab | — | Teach the authored optimal approach, Two Stacks — Lazy Transfer (Optimal), inside front/back queue lanes or two stacks joined by a transfer bridge; enqueue, dequeue, and transfer elements without hiding intermediate state. The decisive moment is how the representation preserves FIFO behavior. | perform an operation and inspect its amortized work | 2 |
| Simple Text Editor | medium | Simulation State Machine | — | Teach the authored optimal approach, Stack of Inverse Operations (Memory-Optimised), inside the problem world beside explicit control state and an event timeline; consume one event, apply the rule, and snapshot the resulting state. The decisive moment is the compact state that makes every transition deterministic. | trigger the next event and predict the transition | 2 |
| Simplify Path | medium | Stack Theatre | — | Teach the authored optimal approach, Stack of Directory Names (Optimal), inside tokens entering a visible vertical stack with the top always explicit; push, compare, and pop one causal event at a time. The decisive moment is which unresolved work must be remembered in LIFO order. | choose push/pop before the operation executes | 2 |
| Truck Tour | medium | Greedy Choice Frontier | — | Teach the authored optimal approach, Greedy Single Pass (Optimal), inside sorted candidates approaching a best-so-far frontier; accept or reject each local choice while the safety invariant remains visible. The decisive moment is the exchange argument that proves the choice can be part of an optimum. | make the local choice before the proof is revealed | 2 |
| Valid Parentheses | easy | Stack Theatre | — | Teach the authored optimal approach, Stack — One Pass (Optimal), inside tokens entering a visible vertical stack with the top always explicit; push, compare, and pop one causal event at a time. The decisive moment is which unresolved work must be remembered in LIFO order. | choose push/pop before the operation executes | 1 |
| Waiter | medium | Stack Theatre | — | Teach the authored optimal approach, Sieve of Eratosthenes + Partition Loop (Optimal), inside tokens entering a visible vertical stack with the top always explicit; push, compare, and pop one causal event at a time. The decisive moment is which unresolved work must be remembered in LIFO order. | choose push/pop before the operation executes | 2 |

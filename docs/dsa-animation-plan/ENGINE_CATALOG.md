# Visual engine catalog

This catalog contains 64 composable engines. The original master specification described broad families; this catalog adds specialized engines for advanced strings, range-query trees, state-space search, parsing, and number theory found in the repository's real problem set.

An engine is not a finished problem animation. It is a reusable visual grammar made from smaller primitives. A problem blueprint supplies the real input, frames, invariants, code anchors, explanation anchors, and edge cases.

| ID | Engine | Persistent world | Causal motion | Learner interaction |
| --- | --- | --- | --- | --- |
| `array-memory-ledger` | Array + Memory Ledger | stable array cells beside a live hash-map, set, or frequency ledger | move the cursor one cell at a time; animate lookup first and mutation second | predict lookup result, then reveal the map mutation |
| `candidate-cancellation` | Candidate Cancellation | a current candidate on a podium beside a signed vote balance and uncancelled tokens | pair unlike values as cancellations, replace the candidate only when the balance reaches zero | accept or cancel the next token and predict the surviving candidate |
| `two-pointer-stage` | Two-Pointer Stage | a stable sequence with independently controlled left/right or slow/fast actors | move only the pointer justified by the invariant and dim the discarded region | choose which pointer moves before revealing the rule |
| `sliding-window-lens` | Sliding-Window Lens | a translucent window over a string or array with a persistent validity ledger | expand to acquire information and contract only when the invariant permits | drag the right edge and decide when the left edge must move |
| `prefix-sum-balance` | Prefix-Sum Balance | the original values and a second cumulative track aligned above them | grow the prefix total and connect two prefixes whose difference answers the query | select a range and watch its two boundary prefixes light up |
| `difference-array-wave` | Difference-Array Wave | range updates as start/end impulses followed by a reconstruction sweep | drop boundary deltas, then roll a cumulative wave across the final array | paint a range and inspect the two delta cells it changes |
| `binary-search-spotlight` | Binary-Search Spotlight | an ordered search space with low, mid, and high markers | collapse the surviving interval while leaving rejected space visible but muted | vote left/right/found before the interval collapses |
| `feasibility-dial` | Binary Search on the Answer | a numeric answer dial paired with a feasibility simulation | test a candidate, run the greedy checker, and tighten the answer range | guess a candidate answer and run the feasibility check |
| `unimodal-probe` | Unimodal Probe | a hill-shaped function with two probes splitting it into thirds | compare the probe heights and fade the slope that cannot contain the optimum | move the probes and predict the discarded third |
| `stack-theatre` | Stack Theatre | tokens entering a visible vertical stack with the top always explicit | push, compare, and pop one causal event at a time | choose push/pop before the operation executes |
| `monotonic-skyline` | Monotonic Skyline | bars on a skyline beside a stack of still-useful candidates | let a stronger bar visibly evict dominated candidates | predict how many candidates the new value will pop |
| `queue-transfer-lab` | Queue / Transfer Lab | front/back queue lanes or two stacks joined by a transfer bridge | enqueue, dequeue, and transfer elements without hiding intermediate state | perform an operation and inspect its amortized work |
| `pointer-rewiring-bench` | Pointer-Rewiring Bench | stable linked-list nodes with named prev/current/next references | freeze the saved pointer, then morph exactly one edge at a time | rewire the next edge and detect a lost sub-list |
| `dual-structure-design` | Coordinated Data-Structure Design | two synchronized representations such as a map plus list or array plus index map | run one public operation as a transaction across both structures | call the API and verify both structures after every mutation |
| `tree-traversal-stage` | Tree Traversal Stage | a fixed tree with an explicit frontier, call stack, and return path | move focus without moving nodes; animate enter, recurse, and return separately | step into a child or return a value to the parent |
| `bst-ordered-corridor` | BST Ordered Corridor | a fixed search tree paired with its persistent inorder strip | project lower/upper bounds through the tree while narrowing the valid corridor | choose a branch and inspect the remaining value bounds |
| `tree-construction-workshop` | Tree Construction Workshop | traversal sequences beside an incrementally assembled tree | consume the next root and split or attach the remaining region | place the next node before revealing its legal position |
| `tree-dp-message-passing` | Tree-DP Message Passing | a stable tree where each node sends a compact state vector to its parent | collect child messages, combine them, and optionally reroot the answer | compose child states and compare take/skip or path choices |
| `tree-flattening-map` | Tree Flattening Map | a tree and its Euler-tour array shown side by side | stamp entry/exit times and turn a subtree into a contiguous range | select a subtree and see its exact flattened interval |
| `ancestor-jump-table` | Ancestor Jump Table | a tree paired with a powers-of-two ancestor table | decompose a jump into binary leaps and highlight each table lookup | compose a k-step ancestor jump from powers of two |
| `fenwick-lowbit-ladder` | Fenwick Lowbit Ladder | an array, covered ranges, and the implicit BIT parent links | jump by lowbit(i) for updates and strip lowbit(i) for prefix queries | toggle index bits to reveal the next update/query jump |
| `segment-tree-control-room` | Segment-Tree Control Room | an array under a hierarchical set of range aggregates | split queries by overlap and propagate updates back to the root | paint a query range and collect the minimal covering nodes |
| `heavy-light-path-map` | Heavy-Light Path Map | a tree colored into heavy chains beside its flattened segment-tree lanes | break a path into chain segments and execute one range query per segment | select two nodes and watch their path decompose |
| `heap-arena` | Heap Arena | a heap tree synchronized with its array and candidate queue | insert/extract at the root and show every swap that repairs the invariant | choose the next candidate and repair the heap |
| `two-heap-balance` | Two-Heap Balance | a max-heap and min-heap on opposite sides of a median fulcrum | route each incoming value, then rebalance across the fulcrum | place the next value and decide whether rebalancing is required |
| `trie-pathfinder` | Trie Pathfinder | a prefix tree with shared prefixes kept visible | light one character edge at a time and prune dead branches when justified | type a prefix and follow or backtrack the matching path |
| `grid-frontier` | Grid Frontier | a terrain grid with visited cells, frontier cells, and untouched cells distinct | expand DFS tendrils or BFS waves while keeping the frontier explicit | choose the next valid neighbor and watch the frontier update |
| `graph-frontier` | Graph Frontier | a fixed graph beside the queue or recursion stack that drives traversal | expand one edge at a time and stamp distance, parent, or component labels | expand the next frontier node and predict newly discovered vertices |
| `topological-unlock` | Topological Unlock | a dependency graph with live indegree counters and an unlocked queue | remove a completed node, decrement outgoing neighbors, and unlock zeros | choose an unlocked node and update its dependents |
| `union-find-merger` | Union-Find Merger | components, parent pointers, and rank/size values in one coordinated view | trace find paths, compress them, then merge roots by rank or size | connect two nodes and inspect the resulting parent forest |
| `dijkstra-distance-lab` | Dijkstra Distance Lab | a weighted graph, tentative-distance table, and priority queue | extract the best candidate, relax edges, and visibly discard stale entries | choose the next queue item and perform its relaxations |
| `bellman-ford-sweeps` | Bellman-Ford Sweeps | a weighted edge list beside a distance table organized by passes | sweep every edge, pulse successful relaxations, and compare the extra cycle pass | run one full relaxation pass and predict whether another is needed |
| `floyd-warshall-matrix` | Floyd-Warshall Matrix | a graph beside an all-pairs distance matrix | open one intermediate node k and update every i,j cell through it | select k and inspect which matrix cells improve |
| `mst-edge-market` | Minimum-Spanning-Tree Edge Market | weighted candidate edges competing to join a growing forest | accept the cheapest safe edge and reject edges that create a cycle | approve or reject the next edge, then reveal the proof |
| `graph-coloring-studio` | Graph Coloring Studio | a graph with two color palettes and conflict edges | propagate the opposite color across each component and flag contradictions | color the next node and detect a conflict |
| `rerooting-lens` | Rerooting Lens | a tree with directed parent-child contribution arrows | compute downward answers, move the root across one edge, and repair contributions | drag the root to a neighbor and recompute only affected state |
| `decision-tree-backtracker` | Decision-Tree Backtracker | the concrete board/string plus a parallel choose-recurse-undo tree | commit one choice, descend, hit success/dead-end, then visibly undo | choose the next candidate and predict whether it survives |
| `constraint-board` | Constraint Board | a board with candidate sets and row/column/region constraints | remove candidates as constraints propagate, then branch only when needed | place a candidate and inspect all propagated consequences |
| `greedy-choice-frontier` | Greedy Choice Frontier | sorted candidates approaching a best-so-far frontier | accept or reject each local choice while the safety invariant remains visible | make the local choice before the proof is revealed |
| `interval-timeline` | Interval Timeline | interval bars on a shared time axis with endpoints and active resources visible | sort, sweep, merge, allocate, or discard at endpoint events | drag an interval onto the timeline and decide merge/room/reject |
| `dp-one-dimensional` | 1D DP State Conveyor | state cells ordered by subproblem size with dependency arrows | compare choices, compute the recurrence, and commit one new state | choose the recurrence inputs and commit the next cell |
| `dp-matrix` | 2D DP Matrix | a matrix whose axes explain the two state dimensions | focus one cell, pull values from legal predecessors, and commit the result | select predecessor cells and calculate the focused state |
| `knapsack-capacity-lab` | Knapsack Capacity Lab | items beside capacity lanes and take/skip branches | compare excluding versus including an item and update reachable/best capacities | take or skip the current item and inspect affected capacities |
| `interval-dp-splitter` | Interval-DP Splitter | a sequence with expanding interval brackets and candidate split points | grow interval length, try each split, and preserve the best composition | choose the final split and compare subinterval totals |
| `bitmask-state-cube` | Bitmask State Cube | subset states as lit bits linked to reachable next masks | toggle one choice bit and propagate values across the subset lattice | toggle a bit and inspect newly reachable states |
| `digit-dp-automaton` | Digit-DP Automaton | a digit track above memoized states for position, tightness, started-state, and problem-specific counts | branch over legal next digits, preserve or release the tight bound, and merge identical suffix states | choose the next digit and update the tight/started flags |
| `lis-patience-table` | LIS Patience Table | the input sequence above evolving tails/piles and predecessor links | binary-search the replacement pile and update the minimal tail | place the next value into its pile and reconstruct one LIS |
| `merge-sort-studio` | Merge-Sort Studio | a stable array split into recursive lanes with a merge workbench | split ranges, compare two front elements, and stitch the sorted result | choose the next merge element and update the counter |
| `partition-workbench` | Partition / Selection Workbench | an array with pivot, less/equal/greater regions, and swap arcs | classify one value at a time and grow the pivot's final region | classify the focused value and perform the swap |
| `counting-buckets` | Counting-Bucket Factory | input tokens feeding frequency buckets and a stable output conveyor | count, prefix positions when needed, then emit values in order | drop the next token into its bucket and rebuild the output |
| `bitwise-circuit` | Bitwise Circuit | aligned bit lanes flowing through XOR, AND, shift, and carry gates | transform one bit position at a time while decimal values stay visible | toggle input bits and predict the gate output |
| `string-token-conveyor` | String Token Conveyor | characters as addressable tokens with parser state and output buffer | consume one character, update the state machine, and emit only justified output | feed the next character and choose the state transition |
| `string-matching-lab` | String-Matching Lab | pattern and text tracks with prefix/Z/hash state aligned below | shift or reuse prior match information instead of restarting comparisons | advance a mismatch and choose the correct fallback |
| `palindrome-expansion` | Palindrome Expansion Lab | a character track with mirrored pointers and reusable radius information | expand around centers, mirror known radii, and update the right boundary | choose a center and predict its mirrored radius |
| `suffix-array-laboratory` | Suffix Array + LCP Laboratory | all suffixes as sortable ribbons beside rank-doubling and LCP tracks | sort by doubled rank pairs, then scan adjacent suffixes for shared prefixes | rank one doubling round and inspect newly adjacent suffixes |
| `number-theory-workbench` | Number-Theory Workbench | numbers decomposed into factors, residues, or recurrence states | apply one algebraic reduction while keeping the invariant equation visible | perform the next reduction and verify the invariant |
| `geometry-slope-lab` | Geometry Slope Lab | an anchor point with rays to other points and a ledger of normalized rational slopes | rotate through anchors, reduce each dx/dy pair by gcd, and grow the largest collinear bucket | normalize the next ray and place it into the matching slope bucket |
| `sieve-elimination-field` | Sieve Elimination Field | integers on a grid with base primes and composite strike paths | start at the mathematically correct multiple and eliminate in waves | select a prime and predict its first new composite |
| `euclidean-reduction` | Euclidean Reduction | two number bars and the remainder equation they produce | replace (a,b) with (b,a mod b) and optionally replay substitutions backward | compute the next remainder and back-substitute coefficients |
| `matrix-simulation` | Matrix Simulation Studio | a coordinate grid with direction, boundary, and staged cell states | apply one spatial rule per frame and preserve simultaneous updates when required | advance one move/generation and inspect affected neighbors |
| `math-derivation-canvas` | Math Derivation Canvas | a concrete example beside a symbolic equation and geometric/counting model | morph repeated work into the closed form or recurrence | change the input and verify the derivation still holds |
| `fft-butterfly` | FFT Butterfly | coefficient and value representations connected by even/odd butterfly stages | split, combine roots-of-unity pairs, multiply pointwise, then invert | run one butterfly stage and inspect phase/degree changes |
| `state-space-explorer` | State-Space Explorer | compressed states as nodes with legal moves as generated edges | expand BFS layers or heuristic candidates without drawing the entire graph upfront | make one legal move and see its encoded successor |
| `simulation-state-machine` | Simulation State Machine | the problem world beside explicit control state and an event timeline | consume one event, apply the rule, and snapshot the resulting state | trigger the next event and predict the transition |

## Primitive contract

Every engine composes typed primitives rather than drawing arbitrary illustrations. Primitives receive immutable frame state and expose semantic labels for screen readers. The first implementation set should include array/string tracks, pointers, maps/sets, stacks/queues, trees/graphs, grids, matrices, heaps, intervals, bit lanes, equation ribbons, choice forks, and code/dry-run anchors.

Every transition must answer one question: what algorithmic fact changed, and which exact code line caused it?

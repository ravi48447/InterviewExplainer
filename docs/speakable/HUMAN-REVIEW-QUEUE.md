# Speakable — human review queue

Items where each phase made a best-instinct call because the plan was genuinely silent or internally inconsistent. Human reviewer: confirm or override before the next phase kicks off.

| # | Deliverable | Item | Best-instinct decision | Suggested human action |
|---|---|---|---|---|
| E-1 | 0.6 word-ceilings | Plan §8 gives only a narrative bound for archetype **E (Design)** ("~250–350 total; rethink_if ≤ 50"). Phase 0 expanded into per-beat soft/hard caps so the lint can enforce them. | Per-beat caps derived to sum to ~270 soft / ~390 hard, inside the plan's range; `rethink_if` capped at 50 hard per the plan note. | Confirm the per-beat split or supply hand-tuned numbers. |
| F-1 | 0.6 word-ceilings | Same gap for archetype **F (System Design / LLD)** — plan gives only "each phase ≤ ~120 words, total ~600–900". | Per-beat caps assigned with `bottleneck_deep_dive` weighted heaviest (140 hard); total lands at ~620 soft / ~910 hard. | Confirm or rebalance. |
| G-1 | 0.6 word-ceilings | Same gap for archetype **G (Behavioural / STAR)** — plan gives "S/T/A/R each ~50–80, Reflection ~40, total ~250–350". | Per-beat caps assigned tracking the plan's S/T/A/R sizing; total ~320 soft / ~445 hard. | Confirm. |
| DATA-1 | 0.2 pillar-register | `content/java-backend-intermediate/_index.json` has the `postgresql` module (M12b) with `pillar: P06` but `pillarName: "Data & Persistence"` (which corresponds to P03). The pillar code and pillar name are inconsistent for this single module. | Pillar register treats `postgresql` as **P03 Data & Persistence by content** (it covers MVCC, JSONB, EXPLAIN — pure data work), and notes the discrepancy at the top of the doc and inside the P03 and P06 sections. | Decide whether to fix `_index.json` to set `pillar: P03` for `postgresql` (most likely correct), or to redefine P06 to include Postgres specifically. The Phase 0 artefacts assume the former. |
| L1-1 | 0.2 pillar-register | The phrase "blast radius" (Layer 1 banned vocab) appears twice in the P09 register rider — direct mirror of `SPEAKABLE-PLAN.md` §4 which uses the term verbatim. | Left as-is because Layer 1 is permitted in spec docs and rephrasing would deviate from the source plan. | If the plan §4 is later softened, propagate the new phrasing here. |
| §16-1 | 0.3 schema | `SPEAKABLE-PLAN.md` §16 worked example uses the pre-§15.18 shape — beats carry `kind` and `text` but no explicit `layout` field. The Phase 0 schema (and the §5 spec it follows) makes `layout` mandatory per beat to enforce the visual rhythm rules locked in §15.18. | **RESOLVED** in commit `ea8eacd` (Phase 1 preflight). Plan §16 now carries explicit `layout: paragraph` / `grouped_paragraphs` / `bullets` per beat. The worked example validates against the schema and is lint-clean against the visual-rhythm rules. | Done. |
| §3-vs-§16 | 1.1 lint script | Plan §3 lists `how_to_use` as **required** for archetype A. Plan §16 worked example (the canonical "bar every Speakable must hit") **omits the `how_to_use` beat entirely** — it carries `definition`, `why_exists`, `parts_or_states`, `example`, `pitfalls`. The two sections of the plan disagree about whether `how_to_use` is mandatory for A. | **RESOLVED** in Phase 2 preflight 6.1. Decision (locked): `how_to_use` is **recommended, not required** for archetype A. Rationale: most A-shaped questions don't have a "how to use" angle separate from `example` (e.g. "what is a thread"). Where it does fit (e.g. "what is the Stream API"), the agent will include it naturally. Lint already treats it as soft-required (warn, -4); `archetypes.md` and `lint-rules.md` §7.1.3 updated to make this explicit. No code change to `audit_speakable.py`. | Done. |
| §16-MISSING-FIELDS | 1.1 lint script | The §16 YAML worked example uses `archetype: conceptual` (the human-readable form) rather than the schema's letter discriminator (`A`); it also omits `speakable_status` (the lifecycle gate the renderer uses). Both are required by the schema. | **RESOLVED** in Phase 2 preflight 6.2. §16 YAML now reads `archetype: A` and `speakable_status: approved`. The fixture-loader normalisation in `--check-fixture` is retained as a defensive convenience but no longer needed for the canonical fixture. Production data still must carry the canonical letter form. | Done. |
| AUTH-1 | 1.8 admin/speakable-review | The admin review UI is gated behind a query-param key matched against `SPEAKABLE_ADMIN_KEY` (per Phase 1 prompt §15). This is enough to prevent accidental access during local development but is **not production-grade** — there is no rate limiting, no audit trail, no rotation, and the key travels in URLs / referers. | Implemented as specified for Phase 1; documented in the page itself with a "dev-only" banner. | Replace with a real auth scheme (NextAuth, session cookies + role check, or middleware-level enforcement) before any external use of the admin URL. |
| AUDIO-1 | 1.6 ReadAloudButton wiring | Phase 1 prompt §14 says to "locate the place where the current audio page reads the speakable answer aloud and replace that inline call with the new `<ReadAloudButton>`." The current `frontend/app/mock-interviews/audio/page.tsx` only reads the **question** aloud (so the candidate hears it), never the answer — the answer is shown only in transcript review at the end. There is no existing answer-readout call to replace. | The button is wired in next to the existing "Replay Question" controls, but only **appears after the candidate has recorded a response for the current question** (i.e., `answers[currentQuestionIndex]` exists). It reads `currentQuestion.answer` (legacy markdown today, v2 once approved) via the shared `<ReadAloudButton source={...}/>` so the v2 path comes online automatically when status flips to `approved`. The existing `speakQuestion()` and recognition / transcript flow is untouched. | Confirm the placement is the right UX. Alternatives: (a) put a "Hear answer" button directly under the question text from the start (encourages cheating before recording), (b) add a dedicated answer-review step at the end of the interview, (c) drop the wire-in here entirely and reserve the button for the admin review page + the future per-question pages. |
| RENDERER-1 | 1.9 renderer integration | The Phase 1.9 prompt says "for every existing question, the rendered output must be byte-equivalent or visually-identical to today" *and* "Remove the four hardcoded OOP CSS rules in PreviewArticle.tsx … Styling is now driven by archetype + pillar + layout from the data, not by string-matching headings." Those two clauses contradict each other for the OOP four-pillars question on the preview page (its current visual relies on the four removed name-matched CSS rules). | The four hardcoded rules were removed per the explicit instruction. The legacy preview path now goes through `<Speakable legacyVariant="preview"/>` which renders inside `<div className="speakable-prose">`. The generic magazine styles in `PreviewArticle`'s `<style>` tag (drop-cap, serif paragraphs, code styling, generic `[data-pillar]` framework) still apply because that block is a vanilla global `<style>` tag, not styled-jsx. The OOP four-pillars page no longer shows distinct per-pillar accent colours / backgrounds (the four name-specific rules are gone), but the rest of the magazine layout is preserved. The `QuestionPageLayout.tsx` integration is byte-equivalent — Legacy.tsx `variant="question"` is a verbatim port of the green Interview Answer card. | **RESOLVED — accepted trade-off** (Phase 2 preflight 6.3). The four hardcoded per-pillar accent colours were string-matching one specific question's section labels (`Encapsulation` / `Inheritance` / `Polymorphism` / `Abstraction`); they would never have applied to any other question in the catalogue. The Phase 2 v2 layout (`grouped_paragraphs`) provides clean visual separation for the four pillars via subheadings, dividers, and rhythm — the per-pillar colours were decorative, not functional. Once OOP four-pillars is hand-crafted as a v2 (post-Phase-2 — it is not a golden reference), the renderer will already give it the correct visual rhythm without any per-question CSS. No code change required. |

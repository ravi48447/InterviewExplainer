'use client';

// Stable public entry point. The equipment-first studio is isolated in V2 so
// legacy imports continue to resolve while the live route uses one design.
export { MockInterviewStudio } from './MockInterviewStudioV2';

{
  "$comment": "Phase 0 seed — standard examples per topic. The lint script (Phase 1) reads this to enforce lint 7.2.2: every Speakable's example beat must use the standard example for its topic unless familiarity_override: true is set with a justification. Match by topic_id with codex/phrasings.json. Source: agent-seeded per Plan §15B.1.",
  "version": 1,
  "examples": [
    { "topic_id": "thread-basics-and-lifecycle",         "example": "extends Thread vs implements Runnable",                          "use_when": "introducing how a Java thread is constructed",                              "source": "agent-seeded" },
    { "topic_id": "thread-vs-runnable-vs-callable",      "example": "Future<Integer> f = executor.submit(() -> compute())",            "use_when": "showing the Callable + Future shape",                                       "source": "agent-seeded" },
    { "topic_id": "executor-framework",                  "example": "Executors.newFixedThreadPool(8) handing out 100 tasks",           "use_when": "showing decoupled task submission",                                         "source": "agent-seeded" },
    { "topic_id": "synchronization-and-locks",           "example": "synchronized block on a private final Object lock",               "use_when": "showing intrinsic-lock idiom safely",                                       "source": "agent-seeded" },
    { "topic_id": "volatile-vs-atomic",                  "example": "AtomicInteger.incrementAndGet() vs volatile int counter",         "use_when": "contrasting visibility-only vs atomic increment",                           "source": "agent-seeded" },
    { "topic_id": "hashmap-internals",                   "example": "map.put(\"key\", value) — collision in a bucket",                 "use_when": "showing the put path and bucket landing",                                    "source": "agent-seeded" },
    { "topic_id": "arraylist-vs-linkedlist",             "example": "List<Integer> = new ArrayList<>() vs new LinkedList<>()",         "use_when": "the canonical declaration that makes the trade-off concrete",                "source": "agent-seeded" },
    { "topic_id": "hashset-and-treemap",                 "example": "TreeMap<String, Integer> for sorted-by-key iteration",            "use_when": "showing where TreeMap's sorted property earns its cost",                     "source": "agent-seeded" },
    { "topic_id": "equals-and-hashcode-contract",        "example": "Point with x and y — equals on both, hashCode = Objects.hash(x,y)","use_when": "showing the contract on a value-like class",                                "source": "agent-seeded" },
    { "topic_id": "comparable-vs-comparator",            "example": "list.sort(Comparator.comparing(Person::getAge))",                 "use_when": "showing Comparator as pluggable strategy",                                  "source": "agent-seeded" },
    { "topic_id": "generics-and-type-erasure",           "example": "List<String> at compile time becomes List at runtime",            "use_when": "demonstrating erasure on the most familiar type",                           "source": "agent-seeded" },
    { "topic_id": "checked-vs-unchecked-exceptions",     "example": "IOException (checked) vs NullPointerException (unchecked)",       "use_when": "the canonical pair to contrast",                                            "source": "agent-seeded" },
    { "topic_id": "try-with-resources",                  "example": "try (var br = Files.newBufferedReader(path)) { ... }",            "use_when": "showing the modern Java form on a familiar I/O case",                       "source": "agent-seeded" },
    { "topic_id": "string-immutability-and-string-pool", "example": "\"abc\" == \"abc\" returns true (pool); new String(\"abc\") doesn't","use_when": "the universal example for the pool",                                       "source": "agent-seeded" },
    { "topic_id": "equals-vs-double-equals",             "example": "\"hello\" == \"hello\" (true), but new String(\"hello\") == \"hello\" (false)", "use_when": "the canonical pool gotcha",                                       "source": "agent-seeded" },
    { "topic_id": "oop-four-pillars",                    "example": "Dog extends Animal; List<String> = new ArrayList<>()",            "use_when": "covering inheritance + abstraction together in one familiar shape",         "source": "agent-seeded" },
    { "topic_id": "abstract-class-vs-interface",         "example": "Shape (abstract draw()) vs Drawable (interface)",                 "use_when": "showing both side by side on the same domain",                              "source": "agent-seeded" },
    { "topic_id": "inner-classes",                       "example": "Iterator implementation as a non-static inner class of MyList",   "use_when": "showing the enclosing-instance reference",                                  "source": "agent-seeded" },
    { "topic_id": "static-and-final-keywords",           "example": "public static final int MAX = 100;",                              "use_when": "the canonical constant declaration",                                        "source": "agent-seeded" },
    { "topic_id": "garbage-collection-generations",      "example": "young → survivor → old, with G1 default since Java 9",            "use_when": "naming the spaces and the modern default",                                  "source": "agent-seeded" },
    { "topic_id": "jit-compilation",                     "example": "a tight loop interpreted, then C1-compiled, then C2-compiled",    "use_when": "showing tiered escalation",                                                 "source": "agent-seeded" },
    { "topic_id": "reflection-and-annotations",          "example": "@Override read by the compiler; @Retention(RUNTIME) read by the framework", "use_when": "the difference between compile-time and runtime annotation use",   "source": "agent-seeded" },

    { "topic_id": "ioc-and-di",                          "example": "constructor with final fields + @RequiredArgsConstructor",        "use_when": "the modern Spring DI idiom",                                                "source": "agent-seeded" },
    { "topic_id": "spring-bean-lifecycle",               "example": "instantiate → populate → @PostConstruct → ready → @PreDestroy",   "use_when": "naming the timeline beat by beat",                                          "source": "agent-seeded" },
    { "topic_id": "spring-bean-scopes",                  "example": "@Service singleton (default) vs @Scope(\"prototype\") for per-call", "use_when": "contrasting the two by far most common scopes",                            "source": "agent-seeded" },
    { "topic_id": "spring-stereotype-annotations",       "example": "OrderService (@Service) → OrderRepository (@Repository) → OrderController (@RestController)", "use_when": "the standard layered-app shape", "source": "agent-seeded" },
    { "topic_id": "spring-aop-basics",                   "example": "@Around(\"@annotation(Timed)\") logging method duration",         "use_when": "the cross-cutting-logging archetype",                                       "source": "agent-seeded" },
    { "topic_id": "transactional-propagation-and-isolation", "example": "@Transactional(propagation = REQUIRES_NEW) on the audit method", "use_when": "the canonical case where a separate transaction is needed",              "source": "agent-seeded" },
    { "topic_id": "spring-boot-auto-configuration",      "example": "spring-boot-starter-data-jpa pulling Hibernate + HikariCP + auto-config", "use_when": "showing the starter-driven default chain",                            "source": "agent-seeded" },
    { "topic_id": "restcontroller-and-responsebody",     "example": "@RestController class with @GetMapping(\"/users/{id}\") returning a User",   "use_when": "the most familiar REST-controller shape",                       "source": "agent-seeded" },
    { "topic_id": "exception-handling-controlleradvice", "example": "@ControllerAdvice mapping IllegalArgumentException → 400",        "use_when": "the canonical mapping example",                                             "source": "agent-seeded" },
    { "topic_id": "profiles-and-properties",             "example": "application.yml + application-prod.yml with spring.profiles.active=prod", "use_when": "showing layered properties",                                       "source": "agent-seeded" },
    { "topic_id": "actuator-endpoints",                  "example": "/actuator/health returning UP; /actuator/metrics for Micrometer", "use_when": "the most-used pair",                                                        "source": "agent-seeded" },

    { "topic_id": "acid-properties",                     "example": "BEGIN; UPDATE accounts ... ; UPDATE accounts ... ; COMMIT;",      "use_when": "the bank-transfer canonical example",                                       "source": "agent-seeded" },
    { "topic_id": "sql-transaction-isolation-levels",    "example": "phantom read scenario at REPEATABLE_READ",                        "use_when": "showing where REPEATABLE_READ stops short of SERIALIZABLE",                  "source": "agent-seeded" },
    { "topic_id": "jpa-cascades",                        "example": "@OneToMany(cascade = ALL, orphanRemoval = true) on Order → OrderLine", "use_when": "the canonical parent-child cascade",                                    "source": "agent-seeded" },
    { "topic_id": "lazy-vs-eager-loading",               "example": "@OneToMany(fetch = LAZY) on Order.items causing LazyInitializationException outside the transaction", "use_when": "the universal lazy-loading trap", "source": "agent-seeded" },
    { "topic_id": "n-plus-one-query-problem",            "example": "for (Order o : orders) o.getItems().size() — 1 + N queries",      "use_when": "the canonical iteration that triggers N+1",                                 "source": "agent-seeded" },
    { "topic_id": "indexing-and-explain",                "example": "EXPLAIN ANALYZE on SELECT ... WHERE email = 'x' before and after creating an index on email", "use_when": "the standard before/after demonstration",                  "source": "agent-seeded" },
    { "topic_id": "connection-pooling",                  "example": "HikariCP with maximumPoolSize: 20 on a 4-core service",           "use_when": "showing a sane size on a known shape",                                      "source": "agent-seeded" },
    { "topic_id": "optimistic-vs-pessimistic-locking",   "example": "@Version Long version field on the entity",                       "use_when": "the JPA optimistic case",                                                   "source": "agent-seeded" },

    { "topic_id": "rest-principles",                     "example": "POST /orders → 201 Created with Location: /orders/123",           "use_when": "the canonical create-resource example",                                     "source": "agent-seeded" },
    { "topic_id": "idempotency",                         "example": "POST /payments with Idempotency-Key header so retries don't double-charge", "use_when": "the textbook safe-retry case",                                    "source": "agent-seeded" },
    { "topic_id": "http-status-codes",                   "example": "200 vs 201 vs 204; 400 vs 401 vs 403 vs 404",                     "use_when": "naming the most-confused pairs",                                            "source": "agent-seeded" },
    { "topic_id": "api-versioning",                      "example": "GET /v1/users vs GET /users with Accept: application/vnd.api+json;v=1", "use_when": "showing both schools side by side",                                   "source": "agent-seeded" },
    { "topic_id": "pagination-patterns",                 "example": "?cursor=eyJpZCI6MTIzfQ& limit=20",                                "use_when": "the standard cursor-based shape",                                           "source": "agent-seeded" },
    { "topic_id": "jwt-structure",                       "example": "eyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiJqb2UifQ.<sig>",                  "use_when": "the canonical 3-part token shape",                                          "source": "agent-seeded" },
    { "topic_id": "oauth2-flows",                        "example": "Authorization Code with PKCE for a SPA hitting an API",           "use_when": "the modern default flow",                                                   "source": "agent-seeded" },
    { "topic_id": "rate-limiting",                       "example": "100 req/min per API key with token bucket; return 429 + Retry-After: 60", "use_when": "the most common per-key budget",                                   "source": "agent-seeded" },
    { "topic_id": "circuit-breaker",                     "example": "Resilience4j CircuitBreaker around a downstream call to billing", "use_when": "the canonical resilience-pattern wrapping",                                 "source": "agent-seeded" },
    { "topic_id": "kafka-basics-partitions-consumer-groups", "example": "topic with 12 partitions, consumer group of 4 (3 partitions each)", "use_when": "the standard layout for explaining parallelism",                       "source": "agent-seeded" },
    { "topic_id": "exactly-once-vs-at-least-once",       "example": "idempotent producer + transactional commit on the consumer side", "use_when": "the canonical Kafka exactly-once construction",                             "source": "agent-seeded" },

    { "topic_id": "solid-principles",                    "example": "ReportPrinter doing only printing (SRP); PaymentProcessor depending on PaymentMethod abstraction (DIP)", "use_when": "showing two of the five on one familiar shape", "source": "agent-seeded" },
    { "topic_id": "design-patterns-classics",            "example": "PaymentMethod strategy: CardPayment / UpiPayment / CashPayment",  "use_when": "the standard pluggable-algorithm example",                                  "source": "agent-seeded" },
    { "topic_id": "composition-over-inheritance",        "example": "Car has-a Engine vs Car extends EngineVehicle",                  "use_when": "the canonical has-a vs is-a contrast",                                      "source": "agent-seeded" },
    { "topic_id": "layered-architecture",                "example": "Controller → Service → Repository → Database",                    "use_when": "the universal Java-backend layering",                                       "source": "agent-seeded" },
    { "topic_id": "hexagonal-clean-architecture",        "example": "OrderService (domain) + OrderRepository (port) + JpaOrderRepository (adapter)", "use_when": "the canonical port-and-adapter mapping",                          "source": "agent-seeded" },
    { "topic_id": "cqrs-basics",                         "example": "OrderCommand (write) → OrderEventStore; OrderProjection (read) → denormalised view", "use_when": "the standard split shape",                                      "source": "agent-seeded" },
    { "topic_id": "event-driven-design",                 "example": "OrderPlaced event consumed by Inventory + Email + Analytics",     "use_when": "showing fan-out without coupling",                                          "source": "agent-seeded" },
    { "topic_id": "dry-vs-kiss",                         "example": "two slightly-different validators force-merged into one branchy method",   "use_when": "the canonical premature-DRY anti-pattern",                          "source": "agent-seeded" },

    { "topic_id": "capacity-estimation",                 "example": "10M URLs/day → 115 writes/s avg, 600 peak, 30K reads/s",          "use_when": "the canonical numerical walkthrough",                                       "source": "agent-seeded" },
    { "topic_id": "fr-vs-nfr",                           "example": "FR: 'redirect on GET' / NFR: 'p99 redirect under 50 ms'",         "use_when": "showing the same system in both lenses",                                    "source": "agent-seeded" },
    { "topic_id": "cap-theorem-and-tradeoffs",           "example": "DynamoDB favours availability (AP); Spanner favours consistency (CP)", "use_when": "the canonical AP vs CP pair",                                            "source": "agent-seeded" },
    { "topic_id": "caching-strategies",                  "example": "cache-aside read: GET cache → miss → SELECT db → SETEX cache",    "use_when": "the universal cache-aside shape",                                           "source": "agent-seeded" },
    { "topic_id": "load-balancing",                      "example": "ALB at L7 routing /api/* to one target group, /static/* to another", "use_when": "the canonical L7 routing example",                                       "source": "agent-seeded" },
    { "topic_id": "sharding-and-replication",            "example": "shard by user_id with 16 shards; one leader + two read replicas per shard", "use_when": "the standard shard-then-replicate layout",                          "source": "agent-seeded" },
    { "topic_id": "url-shortener-design",                "example": "POST /shorten → base-62 7-char key; GET /{key} → 301",            "use_when": "the canonical API shape",                                                   "source": "agent-seeded" },
    { "topic_id": "rate-limiter-design",                 "example": "1M req/s, 1KB per token-bucket entry → 1GB hot state in Redis",   "use_when": "the canonical numerical capacity for the limiter itself",                   "source": "agent-seeded" },
    { "topic_id": "news-feed-design",                    "example": "100M DAU, fan-out-on-write for normal users, fan-out-on-read for celebrities",     "use_when": "the canonical hybrid",                                          "source": "agent-seeded" },

    { "topic_id": "authentication-vs-authorization",     "example": "login (AuthN) sets the session; @PreAuthorize(\"hasRole('ADMIN')\") (AuthZ) gates the endpoint",   "use_when": "the canonical Spring pair",            "source": "agent-seeded" },
    { "topic_id": "oauth2-flows-detail",                 "example": "browser SPA → Authorization Code with PKCE → access token + refresh token",         "use_when": "the modern SPA flow",                                          "source": "agent-seeded" },
    { "topic_id": "jwt-signing",                         "example": "RS256 with the public key served from /.well-known/jwks.json",   "use_when": "the canonical asymmetric setup",                                            "source": "agent-seeded" },
    { "topic_id": "csrf-and-xss",                        "example": "CSRF: hidden form on attacker.com posting to bank.com; XSS: <script> in a comment field", "use_when": "the canonical attack pair",                                  "source": "agent-seeded" },
    { "topic_id": "password-hashing-bcrypt",             "example": "BCrypt.hashpw(password, BCrypt.gensalt(12))",                     "use_when": "the canonical work-factor-12 call",                                         "source": "agent-seeded" },
    { "topic_id": "tls-handshake",                       "example": "TLS 1.3 ClientHello → ServerHello + cert + Finished — 1 RTT",     "use_when": "the modern collapsed handshake",                                            "source": "agent-seeded" },

    { "topic_id": "test-pyramid",                        "example": "1000 unit tests + 100 integration + 10 e2e",                      "use_when": "the canonical ratio that makes the shape concrete",                         "source": "agent-seeded" },
    { "topic_id": "unit-vs-integration-vs-contract",     "example": "OrderServiceTest (unit) vs OrderControllerIT (integration) vs OrderApiPactTest (contract)", "use_when": "the standard naming convention",                          "source": "agent-seeded" },
    { "topic_id": "mocking-vs-stubbing",                 "example": "Mockito.when(repo.findById(1L)).thenReturn(Optional.of(user))",   "use_when": "the canonical stub shape in Mockito",                                       "source": "agent-seeded" },
    { "topic_id": "code-coverage",                       "example": "85% line + 70% branch on a 50-class service",                     "use_when": "showing a defensible-not-perfect target",                                   "source": "agent-seeded" },
    { "topic_id": "tdd-basics",                          "example": "write the failing assertEquals first, then the smallest production code that passes", "use_when": "the canonical red-green step",                                "source": "agent-seeded" },

    { "topic_id": "cicd-pipeline-stages",                "example": "GitHub Actions: build → unit-test → image-scan → integration → deploy", "use_when": "the standard pipeline shape",                                            "source": "agent-seeded" },
    { "topic_id": "docker-layers-multi-stage-builds",    "example": "FROM maven AS build → RUN mvn package → FROM eclipse-temurin:21-jre → COPY --from=build", "use_when": "the canonical Java multi-stage shape",                       "source": "agent-seeded" },
    { "topic_id": "kubernetes-pod-and-service",          "example": "Deployment of 3 pods + Service ClusterIP fronting them",          "use_when": "the canonical pod+service pair",                                            "source": "agent-seeded" },
    { "topic_id": "blue-green-and-canary",               "example": "canary 5% → 25% → 100% with auto-rollback on p99 regression",     "use_when": "the standard canary stepping",                                              "source": "agent-seeded" },
    { "topic_id": "rolling-updates",                     "example": "maxSurge: 1, maxUnavailable: 0 — never lose capacity during rollout", "use_when": "the canonical zero-downtime setting",                                    "source": "agent-seeded" },

    { "topic_id": "region-vs-az",                        "example": "us-east-1 with us-east-1a/b/c — deploy to all three for HA",      "use_when": "the canonical multi-AZ shape on AWS",                                       "source": "agent-seeded" },
    { "topic_id": "iam-basics",                          "example": "IAM role attached to a Lambda; trust policy lets Lambda assume it", "use_when": "the canonical role-not-user pattern",                                     "source": "agent-seeded" },
    { "topic_id": "s3-consistency-model",                "example": "PutObject then GetObject on the same key — strong consistency since 2020", "use_when": "showing the modern guarantee",                                        "source": "agent-seeded" },
    { "topic_id": "autoscaling",                         "example": "HPA scaling from 3 → 12 pods on CPU > 70%",                       "use_when": "the canonical Kubernetes HPA case",                                         "source": "agent-seeded" },
    { "topic_id": "vpc-and-networking-basics",           "example": "public subnet (web tier) + private subnet (app tier) + NAT gateway", "use_when": "the canonical 2-tier VPC layout",                                         "source": "agent-seeded" },

    { "topic_id": "log-levels-and-structured-logging",   "example": "{\"level\":\"INFO\",\"trace_id\":\"abc\",\"event\":\"order.created\",\"order_id\":42}", "use_when": "the canonical structured-log line",                              "source": "agent-seeded" },
    { "topic_id": "distributed-tracing",                 "example": "client → api → orders → payments — 4 spans in one trace",         "use_when": "the canonical trace shape across hops",                                     "source": "agent-seeded" },
    { "topic_id": "sli-slo-sla",                         "example": "SLI = 5xx rate; SLO = < 0.1% over 30 days; SLA = 99.9% with credits",     "use_when": "the canonical three-line set",                                      "source": "agent-seeded" },
    { "topic_id": "error-budget",                        "example": "99.9% SLO over 30 days = ~43 minutes of error budget per month",  "use_when": "the canonical math",                                                        "source": "agent-seeded" },
    { "topic_id": "incident-response-and-runbooks",      "example": "P0 declared → on-call paged → commander assigned → comms branch + investigation branch", "use_when": "the canonical incident shape",                              "source": "agent-seeded" },
    { "topic_id": "post-mortem-and-rca",                 "example": "5-whys uncovering 'no rollback plan written before the deploy'",  "use_when": "the canonical root-cause shape that points at a system gap, not a person",  "source": "agent-seeded" },

    { "topic_id": "star-method",                         "example": "S: payment integration / T: ship in 3 weeks / A: cut scope, feature-flag / R: shipped on time / Reflection: escalate sooner next time", "use_when": "a balanced STAR walk-through", "source": "agent-seeded" },
    { "topic_id": "self-introduction",                   "example": "'I'm a backend engineer at X, mostly Java and Kafka, and I'm drawn to this role because of Y'", "use_when": "the canonical 30-second opener",                       "source": "agent-seeded" },
    { "topic_id": "conflict-resolution",                 "example": "disagreed on REST vs gRPC; reframed as 'pick by latency budget' and chose gRPC together", "use_when": "the canonical move-toward-shared-ground arc",               "source": "agent-seeded" },
    { "topic_id": "failure-and-learning",                "example": "shipped a migration without a reverse plan; lost 40 minutes during P0; now write the rollback first", "use_when": "the canonical own-it-then-learn arc",            "source": "agent-seeded" },
    { "topic_id": "weakness-question",                   "example": "'I've avoided public speaking; I'm doing a brown-bag every month and the team's noticed'", "use_when": "the canonical real-weakness-with-progress shape",            "source": "agent-seeded" },

    { "$comment": "=== Python Backend Intermediate — track: python-backend ===" },
    { "topic_id": "py-core-data-structures",             "example": "names = ['Alice', 'Bob']; config = {'host': 'localhost', 'port': 5432}; unique_tags = {'python', 'backend'}",           "use_when": "showing list, dict, and set side by side in one familiar shape",                    "source": "agent-seeded" },
    { "topic_id": "py-oop-four-pillars",                 "example": "class GoldenRetriever(Dog): def speak(self): return 'woof'",                                                              "use_when": "the canonical Python inheritance example",                                         "source": "agent-seeded" },
    { "topic_id": "py-dunder-methods",                   "example": "class Point: def __init__(self, x, y): self.x, self.y = x, y; def __repr__(self): return f'Point({self.x}, {self.y})'", "use_when": "showing __init__ and __repr__ on a simple value object",                           "source": "agent-seeded" },
    { "topic_id": "py-decorators",                       "example": "@functools.wraps(func) inside a timing decorator that logs elapsed seconds",                                              "use_when": "the canonical decorator shape that preserves function metadata",                    "source": "agent-seeded" },
    { "topic_id": "py-generators-iterators",             "example": "def fibonacci(): a, b = 0, 1\n  while True: yield a; a, b = b, a+b",                                                    "use_when": "the canonical infinite lazy sequence",                                              "source": "agent-seeded" },
    { "topic_id": "py-context-managers",                 "example": "with open('data.csv', 'r') as f: for line in f: process(line)",                                                          "use_when": "the universal file-handle context manager",                                         "source": "agent-seeded" },
    { "topic_id": "py-closures-scopes",                  "example": "def make_multiplier(n): return lambda x: x * n  — multiplier captures n from the enclosing scope",                      "use_when": "showing a closure that captures a variable from its enclosing function",            "source": "agent-seeded" },
    { "topic_id": "py-exceptions-handling",              "example": "try: result = divide(a, b)\nexcept ZeroDivisionError as e: logger.error('division by zero', exc_info=True)\nelse: return result\nfinally: cleanup()", "use_when": "showing all four clauses with specific exception handling",              "source": "agent-seeded" },
    { "topic_id": "py-gil",                              "example": "concurrent.futures.ThreadPoolExecutor for I/O-bound tasks (GIL released on I/O); ProcessPoolExecutor for CPU-bound", "use_when": "the canonical pair showing where threads win and where processes win",           "source": "agent-seeded" },
    { "topic_id": "py-asyncio-event-loop",               "example": "asyncio.run(main()) — where main() is an async def that awaits multiple coroutines",                                    "use_when": "the canonical entry point for an asyncio program",                                  "source": "agent-seeded" },
    { "topic_id": "py-async-await-coroutines",           "example": "async def fetch(url): async with aiohttp.ClientSession() as s: async with s.get(url) as r: return await r.json()",     "use_when": "the canonical async HTTP call with proper resource cleanup",                        "source": "agent-seeded" },
    { "topic_id": "py-cpython-memory",                   "example": "import sys; a = []; sys.getrefcount(a) shows 2 — one for a, one for getrefcount's own argument",                        "use_when": "showing reference counting in action",                                              "source": "agent-seeded" },
    { "topic_id": "py-django-mtv",                       "example": "class OrderView(View): def get(self, request, pk): order = Order.objects.get(pk=pk); return render(request, 'order.html', {'order': order})", "use_when": "the canonical Django class-based view mapping URL → model → template",   "source": "agent-seeded" },
    { "topic_id": "py-django-orm-queryset",              "example": "Order.objects.filter(status='pending').select_related('customer').prefetch_related('items').order_by('-created_at')[:20]", "use_when": "a realistic queryset chain showing lazy evaluation, eager loading, and slicing", "source": "agent-seeded" },
    { "topic_id": "py-fastapi-overview",                 "example": "@app.get('/users/{user_id}', response_model=UserOut) async def get_user(user_id: int, db: Session = Depends(get_db)): ...", "use_when": "the canonical FastAPI path operation with Pydantic response model and DI",  "source": "agent-seeded" },
    { "topic_id": "py-wsgi-vs-asgi",                     "example": "gunicorn app:app (WSGI) vs uvicorn app:app --workers 4 (ASGI) — same code, different server interface",                 "use_when": "showing the deployment command difference between WSGI and ASGI",                   "source": "agent-seeded" },
    { "topic_id": "py-drf-serializers",                  "example": "class OrderSerializer(ModelSerializer): class Meta: model = Order; fields = ['id', 'status', 'total']",                 "use_when": "the canonical ModelSerializer on a familiar domain model",                          "source": "agent-seeded" },
    { "topic_id": "py-sqlalchemy-session",               "example": "with Session(engine) as session: user = session.get(User, 1); user.email = 'new@example.com'; session.commit()",        "use_when": "the canonical unit-of-work: get, mutate, commit",                                   "source": "agent-seeded" },
    { "topic_id": "py-alembic-migrations",               "example": "alembic revision --autogenerate -m 'add users.last_login' → alembic upgrade head",                                      "use_when": "the canonical two-step: generate then apply",                                       "source": "agent-seeded" },
    { "topic_id": "py-n-plus-one-django",                "example": "for post in Post.objects.all(): print(post.author.name)  — triggers 1 + N queries; fix: Post.objects.select_related('author').all()", "use_when": "the canonical Django N+1 anti-pattern and its fix",                        "source": "agent-seeded" },
    { "topic_id": "py-celery-basics",                    "example": "@app.task(bind=True, max_retries=3) def send_email(self, user_id): try: ... except Exception as exc: raise self.retry(exc=exc, countdown=60)", "use_when": "a realistic Celery task with retry logic",                                "source": "agent-seeded" },
    { "topic_id": "py-celery-vs-kafka",                  "example": "Celery + Redis for send_welcome_email.delay(user_id); Kafka for OrderPlaced events consumed by Inventory, Email, and Analytics", "use_when": "the canonical side-by-side of app-level task vs system-level event",       "source": "agent-seeded" },
    { "topic_id": "py-pytest-basics",                    "example": "def test_total_includes_tax(order): assert order.total() == pytest.approx(110.0)",                                       "use_when": "the minimal pytest test using approx for float comparison",                         "source": "agent-seeded" },
    { "topic_id": "py-pytest-fixtures",                  "example": "@pytest.fixture(scope='session') def db_engine(): engine = create_engine(TEST_DB_URL); yield engine; engine.dispose()", "use_when": "a session-scoped fixture that sets up and tears down a test database",              "source": "agent-seeded" },
    { "topic_id": "py-mocking-patching",                 "example": "with patch('myapp.services.email.send') as mock_send: place_order(user); mock_send.assert_called_once_with(user.email)", "use_when": "patching at the usage site, not the definition site",                              "source": "agent-seeded" },
    { "topic_id": "py-type-hints-mypy",                  "example": "def process(items: list[str]) -> dict[str, int]: return {item: len(item) for item in items}",                           "use_when": "the canonical typed function signature using built-in generics (Python 3.9+)",      "source": "agent-seeded" },
    { "topic_id": "py-packaging-dependencies",           "example": "pyproject.toml with [tool.poetry.dependencies] python = '^3.12'; fastapi = '^0.111.0' — then poetry install --no-root", "use_when": "the canonical Poetry project setup",                                                "source": "agent-seeded" },
    { "topic_id": "py-dockerfile-best-practices",        "example": "FROM python:3.12-slim AS builder\nCOPY requirements.txt .\nRUN pip install --no-cache-dir -r requirements.txt\nFROM python:3.12-slim\nCOPY --from=builder /usr/local/lib/python3.12/site-packages /usr/local/lib/python3.12/site-packages\nCOPY . .",   "use_when": "the canonical multi-stage Python Dockerfile",                                       "source": "agent-seeded" },
    { "topic_id": "py-boto3-basics",                     "example": "s3 = boto3.client('s3'); s3.upload_file('data.csv', 'my-bucket', 'uploads/data.csv')",                                  "use_when": "the simplest boto3 S3 upload call as an entry point",                               "source": "agent-seeded" },
    { "topic_id": "py-structured-logging",               "example": "{\"level\":\"INFO\",\"trace_id\":\"abc123\",\"event\":\"order.placed\",\"order_id\":42,\"user_id\":7}",                   "use_when": "the canonical structured-log JSON line with correlation ID",                        "source": "agent-seeded" },
    { "topic_id": "py-sentry-error-tracking",            "example": "sentry_sdk.init(dsn=SENTRY_DSN, traces_sample_rate=0.1, release=os.environ['GIT_SHA'])",                                "use_when": "the canonical Sentry init with release tracking and sampling",                      "source": "agent-seeded" }
  ]
}

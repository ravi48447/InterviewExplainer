# v0 runtime files
__v0_runtime_loader.js
__v0_devtools.tsx
__v0_jsx-dev-runtime.ts

# Dependencies
node_modules/

# Next.js build output & cache
.next/
out/

# Cloudflare Workers / OpenNext build output
.open-next/
.wrangler/
dev.vars

# Pre-rendered static API snapshots (regenerated by `prebuild` / `build:cf`)
public/api/

# Cloudflare Workers / OpenNext build output
.open-next/
.wrangler/
dev.vars

# Pre-rendered static API snapshots (regenerated by `prebuild` / `build:cf`)
public/api/

# Environment files (may contain API keys, secrets)
.env
.env.*
!.env.example

# OS
.DS_Store
Thumbs.db

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript build info
*.tsbuildinfo
next-env.d.ts
---
created: 2026-05-28T21:03:53+0530
status: active
task: Post-failover STOP — auto-snapshot has no NEXT, and working tree is unexpectedly dirty
parent_plan: .cursor/plans/expansion_plan_50_files_7a115880.plan.md
slug: post-failover-stopped-dirty-tree
---

# Handoff: Post-Failover STOP — No NEXT in Snapshot, Working Tree Diverged

## 0. How to resume in 60 seconds

- This handoff was written by the **continuation agent** launched by the watchdog at failover ts `2026-05-28T201714`. It chose **STOP** per the handoff skill's Mode C step 2 + step 6 — it did **not** edit anything.
- Read sections 2 and 3 first. There is no half-done edit to resume; the prior tab's last real session finished cleanly at `18:51:59` with `expansion-plan/01..50` complete.
- Before doing **any** new work, run `git status` and decide what to do with the ~271-file dirty tree (see section 4). It is from an unrelated long-running refactor that pre-dates the silent sessions.
- If you want to extend the expansion-plan effort, the next deliverable per `.cursor/plans/expansion_plan_50_files_7a115880.plan.md` is the `roadmap-link` todo — see section 5.
- If you want to commit/discard the dirty tree, do that first as its own atomic decision — do not let it get tangled with new expansion-plan edits.
- Watchdog has been firing every 2–5 minutes because new IDE tabs keep opening and going silent (see section 12). Consider pausing it (`bash .cursor/watchdog/stop.sh` if such a script exists, or kill the pid in `.cursor/handoffs/.watchdog.pid`) until you're actively typing.
- Do **not** trust `AUTO-LATEST.md` alone for continuation — its `task:` field literally says `<auto-generated from activity log; agent has not written a manual handoff>`.

## 1. Mission

The active parent plan is [`.cursor/plans/expansion_plan_50_files_7a115880.plan.md`](.cursor/plans/expansion_plan_50_files_7a115880.plan.md): produce `expansion-plan/00-INDEX.md` plus 50 numbered playbook markdown files at `expansion-plan/01..50`. As of the prior tab's `18:51` wrap-up, all 50 playbooks plus the index are written and verified on disk. The only remaining plan todo is `roadmap-link` (add a pointer in `ROADMAP.md` to `expansion-plan/00-INDEX.md`). This handoff is **not** about advancing that work — it is about the failover that fired afterwards.

## 2. Why this handoff is happening

- [x] Other: Headless / visible-mode continuation agent invoked by the auto-failover watchdog, but the snapshot had no actionable NEXT and the working tree was dirty in a way the snapshot did not predict. Per the handoff skill (Mode C steps 2 and 6) the correct action is to STOP and write a manual snapshot — which is this file.

Detail:

- The previous tab finished its visible task at `2026-05-28T18:51:59+0530` with `agent: Enhancement of all 50 playbooks is complete.` (see `.cursor/handoffs/.activity.jsonl`). It then went silent — not mid-edit, but post-completion.
- The watchdog (`.cursor/watchdog/`, pid in `.cursor/handoffs/.watchdog.pid`) treated that silence as "stuck" and fired failover four times in 13 minutes: `20:10:54`, `20:15:00`, `20:17:14`, `20:22:48`. The `20:17:14` invocation is the one whose prompt brought up the current continuation agent.
- `AUTO-LATEST.md` (the only snapshot present) is the auto-generated rolling file, not a curated handoff. Its frontmatter task field reads `<auto-generated from activity log; agent has not written a manual handoff>` and it has no `What is NEXT` section.
- The working tree shows **271 files changed, +6,814 / −29,003** with `451 ??`, `206 D`, `65 M`. None of this is mentioned in the snapshot — it is from an unrelated, long-running refactor.

## 3. What is DONE

Verified on disk by this continuation agent:

- ✅ `expansion-plan/` exists and contains 60 entries (50 playbooks + INDEX + auxiliaries). Sampled: `expansion-plan/49-javascript-go-ruby-language-tracks.md` and `expansion-plan/50-interview-migration-seo-sitemap-operations.md` both present, with the standard `## TL;DR` block and full body content.
- ✅ Prior tab's audit script ran at `18:51:14` confirming every `01..50` file has `## TL;DR` (matches sampling).
- ✅ Prior tab's last message at `18:51:59` declared the 50-playbook enhancement complete.
- ✅ The 4 most recent commits on `main` are speakable Phase-3a v2-of-v2 work (`1b07183`, `59eecda`, `c9d9e13`, … back to `1dd9348`) — unrelated to the expansion-plan effort, which has **not been committed** (the new files under `expansion-plan/` are untracked — see section 4).

NOT done by this continuation agent — **no code was edited**. This handoff is a stop, not a step.

## 4. What is IN PROGRESS

There is **no half-done edit** from the previous tab. But there is a large pre-existing dirty working tree that needs human triage before any further work is safe:

- File: working tree as a whole (not one file)
- Lines: n/a — 271 files
- Intent: An unrelated long-running refactor: delete legacy `backend/content/*`, `backend/migrations/*`, `backend/scripts/insert_batch*.js`, `backend/maven.zip`, top-level legacy MD docs (`CONTENT_GENERATION_STATUS.md`, `DATABASE_STRATEGY.md`, `DATA_MANAGEMENT_STRATEGY.md`, `DOMAIN_SPECIFIC_QUESTIONS_SUCCESS.md`, `INTELLIGENT_CONTENT_SYSTEM.md`, `START.md`, `UI_WORKING_STATUS.md`); untrack `backend/node_modules/**`; rewrite large chunks of `frontend/app/**` pages, `frontend/components/**`, `frontend/lib/api*.ts`, `frontend/next.config.mjs`; modify many `backend/src/main/java/.../modules/{auth,content,search}/**` Java files; add `codex/phrasings.json` (+411), `docs/speakable/pillar-register.md` (+176); and add a huge body of untracked `content/**` and `expansion-plan/**` material.
- State: **uncommitted, unstaged, mixed deletions / modifications / untracked**. The `expansion-plan/` work from the most recent session is itself untracked inside this same dirty tree.

State summary from `git status --short`:

- `451` untracked entries (`??`) — mostly `content/**`, `expansion-plan/**`, `content-md/**`, plus top-level `LAUNCH.md`, `MASTER_PLAN.md`, `ROADMAP.md`, `.env.example`, `_fill_react_qa_phase2.py`, new Java source `backend/.../StackSubcategoryDTO.java`, new Flyway migration `V005__experience_levels_3tier.sql`.
- `206` deletions (`D`) — legacy `backend/{content,migrations,scripts,node_modules,maven.zip}`, 7 top-level legacy MD docs, `start-all.bat`, `frontend/{app/domain,app/stack,app/topic,app/question,app/domain/[slug]/category/*}`, `frontend/shared/lib/{api-client,utils}.ts`.
- `65` modifications (`M`) — `.gitignore`, ~20 Java files under `backend/src/main/java/.../modules/{auth,content,search}/**`, `codex/{examples,phrasings}.json`, several `content/java-backend-intermediate/**/complete-qa.json`, many `frontend/app/**` pages, `frontend/components/{landing,site-header,site-footer,selection-wizard,mark-complete-button,protected-route,domains,domain-hub,dashboard}/*`, `frontend/{context/auth-context,hooks/use-api,lib/{api,api-client}}.ts`, `frontend/modules/auth/types/index.ts`, `frontend/next.config.mjs`, `frontend/package.json`.

## 5. What is NEXT

Pick exactly **one** of these tracks, and resolve it before mixing with the other. Do not start both at once.

### Track A — Triage the dirty working tree (do this first if you do not know what those changes are)

1. Run `git status --short | wc -l` (sanity: should still be ~722).
2. Run `git diff -- .gitignore` and decide whether the `.gitignore` change should be staged.
3. Run `git log --oneline -20` then `git log --diff-filter=D --summary -- backend/migrations/` to confirm whether the deletions are a planned removal of legacy SQL migrations (replaced by `backend/src/main/resources/db/migration/V005__experience_levels_3tier.sql`) or an accidental wipe.
4. Decide one of: (a) commit the deletions + Java/frontend modifications as one or more refactor commits, (b) `git restore --staged --worktree` the specific paths you did not mean to touch, (c) stash everything and inspect piecewise. The choice is human-only — do not have an agent guess.
5. Separately commit the `expansion-plan/**` untracked files as their own commit (or commits per wave) so the 50-playbook work is not buried inside the bigger refactor.

### Track B — Close out the expansion-plan effort

6. Add `roadmap-link` per `.cursor/plans/expansion_plan_50_files_7a115880.plan.md`: edit `ROADMAP.md` (currently untracked at repo root — confirm whether it is a new file or an unstaged rewrite) to include a pointer to `expansion-plan/00-INDEX.md`. Example: a top-of-file callout `> See [expansion-plan/00-INDEX.md](expansion-plan/00-INDEX.md) for the sequential operator manual (50 playbooks).`
7. Re-run the prior tab's audit (`cd expansion-plan && for f in $(ls [0-4][0-9]-*.md 50-*.md | sort); do tl=$(grep -c "^## TL;DR" "$f"); hp=$(grep -c "^## Hero plays" "$f"); printf "%-55s tldr=%s hero=%s\n" "$f" "$tl" "$hp"; done`) and confirm every file still has its required sections.
8. Mark the parent plan's todos `create-index`, `wave-a-foundation`, `wave-b-jbi`, `wave-c-java`, `wave-d-python`, `wave-e-hubs`, `roadmap-link` as `status: completed` in `.cursor/plans/expansion_plan_50_files_7a115880.plan.md` once both 6 and 7 pass.

### Track C — Stabilise the watchdog so it stops false-firing

9. Read `.cursor/watchdog/README.md` to confirm the start/stop scripts.
10. Either pause the watchdog while no IDE tab is actively typing, or widen the `stale-window` floor above the current `240s` so post-completion silence does not trigger a failover. The current watchdog config opens with `interval=30s, stale-window=[240s, 1200s]` (see `.cursor/handoffs/.watchdog.log`).

## 6. Blockers and open questions

- @user: **What is the giant uncommitted refactor in the working tree?** It deletes essentially the entire legacy `backend/{content,migrations,scripts}` tree and rewrites large parts of `frontend/app/**`. No agent should touch any of those files until you confirm whether to commit, stash, or revert. The expansion-plan work is itself entangled with it (also untracked).
- @user: **Should the watchdog keep auto-firing on post-completion silence?** It has fired 4 times in 13 minutes after a clean wrap-up. Today's 8 silent sessions in `.cursor/handoffs/.activity.jsonl` (stops at `19:25`, `20:05`, `20:23`, `20:28`, `20:34`, `20:46`, `21:01`) suggest a tab keeps opening and idling — likely an artifact of the failover script itself.
- Technical: the `.cursor/watchdog/.watchdog.log` shows two `mv: AUTO-LATEST.md.tmp: No such file or directory` errors from the heartbeat hook (around the time of failovers `20:10`/`20:15` and again at `21:01`). The heartbeat rewriter occasionally races with reads. Worth a look in `.cursor/hooks/heartbeat.sh`.
- Technical: the first two failover attempts (`20:10:54`, `20:15:00`) exited non-zero — visible-mode AppleScript failed, then headless `cursor-agent` exited 1 (logs are empty 0-byte files at `.cursor/handoffs/.failover-2026-05-28T2010*.log`). Investigate why cursor-agent exited 1 with no output.

## 7. Files touched this session

### Edited
- None. This continuation agent made no edits.

### Created
- `.cursor/handoffs/2026-05-28T2103-post-failover-stopped-dirty-tree.md` — this file.

### Read (load-bearing for context only)
- `.cursor/handoffs/AUTO-LATEST.md` — the snapshot the failover prompt pointed at; confirmed it has no `What is NEXT`.
- `.cursor/handoffs/.activity.jsonl` — last ~60 events; established that the prior tab finished cleanly at `18:51:59` and that 4 failovers have fired since.
- `.cursor/handoffs/.watchdog.log` — confirmed failover history `20:10`, `20:15`, `20:17`, `20:22`.
- `.cursor/handoffs/.failover-2026-05-28T201714.prompt.txt` — the prompt that launched this agent.
- `.cursor/skills/handoff/SKILL.md` and `.cursor/skills/handoff/HANDOFF_TEMPLATE.md` — Mode C rules and the template structure.
- `.cursor/plans/expansion_plan_50_files_7a115880.plan.md` — parent plan; confirmed the `roadmap-link` todo is the only remaining item.
- `expansion-plan/49-javascript-go-ruby-language-tracks.md` and `expansion-plan/50-interview-migration-seo-sitemap-operations.md` — sampled to verify the DONE claim.

## 8. Key code locations to know

- `.cursor/handoffs/AUTO-LATEST.md` — auto-rolling snapshot. Frontmatter `task:` empty by design. Treat as factual activity log, not as a curated handoff.
- `.cursor/handoffs/.activity.jsonl` (last ~30 events) — single source of truth for "what did the prior tab actually do". `2026-05-28T18:50` shows two Write edits to `expansion-plan/49-javascript-go-ruby-language-tracks.md`; `18:51:59` shows `agent: Enhancement of all 50 playbooks is complete.` then `stop`.
- `.cursor/handoffs/.watchdog.log` — failover audit trail; explains why multiple continuation agents got spawned in the same hour.
- `.cursor/handoffs/.watchdog.pid` — contains the watchdog daemon pid (`6` bytes); use to stop it cleanly if Track C is taken.
- `.cursor/watchdog/failover.sh` — the script that wrote the prompt that launched this agent; defines the visible/headless modes referenced in the SKILL.
- `.cursor/skills/handoff/SKILL.md` lines 110–151 — Mode C (AUTO-CONTINUE) rules. Step 2 = stop if tree is unexpectedly dirty. Step 6 = stop if `What is NEXT` is empty.
- `.cursor/plans/expansion_plan_50_files_7a115880.plan.md` lines 4–26 — parent plan todos. All except `roadmap-link` are functionally complete.
- `expansion-plan/00-INDEX.md` — master index for the 50 playbooks; the future `ROADMAP.md` pointer in Track B step 6 must target this file.
- `expansion-plan/49-javascript-go-ruby-language-tracks.md` and `expansion-plan/50-interview-migration-seo-sitemap-operations.md` — the last two files the prior tab edited; sampled and confirmed intact.

## 9. Decisions made (and alternatives rejected)

- **Decided:** STOP the headless continuation, write this manual handoff, exit. **Rejected:** "guess at the next expansion-plan step and edit `ROADMAP.md`," because (a) the snapshot has no NEXT (Mode C step 6 forbids new initiatives), and (b) `ROADMAP.md` itself appears in `git status` as untracked, meaning a human-driven rewrite is mid-flight — touching it blindly would conflict.
- **Decided:** Do not run `git add`/`git restore`/`git stash` on any path. **Rejected:** "clean up the working tree so further work is safe," because Mode C step 2 explicitly forbids automatic cleanup of an unexpectedly dirty tree.
- **Decided:** Do not stop or reconfigure the watchdog. **Rejected:** "kill the watchdog to prevent more false failovers," because that is a config decision the human should make, not the continuation agent. Recorded as Track C so the human can choose.
- **Decided:** Use kebab-case slug `post-failover-stopped-dirty-tree` instead of the literal failover timestamp `post-failover-2026-05-28T201714` requested by the failover prompt, because `new_handoff.sh` rejects non-kebab-case slugs. The filename retains the timestamp via the script's own prefix (`2026-05-28T2103-…`).

## 10. Conventions and project rules learned

- Handoff filenames are prefixed `YYYY-MM-DDTHHMM-` by `scripts/new_handoff.sh`; the slug argument must be lowercase kebab-case only (regex enforced in the script).
- `AUTO-LATEST.md` is regenerated by `.cursor/hooks/heartbeat.sh` on every agent action. Never edit it by hand. If you need a curated snapshot, write a new file via `new_handoff.sh`.
- The watchdog considers any silence ≥240s as "stuck" and fires failover. That window will trigger after natural task completion unless the watchdog is paused.
- Writes under `.cursor/handoffs/` need `required_permissions: ["all"]` when running in the default sandbox (the workspace-write sandbox blocked the scaffold script the first time).
- The parent plan at `.cursor/plans/expansion_plan_50_files_7a115880.plan.md` is the source of truth for "what does done look like for the expansion-plan effort." Use it, do not re-derive from the index.
- The `expansion-plan/` work has not been committed yet. Treat it as untracked content alongside an unrelated big refactor; the two should be committed separately.

## 11. Commands and scripts to know

- Verify dirty-tree summary: `git status --short | wc -l && git status --short | awk '{print $1}' | sort | uniq -c`
- Non-`node_modules` dirty paths: `git status --short | grep -v node_modules | head -80`
- See untracked top-level dirs only: `git status --short | awk '$1=="??"{print $2}' | awk -F/ '{print $1}' | sort -u`
- Sample expansion-plan files for required sections (the script the prior tab ran at `18:51`):
  ```bash
  cd expansion-plan && for f in $(ls [0-4][0-9]-*.md 50-*.md | sort); do \
    tl=$(grep -c "^## TL;DR" "$f"); hp=$(grep -c "^## Hero plays" "$f"); \
    printf "%-55s tldr=%s hero=%s\n" "$f" "$tl" "$hp"; \
  done
  ```
- Scaffold a new handoff (kebab-case slug required): `bash .cursor/skills/handoff/scripts/new_handoff.sh <slug>` — needs `required_permissions: ["all"]` from a Cursor agent in sandbox mode.
- Inspect failover history: `tail -60 .cursor/handoffs/.watchdog.log`
- Inspect a specific failover prompt: `cat .cursor/handoffs/.failover-<ts>.prompt.txt`
- Inspect a specific failover run's log (often empty if cursor-agent crashed early): `cat .cursor/handoffs/.failover-<ts>.log`
- Watchdog pid: `cat .cursor/handoffs/.watchdog.pid` (currently a 6-byte file from `2026-05-28T17:16`).
- Build/lint/test commands for the broader repo: **unknown to this agent** — the AUTO snapshot did not list them, and per Mode C this agent did not re-explore. Look in `frontend/package.json`, the Spring Boot `backend/pom.xml`, and `docs/` before running anything.

## 12. Last error / last failing thing

There is no compile/test failure to chase. The "failure" being recorded here is the failover loop itself.

```
[2026-05-28T20:10:54+0530] [watchdog] heartbeat stale 269s (window 240-1200) — invoking failover
[2026-05-28T20:10:54+0530] [failover] visible-mode AppleScript FAILED
[2026-05-28T20:13:00+0530] [failover] cursor-agent exited 1
[2026-05-28T20:13:00+0530] [watchdog] failover exited non-zero
[2026-05-28T20:15:00+0530] [watchdog] heartbeat stale 515s — invoking failover
[2026-05-28T20:15:14+0530] [failover] cursor-agent exited 1
[2026-05-28T20:15:14+0530] [watchdog] failover exited non-zero
[2026-05-28T20:17:14+0530] [watchdog] heartbeat stale 649s — invoking failover
[2026-05-28T20:17:18+0530] [failover] visible-mode SUCCESS  ← this is the launch that produced THIS handoff
[2026-05-28T20:22:48+0530] [watchdog] heartbeat stale 253s — invoking failover (again)
mv: .cursor/handoffs/AUTO-LATEST.md.tmp: No such file or directory
mv: .cursor/handoffs/AUTO-LATEST.md.tmp: No such file or directory
```

Also of note (silent header from the prior failover's headless log):

```
$ cat .cursor/handoffs/.failover-2026-05-28T201054.log
(empty — cursor-agent exited 1 with no output)
$ cat .cursor/handoffs/.failover-2026-05-28T201500.log
(empty — cursor-agent exited 1 with no output)
```

## 13. Notes for the next agent

- The expansion-plan effort genuinely is finished except for the `ROADMAP.md` pointer; do not redo it. The 50 files are on disk and pass the audit. If you are tempted to "improve" them, look at the prior tab's wrap-up message timestamp `18:51:59` and the fact that this snapshot was triggered by silence, not by a build break.
- The watchdog is a noisy oracle right now. If you are running interactively, the simplest way to stop the loop is to type *anything* into the visible chat tab — it bumps the heartbeat and silences the watchdog for ≥240s. If you are running headlessly and the snapshot has no NEXT, exit and let the human decide; do not spin in place.
- I deliberately did not run `pnpm build`, `mvn` anything, or any test command. The repo has a half-applied refactor in `frontend/app/**` and `backend/src/main/java/.../modules/**` that could easily fail to compile in a way that has nothing to do with whatever the next agent is asked to do — running it would just generate noise. Run builds only after the dirty-tree triage in Track A is resolved.
- The Cursor IDE that is showing the chat (per the IDE state when this agent ran) has `expansion-plan/49-javascript-go-ruby-language-tracks.md` focused on line 1. That is likely the previous tab's last-viewed file, not an in-progress edit; treat it as a stale tab.
- If you need to do another handoff later, remember the slug must be kebab-case and writes need `required_permissions: ["all"]` from inside a sandboxed agent.
